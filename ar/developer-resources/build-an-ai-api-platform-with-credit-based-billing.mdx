---
title: بناء منصة API للذكاء الاصطناعي مع الفوترة المعتمدة على الرصيد
description: ابنِ خدمة API للذكاء الاصطناعي تعتمد على مستويات متعددة باستخدام
  أرصدة الرموز — إصدار الأرصدة عبر خطط الاشتراك والحزم الإضافية لمرة واحدة،
  وخصمها تلقائيًا عند استخدام العملاء لـ API الخاص بك.
icon: coins
keywords:
  - Dodo Payments
  - دليل المطور
  - الفوترة المعتمدة على الرصيد
  - الفوترة بالرموز
  - AI API
  - أرصدة
  - تجاوز
---

<Tip>
  <strong>دع Sentra يكتب لك كود التكامل.</strong><br />
  استخدم مساعدنا الذكي في VS Code أو Cursor أو Windsurf لإنشاء كود SDK/API، ومعالجات webhook، ومنح الأرصدة، وأكثر — فقط من خلال وصف ما تريد.
  <a href="https://dodopayments.com/sentra" target="_blank" rel="noopener noreferrer" >
    جرّب Sentra: التكامل المدعوم بالذكاء الاصطناعي &rarr;
  </a>
</Tip>

في هذا الدليل، ستقوم ببناء **NeuralAPI** — منصة ذكاء اصطناعي تعتمد على مستويات، حيث تأتي كل خطة اشتراك بكمية شهرية من أرصدة الرموز، ويمكن للعملاء شراء حزم إضافية عندما تنخفض الأرصدة، ويقوم الخلفية تلقائيًا بخصم الأرصدة مع معالجة الطلبات بواسطة OpenAI.

<Note>
يستخدم هذا الدليل Node.js/Express + OpenAI SDK. تنطبق مفاهيم Dodo Payments (الأرصدة، المقاييس، webhooks) على أي إطار عمل أو مزود ذكاء اصطناعي — عدل بحرية.
</Note>

بحلول نهاية هذا الدليل، ستعرف كيفية:

- إنشاء تخصيص رصيد مخصص (رموز) وعَدَاد يخصم تلقائيًا منه
- ربط الأرصدة بخطط الاشتراك (مع وبدون تجاوز) ومنتج واحدًا للتجديد
- توصيل نقطة نهاية حقيقية لـ OpenAI التي ترسل الفواتير عبر Dodo Payments
- استعلام عن رصيد مباشر للعملاء عبر SDK
- التحقق من توقيعات webhook وتوجيه أحداث رصيد Dodo Payments

## ما نقوم ببنائه

إليك نموذج التسعير لـ NeuralAPI:

| المنتج | السعر | الرموز | تجاوز |
|---------|-------|--------|---------|
| خطة المبتدئين | $29/شهر | 10,000,000 رمز/دورة | محظورة عند الصفر |
| خطة المحترفين | $99/شهر | 40,000,000 رمز/دورة | $0.005 لكل 1K رمز |
| حزمة الشحن | $19 دفعة واحدة | +5,000,000 رمز | — |

<Info>
قبل أن تبدأ، تأكد من أنك تملك:
- حساب Dodo Payments (وضع الاختبار يكفي)
- مفتاح API لـ OpenAI
- Node.js 18+
- معرفة أساسية بـ TypeScript/Node.js
</Info>

## الخطوة 1: إنشاء تخصيص رصيد الرموز

أولاً، قم بإنشاء تخصيص الرصيد الذي ستشاركه خطط الاشتراك وحزمة الشحن. فكر في هذا على أنه تحديد وحدة "الرمز" التي يستخدمها النظام الأساسي الخاص بك.

<Frame caption="The Credits tab under Products shows all your credit entitlements.">
<img src="/images/CBB/Desktop - Cookbook - NeuralAPI - Credit.png" alt="صفحة قائمة الأرصدة تظهر تخصيصات الأرصدة التي تم إنشاؤها" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

<Steps>
<Step title="Navigate to Credits">
1. قم بتسجيل الدخول إلى لوحة التحكم في Dodo Payments
2. انقر فوق **المنتجات** في الشريط الجانبي الأيسر
3. حدد علامة تبويب **الأرصدة**
4. انقر فوق **إنشاء رصيد**
</Step>

<Step title="Configure the credit unit">
ادخل التفاصيل الأساسية لرصيد الرموز الخاص بك:

**اسم الرصيد**: `API Tokens`

**نوع الرصيد**: حدد **وحدة مخصصة**

**اسم الوحدة**: `token`

**الدقة**: `0` (الرموز دائمًا أرقام كاملة)

**انتهاء الرصيد**: `30 days` (تتم إعادة تعيين الأرصدة كل دورة فواتير)

<Warning>
لا يمكن تغيير الدقة بعد إنشاء الرصيد. بالنسبة لأعداد الرموز، `0` (أرقام كاملة) تكون دائمًا صحيحة تقريبًا.
</Warning>
</Step>

<Step title="Skip overage at the credit level">
اترك تجاوز **معطلاً** هنا — ستقوم بتكوينه لكل خطة عند ربط الرصيد بالمنتجات. يتيح ذلك لخطة المبتدئين حظر الاستخدام عند الصفر بينما يسمح لخطة المحترفين بالتجاوز.

<Tip>
تكوينات التجاوز التي يتم تكوينها هنا هي *افتراضات*. يمكن أن تتجاوز كل مرفق منتج هذه التكوينات — وهذا ما سنفعله في الخطوة الثالثة.
</Tip>
</Step>

<Step title="Save and copy the credit ID">
انقر فوق **إنشاء رصيد**. بمجرد الحفظ، افتح الرصيد ونسخ معرّفه — يبدو مثل `cent_xxxxxxxxxxxx`.

<Check>
تخصيص الرصيد `API Tokens` جاهز. التالي، إنشاء عداد حتى يمكن للأحداث الاستخدامية قيادة الخصومات تلقائيًا.
</Check>
</Step>
</Steps>

## الخطوة 2: إنشاء عداد لاستخدام الرموز

عداد يجمع بين الأحداث الاستخدامية الواردة ويحولها إلى خصومات رصيد. تحتاج هذا *قبل* إنشاء منتجات الخطة، حيث ستقوم بربطه أثناء إنشاء المنتج في الخطوة 3.

<Steps>
<Step title="Open the Meters section">
1. في الشريط الجانبي للوحة التحكم، انتقل إلى **المنتجات → العدادات**
2. انقر فوق **إنشاء عداد**
</Step>

<Step title="Configure the meter">
املأ: 

**اسم العداد**: `Token Usage Meter`

**اسم الحدث**: `api.tokens_used` *(يجب أن يتطابق بالضبط مع ما ترسله تطبيقك)*

**نوع التجميع**: `Sum` — نقوم بجمع عدد الرموز من كل حدث

**الخاصية الزائدة**: `tokens` — مفتاح البيانات الوصفية على كل حدث الذي سيتم جمع قيمته

**وحدة القياس**: `tokens`

<Warning>
أسماء الأحداث حساسة لحالة الأحرف. `api.tokens_used` ≠ `Api.Tokens.Used` — اختر واحدة والتزم بها.
</Warning>

احفظ العداد ونسخ معرفه — ستقوم بالإشارة إليه عند الإرفاق بالمنتجات.

<Check>
تم إنشاء العداد. الآن يمكننا توصيله بالرصيد عند تكوين المنتجات.
</Check>
</Step>
</Steps>

## الخطوة 3: إنشاء منتجات الخطة

تحتاج كلا الخطتين إلى أن تكون منتجات **فواتير قائمة على الاستخدام**، وليست اشتراكات عادية — العدادات يمكن أن تربط فقط بالمنتجات القائمة على الاستخدام، وتحتاج إلى العداد لخصم الأرصدة تلقائيًا عندما يجري العملاء اتصالات بالـ API. المنتجات القائمة على الاستخدام تدعم أيضًا رسوم قاعدة متكررة (`$29` / `$99`)؛ الاستخدام فوق ذلك يتم فوترة في الأرصدة.

<Frame caption="Usage Based Billing pricing type with meter configuration.">
<img src="/images/CBB/Desktop - Attach Credit - UBB.jpg" alt="تكوين أسعار الفواتير القائمة على الاستخدام" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

### خطة المبتدئين ($29/شهر — 10M رمز، بدون تجاوز)

<Steps>
<Step title="Create the Starter UBB product">
1. اذهب إلى **المنتجات → إنشاء منتج**
2. اختر **الفواتير القائمة على الاستخدام** كنوع الأسعار
3. املأ:

**اسم المنتج**: `NeuralAPI Starter`

**الوصف**: `10 million API tokens per month. Perfect for individual developers and small projects.`

**السعر الثابت**: `29.00` (رسوم القاعدة المتكررة — تُفوتر شهريًا حتى قبل أي استخدام)

**دورة الفوترة**: `Monthly`

**العملة**: `USD`
</Step>

<Step title="Attach the meter">
في قسم **تحديد العداد**، انقر فوق **+** وأضف `Token Usage Meter`. ثم على العداد:

1. قم بتشغيل **فوترة الاستخدام في الأرصدة**
2. **تخصيص الرصيد**: اختر `API Tokens`
3. **وحدات العداد لكل رصيد**: `1` — كل رمز في الحدث يترجم إلى 1 رصيد مخصوم
4. **الحد المجاني**: `0` — التخصيص الرصيد نفسه هو "النطاق المجاني" للعميل؛ لا نحتاج إلى نطاق مجاني إضافي

<Frame caption="Toggle 'Bill usage in Credits' on the meter and pick the credit entitlement.">
<img src="/images/CBB/Desktop - Attach Credit - UBB-5.jpg" alt="عداد مع تمكين فوترة الاستخدام في الأرصدة وتم اختيار رموز API" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

هذا هو التوصيل الذي يجعل أحداث `api.tokens_used` الداخلة تخصم فعليًا من رصيد العميل.
</Step>

<Step title="Configure credit issuance for Starter">
مازلت على المنتج، انتقل إلى قسم تكوين الرصيد الذي يظهر بمجرد إرفاق عداد فوترة الرصيد:

**الأرصدة المصدرة لكل دورة فوترة**: `10000000`

**السماح بالتجاوز**: **معطل** — يتم حظر العملاء في خطة المبتدئين عندما تنتهي الرموز

**استيراد إعدادات الرصيد الافتراضية**: تمكين — استخدم انتهاء الصلاحية لمدة 30 يومًا من تخصيص الرصيد

<Frame caption="Configure credit issuance per cycle on the UBB product.">
<img src="/images/CBB/Desktop - Attach Credit - UBB-6.jpg" alt="نموذج تكوين الرصيد مع المبلغ لكل دورة والإعدادات المتجاوزة" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

انقر فوق **حفظ** ونسخ معرف المنتج.

<Check>
خطة المبتدئين: رسوم قاعدة $29/شهر، 10M رمز/دورة، محظورة عند الصفر، تخصم تلقائيًا عبر العداد.
</Check>
</Step>
</Steps>

### خطة المحترفين ($99/شهر — 40M رمز، التجاوز ممكن)

<Steps>
<Step title="Create the Pro UBB product">
نفس الخطوات مثل المبتدئين، مع أرقام أكبر:

**اسم المنتج**: `NeuralAPI Pro`

**الوصف**: `40 million API tokens per month with overage. Built for production applications.`

**السعر الثابت**: `99.00`

**دورة الفوترة**: `Monthly`

**العملة**: `USD`
</Step>

<Step title="Attach the meter">
مطابق لخطة المبتدئين: إضافة `Token Usage Meter`، تمكين **فوترة الاستخدام في الأرصدة**، اختر `API Tokens`، **وحدات العداد لكل رصيد** `1`، **الحد المجاني** `0`.
</Step>

<Step title="Configure credit issuance with overage">
قم بتكوين إصدار الرصيد، هذه المرة تمكين التجاوز:

**الأرصدة المصدرة لكل دورة فوترة**: `40000000`

**استيراد إعدادات الرصيد الافتراضية**: **تعطيل** — نحتاج إلى تخصيص إعدادات التجاوز لكل منتج

**السماح بالتجاوز**: **تمكين**

**السعر لكل وحدة**: `0.000005` USD لكل رمز (أي، $0.005 لكل 1K رمز، أو $5 لكل 1M رمز — فوق معدل الرمز الفعلي للخطة لتجنب التجاوز)

**سلوك التجاوز**: `Bill overage at billing` — يتم تحصيل التجاوز في الفاتورة التالية، ثم يعاد ضبط الرصيد

احفظ المنتج ونسخ معرف المنتج.

<Check>
خطة المحترفين: رسوم قاعدة $99/شهر، 40M رمز/دورة، تجاوز بسعر $0.005 لكل 1K رموز، تخصم تلقائيًا عبر العداد.
</Check>
</Step>
</Steps>

## الخطوة 4: إنشاء حزمة تجديد الرموز

حزمة الشحن هي عملية شراء لمرة واحدة تمنح 5,000,000 رمز لرصيد عميل موجود.

<Frame caption="Single Payment pricing selected for a one-time credit product.">
<img src="/images/CBB/Desktop - Attach Credit - OTP.jpg" alt="قسم تسعير المنتج مع تحديد دفع منفرد" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

<Steps>
<Step title="Create a one-time product">
1. اذهب إلى **المنتجات → إنشاء منتج**
2. اختر **دفع منفرد** كنوع الأسعار
3. املأ:

**اسم المنتج**: `Token Top-Up Pack`

**الوصف**: `Instantly add 5 million tokens to your NeuralAPI balance.`

**السعر**: `19.00`

**العملة**: `USD`
</Step>

<Step title="Attach the token credit">
1. في قسم **الامتيازات**، انقر فوق **إرفاق** بجوار **الأرصدة**
2. اختر `API Tokens`
3. حدد **الأرصدة المصدرة**: `5000000`
4. **تعطيل** *استيراد إعدادات الرصيد الافتراضية* — نريد تجاوز انتهاء الصلاحية الافتراضي لمدة 30 يومًا
5. تعيين **انتهاء الرصيد**: `365 days`
6. احفظ المنتج

نسخ معرف المنتج.

<Tip>
لماذا انتهاء مدة أطول على التجديدات؟ أرصدة الاشتراك يعاد تعيينها كل 30 يومًا لأن هذه هي الدورة. التجديدات هي *مشتريات مدفوعة مقدمًا* — دفع العميل 19 دولارًا مقدمًا ويتوقع بشكل معقول أن تستمر تلك الرموز لأكثر من شهر. 365 يومًا مطابقة لكيفية عمل الأرصدة المدفوعة مقدمًا في OpenAI وAWS وAnthropic، مع تحديد مسؤولياتك بحيث لا يستطيع العملاء تكديسها إلى الأبد.
</Tip>

<Check>
تم تكوين حزمة الشحن — تمنح شراءها 5,000,000 رمز تظل صالحة لمدة 365 يومًا.
</Check>
</Step>
</Steps>

## الخطوة 5: بناء الخلفية

الآن دعنا نبني خادم Express الذي يعالج الشراء للاشتراك، والشراء للشحن، والتكامل الحقيقي مع OpenAI مع فوترة الرموز، والاستعلامات عن الرصيد، وأحداث webhook للرصيد.

<Steps>
<Step title="Set up your project">

```bash
mkdir neural-api-billing
cd neural-api-billing
npm init -y
npm install dodopayments openai express dotenv
npm install -D @types/node @types/express typescript tsx
```

إنشاء `tsconfig.json`:

```json tsconfig.json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "outDir": "./dist",
    "rootDir": "./src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  }
}
```

تحديث سكربتات `package.json`:

```json package.json
{
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  }
}
```

</Step>

<Step title="Set up environment variables">
إنشاء `.env` مع بيانات الاعتماد ومعرفاتك من الخطوات السابقة:

```bash .env
DODO_PAYMENTS_API_KEY=your_dodo_api_key_here
DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_signing_secret_here
DODO_ENVIRONMENT=test_mode
OPENAI_API_KEY=sk-...
CREDIT_ENTITLEMENT_ID=cent_xxxxxxxxxxxx
STARTER_PLAN_PRODUCT_ID=pdt_xxxxxxxxxxxx
PRO_PLAN_PRODUCT_ID=pdt_xxxxxxxxxxxx
TOPUP_PRODUCT_ID=pdt_xxxxxxxxxxxx
BASE_URL=http://localhost:3000
```

<Warning>
لا تقم أبدًا بنشر `.env` في إدارة الإصدارات. أضفها إلى `.gitignore` فورًا.
</Warning>

ستملأ `DODO_PAYMENTS_WEBHOOK_KEY` في الخطوة 7 بعد تسجيل نقطة نهاية webhook الخاصة بك.
</Step>

<Step title="Implement the server">
إنشاء `src/server.ts`:

<CodeGroup>

```typescript src/server.ts expandable
import 'dotenv/config';
import DodoPayments from 'dodopayments';
import OpenAI from 'openai';
import express, { Request, Response } from 'express';

const app = express();

// IMPORTANT: webhook route needs the raw body for signature verification.
// We register the raw parser ONLY on /webhooks/dodo, then JSON for everything else.
app.use('/webhooks/dodo', express.raw({ type: 'application/json' }));
app.use(express.json());
app.use(express.static('public'));

const dodo = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY!,
  webhookKey: process.env.DODO_PAYMENTS_WEBHOOK_KEY,
  environment: (process.env.DODO_ENVIRONMENT as 'test_mode' | 'live_mode') ?? 'test_mode',
});

const openai = new OpenAI({ apiKey: process.env.OPENAI_API_KEY! });

const CREDIT_ENTITLEMENT_ID = process.env.CREDIT_ENTITLEMENT_ID!;
const BASE_URL = process.env.BASE_URL!;
const PLAN_PRODUCTS: Record<string, string> = {
  starter: process.env.STARTER_PLAN_PRODUCT_ID!,
  pro: process.env.PRO_PLAN_PRODUCT_ID!,
};

// ────────────────────────────────────────────────────────────────────────────
// Subscription checkout
// Body: { plan: 'starter' | 'pro', email: string, name: string }
// ────────────────────────────────────────────────────────────────────────────
app.post('/checkout/subscribe', async (req: Request, res: Response) => {
  const { plan, email, name } = req.body;
  if (!PLAN_PRODUCTS[plan]) {
    return res.status(400).json({ error: `Unknown plan: ${plan}` });
  }
  try {
    const session = await dodo.checkoutSessions.create({
      product_cart: [{ product_id: PLAN_PRODUCTS[plan], quantity: 1 }],
      customer: { email, name },
      return_url: `${BASE_URL}/?subscribed=1`,
    });
    res.json({ checkout_url: session.checkout_url, session_id: session.session_id });
  } catch (err) {
    console.error('Subscription checkout error:', err);
    res.status(500).json({ error: 'Failed to create subscription checkout' });
  }
});

// ────────────────────────────────────────────────────────────────────────────
// Top-up checkout — buyer must already be a customer
// Body: { customer_id: string }
// ────────────────────────────────────────────────────────────────────────────
app.post('/checkout/topup', async (req: Request, res: Response) => {
  const { customer_id } = req.body;
  if (!customer_id) return res.status(400).json({ error: 'customer_id required' });
  try {
    const session = await dodo.checkoutSessions.create({
      product_cart: [{ product_id: process.env.TOPUP_PRODUCT_ID!, quantity: 1 }],
      customer: { customer_id },
      return_url: `${BASE_URL}/?topup=1`,
    });
    res.json({ checkout_url: session.checkout_url });
  } catch (err) {
    console.error('Top-up checkout error:', err);
    res.status(500).json({ error: 'Failed to create top-up checkout' });
  }
});

// ────────────────────────────────────────────────────────────────────────────
// Live token balance for a customer
// ────────────────────────────────────────────────────────────────────────────
app.get('/credits/:customerId', async (req: Request, res: Response) => {
  try {
    const result = await dodo.creditEntitlements.balances.retrieve(req.params.customerId, {
      credit_entitlement_id: CREDIT_ENTITLEMENT_ID,
    });
    res.json({
      balance: result.balance,
      overage: result.overage,
      last_transaction_at: result.last_transaction_at,
    });
  } catch (err) {
    console.error('Balance fetch error:', err);
    res.status(500).json({ error: 'Failed to fetch credit balance' });
  }
});

// ────────────────────────────────────────────────────────────────────────────
// AI completion — calls OpenAI, then ingests a usage event with the real
// token count. The meter aggregates these and deducts credits automatically.
// Body: { customer_id: string, prompt: string }
// ────────────────────────────────────────────────────────────────────────────
app.post('/api/generate', async (req: Request, res: Response) => {
  const { customer_id, prompt } = req.body;
  if (!customer_id || !prompt) {
    return res.status(400).json({ error: 'customer_id and prompt required' });
  }

  // Best-effort balance gate for Starter (no overage). Note: balance updates
  // are eventually consistent (~1 min lag from event ingestion), so a Starter
  // customer can technically squeeze through a few extra requests right after
  // running out. Use a stricter rate-limiter on top if you need hard cutoffs.
  try {
    const balance = await dodo.creditEntitlements.balances.retrieve(customer_id, {
      credit_entitlement_id: CREDIT_ENTITLEMENT_ID,
    });
    if (Number(balance.balance) <= 0 && Number(balance.overage) <= 0) {
      return res.status(402).json({
        error: 'Out of tokens. Top up or upgrade to continue.',
      });
    }
  } catch {
    // Fall through — if the balance lookup fails, don't block; rely on metering.
  }

  let completion;
  try {
    completion = await openai.chat.completions.create({
      model: 'gpt-5-mini',
      messages: [{ role: 'user', content: prompt }],
    });
  } catch (err) {
    console.error('OpenAI error:', err);
    return res.status(502).json({ error: 'Upstream AI provider failed' });
  }

  const tokensUsed = completion.usage?.total_tokens ?? 0;

  // Fire-and-forget — don't block the response on metering.
  ingestTokenUsage(customer_id, tokensUsed, completion.model).catch((err) =>
    console.error('Usage ingest failed:', err),
  );

  res.json({
    text: completion.choices[0]?.message?.content ?? '',
    tokens_used: tokensUsed,
    model: completion.model,
  });
});

async function ingestTokenUsage(customerId: string, tokens: number, model: string) {
  await dodo.usageEvents.ingest({
    events: [
      {
        // event_id is the idempotency key. Use a stable, unique value per request.
        event_id: `req_${Date.now()}_${Math.random().toString(36).slice(2, 10)}`,
        customer_id: customerId,
        event_name: 'api.tokens_used',
        timestamp: new Date().toISOString(),
        metadata: { tokens, model },
      },
    ],
  });
}

// ────────────────────────────────────────────────────────────────────────────
// Webhook handler — verifies signature using the SDK, then routes events.
// ────────────────────────────────────────────────────────────────────────────
app.post('/webhooks/dodo', async (req: Request, res: Response) => {
  const rawBody = (req.body as Buffer).toString('utf8');
  const headers = {
    'webhook-id': req.header('webhook-id') ?? '',
    'webhook-signature': req.header('webhook-signature') ?? '',
    'webhook-timestamp': req.header('webhook-timestamp') ?? '',
  };

  let event: { type: string; data: any };
  try {
    event = dodo.webhooks.unwrap(rawBody, { headers }) as any;
  } catch (err) {
    console.error('Webhook verification failed:', err);
    return res.status(401).json({ error: 'Invalid signature' });
  }

  switch (event.type) {
    case 'credit.added':
      console.log(`[credit.added] customer=${event.data.customer_id} amount=${event.data.amount}`);
      break;
    case 'credit.deducted':
      console.log(`[credit.deducted] customer=${event.data.customer_id} amount=${event.data.amount}`);
      break;
    case 'credit.overage_charged':
      console.log(`[credit.overage_charged] customer=${event.data.customer_id}`);
      break;
    default:
      // Ignore other event types
      break;
  }

  res.json({ received: true });
});

app.listen(3000, () => {
  console.log('NeuralAPI billing server running on http://localhost:3000');
});
```

```json package.json
{
  "name": "neural-api-billing",
  "version": "1.0.0",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "dodopayments": "latest",
    "openai": "^4.0.0",
    "express": "^4.18.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/express": "^4.17.0",
    "typescript": "^5.0.0",
    "tsx": "^4.0.0"
  }
}
```

</CodeGroup>

<Check>
الخلفيات منتهية: شراء الاشتراك، وشراء الشحن، وإكمال OpenAI مع فوترة الرموز، واستعلام الرصيد، ومعالج webhook معتمد.
</Check>

<Tip>
[`@dodopayments/ingestion-blueprints`](/features/usage-based-billing/ingestion-blueprints) يوفر متتبعين جاهزين للاستخدام يساعدونك في أتمتة استدعاء `usageEvents.ingest` — بما في ذلك [LLM Blueprint](/developer-resources/ingestion-blueprints/llm)، [API gateway](/developer-resources/ingestion-blueprints/api-gateway)، [object storage](/developer-resources/ingestion-blueprints/object-storage)، [streams](/developer-resources/ingestion-blueprints/stream)، و[time-range](/developer-resources/ingestion-blueprints/time-range) الاستخدام.
</Tip>
</Step>

<Step title="A note on how deductions actually happen">
قد لاحظت أنه لا يوجد استدعاء صريح "خصم N أرصدة". هذا عن عمد:
1. يستدعي معالجك OpenAI ويحصل على `usage.total_tokens` (مثل، 1532).
2. تدخل حدث استخدام واحد: `event_name: api.tokens_used`، `metadata: { tokens: 1532 }`.
3. يجمع `Token Usage Meter` الأحداث بحسب العميل.
4. لأن العداد متصل بالرصيد `API Tokens` مع **فوترة الاستخدام في الأرصدة**، يخصم Dodo Payments 1532 رصيداً من أقدم منحة غير منتهية للعميل (FIFO).
5. إذا كان التجاوز ممكّنًا والعميل وصل إلى ما دون الصفر، يتم تتبع العجز وتحصيله في الفاتورة التالية.


يتعامل العداد مع كل ذلك. الكود الخاص بك فقط يدخل الأحداث.
</Step>
</Steps>

## الخطوة 6: إضافة واجهة تجريبية أمامية

إنشاء `public/index.html` لاختبار جميع العمليات في متصفحك. نحن نحفظ معرف العميل إلى `localStorage` بحيث يشارك الكل الاشتراك → التوليد → الشحن نفس الهوية، مما يحاكي تطبيقًا محميًا بالدخول:

<CodeGroup>

```html public/index.html expandable
<!DOCTYPE html>
<html>
<head>
  <title>NeuralAPI Demo</title>
  <style>
    body { font-family: system-ui, sans-serif; max-width: 760px; margin: 40px auto; padding: 20px; color: #1a1a2e; }
    h1 { font-size: 24px; }
    h2 { margin-top: 36px; border-bottom: 1px solid #eee; padding-bottom: 8px; font-size: 18px; }
    .panel { padding: 16px; background: #fafafe; border: 1px solid #e6e6f0; border-radius: 8px; margin: 12px 0; }
    .form-group { margin: 12px 0; }
    label { display: block; margin-bottom: 4px; font-weight: 600; font-size: 13px; }
    input, select, textarea { width: 100%; padding: 10px; border: 1px solid #ddd; border-radius: 6px; box-sizing: border-box; font-family: inherit; font-size: 14px; }
    textarea { min-height: 80px; resize: vertical; }
    button { background: #6366f1; color: white; padding: 10px 18px; border: none; border-radius: 6px; cursor: pointer; font-size: 14px; font-weight: 600; }
    button:hover { background: #4f46e5; }
    button:disabled { background: #c7c7d4; cursor: not-allowed; }
    .balance { font-size: 32px; font-weight: 700; color: #6366f1; }
    .muted { color: #777; font-size: 13px; margin-top: 4px; }
    .result { margin-top: 12px; padding: 12px; background: #fff; border: 1px solid #e6e6f0; border-radius: 6px; font-size: 14px; white-space: pre-wrap; }
    .row { display: flex; gap: 12px; align-items: center; }
    .row > * { flex: 1; }
  </style>
</head>
<body>
  <h1>NeuralAPI Demo</h1>

  <div class="panel">
    <label>Logged-in customer ID (paste once after subscribing)</label>
    <div class="row">
      <input id="customerId" placeholder="cus_xxxxxxxxxxxx" />
      <button onclick="saveCustomerId()" style="flex:0">Save</button>
    </div>
    <div class="muted">After completing checkout, copy the customer ID from your Dodo Payments dashboard (Customers → most recent) and paste here.</div>
  </div>

  <h2>1. Subscribe to a Plan</h2>
  <div class="form-group"><label>Plan</label>
    <select id="plan">
      <option value="starter">Starter — $29/mo, 10M tokens</option>
      <option value="pro">Pro — $99/mo, 40M tokens + overage</option>
    </select>
  </div>
  <div class="form-group"><label>Email</label><input type="email" id="email" placeholder="you@example.com" /></div>
  <div class="form-group"><label>Name</label><input id="name" placeholder="Your name" /></div>
  <button onclick="subscribe(event)">Get Checkout Link</button>
  <div id="subscribeResult" class="result" style="display:none"></div>

  <h2>2. Generate AI Response (deducts tokens)</h2>
  <div class="form-group"><label>Prompt</label><textarea id="prompt" placeholder="Explain quantum computing in one sentence"></textarea></div>
  <button onclick="generate(event)">Generate</button>
  <div id="generateResult" class="result" style="display:none"></div>

  <h2>3. Live Token Balance</h2>
  <button onclick="checkBalance(event)">Refresh Balance</button>
  <div id="balanceResult" class="result" style="display:none"></div>

  <h2>4. Buy a Top-Up Pack</h2>
  <button onclick="topup(event)">Buy 5M Tokens — $19</button>
  <div id="topupResult" class="result" style="display:none"></div>

  <script>
    const $ = (id) => document.getElementById(id);
    document.addEventListener('DOMContentLoaded', () => {
      $('customerId').value = localStorage.getItem('customerId') || '';
    });

    function getCustomerId() {
      const id = $('customerId').value.trim();
      if (!id) { alert('Save a customer ID first'); throw new Error('no customer'); }
      return id;
    }

    function saveCustomerId() {
      localStorage.setItem('customerId', $('customerId').value.trim());
      alert('Saved');
    }

    async function withLoading(btn, loadingLabel, fn) {
      const original = btn.textContent;
      btn.disabled = true;
      btn.textContent = loadingLabel;
      try { await fn(); } finally {
        btn.disabled = false;
        btn.textContent = original;
      }
    }

    async function subscribe(ev) {
      await withLoading(ev.target, 'Loading…', async () => {
        const res = await fetch('/checkout/subscribe', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ plan: $('plan').value, email: $('email').value, name: $('name').value }),
        });
        const data = await res.json();
        const el = $('subscribeResult');
        el.style.display = 'block';
        el.innerHTML = res.ok
          ? `<a href="${data.checkout_url}" target="_blank">Open Checkout →</a>`
          : `Error: ${data.error}`;
      });
    }

    async function generate(ev) {
      const customer_id = getCustomerId();
      await withLoading(ev.target, 'Generating…', async () => {
        const res = await fetch('/api/generate', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ customer_id, prompt: $('prompt').value }),
        });
        const data = await res.json();
        const el = $('generateResult');
        el.style.display = 'block';
        el.innerHTML = res.ok
          ? `<strong>Response:</strong>\n${data.text}\n\n<em>Tokens used: ${data.tokens_used} (${data.model})</em>`
          : `Error: ${data.error}`;
        if (res.ok) refreshBalanceSilently();
      });
    }

    async function checkBalance(ev) {
      const customer_id = getCustomerId();
      await withLoading(ev.target, 'Refreshing…', async () => {
        const res = await fetch('/credits/' + customer_id);
        const data = await res.json();
        const el = $('balanceResult');
        el.style.display = 'block';
        el.innerHTML = res.ok
          ? `<div class="balance">${Number(data.balance).toLocaleString()} tokens</div>
             <div class="muted">Overage used: ${Number(data.overage).toLocaleString()} · Last activity: ${data.last_transaction_at ?? 'never'}</div>`
          : `Error: ${data.error}`;
      });
    }

    async function refreshBalanceSilently() {
      const customer_id = $('customerId').value.trim();
      if (!customer_id) return;
      const res = await fetch('/credits/' + customer_id);
      const data = await res.json();
      const el = $('balanceResult');
      el.style.display = 'block';
      el.innerHTML = res.ok
        ? `<div class="balance">${Number(data.balance).toLocaleString()} tokens</div>
           <div class="muted">Overage used: ${Number(data.overage).toLocaleString()} · Last activity: ${data.last_transaction_at ?? 'never'}</div>`
        : `Error: ${data.error}`;
    }

    async function topup(ev) {
      const customer_id = getCustomerId();
      await withLoading(ev.target, 'Loading…', async () => {
        const res = await fetch('/checkout/topup', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ customer_id }),
        });
        const data = await res.json();
        const el = $('topupResult');
        el.style.display = 'block';
        el.innerHTML = res.ok
          ? `<a href="${data.checkout_url}" target="_blank">Open Top-Up Checkout →</a>`
          : `Error: ${data.error}`;
      });
    }
  </script>
</body>
</html>
```

</CodeGroup>

## الخطوة 7: توصيل الـ Webhook

تمكنك الويب هوكس من تفاعل خادمك مع تغييرات الرصيد — ستستخدمهم لإرسال رسائل البريد الإلكتروني "تقترب من النفاد" قبل أن تصل إلى الصفر.

<Steps>
<Step title="Expose your local server">
تحتاج الويب هوكس إلى URL عام. للتطوير المحلي، استخدم [ngrok](https://ngrok.com) أو أي نفق:

```bash
ngrok http 3000
```

نسخ URL `https://...ngrok-free.app`.
</Step>

<Step title="Register the webhook in Dodo Payments">
1. في لوحة التحكم، اذهب إلى **المطورون → Webhooks → إضافة نقطة نهاية**
2. **URL**: `https://your-tunnel.ngrok-free.app/webhooks/dodo`
3. الاشتراك في (على الأقل):
   - `credit.added`
   - `credit.deducted`
   - `credit.overage_charged`
4. احفظ ونسخ **التوقيع السري**
5. الصقه في `.env` كـ `DODO_PAYMENTS_WEBHOOK_KEY`، ثم أعد تشغيل INLINE_CODE_PLACEHOLDER_a094353b3164909 END

<Tip>
يقوم `dodo.webhooks.unwrap()` في SDK بالتحقق من `webhook-id`، `webhook-timestamp`، و`webhook-signature` باستخدام توقيعك السري. لا تحتاج إلى تقديم تحقق HMAC بنفسك — ولا ينبغي، لأن Dodo Payments تستخدم [Standard Webhooks](https://www.standardwebhooks.com/) والتي توقّع `id.timestamp.body` بدلاً من مجرد النص.
</Tip>
</Step>
</Steps>

## الخطوة 8: اختبار العملية الكاملة

<Steps>
<Step title="Subscribe a test customer">
1. شغّل `npm run dev`
2. افتح `http://localhost:3000`
3. اختر **خطة المحترفين**، أدخل بريدًا إلكترونيًا تجريبيًا + اسم، انقر فوق **الحصول على رابط الشراء**، أكمل الشراء باستخدام [تفاصيل البطاقة التجريبية](/miscellaneous/testing-process)
4. في لوحة التحكم، اذهب إلى **العملاء → الأحدث** ونسخ معرف `cus_...`
5. الصقه في حقل "معرف العميل المسجل" على العرض التوضيحي وانقر فوق **حفظ**

<Check>
ينبغي أن يكون لدى العميل 40,000,000 رمز. انقر فوق **تحديث الرصيد** للتأكد.
</Check>
</Step>

/* LOCKED_PATTERN_9348a447eaa236e559027a2c0ccdec3c */}
اكتب مؤثرًا وانقر فوق **توليد**. يستدعي الخادم OpenAI، ويحصل على `total_tokens` الفعلي، ويدخل حدث استخدام، ويعيد الرد.

{{-- LOCKED_PATTERN_6fa96040307d68e9fa44436559d63ee8 --}}
يتم معالجة أحداث الاستخدام بواسطة عامل الخلفية تقريبًا كل دقيقة. لن ينخفض الرصيد على الفور — انتظر من 30 إلى 90 ثانية وانقر فوق **تحديث الرصيد** مرة أخرى. لا تستنتج أنه مكسور إذا لم يظهر التجديد الأول أي حركة.

{{-- LOCKED_PATTERN_640d0b31f6faa54914d25c81f5dbf413 --}}

{{-- LOCKED_PATTERN_6860d3c063f83b8bdd751538c39f5dca --}}
انقر فوق **شراء 5M رموز — $19** وأكمل الشراء. بعد نجاح الدفع، حدث الرصيد — ينبغي أن يقفز بمقدار 5,000,000 رمز. يجب أن يظهر سجل الخادم الخاص بك حدث `credit.added`.

{{-- LOCKED_PATTERN_640d0b31f6faa54914d25c81f5dbf413 --}}

{{-- LOCKED_PATTERN_a01a451f7aba1a80f0af7148df811dfc --}}

## استكشاف الأخطاء وإصلاحها

/* LOCKED_PATTERN_3a00efd457b01f610250031fcbf16962 */}
<Accordion title="Credits not deducting after usage events">
**الأسباب المحتملة:**
- لا يتطابق اسم حدث العداد مع `event_name` الذي ترسله (`api.tokens_used` حساسة لحالة الأحرف)
- العداد غير مرتبط بالرصيد `API Tokens` على المنتج — اذهب إلى تكوين العداد الخاص بالمنتج وتأكد من أن **فوترة الاستخدام في الأرصدة** مفعلة
- لا يتطابق مفتاح `metadata.tokens` مع حقل "الخاصية الزائدة" للعداد
- انتهت صلاحية المَنحَة للعميل (تحقق من سجل الرصيد الخاص بالعميل)

**ما الذي يجب التحقق منه:**
1. **المنتجات → العدادات**: افتح العداد وتأكد من أنه يظهر اسم الرصيد المرتبط على مرفق المنتج
2. علامة التبويب **الأحداث** على العداد — يجب أن تظهر الأحداث المدخلة هناك حتى قبل الخصم
3. **العملاء → \[العميل\] → الأرصدة**: يجب أن تظهر إدخالات دفتر الأستاذ في غضون دقيقة أو دقيقتين
</Accordion>

/* LOCKED_PATTERN_2e6ee51d68eb1cce3e4eeeeb4d472380 */}
**الأسباب المحتملة:**
- لم يكمل العميل عملية الشراء بعد — تُصدر الأرصدة فقط بعد دفع ناجح
- تستفسر باستخدام `customer_id` خاطئ (استخدم معرف `cus_...` من لوحة التحكم، وليس معرف قاعدة البيانات الخاصة بك)
- لا يتطابق `CREDIT_ENTITLEMENT_ID` في `.env` مع الرصيد المرتبط بالمنتج

**ما الذي يجب التحقق منه:** افتح **العملاء → \[العميل\] → الأرصدة**. إذا لم تظهر أي أرصدة هناك، لم يتم إرفاق تخصيص المنتج أو لم يكتمل الدفع.
</Accordion>

/* LOCKED_PATTERN_27caecf30429b0544f845c602ece6c9a */}
**الأسباب المحتملة:**
- لم يتم تمكين التجاوز على **ملحق رصيد منتج المحترفين** (يعتبر الإعداد على مستوى الرصيد مجرد افتراضي)
- العميل فعليًا في خطة المبتدئين وليس المحترفين
- تم تعيين حد التجاوز إلى 0

**ما الذي يجب التحقق منه:** تحرير خطة المحترفين → الامتيازات → الأرصدة → تأكيد أن **السماح بالتجاوز** مفعّل و**السعر لكل وحدة** هو `0.000005` (= 5 دولارات لكل مليون رمز؛ تحقق من الأصفار المتصدرة — الحقل يستقبل سعر كل رمز، وليس لكل 1K).
</Accordion>

/* LOCKED_PATTERN_a16d21c3d9a822c822483d66e25c8013 */}
**الأسباب المحتملة:**
- ترتيب تحليل جسد الطلب: `express.json()` تم تطبيقه على `/webhooks/dodo` قبل `express.raw()` — يحتاج SDK إلى **byte الخام** للطلب، وليس JSON مفصول
- الخطأ في التوقيع السري في `DODO_PAYMENTS_WEBHOOK_KEY`
- يغير البروكسي العكسي الرؤوس

**ما الذي يجب التحقق منه:** تأكد من أن سطر `app.use('/webhooks/dodo', express.raw(...))` قبل `app.use(express.json())` في `server.ts`.
</Accordion>
</AccordionGroup>

## بحاجة إلى مساعدة؟

- [مجتمع Discord](https://discord.gg/bYqAp4ayYh)
- [support@dodopayments.com](mailto:support@dodopayments.com)

## تهانينا! لقد قمت ببناء النظام القائم على الرصيد لمنصة NeuralAPI

النظام الخاص بك يحتوي الآن على نظام فوترة رصيد كامل وجاهز للإنتاج:

/* LOCKED_PATTERN_bd3b9ce11ef978f59c6eb5461169b62d */}
<Card title="Token Credit Entitlement" icon="coins">
رصيد `API Tokens` القابل لإعادة الاستخدام مع انتهاء صلاحية لمدة 30 يومًا، مُشارك عبر جميع الخطط وحزمة الشحن
</Card>

/* LOCKED_PATTERN_172cb483d138171e4b2d8f3115718d76 */}
خطة المبتدئين (10M، حد صعب) وخطة المحترفين (40M + تجاوز) مُكوّنة لكل منتج بدون تكرار الرصيد
</Card>

/* LOCKED_PATTERN_c2045688fffde38f509e080d7fd1b05b */}
يمكن للعملاء إضافة 5M رموز مقابل 19 دولارًا دون تغيير اشتراكهم
</Card>

/* LOCKED_PATTERN_2b86ac1ca2cd9c10b1994c5a6f0b74df */}
يتم إدخال أعداد الرموز الحقيقية من OpenAI كأحداث؛ العداد يخصم الأرصدات بنظام FIFO بدون تتبع يدوي
</Card>

/* LOCKED_PATTERN_2f7adf758d4ed4f71eb07cbccdf6de2c */}
رصيد حي عبر SDK للحد من الوصول، عرض الاستخدام، أو تحذير العملاء في التطبيق
</Card>

/* LOCKED_PATTERN_64f557d064ca0180fca545f631afea7b */}
أحداث دفتر الأستاذ للرصيد (`credit.added`، `credit.deducted`، `credit.overage_charged`) يتم توجيهها عبر معالج مُصدق عليه باستخدام مساعد Webhooks القياسي في SDK
</Card>

{{-- LOCKED_PATTERN_639ec37665c9a30d7ddbd3a284a688a5 --}}
