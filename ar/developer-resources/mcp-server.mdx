---
title: خادم MCP
description: تمكين المساعدين الذكيين من التفاعل مع واجهة برمجة تطبيقات مدفوعات
  دودي من خلال بروتوكول سياق النموذج (MCP) لعمليات الدفع السلسة، وإدارة
  الاشتراكات، وتفاعلات العملاء.
icon: /images/logos/mcp.svg
---

## نظرة عامة

بروتوكول سياق النموذج (MCP) هو معيار مفتوح يمكّن تطبيقات الذكاء الاصطناعي من الاتصال بشكل آمن بمصادر البيانات الخارجية وواجهات برمجة التطبيقات. يوفر خادم مدفوعات دودي للمساعدين الذكيين مثل كلود، وكورسر، وغيرهم من العملاء المتوافقين مع MCP وصولاً منظمًا إلى بنية الدفع الخاصة بك.

خادم Dodo Payments MCP مبني باستخدام [Stainless](https://www.stainless.com/) ويستخدم بنية **وضع الكود**، التي تمكن وكلاء الذكاء الاصطناعي من كتابة وتنفيذ كود TypeScript ضد Dodo Payments SDK في بيئة معزولة.

### القدرات الرئيسية

- **عمليات الدفع**: إنشاء واسترجاع وإدارة المدفوعات والمبالغ المستردة
- **إدارة الاشتراكات**: التعامل مع الفوترة المتكررة، والترقيات، والإلغاءات
- **إدارة العملاء**: إدارة بيانات العملاء والوصول إلى البوابة
- **كتالوج المنتجات**: إنشاء وتحديث المنتجات والأسعار والخصومات
- **إدارة التراخيص**: تفعيل والتحقق وإدارة تراخيص البرمجيات
- **الفوترة بناءً على الاستخدام**: تتبع والفوترة للاستخدام المقاس

## كيف يعمل وضع الكود

يستخدم خادم Dodo Payments MCP وضع الكود، مما يوفر لوكيل الذكاء الاصطناعي لديك أداتين قويتين:

1. **أداة بحث الوثائق**: استعلام عن الوثائق المتعلقة بـ Dodo Payments API وSDK لفهم العمليات والمعلمات المتاحة
2. **أداة تنفيذ الكود**: كتابة كود TypeScript ضد SDK الذي يتم تنفيذه في بيئة آمنة معزولة بدون الوصول إلى الويب أو نظام الملفات

تمكن هذه البنية وكلاء الذكاء الاصطناعي من تنفيذ عمليات دفع معقدة ومتعددة الخطوات بشكل حتمي وقابل للتكرار.

<Tip>
باستخدام وضع الكود، يمكن للوكلاء ربط عدة استدعاءات API، ومعالجة المنطق الشرطي، وإجراء الحسابات — كل ذلك في تفعيل أداة واحدة.
</Tip>

## دليلك إلى دودي MCP

بالإضافة إلى خادم دودي للدفع MCP (لتنفيذ عمليات API)، نقدم **دليل دودي MCP** - خادم بحث دلالي يمنح مساعدي الذكاء الاصطناعي الوصول الفوري إلى وثائق دودي للدفع وقاعدة المعرفة.

<Info>
**تم إنشاؤه باستخدام [ContextMCP.ai](https://contextmcp.ai)**: يستمد Dodo Knowledge MCP قوته من ContextMCP، مما يتيح بحثًا دلاليًا سريعًا عبر وثائقنا باستخدام تضمينات متجهية.
</Info>

### ما هو دليل دودي MCP؟

دليل دودي MCP هو خادم MCP عن بُعد يوفر:

- **بحث دلالي في الوثائق**: ابحث عن الوثائق ذات الصلة باستخدام استفسارات باللغة الطبيعية
- **إجابات سياقية**: يحصل مساعدو الذكاء الاصطناعي على معلومات دقيقة ومحدثة حول دودي للدفع
- **عدم الحاجة للإعداد**: لا حاجة لمفاتيح API أو تثبيت محلي - فقط اتصل وابدأ في الاستفسار

### إعداد سريع

اتصل بدليل دودي MCP في عميل الذكاء الاصطناعي الخاص بك:

<Tabs>
<Tab title="Cursor">
أضف إلى `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Desktop">
أضف إلى تكوين Claude Desktop الخاص بك:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

مواقع ملفات التكوين:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Tab>

<Tab title="Windsurf">
أضف إلى `~/.codeium/windsurf/mcp_config.json`:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Code">
قم بتشغيله في الطرفية الخاصة بك:

```bash
claude mcp add dodo-knowledge -- npx -y mcp-remote@latest https://knowledge.dodopayments.com/mcp
```

</Tab>
</Tabs>

<Note>
يتطلب Node.js 18 أو أعلى. تتولى حزمة `mcp-remote` التعامل مع الاتصال بخادم MCP البعيد.
</Note>

### استخدام كلا خادمي MCP معًا

لأفضل تجربة تطوير مدعومة بالذكاء الاصطناعي، نوصي باستخدام كلا خادمي MCP:

| الخادم | الغرض | حالة الاستخدام |
|--------|---------|----------|
| **دليل دودي MCP** | بحث الوثائق | "كيف أتعامل مع webhooks؟"، "ما طرق الدفع المدعومة؟" |
| **دودي للدفع MCP** | عمليات API | إنشاء المدفوعات، إدارة الاشتراكات، التعامل مع المبالغ المستردة |

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    },
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

<Tip>
مع تكوين الخادمين، يمكن لمساعدك الذكي أولًا البحث في الوثائق لفهم كيفية تنفيذ ميزة، ثم تنفيذ استدعاءات API الفعلية — كل ذلك في محادثة واحدة.
</Tip>

### استكشاف أخطاء دليل MCP

إذا واجهت مشاكل في الاتصال:

1. **مسح ذاكرة مصادقة MCP**: `rm -rf ~/.mcp-auth`
2. **إعادة تشغيل تطبيق العميل الخاص بك**
3. **التحقق من سجلات العميل** بحثًا عن رسائل خطأ
4. **التحقق من نسخة Node.js**: يتطلب Node.js 18+

{/* LOCKED_PATTERN_0d288b5f7e2819b6a54bf391a66e053a */}
  قم بالوصول إلى صفحة تكوين Dodo Knowledge MCP
</Card>

## التثبيت

اختر طريقة التثبيت التي تناسب سير العمل الخاص بك.

### خادم MCP عن بُعد (موصى به)

الوصول إلى خادم MCP المستضاف دون أي إعداد أو تثبيت محلي. هذه هي أسرع طريقة للبدء.

<Frame>
  <iframe 
      className="w-full aspect-video rounded-md" 
      src="https://www.youtube.com/embed/TxGgYxNmkT4"
      title="Using Dodo Payment's Remote MCP Server"
      frameBorder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowFullScreen
  ></iframe>
</Frame>

<Steps>
{/* LOCKED_PATTERN_bd1568a9aaf0e86b69853e215d3d5723 */}
    انتقل إلى [https://mcp.dodopayments.com](https://mcp.dodopayments.com) في متصفحك.
</Step>

{/* LOCKED_PATTERN_b8d4e4a1a3c0fcf28fd2afe7ce9e8316 */}
    انسخ تكوين JSON المقدم لعميلك المحدد. بالنسبة إلى Cursor أو Claude Desktop، أضف هذا إلى إعدادات MCP الخاصة بك:
    

    ```json
    {
      "mcpServers": {
        "dodopayments_api": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8be57788a275cad53ee59f91af51bfd5 */}
    ستطلب منك عملية OAuth:
    - إدخال مفتاح API الخاص بـ Dodo Payments
    - تحديد بيئتك (اختبار أو مباشر)
    - اختيار نوع عميل MCP الخاص بك
    
    <Warning>
    احفظ مفتاح API الخاص بك آمنًا. استخدم مفاتيح وضع الاختبار أثناء التطوير.
    </Warning>
</Step>

{/* LOCKED_PATTERN_606fac2bd646ef76f5d66097882887ed */}
    انقر **Login and Approve** لتفويض الاتصال.
    
    <Check>
    بمجرد الاتصال، يمكن لمساعدك الذكي التفاعل مع واجهة برمجة تطبيقات Dodo Payments نيابةً عنك.
    </Check>
</Step>
</Steps>

### حزمة NPM

قم بتثبيت وتشغيل خادم MCP محليًا باستخدام NPM.

<Tabs>
{/* LOCKED_PATTERN_c8776436a7ae17e188a287e7c6852f16 */}

```bash
# Set your environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."
export DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key"
export DODO_PAYMENTS_ENVIRONMENT="live_mode"

# Run the latest version
npx -y dodopayments-mcp@latest
```

</Tab>

{/* LOCKED_PATTERN_8d04e6cca1a5903f80148c5b923e5c3f */}

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

</Tab>
</Tabs>

<Tip>
استخدم `@latest` لالتقاط أحدث إصدار دائمًا، أو ثبّت إصدارًا محددًا مثل `@1.0.0` للثبات.
</Tip>

### دوكر

قم بتشغيل خادم MCP في بيئة محوسبة لضمان نشر متسق.

<CodeGroup>

```bash Docker
# Pull the latest image
docker pull ghcr.io/dodopayments/mcp:latest

# Run the container
docker run -e DODO_PAYMENTS_API_KEY="dodo_test_..." \
  -e DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -e DODO_PAYMENTS_ENVIRONMENT="live_mode" \
  ghcr.io/dodopayments/mcp:latest
```

```yaml Docker Compose
services:
  dodopayments-mcp:
    image: ghcr.io/dodopayments/mcp:latest
    environment:
      - DODO_PAYMENTS_API_KEY=dodo_test_...
      - DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_key
      - DODO_PAYMENTS_ENVIRONMENT=live_mode
    restart: unless-stopped
```

</CodeGroup>

<Note>
تتوفر صور Docker على [GitHub Container Registry](https://github.com/orgs/dodopayments/packages/container/package/mcp).
</Note>

## تكوين العميل

قم بتكوين خادم دودي للدفع MCP في عميل الذكاء الاصطناعي المفضل لديك.

<Tabs>
<Tab title="Cursor">
قم بإعداد خادم MCP الخاص بـ Dodo Payments في Cursor لتمكين الوصول التفاعلي إلى بيانات الدفع الخاصة بك.

**تثبيت بنقرة واحدة**

استخدم الزر أدناه لتثبيت خادم MCP مباشرة في Cursor:

[

![أضف إلى Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)

](https://cursor.com/en-US/install-mcp?name=dodopayments-mcp&config=)

بعد النقر، قم بتعيين متغيرات البيئة في `mcp.json` الخاص بـ Cursor عبر **إعدادات Cursor** > **الأدوات وMCP** > **خادم MCP جديد**.

**التكوين اليدوي**

<Steps>
{/* LOCKED_PATTERN_b5120fe2afbf9bb96d1de6f9549e8f41 */}
    انتقل إلى **إعدادات Cursor** > **الميزات** > **بروتوكول سياق النموذج** أو اضغط `Cmd/Ctrl + Shift + P` وابحث عن "إعدادات MCP".
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    انقر **تحرير التكوين** لفتح ملف `mcp_config.json`.
</Step>

{/* LOCKED_PATTERN_a65e4e236194f7c177caa156c6d0b797 */}
    اختر أحد التكوينات التالية:
    
    **الخادم البعيد (موصى به)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **NPX المحلي**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8368e3e8540b47a92000151cb7e30a33 */}
    احفظ ملف التكوين وأعد تشغيل Cursor.
    
    <Check>
    تحقق من الاتصال عن طريق سؤال المساعد الذكي عن بيانات Dodo Payments الخاصة بك.
    </Check>
</Step>
</Steps>
</Tab>

<Tab title="Claude Desktop">
قم بإعداد خادم MCP الخاص بـ Dodo Payments في Claude Desktop لتمكين الوصول التفاعلي إلى بيانات الدفع الخاصة بك.
<Steps>
{/* LOCKED_PATTERN_ecfdd2e916ed4a93d6b3950e9027df3f */}

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    افتح الملف في محرر نصوص وأضف تكوين Dodo Payments:
    
    **الخادم البعيد (موصى به)**
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **NPX المحلي**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_83563b32c3caf8bd7aa68192b6ebcc7d */}
    أغلق وأعد فتح Claude Desktop لتطبيق التغييرات.
    
    <Tip>
    ابحث عن أيقونة 🔌 في Claude Desktop لتأكيد اتصال خادم MCP.
    </Tip>
</Step>
</Steps>
</Tab>

<Tab title="Claude Code">
قم بإعداد خادم MCP الخاص بـ Dodo Payments في Claude Code CLI.

قم بتشغيل الأمر التالي في الطرفية الخاصة بك:

```bash
claude mcp add --transport stdio dodopayments_api \
  --env DODO_PAYMENTS_API_KEY="dodo_test_..." \
  DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -- npx -y dodopayments-mcp
```

سيتم تخزين التكوين في ملف `.claude.json` في دليل المنزل الخاص بك.

<Tip>
Claude Code يلتقط متغيرات البيئة تلقائيًا من الصدفة الخاصة بك، لذا يمكنك أيضًا تصديرها قبل تشغيل الأمر.
</Tip>
</Tab>

</Tab>

**تثبيت بنقرة واحدة**

[افتح VS Code](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dodopayments-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22dodopayments-mcp%22%5D%2C%22env%22%3A%7B%22DODO_PAYMENTS_API_KEY%22%3A%22قم%20بتعيين%20DODO_PAYMENTS_API_KEY%20هنا.%22%2C%22DODO_PAYMENTS_WEBHOOK_KEY%22%3A%22قم%20بتعيين%20DODO_PAYMENTS_WEBHOOK_KEY%20هنا.%22%7D%7D)

بعد النقر، قم بتعيين متغيرات البيئة في `mcp.json` في VS Code عبر **لوحة الأوامر** > **MCP: فتح تكوين المستخدم**.

**التكوين اليدوي**

<Steps>
{/* LOCKED_PATTERN_01c11d820b190ade4d2a29feff5a49da */}
    افتح لوحة الأوامر (`Cmd/Ctrl + Shift + P`) وابحث عن "MCP: فتح تكوين المستخدم".
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    أضف خادم MCP الخاص بـ Dodo Payments:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_fabb79a0cecb26359867d78b61382dd7 */}
    أعد تحميل VS Code (`Cmd/Ctrl + Shift + P` > "إعادة تحميل النافذة") لتنشيط خادم MCP.
</Step>
</Steps>
</Tab>

<Tab title="Cline (VS Code)">
قم بإعداد خادم MCP الخاص بـ Dodo Payments في Cline (VS Code) لتمكين الوصول التفاعلي إلى بيانات الدفع الخاصة بك.
<Steps>
{/* LOCKED_PATTERN_43db9eecf5eea8e9c4ad60b6c3ff1c0b */}

{/* LOCKED_PATTERN_0849b9a67e72024819745a9889898853 */}
    حدد موقع ملف تكوين Cline:
    
    - **macOS**: `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
    - **Windows**: `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    - **Linux**: `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>
    أضف تكوين خادم MCP الخاص بـ Dodo Payments:
    

{/* LOCKED_PATTERN_43be57c73e24741f16da5f79d5398749 */}
    أعد تحميل نافذة VS Code (`Cmd/Ctrl + Shift + P` > "إعادة تحميل النافذة") لتنشيط اتصال خادم MCP.
</Step>
</Steps>
</Tab>

<Tab title="Zed">
قم بإعداد خادم MCP الخاص بـ Dodo Payments في Zed لتمكين الوصول التفاعلي إلى بيانات الدفع الخاصة بك.
<Steps>
{/* LOCKED_PATTERN_a48799001554eb7ef4b2ac1e56fab915 */}

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    انتقل إلى **الإعدادات** > **المساعد** > **بروتوكول سياق النموذج**.
</Step>

</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    أضف التكوين التالي:
    

{/* LOCKED_PATTERN_b1c8821108a3bf0ac4028a0196176d66 */}
    احفظ التكوين. يجب أن يكون خادم MCP متاحًا الآن في مساعد Zed الذكي.
</Step>
</Steps>
</Tab>

{/* LOCKED_PATTERN_71257692833d303ebb288457e9ce7134 */}
قم بإعداد خادم MCP الخاص بـ Dodo Payments في العملاء الآخرين المتوافقين مع MCP.

**الخادم عن بُعد (موصى به)**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

**NPX المحلي**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
      }
    }
  }
}
```

<Info>
زر [modelcontextprotocol.io/clients](https://modelcontextprotocol.io/clients) للحصول على قائمة كاملة بالعملاء المتوافقين مع MCP وطرق تكوينهم.
</Info>
</Tab>
</Tabs>

## متغيرات البيئة

قم بتكوين سلوك خادم MCP باستخدام متغيرات البيئة.

| المتغير | الوصف | مطلوب |
|----------|-------------|----------|
| `DODO_PAYMENTS_API_KEY` | مفتاح API الخاص بك في Dodo Payments | نعم |
| `DODO_PAYMENTS_WEBHOOK_KEY` | مفتاح توقيع الويب هوك الخاص بك | لا |
| `DODO_PAYMENTS_ENVIRONMENT` | عيّنه إلى `live_mode` للإنتاج | لا |

## التشغيل عن بُعد

قم بنشر خادم MCP كخادم HTTP عن بُعد للعملاء المستندين إلى الويب أو سير العمل الوكيلة.

```bash
# Start as remote server
npx -y dodopayments-mcp --transport=http

# Specify a custom port
npx -y dodopayments-mcp --transport=http --port=3000
```

### تكوين الخادم عن بُعد

بمجرد نشره، يمكن للعملاء الاتصال باستخدام عنوان URL للخادم:

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "url": "http://localhost:3000",
      "headers": {
        "Authorization": "Bearer your_api_key"
      }
    }
  }
}
```

### رؤوس التفويض

يقبل الخادم عن بُعد المصادقة عبر الرؤوس التالية:

| العنوان | الوصف |
|--------|-------------|
| `Authorization` | مصادقة رمز الحامل |
| `x-dodo-payments-api-key` | رأس مفتاح API مباشر |

## قدرات العميل

تتمتع عملاء MCP المختلفة بمستويات دعم متفاوتة لمواصفات MCP. يمكن للخادم ضبط مخططات الأدوات تلقائيًا للتغلب على هذه القيود.

```bash
# Auto-detect client (default)
npx dodopayments-mcp --client=infer

# Configure for specific clients
npx dodopayments-mcp --client=cursor
npx dodopayments-mcp --client=claude
npx dodopayments-mcp --client=claude-code
npx dodopayments-mcp --client=openai-agents
```

### إعدادات العميل المتاحة

| العميل | الوصف |
|--------|-------------|
| `infer` | كشف العميل تلقائيًا (الافتراضي) |
| `cursor` | محرر Cursor |
| `claude` | Claude AI على الويب أو سطح المكتب |
| `claude-code` | Claude Code CLI |
| `openai-agents` | منصة وكلاء OpenAI |

### تكوين القدرات اليدوي

بالنسبة للعملاء غير المدرجين أعلاه، قم بتكوين القدرات يدويًا:

```bash
# Disable $ref pointer support and set max tool name length
npx dodopayments-mcp --no-capability=refs --capability=tool-name-length=40
```

المميزات المتوفرة:
- `top-level-unions` - دعم لأنواع الاتحاد العليا
- `valid-json` - تحليل صحيح لوسيطة سلسلة JSON
- `refs` - دعم لمؤشرات $ref في المخططات
- `unions` - دعم لأنواع الاتحاد (anyOf)
- `formats` - دعم للتحققات من التنسيق
- `tool-name-length=N` - أقصى طول لاسم الأداة

<Tip>
قم بتشغيل `npx dodopayments-mcp --describe-capabilities` للحصول على معلومات مفصلة حول الإمكانات.
</Tip>

## تصفية الأدوات

تحكم في العمليات التي يتم عرضها لمساعد الذكاء الاصطناعي الخاص بك لتحسين الأداء والحفاظ على الأمان.

### تصفية حسب نوع العملية

تقييد الوصول إلى عمليات القراءة فقط أو الكتابة.

```bash
# Only expose read operations (GET requests)
npx dodopayments-mcp --operation=read

# Only expose write operations (POST, PUT, DELETE)
npx dodopayments-mcp --operation=write
```

<Tip>
استخدم `--operation=read` أثناء التطوير لمنع التعديلات العرضية على بياناتك.
</Tip>

### تصفية حسب المورد

تحديد الأدوات لموارد API معينة.

```bash
# Only expose payment-related tools
npx dodopayments-mcp --resource=payments

# Expose multiple resources
npx dodopayments-mcp --resource=payments --resource=customers

# Use wildcards for resource groups
npx dodopayments-mcp --resource=subscription*
```

### تضمين أو استبعاد أدوات معينة

تعديل الأدوات الفردية المتاحة.

```bash
# Include only specific tools
npx dodopayments-mcp --tool=create_payments --tool=list_payments

# Exclude specific tools
npx dodopayments-mcp --no-tool=delete_products

# Disable the docs search tool
npx dodopayments-mcp --no-tools=docs

# List all available tools
npx dodopayments-mcp --list
```

### دمج الفلاتر

تطبيق فلاتر متعددة في وقت واحد للتحكم الدقيق.

```bash
# Only payment and subscription read operations
npx dodopayments-mcp --operation=read --resource=payments --resource=subscriptions
```

### تصفية معلمات استعلام URL (الخادم عن بُعد)

عند استخدام الخادم عن بُعد، قم بتطبيق الفلاتر عبر معلمات استعلام URL:

```
https://mcp.dodopayments.com/?operation=read
https://mcp.dodopayments.com/?resource=payments
```

## وضع الأدوات الديناميكية

بالنسبة لواجهات برمجة التطبيقات الكبيرة، يمكن أن يؤدي تحميل جميع الأدوات دفعة واحدة إلى إرباك نافذة سياق الذكاء الاصطناعي. يوفر وضع الأدوات الديناميكية بديلاً أكثر كفاءة.

```bash
npx dodopayments-mcp --tools=dynamic
```

في الوضع الديناميكي، تحل ثلاث أدوات ميتا محل أدوات API الفردية:

1. **`list_api_endpoints`** - يكتشف الطرق المتاحة مع التصفية الاختيارية
2. **`get_api_endpoint_schema`** - يحصل على مخطط تفصيلي لطريقة معينة
3. **`invoke_api_endpoint`** - ينفذ أي طريقة بالمعاملات المناسبة

<Info>
يتم تمكين الأدوات الديناميكية تلقائيًا لواجهات برمجة التطبيقات التي تحتوي على أكثر من 50 طريقة. تجاوز ذلك باستخدام `--tools=all` لتضمين كل الأدوات.
</Info>

```bash
# Use both dynamic tools and specific resources
npx dodopayments-mcp --tools=dynamic --resource=payments
```

## الاستخدام المتقدم

### التكامل البرمجي

قم ببناء خوادم MCP مخصصة أو قم بتمديد الخادم الحالي برمجيًا.

```javascript
import { server, endpoints, init } from "dodopayments-mcp/server";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

// Initialize with all default endpoints
init({ server, endpoints });

// Start the server
const transport = new StdioServerTransport();
await server.connect(transport);
```

### تطوير أدوات مخصصة

قم بتمديد خادم MCP بأدواتك ومنطق الأعمال الخاصة بك.

```javascript
import { server, init } from "dodopayments-mcp/server";
import createPayments from "dodopayments-mcp/tools/payments/create-payments";
import { zodToJsonSchema } from "zod-to-json-schema";
import { z } from "zod";

// Define a custom tool
const customTool = {
  tool: {
    name: 'calculate_payment_total',
    description: 'Calculate payment total with tax and fees',
    inputSchema: zodToJsonSchema(
      z.object({
        amount: z.number().describe('Base amount in cents'),
        tax_rate: z.number().describe('Tax rate as decimal'),
      })
    ),
  },
  handler: async (client, args) => {
    const total = args.amount * (1 + args.tax_rate);
    return {
      content: [{
        type: 'text',
        text: JSON.stringify({ total, currency: 'usd' }),
      }],
    };
  },
};

// Initialize with custom tools
init({ 
  server, 
  endpoints: [createPayments, customTool] 
});
```

## أفضل ممارسات الأمان

احمِ بيانات اعتماد API الخاصة بك وحافظ على تكاملات آمنة.

<AccordionGroup>
{/* LOCKED_PATTERN_58b09b66cd00f40de384d770573ae848 */}
**لا ترفع بيانات الاعتماد إلى التحكم في النسخ**

قم بتخزين مفاتيح API في متغيرات البيئة أو أنظمة إدارة الأسرار الآمنة.

```bash
# Use environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."

# Use a .env file (add to .gitignore)
echo "DODO_PAYMENTS_API_KEY=dodo_test_..." > .env
```

**قم بتدوير المفاتيح بانتظام**

قم بإنشاء مفاتيح API جديدة بشكل دوري وألغِ صلاحية القديمة من خلال لوحة معلومات دودي للدفع الخاصة بك.

**استخدم مفاتيح الاختبار للتطوير**

استخدم دائمًا مفاتيح API لوضع الاختبار أثناء التطوير لتجنب التأثير على بيانات الإنتاج.
</Accordion>

{/* LOCKED_PATTERN_1d0876565ecc6c625335a3849461e889 */}
**قم بتطبيق تصفية الأدوات في الإنتاج**

قم بتقييد العمليات المعرضة فقط لما يحتاجه مساعد الذكاء الاصطناعي الخاص بك.

```bash
# Production: read-only access
npx dodopayments-mcp --operation=read

# Development: full access
npx dodopayments-mcp
```

**تنفيذ المصادقة للخوادم عن بُعد**

عند النشر عن بُعد، اشترط دائمًا المصادقة عبر رأس `Authorization` أو رأس `x-dodo-payments-api-key`.

**مراقبة استخدام API**

تتبع نشاط خادم MCP من خلال لوحة Dodo Payments الخاصة بك وقم بإعداد تنبيهات للأنماط غير المألوفة.
</Accordion>

{/* LOCKED_PATTERN_ddb4fc82978c8dda72d606214fdad81d */}
**استخدم HTTPS للخوادم البعيدة**

قم دائمًا بنشر خوادم MCP عن بُعد خلف نقاط نهاية HTTPS.

**تنفيذ تحديد المعدل**

احمِ من إساءة الاستخدام من خلال تنفيذ حدود المعدل على كل من خادم MCP وواجهات برمجة التطبيقات.

**تقييد الوصول إلى الشبكة**

قم بتكوين قواعد جدار الحماية لتحديد العملاء المسموح لهم الاتصال بخادم MCP الخاص بك.
</Accordion>
</AccordionGroup>

## استكشاف الأخطاء وإصلاحها

<AccordionGroup>
{/* LOCKED_PATTERN_3e10fc2511884be492da344a929e2311 */}
**تحقق من مفتاح API الخاص بك**

تأكد من تعيين مفتاح API الخاص بك بشكل صحيح وأن لديه الأذونات اللازمة.

```bash
# Test your API key
curl -H "Authorization: Bearer dodo_test_..." \
  https://test.dodopayments.com/payments
```

**تحقق من اتصال الشبكة الخاص بك**

تحقق من أنك تستطيع الوصول إلى نقاط نهاية واجهة برمجة التطبيقات لدودي للدفع.

**راجع سجلات العميل**

قم بتفعيل التسجيل المفصل في عميل MCP الخاص بك لتشخيص مشكلات الاتصال.
</Accordion>

{/* LOCKED_PATTERN_8f29ea613a43315c29bf194043f8ada2 */}
**تأكد من بيئة مفتاح API**

تأكد من أنك تستخدم مفاتيح الاختبار مع نقاط نهاية الاختبار ومفاتيح مباشرة مع نقاط نهاية الإنتاج.

**تحقق من متغير البيئة**

تحقق من تعيين `DODO_PAYMENTS_ENVIRONMENT` بشكل صحيح (`live_mode` للإنتاج).

**إعادة إنشاء بيانات الاعتماد**

إذا استمرت المشاكل، فأنشئ مفتاح API جديدًا عبر لوحة التحكم الخاصة بك.
</Accordion>

{/* LOCKED_PATTERN_565675849d369f82d965d1f3a122632b */}
**تحقق من صحة معلمات الإدخال**

تأكد من أن مساعد الذكاء الاصطناعي يقدم معلمات بتنسيق صحيح لكل أداة.

**راجع رسائل الخطأ**

تحقق من استجابة الخطأ من واجهة برمجة التطبيقات للحصول على إرشادات محددة حول ما حدث خطأ.

**اختبر مع واجهة برمجة التطبيقات مباشرة**

تحقق من أن العملية تعمل عند استدعاء واجهة برمجة تطبيقات Dodo Payments مباشرةً عبر curl أو Postman.
</Accordion>

{/* LOCKED_PATTERN_776734428589daf9897a729aa0a38aed */}
**جرّب إعداد عميل مختلف**

إذا لم تعمل الأدوات بشكل صحيح، جرب تحديد عميلك بشكل صريح:

```bash
npx dodopayments-mcp --client=cursor
```

**تحقق من دعم القدرات**

بعض العملاء لا يدعمون جميع ميزات مخطط JSON. استخدم `--describe-capabilities` لفهم ما هو مدعوم.
</Accordion>
</AccordionGroup>

## الموارد

<CardGroup cols={2}>
{/* LOCKED_PATTERN_4325afcb089e351f99ef591b38203ac2 */}
  استعرض التوثيق الكامل لواجهة برمجة تطبيقات Dodo Payments
</Card>

{/* LOCKED_PATTERN_1cbca36bdeb2bbbc77032220bd30c277 */}
  تعرّف أكثر على معيار بروتوكول سياق النموذج
</Card>

{/* LOCKED_PATTERN_02488b19a875a7c4cd24c2f5651e4831 */}
  عرض كود خادم MCP المصدري والمساهمة
</Card>

{/* LOCKED_PATTERN_d4dbe77bbb87d236d98e318084853b86 */}
  نصب خادم MCP من NPM
</Card>
</CardGroup>