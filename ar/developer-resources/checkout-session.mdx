---
title: جلسات الدفع
description: إنشاء تجارب دفع آمنة ومُستضافة تتعامل مع تدفق الدفع الكامل لكل من
  المشتريات لمرة واحدة والاشتراكات مع التحكم الكامل في التخصيص.
icon: cart-shopping
---

<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  قم بتشغيل جلستك الأولى لإتمام الدفع في أقل من 5 دقائق
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  استكشف التوثيق الكامل لواجهة برمجة التطبيقات وجرب طلبات واستجابات جلسة الدفع تفاعليًا.
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  احسب الأسعار والضرائب والمجاميع قبل إنشاء جلسة.
</Card>
</CardGroup>

<Info>
**صلاحية الجلسة**: تكون جلسات الدفع صالحة لمدة 24 ساعة افتراضيًا. إذا أرسلت `confirm=true` في طلبك، فستنتهي صلاحية الجلسة بعد 15 دقيقة فقط.
</Info>

## المتطلبات الأساسية

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  ستحتاج إلى حساب تاجر نشط في Dodo Payments مع وصول إلى API.
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  قم بإنشاء بيانات اعتماد API من لوحة Dodo Payments:
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  قم بإنشاء منتجاتك في لوحة Dodo Payments قبل تنفيذ جلسات الدفع.
</Step>
</Steps>

## إنشاء أول جلسة دفع لك

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### استجابة API

ترجع جميع الطرق أعلاه نفس هيكل الاستجابة:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
استخرج `checkout_url` من استجابة API.
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
وجّه عميلك إلى عنوان الدفع لإكمال عملية الشراء.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**خيارات التكامل البديلة**: بدلاً من إعادة التوجيه، يمكنك تضمين صفحة الدفع مباشرةً في صفحتك باستخدام [Overlay Checkout](/developer-resources/overlay-checkout) (نافذة منبثقة) أو [Inline Checkout](/developer-resources/inline-checkout) (مضمّن بالكامل). كلا الخيارين يستخدمان نفس عنوان URL لجلسة الدفع.
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
بعد الدفع، يُعاد توجيه العملاء إلى `return_url` مع معاملات استعلام تتضمن معرف الدفع/الاشتراك، الحالة، بريد العميل الإلكتروني، وأي مفاتيح ترخيص. اطلع على [مستندات معلمة `return_url`](#request-body) للقائمة الكاملة.
</Step>
</Steps>

## جسم الطلب

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  الحقول الأساسية المطلوبة لكل جلسة دفع
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  إعدادات إضافية لتخصيص تجربة الدفع الخاصة بك
</Card>
</CardGroup>

### الحقول المطلوبة

<ParamField body="product_cart" type="array" required>
مصفوفة المنتجات التي يتم تضمينها في جلسة الدفع. يجب أن يحتوي كل منتج على `product_id` صالح من لوحة Dodo Payments الخاصة بك.

<Tip>
**الدفع المختلط**: يمكنك مزج منتجات الدفع مرة واحدة ومنتجات الاشتراك في نفس جلسة الدفع. يتيح ذلك حالات استخدام قوية مثل رسوم التأسيس مع الاشتراكات، أو حزم الأجهزة مع SaaS، وغيرها.
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
المعرف الفريد للمنتج من لوحة Dodo Payments الخاصة بك.

**مثال:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
كمية المنتج.

**مثال:** `1` لبند واحد، `3` لكميات متعددة
</ParamField>

<ParamField body="addons" type="array">
مصفوفة الإضافات المرتبطة بالمنتج. صالحة فقط إذا كان المنتج اشتراكًا.

{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
المبلغ الذي يدفعه العميل إذا تم تمكين pay_what_you_want. إذا تم تعطيله، سيتم تجاهل هذا الحقل.

**التنسيق**: ممثل بأقل وحدة نقدية للعملة (مثل السنتات للعملة USD). على سبيل المثال، لفرض 1.00 دولار، أرسل `100`.
</ParamField>
</Expandable>
</ParamField>

<Tip>
**ابحث عن معرفات المنتج الخاصة بك**: يمكنك العثور على معرفات المنتج في لوحة Dodo Payments تحت المنتجات → عرض التفاصيل، أو باستخدام [List Products API](/api-reference/products/get-products).
</Tip>

### الحقول الاختيارية

قم بتكوين هذه الحقول لتخصيص تجربة الدفع وإضافة منطق الأعمال إلى تدفق الدفع الخاص بك.

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
معلومات العميل. يمكنك إرفاق عميل موجود باستخدام معرفه أو إنشاء سجل عميل جديد أثناء الدفع.

<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}
أرفق عميلًا موجودًا إلى جلسة الدفع باستخدام معرفه.

{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
المعرف الفريد للعميل الموجود. استخدمه لربط جلسة الدفع بعميل موجود بدلاً من إنشاء عميل جديد.
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
أنشئ سجل عميل جديد أثناء الدفع.

{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
عنوان البريد الإلكتروني للعميل. مطلوب عند إنشاء عميل جديد.
</ParamField>

<ParamField body="name" type="string">
الاسم الكامل للعميل كما يجب أن يظهر في الإيصالات والفواتير.

**مثال**: `"John Doe"` أو `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
رقم هاتف العميل بصيغة دولية. مطلوب لبعض طرق الدفع ومن أجل منع الاحتيال.

**التنسيق**: أضف رمز البلد، مثل `"+1234567890"` لأرقام الولايات المتحدة
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
معلومات عنوان الفوترة لحساب الضرائب بدقة، ومنع الاحتيال، والامتثال التنظيمي.

<Info>
عندما يتم تعيين `confirm` إلى `true`، تصبح جميع حقول عنوان الفوترة إلزامية لإنشاء الجلسة بنجاح.
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
العنوان الكامل بما في ذلك رقم المنزل واسم الشارع ورقم الشقة/الوحدة إن وجد.

**مثال**: `"123 Main St, Apt 4B"` أو `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
اسم المدينة أو البلدية.

**مثال**: `"San Francisco"`، `"New York"`، `"London"`
</ParamField>

<ParamField body="state" type="string">
اسم الولاية أو المقاطعة أو المنطقة. استخدم الأسماء الكاملة أو الاختصارات القياسية.

**مثال**: `"California"` أو `"CA"`، `"Ontario"` أو `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
رمز الدولة المكون من حرفين حسب ISO (ISO 3166-1 alpha-2). هذا الحقل مطلوب دائمًا عند تقديم billing_address.

**أمثلة**: `"US"` (الولايات المتحدة)، `"CA"` (كندا)، `"GB"` (المملكة المتحدة)، `"DE"` (ألمانيا)

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  اطلع على القائمة الكاملة للبلدان المدعومة ورموز ISO الخاصة بها
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
الرمز البريدي أو ZIP أو ما يعادله حسب متطلبات البلد.

**أمثلة**: `"94102"` (الولايات المتحدة)، `"M5V 3A8"` (كندا)، `"SW1A 1AA"` (المملكة المتحدة)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
تحكم في طرق الدفع المتاحة للعملاء أثناء الدفع. يساعد ذلك في التكيف مع أسواق أو متطلبات أعمال محددة.

**الخيارات المتاحة**: `credit`، `debit`، `upi_collect`، `apple_pay`، `google_pay`، `amazon_pay`، `klarna`، `affirm`، `afterpay_clearpay`، `cashapp`، `multibanco`، `bancontact_card`، `eps`، `ideal`، `przelewy24`، `paypal`

<Warning>
**حرج**: أضف دائمًا `credit` و`debit` كخيارات احتياطية لمنع فشل الدفع عندما لا تتوفر طرق الدفع المفضلة.
</Warning>

**مثال**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
استبدل اختيار العملة الافتراضي بعملة فوترة ثابتة. يستخدم رموز العملة حسب ISO 4217.

**العملات المدعومة**: `USD`، `EUR`، `GBP`، `CAD`، `AUD`، `INR`، والمزيد

**مثال**: `"USD"` للدولار الأمريكي، `"EUR"` لليورو

<Note>
يكون هذا الحقل فعالًا فقط عندما يكون التسعير التكيفي مفعّلًا. إذا كان معطّلًا، فسيتم استخدام عملة المنتج الافتراضية.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
عرض طرق الدفع المحفوظة مسبقًا للعملاء العائدين لتحسين سرعة الدفع وتجربة المستخدم.
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
رابط لإعادة توجيه العملاء بعد إكمال الدفع. يقوم Dodo Payments بإلحاق معاملات الاستعلام التالية إلى عنوان URL الخاص بك عند إعادة التوجيه:

| المعلمة | النوع | الشرط |
|-----------|------|-----------|
| `payment_id` | `string` | موجود دائمًا للدفعات لمرة واحدة |
| `subscription_id` | `string` | موجود دائمًا لمدفوعات الاشتراك |
| `status` | `string` | موجود دائمًا |
| `license_key` | `string` | موجود إذا كان المنتج يحتوي على مفاتيح ترخيص مفعلة. مفصولة بفواصل إذا كان هناك عدة مفاتيح |
| `email` | `string` | موجود إذا كان لدى العميل بريد إلكتروني مسجل |

**روابط إعادة التوجيه النموذجية:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
استخدم معاملات الاستعلام `license_key` و`email` لعرض مفاتيح الترخيص أو إرسال تأكيد فورًا في صفحة العودة الخاصة بك، دون الحاجة إلى استدعاء API إضافي.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
إذا كان true، يتم إنهاء جميع تفاصيل الجلسة فورًا. سيرمي API خطأ إذا كانت البيانات المطلوبة مفقودة.
</ParamField>

<ParamField body="discount_code" type="string">
طبق رمز خصم على جلسة الدفع.
</ParamField>

<ParamField body="metadata" type="object">
أزواج مفتاح-قيمة مخصصة لتخزين معلومات إضافية حول الجلسة.
</ParamField>

<ParamField body="force_3ds" type="boolean">
تجاوز سلوك 3DS الافتراضي للتاجر لهذه الجلسة.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
فعّل وضع جمع العنوان الحد الأدنى. عند التفعيل، تجمع صفحة الدفع فقط:
- **البلد**: مطلوب دائمًا لتحديد الضرائب
- **الرمز البريدي/Postal code**: فقط في المناطق التي يتطلب فيها حساب ضريبة المبيعات أو ضريبة القيمة المضافة أو ضريبة السلع والخدمات

هذا يقلل بشكل كبير من احتكاك الدفع من خلال حذف حقول النموذج غير الضرورية.

<Tip>
فعّل العنوان الحد الأدنى لإتمام الدفع بسرعة أكبر. يظل جمع العنوان الكامل متاحًا للشركات التي تتطلب تفاصيل فوترة كاملة.
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
خصص مظهر وسلوك واجهة الدفع.

{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
سمة واجهة الدفع. الخيارات: `light`، `dark`، أو `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
اعرض قسم تفاصيل الطلب في واجهة الدفع.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
اعرض الوسم "on-demand" للمنتجات المعنية.
</ParamField>

<ParamField body="force_language" type="string">
أجبر واجهة الدفع على العرض بلغة محددة. بشكل افتراضي، تستكشف الواجهة لغة متصفح العميل.

**رموز اللغات المدعومة:**
`ar` (العربية)، `ca` (الكاتالونية)، `de` (الألمانية)، `en` (الإنجليزية)، `es` (الإسبانية)، `fr` (الفرنسية)، `he` (العبرية)، `id` (الإندونيسية)، `it` (الإيطالية)، `ja` (اليابانية)، `ko` (الكورية)، `ms` (الماليزية)، `nl` (الهولندية)، `pl` (البولندية)، `pt` (البرتغالية)، `ro` (الرومانية)، `ru` (الروسية)، `sv` (السويدية)، `th` (التايلاندية)، `tr` (التركية)، `zh` (الصينية)

**مثال**: `"es"` للإسبانية، `"ja"` لليابانية
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
خصص ميزات وسلوكيات محددة لجلسة الدفع.

{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
اسمح للعملاء باختيار العملة المفضلة لديهم أثناء الدفع.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
اعرض حقل إدخال رمز الخصم في واجهة الدفع.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
اجمع أرقام هواتف العملاء أثناء الدفع.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
اسمح للعملاء بإدخال أرقام التعريف الضريبي.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
أجبر على إنشاء سجل عميل جديد بدلاً من تحديث السجلات الموجودة.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
اسمح للعملاء بتحرير عنوان بريدهم الإلكتروني أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
اسمح للعملاء بتحرير أسمائهم أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
اسمح للعملاء بتحرير عنوان الشارع أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
اسمح للعملاء بتحرير المدينة أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
اسمح للعملاء بتحرير الولاية أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
اسمح للعملاء بتحرير البلد أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
اسمح للعملاء بتحرير الرمز البريدي أثناء الدفع.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
فعّل وضع جمع العنوان الحد الأدنى. عند التفعيل، تجمع صفحة الدفع فقط:
- **البلد**: مطلوب دائمًا لتحديد الضرائب
- **الرمز البريدي/Postal code**: فقط في المناطق التي يتطلب فيها حساب ضريبة المبيعات أو ضريبة القيمة المضافة أو ضريبة السلع والخدمات

هذا يقلل بشكل كبير من احتكاك الدفع من خلال حذف حقول النموذج غير الضرورية.

<Tip>
فعّل العنوان الحد الأدنى لإتمام الدفع بسرعة أكبر. يظل جمع العنوان الكامل متاحًا للشركات التي تتطلب تفاصيل فوترة كاملة.
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
تخطى صفحة نجاح الدفع الافتراضية وأعد توجيه العملاء فورًا إلى `return_url` بعد إتمام الدفع.

<Tip>
استخدم `redirect_immediately: true` عندما تمتلك صفحة نجاح مخصصة توفر تجربة مستخدم أفضل من صفحة نجاح الدفع الافتراضية. هذا مفيد بشكل خاص لتطبيقات الجوال وتدفقات الدفع المضمنة.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
اجمع معلومات إضافية من العملاء أثناء الدفع باستخدام حقول مخصصة. يمكنك تعريف ما يصل إلى 5 حقول مخصصة لكل جلسة دفع. يتم تضمين ردود العملاء في حمولة الويب هوك ومتاحة عبر API.

{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
معرف فريد لهذا الحقل. يستخدم كمفتاح في حمولة الويب هوك واستجابات API.

**مثال**: `"company_name"`، `"referral_source"`، `"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
التسمية المعروضة للعميل في نموذج الدفع.

**مثال**: `"Company Name"`، `"How did you hear about us?"`، `"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
نوع الحقل الذي يحدد قواعد التحقق من الإدخال.

**الأنواع المتاحة**:
- `text` - حقل نصي بسطر واحد
- `number` - إدخال رقمي
- `email` - عنوان بريد إلكتروني مع التحقق
- `url` - عنوان URL مع التحقق
- `date` - اختيار تاريخ
- `dropdown` - اختر من خيارات معدة مسبقًا
- `boolean` - مفتاح اختيار نعم/لا

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
ما إذا كان يجب ملء هذا الحقل قبل إتمام الدفع.

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
نص العنصر النائب المعروض في حقل الإدخال.

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
قائمة الخيارات لحقل `dropdown`. مطلوب عندما يكون `field_type` مساويًا لـ `dropdown`، ويُتجاهل للأنواع الأخرى.

**مثال**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
تُدرج ردود العملاء على الحقول المخصصة في:
- **Webhooks**: تحتوي حمولة الأحداث ذات الصلة مثل `payment.succeeded`، `subscription.created` وغيرها على مصفوفة `custom_field_responses`
- **API responses**: تتضمن كائنات الدفع والاشتراك `custom_field_responses`
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
إعدادات إضافية لجلسات الدفع التي تحتوي على منتجات اشتراك.

{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
عدد أيام الفترة التجريبية قبل أول خصم. يجب أن يكون بين 0 و10000 يوم.
</ParamField>

<ParamField body="on_demand" type="object">
إعداد الاشتراك حسب الطلب للفوترة القائمة على الاستخدام أو المقاسة.

{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
إذا تم ضبطه على true، لا يتم فرض أي رسوم ويتم فقط تفويض تفاصيل وسيلة الدفع للاستخدام المستقبلي.
</ParamField>

<ParamField body="product_price" type="integer">
سعر المنتج للرسوم الأولية للعميل. إذا لم يتم تحديده، سيتم استخدام السعر المخزن للمنتج.

**التنسيق**: ممثل بأقل وحدة نقدية للعملة (مثل السنتات للعملة USD). على سبيل المثال، لفرض 1.00 دولار، أرسل `100`.
</ParamField>

<ParamField body="product_currency" type="string">
عملة اختيارية لسعر المنتج. إذا لم يتم تحديدها، يتم الافتراض على عملة المنتج.

<ParamField body="product_description" type="string">
التجاوز الوصفي الاختياري للمنتج في الفوترة وبنود الفاتورة. إذا لم يتم تحديده، سيتم استخدام الوصف المخزن.

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
ما إذا كان يجب تضمين رسوم العملة التكيفية في سعر المنتج (true) أو إضافتها فوقه (false). يتم تجاهلها إذا لم يتم تمكين التسعير التكيفي.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

## أمثلة الاستخدام

فيما يلي 10 أمثلة شاملة تعرض تكوينات مختلفة لجلسات الدفع لسيناريوهات أعمال متنوعة:

### 1. دفع منتج واحد بسيط

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. سلة متعددة المنتجات

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. اشتراك مع فترة تجريبية

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. دفع مؤكد مسبقًا

<Note>
عندما يتم ضبط `confirm` على `true`، سيُوجه العميل مباشرةً إلى صفحة الدفع، متجاوزًا أي خطوات تأكيد.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. الدفع مع تجاوز العملة

<Note>
يُفعّل تجاوز `billing_currency` فقط عندما تكون العملة التكيفية مفعلة في إعدادات حسابك. إذا كانت العملة التكيفية معطلة، فلن يكون لهذا المعامل أي تأثير.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. طرق الدفع المحفوظة للعملاء العائدين

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. دفع الشركات مع جمع رقم التعريف الضريبي

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. الدفع بواجهة داكنة مع رمز خصم

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. طرق الدفع الإقليمية (UPI للهند)

للحصول على معلومات تفصيلية حول إعداد UPI والاختبار، راجع صفحة <a href="/features/payment-methods/india">India Payment Methods</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. الدفع الآن وادفع لاحقًا (BNPL)

للحصول على معلومات مفصلة حول إعداد BNPL والاختبار، راجع صفحة <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. استخدام طرق الدفع الموجودة لتسريع الدفع

استخدم وسيلة الدفع المحفوظة للعميل لإنشاء جلسة دفع تُعالج فورًا، متجاوزًا جمع وسيلة الدفع:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
عند استخدام `payment_method_id`، يجب ضبط `confirm` على `true` ويجب توفير `customer_id` موجود. سيتم التحقق من صلاحية وسيلة الدفع مع عملة الدفع.
</Warning>

<Info>
يجب أن تنتمي وسيلة الدفع إلى العميل وأن تكون متوافقة مع عملة الدفع. يتيح ذلك عمليات شراء بنقرة واحدة للعملاء العائدين.
</Info>

### 12. روابط قصيرة لعناوين دفع أكثر نظافة

أنشئ روابط دفع مختصرة وقابلة للمشاركة مع مسارات مخصصة:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
الروابط القصيرة مثالية للرسائل القصيرة أو البريد الإلكتروني أو وسائل التواصل الاجتماعي. يسهل تذكرها وتُشعر العملاء بثقة أكبر من العناوين الطويلة.
</Tip>

### 13. تخطى صفحة نجاح الدفع مع إعادة توجيه فوري

أعد توجيه العملاء فورًا بعد إتمام الدفع، متجاوزًا صفحة النجاح الافتراضية:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
استخدم `redirect_immediately: true` عندما تمتلك صفحة نجاح مخصصة توفر تجربة مستخدم أفضل من صفحة النجاح الافتراضية. هذا مفيد بشكل خاص لتطبيقات الجوال وتدفقات الدفع المضمنة.
</Tip>

<Note>
عندما يتم تمكين `redirect_immediately`، يُعاد توجيه العملاء إلى `return_url` فورًا بعد إتمام الدفع، متجاوزين صفحة النجاح الافتراضية تمامًا.
</Note>

### 14. فرض لغة

أجبر صفحة الدفع على العرض بلغة محددة، متجاوزًا اكتشاف لغة المتصفح للعميل:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
استخدم `force_language` عندما تعرف لغة العميل المفضلة (مثلًا من إعدادات حسابه) أو عند استهداف أسواق إقليمية محددة.
</Tip>

**اللغات المدعومة:** العربية (`ar`)، الكاتالونية (`ca`)، الصينية (`zh`)، الهولندية (`nl`)، الإنجليزية (`en`)، الفرنسية (`fr`)، الألمانية (`de`)، العبرية (`he`)، الإندونيسية (`id`)، الإيطالية (`it`)، اليابانية (`ja`)، الكورية (`ko`)، الماليزية (`ms`)، البولندية (`pl`)، البرتغالية (`pt`)، الرومانية (`ro`)، الروسية (`ru`)، الإسبانية (`es`)، السويدية (`sv`)، التايلاندية (`th`)، التركية (`tr`)

### 15. جمع الحقول المخصصة

اجمع معلومات إضافية من العملاء أثناء الدفع باستخدام الحقول المخصصة:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
يتم تضمين ردود الحقول المخصصة تلقائيًا في حمولة الويب هوك (`payment.succeeded`، `subscription.created`، إلخ) ويمكن استردادها عبر API. استخدمها لتحسين CRM، أو تفعيل تدفقات البدء، أو تخصيص تجربة العميل.
</Info>

**أنواع الحقول المتاحة:** `text`، `number`، `email`، `url`، `date`، `dropdown`، `boolean`

## معاينة جلسات الدفع

قبل إنشاء جلسة دفع، يمكنك معاينة تفاصيل التسعير بما في ذلك الضرائب والخصومات والمجاميع. هذا مفيد لعرض أسعار دقيقة للعملاء قبل المتابعة إلى الدفع.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  اطلع على وثائق نقطة النهاية الخاصة بالمعاينة بالكامل.
</Card>

## الانتقال من الروابط الديناميكية إلى جلسات الدفع

### الاختلافات الرئيسية

سابقًا، عند إنشاء رابط دفع باستخدام الروابط الديناميكية، كان يتعين عليك تزويد عنوان الفوترة الكامل للعميل.

مع جلسات الدفع، لم يعد هذا ضروريًا. يمكنك تمرير أي معلومات لديك، ونحن سنتولى الباقي. على سبيل المثال:
- إذا كنت تعرف فقط بلد فوترة العميل، فقم بتقديم ذلك.
- ستجمع دورة الدفع تلقائيًا التفاصيل المفقودة قبل نقل العميل إلى صفحة الدفع.
- من ناحية أخرى، إذا كان لديك جميع المعلومات المطلوبة وتريد الانتقال مباشرة إلى صفحة الدفع، يمكنك تمرير مجموعة البيانات الكاملة وتضمين `confirm=true` في جسم الطلب.

### عملية الترحيل

الترحيل من الروابط الديناميكية إلى جلسات الدفع بسيط:

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
حدّث تكاملاتك لاستخدام واجهة API أو طريقة SDK الجديدة.
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
ضبط حمولة الطلب وفقًا لتنسيق جلسات الدفع.
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
نعم. لا داعي لأي معالجة إضافية أو خطوات ترحيل خاصة من جانبك.
</Step>
</Steps>
