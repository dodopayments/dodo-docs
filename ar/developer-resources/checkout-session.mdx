---
title: Checkout Sessions
description: Create secure, hosted checkout experiences that handle the complete
  payment flow for both one-time purchases and subscriptions with full
  customization control.
icon: cart-shopping
keywords:
  - Dodo Payments
  - developer guide
  - checkout sessions
---

<CardGroup cols={2}>
<Card title="Quick Start Guide" icon="rocket" href="#creating-your-first-checkout-session">
  Get your first checkout session running in under 5 minutes
</Card>

<Card title="API Reference & Live Testing" icon="code" href="/api-reference/checkout-sessions/create">
  Explore the full API documentation and interactively test Checkout Session requests and responses.
</Card>

<Card title="Preview Checkout" icon="eye" href="/api-reference/checkout-sessions/preview">
  Calculate pricing, taxes, and totals before creating a session.
</Card>
</CardGroup>

<Info>
**Session Validity**: Checkout sessions are valid for 24 hours by default. If you pass `confirm=true` in your request, the session will only be valid for 15 minutes.
</Info>

## Prerequisites

<Steps>
<Step title="Dodo Payments Account">
  You'll need an active Dodo Payments merchant account with API access.
</Step>

<Step title="API Credentials">
  Generate your API credentials from the Dodo Payments dashboard:
</Step>

<Step title="Products Setup">
  Create your products in the Dodo Payments dashboard before implementing checkout sessions.
</Step>
</Steps>

## Creating Your First Checkout Session

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### API Response

All methods above return the same response structure:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
<Step title="Get the checkout URL">
Extract the `checkout_url` from the API response.
</Step>

<Step title="Redirect your customer">
Direct your customer to the checkout URL to complete their purchase.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Alternative Integration Options**: Instead of redirecting, you can embed the checkout directly in your page using [Overlay Checkout](/developer-resources/overlay-checkout) (modal overlay) or [Inline Checkout](/developer-resources/inline-checkout) (fully embedded). Both options use the same checkout session URL.
</Tip>
</Step>

<Step title="Handle the return">
After payment, customers are redirected to your `return_url` with query parameters including payment/subscription ID, status, customer email, and any license keys. See the [`return_url` parameter docs](#request-body) for the full list.
</Step>
</Steps>

## Request Body

<CardGroup cols={2}>
<Card title="Required Fields" icon="asterisk">
  Essential fields needed for every checkout session
</Card>

<Card title="Optional Fields" icon="gear">
  Additional configuration to customize your checkout experience
</Card>
</CardGroup>

### Required Fields

<ParamField body="product_cart" type="array" required>
Array of products to include in the checkout session. Each product must have a valid `product_id` from your Dodo Payments dashboard.

<Tip>
**Mixed Checkout**: You can combine one-time payment products and subscription products in the same checkout session. This enables powerful use cases like setup fees with subscriptions, hardware bundles with SaaS, and more.
</Tip>

<Expandable title="Product Cart Item Properties">
<ParamField body="product_id" type="string" required>
The unique identifier of the product from your Dodo Payments dashboard.

**Example:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
Quantity of the product.

**Example:** `1` for single item, `3` for multiple quantities
</ParamField>

<ParamField body="addons" type="array">
Array of addons to attach to the product. Only valid if the product is a subscription.

<Expandable title="Addon item properties">
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
Amount the customer pays if pay_what_you_want is enabled. If disabled, this field will be ignored.

**الصيغة**: يتم تمثيلها بأقل فئة من العملة (مثل السنت للـ USD). على سبيل المثال، لفرض رسوم $1.00، قم بتمرير `100`.
</ParamField>

{/* LOCKED_PATTERN_1cc543c3a04a4c1f6d9b021f4a2eca5e */}
تجاوزات لكل جلسة دفع للحقوق الائتمانية المرتبطة بالفعل بهذا المنتج. استخدم هذا لمنح عدد مختلف من الأرصدة لجلسة واحدة — مثلاً، حزمة ترويجية تمنح 1,000 رصيد API بدلاً من 500 الافتراضية على المنتج — دون إنشاء منتج منفصل.

<Info>
كل `credit_entitlement_id` يجب أن يكون متصلاً بالفعل بالمنتج. هذا الحقل يتجاوز فقط `credits_amount` الممنوح عند تحقيق هذه الجلسة؛ لا تزال إعدادات حقوق الائتمان على مستوى المنتج (مثل الانتهاء، التدوير، إلخ.) سارية.
</Info>

{/* LOCKED_PATTERN_b0d6df2f029cdb2dbad4427c93f9d7a5 */}
{/* LOCKED_PATTERN_3772971471266bd03aa44ad580de1758 */}
معرف حق الائتمان لتجاوزه. يجب أن يكون متصلاً بالفعل بالمنتج.
</ParamField>

{/* LOCKED_PATTERN_7a9c0efdd7e8e3c5319e61171d2f9d22 */}
عدد الأرصدة المراد منحها لهذه الجلسة، وتجاوز المستوى المنتجي `credits_amount`. يجب أن تكون أكبر من الصفر.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

<Tip>
**العثور على معرفات المنتجات الخاصة بك**: يمكنك العثور على معرفات المنتجات في لوحة تحكم Dodo Payments ضمن منتجات → عرض التفاصيل، أو باستخدام [List Products API](/api-reference/products/get-products).
</Tip>

### الحقول الاختيارية

قم بتكوين هذه الحقول لتخصيص تجربة الخروج وإضافة منطق الأعمال إلى تدفق الدفع الخاص بك.

<AccordionGroup>
<Accordion title="Customer Information">

<ParamField body="customer" type="object">
معلومات العملاء. يمكنك إما إرفاق عميل موجود باستخدام معرفه أو إنشاء سجل عميل جديد أثناء الخروج.

<Tabs>
<Tab title="Attach Existing Customer">
أرفق عميلاً موجودًا بجلسة الخروج باستخدام معرفه.

<Expandable title="Existing Customer Properties">
<ParamField body="customer_id" type="string" required>
معرف الفريد لعميل موجود. استخدم هذا لإرفاق جلسة الدفع بعميل موجود بدلاً من إنشاء واحدة جديدة.
</ParamField>
</Expandable>
</Tab>

<Tab title="New Customer">
إنشاء سجل عميل جديد أثناء الخروج.

<Expandable title="New Customer Properties">
<ParamField body="email" type="string" required>
عنوان البريد الإلكتروني للعميل. مطلوب عند إنشاء عميل جديد.
</ParamField>

<ParamField body="name" type="string">
الاسم الكامل للعميل كما ينبغي أن يظهر في الإيصالات والفواتير.

**مثال**: `"John Doe"` أو `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
رقم هاتف العميل بالتنسيق الدولي. مطلوب لبعض طرق الدفع ومنع الاحتيال.

**الصيغة**: قم بتضمين رمز البلد، مثلاً، `"+1234567890"` لأرقام الولايات المتحدة
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
معلومات العنوان الفواتيري لحساب الضرائب بدقة، منع الاحتيال، والامتثال التنظيمي.

<Info>
عندما يتم إعداد `confirm` للإعداد `true`، تصبح جميع حقول عنوان الفواتير مطلوبة لإنشاء الجلسة بنجاح.
</Info>

<Expandable title="Billing address object properties">
<ParamField body="street" type="string">
العنوان الشامل للشارع يشمل رقم البيت، اسم الشارع، ورقم الشقة/الوحدة إذا كان ذلك ممكنًا.

**مثال**: `"123 Main St, Apt 4B"` أو `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
اسم المدينة أو البلدية.

**مثال**: `"San Francisco"`، `"New York"`، `"London"`
</ParamField>

<ParamField body="state" type="string">
اسم المنطقة أو المحافظة أو الإقليم. استخدم الأسماء الكاملة أو الاختصارات القياسية.

**مثال**: `"California"` أو `"CA"`، INLINE_CODE_PLACEHOLDER_03d80b84f8cbaa9_END أو `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
رمز الدولة الرسمي المكون من حرفين (ISO 3166-1 alpha-2). هذا الحقل مطلوب دائمًا عند توفير عنوان الفواتير.

**أمثلة**: `"US"` (الولايات المتحدة الأمريكية)، `"CA"` (كندا)، `"GB"` (المملكة المتحدة)، `"DE"` (ألمانيا)

<Card title="Country Code Reference" icon="globe" href="/api-reference/misc/supported-countries">
  عرض قائمة كاملة بالدول المدعومة وأكواد ISO الخاصة بها
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
الرمز البريدي أو ما يعادله بناءً على متطلبات الدولة.

**أمثلة**: `"94102"` (الولايات المتحدة)، `"M5V 3A8"` (كندا)، `"SW1A 1AA"` (المملكة المتحدة)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

<Accordion title="Payment Configuration">

<ParamField body="allowed_payment_method_types" type="array">
تحكم في طرق الدفع المتاحة للعملاء أثناء الخروج. يساعد هذا في تحسين الأسواق المحددة أو متطلبات الأعمال.

**الخيارات المتاحة**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**حرج**: قم دائمًا بتضمين `credit` و`debit` كخيارات احتياطية لمنع فشل الخروج عندما تكون طرق الدفع المفضلة غير متاحة.
</Warning>

**مثال**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
تجاوز اختيار العملة الافتراضي مع عملة فوترة محددة. يستخدم رموز العملات ISO 4217.

**العملات المدعومة**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR`, والمزيد

**مثال**: `"USD"` للدولار الأمريكي، `"EUR"` لليورو

<Note>
هذا الحقل يكون فعالًا فقط عندما تكون التسعير التكيفي ممكنة. إذا كانت التسعير التكيفي معطلة، فسيتم استخدام العملة الافتراضية للمنتج.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
عرض طرق الدفع المحفوظة مسبقًا للعملاء العائدين، مما يحسن سرعة الخروج وتجربة المستخدم.
</ParamField>

</Accordion>

<Accordion title="Session Management">

<ParamField body="return_url" type="string">
عنوان URL لإعادة توجيه العملاء بعد إتمام الدفع. يقوم تطبيق Dodo Payments بإلحاق المعلمات الاستعلامية التالية إلى عنوان URL الخاص بك عند إعادة التوجيه:

| المعامل | النوع | الحالة |
|-----------|------|-----------|
| `payment_id` | `string` | حاضر دائمًا للمدفوعات الفردية |
| `subscription_id` | `string` | حاضر دائمًا لمدفوعات الاشتراكات |
| `status` | `string` | حاضر دائمًا |
| `license_key` | `string` | حاضر إذا كان المنتج يحتوي على مفاتيح ترخيص ممكنة. مفصولة بفواصل إذا كان هناك عدة مفاتيح |
| `email` | `string` | حاضر إذا كان للعميل بريد إلكتروني مسجل |

**أمثلة على عناوين URL لإعادة التوجيه:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
استخدم معاملي `license_key` و`email` لعرض مفاتيح الترخيص أو إرسال تأكيد فورًا على صفحتك للعودة، دون الحاجة إلى استدعاء API إضافي.
</Tip>
</ParamField>

<ParamField body="cancel_url" type="string">
عنوان URL لإعادة توجيه العملاء عند النقر على زر الرجوع أو إلغاء جلسة الدفع. إذا لم يتم توفيره، لن يتم عرض زر الرجوع.

<Tip>
حدد `cancel_url` لتقديم طريقة واضحة للعملاء للعودة إلى موقعك دون إتمام الشراء. هذا يحسن تجربة الخروج ويقلل من العوائق.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
إذا كانت صحيحة، فإنه يُنهي جميع تفاصيل الجلسة على الفور. ستقوم API بإلقاء خطأ إذا كانت البيانات المطلوبة مفقودة.
</ParamField>

{/* LOCKED_PATTERN_db6ea1a90bc6c05f9f398a4adeb2dd67 */}
تطبيق رمز خصم واحد أو أكثر مكدسة على جلسة الدفع. يتم تطبيق الرموز **بالترتيب المصنفي** (يقلل الرمز الأول السعر الأساسي، والرمز الثاني يقلل السعر المخفض بالفعل، وهكذا)، بحد أقصى **20** رمز لكل جلسة.

```typescript
discount_codes: ['WELCOME10', 'BLACKFRIDAY20']
```

<Info>
الحقل الفردي `discount_code` أدناه هو **مهمل** ولكنه لا يزال مدعوم بالكامل — تستمر التكاملات الحالية في العمل بدون تغييرات. لا يمكن دمجه مع `discount_codes` في نفس الطلب. انتقل إلى `discount_codes` عندما يكون ملائم للاستفادة من المكدسة.
</Info>
</ParamField>

{/* LOCKED_PATTERN_e63db2d6e61a978842d79e65089cf7dc */}
**مهملة** — يُفضَّل استخدام `discount_codes` للتكاملات الجديدة. لا يزال هذا الحقل يعمل من أجل التوافق مع الإصدارات القديمة، ولكن لا يمكن دمجه مع `discount_codes` في نفس الطلب.
</ParamField>

<ParamField body="metadata" type="object">
أزواج مفتاح-قيمة مخصصة لتخزين معلومات إضافية حول الجلسة.
</ParamField>

<ParamField body="force_3ds" type="boolean">
تجاوز سلوك 3DS التاجر الافتراضي لهذه الجلسة.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
تمكين وضع جمع العنوان الأدنى. عند تمكينه، يقوم الدفع بجمع فقط:
- **البلد**: دائمًا مطلوب لتحديد الضرائب
- **الرمز البريدي**: فقط في المناطق حيث يكون ضرورياً لحساب ضرائب المبيعات أو ضريبة القيمة المضافة

هذا يقلل بشكل كبير من العوائق أثناء الدفع عن طريق إزالة الحقول غير الضرورية في النموذج.

<Tip>
قم بتمكين الحد الأدنى من العنوان لإكمال الخروج بشكل أسرع. يظل جمع العنوان الكامل متاحًا للشركات التي تتطلب تفاصيل الفوترة الكاملة.
</Tip>
</ParamField>

</Accordion>

<Accordion title="UI Customization & Features">

<ParamField body="customization" type="object">
 تخصيص المظهر والسلوك لواجهة الدفع.

<Expandable title="Customization object properties">
<ParamField body="theme" type="string" default="system">
 السمات لواجهة الدفع. الخيارات: `light`، `dark`، أو `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
 عرض قسم تفاصيل الطلب في واجهة الدفع.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
 عرض علامة "حسب الطلب" للمنتجات القابلة للتطبيق.
</ParamField>

<ParamField body="force_language" type="string">
 إجبار واجهة الدفع على العرض بلغة محددة. افتراضيًا، يكتشف الدفع لغة المتصفح الخاصة بالعميل تلقائيًا.

**الأكواد اللغوية المدعومة:**
`ar` (العربية)، `ca` (الكتالونية)، `de` (الألمانية)، `en` (الإنجليزية)، `es` (الإسبانية)، `fr` (الفرنسية)، `he` (العبرية)، `id` (الإندونيسية)، `it` (الإيطالية)، `ja` (اليابانية)، `ko` (الكورية)، `ms` (الماليزية)، `nl` (الهولندية)، `pl` (البولندية)، `pt` (البرتغالية)، `ro` (الرومانية)، `ru` (الروسية)، `sv` (السويدية)، `th` (التايلاندية)، `tr` (التركية)، `zh` (الصينية)

**مثال**: `"es"` للإسبانية، `"ja"` لليابانية
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
تكوين ميزات وسلوكيات محددة لجلسة الدفع.

<Expandable title="Feature flags object properties">
<ParamField body="allow_currency_selection" type="boolean" default="true">
السماح للعملاء باختيار العملة المفضلة لديهم أثناء الدفع.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
عرض حقل إدخال رمز الخصم في واجهة الدفع.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
جمع أرقام هواتف العملاء أثناء الدفع.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
السماح للعملاء بإدخال أرقام التعريف الضريبي.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
إجبار على إنشاء سجل عميل جديد بدلاً من تحديث الموجودين.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
السماح للعملاء بتحرير عنوان بريدهم الإلكتروني أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
السماح للعملاء بتحرير أسمائهم أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
السماح للعملاء بتحرير عنوان الشارع أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
السماح للعملاء بتحرير اسم المدينة أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
السماح للعملاء بتحرير اسم المحافظة أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
السماح للعملاء بتحرير اسم البلد أثناء الدفع.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
السماح للعملاء بتحرير الرمز البريدي أثناء الدفع.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
تمكين وضع جمع العنوان الأدنى. عند تمكينه، يقوم الدفع بجمع فقط:
- **البلد**: دائمًا مطلوب لتحديد الضرائب
- **الرمز البريدي/الرمز البريدي**: فقط في المناطق حيث يكون ضرورياً لحساب ضرائب المبيعات أو ضريبة القيمة المضافة

هذا يقلل بشكل كبير من العوائق أثناء الدفع عن طريق إزالة الحقول غير الضرورية في النموذج.

<Tip>
قم بتمكين الحد الأدنى من العنوان لإكمال الخروج بشكل أسرع. يظل جمع العنوان الكامل متاحًا للشركات التي تتطلب تفاصيل الفوترة الكاملة.
</Tip>
</ParamField>

<ParamField body="redirect_immediately" type="boolean" default="false">
تخطي صفحة نجاح الدفع الافتراضية وإعادة توجيه العملاء فورًا إلى `return_url` بعد اكتمال الدفع.

<Tip>
استخدم هذا عندما يكون لديك صفحة نجاح مخصصة توفر تجربة مستخدم أفضل، أو لتطبيقات الجوال وتدفقات الدفع المدمجة.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

<Accordion title="Custom Fields">

<ParamField body="custom_fields" type="array">
جمع معلومات إضافية من العملاء أثناء الدفع باستخدام حقول نموذج مخصصة. يمكنك تعريف ما يصل إلى 5 حقول مخصصة لكل جلسة دفع. يتم تضمين ردود العملاء في حمولة الويب وتكون متاحة عبر API.

<Expandable title="Custom field object properties">
<ParamField body="key" type="string" required>
معرف فريد لهذا الحقل. يُستخدم كمفتاح في حمولة الويب واستجابات API.

**مثال**: `"company_name"`، `"referral_source"`، `"team_size"`
</ParamField>

<ParamField body="label" type="string" required>
التسمية العرضية التي تظهر للعميل على نموذج الدفع.

**مثال**: `"Company Name"`، `"How did you hear about us?"`، `"Team Size"`
</ParamField>

<ParamField body="field_type" type="string" required>
نوع الحقل الذي يحدد قواعد التحقق من الإدخال.

**الأنواع المتاحة**:
- `text` - إدخال نصي بسطر واحد
- `number` - إدخال رقمي
- `email` - عنوان بريد إلكتروني مع التحقق
- `url` - URL مع التحقق
- `date` - منتقي تاريخ
- `dropdown` - اختر من بين خيارات محددة مسبقًا
- `boolean` - تبديل نعم/لا
</ParamField>

<ParamField body="required" type="boolean" default="false">
ما إذا كان يجب ملء هذا الحقل قبل إتمام عملية الدفع.
</ParamField>

<ParamField body="placeholder" type="string">
النص الظاهر المعروض في حقل الإدخال.
</ParamField>

<ParamField body="options" type="array">
قائمة الخيارات لنوع الحقل `dropdown`. مطلوب عندما يكون `field_type` هو `dropdown`، ويتجاهل للأنواع الأخرى.

**مثال**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
تتضمن ردود العملاء على الحقول المخصصة:
- **الويب هوكس**: `payment.succeeded`, `subscription.active`، وغيرها من الحمولة ذات الصلة تحتوي على مصفوفة `custom_field_responses`
- **استجابات API**: تشمل كائنات الدفع والاشتراك `custom_field_responses`
</Info>

</Accordion>

<Accordion title="Subscription Configuration">

<ParamField body="subscription_data" type="object">
التكوين الإضافي لجلسات الدفع التي تحتوي على منتجات الاشتراك.

<Expandable title="Subscription data object properties">
<ParamField body="trial_period_days" type="integer">
عدد الأيام لفترة التجربة قبل التحصيل الأول. يجب أن يتراوح بين 0 و10000 يوم.
</ParamField>

<ParamField body="on_demand" type="object">
تكوين الاشتراك عند الطلب للفوترة القائمة على الاستخدام أو القياس.

<Expandable title="On-demand object properties">
<ParamField body="mandate_only" type="boolean" required>
إذا تم تحديده للصحيح، فلا يتم إجراء أي تحصيل ويتم فقط تفويض تفاصيل طريقة الدفع للاستخدام المستقبلي.
{/* LOCKED_PATTERN_ 77dc7d49f01ac1386a904a5fe6b66ba5 */}

<ParamField body="product_price" type="integer">
سعر المنتج للتحصيل الأولي من العميل. إذا لم يتم تحديده، سيتم استخدام السعر المخزن للمنتج.

**الصيغة**: يتم تمثيلها بأقل فئة من العملة (مثل السنت للـ USD). على سبيل المثال، لفرض رسوم $1.00، قم بتمرير `100`.
</ParamField>

<ParamField body="product_currency" type="string">
عملة اختيارية لسعر المنتج. إذا لم يتم تحديدها، ستكون الافتراضية هي للعملة الخاصة بالمنتج.
</ParamField>

<ParamField body="product_description" type="string">
تجاوز اختياري لوصف المنتج لفواتير العناصر المفصلة. إذا لم يتم تحديدها، سيتم استخدام الوصف المخزن للمنتج.
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean"> 
ما إذا كان ينبغي تضمين رسوم العملة التكيفية في سعر المنتج (صحيح) أو إضافة عليها (خاطئ). لا يتم النظر في التسعير التكيفي إذا لم يكن ممكنًا.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## أمثلة الاستخدام

إليك 10 أمثلة شاملة تعرض تكوينات لجلسات الدفع المختلفة لسيناريوهات الأعمال المتنوعة:

### 1. شراء منتج واحد بسيط

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. سلة مشتريات تحتوي على عدة منتجات

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. اشتراك مع فترة تجربة

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. شراء مؤكد مسبقًا

<Note>
عند إعداد `confirm` للإعداد `true`، سيتم أخذ العميل مباشرة إلى صفحة الدفع، متجاوزًا أي خطوات تأكيدية.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. دفع مع تجاوز العملة

<Note>
لا يسري تجاوز `billing_currency` إلا عندما تكون العملة التكيفية ممكنة في إعدادات حسابك. إذا كانت العملة التكيفية معطلة، لن يكون لهذا المعامل أي تأثير.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. طرق الدفع المحفوظة للعملاء القادمين

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. دفع B2B مع جمع رقم التعريف الضريبي

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. دفعة بواجهة مظلمة مع أكواد خصم مكدسة

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_codes: ['BLACKFRIDAY2024'],
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. طرق الدفع الإقليمية (UPI للهند)

لمزيد من المعلومات حول تكوين واختبار UPI، راجع صفحة <a href="/features/payment-methods/india">طرق الدفع في الهند</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. الدفع بواسطة BNPL (اشتر الآن وادفع لاحقًا)

لمزيد من المعلومات حول تكوين BNPL والاختبارات، راجع صفحة <a href="/features/payment-methods/bnpl">اشتر الآن وادفع لاحقًا (BNPL)</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. استخدام طرق الدفع الموجودة للتسوق الفوري

استخدم طريقة دفع محفوظة لدى العميل لإنشاء جلسة دفع تتم معالجتها فورًا، متجاوزةً جمع طريقة الدفع:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
عند استخدام `payment_method_id`، يجب ضبط `confirm` على `true` ويجب توفير `customer_id` الموجود. سيتم التحقق من صحة طريقة الدفع للأهلية بعملة الدفع.
</Warning>

<Info>
يجب أن تكون طريقة الدفع مملوكة للعميل وقابلة للاستخدام مع عملة الدفع. هذا يمكن العملاء العائدين من عمليات الشراء بنقرة واحدة.
</Info>

### 12. روابط قصيرة لروابط دفع أوضح

توليد روابط دفع مختصرة وقابلة للمشاركة مع عناوين مخصصة:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
تعتبر الروابط القصيرة مثالية للمشاركة عبر الرسائل النصية أو البريد الإلكتروني أو وسائل التواصل الاجتماعي. إنها أسهل في التذكر وتبني المزيد من الثقة للعملاء مقارنةً بروابط URL الطويلة.
</Tip>

### 13. تخطي صفحة نجاح الدفع بإعادة التوجيه الفوري

إعادة توجيه العملاء فورًا بعد إتمام الدفع، متجاوزًا صفحة النجاح الافتراضية:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
استخدم `redirect_immediately: true` عندما يكون لديك صفحة نجاح مخصصة تقدم تجربة مستخدم أفضل من صفحة نجاح الدفع الافتراضية. هذا يكون مفيدًا بشكل خاص لتطبيقات الجوال وتدفقات الدفع المدمجة.
</Tip>

<Note>
عند تمكين `redirect_immediately`، يتم توجيه العملاء إلى `return_url` الخاصة بك فورًا بعد إتمام الدفع، متجاوزًا صفحة النجاح الافتراضية بالكامل.
</Note>

### 14. إجبار لغة معينة

أجبر الدفع للعرض بلغة محددة، متجاوزًا اكتشاف لغة المتصفح الخاصة بالعميل:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
استخدم `force_language` عندما تعرف لغة العميل المفضلة (مثلاً، من إعدادات حسابه) أو عند استهداف أسواق إقليمية محددة.
</Tip>

**اللغات المدعومة:** العربية (`ar`)، الكتالونية (`ca`)، الصينية (`zh`)، الهولندية (`nl`)، الإنجليزية (`en`)، الفرنسية (`fr`)، الألمانية (`de`)، العبرية (`he`)، الإندونيسية (`id`)، الإيطالية (`it`)، اليابانية (`ja`)، الكورية (`ko`)، الماليزية (`ms`)، البولندية (`pl`)، البرتغالية (`pt`)، الرومانية (`ro`)، الروسية (`ru`)، الإسبانية (`es`)، السويدية (`sv`)، التايلاندية (`th`)، التركية (`tr`)

### 15. تجميع حقول مخصصة

جمع معلومات إضافية من العملاء أثناء الدفع باستخدام الحقول المخصصة:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
الردود على الحقول المخصصة تضاف تلقائيًا في حمولة الويب هوكس (`payment.succeeded`, `subscription.active`، إلخ.) ويمكن استخراجها عبر API. استخدمها لتعزيز CRM الخاص بك، وتنفيذ تدفقات إعداد العملاء الجدد، أو تخصيص تجربة العملاء.
</Info>

**أنواع الحقول المتاحة**: `text`، `number`، `email`، `url`، `date`، `dropdown`، `boolean`

## معاينة جلسات الدفع

قبل إنشاء جلسة دفع، يمكنك معاينة تفاصيل التسعير بما في ذلك الضرائب، والخصومات، والإجماليات. يكون هذا مفيدًا لعرض تسعير دقيق للعملاء قبل متابعة عملية الدفع.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_codes: ['SAVE20']
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_codes=["SAVE20"]
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

<Card title="Preview API Reference" icon="code" href="/api-reference/checkout-sessions/preview">
عرض وثائق نقطة المعاينة الكاملة.
</Card>

## الانتقال من الروابط الديناميكية إلى جلسات الدفع

### الفروق الرئيسية
في السابق، عند إنشاء رابط دفع باستخدام الروابط الديناميكية، كان عليك تقديم عنوان الفواتير الكامل للعميل.

مع جلسات الدفع، لم يعد ذلك ضروريًا. يمكنك فقط إرسال المعلومات المتوفرة لديك، وسنتولى الباقي. على سبيل المثال:

- إذا كنت تعرف فقط بلد الفواتير الخاص بالعميل، فقط قدم ذلك.
- سيجمع تدفق الدفع التفاصيل المفقودة تلقائيًا قبل الانتقال بالعميل إلى صفحة الدفع.
- من ناحية أخرى، إذا كان لديك جميع المعلومات المطلوبة وتريد الانتقال مباشرة إلى صفحة الدفع، يمكنك تمرير مجموعة البيانات الكاملة وتضمين `confirm=true` في طلبك.

### عملية الانتقال

الانتقال من الروابط الديناميكية إلى جلسات الدفع سهل:

<Steps>
<Step title="Update your integration">
تحديث تكاملاتك لاستخدام الأسلوب الجديد لـ API أو SDK.
</Step>

<Step title="Adjust request payload">
تعديل حمولة الطلب وفقاً لتنسيق جلسات الدفع.
</Step>

<Step title="That's it!">
نعم. لا توجد معالجة إضافية أو خطوات انتقال خاصة مطلوبة من جانبك.
</Step>
</Steps>

## مرجع API المتعلق

<CardGroup cols={2}>
<Card title="Create Checkout Session" icon="code" href="/api-reference/checkout-sessions/create">
  مرجع API الكامل لإنشاء جلسات دفع مع جميع المعامِلات والخيارات المتاحة
</Card>
<Card title="Preview Checkout Session" icon="eye" href="/api-reference/checkout-sessions/preview">
  مرجع API لمعاينة التسعير والضرائب والإجماليات قبل إنشاء جلسة
</Card>
</CardGroup>