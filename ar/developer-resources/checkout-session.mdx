---
title: جلسات الدفع
description: إنشاء تجارب دفع آمنة ومُستضافة تتعامل مع تدفق الدفع الكامل لكل من
  المشتريات لمرة واحدة والاشتراكات مع التحكم الكامل في التخصيص.
icon: cart-shopping
---

<CardGroup cols={2}>
<Card title="دليل البدء السريع" icon="rocket" href="#creating-your-first-checkout-session">
  قم بتشغيل أول جلسة دفع لك في أقل من 5 دقائق
</Card>

<Card title="مرجع API واختبار مباشر" icon="code" href="/api-reference/checkout-sessions/create">
  استكشف الوثائق الكاملة لـ API واختبر تفاعليًا طلبات واستجابات جلسة الدفع.
</Card>

<Card title="معاينة الدفع" icon="eye" href="/api-reference/checkout-sessions/preview">
  احسب الأسعار والضرائب والمجموعات قبل إنشاء جلسة.
</Card>
</CardGroup>

<Info>
**صلاحية الجلسة**: جلسات الخروج صالحة لمدة 24 ساعة بشكل افتراضي. إذا قمت بتمرير `confirm=true` في طلبك، ستكون الجلسة صالحة لمدة 15 دقيقة فقط.
</Info>

## المتطلبات الأساسية

<Steps>
<Step title="حساب مدفوعات دودو">
  ستحتاج إلى حساب تاجر نشط في مدفوعات دودو مع وصول إلى API.
</Step>

<Step title="بيانات اعتماد API">
  قم بإنشاء بيانات اعتماد API الخاصة بك من لوحة معلومات مدفوعات دودو:
</Step>

<Step title="إعداد المنتجات">
  قم بإنشاء منتجاتك في لوحة معلومات مدفوعات دودو قبل تنفيذ جلسات الدفع.
</Step>
</Steps>

## إنشاء أول جلسة دفع لك

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### استجابة API

ترجع جميع الطرق أعلاه نفس هيكل الاستجابة:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
<Step title="احصل على رابط الخروج">
استخرج `checkout_url` من استجابة API.
</Step>

<Step title="إعادة توجيه عميلك">
وجه عميلك إلى عنوان URL للدفع لإكمال عملية الشراء.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**خيارات تكامل بديلة**: بدلاً من إعادة التوجيه، يمكنك تضمين الدفع مباشرة في صفحتك باستخدام [الدفع عبر Overlay](/developer-resources/overlay-checkout) (تراكب نافذة) أو [الدفع المضمن](/developer-resources/inline-checkout) (مضمن بالكامل). كلا الخيارين يستخدمان نفس عنوان URL لجلسة الدفع.
</Tip>
</Step>

<Step title="تعامل مع العودة">
بعد الدفع، يتم إعادة توجيه العملاء إلى `return_url` مع معلمات الاستعلام بما في ذلك معرف الدفع/الاشتراك، الحالة، بريد العميل الإلكتروني، وأي مفاتيح ترخيص. راجع [مستندات معلمات `return_url`](#request-body) للقائمة الكاملة.
</Step>
</Steps>

## جسم الطلب

<CardGroup cols={2}>
<Card title="الحقول المطلوبة" icon="asterisk">
  الحقول الأساسية المطلوبة لكل جلسة دفع
</Card>

<Card title="الحقول الاختيارية" icon="gear">
  تكوين إضافي لتخصيص تجربة الدفع الخاصة بك
</Card>
</CardGroup>

### الحقول المطلوبة

<ParamField body="product_cart" type="array" required>
مجموعة من المنتجات لتضمينها في جلسة الخروج. يجب أن تحتوي كل منتج على `product_id` صالح من لوحة معلومات Dodo Payments.

<Tip>
**دفع مختلط**: يمكنك دمج منتجات الدفع لمرة واحدة ومنتجات الاشتراك في نفس جلسة الدفع. هذا يمكّن حالات استخدام قوية مثل رسوم الإعداد مع الاشتراكات، وحزم الأجهزة مع SaaS، والمزيد.
</Tip>

<Expandable title="خصائص عنصر سلة المنتجات">
<ParamField body="product_id" type="string" required>
المعرف الفريد للمنتج من لوحة معلومات مدفوعات دودو الخاصة بك.

**مثال:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
كمية المنتج.

**مثال:** `1` لعنصر واحد، `3` لكميات متعددة
</ParamField>

<ParamField body="addons" type="array">
مصفوفة من الإضافات المرفقة بالمنتج. صالحة فقط إذا كان المنتج اشتراكًا.

<Expandable title="خصائص عنصر الإضافة">
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
المبلغ الذي يدفعه العميل إذا تم تمكين pay_what_you_want. إذا تم تعطيله، سيتم تجاهل هذا الحقل.

**التنسيق**: مُعاد تمثيله بأدنى فئة من العملة (مثل: سنتات لدولار أمريكي). على سبيل المثال، لتحصيل $1.00، تمرير `100`.
</ParamField>
</Expandable>
</ParamField>

<Tip>
**ابحث عن معرفات منتجاتك**: يمكنك العثور على معرفات المنتجات في لوحة معلومات مدفوعات دودو تحت المنتجات → عرض التفاصيل، أو باستخدام [API قائمة المنتجات](/api-reference/products/get-products).
</Tip>

### الحقول الاختيارية

قم بتكوين هذه الحقول لتخصيص تجربة الدفع وإضافة منطق الأعمال إلى تدفق الدفع الخاص بك.

<AccordionGroup>
<Accordion title="معلومات العميل">

<ParamField body="customer" type="object">
معلومات العميل. يمكنك إما إرفاق عميل موجود باستخدام معرفه أو إنشاء سجل عميل جديد أثناء الدفع.

<Tabs>
<Tab title="إرفاق عميل موجود">
إرفاق عميل موجود بجلسة الدفع باستخدام معرفه.

<Expandable title="خصائص العميل الموجود">
<ParamField body="customer_id" type="string" required>
المعرف الفريد لعميل موجود. استخدم هذا لإرفاق جلسة الدفع بعميل موجود بدلاً من إنشاء عميل جديد.
</ParamField>
</Expandable>
</Tab>

<Tab title="عميل جديد">
إنشاء سجل عميل جديد أثناء الدفع.

<Expandable title="خصائص العميل الجديد">
<ParamField body="email" type="string" required>
عنوان البريد الإلكتروني للعميل. مطلوب عند إنشاء عميل جديد.
</ParamField>

<ParamField body="name" type="string">
الاسم الكامل للعميل كما يجب أن يظهر على الإيصالات والفواتير.

**مثال**: `"John Doe"` أو `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
رقم هاتف العميل بالتنسيق الدولي. مطلوب لبعض طرق الدفع ولمنع الاحتيال.

**التنسيق**: يتضمن رمز البلد، على سبيل المثال، `"+1234567890"` للأرقام الأمريكية
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
معلومات عنوان الفواتير لضمان حساب الضرائب بدقة، ومنع الاحتيال، والامتثال التنظيمي.

<Info>
عندما يتم تعيين `confirm` إلى `true`، تصبح جميع حقول عنوان الفواتير مطلوبة لإنشاء جلسة ناجحة.
</Info>

<Expandable title="خصائص كائن عنوان الفواتير">
<ParamField body="street" type="string">
عنوان الشارع الكامل بما في ذلك رقم المنزل، اسم الشارع، ورقم الشقة/الوحدة إذا كان ذلك مناسبًا.

**مثال**: `"123 Main St, Apt 4B"` أو `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
اسم المدينة أو البلدية.

**مثال**: `"San Francisco"`، `"New York"`، `"London"`
</ParamField>

<ParamField body="state" type="string">
اسم الولاية أو المقاطعة أو المنطقة. استخدم الأسماء الكاملة أو الاختصارات القياسية.

**مثال**: `"California"` أو `"CA"`، `"Ontario"` أو `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
رمز الدولة المكون من حرفين (ISO 3166-1 alpha-2). هذا الحقل مطلوب دائمًا عند تقديم عنوان الفواتير.

**أمثلة**: `"US"` (الولايات المتحدة الأمريكية)، `"CA"` (كندا)، `"GB"` (المملكة المتحدة)، `"DE"` (ألمانيا)

<Card title="مرجع رموز الدول" icon="globe" href="/api-reference/misc/supported-countries">
  عرض القائمة الكاملة للدول المدعومة ورموزها ISO
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
الرمز البريدي، أو رمز ZIP، أو ما يعادله بناءً على متطلبات الدولة.

**أمثلة**: `"94102"` (الولايات المتحدة الأمريكية)، `"M5V 3A8"` (كندا)، `"SW1A 1AA"` (المملكة المتحدة)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

<Accordion title="تكوين الدفع">

<ParamField body="allowed_payment_method_types" type="array">
تحكم في طرق الدفع المتاحة للعملاء أثناء الدفع. يساعد ذلك في تحسين الأسواق أو متطلبات الأعمال المحددة.

**الخيارات المتاحة**: `credit`، `debit`، `upi_collect`، `apple_pay`، `google_pay`، `amazon_pay`، `klarna`، `affirm`، `afterpay_clearpay`، `cashapp`، `multibanco`، `bancontact_card`، `eps`، `ideal`، `przelewy24`، `paypal`

<Warning>
**حرج**: يجب دائماً تضمين `credit` و `debit` كخيارات احتياطية لمنع فشل الخروج عند عدم توفر وسائل الدفع المفضلة.
</Warning>

**مثال**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
تجاوز اختيار العملة الافتراضية مع عملة فواتير ثابتة. يستخدم رموز العملة ISO 4217.

**العملات المدعومة**: `USD`، `EUR`، `GBP`، `CAD`، `AUD`، `INR`، والمزيد

**مثال**: `"USD"` لدولارات الأمريكية، `"EUR"` لليورو

<Note>
هذا الحقل فعال فقط عند تمكين التسعير التكيفي. إذا تم تعطيل التسعير التكيفي، سيتم استخدام العملة الافتراضية للمنتج.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
عرض طرق الدفع المحفوظة مسبقًا للعملاء العائدين، مما يحسن سرعة الدفع وتجربة المستخدم.
</ParamField>

</Accordion>

<Accordion title="إدارة الجلسة">

<ParamField body="return_url" type="string">
رابط لإعادة توجيه العملاء بعد إكمال الدفع. تضيف Dodo Payments معلمات الاستعلام التالية إلى الرابط الخاص بك عند إعادة التوجيه:

| المعلمة | النوع | الشرط |
|-----------|------|-----------|
| `payment_id` | `string` | موجود دائمًا للمدفوعات لمرة واحدة |
| `subscription_id` | `string` | موجود دائمًا لدفع الاشتراكات |
| `status` | `string` | موجود دائمًا |
| `license_key` | `string` | موجود إذا كان المنتج يحتوي على مفاتيح ترخيص مفعلة. مفصولة بفواصل إذا كان هناك مفاتيح متعددة |
| `email` | `string` | موجود إذا كان لدى العميل بريد إلكتروني مسجل |

**روابط إعادة التوجيه المثال:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
استخدم معلمات الاستعلام `license_key` و `email` لعرض مفاتيح الترخيص أو إرسال تأكيد على الفور في صفحة العودة الخاصة بك، دون الحاجة إلى مكالمة API إضافية.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
إذا كانت صحيحة، تكمل تفاصيل الجلسة جميعها على الفور. سيقوم API بإصدار خطأ إذا كانت البيانات المطلوبة مفقودة.
</ParamField>

<ParamField body="discount_code" type="string">
تطبيق رمز خصم على جلسة الخروج.
</ParamField>

<ParamField body="metadata" type="object">
أزواج المفاتيح القابلة للتخصيص لتخزين معلومات إضافية حول الجلسة.
</ParamField>

<ParamField body="force_3ds" type="boolean">
تجاوز سلوك 3DS الافتراضي للمرسال لهذه الجلسة.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
تمكين وضع جمع العناوين البسيطة. عند التمكين، تجمع عملية الخروج فقط:
- **البلد**: مطلوب دائمًا لتحديد الضريبة
- **الرمز البريدي**: فقط في المناطق التي هو ضروري فيها لحساب ضريبة المبيعات أو VAT أو GST

هذا يقلل بشكل كبير من احتكاك الخروج من خلال القضاء على حقول النماذج غير الضرورية.

<Tip>
قم بتمكين العنوان البسيط لإكمال الخروج بشكل أسرع. يظل جمع العنوان الكامل متاحًا للأعمال التي تتطلب تفاصيل فواتير كاملة.
</Tip>
</ParamField>

</Accordion>

<Accordion title="تخصيص الواجهة و الميزات">

<ParamField body="customization" type="object">
خصص مظهر وسلوك واجهة الخروج.

<Expandable title="خصائص كائن التخصيص">
<ParamField body="theme" type="string" default="system">
سمة واجهة الخروج. الخيارات: `light`، `dark`، أو `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
عرض قسم تفاصيل الطلب في واجهة الخروج.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
عرض علامة "عند الطلب" للمنتجات القابلة للتطبيق.
</ParamField>

<ParamField body="force_language" type="string">
إجبار واجهة الخروج على العرض بلغة معينة. بشكل افتراضي، تكتشف واجهة الخروج تلقائيًا لغة متصفح العميل.

**رموز اللغات المدعومة:**
`ar` (العربية)، `ca` (الكاتالونية)، `de` (الألمانية)، `en` (الإنجليزية)، `es` (الإسبانية)، `fr` (الفرنسية)، `he` (العبرية)، `id` (الإندونيسية)، `it` (الإيطالية)، `ja` (اليابانية)، `ko` (الكورية)، `ms` (الماليزية)، `nl` (الهولندية)، `pl` (البولندية)، `pt` (البرتغالية)، `ro` (الرومانية)، `ru` (الروسية)، `sv` (السويدية)، `th` (التايلاندية)، `tr` (التركية)، `zh` (الصينية)

**مثال**: `"es"` للإسبانية، `"ja"` لليابانية
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
قم بتكوين ميزات وسلوكيات محددة لجلسة الخروج.

<Expandable title="خصائص كائن الأعلام المميزة">
<ParamField body="allow_currency_selection" type="boolean" default="true">
السماح للعملاء باختيار عملتهم المفضلة أثناء الخروج.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
عرض حقل إدخال رمز الخصم في واجهة الخروج.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
جمع أرقام هاتف العملاء أثناء الخروج.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
السماح للعملاء بإدخال أرقام التعريف الضريبية.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
إجبار إنشاء سجل عميل جديد بدلاً من تحديث السجلات الموجودة.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
السماح للعملاء بتحرير عنوان بريدهم الإلكتروني أثناء الخروج.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
السماح للعملاء بتحرير اسمهم أثناء الخروج.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
السماح للعملاء بتحرير عنوان الشارع أثناء الخروج.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
السماح للعملاء بتحرير المدينة أثناء الخروج.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
السماح للعملاء بتحرير الولاية أثناء الخروج.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
السماح للعملاء بتحرير البلد أثناء الخروج.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
السماح للعملاء بتحرير الرمز البريدي أثناء الخروج.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
تمكين وضع جمع العناوين البسيطة. عندما يتم التمكين، تجمع عملية الخروج فقط:
- **البلد**: مطلوب دائمًا لتحديد الضريبة
- **الرمز البريدي**: فقط في المناطق التي هو ضروري فيها لحساب ضريبة المبيعات، VAT، أو GST

هذا يقلل بشكل كبير من احتكاك الخروج من خلال القضاء على حقول النماذج غير الضرورية.

<Tip>
قم بتمكين العنوان البسيط لإكمال الخروج بشكل أسرع. يظل جمع العنوان الكامل متاحًا للأعمال التي تتطلب تفاصيل فواتير كاملة.
</Tip>
</ParamField>

<ParamField body="redirect_immediately" type="boolean" default="false">
تخطي صفحة نجاح الدفع الافتراضية وإعادة توجيه العملاء على الفور إلى `return_url` بعد إكمال الدفع.

<Tip>
استخدم هذا عندما يكون لديك صفحة نجاح مخصصة توفر تجربة مستخدم أفضل، أو لتطبيقات الهاتف المحمول وتدفقات الخروج المدمجة.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

<Accordion title="الحقول المخصصة">

<ParamField body="custom_fields" type="array">
جمع معلومات إضافية من العملاء أثناء الخروج باستخدام حقول نموذج مخصصة. يمكنك تعريف ما يصل إلى 5 حقول مخصصة لكل جلسة خروج. يتم تضمين ردود العملاء في حمولة الويب هوك و متاحة عبر API.

<Expandable title="خصائص كائن الحقل المخصص">
<ParamField body="key" type="string" required>
معرف فريد لهذا الحقل. يستخدم كمفتاح في حمولة الويب هوك واستجابات API.

**مثال**: `"company_name"`، `"referral_source"`، `"team_size"`
</ParamField>

<ParamField body="label" type="string" required>
تسمية العرض التي تظهر للعميل على نموذج الخروج.

**مثال**: `"Company Name"`، `"How did you hear about us?"`، `"Team Size"`
</ParamField>

<ParamField body="field_type" type="string" required>
نوع الحقل الذي يحدد قواعد التحقق من الإدخال.

**الأنواع المتاحة**:
- `text` - إدخال نص أحادي السطر
- `number` - إدخال رقمي
- `email` - عنوان بريد إلكتروني مع التحقق
- `url` - URL مع التحقق
- `date` - اختيار تاريخ
- `dropdown` - اختيار من خيارات محددة مسبقًا
- `boolean` - تبديل نعم/لا
</ParamField>

<ParamField body="required" type="boolean" default="false">
ما إذا كان يجب ملء هذا الحقل قبل إتمام الخروج.
</ParamField>

<ParamField body="placeholder" type="string">
نص مكان يتم عرضه في حقل الإدخال.
</ParamField>

<ParamField body="options" type="array">
قائمة الخيارات لنوع حقل `dropdown`. مطلوب عندما `field_type` هو `dropdown`، يتم تجاهله للأنواع الأخرى.

**مثال**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
تتضمن ردود العملاء على الحقول المخصصة في:
- **الويب هوكس**: `payment.succeeded`، `subscription.created`، وحمولات الأحداث الأخرى ذات الصلة تحتوي على مصفوفة `custom_field_responses`
- **استجابات API**: تشمل كائنات الدفع والاشتراك `custom_field_responses`
</Info>

</Accordion>

<Accordion title="تكوين الاشتراك">

<ParamField body="subscription_data" type="object">
تكوين إضافي لجلسات الخروج التي تحتوي على منتجات الاشتراك.

<Expandable title="خصائص كائن بيانات الاشتراك">
<ParamField body="trial_period_days" type="integer">
عدد الأيام لفترة التجربة قبل أول رسوم. يجب أن تكون بين 0 و 10000 يوم.
</ParamField>

<ParamField body="on_demand" type="object">
تكوين اشتراك عند الطلب لفوترة بناءً على الاستخدام أو المدفوعات المتوسطة.

<Expandable title="خصائص كائن عند الطلب">
<ParamField body="mandate_only" type="boolean" required>
إذا تم تعيينها على صحيح، لا يتم إجراء أي خصم وتفويض فقط تفاصيل طريقة الدفع للاستخدام في المستقبل.
</ParamField>

<ParamField body="product_price" type="integer">
سعر المنتج للرسوم الأولية على العميل. إذا لم يتم تحديده، سيتم استخدام السعر المخزن للمنتج.
</ParamField>

**التنسيق**: مُعاد تمثيله بأدنى فئة من العملة (مثل: سنتات لدولار أمريكي). على سبيل المثال، لتحصيل $1.00، تمرير `100`.
</ParamField>

<ParamField body="product_currency" type="string">
عملة اختيارية من سعر المنتج. إذا لم يتم تحديده، يُستخدم العملة الافتراضية للمنتج.
</ParamField>

<ParamField body="product_description" type="string">
وصف المنتج الاختياري الذي يتم تجاوزه لفواتير وعناصر الخط. إذا لم يتم تحديده، سيتم استخدام الوصف المخزن للمنتج.
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
ما إذا كان يجب تضمين رسوم العملة التكيفية في سعر المنتج (صحيح) أو إضافتها فوق ذلك (خطأ). يتم تجاهله إذا لم يكن التسعير التكيفي مفعلاً.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## أمثلة الاستخدام

إليك 10 أمثلة شاملة تظهر تكوينات جلسة الخروج المختلفة لسيناريوهات الأعمال المتنوعة:

### 1. الخروج لمنتج مفرد بسيط

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. عربة متعددة المنتجات

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. الاشتراك مع فترة تجربة

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. الخروج المعتمد مسبقًا

<Note>
عندما يتم تعيين `confirm` إلى `true`، سيتم أخذ العميل مباشرة إلى صفحة الخروج، متجاوزًا أي خطوات تأكيد.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. الخروج مع تجاوز العملة

<Note>
تجاوز `billing_currency` ساري المفعول فقط عندما يكون التسعير التكيفي مفعلًا في إعدادات حسابك. إذا تم تعطيل التسعير التكيفي، فلن يكون لهذا المعامل أي تأثير.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. طرق الدفع المحفوظة للعملاء العائدين

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. الخروج بين الشركات مع جمع الرقم الضريبي

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. الخروج مع الثيم الداكن ورمز الخصم

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. طرق الدفع الإقليمية (UPI للهند)

للحصول على معلومات مفصلة حول تكوين UPI والاختبار، راجع صفحة <a href="/features/payment-methods/india">طرق الدفع في الهند</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. الخروج باستخدام BNPL (اشتر الآن وادفع لاحقًا)

للحصول على معلومات مفصلة حول تكوين BNPL والاختبار، راجع صفحة <a href="/features/payment-methods/bnpl">اشتر الآن وادفع لاحقًا (BNPL)</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. استخدام طرق الدفع الحالية للخروج الفوري

استخدم طريقة الدفع المحفوظة لدى العميل لإنشاء جلسة خروج تعالج على الفور، متجاوزة جمع طرق الدفع:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
عند استخدام `payment_method_id`، يجب تعيين `confirm` إلى `true` ويجب توفير `customer_id` موجود. سيتم التحقق من صحة طريقة الدفع للملاءمة مع عملة الدفع.
</Warning>

<Info>
يجب أن تنتمي طريقة الدفع إلى العميل وأن تكون متوافقة مع عملة الدفع. هذا ممكن عمليات الشراء بنقرة واحدة للعملاء العائدين.
</Info>

### 12. روابط مختصرة لروابط دفع أنظف

قم بإنشاء روابط دفع مختصرة وقابلة للمشاركة مع تجزئات مخصصة:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
الروابط القصيرة مثالية للرسائل النصية، البريد الإلكتروني، أو المشاركة عبر وسائل التواصل الاجتماعي. إنها أسهل في التذكر وتبني المزيد من الثقة لدى العملاء من الروابط الطويلة.
</Tip>

### 13. تخطي صفحة نجاح الدفع مع إعادة التوجيه الفورية

أعد توجيه العملاء على الفور بعد إكمال الدفع، متجاوزًا صفحة النجاح الافتراضية:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
استخدم `redirect_immediately: true` عندما يكون لديك صفحة نجاح مخصصة توفر تجربة مستخدم أفضل من صفحة نجاح الدفع الافتراضية. هذا مفيد بشكل خاص لتطبيقات الهاتف المحمول وتدفقات الخروج المدمجة.
</Tip>

<Note>
عند تمكين `redirect_immediately`، سيتم إعادة توجيه العملاء إلى `return_url` فور إكمال الدفع، متجاوزةً صفحة النجاح الافتراضية تمامًا.
</Note>

### 14. إجبار لغة معينة

إجبار نظام الخروج على العرض بلغة معينة، متجاوزًا اكتشاف لغة متصفح العميل:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
استخدم `force_language` عندما تعرف اللغة المفضلة للعميل (على سبيل المثال، من إعدادات حسابه) أو عند استهداف أسواق إقليمية معينة.
</Tip>

**اللغات المدعومة:** العربية (`ar`)، الكاتالونية (`ca`)، الصينية (`zh`)، الهولندية (`nl`)، الإنجليزية (`en`)، الفرنسية (`fr`)، الألمانية (`de`)، العبرية (`he`)، الإندونيسية (`id`)، الإيطالية (`it`)، اليابانية (`ja`)، الكورية (`ko`)، الماليزية (`ms`)، البولندية (`pl`)، البرتغالية (`pt`)، الرومانية (`ro`)، الروسية (`ru`)، الإسبانية (`es`)، السويدية (`sv`)، التايلاندية (`th`)، التركية (`tr`)

### 15. جمع الحقول المخصصة

اجمع معلومات إضافية من العملاء أثناء الخروج باستخدام الحقول المخصصة:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
تتضمن ردود الحقول المخصصة تلقائيًا في حمولات الويب هوك (`payment.succeeded`، `subscription.created`، إلخ) ويمكن استردادها عبر API. استخدمها لتوسيع CRM الخاص بك، وتحفيز تدفقات الانضمام، أو تخصيص تجربة العميل.
</Info>

**أنواع الحقول المتاحة:** `text`، `number`، `email`، `url`، `date`، `dropdown`، `boolean`

## معاينة جلسات الخروج

قبل إنشاء جلسة الخروج، يمكنك معاينة تفاصيل التسعير بما في ذلك الضرائب والخصومات والمجموعات. هذا مفيد لعرض التسعير الدقيق للعملاء قبل المتابعة للخروج.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

<Card title="معاينة مرجع واجهة برمجة التطبيقات" icon="code" href="/api-reference/checkout-sessions/preview">
  عرض توثيق نقطة النهاية المعاينة الكاملة.
</Card>

## الانتقال من الروابط الديناميكية إلى جلسات الخروج

### الاختلافات الرئيسية
سابقًا، عند إنشاء رابط دفع باستخدام الروابط الديناميكية، كان يُطلب منك تقديم العنوان الكامل للفوترة للعميل.

مع جلسات الخروج، لم يعد هذا ضروريًا. يمكنك ببساطة تمرير أي معلومات لديك، وسنتولى الباقي. على سبيل المثال:

- إذا كنت تعرف فقط بلد الفوترة للعميل، فقط قدم ذلك.
- ستقوم عملية الخروج تلقائيًا بجمع التفاصيل المفقودة قبل نقل العميل إلى صفحة الدفع.
- من ناحية أخرى، إذا كان لديك كل المعلومات المطلوبة وتريد تخطي مباشرة إلى صفحة الدفع، يمكنك تمرير مجموعة البيانات الكاملة وتضمين `confirm=true` في جسم طلبك.

### عملية الهجرة
الهجرة من الروابط الديناميكية إلى جلسات الخروج سهلة:

<Steps>
<Step title="تحديث تكامل">
قم بتحديث تكاملتك لاستخدام طريقة API أو SDK الجديدة.
</Step>

<Step title="ضبط حمولة الطلب">
قم بضبط حمولة الطلب وفقًا لتنسيق جلسات الخروج.
</Step>

<Step title="هذا كل شيء!"><br>نعم. لا توجد معالجة إضافية أو خطوات هجرة خاصة مطلوبة من جانبك.
</Step>
</Steps>

<Step title="That's it!">
Yes. No additional handling or special migration steps are needed on your side.
</Step>
</Steps>
