---
sidebarTitle: 随意支付
title: 随意支付定价
description: 让客户选择超过最低限度的价格。可选地显示建议价格。优化为一次性购买。
icon: dollar-sign
keywords:
  - Dodo Payments
  - 随意付费定价
  - 支付平台
  - SaaS 计费
---

**随意支付 (PWYW)** 让客户决定他们支付的金额——在商家定义的最低限度之上。它增加了参与度，鼓励社区支持，并为数字产品提供灵活的定价。

<Frame>
    <iframe 
        className="w-full aspect-video rounded-md" 
        src="https://www.youtube.com/embed/Lh0Q01b33NY"
        title="Pay What You Want Pricing | Dodo Payments"
        frameBorder="0"
        allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
        allowFullScreen
    ></iframe>
</Frame>

## 什么是随意支付？

PWYW 是一种定价模型，客户在您定义的保护范围内设定自己的价格。它非常适合：

- **数字产品和媒体**：模板、电子书、课程、插件
- **实验性发布**：提前访问、测试版发布、限时促销
- **捆绑和附加**：添加 PWYW 级别以提高参与度

## 主要好处

- **客户驱动的定价**：买家选择他们支付的金额
- **收入底线**：强制执行最低限度以保护利润
- **锚定期望**：可选的建议价格以指导选择
- **无缝结账**：托管结账和收据由您处理
- **开发者优先**：清晰的 API 用于创建、退款和 Webhook

<Warning>
PWYW 仅适用于 **单次付款**（一次性）产品。
</Warning>

## 创建随意支付

创建一个一次性产品并在您的 Dodo Payments 仪表板中启用 PWYW。以下部分直接映射到创建表单。

### 产品创建

#### 产品详情

- **产品名称**（必填）：在结账和发票中显示的名称
- **产品描述**（必填）：客户在支付前看到的清晰价值声明
- **产品图片**（必填）：PNG/JPG/WebP，最大 3 MB
- **品牌**：将产品与特定品牌关联以进行主题和电子邮件
- **税务类别**（必填）：选择正确的类别以确定税务规则

<Tip>
选择最准确的税务分类以确保按地区正确征税。
</Tip>

#### 定价

- **定价类型**：选择 <b>单次支付</b>
- **随意支付**（切换）：启用以让买家选择金额
- **最低价格**（必填）：客户可以支付的最低金额
- **建议价格**（可选）：在产品页面上显示的推荐金额

<Tip>
使用元数据存储系统中的标识符（例如 orderId），以便稍后对事件和收据进行核对。
</Tip>

#### 分享固定金额链接

当你从 **Products** 分享 PWYW 产品的 checkout 链接时，打开 **Advanced Settings** 并设置 **Payment amount**，即可收取该确切金额，而不是让买家自行选择。金额必须大于或等于产品的 Minimum Price，并会以主要货币单位通过链接中的 `paymentAmount` query parameter 传递（例如，`12.5` 表示 $12.50）。留空此字段可保留由买家选择金额的行为。

## API 管理

<AccordionGroup>
<Accordion title="Create checkout sessions (PWYW)">
使用 `POST /checkouts` 从产品创建一次性 checkout sessions。Checkout 会提示买家输入符合 PWYW 最低金额的金额。

<Card title="API Reference" icon="code" href="/api-reference/checkout-sessions/create">
查看 create checkout session API。
</Card>
</Accordion>

<Accordion title="Refund payments">
使用 `POST /refunds` 创建全额或部分退款。客户会收到其实际支付金额的精确退款。

<Card title="API Reference" icon="code" href="/api-reference/refunds/post-refunds">
了解如何对 payment 进行退款。
</Card>
</Accordion>

<Accordion title="List and retrieve">
使用 `GET /payments` 列出 payments，使用 `GET /payments/{payment_id}` 获取单个 payment。使用 `GET /payments/{payment_id}/line-items` 获取 line items。

<Card title="List Payments" icon="code" href="/api-reference/payments/get-payments">
浏览 listing API。
</Card>

<Card title="Get Payment Detail" icon="code" href="/api-reference/payments/get-payments-1">
查看 retrieval API。
</Card>

<Card title="Retrieve Line Items" icon="code" href="/api-reference/payments/retrieve-line-items">
检查 payment 中已购买的 items。
</Card>
</Accordion>

<Accordion title="Payment webhooks">
在后端响应 payment.succeeded 或 payment.failed 等 events。

<Card title="Webhook Guide" icon="code" href="/developer-resources/webhooks/intents/payment">
处理 payment 生命周期 events。
</Card>
</Accordion>
</AccordionGroup>

## 常见使用场景

- **数字资产**：主题、模板、图标集、媒体或插件
- **内容与教育**：电子书、课程、采用支持者定价的工作坊
- **社区与捐赠**：筹款活动、开源项目赞助、创作者支持
- **发布实验**：用于评估需求的 Beta 访问权限和限时活动

## 集成示例

### Checkout Sessions（单次 payment，PWYW）
将 PWYW 产品添加到购物车。Checkout 会收集买家输入的金额（遵循你设置的最低金额）——无需额外字段。

```typescript
const session = await client.checkoutSessions.create({
  product_cart: [
    { product_id: 'prod_pwyw_demo', quantity: 1 }
  ],
  return_url: 'https://yoursite.com/checkout/success'
});
```

## 最佳实践

- **选择合理的最低金额**：覆盖成本，同时降低参与门槛
- **使用建议价格**：锚定预期，以提高平均订单价值
- **清晰说明价值**：告知买家其贡献能够解锁的内容
- **进行 A/B 测试**：限制优惠时长，并试验不同的宣传信息
- **监控分布情况**：跟踪支付金额，以优化最低金额和建议价格

<Info>
PWYW 是一种灵活的方式，可以在保留收入下限的同时吸引更多客户。先从简单方案开始，进行充分测试，并根据转化率、贡献金额分布和退款率持续迭代。
</Info>
