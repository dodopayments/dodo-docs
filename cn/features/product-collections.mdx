---
sidebarTitle: 产品集合
title: 产品集合
description: 将相关产品组合在一起，以便在客户门户中提供统一的结账体验、计划选择和无缝的升级/降级路径。
icon: layer-group
keywords:
  - Dodo Payments
  - product collections
  - payment platform
  - SaaS billing
---

<Info>
产品集合让您可以将相关产品（例如，入门、专业、企业计划）组合在一个范畴内。显示所有选项于单一结账中，定义升级或降级路径，并让客户直接从客户门户灵活切换计划。
</Info>

<Frame>
  <img 
    src="/images/product-collection/checkout-page.png" 
    alt="产品集合结账页面的截图，显示多个产品" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

## 主要亮点

- **基于集合的结构**：将相关产品（计划、层级、价格选项）放入一个集合中进行有序管理。
- **一个集合，多个产品**：包括多个产品如入门、专业、终身等，每个产品都有自己的定价模式。
- **动态结账体验**：在一个结账视图中显示集合中的所有产品，让客户选择他们喜欢的计划。
- **商家级控制**：启用、禁用和重新排序集合中的产品。结账时第一个产品会自动预选。
- **生命周期意识**：通过客户门户，允许客户在同一集合中的产品之间升级或降级。

## 创建产品集合

产品集合可以从仪表板或通过API创建和管理。每个集合都作为相关产品的容器。

<Steps>
<Step title="Create the collection">
定义集合名称和可选描述。在结账中上传图像以直观地代表集合。

<Frame>
  <img 
    src="/images/product-collection/collection-form.png" 
    alt="产品集合创建表单的仪表板截图，显示名称、描述和图像上传的字段" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

**集合字段：**
- **名称**（必需）：集合的显示名称（例如，“SaaS计划”，“许可层级”）
- **描述**（可选）：在结账中显示的简要说明
- **图像**（可选）：集合的视觉品牌

</Step>

<Step title="Add products to the collection">
将现有产品添加到您的集合中。产品可以成组组织以获得更好的结构。

<Frame>
  <img 
    src="/images/product-collection/collection-form-products.png" 
    alt="产品集合产品页面截图，显示产品列表和将它们添加到集合的功能" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

**产品组织：**
- **组**：可选将产品组织到命名组中（例如，“月度计划”，“年度计划”）
- **未分组的产品**：没有组的产品显示在集合级别
- **排序**：拖放设置显示顺序

<Warning>
每个产品只能属于一个集合。如果一个产品已在其他集合中，您需要先将其移除。
</Warning>

</Step>

<Step title="Configure ordering and visibility">
控制集合中产品的显示顺序和可见性。

**配置选项：**
- **产品状态**：启用或禁用集合中的单个产品
- **显示顺序**：拖放设置产品在结账中出现的顺序

<Info>
集合中的第一个产品会自动作为默认选项在结账时预选。重新排序产品以更改默认选择。
</Info>

</Step>
</Steps>

## 集合结账

集合实现统一的结账体验，客户可以在一个地方查看并选择所有可用产品。

### 结账类型

| 类型 | 描述 | 用例 |
|------|-------------|----------|
| **集合结账** | 显示集合中的所有激活产品 | 订阅计划选择、分层定价 |
| **单产品结账** | 仅显示一个特定产品 | 直接购买、促销链接 |

### 集合结账体验

使用集合结账时：

1. **显示所有激活的产品**：客户可以看到集合中启用的每个产品
2. **第一个产品预选**：集合顺序中的第一个产品会自动被选中
3. **显示产品详情**：每个产品显示其名称、描述和定价
4. **单一选择**：客户选择一个产品进行购买
5. **继续标准流程**：选择后，结账将根据选定产品的定价和账单设置进行

<Frame>
  <img 
    src="/images/product-collection/checkout-page.png" 
    alt="产品集合结账页面的截图，显示多个产品" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

<Tip>
集合结账非常适合于订阅型企业，客户可以在购买前并排比较计划。
</Tip>

### API集成

为集合创建一个结账会话：

```typescript
const session = await client.checkoutSessions.create({
  product_collection_id: 'pdc_abc123',
  product_cart: [], // Required: pass an empty array for collection checkout
  return_url: 'https://yoursite.com/return'
});

// Redirect customer to the checkout
window.location.href = session.checkout_url;
```

<Warning>
使用`product_collection_id`时，不能在会话创建时应用折扣码。如果启用，客户仍可以在结账时输入折扣码。
</Warning>

## 客户门户集成

客户可以直接从客户门户在同一集合内的产品之间升级或降级。

<Tip>
**已有订阅产品？** 只需将它们添加到产品集合中以在客户门户中实现升级/降级流程。无需重新创建产品。
</Tip>

### 计划管理操作

| 操作 | 描述 | 商家控制 |
|--------|-------------|------------------|
| **查看当前计划** | 显示当前产品名称、价格和续订日期 | 始终可用 |
| **升级计划** | 移至同一集合中更高层级的产品 | 可配置（默认：允许） |
| **降级计划** | 移至同一集合中较低层级的产品 | 可配置（默认：允许） |
| **取消** | 完全取消订阅 | 始终可用 |

<Frame>
  <img 
    src="/images/product-collection/portal.png" 
    alt="产品集合客户门户计划更改界面的截图，显示计划管理操作" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

### 升级/降级规则

- 升级和降级仅在**同一集合内**的产品之间可用
- 根据您的订阅设置应用比例调整
- 每次升级、降级或取消时，都会向企业发送电子邮件通知

<Frame>
  <img 
    src="/images/product-collection/portal-change-plan.png" 
    alt="产品集合客户门户计划更改界面的截图，显示计划管理操作" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

<Info>
客户无法更改到当前集合外的产品。为不同的产品线创建单独的集合。
</Info>

## 订阅设置

通过仪表板中的**设置 → 订阅**配置您的企业的订阅和计划变更方式。

<Frame>
  <img 
    src="/images/product-collection/business-settings.png" 
    alt="订阅设置页面截图，显示允许多个订阅和允许订阅更新的切换" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

### 可用设置

| 设置 | 描述 | 默认值 |
|---------|-------------|---------|
| **允许多个订阅** | 客户可以同时持有多个有效订阅 | 已启用 |
| **允许更新订阅** | 客户可以随时通过 Customer Portal 升级或降级现有订阅 | 已禁用 |
| **允许取消订阅** | 客户可以从 Customer Portal 取消自己的订阅 | 已启用 |
| **防止试用滥用** | 已兑换过试用的客户将被降级为付费且无试用期的购买，而不是获得新的试用期 | 已禁用 |

<Info>
客户门户的计划更改默认是禁用的。在**设置 → 订阅**中启用“允许订阅更新”，让客户可以在同一集合中的产品之间升级或降级。
</Info>

<Info>
关闭“允许取消订阅”后，客户将无法看到自助取消选项，并且通过 Customer Portal API 发出的取消请求将被拒绝。您在商户端发起的取消操作不受影响。请参阅[取消订阅](/features/customer-portal#cancelling-a-subscription)。
</Info>

<Info>
有关试用兑换如何匹配和记录的详细信息，请参阅[防止试用滥用](/features/subscription#preventing-trial-misuse)。
</Info>

<Card title="Subscription Plan Changes" icon="repeat" href="/features/subscription#subscription-plan-changes">
  详细了解按比例分配模式和方案变更行为。
</Card>

## 管理集合

产品集合既可以通过 Dodo Payments 控制面板进行管理，也可以通过 API 以编程方式进行管理。API 提供对集合创建、更新、图片上传、归档，以及嵌套组和产品管理的完整控制。

### 控制面板操作

- **创建**：使用产品和组设置新的集合
- **更新**：修改名称、描述、图片和产品组织方式
- **重新排序**：拖放以更改产品显示顺序
- **启用/禁用产品**：控制哪些产品显示在结账页面中
- **归档**：隐藏集合而不永久删除它（之后可以取消归档）

<Frame>
  <img 
    src="/images/product-collection/collection-dashboard.png" 
    alt="Screenshot of the Product Collection dashboard showing the collection management operations" 
  style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

### API 管理

以下 endpoints 允许您通过编程方式创建、更新、获取、归档和组织产品集合，包括管理嵌套组及其中的产品。

<AccordionGroup>
<Accordion title="Listing Product Collections">
使用 `GET` request 获取与您账户关联的所有产品集合，调用 `/product-collections` endpoint。支持分页、按品牌筛选以及包含已归档的集合。

<Card title="List Product Collections API" icon="code" href="/api-reference/product-collections/list-product-collections">
  请在 List Product Collections API 文档中查看详细的 request 和 response 结构。
</Card>
</Accordion>

<Accordion title="Creating a Product Collection">
向 `/product-collections` endpoint 发送 `POST` request，并提供名称、描述和品牌等详细信息，以创建新的产品集合。

<Card title="Create Product Collection API" icon="code" href="/api-reference/product-collections/create-product-collection">
  请在 Create Product Collection API 文档中查看详细的 request 和 response 结构。
</Card>
</Accordion>

<Accordion title="Retrieving a Product Collection">
使用 `GET` request 调用 `/product-collections/{id}` endpoint，获取特定产品集合的详细信息，包括其中的组和产品项。

<Card title="Get Product Collection API" icon="code" href="/api-reference/product-collections/get-product-collection">
  请在 Get Product Collection API 文档中查看详细的 request 和 response 结构。
</Card>
</Accordion>

<Accordion title="Updating a Product Collection">
向 `/product-collections/{id}` endpoint 发送 `PATCH` request，以修改产品集合的详细信息（名称、描述、品牌等）。

<Card title="Update Product Collection API" icon="code" href="/api-reference/product-collections/update-product-collection">
  请在 Update Product Collection API 文档中查看详细的 request 和 response 结构。
</Card>
</Accordion>

<Accordion title="Uploading Collection Images">
通过预签名 URL 上传图片，将图片与集合关联。从 `/product-collections/{id}/images` endpoint 请求上传 URL，然后在 60 秒内将图片 `PUT` 到返回的 URL。

<Warning>
预签名 URL 会在 60 秒后过期，因此必须在该时间范围内上传图片。
</Warning>

<Card title="Update Collection Images API" icon="code" href="/api-reference/product-collections/update-product-collection-images">
  请在 Update Collection Images API 文档中查看详细的 request 和 response 结构。
</Card>
</Accordion>

<Accordion title="Archiving a Product Collection">
向 `/product-collections/{id}` endpoint 发送 `DELETE` request，以归档集合。此操作会隐藏集合，使其无法被新请求使用，但不会永久删除集合。

<Card title="Archive Product Collection API" icon="code" href="/api-reference/product-collections/archive-product-collection">
  请在 Archive Product Collection API 文档中查看详细的 request 和 response 结构。
</Card>
</Accordion>

<Accordion title="Unarchiving a Product Collection">
向 `/product-collections/{id}/unarchive` endpoint 发送 `POST` request，以恢复已归档的集合。

<Card title="Unarchive Product Collection API" icon="code" href="/api-reference/product-collections/unarchive-product-collection">
  请在 Unarchive Product Collection API 文档中查看详细的 request 和 response 结构。
</Card>
</Accordion>

<Accordion title="Managing Groups within a Collection">
组可以帮助您组织集合中的产品（例如“月度方案”和“年度方案”）。使用组 endpoints 在集合中添加、更新或删除组。

- **创建组**：`POST /product-collections/{id}/groups`
- **更新组**：`PATCH /product-collections/{id}/groups/{group_id}`
- **删除组**：`DELETE /product-collections/{id}/groups/{group_id}`

<CardGroup cols={3}>
<Card title="Create Group" icon="code" href="/api-reference/product-collections/create-group">
  向产品集合添加新组。
</Card>
<Card title="Update Group" icon="code" href="/api-reference/product-collections/update-group">
  修改组的名称或属性。
</Card>
<Card title="Delete Group" icon="code" href="/api-reference/product-collections/delete-group">
  从集合中删除组。
</Card>
</CardGroup>
</Accordion>

<Accordion title="Managing Products within a Group">
管理组中的独立产品项：添加新产品、更新现有项（例如显示顺序），或将其完全删除。

- **向组中添加产品**：`POST /product-collections/{id}/groups/{group_id}/items`
- **更新组项**：`PATCH /product-collections/{id}/groups/{group_id}/items/{item_id}`
- **删除组项**：`DELETE /product-collections/{id}/groups/{group_id}/items/{item_id}`

<CardGroup cols={3}>
<Card title="Add Products to Group" icon="code" href="/api-reference/product-collections/add-group-items">
  向集合中的组添加一个或多个产品。
</Card>
<Card title="Update Group Item" icon="code" href="/api-reference/product-collections/update-group-item">
  更新组中的产品项。
</Card>
<Card title="Delete Group Item" icon="code" href="/api-reference/product-collections/delete-group-item">
  从组中删除产品项。
</Card>
</CardGroup>
</Accordion>
</AccordionGroup>

## 最佳实践

- **合理分组**：按计费周期（月度/年度）或功能层级（入门版/专业版/企业版）组织产品
- **策略性排序**：将最受欢迎或推荐的方案放在首位，因为它会在结账时预先选中
- **使用清晰的命名**：产品名称应清楚传达价值差异
- **启用双向变更**：同时允许升级和降级，为客户提供灵活性
- **考虑按比例分配**：选择符合您业务模式的按比例分配模式
- **全面测试**：上线前在测试模式下验证结账和方案变更流程

<Check>
您已经准备好创建产品集合，为客户提供统一的方案选择体验。
</Check>

<CardGroup cols={2}>
<Card title="Products" icon="box" href="/features/products">
  创建一次性、订阅型或基于用量的产品，以添加到集合中。
</Card>

<Card title="Checkout" icon="cart-shopping" href="/features/checkout">
  在统一的结账体验中显示集合产品。
</Card>

<Card title="Customer Portal" icon="id-card" href="/features/customer-portal">
  允许客户在同一集合中升级或降级。
</Card>

<Card title="Subscriptions" icon="repeat" href="/features/subscription">
  通过按比例分配和方案变更管理周期性方案。
</Card>
</CardGroup>