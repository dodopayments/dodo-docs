---
sidebarTitle: 产品
title: 产品
description: 在几分钟内创建一次性、订阅或基于使用的产品。管理定价、媒体、字段和自动化权限 - 所有内容都在一个地方。
icon: box
---
<Info>
产品是 Dodo Payments 的基础。无论您是销售一次性下载、定期订阅还是基于使用的访问，您都可以通过创建产品开始。每个产品定义了其定价、在结账时的展示方式以及购买后的履行方式。
</Info>

<CardGroup cols={3}>
<Card title="一次性" icon="credit-card" href="/features/one-time-payment-products">
一次性收费以获得终身访问或单个交付物。
</Card>

<Card title="订阅" icon="repeat" href="/features/subscription">
按计划计费，支持试用、按比例计费和附加功能。
</Card>

<Card title="基于使用" icon="arrow-trend-up" href="/features/usage-based-billing">
计量消费并按实际使用计费。
</Card>
</CardGroup>

## 创建产品

您可以通过仪表板或 API 创建产品。提前选择定价模型：一次性、订阅或基于使用，然后配置详细信息。定价模型无法更改；如果需要不同的模型，请创建新产品。

<Steps>
<Step title="名称和描述">
提供清晰的标题和简明的以价值为导向的描述。描述中支持 Markdown 格式。

<Tip>
保持第一句话面向客户并以结果为导向；它在结账时显著显示。
</Tip>

<Frame>
  <img src="/images/products.png" alt="产品"/>
</Frame>

</Step>

<Step title="定价模型和价格">
选择定价模型：

- **一次性**：一次性支付的固定价格。
- **订阅**：按周期和可选试用的重复价格。
- **基于使用**：价格基于计量事件得出。

然后设置定价：

- **价格**：基本金额和货币。
- **折扣 (%)**：在结账和发票中显示的可选内联折扣。
- 对于订阅，设置 **每次重复**（例如，1 个月或 1 年）和 **试用天数**（如有需要）。

<Warning>
更改价格仅影响新购买。现有订阅遵循计划变更规则。
</Warning>
</Step>

<Step title="产品媒体">
上传图像以在结账和发票中展示产品。支持 PNG/JPG/WebP 格式，最大 3 MB。可以随时重新排序或替换。 
</Step>

<Step title="自动化权限（在高级设置下）">
附加在付款后自动激活的履行：

- **许可证密钥**：发行和验证唯一密钥
- **文件下载**：授予安全文件访问权限
- **自定义**：通过 Webhook 触发您自己的权限逻辑

随着您的产品演变，添加或删除权益。现有订阅者相应地获得或失去访问权限。
</Step>
</Steps>

## 变体和定价选项

与其在一个产品下创建变体，不如为每个定价选项创建单独的产品（例如，按月和按年）。您可以在同一结账中并排展示多个产品，并让客户在它们之间切换。

<Info>
这使得 API 可预测并简化报告。每个产品都有单一的定价模型。
</Info>

## 管理产品

您可以通过仪表板或通过 API 以编程方式管理产品。API 提供对产品创建、更新、检索、图像上传和归档的完全控制。

### 仪表板管理

- **更新**：随时编辑名称、描述、图像、价格、字段和权益（定价模型不可变）。
- **归档**：在不干扰现有客户的情况下，隐藏产品以防止新购买。您可以稍后取消归档。

### API 管理

以下说明允许您创建、更新、管理和检索产品，包括上传图像。

<AccordionGroup>
<Accordion title="创建产品">
产品可以是一次性商品或基于订阅的服务。要创建新产品，请向 `POST` 发送请求到 `/products` 端点，提供名称、描述、价格、货币以及是否为重复产品等详细信息。

对于重复产品，请设置 `recurring: true` 并指定一个 `billing_cycle` （`daily`、`weekly`、`monthly`、`yearly`）。

<Card title="创建产品 API" icon="code" href="/api-reference/products/post-products">
  查看创建产品 API 文档中的详细请求和响应结构。
</Card>
</Accordion>

<Accordion title="更新产品">
要修改现有产品，请向 `PATCH` 发送请求到 `/products/{product_id}` 端点。您可以更新名称、价格和描述等属性，同时保持其他详细信息不变。

确保端点中的 `product_id` 与现有产品匹配。

<AccordionGroup>
<Accordion title="创建产品">
产品可以是一次性商品或基于订阅的服务。要创建新产品，请向 `POST` 端点发送请求，提供名称、描述、价格、货币以及是否为定期产品等详细信息。

对于定期产品，请设置 `recurring: true`，并指定 `billing_cycle` (`daily`, `weekly`, `monthly`, `yearly`)。

<Card title="检索产品 API" icon="code" href="/api-reference/products/get-products">
  查看检索产品 API 文档中的详细请求和响应结构。
</Card>
</Accordion>

<Accordion title="更新产品">
要修改现有产品，请向 `PATCH` 端点发送请求。您可以更新名称、价格和描述等属性，同时保持其他细节不变。

确保端点中的 `product_id` 与现有产品匹配。

一旦从 API 收到预签名 URL，请使用 `PUT` 方法上传图像。这确保了对 AWS S3 的安全和临时访问，以便上传图像。

<Accordion title="获取产品">
您可以使用 `GET` 请求从您的账户中获取存储的产品列表，访问 `/products` 端点。这使您可以检索产品详细信息，包括活动和存档产品。

如果上传成功，AWS S3 将返回一个 `200 OK` 状态，指示图像已成功存储。

<Accordion title="上传产品图片">
您可以通过使用 API 提供的预签名 URL 将图片上传到 AWS S3 来为产品关联图片。首先，请求 `/products/{product_id}/images` 端点的图片上传 URL，然后在 60 秒内使用提供的 URL 上传图片。

<Accordion title="归档产品">
如果您不再希望显示或使用某个产品，可以通过向 `POST` 发送请求到 `/products/{product_id}/archive` 端点来归档它。此操作会隐藏产品，但不会永久删除它。

收到 API 的预签名 URL 后，使用 `PUT` 方法上传图片。这确保了安全和临时访问 AWS S3 以上传图片。

**支持的 S3 上传库：**
- **Node.js**: `axios`, `node-fetch`
- **Python**: `requests`, `boto3`
- **Go**: `net/http`
- **PHP**: `GuzzleHttp`
- **Ruby**: `rest-client`

如果上传成功，AWS S3 将返回 `200 OK` 状态，指示图片已成功存储。

<Accordion title="结账和履行">
从产品创建支付或订阅流程，并通过权益和 Webhook 自动履行。

<Accordion title="存档产品">
如果您不再希望显示或使用某个产品，可以通过向 `POST` 端点发送请求来将其存档。此操作将隐藏产品，但不会永久删除它。

## 最佳实践

<Accordion title="取消存档产品">
如果您需要恢复已存档的产品，请向 `POST` 端点发送请求。这将重新激活产品并使其再次可用。

<Check>
您已准备好创建产品并开始销售 - 一次性、定期或按使用。
</Check>

<Accordion title="结账与履行">
从产品创建支付或订阅流程，并通过优惠和网络钩子自动履行。

<CardGroup cols={3}>
<Card title="结账会话" icon="code" href="/developer-resources/checkout-session">
为一次性或订阅购买创建结账会话。
</Card>
<Card title="支付网络钩子" icon="code" href="/developer-resources/webhooks/intents/payment">
响应支付生命周期事件。
</Card>
<Card title="订阅网络钩子" icon="code" href="/developer-resources/webhooks/intents/subscription">
处理订阅创建、续订和取消事件。
</Card>
</CardGroup>
</Accordion>
</AccordionGroup>

## 最佳实践

- **从清晰开始**: 每个定价选项分开产品（按月与按年）
- **审慎使用试用**: 将试用与入门配对以推动激活
- **自动履行**: 使用优惠和网络钩子即时交付
- **使用元数据标记**: 存储您的系统 ID 以便对账

<Check>
您已准备好创建产品并开始销售——一次性、定期或按使用量计费。
</Check>

## 相关内容

<Card title="产品分析" icon="chart-mixed" href="/features/analytics-and-reporting#product-level-analytics">
跟踪每个独立产品的收入、客户、留存率、订阅者和 MRR。
</Card>
