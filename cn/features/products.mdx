---
sidebarTitle: 产品
title: 产品
description: 在几分钟内创建一次性、订阅或基于使用的产品。管理定价、媒体、字段和自动化权限 - 所有内容都在一个地方。
icon: box
---
<Info>
产品是 Dodo Payments 的基础。无论你出售一次性下载、循环订阅，还是按使用计费的访问，都始于创建产品。每个产品定义了其定价方式、在结账时的展示形式以及购买后的交付方式。
</Info>

<CardGroup cols={3}>
{/* LOCKED_PATTERN_04450aaec5b5e28e3ea5f6b217487cbc */}
一次收费即可提供终身访问或单次交付。
</Card>

{/* LOCKED_PATTERN_a10e92592ab9390be911120f2bcecbd0 */}
按照计划收费，可设置试用、按比例调整和附加服务。
</Card>

{/* LOCKED_PATTERN_f015805f3cf784f3e1ce2a9dc8180a35 */}
按实际使用量计费。
</Card>
</CardGroup>

## 创建产品

您可以通过仪表板或 API 创建产品。提前选择定价模型：一次性、订阅或基于使用，然后配置详细信息。定价模型无法更改；如果需要不同的模型，请创建新产品。

<Steps>
{/* LOCKED_PATTERN_650716067d22ce9b2fc10037a079cf41 */}
提供清晰的标题和简洁的以价值为导向的描述。描述支持 Markdown。


<Tip>
保持第一句面向客户并以成果为导向；它会在结账时突出显示。
</Tip>

<Frame>
  <img src="/images/products.png" alt="Products"/>
</Frame>

</Step>

{/* LOCKED_PATTERN_cbd9515b00efe13b7a75ec5b61fe0c6b */}
选择定价模型：

- **一次性**：一次性支付的固定价格。
- **订阅**：按周期和可选试用的重复价格。
- **基于使用**：价格基于计量事件得出。

然后设置定价：

- **价格**：基本金额和货币。
- **折扣 (%)**：在结账和发票中显示的可选内联折扣。
- 对于订阅，设置 **每次重复**（例如，1 个月或 1 年）和 **试用天数**（如有需要）。

<Warning>
更改价格仅影响新的购买。现有订阅会遵循计划变更规则。
</Warning>
</Step>

{/* LOCKED_PATTERN_7ec2f27e00061fa989770e1c689230ca */}
上传图片以在结账页和发票中展示产品。支持 PNG/JPG/WebP，大小最多 3 MB。可以随时重新排序或替换。
</Step>

{/* LOCKED_PATTERN_fb732d1baf26a73e83e826ecbb616b8f */}
附加在付款后自动激活的交付方式：

- **许可证密钥**：发行和验证唯一密钥
- **文件下载**：授予安全文件访问权限
- **自定义**：通过 Webhook 触发您自己的权限逻辑

随着产品优惠的演变，添加或移除权益。现有订阅者会相应获得或失去访问权限。
</Step>
</Steps>

## 变体和定价选项

与其在一个产品下创建变体，不如为每个定价选项创建单独的产品（例如，按月和按年）。您可以在同一结账中并排展示多个产品，并让客户在它们之间切换。

<Frame>
  <img src="/images/product-collection/checkout-page.png" alt="Product Collections"/>
</Frame>

## 管理产品

您可以通过仪表板或通过 API 以编程方式管理产品。API 提供对产品创建、更新、检索、图像上传和归档的完全控制。

### 仪表板管理

- **更新**：随时编辑名称、描述、图像、价格、字段和权益（定价模型不可变）。
- **归档**：在不干扰现有客户的情况下，隐藏产品以防止新购买。您可以稍后取消归档。

{/* LOCKED_PATTERN_cbe0de1faffb3a1f552c6ce10c001527 */}
  将相关产品组合在一起，打造统一的结账体验和无缝升级/降级路径。
</Card>

以下说明允许您创建、更新、管理和检索产品，包括上传图像。

<AccordionGroup>
<Accordion title="创建产品">
产品可以是一次性商品或基于订阅的服务。要创建新产品，请向 `POST` 发送请求到 `/products` 端点，提供名称、描述、价格、货币以及是否为重复产品等详细信息。

对于重复产品，请设置 `recurring: true` 并指定一个 `billing_cycle` （`daily`、`weekly`、`monthly`、`yearly`）。

<Card title="创建产品 API" icon="code" href="/api-reference/products/post-products">
  查看创建产品 API 文档中的详细请求和响应结构。
</Card>
</Accordion>

<Accordion title="更新产品">
要修改现有产品，请向 `PATCH` 发送请求到 `/products/{product_id}` 端点。您可以更新名称、价格和描述等属性，同时保持其他详细信息不变。

确保端点中的 `product_id` 与现有产品匹配。

<AccordionGroup>
{/* LOCKED_PATTERN_eddf4dc624fa14b64d50b00a7b62a243 */}
产品可以是一次性项目或基于订阅的服务。要创建新产品，请向 `POST` 端点发送请求至 `/products`，并提供名称、描述、价格、货币以及是否为循环产品等详细信息。

对于循环产品，请设置 `recurring: true`，并指定 `billing_cycle`（`daily`、`weekly`、`monthly`、`yearly`）。

{/* LOCKED_PATTERN_928df4172f546004633c66ee1394fc66 */}
  查看创建产品 API 文档中的详细请求与响应结构。
</Card>
</Accordion>

{/* LOCKED_PATTERN_5b0241a7e30101c362a1fa23c61ba5c9 */}
要修改现有产品，请向 `PATCH` 端点发送请求至 `/products/{product_id}`。你可以更新名称、价格和描述等属性，同时保持其他信息不变。

确保端点中的 `product_id` 与现有产品匹配。

{/* LOCKED_PATTERN_547a8a1f64e1600671c88ac42f733bf5 */}
  查看更新产品 API 文档中的详细请求与响应结构。
</Card>
</Accordion>

{/* LOCKED_PATTERN_bb5bf0f0b68e143d62ace39a4c2918f1 */}
你可以通过向 `GET` 端点发送请求来获取账户中存储的产品列表。这可以检索产品详情，包括活跃和已归档的产品。

{/* LOCKED_PATTERN_49a116775840cfaf0a655cdd67c21416 */}
  查看检索产品 API 文档中的详细请求与响应结构。
</Card>
</Accordion>

{/* LOCKED_PATTERN_bb5d7818f6659dbe681096a03760fcba */}
你可以通过使用 API 提供的预签名 URL 将图像上传到 AWS S3，并将其与产品关联。首先，从 `/products/{product_id}/images` 端点请求图像上传 URL，然后在 60 秒内使用提供的 URL 上传图像。

<Warning>
预签名 URL 在 60 秒内过期，因此必须在此时间范围内完成图像上传。
</Warning>

一旦从 API 收到预签名 URL，请使用 `PUT` 方法上传图像。这可确保安全且临时访问 AWS S3 以完成图像上传。

**上传到 S3 支持的库：**
- **Node.js**: `axios`, `node-fetch`
- **Python**: `requests`, `boto3`
- **Go**: `net/http`
- **PHP**: `GuzzleHttp`
- **Ruby**: `rest-client`

如果上传成功，AWS S3 将返回 `200 OK` 状态，表示图像已成功存储。

{/* LOCKED_PATTERN_0e8c82409b28128d958fc51adde377a8 */}
  查看上传产品图像 API 文档中的详细请求与响应结构。
</Card>
</Accordion>

{/* LOCKED_PATTERN_e39596eca26d22865eed77998ed1d61c */}
如果你不再希望显示或使用某个产品，可以向 `/products/{product_id}/archive` 端点发送 `POST` 请求将其归档。此操作会隐藏该产品但不会永久删除。

{/* LOCKED_PATTERN_a26cda50522e246415d276751f0deefc */}
  查看归档产品 API 文档中的详细请求与响应结构。
</Card>
</Accordion>

{/* LOCKED_PATTERN_6ae04208a8e6089a2c7fee7a325fb767 */}
如果需要恢复已归档的产品，请向 `/products/{product_id}/unarchive` 端点发送 `POST` 请求。此操作将重新激活产品并再次提供使用。

{/* LOCKED_PATTERN_520afa32d628c019db87906c43ef7329 */}
  查看取消归档产品 API 文档中的详细请求与响应结构。
</Card>
</Accordion>

{/* LOCKED_PATTERN_aab9a26219cd5af86f705eebd197c660 */}
从产品创建支付或订阅流程，并通过权益与 webhook 自动完成交付。

<CardGroup cols={3}>
{/* LOCKED_PATTERN_a14bbfc83f06c6192c975a83aea4cf58 */}
为一次性或订阅购买创建结账会话。
</Card>
{/* LOCKED_PATTERN_2db6b28a276f50d96d7a24d1ea5ebeb2 */}
响应支付生命周期事件。
</Card>
{/* LOCKED_PATTERN_1cd9a7ac4415843b5e77e9a9493bae92 */}
处理订阅创建、续订和取消事件。
</Card>
</CardGroup>
</Accordion>
</AccordionGroup>

## Best practices

- **从明确性出发**：为每种定价选项（按月与按年）分别创建产品
- **慎用试用**：将试用与入门指导配对以促进激活
- **自动化交付**：使用权益与 webhook 实现即时交付
- **使用元数据标记**：保存系统 ID 以便对账

<Check>
你已经准备好创建产品并开始销售——一次性、循环或按使用量计费。
</Check>

## Related

{/* LOCKED_PATTERN_c64545fe4f0f546d254eef3dd3d1defe */}
跟踪每个单独产品的收入、客户、留存、订阅者和经常性月收入（MRR）。
</Card>
