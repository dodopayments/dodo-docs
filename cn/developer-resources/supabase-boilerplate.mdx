---
sidebarTitle: Supabase
title: Supabase 模板
description: 使用官方的 Next.js + Supabase + Dodo Payments
  订阅入门套件快速发布订阅。包含身份验证、数据库架构、Webhook 处理程序和定价 UI。
icon: /images/logos/supabase.svg
keywords:
  - Dodo Payments
  - Supabase
  - Supabase integration
  - starter template
  - quick start
  - supabase boilerplate
---

<CardGroup cols={2}>
<Card title="GitHub Repository" icon="github" href="https://github.com/dodopayments/dodo-supabase-subscription-starter">
    Minimal Next.js + Supabase + Dodo Payments subscription boilerplate
</Card>
<Card title="Live Demo" icon="rocket" href="https://dodopayments-supabase-nextjs.vercel.app">
    Explore the deployed demo
</Card>
</CardGroup>

## 概述

一个生产就绪的订阅模板，使用 Next.js 15、React 19、Supabase、Drizzle ORM 和 Dodo Payments。它包括 Google OAuth、订阅结算、Webhook 处理、数据库架构和一个基础仪表盘。

<Info>
如果您只需要现有应用程序的路由处理程序，请参阅专用适配器：<a href="/developer-resources/nextjs-adaptor">Next.js 适配器</a> 和 <a href="/developer-resources/express-adaptor">Express 适配器</a>。
</Info>

## 先决条件

- Node.js 18+（或 Bun 1.0+）
- Supabase 项目（URL、匿名密钥、服务角色密钥、数据库 URL）
- Dodo Payments 账户（API 密钥，Webhook 密钥）
- Google Cloud OAuth 客户端（客户端 ID 和密钥）

## 快速入门

<Steps>
<Step title="Clone and install">

```bash
git clone https://github.com/dodopayments/dodo-supabase-subscription-starter.git
cd dodo-supabase-subscription-starter
# choose one
bun install
# or
npm install
# or
pnpm install
```

</Step>

<Step title="Create Supabase project">
创建一个 Supabase 项目并复制：
- NEXT_PUBLIC_SUPABASE_URL
- NEXT_PUBLIC_SUPABASE_ANON_KEY
- SUPABASE_SERVICE_ROLE_KEY
- DATABASE_URL（连接字符串）
</Step>

<Step title="Configure Google OAuth">
在 Google Cloud 中设置重定向 URI 为：`https://[your-project-ref].supabase.co/auth/v1/callback`，然后使用您的客户端 ID 和密钥在 Supabase Auth 中启用 Google 提供商。
</Step>

<Step title="Configure Dodo Payments">
从 Dodo 仪表板生成一个 API 密钥和 Webhook 密钥。在开发过程中，将环境设置为 `test_mode`。
</Step>

<Step title="Create .env.local">

```env
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role

# Database
DATABASE_URL=postgresql://postgres:[password]@db.[project-ref].supabase.co:5432/postgres

# Dodo Payments
DODO_PAYMENTS_API_KEY=your-dodo-api-key
DODO_WEBHOOK_SECRET=your-webhook-secret
DODO_PAYMENTS_ENVIRONMENT=test_mode
```

<Warning>
永远不要提交密钥。在部署环境中使用环境变量。
</Warning>
</Step>

<Step title="Provision database schema">

```bash
bun run db:push
# or
npm run db:push
# or
pnpm run db:push
```

<Check>
创建的表格：`users`, `subscriptions`, `payments`。
</Check>
</Step>

<Step title="Deploy webhook function">

```bash
# login (one-time)
bunx supabase login
# or
npx supabase login

# deploy the edge function
bun run deploy:webhook --project-ref [your-project-ref]
# or
npm run deploy:webhook -- --project-ref [your-project-ref]
# or
pnpm run deploy:webhook --project-ref [your-project-ref]
```

```bash cURL
curl -X POST \
  'https://[your-project-ref].supabase.co/functions/v1/dodo-webhook' \
  -H 'Content-Type: application/json' \
  -H 'Dodo-Signature: <signature>' \
  -d '{"type":"payment.succeeded","data":{}}'
```

</Step>

<Step title="Add webhook in Dodo Payments">
设置端点 URL 为：

```text
https://[your-project-ref].supabase.co/functions/v1/dodo-webhook
```

选择付款和订阅事件。
</Step>

<Step title="Create products and features">
在 Dodo 仪表板 → 产品 → 创建产品。可选择添加元数据：

```json
{
  "features": ["Feature 1", "Feature 2", "Feature 3"]
}
```

定价 UI 读取这个 `features` 数组并动态渲染。
</Step>

<Step title="Run the dev server">

```bash
bun run dev
# or
npm run dev
# or
pnpm run dev
```

打开 [http://localhost:3000](http://localhost:3000)。
</Step>
</Steps>

## 包含内容

- 通过 Supabase 进行身份验证（配置了 Google OAuth）
- 通过 Dodo Payments 进行订阅结算
- 用于 Webhook 的 Supabase 边缘函数（`dodo-webhook`）
- Drizzle ORM 架构和迁移
- 包含发票、订阅状态和计划功能的仪表盘

<Tip>
保留 `DODO_PAYMENTS_ENVIRONMENT` 为 `test_mode`，直到您完成端到端测试。
</Tip>

## 关键文件和路径

<Tabs>
<Tab title="Edge Function">

```text
supabase/functions/dodo-webhook/
  index.ts            # webhook handler verifying signatures
  deno.json           # permissions
```

</Tab>
<Tab title="Next.js routes">

```text
app/(marketing)/*     # landing + pricing UI
app/(dashboard)/*     # protected pages
app/api/*             # server actions & helpers
```

</Tab>
<Tab title="Database (Drizzle)">

```text
lib/db/schema.ts      # users, subscriptions, payments
lib/db/index.ts       # client
```

</Tab>
</Tabs>

## 环境变量

<AccordionGroup>
<Accordion title="Supabase">

```env
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
DATABASE_URL=
```

</Accordion>
<Accordion title="Dodo Payments">

```env
DODO_PAYMENTS_API_KEY=
DODO_WEBHOOK_SECRET=
DODO_PAYMENTS_ENVIRONMENT=test_mode|live_mode
```

</Accordion>
<Accordion title="Google OAuth">

```env
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
```

</Accordion>
</AccordionGroup>

## 验证和故障排除

<AccordionGroup>
<Accordion title="Webhook signature invalid (401)">
- 确保 `DODO_WEBHOOK_SECRET` 匹配 Dodo 仪表板中的值
- 确认您部署了最新的 `dodo-webhook` 函数
- 验证函数中的标题名称是否正确（Dodo-Signature）
</Accordion>
<Accordion title="Database push fails">
- 检查 `DATABASE_URL` 的语法和 Supabase 网络出口
- 项目创建后等待约 2-3 分钟再进行首次推送
</Accordion>
<Accordion title="OAuth redirect mismatch">
- 重定向 URI 必须是 `https://[ref].supabase.co/auth/v1/callback`
- 确保 Google Cloud 和 Supabase Auth 提供商中的相同设置
</Accordion>
</AccordionGroup>

<Check>
您现在有一个工作正常的订阅 SaaS，使用 Supabase 和 Dodo Payments 搭建。
</Check>

<Info>
原始仓库和详细步骤：<a href="https://github.com/dodopayments/dodo-supabase-subscription-starter">dodo-supabase-subscription-starter</a>。
</Info>
