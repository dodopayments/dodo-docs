---
title: 结账会话
description: 创建安全的托管结账体验，处理一次性购买和订阅的完整支付流程，并提供完全的自定义控制。
icon: cart-shopping
---

<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  在 5 分钟内启动首个结账会话
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  探索完整的 API 文档，并交互式测试结账会话请求和响应。
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  在创建会话之前计算价格、税费和总额。
</Card>
</CardGroup>

<Info>
**会话有效性**：默认情况下结账会话有效期为 24 小时。如果在请求中传递 `confirm=true`，则该会话仅在 15 分钟内有效。
</Info>

## 前提条件

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  您需要一个具有 API 访问权限的 Dodo Payments 有效商家账户。
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  从 Dodo Payments 仪表板生成 API 凭证：
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  在实施结账会话前，请先在 Dodo Payments 仪表板中创建产品。
</Step>
</Steps>

## 创建您的第一个 Checkout 会话

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### API 响应

上述所有方法返回相同的响应结构：

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
从 API 响应中提取 `checkout_url`。
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
将客户引导至结账 URL 完成购买。

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**替代集成选项**：您可以使用 [Overlay Checkout](/developer-resources/overlay-checkout)（模态覆盖层）或 [Inline Checkout](/developer-resources/inline-checkout)（完全嵌入）将结账直接嵌入页面。这两种选项都使用相同的结账会话 URL。
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
支付完成后，客户会被重定向到您的 `return_url`，并附加查询参数，包括支付/订阅 ID、状态、客户电子邮件以及任何许可密钥。完整列表请参阅 [`return_url` 参数文档](#request-body)。
</Step>
</Steps>

## 请求体

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  每个结账会话所需的基本字段
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  自定义结账体验的附加配置
</Card>
</CardGroup>

### 必填字段

<ParamField body="product_cart" type="array" required>
结账会话中包含的产品数组。每个产品都必须拥有来自 Dodo Payments 仪表板的有效 `product_id`。


<Tip>
**混合结账**：您可以在同一结账会话中组合一次性支付产品和订阅产品。这支持诸如附带订阅的设置费用、硬件套餐与 SaaS 的组合等高级用例。
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
来自 Dodo Payments 仪表板的产品唯一标识符。


**示例**：`"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
产品数量。


**示例**：单个商品请传 `1`，多个数量请传 `3`
</ParamField>

<ParamField body="addons" type="array">
附加到产品的附加组件数组。仅在产品为订阅时有效。


{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
启用 pay_what_you_want 时客户支付的金额。如果未启用，该字段将被忽略。


**格式**：以货币的最低单位表示（例如 USD 以美分为单位）。例如，收取 1.00 美元请传 `100`。
</ParamField>
</Expandable>
</ParamField>

<Tip>
**查找您的产品 ID**：您可以在 Dodo Payments 仪表板的产品 → 查看详情中找到产品 ID，或使用 [List Products API](/api-reference/products/get-products)。
</Tip>

### 可选字段

配置这些字段以自定义结账体验并为您的支付流程添加业务逻辑。

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
客户信息。您可以使用其 ID 将现有客户附加或在结账过程中创建新的客户记录。


<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}
使用客户 ID 将现有客户附加到结账会话。


{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
现有客户的唯一标识符。使用此字段将结账会话附加到现有客户，而不是创建新客户。
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
在结账过程中创建新的客户记录。


{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
客户的电子邮件地址。在创建新客户时必填。
</ParamField>

<ParamField body="name" type="string">
客户的全名，在收据和发票上显示时所用。


**示例**：`"John Doe"` 或 `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
客户的电话号码（国际格式）。某些支付方式和反欺诈流程需要此项。


**格式**：包含国家代码，例如美国号码请用 `"+1234567890"`
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
用于精确税务计算、反欺诈和合规的账单地址信息。


<Info>
当 `confirm` 设置为 `true` 时，所有账单地址字段在成功创建会话前都变为必填。
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
包含门牌号、街道名称以及（如适用）公寓/单元号的完整街道地址。


**示例**：`"123 Main St, Apt 4B"` 或 `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
城市或市镇名称。


**示例**：`"San Francisco"`、`"New York"`、`"London"`
</ParamField>

<ParamField body="state" type="string">
州、省或地区名称。请使用全称或标准缩写。


**示例**：`"California"` 或 `"CA"`，`"Ontario"` 或 `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
两字母 ISO 国家代码（ISO 3166-1 alpha-2）。只要提供账单地址，此字段始终必填。


**示例**：`"US"`（美国）、`"CA"`（加拿大）、`"GB"`（英国）、`"DE"`（德国）

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  查看受支持国家及其 ISO 代码的完整列表
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
邮政编码、邮编或根据国家要求的等效项。


**示例**：`"94102"`（美国）、`"M5V 3A8"`（加拿大）、`"SW1A 1AA"`（英国）
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
控制结账时可供客户使用的支付方式，帮助您针对特定市场或业务需求进行优化。


**可用选项**：`credit`、`debit`、`upi_collect`、`apple_pay`、`google_pay`、`amazon_pay`、`klarna`、`affirm`、`afterpay_clearpay`、`cashapp`、`multibanco`、`bancontact_card`、`eps`、`ideal`、`przelewy24`、`paypal`

<Warning>
**重要**：始终将 `credit` 和 `debit` 包含为回退选项，以避免在首选支付方式不可用时发生结账失败。
</Warning>

**示例**： 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
使用固定账单货币覆盖默认货币选择。使用 ISO 4217 货币代码。


**支持的货币**：`USD`、`EUR`、`GBP`、`CAD`、`AUD`、`INR` 等

**示例**：使用 `"USD"` 表示美元，使用 `"EUR"` 表示欧元

<Note>
只有在启用自适应定价时，该字段才会生效。如果未启用自适应定价，则会使用产品的默认货币。
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
为回头客显示此前保存的支付方式，从而提升结账速度和用户体验。
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
支付完成后将客户重定向到指定 URL。Dodo Payments 在重定向时会在您的 URL 后附加以下查询参数：

| 参数 | 类型 | 条件 |
|-----------|------|-----------|
| `payment_id` | `string` | 一次性支付时始终存在 |
| `subscription_id` | `string` | 订阅支付时始终存在 |
| `status` | `string` | 始终存在 |
| `license_key` | `string` | 如果产品启用许可密钥，则会包含。多个密钥时以逗号分隔 |
| `email` | `string` | 如果客户记录有电子邮件，则会包含 |

**示例重定向 URL：**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
在返回页面上利用 `license_key` 和 `email` 查询参数来显示许可密钥或立即发送确认，而无需额外的 API 调用。
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
如果为 true，则立即确认所有会话详情。如果缺少必填数据，API 会返回错误。
</ParamField>

<ParamField body="discount_code" type="string">
为结账会话应用折扣码。
</ParamField>

<ParamField body="metadata" type="object">
用于存储关于会话的自定义键值对信息。
</ParamField>

<ParamField body="force_3ds" type="boolean">
为此会话覆盖商户默认的 3DS 行为。
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
启用最少地址收集模式。启用后，结账仅收集：
- **国家**：始终需要用于税务判断
- **邮编/邮政编码**：仅在需计算销售税、增值税或商品服务税的地区才收集


这可以通过消除不必要的表单字段显著降低结账摩擦。


<Tip>
启用最少地址以加快结账完成。需要完整账单信息的企业仍可选择收集完整地址。
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
自定义结账界面的外观和行为。


{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
结账界面的主题。可选项为 `light`、`dark` 或 `system`。
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
在结账界面显示订单详情部分。


<ParamField body="show_on_demand_tag" type="boolean" default="true">
为适用产品显示 “按需” 标签。


<ParamField body="force_language" type="string">
强制结账界面以特定语言呈现。默认情况下，结账会自动检测客户浏览器语言。


**支持的语言代码：**
`ar`（阿拉伯语）、`ca`（加泰罗尼亚语）、`de`（德语）、`en`（英语）、`es`（西班牙语）、`fr`（法语）、`he`（希伯来语）、`id`（印尼语）、`it`（意大利语）、`ja`（日语）、`ko`（韩语）、`ms`（马来语）、`nl`（荷兰语）、`pl`（波兰语）、`pt`（葡萄牙语）、`ro`（罗马尼亚语）、`ru`（俄语）、`sv`（瑞典语）、`th`（泰语）、`tr`（土耳其语）、`zh`（中文）

**示例**：西班牙语请传 `"es"`，日语请传 `"ja"`
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
为结账会话配置特定功能和行为。


{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
允许客户在结账时选择其偏好的货币。
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
在结账界面显示折扣码输入字段。
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
在结账时收集客户电话号码。
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
允许客户输入税号。
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
强制创建新客户记录，而不是更新现有客户。
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
允许客户在结账过程中编辑其电子邮件地址。
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
允许客户在结账过程中编辑其姓名。
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
允许客户在结账过程中编辑街道地址。
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
允许客户在结账过程中编辑城市。
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
允许客户在结账过程中编辑州/省。
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
允许客户在结账过程中编辑国家。
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
允许客户在结账过程中编辑邮政编码。
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
启用最少地址收集模式。启用后，结账仅收集：
- **国家**：用于税务判断时始终需要
- **邮编/邮政编码**：仅在需计算销售税、增值税或商品服务税的地区才收集


这可以通过消除不必要的表单字段显著降低结账摩擦。


<Tip>
启用最少地址以加快结账完成。需要完整账单信息的企业仍可选择收集完整地址。
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
跳过默认付款成功页面，在支付完成后立即将客户重定向到您的 `return_url`。


<Tip>
当您拥有提供更好用户体验的自定义成功页面或在移动应用及嵌入式结账流程中使用时，请采用此选项。
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
在结账过程中通过自定义表单字段从客户收集额外信息。每个结账会话可定义最多 5 个自定义字段。客户填写的内容会包含在 webhook 有效负载中，并可通过 API 获取。


{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
该字段的唯一标识符。在 webhook 有效负载和 API 响应中用作键。


**示例**：`"company_name"`、`"referral_source"`、`"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
展示给客户的表单标签。


**示例**：`"Company Name"`、`"How did you hear about us?"`、`"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
字段类型决定输入验证规则。


**可用类型**：
- `text` - 单行文本输入
- `number` - 数值输入
- `email` - 带验证的电子邮件地址
- `url` - 带验证的 URL
- `date` - 日期选择器
- `dropdown` - 从预定义选项中选择
- `boolean` - 是/否 切换
</ParamField>

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
指示此字段在结账完成前是否必须填写。
</ParamField>

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
输入字段中显示的占位符文本。
</ParamField>

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
针对 `dropdown` 类型字段的选项列表。当 `field_type` 为 `dropdown` 时必填，其他类型忽略。


**示例**：`["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
客户对自定义字段的响应会包含在：
- **Webhooks**：`payment.succeeded`、`subscription.created` 以及其他相关事件的有效负载都包含 `custom_field_responses` 数组
- **API 响应**：支付和订阅对象中包含 `custom_field_responses`
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
包含订阅产品的结账会话的附加配置。


{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
试用期天数（首次收费前）。必须介于 0 到 10000 天之间。
</ParamField>

<ParamField body="on_demand" type="object">
用于基于使用量或计量计费的按需订阅配置。


{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
如果设置为 true，则仅授权支付方式以备将来使用，不执行任何扣款。
</ParamField>

<ParamField body="product_price" type="integer">
首次向客户收取的产品价格金额。如果未指定，将使用存储的产品价格。


**格式**：以货币的最低单位表示（例如 USD 以美分为单位）。例如，收取 1.00 美元请传 `100`。
</ParamField>

<ParamField body="product_currency" type="string">
产品价格的可选货币。如果未指定，则默认使用产品的货币。
</ParamField>

<ParamField body="product_description" type="string">
用于账单和明细行项目的可选产品描述覆盖。如果未指定，则使用产品存储的描述。
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
指示应将自适应货币费用包含在产品价格内（true）还是额外加收（false）。如果未启用自适应定价，则忽略此项。
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## 使用示例

以下提供 10 个涵盖不同业务场景的完整结账会话配置示例：

### 1. 简单单品结账

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. 多产品购物车

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. 带试用期的订阅

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. 预确认结账

<Note>
当 `confirm` 设置为 `true` 时，客户将直接进入结账页面，跳过任何确认步骤。
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. 带货币覆盖的结账

<Note>
`billing_currency` 覆盖仅在帐户设置中启用自适应货币时生效。如果关闭，则该参数不会起作用。
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. 为回头客保留支付方式

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. 面向企业的结账并收集税号

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. 暗色主题结账与折扣码

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. 区域性支付方式（印度 UPI）

有关 UPI 配置和测试的详细信息，请查看 <a href="/features/payment-methods/india">India Payment Methods</a> 页面。

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. 先买后付（BNPL）结账

有关 BNPL 配置和测试的详细信息，请查看 <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a> 页面。

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. 使用现有支付方式实现即时结账

使用客户保存的支付方式创建可立即处理的结账会话，从而跳过支付方式收集：

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
使用 `payment_method_id` 时，必须将 `confirm` 设置为 `true`，并提供一个现有的 `customer_id`。支付方式将根据支付货币验证其资格。
</Warning>

<Info>
支付方式必须归属于该客户并与支付货币兼容。这使得回头客可实现一键购买。
</Info>

### 12. 为更简洁的支付 URL 使用短链接

生成带自定义别名的可共享短链接：

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
短链接非常适合 SMS、电子邮件或社交媒体分享。比起长 URL 更易记忆，能增强客户信任。
</Tip>

### 13. 跳过支付成功页并立即重定向

支付完成后立即将客户重定向，绕过默认成功页面：

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
当您拥有比默认支付成功页更佳用户体验的自定义成功页时，请使用 `redirect_immediately: true`。该选项在移动应用和嵌入式结账流程中特别有用。
</Tip>

<Note>
启用 `redirect_immediately` 时，客户在支付完成后会立即被重定向到您的 `return_url`，完全跳过默认成功页面。
</Note>

### 14. 强制指定语言

强制结账以特定语言显示，覆盖客户浏览器语言检测：

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
当您知道客户偏好的语言（例如来自其账户设置）或针对特定地区市场时，请使用 `force_language`。
</Tip>

**支持的语言**：阿拉伯语（`ar`）、加泰罗尼亚语（`ca`）、中文（`zh`）、荷兰语（`nl`）、英语（`en`）、法语（`fr`）、德语（`de`）、希伯来语（`he`）、印尼语（`id`）、意大利语（`it`）、日语（`ja`）、韩语（`ko`）、马来语（`ms`）、波兰语（`pl`）、葡萄牙语（`pt`）、罗马尼亚语（`ro`）、俄语（`ru`）、西班牙语（`es`）、瑞典语（`sv`）、泰语（`th`）、土耳其语（`tr`）

### 15. 收集自定义字段

在结账过程中通过自定义字段收集客户额外信息：

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
自定义字段的回答会自动包含在 webhook 有效负载（`payment.succeeded`、`subscription.created` 等）中，也可通过 API 检索。可用于丰富 CRM、触发入职流程或定制客户体验。
</Info>

**可用字段类型**：`text`、`number`、`email`、`url`、`date`、`dropdown`、`boolean`

## 预览结账会话

在创建结账会话之前，您可以预览包括税费、折扣和总额在内的价格明细。这有助于在客户进入结账前显示准确的价格。

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  查看完整的预览端点文档。
</Card>

## 从动态链接迁移到结账会话

### 关键差异
以前在创建动态链接支付链接时，您需要提供客户的完整账单地址。


使用结账会话后，这不再是必需的。您只需传递已有的信息，其余部分我们来处理。例如：

- 如果您只知道客户的账单国家，请仅提供该值。
- 结账流程将在将客户引导至支付页面前自动收集缺失的信息。
- 另一方面，如果您已掌握所有必填信息并希望直接跳转至支付页面，请传递完整数据并在请求正文中包含 `confirm=true`。


### 迁移流程

从动态链接迁移到结账会话非常简单：

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
将您的集成更新为使用新的 API 或 SDK 方法。
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
根据结账会话格式调整请求负载。
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
是的。您无需额外处理或特殊迁移步骤。
</Step>
</Steps>
