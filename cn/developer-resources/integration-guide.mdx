---
title: 一次性支付集成指南
description: 本指南将帮助您将 Dodo Payments API 集成到您的网站中。
icon: code-merge
---

## 前提条件

要集成 Dodo Payments API，您需要：
- 一个 Dodo Payments 商户账户
- 从仪表板获取的 API 凭证（API 密钥和 webhook 密钥）

## 仪表板设置

1. 导航到 [Dodo Payments Dashboard](https://app.dodopayments.com/)

2. 创建一个产品（一次性支付或订阅）

4. 生成您的 API 密钥：
   - 转到开发者 > API
   - [详细指南](/api-reference/introduction#api-key-generation)
   - 复制 env 中名为 DODO_PAYMENTS_API_KEY 的 API 密钥

5. 配置 webhooks：
   - 转到开发者 > Webhooks
   - 创建一个用于支付通知的 webhook URL
   - 复制 env 中的 webhook 密钥

## 集成

### 支付链接

选择适合您用例的集成路径：
- **结账会话（推荐）**：适用于大多数集成。在您的服务器上创建一个会话，并将客户重定向到安全的托管结账页面。
- **覆盖结账**：当您需要在页面内体验时使用，它会将结账作为模态覆盖在您的网站上打开。
- **内嵌结账**：将结账直接嵌入到您的页面布局中，以实现完全集成的品牌结账体验。
- **静态支付链接**：无代码、可即时分享的 URL，用于快速收款。
- **动态支付链接**：以编程方式创建的链接。然而，推荐使用结账会话，它提供了更多的灵活性。

#### 1. 结账会话

使用 Checkout Sessions 来创建用于一次性付款或订阅的安全托管结账体验。您可以在服务器上创建会话，然后将客户重定向到返回的 `checkout_url`。

<Info>
结账会话默认有效期为 24 小时。如果您传入 <code>confirm=true</code>，会话有效期为 15 分钟，并且必须提供所有必需字段。
</Info>

<Steps>
{/* LOCKED_PATTERN_9da2f6afa35e431f795c09e61d338bbf */}
  选择您偏好的 SDK 或调用 REST API。

  <Tabs>
  <Tab title="Node.js SDK">

  ```javascript
  import DodoPayments from 'dodopayments';

  const client = new DodoPayments({
    bearerToken: process.env.DODO_PAYMENTS_API_KEY,
    environment: 'test_mode', // defaults to 'live_mode'
  });

  const session = await client.checkoutSessions.create({
    product_cart: [{ product_id: 'prod_123', quantity: 1 }],
    customer: { email: 'customer@example.com', name: 'John Doe' },
    return_url: 'https://yourapp.com/checkout/success',
  });
  ```

  </Tab>
  <Tab title="Python SDK">

  <Tab title="Go SDK">
  {/* LOCKED_PATTERN_a0590adb76e3b213346a01f1fb34a9b8 */}

  ```python
  import os
  from dodopayments import DodoPayments

  client = DodoPayments(
      bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),
      environment="test_mode",  # defaults to "live_mode"
  )

  session = client.checkout_sessions.create(
      product_cart=[{"product_id": "prod_123", "quantity": 1}],
      customer={"email": "customer@example.com", "name": "John Doe"},
      return_url="https://yourapp.com/checkout/success",
  )
  ```

  </Tab>

  <Tab title="Go SDK">
  {/* LOCKED_PATTERN_a0590adb76e3b213346a01f1fb34a9b8 */}

  ```javascript
  const response = await fetch('https://test.dodopayments.com/checkouts', {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`,
    },
    body: JSON.stringify({
      product_cart: [{ product_id: 'prod_123', quantity: 1 }],
      customer: { email: 'customer@example.com', name: 'John Doe' },
      return_url: 'https://yourapp.com/checkout/success',
    }),
  });
  const session = await response.json();
  ```

  </Tab>
  </Tabs>
</Step>

{/* LOCKED_PATTERN_ef5eb854bfdbd59ebc74ca72dbbce7b4 */}
  创建会话后，将用户重定向到 `checkout_url` 以启动托管流程。

  ```javascript
  // Example in a browser context
  window.location.href = session.checkout_url;
  ```

</Step>
</Steps>

<Tip>
对于最快速、最可靠的收款方式，请优先使用 Checkout Sessions。如需高级自定义，请参阅完整的 <a href="/developer-resources/checkout-session">Checkout Sessions 指南</a> 以及 <a href="/api-reference/checkout-sessions/create">API 参考</a>。
</Tip>

#### 2. 嵌入式结账

要获得无缝的页面内结账体验，请探索我们的 [嵌入式结账](/developer-resources/overlay-checkout) 集成，允许客户在不离开您网站的情况下完成支付。

#### 3. 内嵌结账

对于直接嵌入您页面的完全集成结账体验，请使用我们的 [内嵌结账](/developer-resources/inline-checkout) 集成。这使您能够构建自定义订单摘要，并完全控制结账布局，同时 Dodo Payments 安全地处理支付收款。

#### 4. 静态支付链接

静态支付链接让您通过分享简单的 URL 快速接受付款。您可以通过传递查询参数来定制结账体验，以预填客户详细信息、控制表单字段并添加自定义元数据。

<Steps>
{/* LOCKED_PATTERN_0ce2eadef618cf4bf679c917fdcc18cf */}
  从基础 URL 开始并附加您的产品 ID：

  ```text
  https://checkout.dodopayments.com/buy/{productid}
  ```

</Step>
{/* LOCKED_PATTERN_b67414e2600373519d5cb9e3c1e26cb6 */}
  包含必需的查询参数：
  - <ParamField query="quantity" type="integer" default="1">购买数量。</ParamField>
  - <ParamField query="redirect_url" type="string" required>支付完成后的重定向 URL。</ParamField>
  <Note>
  重定向 URL 将包含作为查询参数的支付详情，例如：<br/>
  <code>https://example.com/?payment_id=pay_ts2ySpzg07phGeBZqePbH&status=succeeded&email=customer%40example.com</code><br/><br/>
  如果产品启用了许可证密钥，还会附加 `license_key` 参数（多个密钥以逗号分隔）：<br/>
  <code>https://example.com/?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com</code>
  </Note>
</Step>
{/* LOCKED_PATTERN_bf21721ad8d8535f11242a5860c319ba */}
  通过查询参数添加客户或账单字段，以简化结账流程。
  <AccordionGroup>
    {/* LOCKED_PATTERN_0d88e0ac2dfca863f363aa813e79ec10 */}
      - <ParamField query="fullName" type="string">客户全名（如果提供 firstName 或 lastName，则忽略）。</ParamField>
      - <ParamField query="firstName" type="string">客户名字。</ParamField>
      - <ParamField query="lastName" type="string">客户姓氏。</ParamField>
      - <ParamField query="email" type="string">客户电子邮件地址。</ParamField>
      - <ParamField query="country" type="string">客户国家。</ParamField>
      - <ParamField query="addressLine" type="string">街道地址。</ParamField>
      - <ParamField query="city" type="string">城市。</ParamField>
      - <ParamField query="state" type="string">州或省。</ParamField>
      - <ParamField query="zipCode" type="string">邮政编码/ZIP 码。</ParamField>
      - <ParamField query="showDiscounts" type="boolean">true 或 false</ParamField>
    </Accordion>
  </AccordionGroup>
</Step>
{/* LOCKED_PATTERN_5ba9154f64e93703ead2b375e927bb9d */}
  <Info>
  您可以禁用特定字段，使其对客户只读。这在您已经掌握客户信息（例如已登录用户）时非常有用。
  </Info>
  要禁用某个字段，请提供其值并将对应的 <code>disable...</code> 标志设置为 <code>true</code>：
  <CodeGroup>

  ```text Example
  &email=alice@example.com&disableEmail=true
  ```

  </CodeGroup>
  <Tabs>
    {/* LOCKED_PATTERN_c5fbb9c61b6d6bf86d0af3463918b744 */}
      | 字段           | 禁用标志                | 必需参数 |
      |---------------|------------------------|----------|
      | 全名           | `disableFullName`   | `fullName`        |
      | 名字           | `disableFirstName`  | `firstName`       |
      | 姓氏           | `disableLastName`   | `lastName`        |
      | 电子邮件       | `disableEmail`      | `email`           |
      | 国家           | `disableCountry`    | `country`         |
      | 地址行         | `disableAddressLine`| `addressLine`     |
      | 城市           | `disableCity`       | `city`            |
      | 州             | `disableState`      | `state`           |
      | 邮编           | `disableZipCode`    | `zipCode`         |
    </Tab>
  </Tabs>
  <Tip>
  禁用字段有助于防止意外更改并确保数据一致性。
  </Tip>
  <Note>
  设置 <code>showDiscounts=false</code> 可以禁用并隐藏结账表单中的折扣部分。如果您希望阻止客户在结账期间输入优惠券或促销代码，请使用此设置。
  </Note>

</Step>
{/* LOCKED_PATTERN_f0f0bd2d4c3fc4c84d85d526a2ba7f53 */}
  - <ParamField query="paymentCurrency" type="string">指定支付货币。默认为账单国家的货币。</ParamField>
  - <ParamField query="showCurrencySelector" type="boolean" default="true">显示或隐藏货币选择器。</ParamField>
  - <ParamField query="paymentAmount" type="integer">金额（以分为单位，仅用于“支付您想要的”定价）。</ParamField>
  - <ParamField query="metadata_*" type="string">自定义元数据字段（例如 <code>metadata_orderId=123</code>）。</ParamField>
</Step>
{/* LOCKED_PATTERN_b07d670171a721c10aab40206f22c8d2 */}
  将完成的支付链接发送给客户。当他们访问时，所有查询参数会被收集并与会话 ID 一起存储。然后 URL 会简化，仅保留会话参数（例如 <code>?session=sess_1a2b3c4d</code>）。存储的信息会在页面刷新后持续存在，并在整个结账流程中可获取。
  <Check>
  客户的结账体验现在会根据您的参数变得更加简洁和个性化。
  </Check>
</Step>
</Steps>

#### 4. 动态支付链接
<Tip>
在大多数使用场景中，推荐使用 Checkout Sessions，它们提供更大的灵活性和控制权。
</Tip>

通过 API 调用或我们的 SDK 创建，包含客户详细信息。以下是一个示例：

有两个 API 用于创建动态支付链接：

- 一次性支付链接 API [API 参考](/api-reference/payments/post-payments)
- 订阅支付链接 API [API 参考](/api-reference/subscriptions/post-subscriptions)

下面的指南是关于一次性支付链接创建的。

有关集成订阅的详细说明，请参考此 [订阅集成指南](/developer-resources/subscription-integration-guide)。

<Info>请确保您正在传递 `payment_link = true` 以获取支付链接 </Info>

<Tabs>
  
  <Tab title="Node.js SDK">

    ```javascript
    import DodoPayments from 'dodopayments';

const client = new DodoPayments({
  bearerToken: process.env['DODO_PAYMENTS_API_KEY'], // This is the default and can be omitted
  environment: 'test_mode', // defaults to 'live_mode'
});

async function main() {
  const payment = await client.payments.create({
    payment_link: true,
    billing: { city: 'city', country: 'AF', state: 'state', street: 'street', zipcode: 0 },
    customer: { email: 'email@email.com', name: 'name' },
    product_cart: [{ product_id: 'product_id', quantity: 0 }],
  });

  console.log(payment.payment_id);
}

main();
    ```

    
  </Tab>
  <Tab title="Python SDK">

    ```python
    import os
from dodopayments import DodoPayments

client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted (if using same name `DODO_PAYMENTS_API_KEY`)
    environment="test_mode",  # defaults to "live_mode"
)
payment = client.payments.create(
    payment_link=True,
    billing={
        "city": "city",
        "country": "AF",
        "state": "state",
        "street": "street",
        "zipcode": 0,
    },
    customer={
        "email": "email@email.com",
        "name": "name",
    },
    product_cart=[{
        "product_id": "product_id",
        "quantity": 0,
    }],
)
print(payment.payment_link)
    ```

  </Tab>
  <Tab title="Go SDK">

    ```go
    package main

import (
  "context"
  "fmt"

  "github.com/dodopayments/dodopayments-go"
  "github.com/dodopayments/dodopayments-go/option"
)

func main() {
  client := dodopayments.NewClient(
    option.WithBearerToken("My Bearer Token"), // defaults to os.LookupEnv("DODO_PAYMENTS_API_KEY")
  )
  payment, err := client.Payments.New(context.TODO(), dodopayments.PaymentNewParams{
    PaymentLink: dodopayments.F(true),
    Billing: dodopayments.F(dodopayments.PaymentNewParamsBilling{
      City: dodopayments.F("city"),
      Country: dodopayments.F(dodopayments.CountryCodeAf),
      State: dodopayments.F("state"),
      Street: dodopayments.F("street"),
      Zipcode: dodopayments.F(int64(0)),
    }),
    Customer: dodopayments.F(dodopayments.PaymentNewParamsCustomer{
      Email: dodopayments.F("email"),
      Name: dodopayments.F("name"),
    }),
    ProductCart: dodopayments.F([]dodopayments.PaymentNewParamsProductCart{dodopayments.PaymentNewParamsProductCart{
      ProductID: dodopayments.F("product_id"),
      Quantity: dodopayments.F(int64(0)),
    }}),
  })
  if err != nil {
    panic(err.Error())
  }
  fmt.Printf("%+v\n", payment.PaymentLink)
}

    ```

    </Tab>
{/* LOCKED_PATTERN_a0590adb76e3b213346a01f1fb34a9b8 */}

    ```javascript
import { NextRequest, NextResponse } from "next/server";      

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const { formData, cartItems } = paymentRequestSchema.parse(body);

    const response = await fetch(`${process.env.NEXT_PUBLIC_DODO_TEST_API}/payments`, {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        Authorization: `Bearer ${process.env.DODO_API_KEY}`, // Replace with your API secret key generated from the Dodo Payments Dashboard
      },
      body: JSON.stringify({
        billing: {
          city: formData.city,
          country: formData.country,
          state: formData.state,
          street: formData.addressLine,
          zipcode: parseInt(formData.zipCode),
        },
        customer: {
          email: formData.email,
          name: `${formData.firstName} ${formData.lastName}`,
          phone_number: formData.phoneNumber || undefined,
        },
        payment_link: true,
        product_cart: cartItems.map((id) => ({
          product_id: id,
          quantity: 1,
        })),
        return_url: process.env.NEXT_PUBLIC_RETURN_URL,
      }),
    });

    if (!response.ok) {
      const errorData = await response.json().catch(() => null);
      return NextResponse.json(
        { error: "Payment link creation failed", details: errorData },
        { status: response.status }
      );
    }

    const data = await response.json();
    return NextResponse.json({ paymentLink: data.payment_link });
  } catch (err) {
    console.error("Payment error:", err);
    return NextResponse.json(
      {
        error: err instanceof Error ? err.message : "An unknown error occurred",
      },
      { status: 500 }
    );
  }
}
```

  </Tab>
</Tabs>

<Info>创建支付链接后，请将客户重定向以完成支付。</Info>

### 实现 Webhooks

设置 API 端点以接收支付通知。以下是使用 Next.js 的示例：

```javascript
import { Webhook } from "standardwebhooks";
import { headers } from "next/headers";
import { WebhookPayload } from "@/types/api-types";

const webhook = new Webhook(process.env.DODO_WEBHOOK_KEY!); // Replace with your secret key generated from the Dodo Payments Dashboard

export async function POST(request: Request) {
  const headersList = headers();
  const rawBody = await request.text();

  const webhookHeaders = {
    "webhook-id": headersList.get("webhook-id") || "",
    "webhook-signature": headersList.get("webhook-signature") || "",
    "webhook-timestamp": headersList.get("webhook-timestamp") || "",
  };

  await webhook.verify(rawBody, webhookHeaders);
  const payload = JSON.parse(rawBody) as WebhookPayload;
  
  // Process the payload according to your business logic
}
```

我们的 webhook 实现遵循 [标准 Webhooks](https://standardwebhooks.com/) 规范。有关 webhook 类型定义，请参考我们的 [Webhook 事件指南](/developer-resources/webhooks/intents/webhook-events-guide)。

您可以参考这个在 [GitHub](https://github.com/dodopayments/dodo-checkout-demo) 上使用 Next.js 和 TypeScript 的演示实现项目。

您可以在 [这里](https://atlas.dodopayments.com/) 查看实时实现。