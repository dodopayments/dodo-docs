---
title: Giới Thiệu
sidebarTitle: Giới Thiệu
description: Tự động cung cấp khóa giấy phép, tệp tải về, cờ tính năng, và truy
  cập vào các nền tảng như Discord, GitHub, Telegram, Framer, và Notion khi
  khách hàng thanh toán.
icon: box-archive
keywords:
  - Dodo Payments
  - entitlements
  - fulfillment
  - license keys
  - digital delivery
  - platform access
---

<Info>
Entitlements biến một lần thanh toán thành công hoặc đăng ký hoạt động thành quyền truy cập thực tế: một khóa giấy phép trong hộp thư của khách hàng, một cờ tính năng ứng dụng của bạn kiểm tra, một vai trò Discord, một kho lưu trữ GitHub, một mẫu Notion, một liên kết remix Framer, một lời mời trò chuyện Telegram, hoặc một gói tệp tải về. Dodo Payments tự động phát hành, theo dõi và thu hồi quyền truy cập đó khi vòng đời thanh toán thay đổi.
</Info>

<Frame caption="The Entitlements dashboard. Each entitlement is a reusable template; the right pane shows individual customer grants.">
<img src="/images/entitlements/list.png" alt="Bảng điều khiển Entitlements với danh sách entitlement ở bên trái và hoạt động grant ở bên phải" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

## Entitlements là gì?

Một **entitlement** là một định nghĩa tái sử dụng của thứ bạn cung cấp cho khách hàng: một mã giấy phép Pro, một vai trò "Patrons" trên Discord, quyền truy cập vào kho lưu trữ GitHub riêng của bạn, một gói e-book có thể tải xuống. Bạn gắn entitlements vào sản phẩm, và Dodo Payments xử lý phần còn lại.

Khi khách hàng mua sản phẩm, Dodo Payments tạo một **grant**, tức một lần cấp entitlement cho một khách hàng cụ thể. Grant đi qua một tập hợp nhỏ các trạng thái: `Pending` khi quá trình phân phối đang diễn ra, `Delivered` sau khi khách hàng có quyền truy cập, `Failed` nếu không thể hoàn tất việc phân phối và `Revoked` khi quyền truy cập bị thu hồi.

<Tip>
Entitlements kiểm soát **fulfillment** (khách hàng có quyền truy cập không?). Credits kiểm soát **consumption** (họ có thể sử dụng bao nhiêu?). Cả hai có thể được gắn vào cùng một sản phẩm. Xem [Thanh toán Dựa trên Tín dụng](/features/credit-based-billing) cho thông tin về credits.
</Tip>

## Các Tích Hợp Có Sẵn

Dodo Payments cung cấp mỗi entitlement thông qua một tích hợp chuyên dụng. Chọn tích hợp phù hợp với những gì bạn bán.

<CardGroup cols={2}>
<Card title="License Keys" icon="key" href="/features/license-keys">
Tạo mã giấy phép duy nhất với giới hạn kích hoạt và thời hạn. Phù hợp nhất cho phần mềm, plugin, và CLI.
</Card>

<Card title="Digital Files" icon="download" href="/features/digital-product-delivery">
Cung cấp các tập tin có thể tải xuống (e-books, mẫu, phương tiện) với URL tải xuống được ký trước và hướng dẫn tùy chọn.
</Card>

<Card title="Feature Flags" icon="flag" href="/features/entitlements/feature-flags">
Chặn các tính năng trong ứng dụng của bạn sau khi mua. Được cung cấp ngay lập tức, kiểm tra thông qua API, thu hồi khi hủy bỏ.
</Card>

<Card title="Discord" icon="discord" href="/features/entitlements/discord">
Cấp cho khách hàng một vai trò trong máy chủ Discord của bạn khi họ mua. Thu hồi tự động khi hủy bỏ.
</Card>

<Card title="GitHub" icon="github" href="/features/entitlements/github">
Thêm khách hàng làm người cộng tác vào kho lưu trữ riêng tư theo mức quyền bạn chọn.
</Card>

<Card title="Telegram" icon="telegram" href="/features/entitlements/telegram">
Thêm khách hàng vào trò chuyện hoặc kênh Telegram riêng tư sau khi mua.
</Card>

<Card title="Framer" icon="puzzle-piece" href="/features/entitlements/framer">
Mở khóa một liên kết remix của mẫu Framer cho khách hàng thanh toán.
</Card>

<Card title="Notion" icon="book" href="/features/entitlements/notion">
Nhân bản một mẫu Notion vào không gian làm việc của khách hàng khi mua.
</Card>
</CardGroup>

---

## Cách thức hoạt động của Grants

Grants được điều khiển bởi các sự kiện thanh toán và đăng ký mà bạn đã nhận được dưới dạng webhooks. Bạn không cần phải tự gọi API grant cho các lần mua. Dodo Payments tự động tạo và thu hồi grants dựa trên vòng đời thanh toán cơ bản.

### Vòng đời của Grant

<Steps>
<Step title="Created">
Grant được tạo khi một payment hoàn tất hoặc một subscription trở nên active. Feature flags chuyển thẳng sang `Delivered`. License keys cũng chuyển thẳng sang `Delivered` khi entitlement sử dụng `fulfillment_mode: auto` (mặc định); với `fulfillment_mode: manual`, grant được tạo ở `Pending` mà chưa có key cho đến khi bạn cung cấp một key thông qua [Fulfill License Key Grant](/api-reference/entitlements/fulfill-license-key). Mọi integration khác đều bắt đầu ở `Pending`. Các integration dựa trên OAuth (Discord, GitHub, Notion) cung cấp một `oauth_url` mà customer phải truy cập để hoàn tất consent; field này là `null` trên grant mới được tạo và được điền khi customer bắt đầu accept flow từ email delivery hoặc customer portal của họ. Các integration trực tiếp với platform (Telegram, Framer, Digital Files) chỉ ở `Pending` trong thời gian ngắn khi delivery được provision, sau đó chuyển sang `Delivered`.
</Step>

<Step title="Delivered">
Sau khi việc phân phối hoàn tất (license key được tạo, role được gán, quyền truy cập repository được cấp, các liên kết tệp được xác định và OAuth hoàn tất), grant chuyển sang `Delivered` và `delivered_at` được thiết lập.
</Step>

<Step title="Failed">
Nếu lệnh gọi integration trả về lỗi không thể retry (OAuth token bị thu hồi, quyền bị từ chối hoặc tệp không còn tồn tại), grant chuyển sang `Failed`. Các field `error_code` và `error_message` ghi nhận lý do.
</Step>

<Step title="Revoked">
Khi quyền truy cập bị thu hồi (subscription bị hủy, khoản hoàn tiền được phát hành hoặc merchant chủ động thu hồi), grant chuyển sang `Revoked`. Field `revocation_reason` ghi nhận nguyên nhân.
</Step>
</Steps>

### Hành vi của Grant theo sự kiện

| Event | Behavior |
|---|---|
| `payment.succeeded` (one-time payment) | Cấp một grant cho mỗi entitlement được đính kèm. |
| `payment.succeeded` (subscription-linked payment) | No-op. Grant được điều khiển bởi subscription event bên dưới. |
| `subscription.active` | Cấp grant cho mọi entitlement được đính kèm nhưng chưa có grant. Cấp lại mọi grant trước đó đã bị revoke cho cùng subscription. |
| `subscription.renewed` | No-op. Grant hiện có được giữ nguyên qua các lần renewal. |
| `subscription.on_hold` | Revoke tất cả grant đã delivery và đang pending. `revocation_reason: subscription_on_hold`. |
| `subscription.paused` | Revoke tất cả grant đã delivery và đang pending. `revocation_reason: SubscriptionPaused`. |
| `subscription.unpaused` | Cấp lại mọi grant trước đó đã bị revoke cho cùng subscription, chính xác như `subscription.active`. |
| `subscription.cancelled` | Revoke tất cả. `revocation_reason: subscription_cancelled`. |
| `subscription.expired` | Revoke tất cả. `revocation_reason: subscription_expired`. |
| `subscription.plan_changed` | Revoke tất cả grant hiện tại, sau đó cấp grant cho các entitlement của plan mới. `revocation_reason: plan_changed`. |
| `refund.succeeded` (one-time payment) | Revoke grant cho payment đó. `revocation_reason: refund`. |
| Manual API revoke | Revoke bằng `revocation_reason: manual`. Các lần revoke thủ công không được tự động cấp lại khi subscription renewal. |
| License key disabled | Đối với grant sử dụng license key, việc vô hiệu hóa key nền tảng sẽ revoke grant bằng `revocation_reason: license_key_disabled`. Grant được tự động kích hoạt lại nếu key được bật lại. |
| Platform drift detected | Nếu phía platform của một integration bị lệch trạng thái (một Discord role bị xóa thủ công, GitHub App mất quyền truy cập repository hoặc một reconciliation pass phát hiện target bị thiếu), grant sẽ bị revoke bằng `revocation_reason: platform_external`. Grant không được tự động cấp lại khi subscription renewal cho đến khi vấn đề trên platform nền tảng được giải quyết. |

<Note>
Grants dựa trên đăng ký là idempotent cho mỗi `(entitlement, customer, subscription)`; gia hạn và kích hoạt lại không tạo ra grants trùng lặp. Grants một lần là idempotent cho mỗi `(entitlement, customer, payment)`.
</Note>

---

## Tạo quyền lợi đầu tiên của bạn

<Steps>
<Step title="Open Entitlements">
Đi tới **Entitlements** trong bảng điều khiển Dodo Payments của bạn và nhấp vào **+** để tạo một quyền lợi mới.
</Step>

<Step title="Pick an integration">
Chọn loại tích hợp: Khóa giấy phép, Tệp kỹ thuật số, Cờ tính năng, Discord, GitHub, Telegram, Figma, Framer hoặc Notion. Đối với các tích hợp nền tảng, trước tiên hãy kết nối tài khoản của bạn nếu bạn chưa thực hiện.
</Step>

<Step title="Configure delivery">
Điền vào các trường cụ thể cho tích hợp. Ví dụ, GitHub yêu cầu kho lưu trữ và mức quyền; Discord yêu cầu máy chủ và vai trò tùy chọn; Khóa giấy phép yêu cầu hạn mức kích hoạt và ngày hết hạn.

<Frame caption="Creating a GitHub entitlement. Each integration shows the fields it needs.">
<img src="/images/entitlements/github/create.png" alt="Biểu mẫu quyền lợi mới với bộ chọn tích hợp và các trường cấu hình" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>
</Step>

<Step title="Save">
Lưu quyền lợi. Bây giờ bạn có thể đính kèm nó vào bất kỳ sản phẩm nào.
</Step>
</Steps>

## Đính kèm quyền lợi vào sản phẩm

Mở một sản phẩm, mở rộng **Cài đặt nâng cao → Quyền lợi & Tín dụng**, và chọn các quyền lợi mà nên được cung cấp khi sản phẩm được mua. Một sản phẩm có thể cung cấp nhiều quyền lợi cùng một lúc. Ví dụ, một kế hoạch Pro có thể bao gồm một khóa giấy phép, quyền truy cập GitHub, và một vai trò Discord.

<Frame caption="Attaching entitlements to a product. Selected entitlements are delivered on every successful purchase or active subscription.">
<img src="/images/entitlements/attach-to-product.png" alt="Bảng lựa chọn quyền lợi sản phẩm hiển thị các hộp kiểm cho mỗi quyền lợi có sẵn" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

---

## Trải nghiệm khách hàng

### Email và cổng thông tin khách hàng

Khách hàng nhận được một email cung cấp sau khi mua chứa khóa giấy phép, liên kết tải về, liên kết mời OAuth, hoặc lời mời nền tảng, tùy theo các quyền lợi trên sản phẩm. Các chi tiết này vẫn có sẵn vô thời hạn từ [Cổng thông tin Khách hàng](/features/customer-portal) dưới lịch sử đơn hàng của họ.

### Cung cấp dựa trên OAuth

Quyền truy cập subscriber trên Discord, GitHub và Notion yêu cầu khách hàng ủy quyền cho Dodo Payments cấp quyền truy cập cho họ. Các grant này giữ ở trạng thái `Pending` cho đến khi khách hàng hoàn tất OAuth flow bằng liên kết trong email hoặc customer portal của họ. Sau khi ủy quyền, grant chuyển sang `Delivered` và quyền truy cập platform được cấp ngay lập tức.

### Thu hồi

Các grants bị thu hồi được loại bỏ ở cấp độ nền tảng: vai trò Discord bị loại bỏ, cộng tác viên GitHub bị loại bỏ, khóa giấy phép bị vô hiệu hóa. Khách hàng nhìn thấy sự thay đổi được phản ánh trong cổng thông tin khách hàng.

<Warning>
Đối với Tệp Kỹ Thuật Số, việc thu hồi loại bỏ quyền truy cập vào các URL được định sẵn trong tương lai nhưng không làm vô hiệu hóa các bản sao mà khách hàng đã tải xuống. Đặt nội dung kế hoạch tương ứng.
</Warning>

---

## Quản lý Grants

Mở bất kỳ quyền lợi nào từ bảng điều khiển để xem các grants của nó. Bảng chi tiết grant hiển thị tổng số grants, bộ lọc trạng thái, thông tin khách hàng, ngày cung cấp, và hành động thu hồi.

Bạn cũng có thể quản lý grants bằng chương trình:

<CodeGroup>

```typescript TypeScript theme={null}
import DodoPayments from 'dodopayments';

const client = new DodoPayments({
  bearerToken: process.env['DODO_PAYMENTS_API_KEY'],
});

// List grants for an entitlement
const grants = await client.entitlements.grants.list('ent_abc123', {
  status: 'Delivered',
});

// Revoke a single grant
await client.entitlements.grants.revoke('entg_xyz789', {
  id: 'ent_abc123',
});
```

```python Python theme={null}
client.entitlements.grants.list(
    "ent_abc123",
    status="Delivered",
)

client.entitlements.grants.revoke(
    "entg_xyz789",
    id="ent_abc123",
)
```

```go Go theme={null}
grants, _ := client.Entitlements.Grants.List(
  ctx, "ent_abc123",
  dodopayments.EntitlementGrantListParams{Status: dodopayments.F("Delivered")},
)

_, _ = client.Entitlements.Grants.Revoke(ctx, "ent_abc123", "entg_xyz789")
```

</CodeGroup>

---

## Quản lý API

<CardGroup cols={2}>
<Card title="Create Entitlement" icon="plus" href="/api-reference/entitlements/create-entitlement">
Tạo một quyền lợi mới của bất kỳ loại tích hợp nào.
</Card>
<Card title="List Entitlements" icon="list" href="/api-reference/entitlements/list-entitlements">
Danh sách các quyền lợi với bộ lọc theo loại tích hợp.
</Card>
<Card title="Get Entitlement" icon="magnifying-glass" href="/api-reference/entitlements/get-entitlement">
Truy xuất một quyền lợi và cấu hình đã giải quyết của nó.
</Card>
<Card title="Update Entitlement" icon="pen" href="/api-reference/entitlements/update-entitlement">
Cập nhật tên, mô tả, hoặc cấu hình tích hợp.
</Card>
<Card title="Delete Entitlement" icon="trash" href="/api-reference/entitlements/delete-entitlement">
Xóa mềm một quyền lợi; các grants hiện tại không bị ảnh hưởng.
</Card>
<Card title="Upload File" icon="upload" href="/api-reference/entitlements/upload-file">
Tải lên tệp cho một quyền lợi Tệp Kỹ Thuật Số (tối đa 500 MiB).
</Card>
<Card title="List Grants" icon="users" href="/api-reference/entitlements/list-grants">
Danh sách tất cả grants cho một quyền lợi với bộ lọc trạng thái và khách hàng.
</Card>
<Card title="Revoke Grant" icon="ban" href="/api-reference/entitlements/revoke-grant">
Thu hồi thủ công một grant.
</Card>
</CardGroup>

---

## Webhooks

Dodo Payments phát bốn sự kiện webhook cho vòng đời grant. Đăng ký các sự kiện này để giữ cho ứng dụng của bạn đồng bộ với những gì mỗi khách hàng có thể truy cập.

| Sự kiện | Kích hoạt khi |
|---|---|
| `entitlement_grant.created` | Một grant mới được tạo. Các grant dạng license-key có trạng thái `Delivered` với quy trình fulfillment `auto` và `Pending` với quy trình fulfillment `manual`; mọi integration khác có trạng thái `Pending` và chuyển sang `Delivered` sau khi lệnh gọi platform thành công (hoặc, đối với các integration dựa trên OAuth, sau khi khách hàng ủy quyền). |
| `entitlement_grant.delivered` | Grant chuyển sang trạng thái đã phân phối. Khách hàng hiện đã có quyền truy cập. |
| `entitlement_grant.failed` | Không thể phân phối grant. Kiểm tra `error_code` và `error_message`. |
| `entitlement_grant.revoked` | Quyền truy cập đã bị thu hồi. Kiểm tra `revocation_reason`. |

<Card title="Entitlement Grant Webhook Payloads" icon="bell" href="/developer-resources/webhooks/intents/entitlement-grant">
Xem toàn bộ sơ đồ tải, các sự kiện mẫu, và tham chiếu `revocation_reason`.
</Card>

---

## Thực hành tốt nhất

- **Sử dụng một entitlement cho mỗi delivery channel.** Không dùng chung một Discord entitlement cho các product có mục đích role khác nhau; hãy tạo một entitlement cho mỗi role để việc revoke rõ ràng.
- **Trước tiên hãy test ở test mode.** Tạo entitlement, đính kèm entitlement đó vào một test product, thực hiện checkout và theo dõi quá trình chuyển đổi của grant qua `Pending → Delivered`. Xác nhận rằng việc hủy test subscription sẽ revoke grant.
- **Lắng nghe `entitlement_grant.delivered`, không phải `payment.succeeded`.** Một payment có thể thành công trước khi fulfilment hoàn tất (đặc biệt với các OAuth flow). Hãy đợi event delivered trước khi mở khóa các feature phụ thuộc trong hệ thống của bạn.
- **Xem `entitlement_grant.failed` là một trạng thái cần xử lý.** Grant thất bại có nghĩa là customer đã thanh toán nhưng chưa được cấp quyền truy cập. Hãy chuyển các trường hợp này đến team hỗ trợ hoặc trigger việc cấp lại grant.
- **Map `revocation_reason` vào các retention flow của bạn.** Một lần revoke `subscription_on_hold` có thể khôi phục (customer có thể cập nhật card của họ). Một lần revoke `manual` là có chủ đích. Hãy xử lý chúng khác nhau trong hoạt động giao tiếp với customer.
