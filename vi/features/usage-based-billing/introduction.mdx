---
sidebarTitle: Giới thiệu
title: Thanh toán dựa trên mức sử dụng
description: Tính phí khách hàng dựa trên những gì họ thực sự sử dụng với việc
  đo lường linh hoạt và thanh toán tự động.
icon: box
---

Thanh toán dựa trên mức sử dụng cho phép bạn tính phí khách hàng cho những gì họ thực sự sử dụng—các cuộc gọi API, lưu trữ, token AI, hoặc bất kỳ chỉ số nào bạn định nghĩa. Khách hàng trả tiền cho mức tiêu thụ, không phải phí cố định.

<Info>
**Hoàn hảo cho**: APIs, dịch vụ AI, nền tảng cơ sở hạ tầng, và bất kỳ sản phẩm nào mà giá trị tăng theo mức sử dụng.
</Info>

## Cách hoạt động

<Steps>
<Step title="Send Usage Events">
Ứng dụng của bạn gửi sự kiện khi có hành động phải tính phí.

```javascript
{
  "event_id": "evt_123",
  "customer_id": "cus_abc",
  "event_name": "api.call",
  "metadata": { "endpoint": "/v1/users" }
}
```

</Step>

<Step title="Meters Aggregate Usage">
Meter tổng hợp sự kiện thành các đơn vị tính phí bằng cách sử dụng các phép tổng hợp Count, Sum, Max hoặc Last.
</Step>

<Step title="Automatic Billing">
Đặt giá theo mỗi đơn vị và ngưỡng dùng miễn phí. Khách hàng được tính phí tự động mỗi chu kỳ.
</Step>
</Steps>

## Khái niệm cốt lõi

### Sự kiện

Events đại diện cho các hành động sử dụng trong ứng dụng của bạn. Mỗi sự kiện bao gồm:
- **Tên sự kiện**: Loại sử dụng (ví dụ: `api.call`)
- **Customer ID**: Người đã sử dụng
- **Metadata**: Các thuộc tính bổ sung để tổng hợp

### Đồng hồ

Các đồng hồ tổng hợp sự kiện thành các số lượng tính phí:
- **Count**: Tổng số sự kiện (các cuộc gọi API)
- **Sum**: Cộng các giá trị (tổng số byte)
- **Max**: Giá trị cao nhất (người dùng cao điểm)
- **Last**: Giá trị gần nhất

<Frame>
<img src="/images/usage-based/UBB-2.png" alt="Giao diện tạo meter" />
</Frame>

### Sản phẩm với giá dựa trên mức sử dụng

Đặt giá theo đơn vị và ngưỡng miễn phí tùy chọn:

<Frame>
<img src="/images/usage-based/UBB-4.png" alt="Cấu hình giá" />
</Frame>

**Ví dụ**: 2,500 cuộc gọi - 1,000 miễn phí = 1,500 × $0.02 = $30.00

### Khấu Trừ Dựa trên Tín Dụng

Thay vì tính phí theo đơn vị bằng đô la, bạn có thể cấu hình các đồng hồ đo để trừ vào [số dư tín dụng](/features/credit-based-billing) của khách hàng. Tín dụng được làm mới mỗi chu kỳ thanh toán và áp dụng trước bất kỳ phí vượt mức nào.

## Bắt đầu nhanh

<Steps>
<Step title="Create a Meter">
Trong bảng điều khiển của bạn: **Meters** → **Create Meter**

1. Đặt tên sự kiện (ví dụ: `api.call`)
2. Chọn phép tổng hợp (Count, Sum, Max, Last)
3. Thêm các bộ lọc tùy chọn
</Step>

<Step title="Add to Product">
Liên kết meter với một sản phẩm có định giá:

<Frame>
<img src="/images/usage-based/UBB-5.png" alt="Thêm meter vào sản phẩm" />
</Frame>

1. Chọn **Usage-Based Billing**
2. Chọn meter của bạn
3. Đặt giá trên mỗi đơn vị và ngưỡng dùng miễn phí
</Step>

<Step title="Send Events">

```javascript
await fetch('https://test.dodopayments.com/events/ingest', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${process.env.DODO_API_KEY}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    events: [{
      event_id: "unique_event_id",
      customer_id: "cus_abc123",
      event_name: "api.call",
      metadata: { endpoint: "/v1/users" }
    }]
  })
});
```

</Step>

<Step title="Monitor Usage">
<Frame>
<img src="/images/guides/usage-based-billing/meter-quantities-chart.png" alt="Bảng điều khiển meter" />
</Frame>

Kiểm tra bảng điều khiển của meter để xem các sự kiện và tổng hợp sử dụng. Khách hàng được tính phí tự động mỗi chu kỳ.
</Step>
</Steps>

## Trường hợp sử dụng

<CardGroup cols={2}>
<Card title="APIs & Infrastructure" icon="cloud">
Theo dõi cuộc gọi API, giờ tính toán, băng thông, dung lượng lưu trữ. Ví dụ: $0.001 cho mỗi cuộc gọi sau 10.000 lượt miễn phí.
</Card>

<Card title="AI Usage Tracking" icon="brain">
Theo dõi token, số lần tạo, thời gian xử lý. Ví dụ: \$0.04 cho hình ảnh tiêu chuẩn, \$0.08 cho HD.
</Card>

<Card title="Data & Analytics" icon="chart-line">
Tính phí theo dữ liệu xử lý, truy vấn thực hiện, bản ghi phân tích. Ví dụ: $5 cho mỗi GB sau 10 GB miễn phí.
</Card>

<Card title="Communication" icon="message">
Đếm tin nhắn, phút, dữ liệu truyền. Ví dụ: $0.01 cho mỗi tin nhắn SMS gửi đi.
</Card>
</CardGroup>

## Các bước tiếp theo

<CardGroup cols={2}>
<Card title="Event Ingestion" icon="bolt" href="/features/usage-based-billing/event-ingestion">
Tìm hiểu cách cấu trúc và gửi các sự kiện sử dụng từ ứng dụng của bạn.
</Card>

<Card title="Meters Guide" icon="sliders" href="/features/usage-based-billing/meters">
Thành thạo cấu hình meter, các loại tổng hợp và lọc sự kiện.
</Card>

<Card title="Complete Tutorial" icon="code" href="/developer-resources/usage-based-billing-build-ai-image-generator">
Xây dựng một trình tạo ảnh AI đầy đủ với giá theo mức sử dụng từ đầu.
</Card>

<Card title="Integration Guide" icon="book" href="/developer-resources/usage-based-billing-guide">
Hướng dẫn toàn diện về triển khai tính phí theo mức sử dụng trong ứng dụng của bạn.
</Card>
</CardGroup>
