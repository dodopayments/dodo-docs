---
sidebarTitle: Giới thiệu
title: Mô hình thu thập dữ liệu
description: Các mẫu thu thập sự kiện đã được chuẩn bị sẵn cho các trường hợp sử
  dụng phổ biến với mã mẫu.
icon: box
---

Mô hình thu thập dữ liệu là các mẫu đã được xây dựng sẵn để theo dõi việc sử dụng trong các tình huống phổ biến. Thay vì phải tìm hiểu cấu trúc sự kiện và cấu hình đồng hồ từ đầu, hãy bắt đầu với một mô hình đã được chứng minh và tùy chỉnh nó theo nhu cầu của bạn.

## Mô hình là gì?

Mỗi mô hình bao gồm:
- **Khuyến nghị về cấu trúc sự kiện**: Tên sự kiện và thiết kế siêu dữ liệu tối ưu
- **Cấu hình đồng hồ**: Các loại tổng hợp và bộ lọc được đề xuất
- **Mã mẫu**: Các ví dụ triển khai hoạt động bằng nhiều ngôn ngữ khác nhau
- **Thực tiễn tốt nhất**: Mẹo để đảm bảo độ chính xác, hiệu suất và tối ưu hóa chi phí

<Tip>
Các blueprint giúp tiết kiệm hàng giờ thiết lập và tránh các sai lầm thông thường. Bắt đầu với một blueprint, rồi điều chỉnh theo yêu cầu cụ thể của bạn.
</Tip>

## Tại sao nên sử dụng mô hình?

<CardGroup cols={3}>
{/* LOCKED_PATTERN_7fe74dbfda905b6325848cb6dd2b6edf */}
Thiết lập theo dõi sử dụng trong vài phút thay vì hàng giờ. Sao chép các mẫu đã được chứng thực thay vì thiết kế từ đầu.
</Card>

{/* LOCKED_PATTERN_1b2b0a19db01cf51494e0ab26f35f6de */}
Tránh các lỗi phổ biến với cấu trúc sự kiện đã kiểm chứng, metadata phù hợp và lựa chọn tổng hợp tối ưu.
</Card>

{/* LOCKED_PATTERN_6c466abe1ee82e28fa47cdbc5cfd6cbe */}
Ví dụ mã dựa trên các triển khai thực tế. Chỉ cần thêm khóa API và bắt đầu.
</Card>
</CardGroup>

## Các mô hình có sẵn

Chọn mô hình phù hợp với trường hợp sử dụng của bạn:

<CardGroup cols={1}>
{/* LOCKED_PATTERN_cb13d34a0150ce3dde4b528bdfa94b98 */}
Theo dõi việc sử dụng token AI trên OpenAI, Anthropic, Groq, Gemini, Vercel AI SDK và hơn thế nữa.

**Hoàn hảo cho**: Chatbots AI, trình tạo mã, công cụ tạo nội dung, ứng dụng sử dụng AI.

**Bạn sẽ theo dõi**: Token đầu vào, token đầu ra, mức sử dụng mô hình, số lần yêu cầu.
</Card>

{/* LOCKED_PATTERN_a170aa86ba7c211ab83ec57c550e5b7a */}
Đo lường các yêu cầu API tại cấp gateway với lọc endpoint và hỗ trợ giới hạn tốc độ.

**Hoàn hảo cho**: REST APIs, dịch vụ GraphQL, nhà cung cấp webhook, nền tảng API-first.

**Bạn sẽ theo dõi**: Số lần yêu cầu, mức sử dụng endpoint, phương thức HTTP, thời gian phản hồi.
</Card>

{/* LOCKED_PATTERN_06ce679f6b2cac04dd34930ed580a53c */}
Theo dõi các tệp tải lên, dung lượng lưu trữ và băng thông cho dịch vụ lưu trữ đám mây.

**Hoàn hảo cho**: Lưu trữ tệp, nền tảng truyền thông, dịch vụ sao lưu, quản lý tài liệu.

**Bạn sẽ theo dõi**: Số byte lưu trữ, tệp tải lên, tải xuống, ảnh chụp lưu trữ.
</Card>

{/* LOCKED_PATTERN_0f4a45fb9633cc3bd68e5b3cf5632206 */}
Đo băng thông truyền phát cho video, âm thanh và truyền dữ liệu thời gian thực.

**Hoàn hảo cho**: Phát video, nền tảng âm thanh, phát sóng trực tiếp, dữ liệu thời gian thực.

**Bạn sẽ theo dõi**: Băng thông tiêu thụ, thời lượng luồng, mức chất lượng, luồng đồng thời.
</Card>

{/* LOCKED_PATTERN_157cd69ec190e067606fc563773ef69d */}
Tính phí theo thời gian chạy cho tài nguyên tính toán, hàm serverless và công việc chạy dài.

**Hoàn hảo cho**: Nền tảng không máy chủ, lưu trữ container, phiên bản tính toán, xử lý theo lô.

**Bạn sẽ theo dõi**: Thời gian thực thi, giờ tính toán, lượt gọi hàm, thời lượng công việc.
</Card>
</CardGroup>

## Cách sử dụng một mô hình

<Steps>
{/* LOCKED_PATTERN_d59a3a657eaa746788e5e1026413935d */}
Chọn blueprint phù hợp nhất với tình huống thanh toán của bạn. Mỗi trang blueprint bao gồm hướng dẫn triển khai chi tiết.
</Step>

{/* LOCKED_PATTERN_a84a8c1d781f962d2244309e44e003e7 */}
Hiểu thiết kế tên sự kiện và metadata được khuyến nghị. Sao chép cấu trúc sự kiện cho trường hợp sử dụng của bạn.
</Step>

{/* LOCKED_PATTERN_c90d951972c8c9406253edbbeef4ae33 */}
Theo hướng dẫn cấu hình đồng hồ đo của blueprint. Sử dụng loại tổng hợp và bộ lọc được đề xuất.
</Step>

{/* LOCKED_PATTERN_52ccaa86cb8b76a3b737ce87042d38d6 */}
Sao chép ví dụ mã cho ngôn ngữ/khuôn khổ của bạn. Thay các giá trị giữ chỗ bằng khóa API và ID khách hàng thực tế.
</Step>

{/* LOCKED_PATTERN_81eaca0122d1acfd1902758cc9f89c67 */}
Gửi sự kiện thử nghiệm và xác minh chúng xuất hiện trong bảng điều khiển đồng hồ đo với tổng hợp chính xác.
</Step>

{/* LOCKED_PATTERN_a4756b37e6237177fa6ac0f25c0967d5 */}
Điều chỉnh blueprint theo yêu cầu cụ thể của bạn—thêm metadata tùy chỉnh, điều chỉnh bộ lọc hoặc sửa đổi giá cả.
</Step>
</Steps>

## So sánh mô hình

Không chắc mô hình nào phù hợp với nhu cầu của bạn? Dưới đây là một so sánh nhanh:

| Trường hợp sử dụng | Mô hình | Tổng hợp | Siêu dữ liệu phổ biến |
|----------|-----------|-------------|-----------------|
| Ứng dụng chat AI | LLM | Tổng (token) | model, input_tokens, output_tokens |
| REST API | API Gateway | Đếm | endpoint, method, status_code |
| Lưu trữ tệp | Lưu trữ Đối tượng | Tổng (bytes) | file_size, file_type, operation |
| Nền tảng video | Stream | Tổng (băng thông) | duration, quality, bitrate |
| Không máy chủ | Phạm vi Thời gian | Tổng (thời gian) | function_name, execution_time_ms |

## Triển khai tùy chỉnh

Không thấy mô hình cho trường hợp sử dụng chính xác của bạn? Không vấn đề gì:

1. **Bắt đầu với mô hình gần nhất**: Chọn mô hình tương tự nhất với nhu cầu của bạn
2. **Điều chỉnh cấu trúc sự kiện**: Sửa đổi các trường siêu dữ liệu để phù hợp với yêu cầu của bạn
3. **Điều chỉnh cấu hình đồng hồ**: Thay đổi loại tổng hợp hoặc bộ lọc khi cần
4. **Kiểm tra kỹ lưỡng**: Xác minh các phép tính thanh toán với việc sử dụng mẫu

<Info>
Tất cả blueprint chỉ là điểm khởi đầu. Cứ thoải mái kết hợp các khái niệm từ nhiều blueprint khác nhau hoặc tạo triển khai tùy chỉnh hoàn toàn.
</Info>

## Cần giúp đỡ?

Nếu bạn không chắc mô hình nào nên sử dụng hoặc cần giúp đỡ trong việc tùy chỉnh:

<CardGroup cols={2}>
{/* LOCKED_PATTERN_b84a97fd71b10036af1d667e737cab38 */}
Thực hiện theo hướng dẫn từng bước xây dựng trình tạo ảnh AI với thanh toán theo mức sử dụng.
</Card>

{/* LOCKED_PATTERN_a797a91a6c2cad7854262e0cd866b9a5 */}
Hướng dẫn toàn diện bao quát mọi khía cạnh của triển khai thanh toán dựa trên mức sử dụng.
</Card>
</CardGroup>
