---
sidebarTitle: Thanh Toán Lai
title: Mô Hình Thanh Toán Lai
description: "Kết hợp nhiều chế độ thanh toán để tạo ra các chiến lược định giá
  tinh vi: đăng ký + sử dụng, ghế + bổ sung, cơ sở + vượt mức, và nhiều hơn
  nữa."
icon: layer-group
keywords:
  - Dodo Payments
  - mô hình thanh toán kết hợp
  - nền tảng thanh toán
  - thanh toán SaaS
---

<Info>
Thanh toán lai kết hợp hai hoặc nhiều mô hình thanh toán thành một chiến lược định giá duy nhất. Điều này cho phép bạn thu hút giá trị từ các chiều khác nhau—phí định kỳ, sử dụng, ghế và tính năng—trong khi mang lại cho khách hàng sự linh hoạt và dự đoán.
</Info>

<CardGroup cols={2}>
<Card title="Thanh Toán Dựa Trên Sử Dụng" icon="chart-line" href="/features/usage-based-billing/introduction">
  Nền tảng cho định giá dựa trên tiêu thụ.
</Card>

<Card title="Đăng Ký" icon="repeat" href="/features/subscription">
  Nền tảng cho thanh toán định kỳ.
</Card>

<Card title="Bổ Sung" icon="puzzle" href="/features/addons">
  Mở rộng đăng ký với các nâng cấp tùy chọn.
</Card>

<Card title="Thanh Toán Dựa Trên Ghế" icon="users" href="/features/seat-based-billing">
  Mô hình định giá theo người dùng.
</Card>
</CardGroup>

---

## Thanh Toán Lai Là Gì?

Thanh toán lai kết hợp nhiều chiều định giá thành một sản phẩm duy nhất. Thay vì chọn giữa đăng ký theo mức cố định HOẶC định giá dựa trên sử dụng, bạn sử dụng cả hai cùng nhau.

### Tại Sao Nên Sử Dụng Thanh Toán Lai?

| Mục Tiêu Kinh Doanh | Giải Pháp Lai |
|--------------|-----------------|
| Doanh thu dự đoán + tiềm năng tăng trưởng | Đăng ký cơ bản + phí sử dụng vượt mức |
| Định giá theo nhóm có thể mở rộng | Theo ghế + bổ sung tính năng |
| Đạt được khách hàng, mở rộng sau | Phí cơ bản thấp + phí tiêu thụ |
| Linh hoạt cho doanh nghiệp | Chi tiêu cam kết + phí theo yêu cầu |
| Định giá công bằng cho việc sử dụng biến đổi | Khoản cho phép bao gồm + trả theo mức sử dụng |

### Các Mô Hình Lai Thông Dụng

| Mô Hình | Mô Tả | Ví Dụ | Hỗ Trợ Bản Địa |
|-------|-------------|---------|----------------|
| **1. Đăng Ký + Sử Dụng** | Phí cơ bản + phí tiêu thụ | \$49/tháng + \$0.01/cuộc gọi API sau 10K miễn phí | ✅ Toàn bộ |
| **2. Đăng Ký + Ghế** | Phí nền tảng + phí theo người dùng | \$99/tháng + \$15/ghế | ✅ Toàn bộ |
| **3. Đăng Ký + Bổ Sung Tính Năng** | Kế hoạch cốt lõi + nâng cấp tùy chọn | \$29/tháng + \$19/tháng phân tích + \$9/tháng truy cập API | ✅ Toàn bộ |
| **4. Ghế + Sử Dụng** | Phí theo người dùng + phí tiêu thụ vượt mức | \$10/người/tháng + \$0.05/GB sau 5GB/người | ⚠️ Giải pháp thay thế |
| **5. Đăng Ký + Ghế + Sử Dụng** | Nền tảng + người dùng + tiêu thụ (lai ba) | \$199/tháng + \$20/ghế + phí tiêu thụ vượt mức | ⚠️ Giải pháp thay thế |
| **6. Cơ Sở Tầng + Phí Tiêu Thụ Vượt Mức** | Các tầng khác nhau với các khoản cho phép khác nhau | Khởi đầu (5K cuộc gọi) so với Chuyên nghiệp (50K cuộc gọi) + vượt mức | ✅ Toàn bộ |
| **7. Đăng Ký + Phí Theo Yêu Cầu** | Phí định kỳ + phí thủ công biến đổi | \$99/tháng giữ chỗ + tính phí theo giờ cho công việc | ✅ Toàn bộ |

---

## Mô Hình Lai 1: Đăng Ký + Sử Dụng

Mô hình lai phổ biến nhất. Khách hàng trả một khoản phí đăng ký cơ bản cộng với các khoản phí cho tiêu thụ vượt quá các khoản cho phép bao gồm.

### Cách Thức Hoạt Động

**Kế Hoạch Chuyên Nghiệp: 49 đô la/tháng**

- **Bao gồm**: 10,000 cuộc gọi API/tháng
- **Vượt mức**: 0.005 đô la mỗi cuộc gọi sau 10,000

**Tính toán ví dụ** (khách hàng sử dụng 25,000 cuộc gọi trong tháng này):
- Đăng ký cơ bản: 49.00 đô la
- Vượt mức: (25,000 - 10,000) × 0.005 đô la = 75.00 đô la
- **Tổng: 124.00 đô la**

### Trường Hợp Sử Dụng

- **Nền tảng API**: Truy cập cơ bản + phí theo yêu cầu
- **Dịch vụ AI/ML**: Đăng ký + sử dụng token/generation
- **Dịch vụ lưu trữ**: Kế hoạch cơ bản + phí vượt mức theo GB
- **Nền tảng giao tiếp**: Cơ sở + phí theo tin nhắn/phút

### Triển Khai

<Steps>
<Step title="Tạo Đồng Hồ Sử Dụng">
Thiết lập một đồng hồ để theo dõi chiều sử dụng có thể tính phí.

```bash
Dashboard: Meters → Create Meter
Event Name: "api.call"
Aggregation: Count
This tracks API calls per customer
```

</Step>

<Step title="Tạo Sản Phẩm Đăng Ký với Định Giá Sử Dụng">
Tạo một sản phẩm đăng ký và gắn đồng hồ sử dụng với định giá.

```bash
Dashboard: Create Product → Subscription
Name: "Pro Plan"
Base Price: $49/month

Then attach usage pricing:
- Meter: api.call
- Price per unit: $0.005
- Free threshold: 10,000 (included in subscription)
```

<Info>
Đồng hồ sử dụng được gắn trực tiếp vào sản phẩm đăng ký. Các khoản phí sử dụng được tính toán và thêm vào hóa đơn đăng ký tự động.
</Info>
</Step>

<Step title="Tạo Phiên Thanh Toán">
Tạo một phiên thanh toán với sản phẩm đăng ký của bạn.

```typescript
const session = await client.checkoutSessions.create({
  product_cart: [
    { product_id: 'pdt_pro_plan', quantity: 1 }
  ],
  customer: { email: 'customer@example.com' },
  return_url: 'https://yourapp.com/success'
});
```

</Step>

<Step title="Gửi Sự Kiện Sử Dụng">
Theo dõi việc sử dụng trong suốt thời gian thanh toán.

```typescript
await fetch('https://test.dodopayments.com/events/ingest', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${apiKey}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    events: [{
      event_id: `call_${Date.now()}`,
      customer_id: 'cus_123',
      event_name: 'api.call',
      timestamp: new Date().toISOString(),
      metadata: { endpoint: '/v1/generate' }
    }]
  })
});
```

</Step>
</Steps>

### Biến Thể Định Giá

<Tabs>
<Tab title="Khoản Cho Phép Bao Gồm">
Ngưỡng miễn phí bao gồm việc sử dụng được bao gồm trong đăng ký cơ bản.

**Kế Hoạch Chuyên Nghiệp: 49 đô la/tháng**
- Bao gồm: 10,000 cuộc gọi API
- Vượt mức: 0.005 đô la/cuộc gọi sau 10,000
- Khách hàng sử dụng 8,000 → Trả 49 đô la (không vượt mức)
</Tab>

<Tab title="Cơ Sở Không + Chỉ Sử Dụng">
Không có phí cơ bản, mỗi đơn vị đều có thể tính phí từ lần sử dụng đầu tiên.

**Trả Theo Mức Sử Dụng: 0 đô la/tháng cơ bản**
- Bao gồm: 0 cuộc gọi API
- Sử dụng: 0.01 đô la/cuộc gọi từ cuộc gọi đầu tiên
- Khách hàng sử dụng 5,000 → Trả 50 đô la
</Tab>

<Tab title="Khoản Cho Phép Tầng">
Các tầng khác nhau bao gồm các khoản cho phép khác nhau.

- **Khởi đầu**: 19 đô la/tháng (1,000 cuộc gọi bao gồm)
- **Chuyên nghiệp**: 49 đô la/tháng (10,000 cuộc gọi bao gồm)
- **Doanh nghiệp**: 199 đô la/tháng (100,000 cuộc gọi bao gồm)
- Tất cả các tầng: 0.005 đô la/cuộc gọi vượt mức
</Tab>
</Tabs>

---

## Mô Hình Lai 2: Đăng Ký + Ghế

Phí nền tảng cộng với phí theo người dùng. Lý tưởng cho các công cụ hợp tác nhóm và B2B SaaS.

### Cách Thức Hoạt Động

**Kế Hoạch Nhóm: 99 đô la/tháng + 15 đô la/ghế**

- **Phí nền tảng cơ bản**: 99 đô la/tháng (bao gồm 3 ghế)
- **Ghế bổ sung**: 15 đô la/ghế/tháng

**Tính toán ví dụ** (nhóm 12 người dùng):
- Phí nền tảng: 99.00 đô la
- Ghế bổ sung: (12 - 3) × 15 đô la = 135.00 đô la
- **Tổng: 234.00 đô la/tháng**

### Trường Hợp Sử Dụng

- **Công cụ hợp tác**: Phí không gian làm việc + theo thành viên
- **Hệ thống CRM**: Giấy phép nền tảng + theo đại diện bán hàng
- **Quản lý dự án**: Kế hoạch nhóm + theo người đóng góp
- **Công cụ phát triển**: Phí tổ chức + theo nhà phát triển

### Triển Khai

<Steps>
<Step title="Tạo Bổ Sung Ghế">
Tạo một bổ sung cho các ghế bổ sung.

```bash
Dashboard: Products → Add-ons → Create Add-on
Name: "Additional Seat"
Price: $15/month
Description: "Add another team member"
```

</Step>

<Step title="Tạo Đăng Ký Cơ Bản">
Tạo sản phẩm đăng ký với phí nền tảng và gắn bổ sung.

```bash
Dashboard: Create Product → Subscription
Name: "Team Plan"
Price: $99/month
Description: "Includes 3 team members"

Then in Add-ons section:
- Attach: "Additional Seat" add-on
```

</Step>

<Step title="Tạo Thanh Toán với Ghế">
Chỉ định số lượng ghế trong quá trình thanh toán.

```typescript
const session = await client.checkoutSessions.create({
  product_cart: [{
    product_id: 'pdt_team_plan',
    quantity: 1,
    addons: [{
      addon_id: 'addon_seat',
      quantity: 9  // 9 extra seats (12 total with 3 included)
    }]
  }],
  customer: { email: 'admin@company.com' },
  return_url: 'https://yourapp.com/success'
});
```

</Step>

<Step title="Điều Chỉnh Ghế Khi Cần Thiết">
Thêm hoặc xóa ghế trên các đăng ký hiện có.

```typescript
// Add 5 more seats
await client.subscriptions.changePlan('sub_123', {
  product_id: 'pdt_team_plan',
  quantity: 1,
  proration_billing_mode: 'prorated_immediately',
  addons: [{
    addon_id: 'addon_seat',
    quantity: 14  // New total: 14 extra seats
  }]
});
```

</Step>
</Steps>

### Biến Thể Định Giá

<Tabs>
<Tab title="Ghế Bao Gồm">
Kế hoạch cơ bản bao gồm một số ghế, tính phí cho các ghế bổ sung.

**Kế Hoạch Nhóm: 99 đô la/tháng**
- Bao gồm: 5 ghế
- Ghế bổ sung: 15 đô la/ghế/tháng
- 20 người dùng = \$99 + (15 × \$15) = \$324/tháng
</Tab>

<Tab title="Chỉ Theo Ghế">
Không có phí nền tảng, chỉ có phí theo người dùng.

**Theo Người Dùng: 25 đô la/người/tháng**
- Không có phí nền tảng
- 5 người dùng = 125 đô la/tháng
- 50 người dùng = 1,250 đô la/tháng

Triển khai: Đặt giá đăng ký cơ bản là 0 đô la, chỉ sử dụng bổ sung ghế.
</Tab>

<Tab title="Theo Ghế Tầng">
Giá theo ghế giảm ở các tầng cao hơn.

- **Khởi đầu**: 20 đô la/ghế (1-10 ghế)
- **Tăng trưởng**: 15 đô la/ghế (11-50 ghế)
- **Doanh nghiệp**: 10 đô la/ghế (51+ ghế)

Triển khai: Tạo các sản phẩm đăng ký riêng cho mỗi tầng với các mức giá bổ sung khác nhau.
</Tab>
</Tabs>

---

## Mô Hình Lai 3: Đăng Ký + Bổ Sung Tính Năng

Đăng ký cốt lõi với các nâng cấp tính năng tùy chọn mà khách hàng có thể thêm vào.

### Cách Thức Hoạt Động

**Kế Hoạch Cốt Lõi: 29 đô la/tháng**

**Bổ Sung Tùy Chọn:**
- Phân Tích Nâng Cao: +19 đô la/tháng
- Truy Cập API: +9 đô la/tháng
- Hỗ Trợ Ưu Tiên: +29 đô la/tháng
- Nhãn Trắng: +49 đô la/tháng

**Tính toán ví dụ** (khách hàng chọn Cốt Lõi + Phân Tích + Truy Cập API):
- Kế hoạch cốt lõi: 29.00 đô la
- Phân Tích: 19.00 đô la
- Truy Cập API: 9.00 đô la
- **Tổng: 57.00 đô la/tháng**

### Trường Hợp Sử Dụng

- **Nền tảng SaaS**: Tính năng cốt lõi + mô-đun cao cấp
- **Công cụ tiếp thị**: Công cụ cơ bản + tích hợp
- **Sản phẩm phân tích**: Bảng điều khiển + báo cáo nâng cao
- **Phần mềm bảo mật**: Bảo vệ cơ bản + tính năng nâng cao

### Triển Khai

<Steps>
<Step title="Tạo Bổ Sung Tính Năng">
Tạo một bổ sung cho mỗi tính năng tùy chọn.

```bash
# Add-on 1: Advanced Analytics
Dashboard: Products → Add-ons → Create Add-on
Name: "Advanced Analytics"
Price: $19/month

# Add-on 2: API Access
Name: "API Access"
Price: $9/month

# Add-on 3: Priority Support
Name: "Priority Support"
Price: $29/month

# Add-on 4: White-label
Name: "White-label"
Price: $49/month
```

</Step>

<Step title="Tạo Đăng Ký Cốt Lõi">
Định nghĩa đăng ký cơ bản của bạn và gắn tất cả các bổ sung tính năng.

```bash
Dashboard: Create Product → Subscription
Name: "Core Plan"
Price: $29/month

Then in Add-ons section:
- Attach all feature add-ons
```

</Step>

<Step title="Để Khách Hàng Chọn">
Thanh toán với các tính năng đã chọn.

```typescript
const session = await client.checkoutSessions.create({
  product_cart: [{
    product_id: 'pdt_core_plan',
    quantity: 1,
    addons: [
      { addon_id: 'addon_analytics', quantity: 1 },
      { addon_id: 'addon_api_access', quantity: 1 }
      // Customer didn't select support or white-label
    ]
  }],
  return_url: 'https://yourapp.com/success'
});
```

</Step>

<Step title="Thêm Tính Năng Sau">
Khách hàng có thể thêm tính năng vào các đăng ký hiện có.

```typescript
// Customer wants to add Priority Support
await client.subscriptions.changePlan('sub_123', {
  product_id: 'pdt_core_plan',
  quantity: 1,
  proration_billing_mode: 'prorated_immediately',
  addons: [
    { addon_id: 'addon_analytics', quantity: 1 },
    { addon_id: 'addon_api_access', quantity: 1 },
    { addon_id: 'addon_priority_support', quantity: 1 }  // New!
  ]
});
```

</Step>
</Steps>

---

## Mô Hình Lai 4: Ghế + Sử Dụng

Phí theo người dùng kết hợp với các khoản phí dựa trên tiêu thụ. Mỗi người dùng nhận được một khoản cho phép.

<Warning>
**Ràng buộc**: Dodo Payments hiện không hỗ trợ việc gắn cả đồng hồ sử dụng và bổ sung vào cùng một sản phẩm đăng ký. Mô hình này yêu cầu một giải pháp thay thế bằng cách sử dụng logic cấp ứng dụng.
</Warning>

### Cách thức hoạt động

**Phân tích nhóm: $20/người dùng/tháng**

**Mỗi người dùng bao gồm:**
- 5 GB xử lý dữ liệu/tháng
- Vượt mức: $2/GB sau phần hạn mức

**Ví dụ tính toán** (nhóm 10 người dùng sử dụng tổng cộng 80 GB):
- Phí seat: 10 × $20 = $200.00
- Dữ liệu bao gồm: 10 × 5 GB = 50 GB
- Vượt mức: (80 - 50) × $2 = $60.00
- **Tổng: $260.00/tháng**

### Trường hợp sử dụng

- **Nền tảng phân tích**: Mỗi chuyên viên phân tích + xử lý dữ liệu
- **Công cụ thiết kế**: Mỗi nhà thiết kế + dung lượng lưu trữ/xuất dữ liệu
- **Môi trường phát triển**: Mỗi nhà phát triển + số giờ compute
- **Công cụ giao tiếp**: Mỗi người dùng + số lượng tin nhắn/cuộc gọi

### Các tùy chọn triển khai

Vì bạn không thể gắn cả usage meters và add-ons vào cùng một subscription, hãy chọn một trong các phương án sau:

<Tabs>
<Tab title="Option A: Usage Product + App-Managed Seats">
Sử dụng subscription dựa trên mức sử dụng và quản lý việc tính phí seat trong ứng dụng của bạn.

<Steps>
<Step title="Create Usage Meter">

```bash
Dashboard: Meters → Create Meter
Event Name: "data.processed"
Aggregation: Sum
Property: "bytes"
```

</Step>

<Step title="Create Usage-Based Subscription">

```bash
Dashboard: Create Product → Subscription
Name: "Team Analytics"
Base Price: $0/month

Attach usage pricing:
- Meter: data.processed
- Price per unit: $2/GB
- Free threshold: 0 (managed by your app)
```

</Step>

<Step title="Manage Seats in Your Application">
Theo dõi số lượng seat và tính phí seat riêng.

```typescript
// Your application tracks seats and calculates total cost
async function calculateMonthlyBill(customerId: string) {
  const seatCount = await getSeatCount(customerId);
  const seatFee = seatCount * 20; // $20/seat

  // Usage is billed by Dodo automatically
  // You invoice/charge seat fees separately or include in base price

  // Alternatively, adjust base subscription price when seats change
  const totalBasePrice = seatCount * 2000; // $20/seat in cents
  await client.subscriptions.update('sub_123', {
    // Update subscription to reflect seat-based pricing
  });
}
```

</Step>

<Step title="Calculate Dynamic Free Threshold">
Điều chỉnh mức sử dụng bao gồm dựa trên số lượng seat.

```typescript
// When checking usage, apply per-seat allowance
async function checkUsageOverage(customerId: string) {
  const seatCount = await getSeatCount(customerId);
  const includedGB = seatCount * 5; // 5 GB per user

  const currentUsage = await getUsageFromDodo(customerId);
  const overage = Math.max(0, currentUsage - includedGB);

  // Overage is billed by Dodo at $2/GB
  return { included: includedGB, used: currentUsage, overage };
}
```

</Step>
</Steps>
</Tab>

<Tab title="Option B: Seat Add-on + On-Demand Usage Charges">
Sử dụng add-ons cho seat và tính phí usage thủ công thông qua on-demand charges.

<Steps>
<Step title="Create Seat Add-on">

```bash
Dashboard: Products → Add-ons → Create Add-on
Name: "Team Member"
Price: $20/month
```

</Step>

<Step title="Create Subscription with Add-on">

```bash
Dashboard: Create Product → Subscription
Name: "Team Analytics"
Base Price: $0/month

Attach add-on:
- "Team Member" add-on

Enable on-demand charging
```

</Step>

<Step title="Track Usage in Your Application">

```typescript
// Track usage events in your system
async function trackDataProcessed(customerId: string, bytes: number) {
  await saveUsageEvent({
    customer_id: customerId,
    event_type: 'data.processed',
    bytes: bytes,
    timestamp: new Date()
  });
}
```

</Step>

<Step title="Charge Usage at End of Cycle">
Tính toán và tính phí phần usage vượt mức thủ công.

```typescript
async function billUsageOverage(subscriptionId: string) {
  const subscription = await getSubscription(subscriptionId);
  const seatCount = subscription.addons.find(a => a.id === 'addon_seat')?.quantity || 0;

  const includedGB = seatCount * 5;
  const usedGB = await calculatePeriodUsage(subscription.customer_id);
  const overageGB = Math.max(0, usedGB - includedGB);

  if (overageGB > 0) {
    const overageCharge = overageGB * 200; // $2/GB in cents
    await client.subscriptions.charge(subscriptionId, {
      product_price: overageCharge,
      product_description: `Data overage: ${overageGB} GB × $2/GB`
    });
  }
}
```

</Step>
</Steps>
</Tab>
</Tabs>

<Info>
**Khuyến nghị**: Phương án B (Seat Add-on + On-Demand Usage) thường dễ triển khai hơn vì Dodo tự động xử lý việc tính phí seat. Bạn chỉ cần theo dõi và tính phí phần usage vượt mức.
</Info>

---

## Mô hình Hybrid 5: Subscription + Seats + Usage (Triple Hybrid)

Mô hình toàn diện nhất: phí nền tảng + phí theo người dùng + mức tiêu thụ.

<Warning>
**Hạn chế**: Dodo Payments hiện chưa hỗ trợ gắn cả usage meters và add-ons vào cùng một subscription product. Mô hình này yêu cầu một phương án workaround.
</Warning>

### Cách thức hoạt động

**Nền tảng Enterprise**

- **Phí nền tảng**: $199/tháng
- **Mỗi seat**: $25/người dùng/tháng
- **Usage**: $0.10/1000 API calls (bao gồm 50K)

**Ví dụ tính toán** (công ty có 20 người dùng, 150.000 API calls):
- Nền tảng: $199.00
- Seat: 20 × $25 = $500.00
- Usage: (150K - 50K) × \$0.10/1K = \$10.00
- **Tổng: $709.00/tháng**

### Trường hợp sử dụng

- **Enterprise SaaS**: Nền tảng + nhóm + mức tiêu thụ
- **Nền tảng dữ liệu**: Workspace + chuyên viên phân tích + truy vấn
- **Nền tảng tích hợp**: Hub + connector + giao dịch
- **Nền tảng AI**: Workspace + nhà phát triển + inference

### Các tùy chọn triển khai

Chọn một trong các phương án sau để triển khai tính phí triple hybrid:

<Tabs>
<Tab title="Option A: Base + Seats (Add-on) + On-Demand Usage">
Sử dụng subscription với seat add-ons, tính phí usage thủ công thông qua on-demand charges.

**Đây là phương án được khuyến nghị** vì Dodo tự động xử lý phí nền tảng và tính phí seat.

<Steps>
<Step title="Create Seat Add-on">

```bash
Dashboard: Products → Add-ons → Create Add-on
Name: "User Seat"
Price: $25/month
```

</Step>

<Step title="Create Subscription Product">

```bash
Dashboard: Create Product → Subscription
Name: "Enterprise Platform"
Base Price: $199/month

Attach add-on:
- "User Seat" add-on

Enable on-demand charging
```

</Step>

<Step title="Create Checkout with Seats">

```typescript
const session = await client.checkoutSessions.create({
  product_cart: [{
    product_id: 'pdt_enterprise_platform',
    quantity: 1,
    addons: [{
      addon_id: 'addon_user_seat',
      quantity: 20  // 20 users
    }]
  }],
  customer: { email: 'enterprise@company.com' },
  return_url: 'https://yourapp.com/success'
});
```

</Step>

<Step title="Track Usage in Your Application">
Lưu các usage events trong hệ thống của bạn.

```typescript
// Track API calls in your system
async function trackApiCall(customerId: string, endpoint: string) {
  await saveUsageEvent({
    customer_id: customerId,
    event_type: 'api.call',
    endpoint: endpoint,
    timestamp: new Date()
  });
}
```

</Step>

<Step title="Charge Usage at End of Cycle">
Tính toán và tính phí phần usage vượt mức thông qua on-demand charge.

```typescript
async function billUsageOverage(subscriptionId: string) {
  const usage = await calculatePeriodUsage(subscriptionId);
  const includedCalls = 50000;
  const overageCalls = Math.max(0, usage.totalCalls - includedCalls);

  if (overageCalls > 0) {
    // $0.10 per 1000 calls = $0.0001 per call
    const overageCharge = Math.ceil(overageCalls / 1000) * 10; // cents
    await client.subscriptions.charge(subscriptionId, {
      product_price: overageCharge,
      product_description: `API usage: ${overageCalls.toLocaleString()} calls over 50K included`
    });
  }
}
```

</Step>
</Steps>
</Tab>

<Tab title="Option B: Base + Usage (Meter) + App-Managed Seats">
Sử dụng subscription với usage meter, quản lý việc tính phí seat trong ứng dụng của bạn.

<Steps>
<Step title="Create Usage Meter">

```bash
Dashboard: Meters → Create Meter
Event Name: "api.call"
Aggregation: Count
```

</Step>

<Step title="Create Subscription Product with Usage">

```bash
Dashboard: Create Product → Subscription
Name: "Enterprise Platform"
Base Price: $199/month

Attach usage pricing:
- Meter: api.call
- Price: $0.10 per 1000 calls
- Free threshold: 50,000
```

</Step>

<Step title="Manage Seats in Your Application">
Theo dõi số lượng seat và điều chỉnh giá subscription cơ bản tương ứng.

```typescript
// When seats change, update subscription price
async function updateSeatCount(subscriptionId: string, newSeatCount: number) {
  const basePlatformFee = 19900; // $199 in cents
  const perSeatFee = 2500; // $25 in cents
  const totalPrice = basePlatformFee + (newSeatCount * perSeatFee);

  // Store seat count in your system
  await updateSeatsInDatabase(subscriptionId, newSeatCount);

  // Note: You may need to handle this via plan changes or
  // create multiple tier products for common seat counts
}
```

</Step>

<Step title="Send Usage Events to Dodo">

```typescript
await fetch('https://test.dodopayments.com/events/ingest', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${apiKey}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    events: [{
      event_id: `api_${Date.now()}`,
      customer_id: 'cus_enterprise',
      event_name: 'api.call',
      timestamp: new Date().toISOString()
    }]
  })
});
```

</Step>
</Steps>
</Tab>
</Tabs>

<Info>
**Khuyến nghị**: Phương án A (Base + Seats + On-Demand Usage) thường dễ hơn vì Dodo tự động xử lý việc tính phí nền tảng và seat. Bạn chỉ cần theo dõi usage và gửi các khoản phí vào cuối mỗi billing cycle.
</Info>

---

## Mô hình Hybrid 6: Base theo bậc + Usage vượt mức

Các tier subscription khác nhau với hạn mức bao gồm và mức phí vượt hạn mức khác nhau.

### Cách thức hoạt động

| Tier | Giá | Calls bao gồm | Mức phí vượt mức |
|------|-------|----------------|--------------|
| **Starter** | $19/tháng | 1,000 | $0.02/call |
| **Pro** | $79/tháng | 25,000 | $0.01/call |
| **Business** | $199/tháng | 100,000 | $0.005/call |
| **Enterprise** | $499/tháng | 500,000 | $0.002/call |

### Triển khai

Tạo các subscription products riêng cho từng tier, mỗi product có cấu hình usage riêng:

```bash
# For each tier, create a subscription product:

# Starter Tier
Dashboard: Create Product → Subscription
Name: "Starter"
Base Price: $19/month
Usage Pricing:
- Meter: api.call
- Price: $0.02/call
- Free threshold: 1,000

# Pro Tier
Name: "Pro"
Base Price: $79/month
Usage Pricing:
- Meter: api.call
- Price: $0.01/call
- Free threshold: 25,000

# ... and so on for Business and Enterprise
```

### Lộ trình nâng cấp

Khi khách hàng nâng cấp tier, họ sẽ nhận được:
- Hạn mức bao gồm cao hơn
- Mức phí vượt hạn mức thấp hơn
- Giá trị cao hơn trên mỗi dollar

```typescript
// Customer upgrades from Starter to Pro
await client.subscriptions.changePlan('sub_123', {
  product_id: 'pdt_pro',
  quantity: 1,
  proration_billing_mode: 'prorated_immediately'
});
```

---

## Mô hình Hybrid 7: Subscription + On-Demand Charges

Subscription định kỳ cộng với các khoản phí thủ công biến động cho dịch vụ hoặc phần usage vượt mức.

### Cách thức hoạt động

**Gói Retainer: $199/tháng**

**Bao gồm:**
- Quyền truy cập nền tảng
- 5 giờ tư vấn/tháng
- Hỗ trợ qua email

**On-Demand Charges (khi cần):**
- Tư vấn thêm: $150/giờ
- Phát triển tùy chỉnh: $200/giờ
- Hỗ trợ khẩn cấp: $100/sự cố

**Ví dụ tính toán** (tháng này):
- Retainer: $199.00
- 3 giờ tư vấn thêm: $450.00
- 1 lần hỗ trợ khẩn cấp: $100.00
- **Tổng: $749.00**

### Trường hợp sử dụng

- **Dịch vụ tư vấn**: Retainer + tính phí theo giờ
- **Dịch vụ được quản lý**: Phí cơ bản + phí theo sự cố
- **Dịch vụ agency**: Phí hàng tháng + phí dự án
- **Gói hỗ trợ**: Phí SLA + phí theo ticket hoặc theo giờ

### Triển khai

<Steps>
<Step title="Create On-Demand Subscription">
Thiết lập subscription với tính năng on-demand charging được bật.

```typescript
const subscription = await client.subscriptions.create({
  billing: {
    city: 'San Francisco',
    country: 'US',
    state: 'CA',
    street: '123 Main St',
    zipcode: '94105'
  },
  customer: { customer_id: 'cus_123' },
  product_id: 'pdt_retainer',
  quantity: 1,
  payment_link: true,
  return_url: 'https://yourapp.com/success',
  on_demand: {
    mandate_only: false,
    product_price: 19900  // $199 initial charge
  }
});
```

</Step>

<Step title="Charge for Services">
Tạo charges khi dịch vụ được cung cấp.

```typescript
// Charge for 3 hours of consulting
await client.subscriptions.charge('sub_123', {
  product_price: 45000,  // $450.00 (3 × $150)
  product_description: 'Consulting - 3 hours (March 15)'
});

// Charge for emergency support incident
await client.subscriptions.charge('sub_123', {
  product_price: 10000,  // $100.00
  product_description: 'Emergency support - Server outage (March 18)'
});
```

</Step>

<Step title="Track and Invoice">
Tất cả charges sẽ xuất hiện trên invoice của khách hàng.

```typescript
// Retrieve subscription charges
const payments = await client.payments.list({
  subscription_id: 'sub_123'
});

// Show itemized breakdown to customer
payments.items.forEach(payment => {
  console.log(`${payment.payment_id}: $${payment.total_amount / 100}`);
});
```

</Step>
</Steps>

---

## Ví dụ thực tế

<Info>
Các ví dụ này thể hiện những cấu trúc định giá lý tưởng. Do hạn chế là usage meters và add-ons không thể được gắn vào cùng một product, một số kết hợp cần workaround (sử dụng on-demand charges cho usage hoặc quản lý seat trong ứng dụng).
</Info>

### Ví dụ 1: Nền tảng AI SaaS

**Cấu trúc định giá:**
- **Base Subscription**: $99/tháng (quyền truy cập nền tảng, bao gồm 5 seat)
- **Seat Add-on**: $20/seat/tháng
- **Feature Add-ons**: Custom Models (\$49/tháng), API Access (\$29/tháng), Priority Queue (\$19/tháng)
- **Usage vượt mức**: $0.02 cho mỗi 1.000 token sau 100K (tính phí qua on-demand)

**Triển khai**: Sử dụng subscription với seat và feature add-ons. Theo dõi token usage trong ứng dụng và tính phí phần vượt mức qua on-demand charges vào cuối billing cycle.

**Khách hàng ví dụ** (12 người dùng, 500K token, Custom Models + API Access):
| Thành phần | Tính toán | Số tiền |
|-----------|-------------|--------|
| Base | Phí nền tảng | $99 |
| Seat bổ sung | 7 × $20 | $140 |
| Custom Models | Add-on | $49 |
| API Access | Add-on | $29 |
| Token vượt mức | 400K × \$0.02/1K (on-demand) | \$8 |
| **Tổng** | | **$325/tháng** |

### Ví dụ 2: Nền tảng công cụ dành cho nhà phát triển

**Tùy chọn tier:**

| | Free | Pro | Enterprise |
|---|------|-----|------------|
| **Giá** | $0/tháng | $29/tháng | $199/tháng |
| **Người dùng** | 1 | Bao gồm 5 | Không giới hạn |
| **Builds** | 100 | 1,000 | 10,000 |
| **Storage** | 1 GB | 10 GB | 100 GB |

**Tùy chọn triển khai**:

**Phương án A** (Tập trung vào Usage): Tạo các product với usage meters cho builds/storage. Quản lý người dùng trong ứng dụng của bạn.

**Phương án B** (Tập trung vào Seat): Tạo các product với seat add-ons. Theo dõi usage của builds/storage và tính phí phần vượt mức qua on-demand.

**Add-ons (nếu sử dụng Phương án B):**
- Người dùng bổ sung: $10/người dùng/tháng
- Builds ưu tiên: $19/tháng
- Custom domains: $9/domain/tháng

### Ví dụ 3: Tự động hóa marketing

**Cấu trúc định giá:**
- **Base**: $79/tháng (các tính năng tự động hóa cốt lõi, bao gồm 3 seat)
- **Contact Tiers** (add-ons): Bao gồm 1K, 5K (+\$30), 25K (+\$80), 100K (+\$200)
- **Feature Add-ons**: SMS Marketing (\$29/tháng), Landing Pages (\$19/tháng), A/B Testing (\$29/tháng)
- **Team Seats**: Add-on $15/người dùng/tháng
- **Email Volume**: Theo dõi trong ứng dụng, tính phí phần vượt mức qua on-demand ($1/1.000 email vượt giới hạn)

**Triển khai**: Sử dụng subscription với contact tier add-ons, feature add-ons và seat add-ons. Theo dõi số email gửi trong ứng dụng và tính phí phần vượt mức qua on-demand charges.

---

## Các phương pháp hay nhất khi triển khai

### Tính rõ ràng trên trang định giá

<Tip>
Giúp mô hình định giá hybrid dễ hiểu. Hiển thị nổi bật chi phí cơ bản, các nội dung được bao gồm và cách tính usage vượt mức trên trang định giá.
</Tip>

**Tốt**: "\$49/tháng bao gồm 10.000 API calls. Calls bổ sung: \$0.005 mỗi call"

**Không tốt**: "\$49/tháng + phí usage"

### Khả năng dự đoán chi phí

Giúp khách hàng ước tính chi phí của họ:

```typescript
// Provide a cost calculator
function estimateMonthlyCost({
  plan,
  seats,
  expectedUsage,
  addons
}: EstimateParams): number {
  let total = plan.basePrice;

  // Add seat costs
  const extraSeats = Math.max(0, seats - plan.includedSeats);
  total += extraSeats * plan.seatPrice;

  // Add usage overage
  const overage = Math.max(0, expectedUsage - plan.includedUsage);
  total += overage * plan.overageRate;

  // Add feature add-ons
  total += addons.reduce((sum, addon) => sum + addon.price, 0);

  return total;
}
```

### Khả năng quan sát Usage

Hiển thị usage của khách hàng theo thời gian thực:

```typescript
// Display usage dashboard
async function getUsageSummary(subscriptionId: string) {
  const usage = await client.subscriptions.retrieveUsageHistory(subscriptionId);

  // Each item is a billing period; each meter reports its usage for that period.
  const latestPeriod = usage.items[0];

  return latestPeriod.meters.map((meter) => {
    const consumed = Number(meter.consumed_units);
    const chargeable = Number(meter.chargeable_units);
    return {
      meter: meter.name,
      current: consumed,
      included: meter.free_threshold,
      remaining: Math.max(0, meter.free_threshold - consumed),
      overage: chargeable,
      cost: meter.total_price
    };
  });
}
```

### Tính minh bạch trong tính phí

Cung cấp invoice chi tiết hiển thị tất cả thành phần:

| Hạng mục | Số tiền |
|-----------|--------|
| Gói Pro (hàng tháng) | $49.00 |
| Seat bổ sung (7 × $15.00) | $105.00 |
| API Usage - Bao gồm (10.000 calls) | $0.00 |
| API Usage - Vượt mức (15.420 calls × $0.005) | $77.10 |
| Add-on Advanced Analytics | $19.00 |
| **Tạm tính** | **$250.10** |
| Thuế (8.5%) | $21.26 |
| **Tổng phải trả** | **$271.36** |

---

## Khắc phục sự cố tính phí Hybrid

<AccordionGroup>
<Accordion title="Usage not being tracked correctly">
**Triệu chứng**: Usage hiển thị 0 hoặc giá trị không chính xác.

**Giải pháp**:
1. Xác minh event ingestion đang hoạt động (kiểm tra API responses)
2. Xác nhận `customer_id` khớp với subscription customer
3. Kiểm tra `event_name` khớp với meter configuration
4. Xác minh events có timestamp chính xác (không được đặt trong tương lai)
</Accordion>

<Accordion title="Proration confusion with multiple components">
**Triệu chứng**: Khách hàng bị tính các khoản tiền không 예상 trước khi thay đổi plan.

**Giải pháp**:
1. Sử dụng `previewChangePlan` API để hiển thị chính xác các khoản phí trước khi xác nhận
2. Thông báo rằng proration áp dụng cho cả subscription VÀ add-ons
3. Cân nhắc sử dụng `difference_immediately` để đơn giản hóa việc tính phí khi nâng cấp
</Accordion>

<Accordion title="Free threshold not applying correctly">
**Triệu chứng**: Khách hàng bị tính phí cho usage lẽ ra được miễn phí.

**Giải pháp**:
1. Xác minh free threshold được cấu hình trên usage-based product
2. Kiểm tra threshold unit khớp với event aggregation (calls so với requests)
3. Xác nhận usage meter được gắn chính xác vào subscription product
</Accordion>

<Accordion title="Add-ons not appearing in checkout">
**Triệu chứng**: Không thể thêm seat hoặc feature trong quá trình checkout.

**Giải pháp**:
1. Xác minh add-ons được gắn vào subscription product trong dashboard
2. Kiểm tra add-on IDs chính xác trong API calls
3. Đảm bảo currency của add-on khớp với currency của subscription product
</Accordion>
</AccordionGroup>

---

## Tài liệu liên quan

<CardGroup cols={2}>
<Card title="Products" icon="box" href="/features/products">
  Tổng quan về tất cả loại product và hướng dẫn.
</Card>

<Card title="Usage-Based Billing Guide" icon="chart-line" href="/developer-resources/usage-based-billing-guide">
  Hướng dẫn triển khai hoàn chỉnh về tính phí usage.
</Card>

<Card title="Subscription Management" icon="repeat" href="/features/subscription">
  Quản lý các subscription định kỳ.
</Card>

<Card title="Add-ons" icon="puzzle" href="/features/addons">
  Mở rộng subscription bằng add-ons.
</Card>
</CardGroup>
