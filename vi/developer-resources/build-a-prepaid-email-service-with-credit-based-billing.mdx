---
title: Xây dựng Dịch Vụ Email Trả Trước với Thanh Toán Dựa Trên Tín Dụng
description: Xây dựng MailKit, một nền tảng email giao dịch với các tín dụng
  email trả trước. Bao gồm gói thuê bao hàng tháng, gói nạp tiền một lần, khấu
  trừ tín dụng ngay lập tức mỗi khi gửi, và cảnh báo sớm khi tín dụng thấp được
  hỗ trợ bởi Resend và Dodo Payments.
icon: envelope
keywords:
  - Dodo Payments
  - hướng dẫn nhà phát triển
  - tính phí dựa trên tín dụng
  - email giao dịch
  - Resend
  - tín dụng trả trước
---

<Tip>
  <strong>Hãy để Sentra viết mã tích hợp cho bạn.</strong><br />
  Sử dụng trợ lý AI của chúng tôi trong VS Code, Cursor hoặc Windsurf để tạo mã SDK/API, xử lý webhook, và hơn thế nữa, chỉ bằng cách mô tả điều bạn muốn.
  <a href="https://dodopayments.com/sentra" target="_blank" rel="noopener noreferrer" >
    Thử Sentra: Tích Hợp Sử Dụng AI &rarr;
  </a>
</Tip>

Trong hướng dẫn này, bạn sẽ xây dựng **MailKit**, một nền tảng email giao dịch nơi khách hàng trả trước cho một nhóm tín dụng email. Gói này cấp một hạn mức email hàng tháng; khi khách hàng gần như hết, họ có thể mua thêm một gói nạp tiền thay vì chờ đến chu kỳ tiếp theo. Mỗi lần gửi sẽ tự động trừ một tín dụng.

<Note>
Hướng dẫn này sử dụng [Resend](https://resend.com) làm nhà cung cấp email. Gói miễn phí (3.000 email/tháng) đủ để xây dựng và kiểm tra toàn bộ luồng mà không cần tài khoản trả phí. Mẫu này hoạt động với bất kỳ nhà cung cấp nào; hoán đổi `resend.emails.send` với SendGrid, Postmark, SES, hoặc SMTP relay của riêng bạn.
</Note>

Kết thúc hướng dẫn này, bạn sẽ biết cách:

- Tạo quyền tín dụng tùy chỉnh (emails) trong bảng điều khiển của bạn
- Gắn tín dụng vào gói thuê bao và sản phẩm nạp tiền một lần
- Gửi email thực qua Resend và trừ một tín dụng mỗi lần gửi qua mục nhập ledger
- Truy vấn số dư tín dụng trực tuyến từ frontend của bạn
- Xác minh đúng webhook Dodo và xử lý `credit.balance_low` để nhắc nhở khách hàng trước khi họ đạt mức 0

## Những Gì Chúng Ta Xây Dựng

Đây là mô hình giá cho MailKit:

| Sản phẩm | Giá | Emails |
|---------|-------|--------|
| Gói MailKit | $19/tháng | 5.000 emails/chu kỳ |
| Gói Nạp Tiền | $9 một lần | +5.000 emails |

Đơn vị là **một email = một tín dụng**. Khách hàng không cần nghĩ về token, batch, hay đơn vị trọng số. Họ chỉ thấy "bạn còn 4.231 emails trong tháng này."

<Info>
Trước khi bạn bắt đầu, hãy chắc chắn rằng bạn có:
- Một tài khoản Dodo Payments (chế độ thử nghiệm là đủ)
- Một tài khoản [Resend](https://resend.com) miễn phí và khóa API
- Node.js 18+ và quen thuộc với TypeScript cơ bản
</Info>

## Bước 1: Tạo Quyền Tín Dụng Email của Bạn

Quyền tín dụng xác định đơn vị nền tảng của bạn bán: trong trường hợp này, là một lần gửi email.

<Frame caption="The Credits tab under Products lists all your credit entitlements.">
<img src="/images/CBB/Desktop - Cookbook - Credits.png" alt="Trang danh sách tín dụng" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

<Steps>
<Step title="Open the Credits section">
1. Đăng nhập vào bảng điều khiển Dodo Payments của bạn
2. Nhấp vào **Sản phẩm** ở thanh bên trái
3. Chọn tab **Tín Dụng**
4. Nhấp vào **Tạo Tín Dụng**
</Step>

<Step title="Configure the credit unit">
Điền vào các chi tiết tín dụng:

**Tên Tín Dụng**: `Email Credits`

**Loại Tín Dụng**: Chọn **Đơn Vị Tùy Chỉnh**

**Tên Đơn Vị**: `email`

**Độ chính xác**: `0` (một email luôn là một đơn vị hoàn chỉnh; bạn không thể gửi nửa email)

**Hạn Sử Dụng Tín Dụng**: `30 days` (hạn mức mỗi chu kỳ được đặt lại)

<Warning>
Độ chính xác không thể thay đổi sau khi tạo. Đối với các đơn vị rời rạc như emails, tin nhắn, hoặc phiên, `0` là chính xác.
</Warning>
</Step>

<Step title="Leave the other defaults as-is">
Chúng tôi sẽ không bật rollover hoặc overage trong sách này; mục tiêu là luồng CBB đơn giản nhất có thể. Bạn có thể xem lại điều này trên phần liên kết tín dụng sau.
</Step>

<Step title="Save and copy the credit ID">
Nhấp vào **Tạo Tín Dụng**. Mở tín dụng và sao chép ID của nó. Bạn sẽ cần nó cho các truy vấn số dư ở backend. Nó trông như `cent_xxxxxxxxxxxx`.
<Check>
Quyền `Email Credits` của bạn đã sẵn sàng. Tiếp theo: các sản phẩm mà cấp tín dụng cho khách hàng.
</Check>
</Step>
</Steps>

## Bước 2: Tạo Gói và Gói Nạp Tiền

Bạn sẽ tạo hai sản phẩm: một gói thuê bao **Định Kỳ** và một gói nạp tiền **Thanh Toán Một Lần**. Gói định kỳ cấp 5.000 emails mỗi chu kỳ; gói nạp tiền thêm 5.000 khi cần. Cả hai đều gắn quyền `Email Credits` như nhau.

/* LOCKED_PATTERN_317ec56569e36d0c9e56c2648890a76e */
Sách này trừ tín dụng thông qua mục nhập ledger trực tiếp thay vì các bộ đo dựa trên mức sử dụng. Mục nhập ledger là ngay lập tức (số dư cập nhật trong mili giây), không cần thiết lập thêm, và rất phù hợp khi một hành động của người dùng bằng đúng một tín dụng. Nếu bạn thích tự động trừ từ các sự kiện sử dụng thu được (tiện với các đơn vị có trọng số như "tokens" hoặc "MB processed"), hãy xem [Tính Phí Dựa Trên Tín Dụng → Thanh Toán Sử Dụng Với Tín Dụng](/features/credit-based-billing) cho mẫu dựa trên đồng hồ đo.
/* LOCKED_PATTERN_4dec52ce04aa8849a8a60508baae30ae */

### Gói MailKit ($19/tháng, 5.000 emails)

/* LOCKED_PATTERN_bd2642ca094c2e4ce733f0386172be2d */
/* LOCKED_PATTERN_f42d5a3b7b669b3d13a76cba7da9019d */
1. Đi tới **Sản phẩm → Tạo Sản Phẩm**
2. Điền thông tin sản phẩm:

**Tên Sản Phẩm**: `MailKit Plan`

**Mô tả**: `5,000 transactional emails per month.`

3. Chọn **Thuê Bao** làm loại sản phẩm
4. Đặt giá định kỳ:

**Giá Định Kỳ**: `19.00`

**Chu Kỳ Thanh Toán**: `Monthly`

**Tiền Tệ**: `USD`
</Step>

/* LOCKED_PATTERN_1057f8df8096efbe13a6d346959c0990 */
Cuộn xuống **Quyền Hạn → Tín Dụng → Đính Kèm** và cấu hình:

**Quyền Tín Dụng**: `Email Credits`

**Tín Dụng được phát hành mỗi chu kỳ thanh toán**: `5000`

**Ngưỡng Số Dư Thấp**: `20` (phần trăm; kích hoạt `credit.balance_low` khi số dư giảm dưới 20% của hạn mức chu kỳ, tức là 1.000 emails)

**Nhập Cài Đặt Tín Dụng Mặc Định**: bật (sử dụng hạn 30 ngày từ Bước 1)

Nhấp vào **Thêm vào Sản Phẩm**, sau đó **Lưu** sản phẩm. Sao chép ID sản phẩm (`pdt_xxxxxxxxxxxx`).

/* LOCKED_PATTERN_3c8e9103f1fb437472f20f9142ad5c56 */
Gói: $19/tháng → 5.000 emails được làm mới mỗi chu kỳ.
</Check>
</Step>
</Steps>

### Gói Nạp Tiền ($9 một lần, 5.000 emails)

/* LOCKED_PATTERN_bd2642ca094c2e4ce733f0386172be2d */
/* LOCKED_PATTERN_c5a84493a1db0067f1776bc768c5067d */
1. Đi tới **Sản phẩm → Tạo Sản Phẩm**
2. Điền thông tin sản phẩm:

**Tên Sản Phẩm**: `Email Top-Up Pack`

**Mô tả**: `Add 5,000 emails to your MailKit balance instantly.`

3. Chọn **Thanh Toán Một Lần** làm loại sản phẩm
4. Đặt giá:

**Giá**: `9.00`

**Tiền Tệ**: `USD`
</Step>

/* LOCKED_PATTERN_d9a76bfa3afc3ce5a94ba49dee10c8b4 */
Trong mục **Quyền Hạn → Tín Dụng → Đính Kèm**:
- Quyền Tín Dụng: `Email Credits`
- Tín Dụng được phát hành: `5000`

/* LOCKED_PATTERN_6fa96040307d68e9fa44436559d63ee8 */
Sản phẩm một lần cấp tín dụng với hạn dùng riêng của chúng (30 ngày từ khi mua, theo Bước 1). Các gói nạp tiền chồng lên tín dụng thuê bao; chúng không thay thế.

Lưu và sao chép ID sản phẩm.

/* LOCKED_PATTERN_3c8e9103f1fb437472f20f9142ad5c56 */
Gói Nạp Tiền: $9 → +5.000 emails, có sẵn ngay lập tức.
</Check>
</Step>
</Steps>

## Bước 3: Thiết Lập Backend

Bây giờ xây dựng server Express xử lý checkout, gửi, truy vấn số dư, và webhook.

/* LOCKED_PATTERN_bd2642ca094c2e4ce733f0386172be2d */
/* LOCKED_PATTERN_bf4cf6a5db44cb7e58524a05e1f32e31 */

```bash
mkdir mailkit && cd mailkit
npm init -y
npm install dodopayments resend express dotenv
npm install -D tsx @types/node @types/express
```

Thêm script dev vào `package.json`:

```json
{
  "scripts": {
    "dev": "tsx watch server.ts"
  }
}
```

/* LOCKED_PATTERN_317ec56569e36d0c9e56c2648890a76e */
[`tsx`](https://tsx.is) chạy TypeScript trực tiếp mà không cần bước build hoặc `tsconfig.json`, điều này rất phù hợp cho hướng dẫn. Đối với sản xuất, thêm `tsconfig.json` và script `build`.
</Tip>
</Step>

/* LOCKED_PATTERN_0c512f5b7e825fa7ca5d28ab2bd66cb0 */
Tạo `.env`:

```bash .env
# Dodo Payments
DODO_PAYMENTS_API_KEY=your_dodo_test_api_key
DODO_WEBHOOK_KEY=your_dodo_webhook_signing_key
CREDIT_ENTITLEMENT_ID=cent_xxxxxxxxxxxx
PLAN_PRODUCT_ID=pdt_xxxxxxxxxxxx
TOPUP_PRODUCT_ID=pdt_xxxxxxxxxxxx

# Resend
RESEND_API_KEY=re_xxxxxxxxxxxx

# App
BASE_URL=http://localhost:3000
PORT=3000
```

Bạn sẽ điền vào `DODO_WEBHOOK_KEY` trong Bước 4 sau khi tạo endpoint. Khóa API Resend lấy từ [resend.com/api-keys](https://resend.com/api-keys).

/* LOCKED_PATTERN_4907e9f6f7fbd509120d7a87afc829e9 */
Thêm `.env` vào `.gitignore` ngay lập tức. Đừng bao giờ commit khóa API.
</Warning>
</Step>

/* LOCKED_PATTERN_5117af3ef391312fabfae217baf864b0 */
Tạo `server.ts` trong gốc dự án:

/* LOCKED_PATTERN_278c8ff892f265e3e925879c7aa762a2 */

```typescript server.ts expandable
import 'dotenv/config';
import express, { Request, Response } from 'express';
import DodoPayments from 'dodopayments';
import { Resend } from 'resend';

const app = express();

const dodo = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY!,
  webhookKey: process.env.DODO_WEBHOOK_KEY!,
  environment: 'test_mode',
});

const resend = new Resend(process.env.RESEND_API_KEY!);

const CREDIT_ENTITLEMENT_ID = process.env.CREDIT_ENTITLEMENT_ID!;
const BASE_URL = process.env.BASE_URL!;

// ---------------------------------------------------------------
// Webhook endpoint MUST receive the raw body for signature
// verification. Register it BEFORE express.json().
// ---------------------------------------------------------------
app.post(
  '/webhooks/dodo',
  express.raw({ type: 'application/json' }),
  async (req: Request, res: Response) => {
    const headers = {
      'webhook-id': req.headers['webhook-id'] as string,
      'webhook-signature': req.headers['webhook-signature'] as string,
      'webhook-timestamp': req.headers['webhook-timestamp'] as string,
    };

    let event: any;
    try {
      event = await dodo.webhooks.unwrap(req.body.toString('utf8'), { headers });
    } catch (err) {
      console.error('Webhook signature verification failed:', err);
      return res.status(401).json({ error: 'invalid signature' });
    }

    switch (event.type) {
      case 'credit.balance_low': {
        const { customer_id, credit_entitlement_name, available_balance, threshold_percent } =
          event.data;
        console.log(
          `[low-balance] ${customer_id} has ${available_balance} ${credit_entitlement_name} ` +
            `left (under ${threshold_percent}%)`
        );
        await notifyCustomerLowBalance(customer_id, Number(available_balance));
        break;
      }
      case 'credit.added':
        console.log('[credit.added]', event.data);
        break;
      case 'credit.rolled_over':
        console.log('[rolled_over]', event.data);
        break;
    }

    res.json({ received: true });
  }
);

// JSON parsing for everything else.
app.use(express.json());

// ---------------------------------------------------------------
// POST /checkout/subscribe → start the MailKit subscription.
// ---------------------------------------------------------------
app.post('/checkout/subscribe', async (req, res) => {
  const { email, name } = req.body as { email: string; name: string };

  const session = await dodo.checkoutSessions.create({
    product_cart: [{ product_id: process.env.PLAN_PRODUCT_ID!, quantity: 1 }],
    customer: { email, name },
    return_url: `${BASE_URL}/?subscribed=1`,
  });

  res.json({ checkout_url: session.checkout_url });
});

// ---------------------------------------------------------------
// POST /checkout/topup → buy a 5,000-email top-up for an existing
// customer. In a real app, customer_id is resolved from the
// authenticated session, never trusted from request input.
// ---------------------------------------------------------------
app.post('/checkout/topup', async (req, res) => {
  const { customer_id } = req.body as { customer_id: string };

  const session = await dodo.checkoutSessions.create({
    product_cart: [{ product_id: process.env.TOPUP_PRODUCT_ID!, quantity: 1 }],
    customer: { customer_id },
    return_url: `${BASE_URL}/?topped_up=1`,
  });

  res.json({ checkout_url: session.checkout_url });
});

// ---------------------------------------------------------------
// GET /credits/:customerId → live balance for the dashboard widget.
// ---------------------------------------------------------------
app.get('/credits/:customerId', async (req, res) => {
  const balance = await dodo.creditEntitlements.balances.retrieve(req.params.customerId, {
    credit_entitlement_id: CREDIT_ENTITLEMENT_ID,
  });

  res.json({ balance: balance.balance });
});

// ---------------------------------------------------------------
// POST /send → send an email via Resend, then write a ledger entry
// to debit 1 credit from the customer's balance. The deduction is
// instant; the next /credits call reflects it.
// ---------------------------------------------------------------
app.post('/send', async (req, res) => {
  const { customer_id, to, subject, html } = req.body as {
    customer_id: string;
    to: string;
    subject: string;
    html: string;
  };

  // 1. Pre-flight balance check: refuse to send if the balance is at zero.
  const balance = await dodo.creditEntitlements.balances.retrieve(customer_id, {
    credit_entitlement_id: CREDIT_ENTITLEMENT_ID,
  });

  if (Number(balance.balance) <= 0) {
    return res.status(402).json({
      error: 'No email credits remaining. Buy a top-up pack or upgrade your plan.',
    });
  }

  // 2. Send via Resend.
  const { data, error } = await resend.emails.send({
    from: 'MailKit <onboarding@resend.dev>', // swap for your verified domain
    to: [to],
    subject,
    html,
  });

  if (error) {
    return res.status(500).json({ error: error.message });
  }

  // 3. Debit 1 credit. Resend's message id is the idempotency key, so if
  //    the client retries this request, Dodo deduplicates and the
  //    customer is only debited once for that send.
  await dodo.creditEntitlements.balances.createLedgerEntry(customer_id, {
    credit_entitlement_id: CREDIT_ENTITLEMENT_ID,
    amount: '1',
    entry_type: 'debit',
    reason: `email send ${data!.id}`,
    idempotency_key: data!.id,
  });

  res.json({ id: data!.id });
});

async function notifyCustomerLowBalance(customerId: string, available: number) {
  // In production: send an email to the account owner, push a banner,
  // open an in-app modal, etc. For the demo we just log.
  console.log(`[NOTIFY] ${customerId}: ${available} emails left. Consider topping up.`);
}

app.use(express.static('public'));

const port = Number(process.env.PORT) || 3000;
app.listen(port, () => {
  console.log(`MailKit running on http://localhost:${port}`);
});
```

/* LOCKED_PATTERN_2a2518a6b81e50ec206d5e6c9d83c4eb */

/* LOCKED_PATTERN_4907e9f6f7fbd509120d7a87afc829e9 */
**Webhook body phải là raw.** `express.json()` phân tích và tuần tự hóa lại body, điều này phá vỡ xác minh chữ ký. Định nghĩa `/webhooks/dodo` với `express.raw()` *trước* dòng `app.use(express.json())`.
</Warning>

/* LOCKED_PATTERN_3c8e9103f1fb437472f20f9142ad5c56 */
Backend sẵn sàng: đăng ký, nạp tiền, số dư, gửi, và xử lý webhook đều được nối dây.
</Check>
</Step>

/* LOCKED_PATTERN_44e0d7cc1817324269d5267e0fa13347 */
Tạo `public/index.html`:

/* LOCKED_PATTERN_278c8ff892f265e3e925879c7aa762a2 */

```html public/index.html expandable
<!doctype html>
<html>
  <head>
    <title>MailKit Demo</title>
    <style>
      body {
        font-family: system-ui, -apple-system, sans-serif;
        max-width: 720px;
        margin: 40px auto;
        padding: 0 20px;
        color: #1a1a2e;
      }
      h1 { font-size: 28px; margin-bottom: 4px; }
      h2 { font-size: 16px; margin-top: 32px; padding-bottom: 6px; border-bottom: 1px solid #eee; }
      label { display: block; font-size: 13px; font-weight: 600; margin: 12px 0 4px; }
      input, select, textarea {
        width: 100%;
        padding: 10px;
        border: 1px solid #ddd;
        border-radius: 6px;
        font-family: inherit;
        font-size: 14px;
        box-sizing: border-box;
      }
      button {
        background: #1a1a2e;
        color: white;
        padding: 10px 18px;
        border: none;
        border-radius: 6px;
        cursor: pointer;
        font-size: 14px;
        margin-top: 12px;
      }
      button:hover { background: #2d2d4a; }
      .out {
        background: #f6f6fa;
        padding: 12px;
        border-radius: 6px;
        margin-top: 12px;
        font-size: 13px;
        font-family: ui-monospace, monospace;
        white-space: pre-wrap;
        word-break: break-all;
      }
      .balance { font-size: 36px; font-weight: 700; color: #4f46e5; }
      .balance-sub { color: #888; font-size: 13px; margin-top: 4px; }
    </style>
  </head>
  <body>
    <h1>MailKit</h1>
    <p>Prepaid transactional email, billed per send.</p>

    <h2>1. Subscribe to MailKit ($19/mo, 5,000 emails)</h2>
    <label>Email</label>
    <input id="subEmail" type="email" placeholder="you@example.com" />
    <label>Name</label>
    <input id="subName" type="text" placeholder="Your name" />
    <button onclick="subscribe()">Get checkout link</button>
    <div id="subOut" class="out" hidden></div>

    <h2>2. Check your balance</h2>
    <label>Customer ID</label>
    <input id="balCust" type="text" placeholder="cus_xxxxxxxxxxxx" />
    <button onclick="checkBalance()">Refresh</button>
    <div id="balOut" class="out" hidden></div>

    <h2>3. Send a transactional email</h2>
    <label>Customer ID</label>
    <input id="sendCust" type="text" placeholder="cus_xxxxxxxxxxxx" />
    <label>To (Resend's sandbox accepts delivered@resend.dev)</label>
    <input id="sendTo" type="email" value="delivered@resend.dev" />
    <label>Subject</label>
    <input id="sendSubj" type="text" value="Hello from MailKit" />
    <label>HTML body</label>
    <textarea id="sendBody" rows="3">&lt;strong&gt;It works!&lt;/strong&gt;</textarea>
    <button onclick="sendEmail()">Send</button>
    <div id="sendOut" class="out" hidden></div>

    <h2>4. Run low? Buy a top-up pack</h2>
    <label>Customer ID</label>
    <input id="topCust" type="text" placeholder="cus_xxxxxxxxxxxx" />
    <button onclick="topup()">Buy 5,000 emails ($9)</button>
    <div id="topOut" class="out" hidden></div>

    <script>
      const show = (id, content) => {
        const el = document.getElementById(id);
        el.hidden = false;
        el.innerHTML = content;
      };

      async function subscribe() {
        const r = await fetch('/checkout/subscribe', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            email: document.getElementById('subEmail').value,
            name: document.getElementById('subName').value,
          }),
        });
        const data = await r.json();
        show('subOut', r.ok
          ? `<a href="${data.checkout_url}" target="_blank">Open checkout →</a>`
          : `Error: ${data.error}`);
      }

      async function checkBalance() {
        const id = document.getElementById('balCust').value;
        const r = await fetch(`/credits/${id}`);
        const data = await r.json();
        show('balOut', r.ok
          ? `<div class="balance">${Number(data.balance).toLocaleString()}</div>
             <div class="balance-sub">emails available</div>`
          : `Error: ${data.error}`);
      }

      async function sendEmail() {
        const r = await fetch('/send', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({
            customer_id: document.getElementById('sendCust').value,
            to: document.getElementById('sendTo').value,
            subject: document.getElementById('sendSubj').value,
            html: document.getElementById('sendBody').value,
          }),
        });
        const data = await r.json();
        show('sendOut', r.ok ? `Sent. Message id: ${data.id}` : `Error: ${data.error}`);
      }

      async function topup() {
        const r = await fetch('/checkout/topup', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ customer_id: document.getElementById('topCust').value }),
        });
        const data = await r.json();
        show('topOut', r.ok
          ? `<a href="${data.checkout_url}" target="_blank">Open top-up checkout →</a>`
          : `Error: ${data.error}`);
      }
    </script>
  </body>
</html>
```

/* LOCKED_PATTERN_2a2518a6b81e50ec206d5e6c9d83c4eb */
</Step>
</Steps>

## Bước 4: Kết Nối Endpoint Webhook

Sự kiện `credit.balance_low` cho phép bạn nhắc nhở khách hàng *trước* khi họ hết. Nếu không có nó, lần đầu tiên họ nhận thấy vấn đề là khi một email không gửi được.

/* LOCKED_PATTERN_bd2642ca094c2e4ce733f0386172be2d */
/* LOCKED_PATTERN_f46153c73ef1422a51dfa6b646bceb83 */
Webhooks cần một URL công khai. Sử dụng [ngrok](https://ngrok.com) (hoặc bất kỳ tunnel nào) trong khi phát triển:

```bash
ngrok http 3000
```

Sao chép URL forwarding HTTPS (ví dụ: `https://1234abcd.ngrok-free.app`).
</Step>

/* LOCKED_PATTERN_06211bf558d5ed34909a962f2d992da9 */
1. Đi tới **Developers → Webhooks → Thêm Endpoint**
2. **URL**: `https://1234abcd.ngrok-free.app/webhooks/dodo`
3. **Sự Kiện**: đăng ký `credit.added`, `credit.balance_low`, và `credit.rolled_over`
4. Lưu, sau đó sao chép **khóa ký** vào `.env` của bạn dưới dạng `DODO_WEBHOOK_KEY`
5. Khởi động lại server của bạn
</Step>
</Steps>

## Bước 5: Kiểm Tra Toàn Bộ Quy Trình

/* LOCKED_PATTERN_bd2642ca094c2e4ce733f0386172be2d */
/* LOCKED_PATTERN_1bd92175e42a8ccd3eaaf7b07dc839eb */

```bash
npm run dev
```

Bạn sẽ thấy `MailKit running on http://localhost:3000`. Mở nó trong trình duyệt của bạn.
</Step>

/* LOCKED_PATTERN_c0916ec9ccdb6841d16158c675b18d31 */
1. Trong phần 1, nhập email và tên thử nghiệm, nhấp vào **Lấy liên kết checkout**
2. Mở liên kết, hoàn thành thanh toán với [thẻ thử nghiệm](/miscellaneous/testing-process)
3. Sau khi thanh toán, tìm `customer_id` trong bảng điều khiển của bạn dưới mục **Khách Hàng**

/* LOCKED_PATTERN_3c8e9103f1fb437472f20f9142ad5c56 */
Khách hàng bây giờ sẽ có **5.000 emails** trong số dư của họ. Kiểm tra **Khách Hàng → [Khách Hàng] → Tín Dụng**.
</Check>
</Step>

/* LOCKED_PATTERN_9d8679e2f977394a0f190f159f950a20 */
1. Dán `customer_id` vào phần 3
2. Để `to` giữ nguyên `delivered@resend.dev` (hộp thư sandbox của Resend chấp nhận mọi thứ)
3. Nhấp vào **Gửi**

Bạn sẽ nhận được một id tin nhắn từ Resend. Làm mới số dư trong phần 2 và số lượng giảm ngay lập tức xuống 4.999. Mỗi lần ghi nợ ledger được phản ánh trong số dư trực tiếp ngay khi nó được viết.
</Step>

/* LOCKED_PATTERN_fdaa4d54d90bef72ba04fbb875bdc9c4 */
Ngưỡng là 20% (1.000 của 5.000-email hạn mức). Để kích hoạt nó mà không gửi 4.000 email thực, **điều chỉnh số dư theo cách thủ công** từ bảng điều khiển:

1. Đi tới **Khách Hàng → [Khách Hàng] → Tín Dụng → Tín Dụng Email**
2. Nhấp vào **Điều chỉnh Số dư** và ghi nợ `4000`
3. Gửi thêm một email qua demo

Server của bạn sẽ ghi nhật ký trong vài giây:

```
[low-balance] cus_xxx has 999 Email Credits left (under 20%)
[NOTIFY] cus_xxx: 999 emails left. Consider topping up.
```

/* LOCKED_PATTERN_3c8e9103f1fb437472f20f9142ad5c56 */
Server của bạn đã nhận và xác minh webhook. Trong quá trình sản xuất, đây là nơi bạn sẽ gửi email cho khách hàng hoặc hiển thị một banner trong ứng dụng.
</Check>
</Step>

/* LOCKED_PATTERN_9aa47a3c1c909f7b771786a43bb1cff7 */
1. Dán `customer_id` vào phần 4
2. Nhấp vào **Mua 5.000 emails**, hoàn thành thanh toán thử nghiệm
3. Làm mới số dư, và nó nhảy lên 5.000

/* LOCKED_PATTERN_3c8e9103f1fb437472f20f9142ad5c56 */
Một sự kiện `credit.added` được kích hoạt với `grant_source: one_time`. Gói nạp tiền chồng lên tín dụng thuê bao; cả hai pool đều được tiêu FIFO (grant không hết hạn cũ nhất trước).
</Check>
</Step>

/* LOCKED_PATTERN_5314eeda64a3354331c57cd82e520f72 */
Điều chỉnh số dư theo cách thủ công xuống bằng không, sau đó thử gửi thêm một email. Bạn sẽ nhận được:

```json
{ "error": "No email credits remaining. Buy a top-up pack or upgrade your plan." }
```

402 đó là sự thực thi cấp độ ứng dụng của bạn. API số dư Dodo là nguồn sự thật; không bao giờ cache nó trên client.
</Step>
</Steps>

## Xử Lý Sự Cố

/* LOCKED_PATTERN_3a00efd457b01f610250031fcbf16962 */
/* LOCKED_PATTERN_db62ef8462838bae902c5d2a04a52536 */
Chữ ký được tính trên raw HTTP body. `express.json()` phân tích và tuần tự hóa lại payload, điều này phá vỡ HMAC. Hãy chắc chắn `/webhooks/dodo` được đăng ký với `express.raw({ type: 'application/json' })` *trên* dòng `app.use(express.json())`, và rằng `DODO_WEBHOOK_KEY` khớp với khóa ký được hiển thị trên trang chi tiết endpoint.
</Accordion>

/* LOCKED_PATTERN_b77d71ae87823d9ac437a09f41227033 */
Ba điều cần xác minh, theo thứ tự này:

1. Khách hàng **hoàn thành checkout** (tín dụng được phát hành khi thanh toán thành công, không phải khi tạo phiên)
2. `CREDIT_ENTITLEMENT_ID` trong `.env` của bạn khớp với tín dụng được gắn vào sản phẩm (ID không khớp sẽ âm thầm ghi vào tín dụng sai)

3. `customer_id` bạn đang truyền đến từ Dodo (bảng `customers` trong bảng điều khiển), không phải từ cơ sở dữ liệu của bạn

/* LOCKED_PATTERN_498e292b83b8c4305b2edb8b439639a8 */
Sender sandbox `onboarding@resend.dev` chỉ chuyển đến email trên tài khoản Resend của bạn hoặc đến `delivered@resend.dev`. Để gửi đến bất kỳ ai khác, [xác minh một miền](https://resend.com/docs/dashboard/domains/introduction) và sử dụng địa chỉ `from` trên đó.
</Accordion>
</AccordionGroup>

## Những Gì Bạn Đã Xây Dựng

/* LOCKED_PATTERN_bd3b9ce11ef978f59c6eb5461169b62d */
/* LOCKED_PATTERN_3dd3b9c32612a12cffe0a191a0244ebd */
`Email Credits`, được định nghĩa một lần và gắn cho cả gói thuê bao và gói nạp tiền.

/* LOCKED_PATTERN_49a1eea087dd07c15ee24957419de561 */
$19/tháng cấp 5.000 emails mỗi chu kỳ. Khách hàng biết những gì họ đang trả, bạn biết chi phí tối tệ nhất của bạn.

/* LOCKED_PATTERN_94b25477c0c8998aec62a5357c63816c */
Một sản phẩm một lần cấp 5.000 emails. Chồng lên tín dụng thuê bao mà không cần thay đổi gói.

/* LOCKED_PATTERN_593fa2d9ea52197a1d9541cd91ee46e4 */
Một cuộc gọi `createLedgerEntry` đơn sau mỗi lần gửi. Không đồng hồ đo, không độ trễ tổng hợp, idempotent trên retry qua id tin nhắn của Resend.

/* LOCKED_PATTERN_c10919833b5f2b2159a0c76c1520f8a8 */
Đọc toàn bộ tài liệu CBB cho rollover, chế độ overage, quản lý ledger, và bề mặt API hoàn chỉnh.
</Card>

Cần trợ giúp?
- [Cộng Đồng Discord](https://discord.gg/bYqAp4ayYh)
- support@dodopayments.com
