---
title: MCP Server
description: Cho phép các trợ lý AI tương tác với API Dodo Payments thông qua
  Giao thức Ngữ cảnh Mô hình (MCP) để thực hiện các hoạt động thanh toán, quản
  lý đăng ký và tương tác với khách hàng một cách liền mạch.
icon: /images/logos/mcp.svg
---

## Tổng quan

Giao thức Ngữ cảnh Mô hình (MCP) là một tiêu chuẩn mở cho phép các ứng dụng AI kết nối an toàn với các nguồn dữ liệu và API bên ngoài. Máy chủ MCP Dodo Payments cung cấp cho các trợ lý AI như Claude, Cursor và các khách hàng tương thích với MCP khác quyền truy cập có cấu trúc vào hạ tầng thanh toán của bạn.

Máy chủ MCP Dodo Payments được xây dựng bằng [Stainless](https://www.stainless.com/) và sử dụng kiến trúc **Code Mode**, cho phép các tác nhân AI viết và thực thi mã TypeScript chống lại SDK Dodo Payments trong một môi trường sandbox cách ly.

### Các khả năng chính

- **Hoạt động thanh toán**: Tạo, truy xuất và quản lý thanh toán và hoàn tiền
- **Quản lý đăng ký**: Xử lý thanh toán định kỳ, nâng cấp và hủy bỏ
- **Quản lý khách hàng**: Quản lý dữ liệu khách hàng và quyền truy cập cổng thông tin
- **Danh mục sản phẩm**: Tạo và cập nhật sản phẩm, giá cả và giảm giá
- **Quản lý giấy phép**: Kích hoạt, xác thực và quản lý giấy phép phần mềm
- **Thanh toán dựa trên mức sử dụng**: Theo dõi và lập hóa đơn cho mức sử dụng theo chỉ số

## Cách hoạt động của Code Mode

Máy chủ MCP Dodo Payments sử dụng Code Mode, cung cấp cho tác nhân AI của bạn hai công cụ mạnh mẽ:

1. **Công cụ Tìm kiếm Tài liệu**: Truy vấn tài liệu về API và SDK Dodo Payments để hiểu các hoạt động và tham số có sẵn
2. **Công cụ Thực thi Mã**: Viết mã TypeScript chống lại SDK mà thực thi trong một môi trường sandbox an toàn mà không có quyền truy cập web hoặc hệ thống tệp

Kiến trúc này cho phép các tác nhân AI thực hiện các hoạt động thanh toán phức tạp, nhiều bước một cách xác định và lặp lại.

<Tip>
Sử dụng Code Mode, các tác nhân có thể kết nối nhiều cuộc gọi API, xử lý logic điều kiện và thực hiện các phép tính—tất cả trong một lần gọi công cụ duy nhất.
</Tip>

## Dodo Knowledge MCP

Ngoài Dodo Payments MCP Server (để thực hiện các thao tác API), chúng tôi cung cấp **Dodo Knowledge MCP** - một máy chủ tìm kiếm ngữ nghĩa giúp các trợ lý AI truy cập ngay lập tức vào tài liệu và cơ sở kiến thức của Dodo Payments.

<Info>
**Được xây dựng với [ContextMCP.ai](https://contextmcp.ai)**: Dodo Knowledge MCP được hỗ trợ bởi ContextMCP, cho phép tìm kiếm ngữ nghĩa nhanh chóng trên tài liệu của chúng tôi bằng cách sử dụng vector embedding.
</Info>

### Dodo Knowledge MCP là gì?

Dodo Knowledge MCP là một máy chủ MCP từ xa cung cấp:

- **Tìm kiếm tài liệu ngữ nghĩa**: Tìm tài liệu liên quan bằng cách sử dụng các truy vấn ngôn ngữ tự nhiên
- **Câu trả lời theo ngữ cảnh**: Các trợ lý AI nhận được thông tin chính xác, cập nhật về Dodo Payments
- **Không cần thiết lập**: Không cần khóa API hoặc cài đặt cục bộ - chỉ cần kết nối và bắt đầu truy vấn

### Thiết lập nhanh

Kết nối với Dodo Knowledge MCP trong khách hàng AI của bạn:

<Tabs>
<Tab title="Cursor">
Thêm vào `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Desktop">
Thêm vào cấu hình Claude Desktop của bạn:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

Vị trí tệp cấu hình:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Tab>

{/* LOCKED_PATTERN_e01afe5ad2b70c1861708d679d3c0fa9 */}
Thêm vào `~/.codeium/windsurf/mcp_config.json`:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Code">
Chạy trong terminal của bạn:

```bash
claude mcp add dodo-knowledge -- npx -y mcp-remote@latest https://knowledge.dodopayments.com/mcp
```

</Tab>
</Tabs>

<Note>
Yêu cầu Node.js 18 hoặc cao hơn. Gói `mcp-remote` đảm nhiệm kết nối với máy chủ MCP từ xa.
</Note>

### Sử dụng cả hai máy chủ MCP cùng nhau

Để có trải nghiệm phát triển hỗ trợ AI tốt nhất, chúng tôi khuyên bạn nên sử dụng cả hai máy chủ MCP:

| Máy chủ | Mục đích | Trường hợp sử dụng |
|--------|---------|----------|
| **Dodo Knowledge MCP** | Tìm kiếm tài liệu | "Làm thế nào để tôi xử lý webhooks?", "Những phương thức thanh toán nào được hỗ trợ?" |
| **Dodo Payments MCP** | Thao tác API | Tạo thanh toán, quản lý đăng ký, xử lý hoàn tiền |

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    },
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

<Tip>
Với cả hai máy chủ được cấu hình, trợ lý AI của bạn có thể trước tiên tìm kiếm tài liệu để hiểu cách triển khai một tính năng, sau đó thực hiện các cuộc gọi API thực tế—tất cả trong một cuộc trò chuyện duy nhất.
</Tip>

### Khắc phục sự cố Knowledge MCP

Nếu bạn gặp sự cố kết nối:

1. **Xóa bộ nhớ cache xác thực MCP**: `rm -rf ~/.mcp-auth`
2. **Khởi động lại ứng dụng khách của bạn**
3. **Kiểm tra nhật ký khách hàng** để tìm thông báo lỗi
4. **Xác minh phiên bản Node.js**: Yêu cầu Node.js 18+

<Card title="Knowledge MCP Server" icon="book-open" href="https://knowledge.dodopayments.com">
  Truy cập trang cấu hình Dodo Knowledge MCP
</Card>

## Cài đặt

Chọn phương pháp cài đặt phù hợp nhất với quy trình làm việc của bạn.

### Máy chủ MCP từ xa (Được khuyến nghị)

Truy cập máy chủ MCP được lưu trữ mà không cần thiết lập hoặc cài đặt cục bộ. Đây là cách nhanh nhất để bắt đầu.

<Frame>
  <iframe 
      className="w-full aspect-video rounded-md" 
      src="https://www.youtube.com/embed/TxGgYxNmkT4"
      title="Using Dodo Payment's Remote MCP Server"
      frameBorder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowFullScreen
  ></iframe>
</Frame>

<Steps>
{/* LOCKED_PATTERN_bd1568a9aaf0e86b69853e215d3d5723 */}
    Truy cập [https://mcp.dodopayments.com](https://mcp.dodopayments.com) trong trình duyệt của bạn.
</Step>

{/* LOCKED_PATTERN_b8d4e4a1a3c0fcf28fd2afe7ce9e8316 */}
    Sao chép cấu hình JSON được cung cấp cho khách hàng cụ thể của bạn. Đối với Cursor hoặc Claude Desktop, thêm điều này vào cài đặt MCP của bạn:
    
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments_api": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8be57788a275cad53ee59f91af51bfd5 */}
    Luồng OAuth sẽ yêu cầu bạn:
    - Nhập khóa API Dodo Payments của bạn
    - Chọn môi trường của bạn (test hoặc live)
    - Chọn loại khách hàng MCP của bạn
    
    <Warning>
    Giữ khóa API của bạn an toàn. Sử dụng khóa chế độ thử nghiệm trong quá trình phát triển.
    </Warning>

{/* LOCKED_PATTERN_606fac2bd646ef76f5d66097882887ed */}
    Nhấp **Login and Approve** để cấp quyền kết nối.
    
    <Check>
    Sau khi kết nối, trợ lý AI của bạn có thể tương tác với API Dodo Payments thay mặt bạn.
    </Check>
</Step>
</Steps>

### Gói NPM

Cài đặt và chạy máy chủ MCP cục bộ bằng NPM.

<Tabs>
{/* LOCKED_PATTERN_c8776436a7ae17e188a287e7c6852f16 */}

```bash
# Set your environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."
export DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key"
export DODO_PAYMENTS_ENVIRONMENT="live_mode"

# Run the latest version
npx -y dodopayments-mcp@latest
```

</Tab>

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

</Tab>
</Tabs>

<Tip>
Sử dụng `@latest` để luôn lấy phiên bản mới nhất, hoặc khoá vào một phiên bản cụ thể như `@1.0.0` để ổn định.
</Tip>

### Docker

Chạy máy chủ MCP trong môi trường container hóa để triển khai nhất quán.

<CodeGroup>

```bash Docker
# Pull the latest image
docker pull ghcr.io/dodopayments/mcp:latest

# Run the container
docker run -e DODO_PAYMENTS_API_KEY="dodo_test_..." \
  -e DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -e DODO_PAYMENTS_ENVIRONMENT="live_mode" \
  ghcr.io/dodopayments/mcp:latest
```

```yaml Docker Compose
services:
  dodopayments-mcp:
    image: ghcr.io/dodopayments/mcp:latest
    environment:
      - DODO_PAYMENTS_API_KEY=dodo_test_...
      - DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_key
      - DODO_PAYMENTS_ENVIRONMENT=live_mode
    restart: unless-stopped
```

</CodeGroup>

<Note>
Ảnh Docker có sẵn trên [GitHub Container Registry](https://github.com/orgs/dodopayments/packages/container/package/mcp).
</Note>

## Cấu hình khách hàng

Cấu hình máy chủ Dodo Payments MCP trong khách hàng AI mà bạn ưa thích.

<Tabs>
<Tab title="Cursor">
Thiết lập máy chủ MCP Dodo Payments trong Cursor để kích hoạt truy cập hội thoại tới dữ liệu thanh toán của bạn.

**Cài đặt một lần nhấp**

Sử dụng nút bên dưới để cài đặt máy chủ MCP trực tiếp trong Cursor:

[

![Thêm vào Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)

](https://cursor.com/en-US/install-mcp?name=dodopayments-mcp&config=)

Sau khi nhấp, thiết lập biến môi trường trong `mcp.json` của Cursor thông qua **Cursor Settings** > **Tools & MCP** > **New MCP Server**.

**Cấu hình thủ công**

<Steps>
{/* LOCKED_PATTERN_b5120fe2afbf9bb96d1de6f9549e8f41 */}
    Chuyển đến **Cursor Settings** > **Features** > **Model Context Protocol** hoặc nhấn `Cmd/Ctrl + Shift + P` và tìm kiếm "MCP Settings".
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    Nhấp **Edit Config** để mở tệp `mcp_config.json`.
</Step>

{/* LOCKED_PATTERN_a65e4e236194f7c177caa156c6d0b797 */}
    Chọn một trong các cấu hình sau:
    
    **Remote Server (Được khuyến nghị)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **Local NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8368e3e8540b47a92000151cb7e30a33 */}
    Lưu tệp cấu hình và khởi động lại Cursor.
    
    <Check>
    Xác minh kết nối bằng cách hỏi trợ lý AI về dữ liệu Dodo Payments của bạn.
    </Check>
</Step>
</Steps>
</Tab>

<Tab title="Claude Desktop">
Thiết lập máy chủ MCP Dodo Payments trong Claude Desktop để kích hoạt truy cập hội thoại tới dữ liệu thanh toán của bạn.
<Steps>
{/* LOCKED_PATTERN_ecfdd2e916ed4a93d6b3950e9027df3f */}
    Tìm tệp cấu hình Claude Desktop:
    
    - **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
    - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
    - **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    Mở tệp trong trình chỉnh sửa văn bản và thêm cấu hình Dodo Payments:
    
    **Remote Server (Được khuyến nghị)**
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **Local NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_83563b32c3caf8bd7aa68192b6ebcc7d */}
    Đóng rồi mở lại Claude Desktop để các thay đổi có hiệu lực.
    
    <Tip>
    Tìm biểu tượng 🔌 trong Claude Desktop để xác nhận máy chủ MCP đã kết nối.
    </Tip>
</Step>
</Steps>
</Tab>

Thiết lập máy chủ MCP Dodo Payments trong Claude Code CLI.

Chạy lệnh sau trong terminal của bạn:

```bash
claude mcp add --transport stdio dodopayments_api \
  --env DODO_PAYMENTS_API_KEY="dodo_test_..." \
  DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -- npx -y dodopayments-mcp
```

Cấu hình sẽ được lưu trong tệp `.claude.json` tại thư mục chính của bạn.

<Tip>
Claude Code tự động lấy các biến môi trường từ shell của bạn, nên bạn cũng có thể export chúng trước khi chạy lệnh.
</Tip>

</Tab>

**Cài đặt một lần nhấp**

[Mở VS Code](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dodopayments-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22dodopayments-mcp%22%5D%2C%22env%22%3A%7B%22DODO_PAYMENTS_API_KEY%22%3A%22Đặt%20DODO_PAYMENTS_API_KEY%20của%20bạn%20tại%20đây.%22%2C%22DODO_PAYMENTS_WEBHOOK_KEY%22%3A%22Đặt%20DODO_PAYMENTS_WEBHOOK_KEY%20của%20bạn%20tại%20đây.%22%7D%7D)

Sau khi nhấp, thiết lập biến môi trường trong `mcp.json` của VS Code thông qua **Command Palette** > **MCP: Open User Configuration**.

**Cấu hình thủ công**

<Steps>
{/* LOCKED_PATTERN_01c11d820b190ade4d2a29feff5a49da */}
    Mở Command Palette (`Cmd/Ctrl + Shift + P`) và tìm kiếm "MCP: Open User Configuration".
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Thêm máy chủ MCP Dodo Payments:
    
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_fabb79a0cecb26359867d78b61382dd7 */}
    Tải lại VS Code (`Cmd/Ctrl + Shift + P` > "Reload Window") để kích hoạt máy chủ MCP.
</Step>
</Steps>
</Tab>

Thiết lập máy chủ MCP Dodo Payments trong Cline (VS Code) để kích hoạt truy cập hội thoại tới dữ liệu thanh toán của bạn.

<Steps>
{/* LOCKED_PATTERN_43db9eecf5eea8e9c4ad60b6c3ff1c0b */}
    Xác định vị trí tệp cấu hình Cline:
    
    - **macOS**: `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
    - **Windows**: `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    - **Linux**: `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
</Step>

{/* LOCKED_PATTERN_0849b9a67e72024819745a9889898853 */}
    Thêm cấu hình máy chủ MCP Dodo Payments:
    
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

{/* LOCKED_PATTERN_43be57c73e24741f16da5f79d5398749 */}
    Tải lại cửa sổ VS Code (`Cmd/Ctrl + Shift + P` > "Reload Window") để kích hoạt kết nối máy chủ MCP.
</Step>
</Steps>
</Tab>

<Tab title="Zed">
Thiết lập máy chủ MCP Dodo Payments trong Zed để kích hoạt truy cập hội thoại tới dữ liệu thanh toán của bạn.
<Steps>
{/* LOCKED_PATTERN_a48799001554eb7ef4b2ac1e56fab915 */}
    Chuyển đến **Settings** > **Assistant** > **Model Context Protocol**.
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Thêm cấu hình sau:
    
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_b1c8821108a3bf0ac4028a0196176d66 */}
    Lưu cấu hình. Máy chủ MCP giờ sẽ có trong trợ lý AI của Zed.
</Step>
</Steps>
</Tab>

Thiết lập máy chủ MCP Dodo Payments trong các khách hàng tương thích MCP khác.

**Máy chủ từ xa (Được khuyến nghị)**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

**Local NPX**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
      }
    }
  }
}
```

Truy cập [modelcontextprotocol.io/clients](https://modelcontextprotocol.io/clients) để xem danh sách đầy đủ các khách hàng tương thích MCP và phương pháp cấu hình của họ.

## Biến môi trường

Cấu hình hành vi của máy chủ MCP bằng cách sử dụng các biến môi trường.

| Biến | Mô tả | Bắt buộc |
|----------|-------------|----------|
| `DODO_PAYMENTS_API_KEY` | Khóa API Dodo Payments của bạn | Có |
| `DODO_PAYMENTS_WEBHOOK_KEY` | Khóa ký webhook của bạn | Không |
| `DODO_PAYMENTS_ENVIRONMENT` | Đặt thành `live_mode` cho môi trường production | Không |

## Chạy từ xa

Triển khai máy chủ MCP như một máy chủ HTTP từ xa cho các khách hàng dựa trên web hoặc quy trình agentic.

```bash
# Start as remote server
npx -y dodopayments-mcp --transport=http

# Specify a custom port
npx -y dodopayments-mcp --transport=http --port=3000
```

### Cấu hình máy chủ từ xa

Khi đã triển khai, các khách hàng có thể kết nối bằng cách sử dụng URL máy chủ:

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "url": "http://localhost:3000",
      "headers": {
        "Authorization": "Bearer your_api_key"
      }
    }
  }
}
```

### Tiêu đề ủy quyền

Máy chủ từ xa chấp nhận xác thực qua các tiêu đề sau:

| Header | Mô tả |
|--------|-------------|
| `Authorization` | Xác thực token Bearer |
| `x-dodo-payments-api-key` | Header khóa API trực tiếp |

## Khả năng của khách hàng

Các khách hàng MCP khác nhau có mức độ hỗ trợ khác nhau cho thông số kỹ thuật MCP. Máy chủ có thể tự động điều chỉnh các sơ đồ công cụ để làm việc xung quanh những hạn chế này.

```bash
# Auto-detect client (default)
npx dodopayments-mcp --client=infer

# Configure for specific clients
npx dodopayments-mcp --client=cursor
npx dodopayments-mcp --client=claude
npx dodopayments-mcp --client=claude-code
npx dodopayments-mcp --client=openai-agents
```

### Các cài đặt khách hàng có sẵn

| Client | Mô tả |
|--------|-------------|
| `infer` | Tự động phát hiện khách hàng (mặc định) |
| `cursor` | Trình chỉnh sửa Cursor |
| `claude` | Claude AI web hoặc desktop |
| `claude-code` | Claude Code CLI |
| `openai-agents` | Nền tảng đại lý OpenAI |

### Cấu hình khả năng thủ công

Đối với các khách hàng không được liệt kê ở trên, hãy cấu hình khả năng một cách thủ công:

```bash
# Disable $ref pointer support and set max tool name length
npx dodopayments-mcp --no-capability=refs --capability=tool-name-length=40
```

Khả năng sẵn có:
- `top-level-unions` - Hỗ trợ các kiểu union cấp cao nhất
- `valid-json` - Phân tích đối số chuỗi JSON chính xác
- `refs` - Hỗ trợ các con trỏ $ref trong schema
- `unions` - Hỗ trợ các kiểu union (anyOf)
- `formats` - Hỗ trợ kiểm tra định dạng
- `tool-name-length=N` - Độ dài tên công cụ tối đa

<Tip>
Chạy `npx dodopayments-mcp --describe-capabilities` để biết thông tin chi tiết về khả năng.
</Tip>

## Lọc công cụ

Kiểm soát các thao tác API nào được hiển thị cho trợ lý AI của bạn để tối ưu hóa hiệu suất và duy trì bảo mật.

### Lọc theo loại thao tác

Hạn chế quyền truy cập vào các thao tác chỉ đọc hoặc ghi.

```bash
# Only expose read operations (GET requests)
npx dodopayments-mcp --operation=read

# Only expose write operations (POST, PUT, DELETE)
npx dodopayments-mcp --operation=write
```

<Tip>
Sử dụng `--operation=read` trong quá trình phát triển để ngăn chặn việc chỉnh sửa dữ liệu của bạn do nhầm lẫn.
</Tip>

### Lọc theo tài nguyên

Giới hạn các công cụ cho các tài nguyên API cụ thể.

```bash
# Only expose payment-related tools
npx dodopayments-mcp --resource=payments

# Expose multiple resources
npx dodopayments-mcp --resource=payments --resource=customers

# Use wildcards for resource groups
npx dodopayments-mcp --resource=subscription*
```

### Bao gồm hoặc loại trừ các công cụ cụ thể

Tinh chỉnh các công cụ riêng lẻ có sẵn.

```bash
# Include only specific tools
npx dodopayments-mcp --tool=create_payments --tool=list_payments

# Exclude specific tools
npx dodopayments-mcp --no-tool=delete_products

# Disable the docs search tool
npx dodopayments-mcp --no-tools=docs

# List all available tools
npx dodopayments-mcp --list
```

### Kết hợp các bộ lọc

Áp dụng nhiều bộ lọc đồng thời để kiểm soát chính xác.

```bash
# Only payment and subscription read operations
npx dodopayments-mcp --operation=read --resource=payments --resource=subscriptions
```

### Lọc tham số truy vấn URL (Máy chủ từ xa)

Khi sử dụng máy chủ từ xa, áp dụng bộ lọc qua các tham số truy vấn URL:

```
https://mcp.dodopayments.com/?operation=read
https://mcp.dodopayments.com/?resource=payments
```

## Chế độ công cụ động

Đối với các API lớn, việc tải tất cả các công cụ cùng một lúc có thể làm quá tải cửa sổ ngữ cảnh của AI. Chế độ công cụ động cung cấp một lựa chọn hiệu quả hơn.

```bash
npx dodopayments-mcp --tools=dynamic
```

Trong chế độ động, ba công cụ meta thay thế các công cụ API riêng lẻ:

1. **`list_api_endpoints`** - Khám phá các phương thức có sẵn với bộ lọc tùy chọn
2. **`get_api_endpoint_schema`** - Lấy schema chi tiết cho một phương thức cụ thể
3. **`invoke_api_endpoint`** - Thực thi bất kỳ phương thức nào với các tham số phù hợp

<Info>
Các công cụ động được bật tự động cho API có hơn 50 phương thức. Ghi đè bằng `--tools=all` để bao gồm tất cả công cụ.
</Info>

```bash
# Use both dynamic tools and specific resources
npx dodopayments-mcp --tools=dynamic --resource=payments
```

## Sử dụng nâng cao

### Tích hợp lập trình

Xây dựng các máy chủ MCP tùy chỉnh hoặc mở rộng máy chủ hiện có một cách lập trình.

```javascript
import { server, endpoints, init } from "dodopayments-mcp/server";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

// Initialize with all default endpoints
init({ server, endpoints });

// Start the server
const transport = new StdioServerTransport();
await server.connect(transport);
```

### Phát triển công cụ tùy chỉnh

Mở rộng máy chủ MCP với các công cụ và logic kinh doanh của riêng bạn.

```javascript
import { server, init } from "dodopayments-mcp/server";
import createPayments from "dodopayments-mcp/tools/payments/create-payments";
import { zodToJsonSchema } from "zod-to-json-schema";
import { z } from "zod";

// Define a custom tool
const customTool = {
  tool: {
    name: 'calculate_payment_total',
    description: 'Calculate payment total with tax and fees',
    inputSchema: zodToJsonSchema(
      z.object({
        amount: z.number().describe('Base amount in cents'),
        tax_rate: z.number().describe('Tax rate as decimal'),
      })
    ),
  },
  handler: async (client, args) => {
    const total = args.amount * (1 + args.tax_rate);
    return {
      content: [{
        type: 'text',
        text: JSON.stringify({ total, currency: 'usd' }),
      }],
    };
  },
};

// Initialize with custom tools
init({ 
  server, 
  endpoints: [createPayments, customTool] 
});
```

## Thực hành bảo mật tốt nhất

Bảo vệ thông tin xác thực API của bạn và duy trì các tích hợp an toàn.

<AccordionGroup>
{/* LOCKED_PATTERN_58b09b66cd00f40de384d770573ae848 */}
**Không bao giờ cam kết thông tin xác thực vào hệ thống quản lý phiên bản**

Lưu trữ khóa API trong các biến môi trường hoặc hệ thống quản lý bí mật an toàn.

```bash
# Use environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."

# Use a .env file (add to .gitignore)
echo "DODO_PAYMENTS_API_KEY=dodo_test_..." > .env
```

**Thay đổi khóa thường xuyên**

Tạo các khóa API mới định kỳ và thu hồi các khóa cũ thông qua bảng điều khiển Dodo Payments của bạn.

**Sử dụng khóa thử nghiệm cho phát triển**

Luôn sử dụng khóa API chế độ thử nghiệm trong quá trình phát triển để tránh ảnh hưởng đến dữ liệu production.
</Accordion>

{/* LOCKED_PATTERN_1d0876565ecc6c625335a3849461e889 */}
**Áp dụng lọc công cụ trong môi trường production**

Giới hạn các thao tác được hiển thị chỉ cho những gì trợ lý AI của bạn cần.

```bash
# Production: read-only access
npx dodopayments-mcp --operation=read

# Development: full access
npx dodopayments-mcp
```

**Thực hiện xác thực cho các máy chủ từ xa**

Khi triển khai từ xa, luôn yêu cầu xác thực thông qua header `Authorization` hoặc header `x-dodo-payments-api-key`.

**Theo dõi việc sử dụng API**

Theo dõi hoạt động máy chủ MCP qua bảng điều khiển Dodo Payments của bạn và thiết lập cảnh báo cho các mẫu bất thường.
</Accordion>

{/* LOCKED_PATTERN_ddb4fc82978c8dda72d606214fdad81d */}
**Sử dụng HTTPS cho máy chủ từ xa**
</Accordion>

Luôn triển khai các máy chủ MCP từ xa phía sau các điểm cuối HTTPS.

**Thực hiện giới hạn tỷ lệ**

Bảo vệ chống lại lạm dụng bằng cách thực hiện giới hạn tỷ lệ ở cả cấp máy chủ MCP và API.

**Hạn chế quyền truy cập mạng**

Cấu hình các quy tắc tường lửa để giới hạn những khách hàng có thể kết nối với máy chủ MCP của bạn.
</Accordion>
</AccordionGroup>

## Khắc phục sự cố

<AccordionGroup>
{/* LOCKED_PATTERN_3e10fc2511884be492da344a929e2311 */}
**Xác minh khóa API của bạn**

Đảm bảo khóa API của bạn được thiết lập chính xác và có các quyền cần thiết.

```bash
# Test your API key
curl -H "Authorization: Bearer dodo_test_..." \
  https://test.dodopayments.com/payments
```

**Kiểm tra kết nối mạng của bạn**

Xác minh rằng bạn có thể truy cập các điểm cuối API Dodo Payments.

**Xem xét nhật ký của khách hàng**

Bật ghi nhật ký chi tiết trong khách hàng MCP của bạn để chẩn đoán sự cố kết nối.
</Accordion>

{/* LOCKED_PATTERN_8f29ea613a43315c29bf194043f8ada2 */}
**Xác nhận môi trường khóa API**
</Accordion>

Đảm bảo bạn đang sử dụng các khóa thử nghiệm với các điểm cuối thử nghiệm và các khóa trực tiếp với các điểm cuối sản xuất.

**Kiểm tra biến môi trường**

Xác minh `DODO_PAYMENTS_ENVIRONMENT` được thiết lập chính xác (`live_mode` cho môi trường production).

**Tạo lại thông tin xác thực**

Nếu sự cố vẫn tiếp diễn, tạo khóa API mới thông qua bảng điều khiển của bạn.
</Accordion>

{/* LOCKED_PATTERN_565675849d369f82d965d1f3a122632b */}
**Xác thực tham số đầu vào**

Đảm bảo trợ lý AI đang cung cấp các tham số được định dạng chính xác cho mỗi công cụ.

**Xem xét thông báo lỗi**

Kiểm tra phản hồi lỗi từ API để có hướng dẫn cụ thể về những gì đã sai.

**Kiểm tra trực tiếp với API**

Xác minh thao tác hoạt động khi gọi trực tiếp API Dodo Payments bằng curl hoặc Postman.
</Accordion>

{/* LOCKED_PATTERN_776734428589daf9897a729aa0a38aed */}
**Thử một cài đặt trước của khách hàng khác**

Nếu các công cụ không hoạt động đúng cách, hãy thử chỉ định khách hàng của bạn một cách rõ ràng:

```bash
npx dodopayments-mcp --client=cursor
```

**Kiểm tra hỗ trợ khả năng**

Một số khách hàng không hỗ trợ tất cả tính năng schema JSON. Sử dụng `--describe-capabilities` để hiểu những gì được hỗ trợ.
</Accordion>
</AccordionGroup>

## Tài nguyên

<CardGroup cols={2}>
{/* LOCKED_PATTERN_4325afcb089e351f99ef591b38203ac2 */}
  Khám phá toàn bộ tài liệu API Dodo Payments
</Card>

{/* LOCKED_PATTERN_1cbca36bdeb2bbbc77032220bd30c277 */}
  Tìm hiểu thêm về tiêu chuẩn Model Context Protocol
</Card>

{/* LOCKED_PATTERN_02488b19a875a7c4cd24c2f5651e4831 */}
  Xem mã nguồn máy chủ MCP và đóng góp
</Card>

{/* LOCKED_PATTERN_d4dbe77bbb87d236d98e318084853b86 */}
  Cài đặt máy chủ MCP từ NPM
</Card>
</CardGroup>