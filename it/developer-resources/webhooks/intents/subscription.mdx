---
openapi-schema: SubscriptionResponse
title: Abbonamento
description: Il payload inviato al tuo endpoint webhook quando un abbonamento
  viene creato, aggiornato o cambia stato.
icon: repeat
keywords:
  - Dodo Payments
  - webhook abbonamento
  - eventi di abbonamento
  - integrazione webhook
  - abbonamento
---

## Eventi Webhook di Abbonamento

I seguenti eventi webhook sono disponibili per il monitoraggio delle modifiche al ciclo di vita dell'abbonamento:

| Evento | Descrizione |
|-------|-------------|
| `subscription.active` | L'abbonamento è stato attivato correttamente |
| `subscription.updated` | L'oggetto dell'abbonamento è stato aggiornato (qualsiasi modifica a un campo attiva questo evento) |
| `subscription.on_hold` | L'abbonamento è stato sospeso a causa di un rinnovo non riuscito |
| `subscription.paused` | L'abbonamento è stato messo in pausa |
| `subscription.renewed` | L'abbonamento è stato rinnovato per il periodo di fatturazione successivo |
| `subscription.plan_changed` | Il piano dell'abbonamento è stato sottoscritto con un upgrade, un downgrade o modificato |
| `subscription.update_payment_method` | Il metodo di pagamento dell'abbonamento è stato aggiornato |
| `subscription.cancelled` | L'abbonamento è stato annullato |
| `subscription.failed` | La creazione dell'abbonamento non è riuscita durante la creazione del mandato |
| `subscription.expired` | L'abbonamento ha raggiunto la fine del periodo previsto |

### Utilizzo di `subscription.updated` per la sincronizzazione in tempo reale

Il webhook `subscription.updated` viene attivato ogni volta che cambia un campo dell'abbonamento, consentendoti di mantenere lo stato dell'applicazione sincronizzato senza effettuare polling:

```javascript
app.post('/webhooks/dodo', async (req, res) => {
  const event = req.body;
  
  if (event.type === 'subscription.updated') {
    const subscription = event.data;
    
    // Sync subscription changes to your database
    await syncSubscription(subscription.subscription_id, {
      status: subscription.status,
      next_billing_date: subscription.next_billing_date,
      metadata: subscription.metadata,
      // ... other fields you want to track
    });
    
    console.log(`Subscription ${subscription.subscription_id} updated`);
  }
  
  res.json({ received: true });
});
```

<Tip>
Iscriviti a `subscription.updated` per ricevere notifiche in tempo reale su qualsiasi modifica dell'abbonamento, eliminando la necessità di interrogare periodicamente l'API per gli aggiornamenti.
</Tip>

## Schema del Payload Webhook