---
title: MCP Server
description: Abilita gli assistenti AI a interagire con l'API Dodo Payments
  tramite il Model Context Protocol (MCP) per operazioni di pagamento senza
  soluzione di continuità, gestione degli abbonamenti e interazioni con i
  clienti.
icon: /images/logos/mcp.svg
---

## Panoramica

Il Model Context Protocol (MCP) è uno standard aperto che consente alle applicazioni AI di connettersi in modo sicuro a fonti di dati esterne e API. Il Dodo Payments MCP Server fornisce agli assistenti AI come Claude, Cursor e altri client compatibili con MCP un accesso strutturato alla tua infrastruttura di pagamento.

Il Dodo Payments MCP Server è costruito con [Stainless](https://www.stainless.com/) e utilizza l'architettura **Code Mode**, che consente agli agenti AI di scrivere ed eseguire codice TypeScript contro il Dodo Payments SDK in un ambiente sandbox isolato.

### Capacità chiave

- **Operazioni di pagamento**: Crea, recupera e gestisci pagamenti e rimborsi
- **Gestione degli abbonamenti**: Gestisci fatturazione ricorrente, aggiornamenti e cancellazioni
- **Amministrazione dei clienti**: Gestisci i dati dei clienti e l'accesso al portale
- **Catalogo prodotti**: Crea e aggiorna prodotti, prezzi e sconti
- **Gestione delle licenze**: Attiva, convalida e gestisci le licenze software
- **Fatturazione basata sull'uso**: Monitora e fattura per l'uso misurato

## Come funziona Code Mode

Il Dodo Payments MCP Server utilizza Code Mode, fornendo al tuo agente AI due strumenti potenti:

1. **Strumento di ricerca documenti**: Interroga la documentazione sull'API e SDK di Dodo Payments per comprendere le operazioni e i parametri disponibili
2. **Strumento di esecuzione del codice**: Scrivi codice TypeScript contro l'SDK che viene eseguito in un ambiente sandbox sicuro senza accesso al web o al filesystem

Questa architettura consente agli agenti AI di eseguire operazioni di pagamento complesse e multi-passaggio in modo deterministico e ripetibile.

<Tip>
Usando Code Mode, gli agenti possono concatenare più chiamate API, gestire la logica condizionale ed eseguire calcoli—tutto in un'unica invocazione dello strumento.
</Tip>

## Dodo Knowledge MCP

Oltre al Dodo Payments MCP Server (per eseguire operazioni API), forniamo **Dodo Knowledge MCP** - un server di ricerca semantica che offre agli assistenti AI accesso immediato alla documentazione e alla base di conoscenza di Dodo Payments.

<Info>
**Realizzato con [ContextMCP.ai](https://contextmcp.ai)**: Dodo Knowledge MCP è alimentato da ContextMCP, consentendo una ricerca semantica rapida nella nostra documentazione grazie agli embedding vettoriali.
</Info>

### Cos'è Dodo Knowledge MCP?

Dodo Knowledge MCP è un server MCP remoto che fornisce:

- **Ricerca Documentazione Semantica**: Trova documentazione pertinente utilizzando query in linguaggio naturale
- **Risposte Contestuali**: Gli assistenti AI ottengono informazioni accurate e aggiornate su Dodo Payments
- **Zero Configurazione**: Nessuna chiave API o installazione locale richiesta - basta connettersi e iniziare a interrogare

### Configurazione Rapida

Collegati a Dodo Knowledge MCP nel tuo client AI:

<Tabs>
<Tab title="Cursor">
Aggiungi a `~/.cursor/mcp.json`:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Desktop">
Aggiungi alla configurazione di Claude Desktop:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

Posizioni dei file di configurazione:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Tab>

<Tab title="Windsurf">
Aggiungi a `~/.codeium/windsurf/mcp_config.json`:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Code">
Esegui nel terminale:

```bash
claude mcp add dodo-knowledge -- npx -y mcp-remote@latest https://knowledge.dodopayments.com/mcp
```

</Tab>
</Tabs>

<Note>
Richiede Node.js 18 o superiore. Il pacchetto `mcp-remote` gestisce la connessione al server MCP remoto.
</Note>

### Utilizzare Entrambi i Server MCP Insieme

Per la migliore esperienza di sviluppo assistita da AI, ti consigliamo di utilizzare entrambi i server MCP:

| Server | Scopo | Caso d'uso |
|--------|---------|----------|
| **Dodo Knowledge MCP** | Ricerca documentazione | "Come gestisco i webhook?", "Quali metodi di pagamento sono supportati?" |
| **Dodo Payments MCP** | Operazioni API | Crea pagamenti, gestisci abbonamenti, gestisci rimborsi |

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    },
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

<Tip>
Con entrambi i server configurati, il tuo assistente IA può prima cercare nella documentazione per capire come implementare una funzione, poi eseguire le chiamate API reali—tutto in una singola conversazione.
</Tip>

### Risoluzione dei Problemi di Knowledge MCP

Se riscontri problemi di connessione:

1. **Svuota la cache di autenticazione MCP**: `rm -rf ~/.mcp-auth`
2. **Riavvia la tua applicazione client**
3. **Controlla i log del client** per messaggi di errore
4. **Verifica la versione di Node.js**: Richiede Node.js 18+

<Card title="Knowledge MCP Server" icon="book-open" href="https://knowledge.dodopayments.com">
  Accedi alla pagina di configurazione di Dodo Knowledge MCP
</Card>

## Installazione

Scegli il metodo di installazione che meglio si adatta al tuo flusso di lavoro.

### Server MCP Remoto (Consigliato)

Accedi al server MCP ospitato senza alcuna configurazione o installazione locale. Questo è il modo più veloce per iniziare.

<Frame>
  <iframe 
      className="w-full aspect-video rounded-md" 
      src="https://www.youtube.com/embed/TxGgYxNmkT4"
      title="Using Dodo Payment's Remote MCP Server"
      frameBorder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowFullScreen
  ></iframe>
</Frame>

<Steps>
    Vai su [https://mcp.dodopayments.com](https://mcp.dodopayments.com) nel tuo browser.
</Step>

{/* LOCKED_PATTERN_b8d4e4a1a3c0fcf28fd2afe7ce9e8316 */}
    Copia il JSON fornito per il tuo client specifico. Per Cursor o Claude Desktop, aggiungilo alle tue impostazioni MCP:
    

    ```json
    {
      "mcpServers": {
        "dodopayments_api": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8be57788a275cad53ee59f91af51bfd5 */}
    Il flusso OAuth ti chiederà di:
    - Inserire la tua chiave API di Dodo Payments
    - Selezionare il tuo ambiente (test o live)
    - Scegliere il tipo di client MCP
    
    <Warning>
    Tieni la tua chiave API al sicuro. Usa chiavi in modalità test durante lo sviluppo.
    </Warning>
</Step>

{/* LOCKED_PATTERN_606fac2bd646ef76f5d66097882887ed */}
    Clicca **Login and Approve** per autorizzare la connessione.
    
    <Check>
    Una volta connesso, il tuo assistente IA può interagire con l'API Dodo Payments per tuo conto.
    </Check>
</Step>
</Steps>

### Pacchetto NPM

Installa ed esegui il server MCP localmente utilizzando NPM.

<Tabs>
{/* LOCKED_PATTERN_c8776436a7ae17e188a287e7c6852f16 */}

```bash
# Set your environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."
export DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key"
export DODO_PAYMENTS_ENVIRONMENT="live_mode"

# Run the latest version
npx -y dodopayments-mcp@latest
```

</Tab>

{/* LOCKED_PATTERN_8d04e6cca1a5903f80148c5b923e5c3f */}

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

</Tab>
</Tabs>

<Tip>
Usa `@latest` per ottenere sempre la versione più recente oppure fissa una versione specifica come `@1.0.0` per maggiore stabilità.
</Tip>

### Docker

Esegui il server MCP in un ambiente containerizzato per un deployment coerente.

<CodeGroup>

```bash Docker
# Pull the latest image
docker pull ghcr.io/dodopayments/mcp:latest

# Run the container
docker run -e DODO_PAYMENTS_API_KEY="dodo_test_..." \
  -e DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -e DODO_PAYMENTS_ENVIRONMENT="live_mode" \
  ghcr.io/dodopayments/mcp:latest
```

```yaml Docker Compose
services:
  dodopayments-mcp:
    image: ghcr.io/dodopayments/mcp:latest
    environment:
      - DODO_PAYMENTS_API_KEY=dodo_test_...
      - DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_key
      - DODO_PAYMENTS_ENVIRONMENT=live_mode
    restart: unless-stopped
```

</CodeGroup>

<Note>
Le immagini Docker sono disponibili su [GitHub Container Registry](https://github.com/orgs/dodopayments/packages/container/package/mcp).
</Note>

## Configurazione Client

Configura il server Dodo Payments MCP nel tuo client AI preferito.

<Tabs>
<Tab title="Cursor">
Configura il server MCP di Dodo Payments in Cursor per abilitare l'accesso conversazionale ai tuoi dati di pagamento.

**Installazione con un clic**

Usa il pulsante qui sotto per installare direttamente il server MCP in Cursor:

[

![Aggiungi a Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)

](https://cursor.com/en-US/install-mcp?name=dodopayments-mcp&config=)

Dopo aver cliccato, imposta le variabili d'ambiente in Cursor's `mcp.json` tramite **Cursor Settings** > **Tools & MCP** > **New MCP Server**.

**Configurazione Manuale**

<Steps>
{/* LOCKED_PATTERN_b5120fe2afbf9bb96d1de6f9549e8f41 */}
    Vai su **Cursor Settings** > **Features** > **Model Context Protocol** oppure premi `Cmd/Ctrl + Shift + P` e cerca "MCP Settings".
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    Clicca **Edit Config** per aprire il file `mcp_config.json`.
</Step>

{/* LOCKED_PATTERN_a65e4e236194f7c177caa156c6d0b797 */}
    Scegli una delle seguenti configurazioni:
    
    **Server remoto (consigliato)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **NPX Locale**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8368e3e8540b47a92000151cb7e30a33 */}
    Salva il file di configurazione e riavvia Cursor.
    
    <Check>
    Verifica la connessione chiedendo all'assistente IA dei tuoi dati Dodo Payments.
    </Check>
</Step>
</Steps>
</Tab>

<Tab title="Claude Desktop">
Configura il server MCP di Dodo Payments in Claude Desktop per abilitare l'accesso conversazionale ai tuoi dati di pagamento.
<Steps>
{/* LOCKED_PATTERN_ecfdd2e916ed4a93d6b3950e9027df3f */}
    Trova il file di configurazione di Claude Desktop:
    
    - **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
    - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
    - **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    Apri il file in un editor di testo e aggiungi la configurazione di Dodo Payments:
    
    **Server remoto (consigliato)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **NPX Locale**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_83563b32c3caf8bd7aa68192b6ebcc7d */}
    Chiudi e riapri Claude Desktop perché le modifiche abbiano effetto.
    
    <Tip>
    Cerca l'icona 🔌 in Claude Desktop per confermare che il server MCP sia connesso.
    </Tip>
</Step>
</Steps>
</Tab>

<Tab title="Claude Code">
Configura il server MCP di Dodo Payments in Claude Code CLI.

Esegui il seguente comando nel tuo terminale:

```bash
claude mcp add --transport stdio dodopayments_api \
  --env DODO_PAYMENTS_API_KEY="dodo_test_..." \
  DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -- npx -y dodopayments-mcp
```

La configurazione verrà salvata nel file `.claude.json` nella tua directory home.

Claude Code rileva automaticamente le variabili d'ambiente dalla tua shell, quindi puoi anche esportarle prima di eseguire il comando.

<Tab title="VS Code">
Configura il server MCP di Dodo Payments in VS Code.

**Installazione con un clic**

[Apri VS Code](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dodopayments-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22dodopayments-mcp%22%5D%2C%22env%22%3A%7B%22DODO_PAYMENTS_API_KEY%22%3A%22Imposta%20qui%20la%20tua%20DODO_PAYMENTS_API_KEY.%22%2C%22DODO_PAYMENTS_WEBHOOK_KEY%22%3A%22Imposta%20qui%20la%20tua%20DODO_PAYMENTS_WEBHOOK_KEY.%22%7D%7D)

Dopo aver cliccato, imposta le variabili d'ambiente in VS Code's `mcp.json` tramite **Command Palette** > **MCP: Open User Configuration**.

**Configurazione Manuale**

<Steps>
{/* LOCKED_PATTERN_01c11d820b190ade4d2a29feff5a49da */}
    Apri la Command Palette (`Cmd/Ctrl + Shift + P`) e cerca "MCP: Open User Configuration".
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Aggiungi il server MCP di Dodo Payments:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_fabb79a0cecb26359867d78b61382dd7 */}
    Ricarica VS Code (`Cmd/Ctrl + Shift + P` > "Reload Window") per attivare il server MCP.
</Step>
</Steps>
</Tab>

<Tab title="Cline (VS Code)">
Configura il server MCP di Dodo Payments in Cline (VS Code) per abilitare l'accesso conversazionale ai tuoi dati di pagamento.

<Steps>
{/* LOCKED_PATTERN_43db9eecf5eea8e9c4ad60b6c3ff1c0b */}
    Individua il file di configurazione di Cline:
    
    - **macOS**: `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
    - **Windows**: `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    - **Linux**: `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
</Step>

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_43be57c73e24741f16da5f79d5398749 */}
    Ricarica la finestra di VS Code (`Cmd/Ctrl + Shift + P` > "Reload Window") per attivare la connessione al server MCP.
</Step>
</Steps>
</Tab>

<Tab title="Zed">
Configura il server MCP di Dodo Payments in Zed per abilitare l'accesso conversazionale ai tuoi dati di pagamento.
<Steps>
{/* LOCKED_PATTERN_a48799001554eb7ef4b2ac1e56fab915 */}
    Vai su **Settings** > **Assistant** > **Model Context Protocol**.
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Aggiungi la seguente configurazione:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_b1c8821108a3bf0ac4028a0196176d66 */}
    Salva la configurazione. Il server MCP dovrebbe ora essere disponibile nell'assistente IA di Zed.
</Step>
</Steps>
</Tab>

{/* LOCKED_PATTERN_71257692833d303ebb288457e9ce7134 */}
Configura il server MCP di Dodo Payments in altri client compatibili MCP.

**Server Remoto (Consigliato)**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

**NPX Locale**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
      }
    }
  }
}
```

<Info>
Visita [modelcontextprotocol.io/clients](https://modelcontextprotocol.io/clients) per un elenco completo dei client compatibili MCP e dei loro metodi di configurazione.
</Info>
</Tab>
</Tabs>

## Variabili d'Ambiente

Configura il comportamento del server MCP utilizzando variabili d'ambiente.

| Variabile | Descrizione | Obbligatorio |
|----------|-------------|----------|
| `DODO_PAYMENTS_API_KEY` | La tua chiave API di Dodo Payments | Sì |
| `DODO_PAYMENTS_WEBHOOK_KEY` | La tua chiave di firma webhook | No |
| `DODO_PAYMENTS_ENVIRONMENT` | Imposta su `live_mode` per la produzione | No |

## Esecuzione Remota

Distribuisci il server MCP come server HTTP remoto per client web o flussi di lavoro agentici.

```bash
# Start as remote server
npx -y dodopayments-mcp --transport=http

# Specify a custom port
npx -y dodopayments-mcp --transport=http --port=3000
```

### Configurazione Server Remoto

Una volta distribuito, i client possono connettersi utilizzando l'URL del server:

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "url": "http://localhost:3000",
      "headers": {
        "Authorization": "Bearer your_api_key"
      }
    }
  }
}
```

### Intestazioni di Autorizzazione

Il server remoto accetta l'autenticazione tramite le seguenti intestazioni:

| Intestazione | Descrizione |
|--------|-------------|
| `Authorization` | Autenticazione tramite token Bearer |
| `x-dodo-payments-api-key` | Intestazione diretta della chiave API |

## Capacità del Client

Diversi client MCP hanno livelli di supporto variabili per la specifica MCP. Il server può regolare automaticamente gli schemi degli strumenti per aggirare queste limitazioni.

```bash
# Auto-detect client (default)
npx dodopayments-mcp --client=infer

# Configure for specific clients
npx dodopayments-mcp --client=cursor
npx dodopayments-mcp --client=claude
npx dodopayments-mcp --client=claude-code
npx dodopayments-mcp --client=openai-agents
```

### Preset Client Disponibili

| Client | Descrizione |
|--------|-------------|
| `infer` | Rilevamento automatico del client (predefinito) |
| `cursor` | Editor Cursor |
| `claude` | Claude AI web o desktop |
| `claude-code` | Claude Code CLI |
| `openai-agents` | Piattaforma agenti OpenAI |

### Configurazione Manuale delle Capacità

Per i client non elencati sopra, configura manualmente le capacità:

```bash
# Disable $ref pointer support and set max tool name length
npx dodopayments-mcp --no-capability=refs --capability=tool-name-length=40
```

Funzionalità disponibili:
- `top-level-unions` - Supporto per union type di livello superiore
- `valid-json` - Parsing corretto degli argomenti stringa JSON
- `refs` - Supporto per riferimenti $ref negli schemi
- `unions` - Supporto per tipi union (anyOf)
- `formats` - Supporto per le validazioni di formato
- `tool-name-length=N` - Lunghezza massima del nome dello strumento

<Tip>
Esegui `npx dodopayments-mcp --describe-capabilities` per ottenere informazioni dettagliate sulle funzionalità disponibili.
</Tip>

## Filtro degli Strumenti

Controlla quali operazioni API sono esposte al tuo assistente AI per ottimizzare le prestazioni e mantenere la sicurezza.

### Filtra per tipo di operazione

Restringi l'accesso alle operazioni di sola lettura o scrittura.

```bash
# Only expose read operations (GET requests)
npx dodopayments-mcp --operation=read

# Only expose write operations (POST, PUT, DELETE)
npx dodopayments-mcp --operation=write
```

<Tip>
Usa `--operation=read` durante lo sviluppo per evitare modifiche accidentali ai tuoi dati.
</Tip>

### Filtra per risorsa

Limita gli strumenti a risorse API specifiche.

```bash
# Only expose payment-related tools
npx dodopayments-mcp --resource=payments

# Expose multiple resources
npx dodopayments-mcp --resource=payments --resource=customers

# Use wildcards for resource groups
npx dodopayments-mcp --resource=subscription*
```

### Includi o escludi strumenti specifici

Affina quali strumenti individuali sono disponibili.

```bash
# Include only specific tools
npx dodopayments-mcp --tool=create_payments --tool=list_payments

# Exclude specific tools
npx dodopayments-mcp --no-tool=delete_products

# Disable the docs search tool
npx dodopayments-mcp --no-tools=docs

# List all available tools
npx dodopayments-mcp --list
```

### Combina filtri

Applica più filtri simultaneamente per un controllo preciso.

```bash
# Only payment and subscription read operations
npx dodopayments-mcp --operation=read --resource=payments --resource=subscriptions
```

### Filtro dei Parametri di Query URL (Server Remoto)

Quando utilizzi il server remoto, applica filtri tramite parametri di query URL:

```
https://mcp.dodopayments.com/?operation=read
https://mcp.dodopayments.com/?resource=payments
```

## Modalità Strumenti Dinamici

Per API di grandi dimensioni, caricare tutti gli strumenti contemporaneamente può sovraccaricare la finestra di contesto dell'AI. La modalità strumenti dinamici fornisce un'alternativa più efficiente.

```bash
npx dodopayments-mcp --tools=dynamic
```

In modalità dinamica, tre meta-strumenti sostituiscono strumenti API individuali:

1. **`list_api_endpoints`** - Scopre i metodi disponibili con filtraggio opzionale
2. **`get_api_endpoint_schema`** - Ottiene lo schema dettagliato per un metodo specifico
3. **`invoke_api_endpoint`** - Esegue qualsiasi metodo con i parametri appropriati

<Info>
Gli strumenti dinamici vengono abilitati automaticamente per le API con più di 50 metodi. Sovrascrivili con `--tools=all` per includere tutti gli strumenti.
</Info>

```bash
# Use both dynamic tools and specific resources
npx dodopayments-mcp --tools=dynamic --resource=payments
```

## Utilizzo Avanzato

### Integrazione Programmatica

Costruisci server MCP personalizzati o estendi quello esistente programmaticamente.

```javascript
import { server, endpoints, init } from "dodopayments-mcp/server";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

// Initialize with all default endpoints
init({ server, endpoints });

// Start the server
const transport = new StdioServerTransport();
await server.connect(transport);
```

### Sviluppo di Strumenti Personalizzati

Estendi il server MCP con i tuoi strumenti e logica aziendale.

```javascript
import { server, init } from "dodopayments-mcp/server";
import createPayments from "dodopayments-mcp/tools/payments/create-payments";
import { zodToJsonSchema } from "zod-to-json-schema";
import { z } from "zod";

// Define a custom tool
const customTool = {
  tool: {
    name: 'calculate_payment_total',
    description: 'Calculate payment total with tax and fees',
    inputSchema: zodToJsonSchema(
      z.object({
        amount: z.number().describe('Base amount in cents'),
        tax_rate: z.number().describe('Tax rate as decimal'),
      })
    ),
  },
  handler: async (client, args) => {
    const total = args.amount * (1 + args.tax_rate);
    return {
      content: [{
        type: 'text',
        text: JSON.stringify({ total, currency: 'usd' }),
      }],
    };
  },
};

// Initialize with custom tools
init({ 
  server, 
  endpoints: [createPayments, customTool] 
});
```

## Migliori Pratiche di Sicurezza

Proteggi le tue credenziali API e mantieni integrazioni sicure.

<AccordionGroup>
{/* LOCKED_PATTERN_58b09b66cd00f40de384d770573ae848 */}
**Non inserire mai le credenziali nel controllo versione**

Memorizza le chiavi API in variabili d'ambiente o sistemi di gestione segreti sicuri.

```bash
# Use environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."

# Use a .env file (add to .gitignore)
echo "DODO_PAYMENTS_API_KEY=dodo_test_..." > .env
```

**Ruota le chiavi regolarmente**

Genera nuove chiavi API periodicamente e revoca quelle vecchie tramite il tuo dashboard di Dodo Payments.

**Usa chiavi di test per lo sviluppo**

Usa sempre chiavi API in modalità test durante lo sviluppo per evitare di influire sui dati di produzione.
</Accordion>

{/* LOCKED_PATTERN_1d0876565ecc6c625335a3849461e889 */}
**Applica il filtraggio degli strumenti in produzione**

Limita le operazioni esposte solo a ciò di cui il tuo assistente AI ha bisogno.

```bash
# Production: read-only access
npx dodopayments-mcp --operation=read

# Development: full access
npx dodopayments-mcp
```

**Implementa l'autenticazione per i server remoti**

Quando distribuisci da remoto, richiedi sempre l'autenticazione tramite l'intestazione `Authorization` o `x-dodo-payments-api-key`.

**Monitora l'uso delle API**

Monitora l'attività del server MCP tramite la dashboard Dodo Payments e imposta avvisi per schemi insoliti.
</Accordion>

{/* LOCKED_PATTERN_ddb4fc82978c8dda72d606214fdad81d */}
**Usa HTTPS per i server remoti**

Distribuisci sempre i server MCP remoti dietro endpoint HTTPS.

**Implementa il rate limiting**

Proteggi contro abusi implementando limiti di frequenza sia a livello di server MCP che di API.

**Limita l'accesso alla rete**

Configura regole firewall per limitare i client che possono connettersi al tuo server MCP.
</Accordion>
</AccordionGroup>

## Risoluzione dei Problemi

<AccordionGroup>
{/* LOCKED_PATTERN_3e10fc2511884be492da344a929e2311 */}
**Verifica la tua chiave API**

Assicurati che la tua chiave API sia impostata correttamente e abbia le autorizzazioni necessarie.

```bash
# Test your API key
curl -H "Authorization: Bearer dodo_test_..." \
  https://test.dodopayments.com/payments
```

**Controlla la tua connessione di rete**

Verifica di poter raggiungere gli endpoint API di Dodo Payments.

**Esamina i log del client**

Abilita il logging verboso nel tuo client MCP per diagnosticare problemi di connessione.
</Accordion>

{/* LOCKED_PATTERN_8f29ea613a43315c29bf194043f8ada2 */}
**Conferma l'ambiente della chiave API**

Assicurati di utilizzare chiavi di test con endpoint di test e chiavi live con endpoint di produzione.

**Controlla la variabile d'ambiente**

Verifica che `DODO_PAYMENTS_ENVIRONMENT` sia impostato correttamente (`live_mode` per la produzione).

**Rigenera le credenziali**

Se i problemi persistono, genera una nuova chiave API tramite la tua dashboard.
</Accordion>

{/* LOCKED_PATTERN_565675849d369f82d965d1f3a122632b */}
**Valida i parametri di input**

Assicurati che l'assistente AI stia fornendo parametri formattati correttamente per ciascun strumento.

**Esamina i messaggi di errore**

Controlla la risposta di errore dall'API per indicazioni specifiche su cosa sia andato storto.

**Testa direttamente con l'API**

Verifica che l'operazione funzioni chiamando direttamente l'API di Dodo Payments tramite curl o Postman.
</Accordion>

{/* LOCKED_PATTERN_776734428589daf9897a729aa0a38aed */}
**Prova un preset client diverso**

Se gli strumenti non funzionano correttamente, prova a specificare esplicitamente il tuo client:

```bash
npx dodopayments-mcp --client=cursor
```

**Controlla il supporto delle capacità**

Alcuni client non supportano tutte le funzionalità di JSON schema. Usa `--describe-capabilities` per capire cosa è supportato.
</Accordion>
</AccordionGroup>

## Risorse

<CardGroup cols={2}>
{/* LOCKED_PATTERN_4325afcb089e351f99ef591b38203ac2 */}
  Esplora la documentazione completa dell'API Dodo Payments
</Card>

{/* LOCKED_PATTERN_1cbca36bdeb2bbbc77032220bd30c277 */}
  Scopri di più sullo standard Model Context Protocol
</Card>

{/* LOCKED_PATTERN_02488b19a875a7c4cd24c2f5651e4831 */}
  Visualizza il codice sorgente del server MCP e contribuisci
</Card>

{/* LOCKED_PATTERN_d4dbe77bbb87d236d98e318084853b86 */}
  Installa il server MCP da NPM
</Card>
</CardGroup>
