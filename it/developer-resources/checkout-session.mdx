---
title: Sessioni di Checkout
description: Crea esperienze di checkout sicure e ospitate che gestiscono
  l'intero flusso di pagamento per acquisti una tantum e abbonamenti con pieno
  controllo sulla personalizzazione.
icon: cart-shopping
---
<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  Avvia la tua prima sessione di checkout in meno di 5 minuti
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  Esplora la documentazione API completa e testa interattivamente le richieste e le risposte delle sessioni di Checkout.
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  Calcola prezzi, tasse e totali prima di creare una sessione.
</Card>
</CardGroup>

<Info>
**Validità della sessione**: Le sessioni di checkout sono valide per 24 ore di default. Se passi `confirm=true` nella tua richiesta, la sessione sarà valida solo 15 minuti.
</Info>

## Requisiti

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  Avrai bisogno di un account merchant Dodo Payments attivo con accesso API.
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  Genera le tue credenziali API dalla dashboard Dodo Payments:
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  Crea i tuoi prodotti nella dashboard Dodo Payments prima di implementare le sessioni di checkout.
</Step>
</Steps>

## Creare la tua Prima Checkout Session

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### Risposta API

Tutti i metodi sopra restituiscono la stessa struttura di risposta:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
Estrai il `checkout_url` dalla risposta API.
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
Indirizza il tuo cliente all'URL di checkout per completare l'acquisto.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Opzioni alternative di integrazione**: Invece di reindirizzare, puoi incorporare il checkout direttamente nella tua pagina usando [Overlay Checkout](/developer-resources/overlay-checkout) (modal overlay) o [Inline Checkout](/developer-resources/inline-checkout) (fully embedded). Entrambe le opzioni usano lo stesso URL della sessione di checkout.
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
Dopo il pagamento, i clienti vengono reindirizzati al tuo `return_url` con parametri di query che includono l'ID del pagamento/abbonamento, lo stato, l'email del cliente e eventuali chiavi di licenza. Consulta la [documentazione dei parametri `return_url`](#request-body) per l'elenco completo.
</Step>
</Steps>

## Corpo della Richiesta

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  Campi essenziali necessari per ogni sessione di checkout
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  Configurazione aggiuntiva per personalizzare l'esperienza di checkout
</Card>
</CardGroup>

### Campi Obbligatori

<ParamField body="product_cart" type="array" required>
Array di prodotti da includere nella sessione di checkout. Ogni prodotto deve avere un `product_id` valido dalla tua dashboard Dodo Payments.

<Tip>
**Checkout misto**: Puoi combinare prodotti con pagamento una tantum e abbonamenti nella stessa sessione di checkout. Questo abilita casi d'uso potenti come commissioni di setup con abbonamenti, bundle hardware con SaaS e altro ancora.
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
L'identificatore univoco del prodotto dalla tua dashboard Dodo Payments.

**Esempio:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
Quantità del prodotto.

**Esempio:** `1` per un singolo articolo, `3` per quantità multiple
</ParamField>

<ParamField body="addons" type="array">
Array di componenti aggiuntivi da associare al prodotto. Valido solo se il prodotto è un abbonamento.

{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
Importo che il cliente paga se pay_what_you_want è abilitato. Se disabilitato, questo campo viene ignorato.

**Formato**: Rappresentato nella denominazione più bassa della valuta (ad es. centesimi per USD). Per esempio, per addebitare $1,00, passa `100`.
</ParamField>
</Expandable>
</ParamField>

<Tip>
**Trova i tuoi ID prodotto**: Puoi trovare gli ID prodotto nella tua dashboard Dodo Payments sotto Prodotti → Visualizza dettagli, o utilizzando la [List Products API](/api-reference/products/get-products).
</Tip>

### Campi Facoltativi

Configura questi campi per personalizzare l'esperienza di checkout e aggiungere logica aziendale al tuo flusso di pagamento.

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
Informazioni sul cliente. Puoi allegare un cliente esistente utilizzando il suo ID oppure creare un nuovo record durante il checkout.

<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}
Allega un cliente esistente alla sessione di checkout usando il suo ID.

{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
L'identificatore univoco di un cliente esistente. Usalo per associare la sessione di checkout a un cliente già presente invece di crearne uno nuovo.
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
Crea un nuovo record cliente durante il checkout.

{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
Indirizzo email del cliente. Obbligatorio quando si crea un nuovo cliente.
</ParamField>

<ParamField body="name" type="string">
Nome completo del cliente come dovrebbe apparire nelle ricevute e nelle fatture.

**Esempio**: `"John Doe"` o `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
Numero di telefono del cliente in formato internazionale. Obbligatorio per alcuni metodi di pagamento e per la prevenzione delle frodi.

**Formato**: Includi il prefisso internazionale, ad es. `"+1234567890"` per numeri USA
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
Informazioni sull'indirizzo di fatturazione per un calcolo accurato delle tasse, la prevenzione delle frodi e la conformità normativa.

<Info>
Quando `confirm` è impostato su `true`, tutti i campi dell'indirizzo di fatturazione diventano obbligatori per creare correttamente la sessione.
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
Indirizzo completo (via, numero civico e numero dell'appartamento/unità se applicabile).

**Esempio**: `"123 Main St, Apt 4B"` o `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
Nome della città o del comune.

**Esempio**: `"San Francisco"`, `"New York"`, `"London"`
</ParamField>

<ParamField body="state" type="string">
Nome dello stato, provincia o regione. Usa nomi completi o abbreviazioni standard.

**Esempio**: `"California"` o `"CA"`, `"Ontario"` o `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
Codice paese ISO a due lettere (ISO 3166-1 alpha-2). Questo campo è sempre obbligatorio quando viene fornito billing_address.

**Esempi**: `"US"` (Stati Uniti), `"CA"` (Canada), `"GB"` (Regno Unito), `"DE"` (Germania)

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  Visualizza l'elenco completo dei paesi supportati e dei loro codici ISO
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
Codice postale, CAP o equivalente in base ai requisiti del paese.

**Esempi**: `"94102"` (USA), `"M5V 3A8"` (Canada), `"SW1A 1AA"` (Regno Unito)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
Controlla quali metodi di pagamento sono disponibili per i clienti durante il checkout. Questo aiuta a ottimizzare per mercati o requisiti aziendali specifici.

**Opzioni disponibili**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**Critico**: Includi sempre `credit` e `debit` come opzioni di fallback per evitare errori di checkout quando i metodi di pagamento preferiti non sono disponibili.
</Warning>

**Esempio**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
Sovrascrivi la selezione della valuta predefinita con una valuta di fatturazione fissa. Utilizza i codici valuta ISO 4217.

**Valute supportate**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR` e altre

**Esempio**: `"USD"` per dollari USA, `"EUR"` per euro

<Note>
Questo campo è efficace solo quando il pricing adattivo è abilitato. Se il pricing adattivo è disabilitato, verrà utilizzata la valuta predefinita del prodotto.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
Mostra i metodi di pagamento salvati in precedenza per i clienti di ritorno, migliorando la velocità del checkout e l'esperienza utente.
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
URL a cui reindirizzare i clienti dopo il completamento del pagamento. Dodo Payments aggiunge i seguenti parametri di query al tuo URL durante il reindirizzamento:

| Parametro | Tipo | Condizione |
|-----------|------|-----------|
| `payment_id` | `string` | Sempre presente per pagamenti una tantum |
| `subscription_id` | `string` | Sempre presente per pagamenti in abbonamento |
| `status` | `string` | Sempre presente |
| `license_key` | `string` | Presente se il prodotto ha chiavi di licenza abilitate. Separato da virgole se ci sono più chiavi |
| `email` | `string` | Presente se il cliente ha un'email registrata |


**URL di reindirizzamento di esempio:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
Usa i parametri di query `license_key` e `email` per mostrare le chiavi di licenza o inviare una conferma immediata nella tua pagina di ritorno, senza dover effettuare una chiamata API aggiuntiva.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
Se impostato su true, finalizza immediatamente tutti i dettagli della sessione. L'API restituirà un errore se mancano dati obbligatori.
</ParamField>

<ParamField body="discount_code" type="string">
Applica un codice sconto alla sessione di checkout.
</ParamField>

<ParamField body="metadata" type="object">
Coppie chiave-valore personalizzate per memorizzare informazioni aggiuntive sulla sessione.
</ParamField>

<ParamField body="force_3ds" type="boolean">
Sovrascrivi il comportamento 3DS predefinito del merchant per questa sessione.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Abilita la modalità di raccolta indirizzi minima. Quando è abilitata, il checkout raccoglie solo:
- **Paese**: sempre obbligatorio per la determinazione delle tasse
- **CAP/Codice postale**: solo nelle regioni in cui è necessario per il calcolo di IVA, sales tax o GST

Ciò riduce significativamente l'attrito durante il checkout eliminando campi del modulo non necessari.

<Tip>
Abilita indirizzo minimo per completare il checkout più rapidamente. La raccolta completa dell'indirizzo rimane disponibile per le aziende che richiedono dettagli di fatturazione completi.
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
Personalizza l'aspetto e il comportamento dell'interfaccia di checkout.

{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
Tema per l'interfaccia di checkout. Opzioni: `light`, `dark` o `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
Mostra la sezione dei dettagli dell'ordine nell'interfaccia di checkout.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
Mostra il tag “on-demand” per i prodotti applicabili.
</ParamField>

<ParamField body="force_language" type="string">
Forza l'interfaccia di checkout a renderizzarsi in una lingua specifica. Di default, il checkout rileva automaticamente la lingua del browser del cliente.

**Codici lingua supportati:**
`ar` (arabo), `ca` (catalano), `de` (tedesco), `en` (inglese), `es` (spagnolo), `fr` (francese), `he` (ebraico), `id` (indonesiano), `it` (italiano), `ja` (giapponese), `ko` (coreano), `ms` (malese), `nl` (olandese), `pl` (polacco), `pt` (portoghese), `ro` (rumeno), `ru` (russo), `sv` (svedese), `th` (tailandese), `tr` (turco), `zh` (cinese)

**Esempio**: `"es"` per lo spagnolo, `"ja"` per il giapponese
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
Configura funzionalità e comportamenti specifici per la sessione di checkout.

{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
Consenti ai clienti di selezionare la valuta preferita durante il checkout.

<ParamField body="allow_discount_code" type="boolean" default="true">
Mostra il campo per inserire un codice sconto nell'interfaccia di checkout.

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
Raccogli i numeri di telefono dei clienti durante il checkout.

<ParamField body="allow_tax_id" type="boolean" default="true">
Consenti ai clienti di inserire numeri di identificazione fiscale.

<ParamField body="always_create_new_customer" type="boolean" default="false">
Forza la creazione di un nuovo record cliente invece di aggiornare quelli esistenti.

<ParamField body="allow_customer_editing_email" type="boolean">
Consenti ai clienti di modificare il proprio indirizzo email durante il checkout.

<ParamField body="allow_customer_editing_name" type="boolean">
Consenti ai clienti di modificare il proprio nome durante il checkout.

<ParamField body="allow_customer_editing_street" type="boolean">
Consenti ai clienti di modificare l'indirizzo durante il checkout.

<ParamField body="allow_customer_editing_city" type="boolean">
Consenti ai clienti di modificare la città durante il checkout.

<ParamField body="allow_customer_editing_state" type="boolean">
Consenti ai clienti di modificare lo stato durante il checkout.

<ParamField body="allow_customer_editing_country" type="boolean">
Consenti ai clienti di modificare il paese durante il checkout.

<ParamField body="allow_customer_editing_zipcode" type="boolean">
Consenti ai clienti di modificare il CAP durante il checkout.

<ParamField body="minimal_address" type="boolean" default="false">
Abilita la modalità di raccolta indirizzi minima. Quando è abilitata, il checkout raccoglie solo:
- **Paese**: sempre obbligatorio per la determinazione delle tasse
- **CAP/Codice postale**: solo nelle regioni in cui è necessario per il calcolo di IVA, sales tax o GST

Ciò riduce significativamente l'attrito durante il checkout eliminando campi del modulo non necessari.

<Tip>
Abilita indirizzo minimo per completare il checkout più rapidamente. La raccolta completa dell'indirizzo rimane disponibile per le aziende che richiedono dettagli di fatturazione completi.
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
Salta la pagina di successo di pagamento predefinita e reindirizza immediatamente i clienti al tuo `return_url` dopo il completamento del pagamento.

<Tip>
Usalo quando hai una pagina di successo personalizzata che offre un'esperienza utente migliore, oppure per app mobili e flussi di checkout incorporati.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
Raccogli informazioni aggiuntive dai clienti durante il checkout con campi modulo personalizzati. Puoi definire fino a 5 campi personalizzati per sessione di checkout. Le risposte dei clienti sono incluse nei payload dei webhook e disponibili via API.

{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
Identificatore univoco per questo campo. Usato come chiave nei payload dei webhook e nelle risposte API.

**Esempio**: `"company_name"`, `"referral_source"`, `"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
Etichetta visualizzata al cliente nel modulo di checkout.

**Esempio**: `"Company Name"`, `"How did you hear about us?"`, `"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
Tipo di campo che determina le regole di validazione dell'input.

**Tipi disponibili**:
- `text` - Campo di testo a riga singola
- `number` - Input numerico
- `email` - Indirizzo email con validazione
- `url` - URL con validazione
- `date` - Selettore di data
- `dropdown` - Selezione da opzioni predefinite
- `boolean` - Interruttore Sì/No
</ParamField>

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
Se questo campo deve essere compilato prima che il checkout possa completarsi.

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
Testo segnaposto visualizzato nel campo di input.

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
Elenco delle opzioni per il tipo di campo `dropdown`. Obbligatorio quando `field_type` è `dropdown`, ignorato per altri tipi.

**Esempio**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
Le risposte dei clienti ai campi personalizzati sono incluse in:
- **Webhook**: `payment.succeeded`, `subscription.created` e altri payload di eventi pertinenti contengono l'array `custom_field_responses`
- **Risposte API**: gli oggetti pagamento e abbonamento includono `custom_field_responses`
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
Configurazione aggiuntiva per sessioni di checkout che contengono prodotti in abbonamento.

{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
Numero di giorni per il periodo di prova prima del primo addebito. Deve essere compreso tra 0 e 10000 giorni.
</ParamField>

<ParamField body="on_demand" type="object">
Configurazione per abbonamenti on-demand per fatturazione basata su utilizzo o metrica.

{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
Se impostato su true, non viene effettuato alcun addebito e viene solo autorizzato il metodo di pagamento per usi futuri.
</ParamField>

<ParamField body="product_price" type="integer">
Prezzo del prodotto per l'addebito iniziale al cliente. Se non specificato, viene utilizzato il prezzo memorizzato del prodotto.

**Formato**: Rappresentato nella denominazione più bassa della valuta (ad es. centesimi per USD). Per esempio, per addebitare $1,00, passa `100`.
</ParamField>

<ParamField body="product_currency" type="string">
Valuta opzionale per il prezzo del prodotto. Se non specificata, viene usata la valuta del prodotto.

<ParamField body="product_description" type="string">
Sovrascrittura opzionale della descrizione del prodotto per fatturazione e voci di linea. Se non specificata, viene usata la descrizione memorizzata del prodotto.

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
Se le commissioni in valuta adattiva devono essere incluse nel prezzo del prodotto (true) o aggiunte sopra (false). Ignorato se il pricing adattivo non è abilitato.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## Esempi di utilizzo

Ecco 10 esempi completi che mostrano diverse configurazioni di sessioni di checkout per vari scenari aziendali:

### 1. Checkout semplice per prodotto singolo

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. Carrello con più prodotti

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. Abbonamento con periodo di prova

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. Checkout preconfermato

<Note>
Quando `confirm` è impostato su `true`, il cliente viene portato direttamente alla pagina di checkout, saltando eventuali passaggi di conferma.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. Checkout con sovrascrittura valuta

<Note>
La sovrascrittura `billing_currency` ha effetto solo quando la valuta adattiva è abilitata nelle impostazioni del tuo account. Se la valuta adattiva è disabilitata, questo parametro non avrà effetto.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. Metodi di pagamento salvati per clienti di ritorno

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. Checkout B2B con raccolta del codice fiscale

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. Checkout con tema scuro e codice sconto

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. Metodi di pagamento regionali (UPI per l'India)

Per informazioni dettagliate sulla configurazione e i test di UPI, consulta la pagina <a href="/features/payment-methods/india">India Payment Methods</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. Checkout BNPL (Buy Now Pay Later)

Per informazioni dettagliate sulla configurazione e i test BNPL, consulta la pagina <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. Utilizzo di metodi di pagamento esistenti per checkout istantaneo

Usa un metodo di pagamento salvato del cliente per creare una sessione di checkout che viene processata immediatamente, saltando la raccolta del metodo di pagamento:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
Quando usi `payment_method_id`, `confirm` deve essere impostato su `true` e deve essere fornito un `customer_id` esistente. Il metodo di pagamento verrà validato per l'idoneità con la valuta del pagamento.
</Warning>

<Info>
Il metodo di pagamento deve appartenere al cliente ed essere compatibile con la valuta del pagamento. Questo abilita acquisti con un solo clic per i clienti di ritorno.
</Info>

### 12. Short Link per URL di pagamento più puliti

Genera link di pagamento abbreviati e condivisibili con slug personalizzati:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
I short link sono perfetti per SMS, email o condivisioni sui social media. Sono più facili da ricordare e trasmettono maggiore fiducia rispetto agli URL lunghi.
</Tip>

### 13. Salta la pagina di successo con reindirizzamento immediato

Reindirizza immediatamente i clienti dopo il completamento del pagamento, bypassando la pagina di successo predefinita:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
Usa `redirect_immediately: true` quando hai una pagina di successo personalizzata che offre una migliore esperienza utente rispetto alla pagina di successo predefinita. Questo è particolarmente utile per app mobili e flussi di checkout incorporati.
</Tip>

<Note>
Quando `redirect_immediately` è abilitato, i clienti vengono reindirizzati al tuo `return_url` immediatamente dopo il completamento del pagamento, saltando completamente la pagina di successo predefinita.
</Note>

### 14. Forzare una lingua

Forza il checkout a visualizzare una lingua specifica, sovrascrivendo il rilevamento della lingua del browser del cliente:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
Usa `force_language` quando conosci la lingua preferita del cliente (ad es. dalle impostazioni del suo account) o quando ti rivolgi a mercati regionali specifici.
</Tip>

**Lingue supportate:** Arabo (`ar`), Catalano (`ca`), Cinese (`zh`), Olandese (`nl`), Inglese (`en`), Francese (`fr`), Tedesco (`de`), Ebraico (`he`), Indonesiano (`id`), Italiano (`it`), Giapponese (`ja`), Coreano (`ko`), Malese (`ms`), Polacco (`pl`), Portoghese (`pt`), Romeno (`ro`), Russo (`ru`), Spagnolo (`es`), Svedese (`sv`), Tailandese (`th`), Turco (`tr`)

### 15. Raccolta di campi personalizzati

Raccogli informazioni aggiuntive dai clienti durante il checkout usando campi personalizzati:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
Le risposte ai campi personalizzati vengono automaticamente incluse nei payload dei webhook (`payment.succeeded`, `subscription.created`, ecc.) e possono essere recuperate tramite API. Usale per arricchire il tuo CRM, attivare flussi di onboarding o personalizzare l'esperienza cliente.
</Info>

**Tipi di campo disponibili:** `text`, `number`, `email`, `url`, `date`, `dropdown`, `boolean`

## Anteprima delle sessioni di checkout

Prima di creare una sessione di checkout, puoi visualizzare l'anteprima del calcolo dei prezzi includendo tasse, sconti e totali. Questo è utile per mostrare prezzi accurati ai clienti prima che procedano al checkout.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  Visualizza la documentazione completa dell'endpoint di anteprima.
</Card>

## Passare da Dynamic Links a Checkout Sessions

### Differenze chiave
In precedenza, quando creavi un link di pagamento con Dynamic Links, era necessario fornire l'indirizzo di fatturazione completo del cliente.

Con Checkout Sessions, non è più necessario. Puoi semplicemente inviare le informazioni che hai, e ci pensiamo noi. Ad esempio:

- Se conosci solo il paese di fatturazione del cliente, forniscilo.
- Il flusso di checkout raccoglierà automaticamente i dettagli mancanti prima di portare il cliente alla pagina di pagamento.
- D'altra parte, se hai già tutte le informazioni richieste e vuoi saltare direttamente alla pagina di pagamento, puoi inviare l'intero set di dati e includere `confirm=true` nel body della richiesta.

### Processo di migrazione

La migrazione da Dynamic Links a Checkout Sessions è semplice:

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
Aggiorna la tua integrazione per usare il nuovo metodo API o SDK.
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
Adatta il payload della richiesta in base al formato di Checkout Sessions.
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
Sì. Non sono necessari passaggi aggiuntivi o migrazioni speciali da parte tua.
</Step>
</Steps>
