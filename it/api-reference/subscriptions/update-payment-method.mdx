---
openapi: post /subscriptions/{subscription_id}/update-payment-method
title: Aggiorna Metodo di Pagamento
description: Aggiorna il metodo di pagamento per un abbonamento esistente. Puoi
  aggiungere un nuovo metodo di pagamento o utilizzare uno esistente dai metodi
  di pagamento salvati del cliente.
---

Aggiorna il metodo di pagamento per un abbonamento. Questo endpoint supporta sia abbonamenti attivi che abbonamenti in `on_hold` stato.

<Info>
Per gli abbonamenti in `on_hold` stato, l'aggiornamento del metodo di pagamento crea automaticamente un addebito per le somme dovute, genera una fattura e riattiva l'abbonamento allo stato di `active` dopo un pagamento riuscito.
</Info>

### Casi d'Uso

- **Abbonamenti attivi**: Aggiorna il metodo di pagamento quando una carta scade o il cliente desidera utilizzare un metodo di pagamento diverso
- **Abbonamenti in sospeso**: Riattiva abbonamenti che sono stati messi in sospeso a causa di pagamenti non riusciti aggiornando il metodo di pagamento
- **Gestione dei metodi di pagamento**: Passa tra i metodi di pagamento salvati o aggiungi nuovi metodi

<Info>
Per elencare i metodi di pagamento esistenti per un cliente, utilizza l'[API Elenco Metodi di Pagamento](/api-reference/customers/get-customer-payment-methods). Questo ti aiuta a recuperare gli ID dei metodi di pagamento disponibili quando utilizzi `type: "existing"` per aggiornare il metodo di pagamento di un abbonamento.
</Info>

### Comportamento per Abbonamenti Attivi

Quando aggiorni il metodo di pagamento per un abbonamento attivo:

- Il metodo di pagamento viene aggiornato immediatamente
- Non viene creato alcun addebito
- L'abbonamento rimane attivo
- I rinnovi futuri utilizzeranno il nuovo metodo di pagamento

### Comportamento per Abbonamenti in Sospeso

Quando aggiorni il metodo di pagamento per un abbonamento in `on_hold` stato:

1. Viene automaticamente creato un addebito per le somme dovute
2. Viene generata una fattura per l'addebito
3. Il pagamento viene elaborato utilizzando il nuovo metodo di pagamento
4. Dopo un pagamento riuscito, l'abbonamento viene riattivato allo stato di `active`
5. Riceverai eventi webhook: `payment.succeeded` seguiti da `subscription.active`

<Warning>
Se il pagamento fallisce dopo aver aggiornato il metodo di pagamento per un abbonamento in `on_hold`, l'abbonamento rimarrà nello stato di `on_hold`. Monitora gli eventi webhook per tracciare lo stato del pagamento.
</Warning>

### Eventi Webhook

Quando aggiorni un metodo di pagamento per un abbonamento in `on_hold`, riceverai i seguenti eventi webhook:

1. **`payment.succeeded`** - L'addebito per le somme dovute è stato effettuato con successo
2. **`subscription.active`** - L'abbonamento è stato riattivato
