---
title: Dub
description: Verfolgen Sie Verkaufsumwandlungsereignisse in Dub, wenn Kunden
  Käufe über Dodo Payments abschließen.
icon: /images/logos/dub.svg
---

## Einführung

[Dub](https://dub.co) ist eine leistungsstarke Link-Management-Plattform, die Ihnen hilft, kurze Links zu erstellen, zu teilen und zu verfolgen. Durch die Integration von Dodo Payments mit Dub können Sie automatisch Verkaufsumwandlungsereignisse verfolgen, wenn Kunden Käufe abschließen, sodass Sie den ROI Ihrer Marketingkampagnen und Empfehlungsprogramme messen können.

Ein "Verkauf"-Ereignis wird in Dub aufgezeichnet, wenn ein Kunde:
- Eine einmalige Zahlung abschließt
- Ein kostenpflichtiges Abonnement abonniert
- Eine wiederkehrende Abonnementzahlung leistet

<Info>
Diese Integration erfordert ein Dub-Konto mit aktiviertem Conversion-Tracking für deine Links.
</Info>

<Tip>
**Integration des Affiliate-Programms**: Diese Integration funktioniert auch nahtlos mit **Dub Partners** zur Verfolgung von Affiliate-Empfehlungen und Provisionen. Nutze Dubs Conversion-Tracking, um Verkäufe deinen Affiliate-Links zuzuordnen und die Partnerleistung zu messen. Erfahre mehr über die Einrichtung von Affiliate-Programmen in unserem [Affiliates feature guide](/features/affiliates).
</Tip>

## So funktioniert es

Dub verfolgt Besucher über eine einzigartige Klick-ID (`dub_id`), die in einem Cookie gespeichert wird, wenn Nutzer auf deine Dub-Shortlinks klicken. Um Verkäufe deinen Links zuzuordnen, musst du:

1. **Erfasse die Klick-ID von Dub** aus dem `dub_id`-Cookie, wenn du Checkout-Sessions erstellst
2. **Speichere die Klick-ID** in deinen Zahlungs-Metadaten zusammen mit der externen Kunden-ID
3. **Sende Verkaufsdaten an Dub**, wenn Zahlungen erfolgreich sind, und nutze dazu deren Track API

Dies ermöglicht es Dub, erfolgreiche Verkäufe mit dem ursprünglichen Link-Klick abzugleichen, sodass Sie eine vollständige Conversion-Zuordnung erhalten.

## Voraussetzungen

Bevor Sie diese Integration einrichten, stellen Sie sicher, dass Sie:

1. Ein [Dub-Konto](https://dub.co) mit einem Arbeitsbereich haben
2. Conversion-Tracking für Ihre Links aktiviert ist
3. Ihren Dub-API-Schlüssel (verfügbar in Ihrem Dub-Dashboard unter Einstellungen → API-Schlüssel)

## Erste Schritte

<Steps>
{/* LOCKED_PATTERN_61bbf97d09c18ff0f49ccd9c0f216ae2 */}
  Aktiviere in deinem Dub-Dashboard das Conversion-Tracking für die Links, deren Verkäufe du verfolgen möchtest. Dadurch kann Dub Verkaufsevents aufzeichnen, wenn Kunden Käufe abschließen.
<Info>
  Erfahre mehr über die Aktivierung des Conversion-Trackings in der [Dub documentation](https://dub.co/docs/conversions/quickstart).
  </Info>
</Step>

  <Info>
  Erfahren Sie mehr über die Aktivierung des Conversion-Trackings in der [Dub-Dokumentation](https://dub.co/docs/conversions/quickstart).
  </Info>
</Step>

{/* LOCKED_PATTERN_09ec83561863ba8dfb1b09650875a82a */}
  Navigiere zu deinem [Dub dashboard](https://app.dub.co) → Settings → API Keys und erstelle einen neuen API-Schlüssel mit dem Umfang `conversions.write`.
  <Warning>
  Halte deinen API-Schlüssel sicher und gib ihn niemals im Client-Code preis.
  </Warning>
</Step>

  <Warning>
  Halten Sie Ihren API-Schlüssel sicher und geben Sie ihn niemals im Client-Code preis.
  </Warning>
</Step>

{/* LOCKED_PATTERN_528142ce64ee996154bcf8fdaffc120b */}
  Wenn du eine Checkout-Session erstellst, erfasse die Dub-Klick-ID aus dem Cookie und füge sie deinen Zahlungs-Metadaten hinzu.
</Step>

{/* LOCKED_PATTERN_6e8a18d6b9667ee12eda72dd5a47db36 */}
  Konfiguriere einen Webhook, um Verkaufsdaten an Dubs Track API zu senden, wenn Zahlungen erfolgreich sind.
</Step>

{/* LOCKED_PATTERN_8d2cbcb809e6363cb1f41f0f62f98444 */}
  Verkaufs-Conversionsereignisse erscheinen nun in deinem Dub-Analytics-Dashboard mit vollständiger Zuordnung zu deinen Links.
</Step>
</Steps>

## Implementierungsleitfaden

### Schritt 1: Klick-ID und Kunden-ID zu den Checkout-Metadaten hinzufügen

Beim Erstellen einer Checkout-Sitzung erfassen Sie die Dub-Klick-ID aus dem Cookie und fügen Sie sie Ihren Zahlungsmetadaten zusammen mit der externen ID Ihres Kunden hinzu.

<CodeGroup>

```typescript Next.js
import { cookies } from 'next/headers';
import DodoPayments from 'dodopayments';

const client = new DodoPayments();

export async function createCheckout(productId: string, customerId: string) {
  // Capture Dub click ID from cookie
  const dubClickId = cookies().get('dub_id')?.value;

  const payment = await client.payments.create({
    billing: {
      city: 'New York',
      country: 'US',
      state: 'NY',
      street: '123 Main St',
      zipcode: '10001',
    },
    customer: {
      email: 'customer@example.com',
      name: 'John Doe',
    },
    product_id: productId,
    metadata: {
      dub_click_id: dubClickId,           // Store Dub click ID
      dub_external_id: customerId,        // Store your customer's unique ID
    },
  });

  return payment;
}
```

```javascript Express.js
const express = require('express');
const DodoPayments = require('dodopayments').default;
const router = express.Router();

const client = new DodoPayments();

router.post('/create-checkout', async (req, res) => {
  // Capture Dub click ID from cookie
  const dubClickId = req.cookies?.dub_id;

  const payment = await client.payments.create({
    billing: {
      city: 'New York',
      country: 'US',
      state: 'NY',
      street: '123 Main St',
      zipcode: '10001',
    },
    customer: {
      email: req.body.email,
      name: req.body.name,
    },
    product_id: req.body.productId,
    metadata: {
      dub_click_id: dubClickId,           // Store Dub click ID
      dub_external_id: req.body.customerId, // Store your customer's unique ID
    },
  });

  res.json({ payment });
});
```

```python Python (FastAPI)
from fastapi import FastAPI, Request
from dodopayments import DodoPayments

app = FastAPI()
client = DodoPayments()

@app.post("/create-checkout")
async def create_checkout(request: Request):
    # Capture Dub click ID from cookie
    dub_click_id = request.cookies.get("dub_id")

    body = await request.json()

    payment = client.payments.create(
        billing={
            "city": "New York",
            "country": "US",
            "state": "NY",
            "street": "123 Main St",
            "zipcode": "10001",
        },
        customer={
            "email": body["email"],
            "name": body["name"],
        },
        product_id=body["product_id"],
        metadata={
            "dub_click_id": dub_click_id,          # Store Dub click ID
            "dub_external_id": body["customer_id"], # Store your customer's unique ID
        },
    )

    return {"payment": payment}
```

```go Go
package main

import (
    "github.com/dodopayments/dodopayments-go"
)

func createCheckout(dubClickId, customerId, productId string) (*dodopayments.Payment, error) {
    client := dodopayments.NewClient()

    payment, err := client.Payments.Create(context.Background(), dodopayments.PaymentCreateParams{
        Billing: dodopayments.BillingParam{
            City:    "New York",
            Country: "US",
            State:   "NY",
            Street:  "123 Main St",
            Zipcode: "10001",
        },
        Customer: dodopayments.CustomerParam{
            Email: "customer@example.com",
            Name:  "John Doe",
        },
        ProductID: productId,
        Metadata: map[string]string{
            "dub_click_id":   dubClickId,  // Store Dub click ID
            "dub_external_id": customerId, // Store your customer's unique ID
        },
    })

    return payment, err
}
```

</CodeGroup>

### Schritt 2: Verkaufsdaten an Dub senden

Konfigurieren Sie einen Webhook-Endpunkt, um Verkaufsdaten an Dubs Track API zu senden, wenn Zahlungen erfolgreich sind.

<Steps>
{/* LOCKED_PATTERN_c337a250e2e6db475d2eb18e95cb3e6a */}
  Navigiere in deinem Dodo Payments-Dashboard zu **Webhooks → + Add Endpoint** und öffne das Integrationen-Dropdown.
  <Frame>
    <img src="/images/integrations/dub/add.png" alt="Endpunkt hinzufügen und Integrationen-Dropdown" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_99685f3488b80b5b23189beda3a369d0 */}
  Wähle die **Dub**-Integrationskarte aus.
</Step>

{/* LOCKED_PATTERN_b0cbf67cc15e78743c0724f27d5e90ef */}
  Gib deinen Dub-API-Schlüssel in das Konfigurationsfeld ein.
  <Frame>
    <img src="/images/integrations/dub/api-key.png" alt="API-Schlüssel hinzufügen" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_4ac4616f59ca1d74262754150410c05f */}
  Passe den Transformation-Code an, um Zahlungsdaten für Dubs Track Sale API zu formatieren.
</Step>

{/* LOCKED_PATTERN_f6e4d766e7306e52042eed3b757944bb */}
  Teste mit Beispiel-Payloads und klicke auf **Create**, um die Integration zu aktivieren.
</Step>
</Steps>

## Transformationscode-Beispiele

### Grundlegende Verkaufsverfolgung

Verfolgen Sie Verkäufe, wenn Zahlungen erfolgreich sind:

```javascript basic_sale.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    // Only send to Dub if click ID exists in metadata
    if (payment.metadata && payment.metadata.dub_click_id) {
      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount, // Ensure the amount is in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          customer_email: payment.customer.email,
          customer_name: payment.customer.name,
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

### Abonnementverkäufe verfolgen

Verfolgen Sie sowohl anfängliche Abonnements als auch wiederkehrende Zahlungen:

```javascript subscription_sale.js icon="js" expandable
function handler(webhook) {
  const data = webhook.payload.data;

  // Track initial subscription activation
  if (webhook.eventType === "subscription.active") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount, // Amount in cents
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: data.subscription_id,
        eventName: "Subscription Started",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          billing_interval: data.payment_frequency_interval,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  // Track recurring subscription payments
  if (webhook.eventType === "subscription.renewed") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount,
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: `${data.subscription_id}_${Date.now()}`,
        eventName: "Subscription Renewed",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  return webhook;
}
```

### Verkäufe ohne Steuerverrechnung verfolgen

Senden Sie nur den Betrag vor Steuern an Dub für eine genaue Umsatzverfolgung:

```javascript sale_without_tax.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Calculate pre-tax amount (total minus tax)
      const preTaxAmount = payment.total_amount - (payment.tax || 0);

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: preTaxAmount, // Pre-tax amount in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          total_amount: payment.total_amount,
          tax_amount: payment.tax || 0,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

### Verkäufe mit benutzerdefinierten Ereignisnamen verfolgen

Verwenden Sie benutzerdefinierte Ereignisnamen, um verschiedene Arten von Verkäufen zu kategorisieren:

```javascript custom_events.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Determine event name based on payment type
      let eventName = "Purchase";
      if (payment.subscription_id) {
        eventName = "Subscription Purchase";
      } else if (payment.metadata && payment.metadata.is_upgrade) {
        eventName = "Plan Upgrade";
      }

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount,
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        eventName: eventName,
        metadata: {
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

## Alternative: Client-seitige Implementierung

Wenn Sie Verkäufe von Ihrem Server aus verfolgen möchten, anstatt Webhooks zu verwenden, können Sie Dubs Track API direkt nach einer erfolgreichen Zahlung aufrufen:

<CodeGroup>

```typescript Next.js (Server Action)
'use server';

import { Dub } from 'dub';

const dub = new Dub();

export async function trackSale(
  paymentId: string,
  clickId: string,
  customerId: string,
  amount: number,
  currency: string
) {
  await dub.track.sale({
    clickId: clickId,
    externalId: customerId,
    amount: amount,
    currency: currency,
    paymentProcessor: 'dodo',
    invoiceId: paymentId,
  });
}
```

```javascript Node.js
const { Dub } = require('dub');

const dub = new Dub();

async function trackSale(payment) {
  if (payment.metadata?.dub_click_id) {
    await dub.track.sale({
      clickId: payment.metadata.dub_click_id,
      externalId: payment.metadata.dub_external_id,
      amount: payment.total_amount,
      currency: payment.currency,
      paymentProcessor: 'dodo',
      invoiceId: payment.payment_id,
    });
  }
}
```

</CodeGroup>

## Best Practices

<Tip>
**Erfasse die Klick-ID frühzeitig**: Speichere die Dub-Klick-ID so früh wie möglich in deinem Checkout-Prozess, um eine genaue Attribution sicherzustellen, selbst wenn der Nutzer weg navigiert und später zurückkehrt.
</Tip>

- **Schließe die Klick-ID stets in den Metadaten ein**: Ohne die Klick-ID kann Dub den Umsatz deinen Links nicht zuordnen
- **Verwende externe IDs konsequent**: Übermittle dieselbe Kunden-ID, die du in deinem System nutzt, um genaue Analysen auf Kundenebene zu erhalten
- **Behandle organischen Traffic angemessen**: Setze `webhook.cancel = true`, wenn keine Klick-ID vorhanden ist, um unnötige API-Aufrufe zu vermeiden
- **Teste mit Beispielzahlungen**: Verifiziere, dass die Integration funktioniert, bevor du live gehst
- **Überwache dein Dub-Dashboard**: Prüfe, dass Verkäufe korrekt mit der richtigen Attribution angezeigt werden

## Wichtige Hinweise

<Note>
- **Betragsformat**: Dub erwartet Beträge in Cent (z. B. $10,00 = 1000)
- **Währung**: Verwende ISO-4217-Währungscodes (USD, EUR, GBP usw.)
- **Kostenlose Testphasen**: $0-Zahlungen werden nicht als Verkäufe erfasst
- **Rückerstattungen**: Ziehe in Erwägung, Rückerstattungen separat zu verfolgen, falls du für eine genaue Umsatzberichterstattung darauf angewiesen bist
</Note>

## Fehlersuche

<AccordionGroup>
{/* LOCKED_PATTERN_366b5018c3ba3b70a06fdb2ce6f29df0 */}
- Überprüfe, ob dein Dub-API-Schlüssel korrekt ist und den Umfang `conversions.write` besitzt
- Prüfe, ob der `dub_click_id` erfasst und in den Zahlungs-Metadaten gespeichert wird
- Stelle sicher, dass die Webhook-Transformation die Nutzlast korrekt formatiert
- Verifiziere, dass der Webhook bei `payment.succeeded`-Ereignissen ausgelöst wird
- Bestätige, dass das Conversion-Tracking für deine Dub-Links aktiviert ist
</Accordion>

{/* LOCKED_PATTERN_a32709eeb5a64477cb4e009bd30bfdd2 */}
- Bestätige, dass Nutzer deine Dub-Shortlinks vor dem Checkout anklicken
- Prüfe, ob das `dub_id`-Cookie korrekt auf deiner Domain gesetzt wird
- Vergewissere dich, dass Klick-IDs beim Erstellen des Checkouts und beim Zahlungseingang übereinstimmen
- Stelle sicher, dass du die Klick-ID erfasst, bevor du die Checkout-Session erstellst
</Accordion>

{/* LOCKED_PATTERN_14f1ac2eb6bb156b1fd74792fba48a19 */}
- Validieren die JSON-Struktur, sodass sie dem Format der Dub Track Sale API entspricht
- Prüfe, dass alle erforderlichen Felder (`clickId`, `externalId`, `amount`) vorhanden sind
- Stelle sicher, dass der Betrag in Cent angegeben ist (Ganzzahl, keine Dezimalzahl)
- Verifiziere, dass die API-Endpunkt-URL korrekt ist: `https://api.dub.co/track/sale`
- Teste die Transformation mit Beispiel-Webhooks
</Accordion>

{/* LOCKED_PATTERN_5f0ac54eda91f1b5f2683659cb5c19a5 */}
- Stelle sicher, dass du nur `payment.succeeded`-Ereignisse und keine `payment.processing` verfolgst
- Verwende einzigartige `invoiceId`-Werte für jeden Verkauf
- Hänge bei Abonnements Zeitstempel oder Abrechnungszeiträume an, um Duplikate bei Verlängerungen zu vermeiden
</Accordion>
</AccordionGroup>

## Zusätzliche Ressourcen

{/* LOCKED_PATTERN_bd3b9ce11ef978f59c6eb5461169b62 */}
{/* LOCKED_PATTERN_b26c5b8ead1d8d61de894caed271203c */}
  Erfahre mehr über Dubs Conversion-Tracking- und Analysefunktionen.
</Card>

<Card title="Dub Track Sale API" icon="code" href="https://dub.co/docs/api-reference/track/track-sale">
  Sieh dir die vollständige API-Referenz für Dubs Track Sale-Endpunkt an.
</Card>

<Card title="Dub Dashboard" icon="chart-line" href="https://app.dub.co">
  Greife auf dein Dub-Dashboard zu, um Conversion-Analysen und Attributionsdaten einzusehen.
</Card>

{/* LOCKED_PATTERN_08edaa2b4bf5c1db98d7c617dc2cf851 */}
  Erfahre mehr über alle verfügbaren Dodo Payments Webhook-Ereignisse.
</Card>
</CardGroup>

<Info>
Brauchst du Hilfe? Kontaktiere den Dodo Payments Support unter [support@dodopayments.com](mailto:support@dodopayments.com), um Unterstützung bei der Integration zu erhalten.
</Info>
