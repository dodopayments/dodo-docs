---
title: SendGrid
description: Senden Sie transaktionale E-Mails basierend auf Dodo
  Payments-Ereignissen mit der Mail Send API von SendGrid.
icon: inbox
---

## Einführung

Versenden Sie automatisch transaktionale E-Mails für Zahlungsbestätigungen, Abonnementaktualisierungen und wichtige Benachrichtigungen mit SendGrid. Auslösen von personalisierten E-Mails basierend auf Zahlungsereignissen mit dynamischen Inhalten und professionellen Vorlagen.

<Info>
Diese Integration erfordert einen SendGrid-API-Schlüssel mit Mail Send-Berechtigungen.
</Info>

## Erste Schritte

<Steps>
{/* LOCKED_PATTERN_c337a250e2e6db475d2eb18e95cb3e6a */}
  Navigiere in deinem Dodo Payments-Dashboard zu <b>Webhooks → + Add Endpoint</b> und erweitere das Dropdown-Menü für Integrationen.
  <Frame>
    <img src="/images/integrations/sendgrid.png" alt="Add Endpoint and integrations dropdown" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_5d0724f16f1521162d4bf16b8d9ce88f */}
  Wähle die <b>SendGrid</b>-Integrationskarte.
</Step>

{/* LOCKED_PATTERN_b0cbf67cc15e78743c0724f27d5e90ef */}
  Gib deinen SendGrid-API-Schlüssel in der Konfiguration ein.
</Step>

{/* LOCKED_PATTERN_4ac4616f59ca1d74262754150410c05f */}
  Bearbeite den Transformation-Code, um E-Mails für SendGrids Mail Send-API zu formatieren.
</Step>

{/* LOCKED_PATTERN_f6e4d766e7306e52042eed3b757944bb */}
  Teste mit Beispiel-Payloads und klicke auf <b>Create</b>, um das Versenden von E-Mails zu aktivieren.
</Step>

{/* LOCKED_PATTERN_8d2cbcb809e6363cb1f41f0f62f98444 */}
  🎉 Zahlungsereignisse lösen nun automatisch transaktionale E-Mails über SendGrid aus.
</Step>
</Steps>

## Transformationscode-Beispiele

### Zahlungsbestätigungs-E-Mail

```javascript payment_confirmation.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const p = webhook.payload.data;
    webhook.url = "https://api.sendgrid.com/v3/mail/send";
    webhook.payload = {
      personalizations: [
        {
          to: [{ email: p.customer.email }],
          dynamic_template_data: {
            customer_name: p.customer.name,
            payment_amount: (p.total_amount / 100).toFixed(2),
            payment_id: p.payment_id,
            payment_date: new Date(webhook.payload.timestamp).toLocaleDateString(),
            currency: p.currency || "USD"
          }
        }
      ],
      from: {
        email: "payments@yourdomain.com",
        name: "Your Company"
      },
      template_id: "d-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    };
  }
  return webhook;
}
```

### Willkommens-E-Mail für Abonnements

```javascript subscription_welcome.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "subscription.active") {
    const s = webhook.payload.data;
    webhook.url = "https://api.sendgrid.com/v3/mail/send";
    webhook.payload = {
      personalizations: [
        {
          to: [{ email: s.customer.email }],
          dynamic_template_data: {
            customer_name: s.customer.name,
            subscription_id: s.subscription_id,
            product_name: s.product_id,
            amount: (s.recurring_pre_tax_amount / 100).toFixed(2),
            frequency: s.payment_frequency_interval,
            next_billing: new Date(s.next_billing_date).toLocaleDateString()
          }
        }
      ],
      from: {
        email: "welcome@yourdomain.com",
        name: "Your Company"
      },
      template_id: "d-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    };
  }
  return webhook;
}
```

### Benachrichtigung über Zahlungsfehler

```javascript payment_failure.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.failed") {
    const p = webhook.payload.data;
    webhook.url = "https://api.sendgrid.com/v3/mail/send";
    webhook.payload = {
      personalizations: [
        {
          to: [{ email: p.customer.email }],
          dynamic_template_data: {
            customer_name: p.customer.name,
            payment_amount: (p.total_amount / 100).toFixed(2),
            error_message: p.error_message || "Payment processing failed",
            payment_id: p.payment_id,
            retry_link: `https://yourdomain.com/retry-payment/${p.payment_id}`
          }
        }
      ],
      from: {
        email: "support@yourdomain.com",
        name: "Your Company Support"
      },
      template_id: "d-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
    };
  }
  return webhook;
}
```

## Tipps

- Verwenden Sie dynamische Vorlagen von SendGrid für personalisierte Inhalte
- Fügen Sie relevante Zahlungsdaten in die Vorlagenvariablen ein
- Stellen Sie korrekte Absenderadressen und Absendernamen ein
- Verwenden Sie Vorlagen-IDs für konsistente E-Mail-Formatierung
- Fügen Sie Abmeldelinks zur Einhaltung der Vorschriften hinzu

## Fehlersuche

<AccordionGroup>
{/* LOCKED_PATTERN_b37e11136a1677d8fc025ee71dd2550d */}
- Überprüfe, ob der API-Schlüssel Mail Send-Berechtigungen hat
- Stelle sicher, dass die Template-IDs gültig und aktiv sind
- Vergewissere dich, dass die Empfänger-E-Mail-Adressen gültig sind
- Prüfe SendGrids Versandlimits und Kontingente
</Accordion>

{/* LOCKED_PATTERN_14f1ac2eb6bb156b1fd74792fba48a19 */}
- Validieren, dass die JSON-Struktur dem SendGrid-API-Format entspricht
- Überprüfen, dass alle erforderlichen Felder vorhanden sind
- Sicherstellen, dass Template-Datenvariablen korrekt formatiert sind
- Verifizieren, dass Absender-E-Mail-Adressen in SendGrid bestätigt sind
</Accordion>
</AccordionGroup>