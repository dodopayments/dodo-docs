---
title: Kunden-Wallets
description: Verwalten Sie vorausbezahlte Geldkonten für Ihre Kunden. Laden Sie
  Wallets, wenden Sie Guthaben auf Abonnementrechnungen an, bearbeiten Sie
  Rückerstattungen als Wallet-Guthaben und verfolgen Sie Transaktionsverläufe.
icon: wallet
tag: NEW
keywords:
  - Dodo Payments
  - Kunden-Wallets
  - Zahlungsplattform
  - SaaS-Abrechnung
---
<CardGroup cols={3}>
<Card title="Get Customer Wallets" icon="code" href="/api-reference/customers/get-customer-wallets">
  Überprüfen Sie die Geldguthaben von Kunden in verschiedenen Währungen.
</Card>

<Card title="Create Ledger Entry" icon="plus" href="/api-reference/customers/post-customer-wallets-ledger-entries">
  Fügen Sie Kunden-Wallets Geld hinzu oder ziehen Sie es ab.
</Card>

<Card title="List Ledger Entries" icon="list" href="/api-reference/customers/get-customer-wallets-ledger-entries">
  Sehen Sie sich den vollständigen Transaktionsverlauf mit Paginierung an.
</Card>
</CardGroup>

## Was sind Kunden-Wallets?

Kunden-Wallets sind Konten für Geldguthaben, die echte Gelder für Ihre Nutzer enthalten. Jeder Kunde erhält automatisch ein solches Wallet, wenn Sie dessen Konto erstellen. Sie können diese Wallets verwenden, um:

- **Speichern Sie vorausbezahlte Gelder** für zukünftige Abonnementzahlungen
- **Behandeln Sie Rückerstattungen** als Wallet-Guthaben statt als Kartenrückerstattung
- **Stellen Sie Werbeguthaben aus**, z. B. Willkommensboni oder Treueprämien
- **Wenden Sie Wallet-Guthaben auf Rechnungen an** und zwar automatisch während der Abrechnung
- **Verfolgen Sie Geldtransaktionen** mithilfe eines detaillierten Hauptbuchverlaufs

<Info>
Jeder Kunde erhält automatisch eine Wallet, wenn Sie sein Konto erstellen. Wallets unterstützen die Währungen USD, EUR und GBP mit jeweils separaten Geldguthaben.
</Info>

<Warning>
**Kunden-Wallets ≠ Credit-Based Billing**

Kunden-Wallets enthalten **echte Geldguthaben** (USD, EUR, GBP), die auf Rechnungen und Abonnementzahlungen angewendet werden können.

Wenn Sie virtuelle Nutzungseinheiten (API-Aufrufe, Tokens, Rechenstunden) verfolgen möchten, sehen Sie stattdessen [Credit-Based Billing](/features/credit-based-billing).
</Warning>

<Frame>
  <img src="/images/customer/customer-wallet.png" alt="Kunden-Wallets" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

## Funktionsweise

Kunden-Wallets enthalten Gelder, die Kunden auf ihre Einkäufe anwenden können. Wenn ein Kunde eine Rechnung oder eine wiederkehrende Abonnementgebühr hat, wird zuerst sein Wallet-Guthaben überprüft. Verfügbare Beträge werden automatisch auf die Rechnung angewendet, bevor seine primäre Zahlungsmethode belastet wird.

### Automatische Einrichtung

Wenn Sie einen neuen Kunden erstellen, legt Dodo Payments automatisch ein Wallet mit einem Guthaben von null an. Es ist sofort bereit, über unsere API Gelder zu empfangen.

### Unterstützung mehrerer Währungen

Jedes Wallet kann Guthaben in verschiedenen Währungen halten:

<ResponseField name="USD Balance" type="integer">
Guthaben in US-Dollar (in Cent gespeichert)
</ResponseField>

<ResponseField name="EUR Balance" type="integer">
Guthaben in Euro (in Cent gespeichert)
</ResponseField>

<ResponseField name="GBP Balance" type="integer">
Guthaben in britischen Pfund (in Pence gespeichert)
</ResponseField>

<Info>
Derzeit sind nur Guthaben in **USD**, **EUR** und **GBP** verfügbar. Die native INR-Wallet wurde eingestellt – siehe [Auszahlungsstruktur](/features/payouts/payout-structure). INR wird weiterhin als Transaktionswährung für UPI-, RuPay- und indische Kartenabonnements unterstützt.
</Info>

## Mit Wallets arbeiten

### Kundenguthaben prüfen

Sehen Sie, über wie viel Guthaben ein Kunde in allen Währungen verfügt. Dies ist nützlich, um das verfügbare Guthaben vor der Verarbeitung eines Kaufs zu überprüfen oder das Guthaben in der Benutzeroberfläche Ihrer Anwendung anzuzeigen.

<Card title="Get Customer Wallet Balances" icon="wallet" href="/api-reference/customers/get-customer-wallets">
  Prüfen Sie die Geldguthaben einer Kunden-Wallet in allen unterstützten Währungen.
</Card>

### Guthaben hinzufügen oder abziehen

Laden Sie Kunden-Wallets auf (z. B. mit Willkommensboni oder Erstattungsguthaben) oder ziehen Sie Guthaben ab (z. B. für Abonnementgebühren). Sie können für jede Transaktion einen Grund angeben, um eine klare Prüfspur zu gewährleisten.

<Note>
Das Feld `entry_type` verwendet `'credit'`, um der Wallet Guthaben hinzuzufügen, und `'debit'`, um Guthaben aus der Wallet abzuziehen.
</Note>

<Card title="Create Customer Wallet Ledger Entry" icon="plus" href="/api-reference/customers/post-customer-wallets-ledger-entries">
  Guthaben zu einer Kunden-Wallet hinzufügen oder daraus abziehen.
</Card>

### Transaktionsverlauf anzeigen

Sehen Sie jede Gutschrift und Abbuchung eines Kunden. Dieses detaillierte Hauptbuch unterstützt Sie beim Abgleichen von Konten und schafft Transparenz für Ihre Kunden.

<Card title="List Customer Wallet Ledger Entries" icon="list" href="/api-reference/customers/get-customer-wallets-ledger-entries">
  Jede Geldtransaktion eines Kunden anzeigen.
</Card>

## Beispiele aus der Praxis

### Erstattung auf die Wallet

Wenn ein Kunde eine Erstattung anfordert, können Sie den Betrag seinem Wallet-Guthaben hinzufügen, anstatt eine herkömmliche Kartenerstattung durchzuführen. So verbleibt das Guthaben für zukünftige Käufe in Ihrem Ökosystem.

```javascript
async function refundToWallet(customerId, refundAmount, originalPaymentId) {
  await client.customers.wallets.ledgerEntries.create(customerId, {
    amount: refundAmount, // Amount in cents
    currency: 'USD',
    entry_type: 'credit',
    reason: `Refund for payment ${originalPaymentId}`,
    idempotency_key: `refund_${originalPaymentId}`
  });
}
```

### Willkommensbonus / Aktionsguthaben

Geben Sie neuen Kunden einen Geldbonus als Willkommensgeschenk, um sie zu ihrem ersten Kauf zu motivieren.

```javascript
async function addWelcomeBonus(customerId) {
  await client.customers.wallets.ledgerEntries.create(customerId, {
    amount: 1000, // $10.00 promotional balance
    currency: 'USD',
    entry_type: 'credit',
    reason: 'Welcome bonus - $10 promotional balance',
    idempotency_key: `welcome_${customerId}`
  });
}
```

### Abonnementzahlung aus der Wallet

Ziehen Sie Guthaben aus einer Wallet ab, um eine Abonnementgebühr oder einen manuellen Kauf zu begleichen.

```javascript
async function deductForPurchase(customerId, purchaseAmount, purchaseId) {
  try {
    await client.customers.wallets.ledgerEntries.create(customerId, {
      amount: purchaseAmount,
      currency: 'USD',
      entry_type: 'debit',
      reason: `Subscription charge - monthly billing`,
      idempotency_key: `charge_${purchaseId}`
    });
  } catch (error) {
    if (error.status === 400) {
      console.log('Insufficient wallet balance');
    }
  }
}
```

### Prepaid-Abrechnungssystem

Ermöglichen Sie Kunden, ihre Konten im Voraus aufzuladen und dieses Guthaben im Laufe der Zeit aufzubrauchen.

<Steps>
<Step title="Add Initial Funds">
Fügen Sie der Wallet des Kunden Guthaben hinzu, wenn er eine Einzahlung vornimmt.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 5000, // $50.00 deposit
  currency: 'USD',
  entry_type: 'credit',
  reason: 'Account funding - prepaid deposit',
  idempotency_key: `deposit_${paymentId}`
});
```

</Step>

<Step title="Apply Balance to Purchases">
Ziehen Sie Guthaben ab, wenn der Kunde Ihre Dienste nutzt oder Abonnements verlängert.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 1500, // $15.00 charge
  currency: 'USD', 
  entry_type: 'debit',
  reason: 'Service purchase - monthly subscription',
  idempotency_key: `purchase_${purchaseId}`
});
```

</Step>

<Step title="Monitor Balances">
Prüfen Sie, ob Kunden nur noch über wenig Guthaben verfügen, um sie zu einer Aufladung aufzufordern.

```javascript
const wallets = await client.customers.wallets.list(customerId);
const usdWallet = wallets.items.find(w => w.currency === 'USD');
const balance = usdWallet.balance;

if (balance < 1000) { // Less than $10.00
  // Send low balance notification
  await sendLowBalanceNotification(customerId, balance);
}
```

</Step>
</Steps>

### Unterstützung mehrerer Währungen

Verwalten Sie separate Geldguthaben für Kunden in verschiedenen Regionen.

<AccordionGroup>
<Accordion title="US Customers">
Verwalten Sie USD-Guthaben für Kunden in den USA.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 20000, // $200.00 in cents
  currency: 'USD',
  entry_type: 'credit',
  reason: 'USD account funding',
  idempotency_key: `usd_deposit_${paymentId}`
});
```

</Accordion>

<Accordion title="European Customers">
Verwalten Sie EUR-Guthaben für Kunden in Europa.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 18000, // €180.00 in cents
  currency: 'EUR',
  entry_type: 'credit',
  reason: 'EUR account funding',
  idempotency_key: `eur_deposit_${paymentId}`
});
```

</Accordion>

<Accordion title="UK Customers">
Verwalten Sie GBP-Guthaben für Kunden im Vereinigten Königreich.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 15000, // £150.00 in pence
  currency: 'GBP',
  entry_type: 'credit',
  reason: 'GBP account funding',
  idempotency_key: `gbp_deposit_${paymentId}`
});
```

</Accordion>
</AccordionGroup>

## Best Practices

### Doppelte Transaktionen verhindern

Verwenden Sie Idempotenzschlüssel, um sicherzustellen, dass Sie nicht versehentlich zweimal Guthaben für dasselbe Ereignis hinzufügen oder abziehen.

```javascript
async function addFundsSafely(customerId, amount, reason) {
  const idempotencyKey = `${reason}_${customerId}_${Date.now()}`;
  
  try {
    const result = await client.customers.wallets.ledgerEntries.create(customerId, {
      amount: amount,
      currency: 'USD',
      entry_type: 'credit',
      reason: reason,
      idempotency_key: idempotencyKey
    });
    
    // Replaying the same idempotency_key returns the original entry rather than
    // creating a duplicate, so no special duplicate handling is required.
    return { success: true, wallet: result };
  } catch (error) {
    if (error.status === 400) {
      // e.g. insufficient balance on a debit
      return { success: false, reason: 'insufficient_balance' };
    }

    throw error;
  }
}
```

### Guthaben vor der Belastung prüfen

Überprüfen Sie, ob Kunden über ausreichendes Guthaben verfügen, bevor Sie versuchen, große Transaktionen aus der Wallet zu verarbeiten.

```javascript
async function checkBalanceBeforeOperation(customerId, requiredAmount) {
  const wallets = await client.customers.wallets.list(customerId);
  const usdWallet = wallets.items.find(w => w.currency === 'USD');
  
  if (!usdWallet || usdWallet.balance < requiredAmount) {
    throw new Error('Insufficient funds for this operation');
  }
  
  return usdWallet.balance;
}
```

## Was als Nächstes kommt

<Warning>
Diese Funktionen sind für zukünftige Releases geplant:
</Warning>

- **Ablauf des Guthabens**: Legen Sie fest, dass Guthaben nach einem bestimmten Zeitraum verfällt
- **Bessere Analysen**: Detaillierte Ausgabenberichte und Guthabenentwicklungen
- **Mehr Webhooks**: Echtzeitbenachrichtigungen über Guthabenänderungen und Warnungen bei niedrigem Guthaben

<Tip>
Beginnen Sie mit grundlegenden Vorgängen zum Aufladen und Abbuchen und integrieren Sie anschließend komplexere automatisierte Abrechnungsabläufe, wenn Ihr Unternehmen wächst.
</Tip>
