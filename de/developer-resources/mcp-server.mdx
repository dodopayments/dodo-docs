---
title: MCP Server
description: Aktivieren Sie KI-Assistenten, um über das Model Context Protocol
  (MCP) nahtlos mit der Dodo Payments API zu interagieren, um
  Zahlungsoperationen, Abonnementverwaltung und Kundeninteraktionen zu
  ermöglichen.
icon: /images/logos/mcp.svg
---

## Übersicht

Das Model Context Protocol (MCP) ist ein offener Standard, der es KI-Anwendungen ermöglicht, sicher mit externen Datenquellen und APIs zu verbinden. Der Dodo Payments MCP Server bietet KI-Assistenten wie Claude, Cursor und anderen MCP-kompatiblen Clients strukturierten Zugriff auf Ihre Zahlungsinfrastruktur.

Der Dodo Payments MCP-Server ist mit [Stainless](https://www.stainless.com/) erstellt und verwendet die **Code Mode**-Architektur, die es KI-Agenten ermöglicht, TypeScript-Code gegen das Dodo Payments SDK in einer isolierten Sandbox-Umgebung zu schreiben und auszuführen.

### Hauptfunktionen

- **Zahlungsoperationen**: Zahlungen und Rückerstattungen erstellen, abrufen und verwalten
- **Abonnementverwaltung**: Wiederkehrende Abrechnungen, Upgrades und Stornierungen verwalten
- **Kundenverwaltung**: Kundendaten und Portalzugang verwalten
- **Produktkatalog**: Produkte, Preise und Rabatte erstellen und aktualisieren
- **Lizenzverwaltung**: Softwarelizenzen aktivieren, validieren und verwalten
- **Nutzungsbasierte Abrechnung**: Abrechnungen für gemessene Nutzung verfolgen und erstellen

## Wie Code Mode funktioniert

Der Dodo Payments MCP-Server verwendet Code Mode und bietet Ihrem KI-Agenten zwei leistungsstarke Werkzeuge:

1. **Docs Search Tool**: Dokumentation über die Dodo Payments API und SDK abfragen, um verfügbare Operationen und Parameter zu verstehen
2. **Code Execution Tool**: TypeScript-Code gegen das SDK schreiben, der in einer sicheren Sandbox-Umgebung ohne Web- oder Dateisystemzugriff ausgeführt wird

Diese Architektur ermöglicht es KI-Agenten, komplexe, mehrstufige Zahlungsoperationen deterministisch und wiederholbar durchzuführen.

<Tip>
Mit dem Code-Modus können Agenten mehrere API-Aufrufe verketten, bedingte Logik auswerten und Berechnungen durchführen – alles in einem einzigen Werkzeugaufruf.
</Tip>

## Dodo Knowledge MCP

Neben dem Dodo Payments MCP Server (zum Ausführen von API-Operationen) bieten wir **Dodo Knowledge MCP** an - einen semantischen Suchserver, der KI-Assistenten sofortigen Zugriff auf die Dokumentation und Wissensdatenbank von Dodo Payments ermöglicht.

<Info>
**Built with [ContextMCP.ai](https://contextmcp.ai)**: Dodo Knowledge MCP basiert auf ContextMCP und ermöglicht schnelle semantische Suche in unserer Dokumentation mithilfe von Vektor-Embeddings.
</Info>

### Was ist Dodo Knowledge MCP?

Dodo Knowledge MCP ist ein Remote-MCP-Server, der Folgendes bietet:

- **Semantische Dokumentationssuche**: Finden Sie relevante Dokumentation mit natürlichen Sprachabfragen
- **Kontextuelle Antworten**: KI-Assistenten erhalten genaue, aktuelle Informationen über Dodo Payments
- **Null Einrichtung**: Keine API-Schlüssel oder lokale Installation erforderlich - einfach verbinden und Abfragen starten

### Schnelle Einrichtung

Verbinden Sie sich mit Dodo Knowledge MCP in Ihrem KI-Client:

<Tabs>
<Tab title="Cursor">
Fügen Sie zu `~/.cursor/mcp.json` hinzu:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Desktop">
Fügen Sie Ihrer Claude Desktop-Konfiguration hinzu:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

Configuration file locations:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Tab>

<Tab title="Windsurf">
Fügen Sie zu `~/.codeium/windsurf/mcp_config.json` hinzu:

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Code">
Führen Sie im Terminal aus:

```bash
claude mcp add dodo-knowledge -- npx -y mcp-remote@latest https://knowledge.dodopayments.com/mcp
```

</Tab>
</Tabs>

<Note>
Erfordert Node.js 18 oder höher. Das `mcp-remote`-Paket übernimmt die Verbindung zum entfernten MCP-Server.
</Note>

### Verwendung beider MCP-Server zusammen

Für das beste KI-unterstützte Entwicklungserlebnis empfehlen wir die Verwendung beider MCP-Server:

| Server | Zweck | Anwendungsfall |
|--------|---------|----------|
| **Dodo Knowledge MCP** | Dokumentationssuche | "Wie gehe ich mit Webhooks um?", "Welche Zahlungsmethoden werden unterstützt?" |
| **Dodo Payments MCP** | API-Operationen | Zahlungen erstellen, Abonnements verwalten, Rückerstattungen bearbeiten |

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    },
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

<Tip>
Mit beiden konfigurierten Servern kann Ihr KI-Assistent zuerst die Dokumentation durchsuchen, um zu verstehen, wie ein Feature implementiert wird, und dann alle notwendigen API-Aufrufe in einem einzigen Gespräch ausführen.
</Tip>

### Fehlerbehebung Knowledge MCP

Wenn Sie auf Verbindungsprobleme stoßen:

1. **Cache für MCP-Authentifizierung löschen**: `rm -rf ~/.mcp-auth`
2. **Starten Sie Ihre Client-Anwendung neu**
3. **Prüfen Sie die Client-Protokolle** auf Fehlermeldungen
4. **Überprüfen Sie die Node.js-Version**: Erfordert Node.js 18+

<Card title="Knowledge MCP Server" icon="book-open" href="https://knowledge.dodopayments.com">
  Greifen Sie auf die Dodo Knowledge MCP-Konfigurationsseite zu
</Card>

## Installation

Wählen Sie die Installationsmethode, die am besten zu Ihrem Workflow passt.

### Remote MCP-Server (Empfohlen)

Greifen Sie auf den gehosteten MCP-Server zu, ohne lokale Einrichtung oder Installation. Dies ist der schnellste Weg, um zu beginnen.

<Frame>
  <iframe 
      className="w-full aspect-video rounded-md" 
      src="https://www.youtube.com/embed/TxGgYxNmkT4"
      title="Using Dodo Payment's Remote MCP Server"
      frameBorder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowFullScreen
  ></iframe>
</Frame>

<Steps>
{/* LOCKED_PATTERN_bd1568a9aaf0e86b69853e215d3d5723 */}
    Navigieren Sie in Ihrem Browser zu [https://mcp.dodopayments.com](https://mcp.dodopayments.com).
</Step>

{/* LOCKED_PATTERN_b8d4e4a1a3c0fcf28fd2afe7ce9e8316 */}
    Kopieren Sie die bereitgestellte JSON-Konfiguration für Ihren spezifischen Client. Fügen Sie sie für Cursor oder Claude Desktop zu Ihren MCP-Einstellungen hinzu:
    

    ```json
    {
      "mcpServers": {
        "dodopayments_api": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8be57788a275cad53ee59f91af51bfd5 */}
    Der OAuth-Ablauf fordert Sie auf:
    - Geben Sie Ihren Dodo Payments API-Schlüssel ein
    - Wählen Sie Ihre Umgebung (Test oder Live)
    - Wählen Sie Ihren MCP-Clienttyp
    
    Halten Sie Ihren API-Schlüssel sicher. Verwenden Sie während der Entwicklung Testmodus-Schlüssel.
    </Warning>
</Step>

{/* LOCKED_PATTERN_606fac2bd646ef76f5d66097882887ed */}
    Klicken Sie auf **Login and Approve**, um die Verbindung zu autorisieren.
    
    Sobald die Verbindung steht, kann Ihr KI-Assistent im Namen mit der Dodo Payments API interagieren.
    </Check>
</Step>
</Steps>

### NPM-Paket

Installieren und führen Sie den MCP-Server lokal mit NPM aus.

<Tabs>
{/* LOCKED_PATTERN_c8776436a7ae17e188a287e7c6852f16 */}

```bash
# Set your environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."
export DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key"
export DODO_PAYMENTS_ENVIRONMENT="live_mode"

# Run the latest version
npx -y dodopayments-mcp@latest
```

</Tab>

{/* LOCKED_PATTERN_8d04e6cca1a5903f80148c5b923e5c3f */}

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

</Tab>
</Tabs>

<Tip>
Verwenden Sie `@latest`, um stets die aktuellste Version zu beziehen, oder fixieren Sie für Stabilität eine bestimmte Version wie `@1.0.0`.
</Tip>

### Docker

Führen Sie den MCP-Server in einer containerisierten Umgebung für konsistente Bereitstellung aus.

<CodeGroup>

```bash Docker
# Pull the latest image
docker pull ghcr.io/dodopayments/mcp:latest

# Run the container
docker run -e DODO_PAYMENTS_API_KEY="dodo_test_..." \
  -e DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -e DODO_PAYMENTS_ENVIRONMENT="live_mode" \
  ghcr.io/dodopayments/mcp:latest
```

```yaml Docker Compose
services:
  dodopayments-mcp:
    image: ghcr.io/dodopayments/mcp:latest
    environment:
      - DODO_PAYMENTS_API_KEY=dodo_test_...
      - DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_key
      - DODO_PAYMENTS_ENVIRONMENT=live_mode
    restart: unless-stopped
```

</CodeGroup>

<Note>
Docker-Images sind auf [GitHub Container Registry](https://github.com/orgs/dodopayments/packages/container/package/mcp) verfügbar.
</Note>

## Client-Konfiguration

Konfigurieren Sie den Dodo Payments MCP-Server in Ihrem bevorzugten KI-Client.

<Tabs>
<Tab title="Cursor">
Richten Sie den Dodo Payments MCP-Server in Cursor ein, um konversationellen Zugriff auf Ihre Zahlungsdaten zu ermöglichen.

**Ein-Klick-Installation**

Verwenden Sie die Schaltfläche unten, um den MCP-Server direkt in Cursor zu installieren:

[

![Zu Cursor hinzufügen](https://cursor.com/deeplink/mcp-install-dark.svg)

](https://cursor.com/en-US/install-mcp?name=dodopayments-mcp&config=)

Nachdem Sie geklickt haben, legen Sie Ihre Umgebungsvariablen in Cursors `mcp.json` über **Cursor Settings** > **Tools & MCP** > **New MCP Server** fest.

**Manuelle Konfiguration**

<Steps>
{/* LOCKED_PATTERN_b5120fe2afbf9bb96d1de6f9549e8f41 */}
    Navigieren Sie zu **Cursor Settings** > **Features** > **Model Context Protocol** oder drücken Sie `Cmd/Ctrl + Shift + P` und suchen Sie nach „MCP Settings“. 
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    Klicken Sie auf **Edit Config**, um die Datei `mcp_config.json` zu öffnen.
</Step>

{/* LOCKED_PATTERN_a65e4e236194f7c177caa156c6d0b797 */}
    Wählen Sie eine der folgenden Konfigurationen:
    
    **Remote Server (Recommended)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **Lokales NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8368e3e8540b47a92000151cb7e30a33 */}
    Speichern Sie die Konfigurationsdatei und starten Sie Cursor neu.
    
    Bestätigen Sie die Verbindung, indem Sie den KI-Assistenten nach Ihren Dodo Payments-Daten fragen.
    </Check>
</Step>
</Steps>
</Tab>

<Tab title="Claude Desktop">
Richten Sie den Dodo Payments MCP-Server in Claude Desktop ein, um konversationellen Zugriff auf Ihre Zahlungsdaten zu ermöglichen.
<Steps>
{/* LOCKED_PATTERN_ecfdd2e916ed4a93d6b3950e9027df3f */}
    Finden Sie die Claude Desktop-Konfigurationsdatei:
    
    - **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
    - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
    - **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    Öffnen Sie die Datei in einem Texteditor und fügen Sie die Dodo Payments-Konfiguration hinzu:
    
    **Remote Server (Recommended)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **Lokales NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_83563b32c3caf8bd7aa68192b6ebcc7d */}
    Schließen Sie Claude Desktop und öffnen Sie es erneut, damit die Änderungen wirksam werden.
    
    Suchen Sie in Claude Desktop nach dem 🔌-Symbol, um zu bestätigen, dass der MCP-Server verbunden ist.
    </Tip>
</Step>
</Steps>
</Tab>

<Tab title="Claude Code">
Richten Sie den Dodo Payments MCP-Server in Claude Code CLI ein.

Führen Sie den folgenden Befehl in Ihrem Terminal aus:

```bash
claude mcp add --transport stdio dodopayments_api \
  --env DODO_PAYMENTS_API_KEY="dodo_test_..." \
  DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -- npx -y dodopayments-mcp
```

Die Konfiguration wird in der Datei `.claude.json` in Ihrem Home-Verzeichnis gespeichert.

<Tip>
Claude Code übernimmt automatisch Umgebungsvariablen aus Ihrer Shell, sodass Sie diese auch vor dem Ausführen des Befehls exportieren können.
</Tip>
</Tab>

<Tab title="VS Code">
Richten Sie den Dodo Payments MCP-Server in VS Code ein.

**Ein-Klick-Installation**

[VS Code öffnen](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dodopayments-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22dodopayments-mcp%22%5D%2C%22env%22%3A%7B%22DODO_PAYMENTS_API_KEY%22%3A%22Set%20your%20DODO_PAYMENTS_API_KEY%20here.%22%2C%22DODO_PAYMENTS_WEBHOOK_KEY%22%3A%22Set%20your%20DODO_PAYMENTS_WEBHOOK_KEY%20here.%22%7D%7D)

Nachdem Sie geklickt haben, legen Sie Ihre Umgebungsvariablen in VS Codes `mcp.json` über **Command Palette** > **MCP: Open User Configuration** fest.

**Manuelle Konfiguration**

<Steps>
{/* LOCKED_PATTERN_01c11d820b190ade4d2a29feff5a49da */}
    Öffnen Sie die Command Palette (`Cmd/Ctrl + Shift + P`) und suchen Sie nach „MCP: Open User Configuration“. 
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Fügen Sie den Dodo Payments MCP-Server hinzu:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_fabb79a0cecb26359867d78b61382dd7 */}
    Laden Sie VS Code neu (`Cmd/Ctrl + Shift + P` > „Reload Window“), um den MCP-Server zu aktivieren.
</Step>
</Steps>
</Tab>

<Tab title="Cline (VS Code)">
Richten Sie den Dodo Payments MCP-Server in Cline (VS Code) ein, um konversationellen Zugriff auf Ihre Zahlungsdaten zu ermöglichen.
<Steps>
{/* LOCKED_PATTERN_43db9eecf5eea8e9c4ad60b6c3ff1c0b */}
    Lokalisieren Sie die Cline-Konfigurationsdatei:
    
    - **macOS**: `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
    - **Windows**: `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    - **Linux**: `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
</Step>

{/* LOCKED_PATTERN_0849b9a67e72024819745a9889898853 */}
    Fügen Sie die Dodo Payments MCP-Serverkonfiguration hinzu:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_43be57c73e24741f16da5f79d5398749 */}
    Laden Sie das VS Code-Fenster neu (`Cmd/Ctrl + Shift + P` > „Reload Window“), um die MCP-Serververbindung zu aktivieren.
</Step>
</Steps>
</Tab>

<Tab title="Zed">
Richten Sie den Dodo Payments MCP-Server in Zed ein, um konversationellen Zugriff auf Ihre Zahlungsdaten zu ermöglichen.
<Steps>
{/* LOCKED_PATTERN_a48799001554eb7ef4b2ac1e56fab915 */}
    Navigieren Sie zu **Settings** > **Assistant** > **Model Context Protocol**.
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Fügen Sie die folgende Konfiguration hinzu:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>
{/* LOCKED_PATTERN_b1c8821108a3bf0ac4028a0196176d66 */}
    Speichern Sie die Konfiguration. Der MCP-Server sollte nun im KI-Assistenten von Zed verfügbar sein.
</Step>
</Steps>
</Tab>

<Step title="Speichern und überprüfen">
    Speichern Sie die Konfiguration. Der MCP-Server sollte jetzt im Zeds KI-Assistenten verfügbar sein.
</Step>
</Steps>
</Tab>

{/* LOCKED_PATTERN_71257692833d303ebb288457e9ce7134 */}
Richten Sie den Dodo Payments MCP-Server in anderen MCP-kompatiblen Clients ein.

**Remote-Server (Empfohlen)**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

**Lokales NPX**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
      }
    }
  }
}
```

<Info>
Besuchen Sie [modelcontextprotocol.io/clients](https://modelcontextprotocol.io/clients), um eine vollständige Liste MCP-kompatibler Clients und deren Konfigurationsmethoden zu sehen.
</Info>
</Tab>
</Tabs>

## Umgebungsvariablen

Konfigurieren Sie das Verhalten des MCP-Servers mithilfe von Umgebungsvariablen.

| Variable | Beschreibung | Erforderlich |
|----------|--------------|--------------|
| `DODO_PAYMENTS_API_KEY` | Ihr Dodo Payments API-Schlüssel | Ja |
| `DODO_PAYMENTS_WEBHOOK_KEY` | Ihr Webhook-Signierschlüssel | Nein |
| `DODO_PAYMENTS_ENVIRONMENT` | Setzen Sie auf `live_mode` für Produktionsbetrieb | Nein |

## Remote Ausführen

Setzen Sie den MCP-Server als Remote-HTTP-Server für webbasierte Clients oder agentische Workflows ein.

```bash
# Start as remote server
npx -y dodopayments-mcp --transport=http

# Specify a custom port
npx -y dodopayments-mcp --transport=http --port=3000
```

### Remote-Server-Konfiguration

Sobald bereitgestellt, können sich Clients über die Server-URL verbinden:

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "url": "http://localhost:3000",
      "headers": {
        "Authorization": "Bearer your_api_key"
      }
    }
  }
}
```

### Autorisierungsheader

Der Remote-Server akzeptiert die Authentifizierung über die folgenden Header:

| Header | Beschreibung |
|--------|--------------|
| `Authorization` | Bearer-Token-Authentifizierung |
| `x-dodo-payments-api-key` | Direkter API-Schlüssel-Header |

## Client-Fähigkeiten

Verschiedene MCP-Clients unterstützen unterschiedliche Ebenen der MCP-Spezifikation. Der Server kann die Tool-Schemas automatisch anpassen, um diese Einschränkungen zu umgehen.

```bash
# Auto-detect client (default)
npx dodopayments-mcp --client=infer

# Configure for specific clients
npx dodopayments-mcp --client=cursor
npx dodopayments-mcp --client=claude
npx dodopayments-mcp --client=claude-code
npx dodopayments-mcp --client=openai-agents
```

### Verfügbare Client-Presets

| Client | Beschreibung |
|--------|--------------|
| `infer` | Client automatisch erkennen (Standard) |
| `cursor` | Cursor-Editor |
| `claude` | Claude AI (Web oder Desktop) |
| `claude-code` | Claude Code CLI |
| `openai-agents` | OpenAI Agents-Plattform |

### Manuelle Fähigkeitskonfiguration

Für Clients, die oben nicht aufgeführt sind, konfigurieren Sie die Fähigkeiten manuell:

```bash
# Disable $ref pointer support and set max tool name length
npx dodopayments-mcp --no-capability=refs --capability=tool-name-length=40
```

Verfügbare Funktionen:
- `top-level-unions` - Unterstützung für Union-Typen auf oberster Ebene
- `valid-json` - Korrekte Analyse von JSON-String-Argumenten
- `refs` - Unterstützung für $ref-Verweise in Schemas
- `unions` - Unterstützung für Union-Typen (anyOf)
- `formats` - Unterstützung für Formatvalidierungen
- `tool-name-length=N` - Maximale Werkzeugnamenlänge

<Tip>
Führen Sie `npx dodopayments-mcp --describe-capabilities` aus, um detaillierte Informationen zu den Funktionen zu erhalten.
</Tip>

## Tool-Filterung

Steuern Sie, welche API-Operationen Ihrem KI-Assistenten zur Verfügung stehen, um die Leistung zu optimieren und die Sicherheit zu gewährleisten.

### Nach Operationstyp filtern

Zugriff auf schreibgeschützte oder schreibbare Operationen einschränken.

```bash
# Only expose read operations (GET requests)
npx dodopayments-mcp --operation=read

# Only expose write operations (POST, PUT, DELETE)
npx dodopayments-mcp --operation=write
```

<Tip>
Verwenden Sie `--operation=read` während der Entwicklung, um versehentliche Änderungen an Ihren Daten zu verhindern.
</Tip>

### Nach Ressource filtern

Tools auf bestimmte API-Ressourcen beschränken.

```bash
# Only expose payment-related tools
npx dodopayments-mcp --resource=payments

# Expose multiple resources
npx dodopayments-mcp --resource=payments --resource=customers

# Use wildcards for resource groups
npx dodopayments-mcp --resource=subscription*
```

### Bestimmte Tools ein- oder ausschließen

Feinabstimmung, welche einzelnen Tools verfügbar sind.

```bash
# Include only specific tools
npx dodopayments-mcp --tool=create_payments --tool=list_payments

# Exclude specific tools
npx dodopayments-mcp --no-tool=delete_products

# Disable the docs search tool
npx dodopayments-mcp --no-tools=docs

# List all available tools
npx dodopayments-mcp --list
```

### Filter kombinieren

Wenden Sie mehrere Filter gleichzeitig für präzise Kontrolle an.

```bash
# Only payment and subscription read operations
npx dodopayments-mcp --operation=read --resource=payments --resource=subscriptions
```

### URL-Abfrageparameterfilterung (Remote-Server)

Bei Verwendung des Remote-Servers Filter über URL-Abfrageparameter anwenden:

```
https://mcp.dodopayments.com/?operation=read
https://mcp.dodopayments.com/?resource=payments
```

## Dynamischer Tools-Modus

Für große APIs kann das gleichzeitige Laden aller Tools den Kontextbereich der KI überfordern. Der dynamische Tools-Modus bietet eine effizientere Alternative.

```bash
npx dodopayments-mcp --tools=dynamic
```

Im dynamischen Modus ersetzen drei Meta-Tools einzelne API-Tools:

1. **`list_api_endpoints`** - Entdeckt verfügbare Methoden mit optionaler Filterung
2. **`get_api_endpoint_schema`** - Ruft detailliertes Schema für eine bestimmte Methode ab
3. **`invoke_api_endpoint`** - Führt jede Methode mit passenden Parametern aus

<Info>
Dynamische Tools werden automatisch für APIs mit mehr als 50 Methoden aktiviert. Überschreiben Sie dies mit `--tools=all`, um alle Tools einzubeziehen.
</Info>

```bash
# Use both dynamic tools and specific resources
npx dodopayments-mcp --tools=dynamic --resource=payments
```

## Erweiterte Nutzung

### Programmatische Integration

Erstellen Sie benutzerdefinierte MCP-Server oder erweitern Sie den vorhandenen programmatisch.

```javascript
import { server, endpoints, init } from "dodopayments-mcp/server";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

// Initialize with all default endpoints
init({ server, endpoints });

// Start the server
const transport = new StdioServerTransport();
await server.connect(transport);
```

### Entwicklung benutzerdefinierter Tools

Erweitern Sie den MCP-Server mit Ihren eigenen Tools und Geschäftslogik.

```javascript
import { server, init } from "dodopayments-mcp/server";
import createPayments from "dodopayments-mcp/tools/payments/create-payments";
import { zodToJsonSchema } from "zod-to-json-schema";
import { z } from "zod";

// Define a custom tool
const customTool = {
  tool: {
    name: 'calculate_payment_total',
    description: 'Calculate payment total with tax and fees',
    inputSchema: zodToJsonSchema(
      z.object({
        amount: z.number().describe('Base amount in cents'),
        tax_rate: z.number().describe('Tax rate as decimal'),
      })
    ),
  },
  handler: async (client, args) => {
    const total = args.amount * (1 + args.tax_rate);
    return {
      content: [{
        type: 'text',
        text: JSON.stringify({ total, currency: 'usd' }),
      }],
    };
  },
};

// Initialize with custom tools
init({ 
  server, 
  endpoints: [createPayments, customTool] 
});
```

## Sicherheitsbestimmungen

Schützen Sie Ihre API-Anmeldeinformationen und gewährleisten Sie sichere Integrationen.

<AccordionGroup>
{/* LOCKED_PATTERN_58b09b66cd00f40de384d770573ae848 */}
**Geben Sie niemals Zugangsdaten in die Versionskontrolle ein**

Speichern Sie API-Schlüssel in Umgebungsvariablen oder sicheren Geheimnisverwaltungssystemen.

```bash
# Use environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."

# Use a .env file (add to .gitignore)
echo "DODO_PAYMENTS_API_KEY=dodo_test_..." > .env
```

**Drehen Sie Schlüssel regelmäßig**

Generieren Sie regelmäßig neue API-Schlüssel und widerrufen Sie alte über Ihr Dodo Payments-Dashboard.

**Verwenden Sie Testschlüssel für die Entwicklung**

Immer während der Entwicklung Testmodus-API-Schlüssel verwenden, um Auswirkungen auf Produktionsdaten zu vermeiden.
</Accordion>

{/* LOCKED_PATTERN_1d0876565ecc6c625335a3849461e889 */}
**Wenden Sie Tool-Filterung in der Produktion an**

Beschränken Sie die exponierten Operationen auf das, was Ihr KI-Assistent benötigt.

```bash
# Production: read-only access
npx dodopayments-mcp --operation=read

# Development: full access
npx dodopayments-mcp
```

**Implementieren Sie Authentifizierung für Remote-Server**

Wenn Sie remote bereitstellen, verlangen Sie stets Authentifizierung über den Header `Authorization` oder den Header `x-dodo-payments-api-key`.

**Überwachen Sie die API-Nutzung**

Verfolgen Sie die Aktivität des MCP-Servers über Ihr Dodo Payments-Dashboard und richten Sie Warnmeldungen für ungewöhnliche Muster ein.
</Accordion>

{/* LOCKED_PATTERN_ddb4fc82978c8dda72d606214fdad81d */}
**Verwenden Sie HTTPS für Remote-Server**

Setzen Sie Remote-MCP-Server immer hinter HTTPS-Endpunkten ein.

**Implementieren Sie Ratenbegrenzung**

Schützen Sie sich vor Missbrauch, indem Sie Ratenlimits sowohl auf MCP-Server- als auch auf API-Ebene implementieren.

**Beschränken Sie den Netzwerkzugriff**

Konfigurieren Sie Firewall-Regeln, um zu begrenzen, welche Clients eine Verbindung zu Ihrem MCP-Server herstellen können.
</Accordion>
</AccordionGroup>

## Fehlerbehebung

<AccordionGroup>
{/* LOCKED_PATTERN_3e10fc2511884be492da344a929e2311 */}
**Überprüfen Sie Ihren API-Schlüssel**

Stellen Sie sicher, dass Ihr API-Schlüssel korrekt gesetzt ist und die erforderlichen Berechtigungen hat.

```bash
# Test your API key
curl -H "Authorization: Bearer dodo_test_..." \
  https://test.dodopayments.com/payments
```

**Überprüfen Sie Ihre Netzwerkverbindung**

Stellen Sie sicher, dass Sie die Dodo Payments API-Endpunkte erreichen können.

**Überprüfen Sie die Client-Protokolle**

Aktivieren Sie ausführliches Logging in Ihrem MCP-Client, um Verbindungsprobleme zu diagnostizieren.
</Accordion>

{/* LOCKED_PATTERN_8f29ea613a43315c29bf194043f8ada2 */}
**Bestätigen Sie die API-Schlüssel-Umgebung**

Stellen Sie sicher, dass Sie Testschlüssel mit Testendpunkten und Live-Schlüssel mit Produktionsendpunkten verwenden.

**Überprüfen Sie die Umgebungsvariable**

Überprüfen Sie, ob `DODO_PAYMENTS_ENVIRONMENT` korrekt gesetzt ist (`live_mode` für Produktionsbetrieb).

**Generieren Sie Anmeldeinformationen neu**

Wenn die Probleme weiterhin bestehen, erstellen Sie über Ihr Dashboard einen neuen API-Schlüssel.
</Accordion>

{/* LOCKED_PATTERN_565675849d369f82d965d1f3a122632b */}
**Validieren Sie Eingabeparameter**

Stellen Sie sicher, dass der KI-Assistent korrekt formatierte Parameter für jedes Tool bereitstellt.

**Überprüfen Sie Fehlermeldungen**

Überprüfen Sie die Fehlermeldung von der API für spezifische Hinweise darauf, was schiefgelaufen ist.

**Testen Sie direkt mit der API**

Überprüfen Sie, ob die Operation funktioniert, indem Sie die Dodo Payments API direkt über curl oder Postman aufrufen.
</Accordion>

{/* LOCKED_PATTERN_776734428589daf9897a729aa0a38aed */}
**Probieren Sie eine andere Client-Voreinstellung**

Wenn Tools nicht richtig funktionieren, versuchen Sie, Ihren Client explizit anzugeben:

```bash
npx dodopayments-mcp --client=cursor
```

**Überprüfen Sie die Unterstützung von Fähigkeiten**

Einige Clients unterstützen nicht alle JSON-Schema-Features. Verwenden Sie `--describe-capabilities`, um zu verstehen, was unterstützt wird.
</Accordion>
</AccordionGroup>

## Ressourcen

<CardGroup cols={2}>
{/* LOCKED_PATTERN_4325afcb089e351f99ef591b38203ac2 */}
  Erkunden Sie die vollständige Dodo Payments API-Dokumentation
</Card>

{/* LOCKED_PATTERN_1cbca36bdeb2bbbc77032220bd30c277 */}
  Erfahren Sie mehr über den Model Context Protocol-Standard
</Card>

{/* LOCKED_PATTERN_02488b19a875a7c4cd24c2f5651e4831 */}
  Sehen Sie sich den Quellcode des MCP-Servers an und leisten Sie Beiträge
</Card>

{/* LOCKED_PATTERN_d4dbe77bbb87d236d98e318084853b86 */}
  Installieren Sie den MCP-Server über NPM
</Card>
</CardGroup>