---
title: Checkout-Sitzungen
description: Erstellen Sie sichere, gehostete Checkout-Erlebnisse, die den
  vollständigen Zahlungsfluss sowohl für einmalige Käufe als auch für
  Abonnements mit vollständiger Anpassungssteuerung abwickeln.
icon: cart-shopping
---
<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  Lassen Sie Ihre erste Checkout-Sitzung in weniger als 5 Minuten laufen
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  Erkunden Sie die vollständige API-Dokumentation und testen Sie interaktiv Checkout Session-Anfragen und -Antworten.
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  Berechnen Sie Preise, Steuern und Gesamtsummen, bevor Sie eine Sitzung erstellen.
</Card>
</CardGroup>

<Info>
**Gültigkeit der Sitzung**: Checkout-Sitzungen sind standardmäßig 24 Stunden gültig. Wenn Sie `confirm=true` in Ihre Anfrage aufnehmen, ist die Sitzung nur 15 Minuten gültig.
</Info>

## Voraussetzungen

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  Sie benötigen ein aktives Dodo Payments Händlerkonto mit API-Zugang.
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  Generieren Sie Ihre API-Zugangsdaten über das Dodo Payments Dashboard:
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  Legen Sie Ihre Produkte im Dodo Payments Dashboard an, bevor Sie Checkout-Sitzungen implementieren.
</Step>
</Steps>

## Erstellen Ihrer ersten Checkout-Sitzung

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### API-Antwort

Alle oben genannten Methoden geben die gleiche Antwortstruktur zurück:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
Extrahieren Sie `checkout_url` aus der API-Antwort.
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
Leiten Sie Ihren Kunden auf die Checkout-URL weiter, um den Kauf abzuschließen.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Alternative Integrationsoptionen**: Anstatt umzuleiten, können Sie den Checkout direkt auf Ihrer Seite einbetten, indem Sie [Overlay Checkout](/developer-resources/overlay-checkout) (Modal-Overlay) oder [Inline Checkout](/developer-resources/inline-checkout) (vollständig eingebettet) verwenden. Beide Optionen nutzen dieselbe Checkout-Session-URL.
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
Nach der Zahlung werden Kunden zu Ihrem `return_url` mit Abfrageparametern weitergeleitet, darunter Zahlungs-/Abonnement-ID, Status, Kunden-E-Mail und eventuelle Lizenzschlüssel. Siehe die [`return_url`-Parameter-Dokumentation](#request-body) für die vollständige Liste.
</Step>
</Steps>

## Anfragekörper

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  Wesentliche Felder, die für jede Checkout-Sitzung benötigt werden
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  Zusätzliche Konfiguration zur Anpassung Ihres Checkout-Erlebnisses
</Card>
</CardGroup>

### Erforderliche Felder

<ParamField body="product_cart" type="array" required>
Array von Produkten, die in die Checkout-Sitzung aufgenommen werden sollen. Jedes Produkt muss eine gültige `product_id` aus Ihrem Dodo Payments Dashboard besitzen.

<Tip>
**Gemischter Checkout**: Sie können Einmalzahlungsprodukte und Abonnementprodukte in derselben Checkout-Sitzung kombinieren. Das ermöglicht leistungsstarke Anwendungsfälle wie Einrichtungsgebühren mit Abos, Hardware-Bundles mit SaaS und mehr.
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
Die eindeutige Kennung des Produkts aus Ihrem Dodo Payments Dashboard.

**Beispiel:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
Menge des Produkts.

**Beispiel:** `1` für einzelne Artikel, `3` für mehrere Mengen
</ParamField>

<ParamField body="addons" type="array">
Array von Add-ons, die dem Produkt angefügt werden. Nur gültig, wenn das Produkt ein Abonnement ist.

{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
Betrag, den der Kunde zahlt, wenn pay_what_you_want aktiviert ist. Wenn deaktiviert, wird dieses Feld ignoriert.

**Format**: Wird in der kleinsten Währungseinheit dargestellt (z. B. Cent für USD). Um 1,00 $ zu berechnen, übergeben Sie `100`.
</ParamField>
</Expandable>
</ParamField>

<Tip>
**Finden Sie Ihre Produkt-IDs**: Sie finden Produkt-IDs in Ihrem Dodo Payments Dashboard unter Produkte → Details anzeigen oder über die [List Products API](/api-reference/products/get-products).
</Tip>

### Optionale Felder

Konfigurieren Sie diese Felder, um das Checkout-Erlebnis anzupassen und Geschäftslogik in Ihren Zahlungsfluss einzufügen.

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
Kundeninformationen. Sie können entweder einen bestehenden Kunden anhand seiner ID anhängen oder während des Checkouts einen neuen Kunden anlegen.

<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}
Hängen Sie einen bestehenden Kunden mit seiner ID an die Checkout-Sitzung an.

{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
Die eindeutige Kennung eines bestehenden Kunden. Verwenden Sie diese, um die Checkout-Sitzung einem bestehenden Kunden zuzuordnen, anstatt einen neuen zu erstellen.
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
Erstellen Sie während des Checkouts einen neuen Kunden.

{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
E-Mail-Adresse des Kunden. Erforderlich, wenn ein neuer Kunde erstellt wird.
</ParamField>

<ParamField body="name" type="string">
Vollständiger Name des Kunden, wie er auf Belegen und Rechnungen erscheinen soll.

**Beispiel**: `"John Doe"` oder `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
Telefonnummer des Kunden im internationalen Format. Für einige Zahlungsmethoden und zur Betrugsprävention erforderlich.

**Format**: Ländervorwahl angeben, z. B. `"+1234567890"` für US-Nummern
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
Rechnungsadresse für eine präzise Steuerberechnung, Betrugsprävention und regulatorische Anforderungen.

<Info>
Wenn `confirm` auf `true` gesetzt wird, werden alle Rechnungsadressefelder für die erfolgreiche Erstellung einer Sitzung erforderlich.
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
Vollständige Straßenadresse inklusive Hausnummer, Straßenname und ggf. Apartment-/Wohnungseinheit.

**Beispiel**: `"123 Main St, Apt 4B"` oder `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
Stadt oder Gemeinde.

**Beispiel**: `"San Francisco"`, `"New York"`, `"London"`
</ParamField>

<ParamField body="state" type="string">
Name des Bundeslandes, der Provinz oder Region. Verwenden Sie vollständige Bezeichnungen oder gängige Abkürzungen.

**Beispiel**: `"California"` oder `"CA"`, `"Ontario"` oder `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
Zwei-Buchstaben-ISO-Ländercode (ISO 3166-1 alpha-2). Dieses Feld ist immer erforderlich, wenn billing_address angegeben ist.

**Beispiele**: `"US"` (Vereinigte Staaten), `"CA"` (Kanada), `"GB"` (Vereinigtes Königreich), `"DE"` (Deutschland)

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  Vollständige Liste der unterstützten Länder und deren ISO-Codes anzeigen
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
Postleitzahl, ZIP-Code oder Äquivalent entsprechend den Länderanforderungen.

**Beispiele**: `"94102"` (US), `"M5V 3A8"` (Kanada), `"SW1A 1AA"` (UK)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
Steuern Sie, welche Zahlungsmethoden Kunden während des Checkouts zur Verfügung stehen. Das hilft, sich auf bestimmte Märkte oder geschäftliche Anforderungen einzustellen.

**Verfügbare Optionen**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**Kritisch**: Fügen Sie immer `credit` und `debit` als Fallback-Optionen ein, um Checkout-Fehler zu vermeiden, wenn bevorzugte Zahlungsmethoden nicht verfügbar sind.
</Warning>

**Beispiel**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
Überschreiben Sie die standardmäßige Währungsauswahl mit einer festen Abwicklungswährung. Verwendet ISO-4217-Währungscodes.

**Unterstützte Währungen**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR` und mehr

**Beispiel**: `"USD"` für US-Dollar, `"EUR"` für Euro

<Note>
Dieses Feld ist nur wirksam, wenn die adaptive Preisgestaltung aktiviert ist. Wenn adaptive Preisgestaltung deaktiviert ist, wird die Standardwährung des Produkts verwendet.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
Zeigen Sie zuvor gespeicherte Zahlungsmethoden für wiederkehrende Kunden an, um den Checkout zu beschleunigen und das Nutzererlebnis zu verbessern.
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
URL, zu der Kunden nach Abschluss der Zahlung weitergeleitet werden. Dodo Payments hängt beim Redirect folgende Query-Parameter an Ihre URL an:

| Parameter | Typ | Bedingung |
|-----------|------|-----------|
| `payment_id` | `string` | Immer vorhanden bei Einmalzahlungen |
| `subscription_id` | `string` | Immer vorhanden bei Abonnementzahlungen |
| `status` | `string` | Immer vorhanden |
| `license_key` | `string` | Vorhanden, wenn das Produkt Lizenzschlüssel aktiviert hat. Komma-getrennt bei mehreren Schlüsseln |
| `email` | `string` | Vorhanden, wenn der Kunde eine E-Mail-Adresse hinterlegt hat |

**Beispiel-Redirect-URLs:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
Verwenden Sie die Query-Parameter `license_key` und `email`, um Lizenzschlüssel anzuzeigen oder sofort auf Ihrer Rückkehrseite eine Bestätigung zu senden, ohne einen zusätzlichen API-Aufruf zu benötigen.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
Wenn true, werden alle Sitzungsdetails sofort festgeschrieben. Die API wirft einen Fehler, wenn erforderliche Daten fehlen.
</ParamField>

<ParamField body="discount_code" type="string">
Wenden Sie einen Rabattcode auf die Checkout-Sitzung an.
</ParamField>

<ParamField body="metadata" type="object">
Benutzerdefinierte Schlüssel-Wert-Paare zur Speicherung zusätzlicher Informationen über die Sitzung.
</ParamField>

<ParamField body="force_3ds" type="boolean">
Überschreiben Sie das standardmäßige 3DS-Verhalten des Händlers für diese Sitzung.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Aktivieren Sie den Modus für minimale Adressabfrage. Wenn aktiviert, erhebt der Checkout nur:
- **Land**: Immer erforderlich zur Steuerbestimmung
- **PLZ/Postleitzahl**: Nur in Regionen, in denen sie für Sales Tax, VAT oder GST notwendig ist

Dies reduziert die Checkout-Reibung erheblich, indem unnötige Formularfelder eliminiert werden.

<Tip>
Aktivieren Sie minimale Adressangabe für schnelleren Checkout. Die vollständige Adressabfrage bleibt für Unternehmen verfügbar, die vollständige Rechnungsdaten benötigen.
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
Passen Sie das Erscheinungsbild und Verhalten der Checkout-Oberfläche an.

{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
Design für die Checkout-Oberfläche. Optionen: `light`, `dark` oder `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
Zeigen Sie den Abschnitt mit Bestelldetails in der Checkout-Oberfläche an.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
Zeigen Sie das “on-demand”-Label für entsprechende Produkte an.
</ParamField>

<ParamField body="force_language" type="string">
Erzwingen Sie, dass die Checkout-Oberfläche in einer bestimmten Sprache gerendert wird. Standardmäßig erkennt der Checkout automatisch die Sprache des Browsers des Kunden.

**Unterstützte Sprachcodes:**
`ar` (Arabisch), `ca` (Katalanisch), `de` (Deutsch), `en` (Englisch), `es` (Spanisch), `fr` (Französisch), `he` (Hebräisch), `id` (Indonesisch), `it` (Italienisch), `ja` (Japanisch), `ko` (Koreanisch), `ms` (Malaiisch), `nl` (Niederländisch), `pl` (Polnisch), `pt` (Portugiesisch), `ro` (Rumänisch), `ru` (Russisch), `sv` (Schwedisch), `th` (Thailändisch), `tr` (Türkisch), `zh` (Chinesisch)

**Beispiel**: `"es"` für Spanisch, `"ja"` für Japanisch
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
Konfigurieren Sie spezifische Funktionen und Verhaltensweisen für die Checkout-Sitzung.

{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
Ermöglichen Sie Kunden die Auswahl ihrer bevorzugten Währung während des Checkouts.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
Zeigen Sie das Eingabefeld für Rabattcodes in der Checkout-Oberfläche an.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
Erfassen Sie Telefonnummern der Kunden während des Checkouts.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
Erlauben Sie Kunden, Steueridentifikationsnummern einzugeben.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
Erzwingen Sie die Erstellung eines neuen Kundenkontos anstatt vorhandene zu aktualisieren.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
Ermöglichen Sie Kunden, ihre E-Mail-Adresse während des Checkouts zu bearbeiten.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
Ermöglichen Sie Kunden, ihren Namen während des Checkouts zu bearbeiten.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
Ermöglichen Sie Kunden, die Straßenadresse während des Checkouts zu bearbeiten.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
Ermöglichen Sie Kunden, die Stadt während des Checkouts zu bearbeiten.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
Ermöglichen Sie Kunden, das Bundesland während des Checkouts zu bearbeiten.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
Ermöglichen Sie Kunden, das Land während des Checkouts zu bearbeiten.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
Ermöglichen Sie Kunden, die Postleitzahl während des Checkouts zu bearbeiten.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Aktivieren Sie den Modus für minimale Adressabfrage. Wenn aktiviert, erhebt der Checkout nur:
- **Land**: Immer erforderlich zur Steuerbestimmung
- **PLZ/Postleitzahl**: Nur in Regionen, in denen sie für Sales Tax, VAT oder GST notwendig ist

Dies reduziert die Checkout-Reibung erheblich, indem unnötige Formularfelder eliminiert werden.

<Tip>
Aktivieren Sie minimale Adressangabe für schnelleren Checkout. Die vollständige Adressabfrage bleibt für Unternehmen verfügbar, die vollständige Rechnungsdaten benötigen.
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
Überspringen Sie die Standard-Zahlungserfolgsseite und leiten Sie Kunden nach Abschluss der Zahlung sofort zu Ihrem `return_url` weiter.

<Tip>
Verwenden Sie dies, wenn Sie eine benutzerdefinierte Erfolgsseite haben, die ein besseres Nutzererlebnis bietet, oder für mobile Apps und eingebettete Checkout-Flows.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
Sammeln Sie während des Checkouts zusätzliche Informationen von Kunden mittels benutzerdefinierter Formularfelder. Sie können bis zu 5 benutzerdefinierte Felder pro Checkout-Sitzung definieren. Kundenantworten sind in Webhook-Payloads enthalten und über die API abrufbar.

{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
Eindeutige Kennung für dieses Feld. Wird als Schlüssel in Webhook-Payloads und API-Antworten verwendet.

**Beispiel**: `"company_name"`, `"referral_source"`, `"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
Anzuzeigende Bezeichnung, die dem Kunden im Checkout-Formular angezeigt wird.

**Beispiel**: `"Company Name"`, `"How did you hear about us?"`, `"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
Feldtyp bestimmt die Validierungsregeln für die Eingabe.

**Verfügbare Typen**:
- `text` - Einzeilige Texteingabe
- `number` - Numerische Eingabe
- `email` - E-Mail-Adresse mit Validierung
- `url` - URL mit Validierung
- `date` - Datumsauswahl
- `dropdown` - Auswahl aus vordefinierten Optionen
- `boolean` - Ja/Nein-Umschalter
</ParamField>

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
Ob dieses Feld ausgefüllt sein muss, bevor der Checkout abgeschlossen werden kann.

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
Platzhaltertext, der im Eingabefeld angezeigt wird.

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
Liste von Optionen für den Feldtyp `dropdown`. Erforderlich, wenn `field_type` `dropdown` ist, wird für andere Typen ignoriert.

**Beispiel**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
Kundenantworten auf benutzerdefinierte Felder sind enthalten in:
- **Webhooks**: `payment.succeeded`, `subscription.created` und andere relevante Ereignis-Payloads enthalten das `custom_field_responses`-Array
- **API-Antworten**: Zahlungs- und Abonnementobjekte enthalten `custom_field_responses`
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
Zusätzliche Konfiguration für Checkout-Sitzungen mit Abonnementprodukten.

{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
Anzahl der Tage für den Testzeitraum vor der ersten Abbuchung. Muss zwischen 0 und 10000 Tagen liegen.
</ParamField>

<ParamField body="on_demand" type="object">
On-Demand-Abonnementkonfiguration für nutzungsbasierte oder messbare Abrechnung.

{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
Wenn auf true gesetzt, erfolgt keine Abbuchung und es werden nur Zahlungsdetails für die zukünftige Verwendung autorisiert.
</ParamField>

<ParamField body="product_price" type="integer">
Produktpreis für die erste Belastung des Kunden. Wenn nicht angegeben, wird der hinterlegte Preis des Produkts verwendet.

**Format**: Wird in der kleinsten Währungseinheit dargestellt (z. B. Cent für USD). Um 1,00 $ zu berechnen, übergeben Sie `100`.
</ParamField>

<ParamField body="product_currency" type="string">
Optionale Währung des Produktpreises. Wenn nicht angegeben, wird die Währung des Produkts verwendet.
</ParamField>

<ParamField body="product_description" type="string">
Optionale Überschreibung der Produktbeschreibung für Abrechnung und Warenpositionen. Wenn nicht angegeben, wird die gespeicherte Beschreibung verwendet.
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
Ob adaptive Währungsgebühren im Produktpreis enthalten sein sollen (true) oder zusätzlich verrechnet werden (false). Wird ignoriert, wenn adaptive Preisgestaltung nicht aktiviert ist.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

## Praxisbeispiele

Hier sind 10 umfassende Beispiele, die verschiedene Checkout-Session-Konfigurationen für unterschiedliche Geschäftsszenarien zeigen:

### 1. Einfacher Checkout für ein Einzelprodukt

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. Warenkorb mit mehreren Produkten

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. Abo mit Testphase

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. Vorbestätigter Checkout

<Note>
Wenn `confirm` auf `true` gesetzt wird, gelangt der Kunde direkt zur Checkout-Seite und überspringt alle Bestätigungsschritte.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. Checkout mit Währungsüberschreibung

<Note>
Die `billing_currency`-Überschreibung greift nur, wenn adaptive Währung in Ihren Kontoeinstellungen aktiviert ist. Wenn adaptive Währung deaktiviert ist, hat dieser Parameter keine Wirkung.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. Gespeicherte Zahlungsmethoden für wiederkehrende Kunden

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. B2B-Checkout mit Steuernummer-Erfassung

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. Checkout mit Dark Theme und Rabattcode

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. Regionale Zahlungsmethoden (UPI für Indien)

Für detaillierte Informationen zur UPI-Konfiguration und Prüfung siehe die <a href="/features/payment-methods/india">India Payment Methods</a>-Seite.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. BNPL (Buy Now Pay Later) Checkout

Für detaillierte Informationen zur BNPL-Konfiguration und Prüfung siehe die <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a>-Seite.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. Verwendung vorhandener Zahlungsmethoden für sofortigen Checkout

Verwenden Sie die gespeicherte Zahlungsmethode eines Kunden, um eine Checkout-Sitzung zu erstellen, die sofort verarbeitet wird, ohne dass eine erneute Sammlung der Zahlungsmethode erforderlich ist:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
Wenn Sie `payment_method_id` verwenden, muss `confirm` auf `true` gesetzt sein, und eine bestehende `customer_id` muss bereitgestellt werden. Die Zahlungsmethode wird auf ihre Kompatibilität mit der Zahlungswährung geprüft.
</Warning>

<Info>
Die Zahlungsmethode muss dem Kunden gehören und mit der Zahlungswährung kompatibel sein. Dadurch sind Ein-Klick-Käufe für wiederkehrende Kunden möglich.
</Info>

### 12. Kurze Links für sauberere Zahlungs-URLs

Generieren Sie verkürzte, teilbare Zahlungslinks mit benutzerdefinierten Slugs:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
Kurze Links eignen sich perfekt für SMS, E-Mails oder Social-Media-Teilen. Sie sind leichter zu merken und schaffen mehr Vertrauen als lange URLs.
</Tip>

### 13. Überspringen der Erfolgsseite mit sofortigem Redirect

Leiten Sie Kunden nach Abschluss der Zahlung sofort weiter und überspringen Sie die Standard-Erfolgsseite:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
Verwenden Sie `redirect_immediately: true`, wenn Sie eine benutzerdefinierte Erfolgsseite haben, die ein besseres Nutzererlebnis bietet als die Standardseite. Dies ist besonders nützlich für mobile Apps und eingebettete Checkout-Flows.
</Tip>

<Note>
Wenn `redirect_immediately` aktiviert ist, werden Kunden direkt nach Abschluss der Zahlung zu Ihrem `return_url` weitergeleitet und die Standard-Erfolgsseite wird vollständig übersprungen.
</Note>

### 14. Sprache erzwingen

Erzwingen Sie, dass der Checkout in einer bestimmten Sprache angezeigt wird und überschreiben Sie die Browser-Spracherkennung des Kunden:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
Verwenden Sie `force_language`, wenn Sie die bevorzugte Sprache Ihres Kunden kennen (z. B. aus seinen Kontoeinstellungen) oder wenn Sie auf bestimmte regionale Märkte abzielen.
</Tip>

**Unterstützte Sprachen:** Arabisch (`ar`), Katalanisch (`ca`), Chinesisch (`zh`), Niederländisch (`nl`), Englisch (`en`), Französisch (`fr`), Deutsch (`de`), Hebräisch (`he`), Indonesisch (`id`), Italienisch (`it`), Japanisch (`ja`), Koreanisch (`ko`), Malaiisch (`ms`), Polnisch (`pl`), Portugiesisch (`pt`), Rumänisch (`ro`), Russisch (`ru`), Spanisch (`es`), Schwedisch (`sv`), Thailändisch (`th`), Türkisch (`tr`)

### 15. Sammeln benutzerdefinierter Felder

Sammeln Sie während des Checkouts zusätzliche Informationen von Kunden mithilfe benutzerdefinierter Felder:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
Antworten auf benutzerdefinierte Felder werden automatisch in Webhook-Payloads (`payment.succeeded`, `subscription.created` usw.) enthalten und können über die API abgefragt werden. Nutzen Sie sie, um Ihr CRM anzureichern, Onboarding-Flows auszulösen oder das Kundenerlebnis zu personalisieren.
</Info>

**Verfügbare Feldtypen**: `text`, `number`, `email`, `url`, `date`, `dropdown`, `boolean`

## Vorschau von Checkout-Sitzungen

Bevor Sie eine Checkout-Sitzung erstellen, können Sie die Preisaufschlüsselung inklusive Steuern, Rabatten und Gesamtsummen vorab ansehen. Das ist nützlich, um Kunden vor dem Checkout präzise Preise anzuzeigen.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  Sehen Sie sich die vollständige Dokumentation des Preview-Endpunkts an.
</Card>

## Wechsel von Dynamic Links zu Checkout-Sitzungen

### Wichtige Unterschiede

Früher, als Sie einen Zahlungslink mit Dynamic Links erstellt haben, mussten Sie die vollständige Rechnungsadresse des Kunden angeben.

Mit Checkout-Sitzungen ist das nicht mehr nötig. Sie können einfach die Informationen übermitteln, die Ihnen vorliegen. Wir kümmern uns um den Rest. Zum Beispiel:
- Wenn Sie nur das Rechnungsland des Kunden kennen, geben Sie nur dieses an.
- Der Checkout-Prozess sammelt automatisch die fehlenden Daten, bevor der Kunde zur Zahlungsseite weitergeleitet wird.
- Wenn Sie hingegen bereits alle erforderlichen Informationen haben und direkt zur Zahlungsseite springen möchten, können Sie das vollständige Datenset übermitteln und `confirm=true` in Ihren Request-Body aufnehmen.

### Migrationsprozess

Die Migration von Dynamic Links zu Checkout-Sitzungen ist unkompliziert:

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
Aktualisieren Sie Ihre Integration, um die neue API- oder SDK-Methode zu verwenden.
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
Passen Sie die Request-Payload an das Checkout-Sessions-Format an.
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
Ja. Es sind keine zusätzlichen Maßnahmen oder speziellen Migrationsschritte Ihrerseits erforderlich.
</Step>
</Steps>
