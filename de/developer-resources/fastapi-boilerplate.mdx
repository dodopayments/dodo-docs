---
sidebarTitle: FastAPI
title: FastAPI-Boilerplate
description: Schneller Einstieg mit unserem FastAPI-Boilerplate zur Integration
  von Dodo Payments in Ihre Python-Backend-Anwendungen
icon: /images/logos/fastapi.svg
tag: NEW
keywords:
  - Dodo Payments
  - FastAPI
  - Python
  - Starter-Vorlage
  - Schnellstart
  - FastAPI-Boilerplate
---

<Card title="GitHub Repository" icon="github" href="https://github.com/dodopayments/fastapi-boilerplate">
  Vollständiges FastAPI- und Dodo Payments-Boilerplate
</Card>

## Überblick

Das FastAPI-Boilerplate bietet einen produktionsbereiten Ausgangspunkt für die Integration von Dodo Payments in Ihr Python-Backend. Diese Vorlage umfasst die Verwaltung von Checkout-Sitzungen, die Verifizierung von Webhooks, die Integration des Customer Portal sowie asynchrone API-Muster, damit Sie schnell Zahlungen akzeptieren können.

<Info>
Dieses Boilerplate verwendet FastAPI mit async/await-Mustern, Pydantic für die Validierung und das `dodopayments` Python SDK für eine nahtlose API-Integration.
</Info>

### Funktionen

- **Schnelle Einrichtung** – In weniger als 5 Minuten startklar
- **Async-Unterstützung** – Mit den nativen async/await-Mustern von FastAPI entwickelt
- **Checkout-Sitzungen** – Vorkonfigurierter Checkout-Ablauf mit dem Python SDK
- **Webhook-Verarbeitung** – Sicherer Webhook-Endpunkt mit Signaturverifizierung
- **Customer Portal** – Einfache Erstellung von Customer-Portal-Sitzungen
- **Typsicherheit** – Vollständige Pydantic-Validierung und Type Hints
- **Umgebungskonfiguration** – Direkt einsetzbare Einrichtung von Umgebungsvariablen

## Voraussetzungen

Stellen Sie vor Beginn sicher, dass Folgendes vorhanden ist:

- **Python 3.9+** (empfohlen: Python 3.11+)
- **pip** oder **uv** für die Paketverwaltung
- **Dodo Payments-Konto** (für den Zugriff auf API- und Webhook Keys im Dashboard)

## Schnellstart

<Steps>
<Step title="Clone the Repository">

```bash
git clone https://github.com/dodopayments/fastapi-boilerplate.git
cd fastapi-boilerplate
```

</Step>

<Step title="Create Virtual Environment">
Richten Sie eine isolierte Python-Umgebung ein:

```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

Oder verwenden Sie uv für eine schnellere Abhängigkeitsverwaltung:

```bash
uv venv
source .venv/bin/activate
```

</Step>

<Step title="Install Dependencies">

```bash
pip install -r requirements.txt
```

Oder mit uv:

```bash
uv pip install -r requirements.txt
```

</Step>

<Step title="Get API Credentials">
Registrieren Sie sich unter [Dodo Payments](https://dodopayments.com/) und rufen Sie Ihre Zugangsdaten im Dashboard ab:

- **API Key:** [Dashboard → Developer → API Keys](https://app.dodopayments.com/developer/api-keys)
- **Webhook Key:** [Dashboard → Developer → Webhooks](https://app.dodopayments.com/developer/webhooks)

<Tip>
Stellen Sie sicher, dass Sie sich während der Entwicklung im **Test Mode** befinden!
</Tip>
</Step>

<Step title="Configure Environment Variables">
Erstellen Sie im Stammverzeichnis eine `.env`-Datei:

```bash
cp .env.example .env
```

Aktualisieren Sie die Werte mit Ihren Dodo Payments-Zugangsdaten:

```bash .env
DODO_PAYMENTS_API_KEY=your_api_key_here
DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_signing_key_here
DODO_PAYMENTS_ENVIRONMENT=test_mode
```

<Warning>
Committen Sie Ihre `.env`-Datei niemals in die Versionsverwaltung. Sie ist bereits in `.gitignore` enthalten.
</Warning>
</Step>

<Step title="Run the Development Server">

```bash
uvicorn main:app --reload
```

Öffnen Sie [http://localhost:8000/docs](http://localhost:8000/docs), um die interaktive API-Dokumentation anzuzeigen!

<Check>
Sie sollten die Swagger UI von FastAPI mit allen verfügbaren Endpunkten sehen, die zum Testen bereitstehen.
</Check>
</Step>
</Steps>

## Projektstruktur

```text
fastapi-boilerplate/
├── main.py                 # FastAPI application entry point
├── routers/
│   ├── checkout.py         # Checkout session endpoints
│   ├── webhook.py          # Webhook handler endpoint
│   └── customer_portal.py  # Customer portal endpoints
├── config.py               # Environment configuration
├── requirements.txt        # Python dependencies
├── .env.example            # Environment template
└── README.md
```

## API-Endpunkte

Das Boilerplate umfasst die folgenden vorkonfigurierten Endpunkte:

| Endpunkt | Methode | Beschreibung |
|----------|--------|-------------|
| `/checkout` | POST | Neue Checkout-Sitzung erstellen |
| `/webhook` | POST | Dodo Payments-Webhooks verarbeiten |
| `/customer-portal` | POST | URL für das Customer Portal generieren |

## Codebeispiele

### Checkout-Sitzung erstellen

```python
from fastapi import APIRouter, HTTPException
from dodopayments import AsyncDodoPayments
from pydantic import BaseModel
from config import settings

router = APIRouter()
dodo = AsyncDodoPayments(
    bearer_token=settings.DODO_PAYMENTS_API_KEY,
    environment=settings.DODO_PAYMENTS_ENVIRONMENT
)

class CheckoutRequest(BaseModel):
    product_id: str
    quantity: int = 1
    customer_email: str | None = None
    customer_name: str | None = None

@router.post("/checkout")
async def create_checkout(request: CheckoutRequest):
    try:
        session = await dodo.checkout_sessions.create(
            product_cart=[{
                "product_id": request.product_id,
                "quantity": request.quantity
            }],
            customer={
                "email": request.customer_email,
                "name": request.customer_name
            } if request.customer_email else None,
            return_url="http://localhost:8000/success"
        )
        return {"checkout_url": session.checkout_url, "session_id": session.session_id}
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))
```

### Webhooks verarbeiten

```python
from fastapi import APIRouter, Request, HTTPException
from standardwebhooks import Webhook
from config import settings

router = APIRouter()
webhook = Webhook(settings.DODO_PAYMENTS_WEBHOOK_KEY)

@router.post("/webhook")
async def handle_webhook(request: Request):
    payload = await request.body()

    # Dodo Payments follows the Standard Webhooks spec: the signature covers
    # `id.timestamp.body`, so all three webhook-* headers must be passed in.
    try:
        webhook.verify(payload, dict(request.headers))
    except Exception:
        raise HTTPException(status_code=401, detail="Invalid signature")

    event = await request.json()
    event_type = event.get("type")
    
    match event_type:
        case "payment.succeeded":
            # Handle successful payment
            payment_id = event["data"]["payment_id"]
            print(f"Payment succeeded: {payment_id}")
            
        case "subscription.active":
            # Handle subscription activation
            subscription_id = event["data"]["subscription_id"]
            print(f"Subscription activated: {subscription_id}")
            
        case "refund.succeeded":
            # Handle refund
            refund_id = event["data"]["refund_id"]
            print(f"Refund processed: {refund_id}")
    
    return {"status": "received"}
```

### Integration von Customer Portal

```python
from fastapi import APIRouter, HTTPException
from dodopayments import AsyncDodoPayments
from pydantic import BaseModel
from config import settings

router = APIRouter()
dodo = AsyncDodoPayments(
    bearer_token=settings.DODO_PAYMENTS_API_KEY,
    environment=settings.DODO_PAYMENTS_ENVIRONMENT
)

class PortalRequest(BaseModel):
    customer_id: str

@router.post("/customer-portal")
async def create_portal_session(request: PortalRequest):
    try:
        session = await dodo.customers.customer_portal.create(
            customer_id=request.customer_id,
        )
        return {"portal_url": session.link}
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))
```

## Webhook-Ereignisse

Das Boilerplate veranschaulicht die Verarbeitung gängiger Webhook-Ereignisse:

| Ereignis | Beschreibung |
|-------|-------------|
| `payment.succeeded` | Zahlung erfolgreich abgeschlossen |
| `payment.failed` | Zahlungsveruch fehlgeschlagen |
| `subscription.active` | Abonnement ist jetzt aktiv |
| `subscription.cancelled` | Abonnement wurde gekündigt |
| `refund.succeeded` | Rückerstattung erfolgreich verarbeitet |

Fügen Sie Ihre Geschäftslogik in den Webhook-Handler ein, um:
- Benutzerberechtigungen in Ihrer Datenbank zu aktualisieren
- Bestätigungs-E-Mails zu senden
- Zugriff auf digitale Produkte bereitzustellen
- Analysen und Metriken zu erfassen

## Webhooks lokal testen

Verwenden Sie für die lokale Entwicklung Tools wie [ngrok](https://ngrok.com/), um Ihren lokalen Server öffentlich zugänglich zu machen:

```bash
ngrok http 8000
```

Aktualisieren Sie die Webhook-URL in Ihrem [Dodo Payments Dashboard](https://app.dodopayments.com/developer/webhooks):

```
https://your-ngrok-url.ngrok.io/webhook
```

## Bereitstellung

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

Erstellen und ausführen:

```bash
docker build -t fastapi-dodo .
docker run -p 8000:8000 --env-file .env fastapi-dodo
```

### Überlegungen zur Produktion

<Warning>
Vor der Bereitstellung in der Produktion:
- Ändern Sie `DODO_PAYMENTS_ENVIRONMENT` zu `live_mode`
- Verwenden Sie Produktions-API Keys aus dem Dashboard
- Aktualisieren Sie die Webhook-URL auf Ihre Produktionsdomain
- Aktivieren Sie HTTPS für alle Endpunkte
</Warning>

## Fehlerbehebung

<AccordionGroup>
<Accordion title="Import errors or missing modules">
Stellen Sie sicher, dass Ihre virtuelle Umgebung aktiviert und die Abhängigkeiten installiert sind:

```bash
source venv/bin/activate
pip install -r requirements.txt
```

</Accordion>

<Accordion title="Checkout session creation fails">
**Häufige Ursachen:**
- Ungültige Produkt-ID – überprüfen Sie, ob sie in Ihrem Dodo Dashboard vorhanden ist
- Falscher API Key oder falsche Umgebungseinstellung in `.env`
- Prüfen Sie die FastAPI-Logs auf detaillierte Fehlermeldungen
</Accordion>

<Accordion title="Webhooks not receiving events">
Verwenden Sie für lokale Tests [ngrok](https://ngrok.com), um Ihren Server öffentlich zugänglich zu machen:

```bash
ngrok http 8000
```

Aktualisieren Sie die Webhook-URL in Ihrem [Dodo Dashboard](https://app.dodopayments.com/developer/webhooks) auf die ngrok-URL. Aktualisieren Sie unbedingt Ihre `.env`-Datei mit dem korrekten Webhook-Verifizierungsschlüssel.
</Accordion>

<Accordion title="Webhook signature verification fails">
- Stelle sicher, dass `DODO_PAYMENTS_WEBHOOK_KEY` in deinem `.env` korrekt gesetzt ist
- Überprüfe, dass du den rohen Request-Body für die Signaturüberprüfung verwendest
- Übergebe alle drei `webhook-id`-, `webhook-timestamp`- und `webhook-signature`-Header an `webhook.verify()` — die Standard-Webhooks-Signatur umfasst `id.timestamp.body`, nicht nur den Body
</Accordion>
</AccordionGroup>

## Weitere Informationen

<CardGroup cols={2}>
<Card title="Python SDK" icon="python" href="/developer-resources/sdks/python">
  Vollständige Dokumentation des Python SDK mit Async-Unterstützung
</Card>

<Card title="Webhooks Documentation" icon="webhook" href="/developer-resources/webhooks">
  Erfahren Sie mehr über alle Webhook-Ereignisse und bewährte Vorgehensweisen
</Card>

<Card title="Checkout Sessions" icon="credit-card" href="/developer-resources/checkout-session">
  Detaillierter Einblick in die Konfiguration von Checkout-Sitzungen
</Card>

<Card title="API Reference" icon="book" href="/api-reference/introduction">
  Vollständige Dodo Payments-API-Dokumentation
</Card>
</CardGroup>

## Support

Benötigen Sie Hilfe mit dem Boilerplate?

- Treten Sie unserer [Discord-Community](https://discord.gg/bYqAp4ayYh) für Fragen und Diskussionen bei
- Sehen Sie im [GitHub-Repository](https://github.com/dodopayments/fastapi-boilerplate) nach Problemen und Aktualisierungen
- Kontaktieren Sie unser [Support-Team](mailto:support@dodopayments.com), wenn Sie Hilfe benötigen
