---
title: Object Storage Blueprint
description: Verfolgen Sie Datei-Uploads und Speichernutzung für S3, Google
  Cloud Storage, Azure Blob und andere Object Storage-Dienste.
icon: box-archive
---

## Anwendungsfälle

Erforschen Sie gängige Szenarien, die vom Object Storage Blueprint unterstützt werden:

<CardGroup cols={2}>
<Card title="File Hosting" icon="folder">
Stellen Sie Kunden basierend auf der gesamten Speichernutzung und dem Upload-Volumen in Rechnung.
</Card>
<Card title="Backup Services" icon="shield">
Verfolgen Sie Backup-Daten-Uploads und berechnen Sie pro gespeichertem GB.
</Card>
<Card title="Media CDN" icon="photo-film">
Überwachen Sie Media-Uploads und berechnen Sie Speicherplatz sowie Bandbreite.
</Card>
<Card title="Document Management" icon="file">
Verfolgen Sie Dokumenten-Uploads pro Kunde für eine nutzungsabhängige Preisgestaltung.
</Card>
</CardGroup>

<Info>
Perfekt für die Abrechnung basierend auf Speicher-Uploads, Dateihosting, CDN-Nutzung oder Backup-Diensten.
</Info>

## Schnellstart

Verfolgen Sie Object Storage-Uploads mit verbrauchten Bytes:

<Steps>
<Step title="Install the SDK">

```bash
npm install @dodopayments/ingestion-blueprints
```

</Step>

<Step title="Get Your API Keys">
- **Dodo Payments API Key**: Holen Sie ihn sich vom [Dodo Payments Dashboard](https://app.dodopayments.com/developer/api-keys)
- **Storage Provider API Key**: Von AWS S3, Google Cloud Storage, Azure usw.
</Step>

<Step title="Create a Meter">
Erstellen Sie einen Meter in Ihrem [Dodo Payments Dashboard](https://app.dodopayments.com/):
- **Event Name**: `object_storage_upload` (oder Ihr bevorzugter Name)
- **Aggregation Type**: `sum`, um die gesamten hochgeladenen Bytes zu verfolgen
- **Over Property**: `bytes`, um basierend auf der Speichergröße abzurechnen
</Step>

<Step title="Track Storage Usage">
<CodeGroup>

```javascript AWS S3 Upload
import { Ingestion, trackObjectStorage } from '@dodopayments/ingestion-blueprints';
import { S3Client, PutObjectCommand } from '@aws-sdk/client-s3';
import fs from 'fs';

const ingestion = new Ingestion({
  apiKey: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode',
  eventName: 'object_storage_upload'
});

const s3 = new S3Client({ region: 'us-east-1' });

// Read the file (example: from disk or request)
const fileBuffer = fs.readFileSync('./document.pdf');

// Upload to S3
const command = new PutObjectCommand({
  Bucket: 'my-bucket',
  Key: 'uploads/document.pdf',
  Body: fileBuffer
});

await s3.send(command);

// Track the upload
await trackObjectStorage(ingestion, {
  customerId: 'customer_123',
  bytes: fileBuffer.length
});
```

```javascript Google Cloud Storage
import { Ingestion, trackObjectStorage } from '@dodopayments/ingestion-blueprints';
import { Storage } from '@google-cloud/storage';
import fs from 'fs';

const ingestion = new Ingestion({
  apiKey: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode',
  eventName: 'object_storage_upload'
});

const storage = new Storage();
const bucket = storage.bucket('my-bucket');

// Read the file
const fileBuffer = fs.readFileSync('./image.png');

// Upload to GCS
await bucket.file('uploads/image.png').save(fileBuffer);

// Track the upload
await trackObjectStorage(ingestion, {
  customerId: 'customer_456',
  bytes: fileBuffer.length,
  metadata: {
    bucket: 'my-bucket',
    key: 'uploads/image.png'
  }
});
```

</CodeGroup>
</Step>
</Steps>

## Konfiguration

### Ingestion-Konfiguration

<ParamField path="apiKey" type="string" required>
Ihr Dodo Payments API-Schlüssel aus dem Dashboard.
</ParamField>

<ParamField path="environment" type="string" required>
Umgebungsmodus: `test_mode` oder `live_mode`.
</ParamField>

<ParamField path="eventName" type="string" required>
Ereignisname, der mit Ihrer Meter-Konfiguration übereinstimmt.
</ParamField>

### Optionen zur Verfolgung von Object Storage

<ParamField path="customerId" type="string" required>
Die Kunden-ID für die Abrechnungszuordnung.
</ParamField>

<ParamField path="bytes" type="number">
Anzahl der hochgeladenen Bytes. Erforderlich für die byte-basierte Abrechnung.
</ParamField>

<ParamField path="metadata" type="object">
Optionale Metadaten zum Upload wie Bucket-Name, Inhaltstyp usw.
</ParamField>

## Best Practices

<Tip>
**Vor oder nach dem Upload verfolgen**: Sie können das Ereignis je nach Fehlerbehandlungsstrategie vor oder nach dem tatsächlichen Upload verfolgen.
</Tip>

<Warning>
**Upload-Fehler behandeln**: Verfolgen Sie nur erfolgreiche Uploads, um Abrechnungen für fehlgeschlagene Vorgänge zu vermeiden.
</Warning>
