---
sidebarTitle: サブスクリプション決済の再試行
title: サブスクリプション決済の再試行
description: スマートなバックオフスケジュールで失敗したサブスクリプション更新決済を自動的に再試行し、統合作業なしで収益を回収します。
icon: arrow-rotate-right
keywords:
  - "Dodo Payments"
  - 決済の再試行
  - サブスクリプション回収
  - 更新失敗
  - 督促
  - スマート再試行
  - 収益回収
  - 保留中のサブスクリプション
  - 更新の再試行
  - 自動再試行
---

<Info>
Payment Retries は、失敗したサブスクリプションの**更新**決済を、段階的なバックオフスケジュールに従って自動的に再試行します。再試行が成功すると、顧客による操作や統合作業なしで、サブスクリプションが自動的に再開されます。
</Info>

## Payment Retries とは？

サブスクリプションの更新決済に失敗すると、サブスクリプションは**保留中**になります。Payment Retries を有効にすると、Dodo Payments は、決済が成功するか回収期間が終了するまで、スマートなスケジュールに従って顧客の既存の支払い方法に自動で再請求します。

これにより、期限切れカードの保留、チャージによって残高が補充されるまでの残高不足、一時的なネットワークエラーなど、一時的な失敗によって失われた収益を、顧客へのメール送信や情報更新の依頼なしで回収できます。

<Note>
Payment Retries は、サブスクリプションの**更新**決済にのみ適用されます。初回決済（mandate の設定）、一回限りの決済、プラン変更時の請求、オンデマンド請求は、この機能による再試行の対象外です。
</Note>

## Payment Retries の仕組み

<Steps>
<Step title="Renewal fails">
サブスクリプションの更新決済に失敗し、サブスクリプションが `on_hold` 状態に移行します。
</Step>

<Step title="Retryability check">
失敗時のエラーコードが確認されます。**ソフトディクライン**（残高不足、一般的な拒否、処理エラーやネットワークエラーなど）は再試行の対象です。**ハードディクライン**の場合は、再試行しても結果が変わらないため、再試行チェーンが直ちに終了します。
</Step>

<Step title="Scheduled retry">
拒否が再試行可能で、回収期間内に収まる場合は、次の試行がスケジュールされます。再試行は、段階的なバックオフスケジュールに従い、顧客の既存の支払い方法に対してオフセッションで実行されます。
</Step>

<Step title="Recovery">
最初の再試行が成功すると、サブスクリプションは `active` に戻り、次回の請求日は通常どおり更新されます。再試行が一度も成功しないまま期間が終了すると、再試行は停止し、サブスクリプションは保留中のままになります。
</Step>
</Steps>

## Payment Retries の設定

ダッシュボードの **Settings → Recovery** から Payment Retries を有効化し、設定します。

<Frame caption="Payment Retries settings under Settings → Recovery">
<img src="/images/recovery/payment-retries-settings.png" alt="Recovery Settings page with the Enable Payment Retries toggle on and a Recovery window (days) field set to 13" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

| 設定 | 説明 | デフォルト |
|---------|-------------|---------|
| **Enable Payment Retries** | 失敗したサブスクリプション更新決済を自動的に再試行し、収益を回収します。 | オフ（オプトイン） |
| **Recovery window (days)** | 失敗した決済を諦めるまで再試行する期間です。**1～30** の範囲で指定する必要があります。 | 13 |

**回収期間**は、失敗した更新請求書が作成された時点を起点とします。累積バックオフ遅延がこの期間内に収まる間のみ、再試行がスケジュールされます。

## 再試行スケジュール

再試行間隔は段階的に長くなります。各試行が回収期間内に収まる限り、最大 **8 回** 試行されます。

| 試行 | 前回の試行からの遅延 | 失敗からの経過時間（目安） |
|---------|------------------------------|----------------------------|
| 1 | 12 時間 | 12 時間 |
| 2 | 24 時間 | 36 時間 |
| 3 | 48 時間 | 約 3.5 日 |
| 4 | 72 時間 | 約 6.5 日 |
| 5 | 96 時間 | 約 10.5 日 |
| 6 | 120 時間 | 約 15.5 日 |
| 7 | 7 日 | 約 22.5 日 |
| 8 | 7 日 | 約 29.5 日 |

<Tip>
デフォルトの**13 日間**の回収期間では、1～5 回目の試行が対象になります（5 回目は失敗から約 10.5 日後に実行されます）。より間隔の長い後半の試行（6～8 回目）も実行したい場合は、期間を最大 30 日まで延長してください。
</Tip>

## サブスクリプションのステータス遷移

| イベント | サブスクリプションのステータス |
|-------|---------------------|
| 更新決済に失敗 | `active` → `on_hold` |
| 再試行に失敗 | `on_hold` のまま（期間内であれば次の再試行をスケジュール） |
| 再試行に成功 | `on_hold` → `active`、次回の請求日を更新 |
| 回収期間が終了 | `on_hold` のまま |
| サブスクリプションがキャンセル | スケジュール済みの再試行を直ちに停止（終端状態 — 以降の試行なし） |

<Note>
再試行がスケジュールされている間にサブスクリプションがキャンセルされた場合、再試行チェーンは直ちに終了し、それ以降の試行は行われません。その他の非アクティブなステータス（`on_hold`、`expired`、`pending`、`failed`）では再試行が継続されます。これらの未決済の更新請求書は、顧客がすでに利用した期間に対する債務を表すためです。
</Note>

これらの遷移では標準のサブスクリプション webhook イベントが発生するため、特別な再試行処理なしで、これらのイベントから権限ロジックを制御できます。

| イベント | 発生するタイミング |
|-------|-----------|
| `subscription.on_hold` | 更新に失敗し、サブスクリプションが保留中になったとき |
| `subscription.active` | 再試行が成功し、サブスクリプションが再開されたとき |

<Card title="Subscription Webhook Payloads" icon="webhook" href="/developer-resources/webhooks/intents/subscription">
サブスクリプションのライフサイクルイベントに関する webhook payload スキーマの全文を確認できます。
</Card>

## 再試行可能な失敗と再試行不可能な失敗

| 失敗の種類 | 例 | 再試行されるか |
|--------------|----------|----------|
| **ソフトディクライン** | 残高不足、一般的な拒否、カード利用回数の上限超過、処理エラー、ネットワークエラー／タイムアウト、後でもう一度試すよう求めるエラー | はい |
| **ハードディクライン** | 盗難・紛失カード、無効なカード、do-not-honor、閉鎖済みアカウント、その他の終端的な拒否 | いいえ — チェーンは直ちに終了 |

<Info>
ハードディクラインを再試行しても結果は変わらないため、ハードディクラインが検出されると再試行チェーンは直ちに終了します。このような場合は、Payment Retries と [Subscription Dunning](/features/recovery/subscription-dunning) を組み合わせて、顧客に支払い方法の更新を促してください。
</Info>

## Payment Retries と Dunning の比較

Payment Retries と [Subscription Dunning](/features/recovery/subscription-dunning) は、相互に補完する回収ツールです。

| | Payment Retries | Subscription Dunning |
|--|-----------------|----------------------|
| **仕組み** | 既存の支払い方法に対して静かに再請求します | 顧客に支払い方法の更新をメールで促します |
| **顧客による操作** | 不要 | 顧客がポータルで支払い方法を更新します |
| **最適な対象** | 自然に解消する一時的なエラー／ソフトディクライン | 交換が必要な期限切れまたは無効なカード |

両方を有効にすると、最も幅広く回収できます。自動再試行で一時的な失敗を検出し、Dunning で支払い方法自体の更新が必要な顧客を呼び戻せます。

## 関連項目

<CardGroup cols={2}>
<Card title="Subscription Dunning" icon="repeat" href="/features/recovery/subscription-dunning">
顧客に支払い方法の更新を促すメールシーケンスです。
</Card>

<Card title="Abandoned Cart Recovery" icon="cart-shopping" href="/features/recovery/abandoned-cart-recovery">
対象を絞ったメールで、未完了または失敗した一回限りのチェックアウトを回収します。
</Card>

<Card title="Subscriptions" icon="repeat" href="/features/subscription">
回収フローに関係するサブスクリプションの状態を理解します。
</Card>

<Card title="Subscription Webhooks" icon="webhook" href="/developer-resources/webhooks/intents/subscription">
`subscription.on_hold` および `subscription.active` イベントに対応します。
</Card>
</CardGroup>
