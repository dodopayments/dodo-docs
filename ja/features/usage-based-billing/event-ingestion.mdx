---
sidebarTitle: イベント取り込み
title: イベント取り込み
description: アプリケーションから使用状況イベントを送信して、顧客の消費を追跡し、請求を行います。
icon: bolt
---

イベントは使用量ベースの請求の基盤です。請求可能なアクションが発生したときにイベントを送信し、メーターがそれらを集計して料金にします。

{/* LOCKED_PATTERN_f67275d778880e9e57a8db8b7973e6d3 */}
例とレスポンスコードを含む完全なAPIドキュメント。
</Card>

## イベント構造

{/* LOCKED_PATTERN_4d7f2e9ce45e7323dd2d626df4a7c5b8 */}
<ParamField body="event_id" type="string" required>
一意の識別子。UUIDを使用するか、顧客ID＋タイムスタンプ＋アクションを組み合わせてください。
</ParamField>

<ParamField body="customer_id" type="string" required>
Dodo Paymentsの顧客ID。有効な既存の顧客である必要があります。
</ParamField>

<ParamField body="event_name" type="string" required>
メーターのイベント名と一致するイベントタイプ（大文字小文字を区別）。例: `api.call`、`image.generated`
</ParamField>
</Accordion>

{/* LOCKED_PATTERN_94ee55474fc7877494948c444fe08c91 */}

<ParamField body="timestamp" type="string">
ISO 8601タイムスタンプ。省略するとサーバー時刻がデフォルトになります。遅延/バッチイベントでは正確な課金のために含めてください。
</ParamField>

<ParamField body="metadata" type="object">
集計やフィルタリング用の追加プロパティ:
- 数値: `bytes`、`tokens`、`duration_ms`
- フィルタ: `endpoint`、`method`、`quality`

```javascript
metadata: {
  endpoint: "/v1/orders",
  method: "POST",
  tokens: 1024
}
```

</ParamField>
</Accordion>

## イベントの送信

<CodeGroup>

```javascript Single Event
await fetch('https://test.dodopayments.com/events/ingest', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${process.env.DODO_API_KEY}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    events: [{
      event_id: "api_call_1234",
      customer_id: "cus_abc123",
      event_name: "api.call",
      metadata: { endpoint: "/v1/orders" }
    }]
  })
});
```

```javascript Batch Events
await fetch('https://test.dodopayments.com/events/ingest', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${process.env.DODO_API_KEY}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    events: [
      { event_id: "call_1", customer_id: "cus_abc", event_name: "api.call" },
      { event_id: "call_2", customer_id: "cus_abc", event_name: "api.call" },
      { event_id: "call_3", customer_id: "cus_abc", event_name: "api.call" }
    ]
  })
});
```

```python Python
import requests

requests.post(
    'https://test.dodopayments.com/events/ingest',
    headers={'Authorization': f'Bearer {api_key}', 'Content-Type': 'application/json'},
    json={'events': [{'event_id': 'call_1', 'customer_id': 'cus_abc', 'event_name': 'api.call'}]}
)
```

```curl cURL
curl -X POST 'https://test.dodopayments.com/events/ingest' \
  -H 'Authorization: Bearer YOUR_API_KEY' \
  -H 'Content-Type: application/json' \
  -d '{"events": [{"event_id": "call_1", "customer_id": "cus_abc", "event_name": "api.call"}]}'
```

</CodeGroup>

<Tip>
パフォーマンス向上のため、1リクエストあたり最大100件のイベントをバッチ処理できます。
</Tip>

## 取り込みブループリント

一般的なユースケースのための既製のイベントパターン。ゼロから構築するのではなく、実績のあるブループリントから始めましょう。

<CardGroup cols={2}>
{/* LOCKED_PATTERN_cb13d34a0150ce3dde4b528bdfa94b98 */}
OpenAI、Anthropic、Groq、GeminiなどのAIトークン使用量を追跡します。
</Card>

{/* LOCKED_PATTERN_a170aa86ba7c211ab83ec57c550e5b7a */}
エンドポイントフィルタリングとレート制限に対応したMeter APIリクエストを測定します。
</Card>

{/* LOCKED_PATTERN_06ce679f6b2cac04dd34930ed580a53c */}
クラウドストレージサービス向けにファイルアップロードとストレージ消費量を追跡します。
</Card>

{/* LOCKED_PATTERN_0f4a45fb9633cc3bd68e5b3cf5632206 */}
ビデオ、オーディオ、リアルタイムデータのストリーミング帯域幅を計測します。
</Card>

{/* LOCKED_PATTERN_157cd69ec190e067606fc563773ef69d */}
サーバーレス関数やコンピュートインスタンスの経過時間に基づいて課金します。
</Card>

{/* LOCKED_PATTERN_2d5389c09ca1dfc5146f8331e378a823 */}
詳細な実装ガイド付きの利用可能なブループリントをすべて確認できます。
</Card>
</CardGroup>

## ベストプラクティス

<AccordionGroup>
{/* LOCKED_PATTERN_c11962fb3ea9e1a82d1994f0860ee77a */}
重複を防ぐために決定論的なIDを使用してください: `${customerId}_${action}_${timestamp}`
</Accordion>

{/* LOCKED_PATTERN_d4b029e15b52ca6ac907535b9dad9ee9 */}
5xxエラー時には指数バックオフでリトライしてください。4xxエラーはリトライしないでください。
</Accordion>

{/* LOCKED_PATTERN_90e261cbcc49667becacc297210d42c3 */}
リアルタイムイベントでは省略してください。遅延/バッチイベントでは正確性のために含めてください。
</Accordion>

{/* LOCKED_PATTERN_fa83e100effc6c87ff1c68c56ea8afa9 */}
成功率を追跡し、失敗したイベントを再試行用にキューイングします。
</Accordion>
</AccordionGroup>

## トラブルシューティング

<AccordionGroup>
{/* LOCKED_PATTERN_fc1788ddf0982a3a2fe9f6e81812c541 */}
- イベント名はメーターと完全一致させてください（大文字小文字を区別）
- 顧客IDが存在することを確認してください
- メーターのフィルタがイベントを除外していないか確認してください
- タイムスタンプが最近のものであることを確認してください
</Accordion>

{/* LOCKED_PATTERN_1af6f154c82eaebbb38a748cef2be54a */}
APIキーが正しいことを確認し、形式を使用してください: `Bearer YOUR_API_KEY`
</Accordion>

{/* LOCKED_PATTERN_bfe0427c6207c44242acb102b2467be5 */}
必要なフィールドがすべて含まれていることを確認してください: `event_id`、`customer_id`、`event_name`
</Accordion>

{/* LOCKED_PATTERN_61cd934926cd73f663eac6935ef56a1b */}
- メタデータキーはメーターの「Over Property」と完全に一致させてください
- 文字列ではなく数値を使用してください: `tokens: 150`（`tokens: "150"`ではありません）
</Accordion>
</AccordionGroup>

## 次のステップ

<CardGroup cols={2}>
{/* LOCKED_PATTERN_7febaa0d6629309525313c7e9c57326c */}
フィルタと集計関数を使ってイベントが課金対象の数量にどのように集計されるかを定義します。
</Card>

{/* LOCKED_PATTERN_6689573160770e9b2834bd7f18ddfe6b */}
LLMトラッキング、APIゲートウェイ、ストレージなどの一般的なユースケース向けの既製ブループリントを利用してください。
</Card>

{/* LOCKED_PATTERN_b84a97fd71b10036af1d667e737cab38 */}
使用量ベースの課金を備えた完全なAI画像ジェネレーターをゼロから構築します。
</Card>

{/* LOCKED_PATTERN_6cf38e9ce4cdee1948462b0af44d1016 */}
すべてのパラメーター、レスポンスコード、およびインタラクティブテストを含む完全なAPIドキュメント。
</Card>
</CardGroup>
