---
title: Dub
description: Dodo Paymentsを通じて顧客が購入を完了したときに、Dubで販売転換イベントを追跡します。
icon: /images/logos/dub.svg
---

## はじめに

[Dub](https://dub.co)は、短いリンクを作成、共有、追跡するのに役立つ強力なリンク管理プラットフォームです。Dodo PaymentsをDubと統合することで、顧客が購入を完了したときに自動的に販売転換イベントを追跡でき、マーケティングキャンペーンや紹介プログラムのROIを測定できます。

顧客が以下のいずれかを行ったときに、Dubに「販売」イベントが記録されます。
- 一回限りの支払いを完了する
- 有料プランに加入する
- 定期購読の支払いを行う

<Info>
この統合を利用するには、リンクでコンバージョントラッキングが有効になっているDubアカウントが必要です。
</Info>

<Tip>
**アフィリエイトプログラム統合**: この統合は、アフィリエイト紹介とコミッションの追跡のために**Dub Partners**ともシームレスに連携します。Dubのコンバージョントラッキングを使用して、アフィリエイトリンクへの売上を帰属させ、パートナーのパフォーマンスを測定します。アフィリエイトプログラムの設定方法については、[アフィリエイト機能ガイド](/features/affiliates) をご覧ください。
</Tip>

## 仕組み

Dub は、ユーザーが Dub のショートリンクをクリックしたときにクッキーに保存される一意のクリック ID (`dub_id`) を使って訪問者を追跡します。売上をリンクに帰属させるには、次のことが必要です。

1. **Dub のクリック ID を取得する**: チェックアウトセッションを作成する際に `dub_id` クッキーから取得します
2. **クリック ID を保存する**: 顧客の外部 ID とともに支払いメタデータに格納します
3. **売上データを Dub に送信する**: 支払いが成功したタイミングで Track API を使用して送信します

これにより、Dubは成功した販売を元のリンククリックに一致させ、完全なコンバージョン帰属を提供します。

## 前提条件

この統合を設定する前に、次のことを確認してください:

1. [Dubアカウント](https://dub.co) とワークスペースを持っていること
2. リンクのコンバージョントラッキングが有効になっていること
3. DubのAPIキー（設定 → APIキーの下にあるDubダッシュボードで入手可能）

## 始め方

<Steps>
{/* LOCKED_PATTERN_61bbf97d09c18ff0f49ccd9c0f216ae2 */}
  Dub のダッシュボードで、販売をトラッキングしたいリンクについてコンバージョントラッキングを有効にします。これにより、顧客が購入を完了したときに Dub が販売イベントを記録できます。
</Step>

  <Info>
  コンバージョントラッキングを有効にする方法については、[Dub documentation](https://dub.co/docs/conversions/quickstart) をご覧ください。
  </Info>
</Step>

  {/* LOCKED_PATTERN_09ec83561863ba8dfb1b09650875a82a */}
  [Dub dashboard](https://app.dub.co) → Settings → API Keys に移動し、`conversions.write` スコープを持つ新しい API キーを作成します。

  <Warning>
  API キーは安全に管理し、クライアント側のコードで公開しないでください。
  </Warning>
</Step>

{/* LOCKED_PATTERN_528142ce64ee996154bcf8fdaffc120b */}
  チェックアウトセッションを作成する際にクッキーから Dub のクリック ID を取得し、支払いメタデータに追加します。
</Step>

{/* LOCKED_PATTERN_6e8a18d6b9667ee12eda72dd5a47db36 */}
  支払い成功時に販売データを Dub の Track API に送信するよう Webhook を構成します。
</Step>

{/* LOCKED_PATTERN_8d2cbcb809e6363cb1f41f0f62f98444 */}
  販売コンバージョンイベントが Dub の分析ダッシュボードに表示され、リンクへの正確な帰属が記録されるようになります。
</Step>
</Steps>

## 実装ガイド

### ステップ1: チェックアウトメタデータにクリックIDと顧客IDを追加する

チェックアウトセッションを作成する際に、クッキーからDubのクリックIDを取得し、顧客の外部IDとともに支払いメタデータに含めます。

<CodeGroup>

```typescript Next.js
import { cookies } from 'next/headers';
import DodoPayments from 'dodopayments';

const client = new DodoPayments();

export async function createCheckout(productId: string, customerId: string) {
  // Capture Dub click ID from cookie
  const dubClickId = cookies().get('dub_id')?.value;

  const payment = await client.payments.create({
    billing: {
      city: 'New York',
      country: 'US',
      state: 'NY',
      street: '123 Main St',
      zipcode: '10001',
    },
    customer: {
      email: 'customer@example.com',
      name: 'John Doe',
    },
    product_id: productId,
    metadata: {
      dub_click_id: dubClickId,           // Store Dub click ID
      dub_external_id: customerId,        // Store your customer's unique ID
    },
  });

  return payment;
}
```

```javascript Express.js
const express = require('express');
const DodoPayments = require('dodopayments').default;
const router = express.Router();

const client = new DodoPayments();

router.post('/create-checkout', async (req, res) => {
  // Capture Dub click ID from cookie
  const dubClickId = req.cookies?.dub_id;

  const payment = await client.payments.create({
    billing: {
      city: 'New York',
      country: 'US',
      state: 'NY',
      street: '123 Main St',
      zipcode: '10001',
    },
    customer: {
      email: req.body.email,
      name: req.body.name,
    },
    product_id: req.body.productId,
    metadata: {
      dub_click_id: dubClickId,           // Store Dub click ID
      dub_external_id: req.body.customerId, // Store your customer's unique ID
    },
  });

  res.json({ payment });
});
```

```python Python (FastAPI)
from fastapi import FastAPI, Request
from dodopayments import DodoPayments

app = FastAPI()
client = DodoPayments()

@app.post("/create-checkout")
async def create_checkout(request: Request):
    # Capture Dub click ID from cookie
    dub_click_id = request.cookies.get("dub_id")

    body = await request.json()

    payment = client.payments.create(
        billing={
            "city": "New York",
            "country": "US",
            "state": "NY",
            "street": "123 Main St",
            "zipcode": "10001",
        },
        customer={
            "email": body["email"],
            "name": body["name"],
        },
        product_id=body["product_id"],
        metadata={
            "dub_click_id": dub_click_id,          # Store Dub click ID
            "dub_external_id": body["customer_id"], # Store your customer's unique ID
        },
    )

    return {"payment": payment}
```

```go Go
package main

import (
    "github.com/dodopayments/dodopayments-go"
)

func createCheckout(dubClickId, customerId, productId string) (*dodopayments.Payment, error) {
    client := dodopayments.NewClient()

    payment, err := client.Payments.Create(context.Background(), dodopayments.PaymentCreateParams{
        Billing: dodopayments.BillingParam{
            City:    "New York",
            Country: "US",
            State:   "NY",
            Street:  "123 Main St",
            Zipcode: "10001",
        },
        Customer: dodopayments.CustomerParam{
            Email: "customer@example.com",
            Name:  "John Doe",
        },
        ProductID: productId,
        Metadata: map[string]string{
            "dub_click_id":   dubClickId,  // Store Dub click ID
            "dub_external_id": customerId, // Store your customer's unique ID
        },
    })

    return payment, err
}
```

</CodeGroup>

### ステップ2: Dubに販売データを送信する

支払いが成功したときに、DubのTrack APIに販売データを送信するためのWebhookエンドポイントを設定します。

<Steps>
{/* LOCKED_PATTERN_c337a250e2e6db475d2eb18e95cb3e6a */}
  Dodo Payments のダッシュボードで **Webhooks → + Add Endpoint** に移動し、統合のドロップダウンを展開します。
  <Frame>
    <img src="/images/integrations/dub/add.png" alt="エンドポイントの追加と統合ドロップダウン" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_99685f3488b80b5b23189beda3a369d0 */}
  **Dub** 統合カードを選択します。
</Step>

{/* LOCKED_PATTERN_b0cbf67cc15e78743c0724f27d5e90ef */}
  構成フィールドに Dub API キーを入力します。
  <Frame>
    <img src="/images/integrations/dub/api-key.png" alt="API キーを追加" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_4ac4616f59ca1d74262754150410c05f */}
  トランスフォーメーションコードを編集して、支払いデータを Dub の Track Sale API 用にフォーマットします。
</Step>

{/* LOCKED_PATTERN_f6e4d766e7306e52042eed3b757944bb */}
  サンプルペイロードでテストし、**Create** をクリックして統合を有効化します。
</Step>
</Steps>

## 変換コードの例

### 基本的な販売追跡

支払いが成功したときに販売を追跡します:

```javascript basic_sale.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    // Only send to Dub if click ID exists in metadata
    if (payment.metadata && payment.metadata.dub_click_id) {
      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount, // Ensure the amount is in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          customer_email: payment.customer.email,
          customer_name: payment.customer.name,
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

### サブスクリプション販売の追跡

初回のサブスクリプションと定期的な支払いの両方を追跡します:

```javascript subscription_sale.js icon="js" expandable
function handler(webhook) {
  const data = webhook.payload.data;

  // Track initial subscription activation
  if (webhook.eventType === "subscription.active") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount, // Amount in cents
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: data.subscription_id,
        eventName: "Subscription Started",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          billing_interval: data.payment_frequency_interval,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  // Track recurring subscription payments
  if (webhook.eventType === "subscription.renewed") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount,
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: `${data.subscription_id}_${Date.now()}`,
        eventName: "Subscription Renewed",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  return webhook;
}
```

### 税金除外での販売追跡

正確な収益追跡のために、税抜き金額のみをDubに送信します:

```javascript sale_without_tax.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Calculate pre-tax amount (total minus tax)
      const preTaxAmount = payment.total_amount - (payment.tax || 0);

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: preTaxAmount, // Pre-tax amount in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          total_amount: payment.total_amount,
          tax_amount: payment.tax || 0,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

### カスタムイベント名での販売追跡

異なるタイプの販売を分類するためにカスタムイベント名を使用します:

```javascript custom_events.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Determine event name based on payment type
      let eventName = "Purchase";
      if (payment.subscription_id) {
        eventName = "Subscription Purchase";
      } else if (payment.metadata && payment.metadata.is_upgrade) {
        eventName = "Plan Upgrade";
      }

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount,
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        eventName: eventName,
        metadata: {
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

## 代替: クライアントサイドの実装

Webhookを使用する代わりに、サーバーから販売を追跡したい場合は、成功した支払いの後にDubのTrack APIを直接呼び出すことができます:

<CodeGroup>

```typescript Next.js (Server Action)
'use server';

import { Dub } from 'dub';

const dub = new Dub();

export async function trackSale(
  paymentId: string,
  clickId: string,
  customerId: string,
  amount: number,
  currency: string
) {
  await dub.track.sale({
    clickId: clickId,
    externalId: customerId,
    amount: amount,
    currency: currency,
    paymentProcessor: 'dodo',
    invoiceId: paymentId,
  });
}
```

```javascript Node.js
const { Dub } = require('dub');

const dub = new Dub();

async function trackSale(payment) {
  if (payment.metadata?.dub_click_id) {
    await dub.track.sale({
      clickId: payment.metadata.dub_click_id,
      externalId: payment.metadata.dub_external_id,
      amount: payment.total_amount,
      currency: payment.currency,
      paymentProcessor: 'dodo',
      invoiceId: payment.payment_id,
    });
  }
}
```

</CodeGroup>

## ベストプラクティス

<Tip>
**クリック ID を早めに取得する**: チェックアウトフローの中でできるだけ早く Dub のクリック ID を保存して、ユーザーが離脱して戻ってきた場合でも正確に帰属できるようにします。
</Tip>

- **メタデータには常にクリック ID を含める**: クリック ID がなければ Dub はリンクへの収益を帰属できません
- **外部 ID を一貫して使用する**: システムで使用している同じ顧客 ID を渡して、顧客レベルの分析を正確に行います
- **オーガニックトラフィックを適切に処理する**: クリック ID がない場合は `webhook.cancel = true` を設定し、不要な API 呼び出しを避けます
- **サンプル支払いでテストする**: 本番運用前に統合が正しく機能することを確認します
- **Dub ダッシュボードを監視する**: 販売が適切な帰属で表示されているか確認します

## 重要な注意事項

<Note>
- **金額の形式**: Dub は金額をセント単位（例: $10.00 = 1000）で期待します
- **通貨**: ISO 4217 通貨コード（USD、EUR、GBP など）を使用します
- **無料トライアル**: $0 の支払いは販売として追跡されません
- **返金**: 正確な収益報告のために必要に応じて返金を別途追跡することを検討してください
</Note>

## トラブルシューティング

<AccordionGroup>
{/* LOCKED_PATTERN_366b5018c3ba3b70a06fdb2ce6f29df0 */}
- Dub API キーが正しく、`conversions.write` スコープを持っていることを確認します
- `dub_click_id` が取得されて支払いメタデータに保存されていることを確認します
- Webhook の変換がペイロードを正しくフォーマットしていることを確認します
- Webhook が `payment.succeeded` イベントでトリガーされていることを確認します
- Dub リンクに対してコンバージョントラッキングが有効になっていることを確認します
</Accordion>

{/* LOCKED_PATTERN_a32709eeb5a64477cb4e009bd30bfdd2 */}
- ユーザーが Dub ショートリンクをチェックアウト前にクリックしていることを確認します
- `dub_id` クッキーが自社ドメインで正しく設定されていることを確認します
- チェックアウト作成時と支払い完了時でクリック ID が一致していることを確認します
- チェックアウトセッション作成前にクリック ID を取得していることを確認します
</Accordion>

{/* LOCKED_PATTERN_14f1ac2eb6bb156b1fd74792fba48a19 */}
- JSON 構造が Dub の Track Sale API 形式と一致していることを検証します
- 必要なすべてのフィールド (`clickId`, `externalId`, `amount`) が存在していることを確認します
- 金額がセント単位（整数、小数点ではない）であることを確認します
- API エンドポイント URL が正しいことを確認します: `https://api.dub.co/track/sale`
- サンプル webhook ペイロードで変換をテストします
</Accordion>

{/* LOCKED_PATTERN_5f0ac54eda91f1b5f2683659cb5c19a5 */}
- `payment.succeeded` イベント時のみに追跡して、`payment.processing` ではないことを確認します
- 売上ごとに一意の `invoiceId` 値を使用します
- サブスクリプションの場合は更新時の重複を防ぐためにタイムスタンプや請求期間を追加します
</Accordion>
</AccordionGroup>

## 追加リソース

<CardGroup cols={2}>
{/* LOCKED_PATTERN_b26c5b8ead1d8d61de894caed271203c */}
  Dub のコンバージョントラッキングと分析機能の詳細をご覧ください。
</Card>

<Card title="Dub Track Sale API" icon="code" href="https://dub.co/docs/api-reference/track/track-sale">
  Dub の Track Sale エンドポイントの完全な API リファレンスを確認します。
</Card>

{/* LOCKED_PATTERN_8b5d6bd2d637fa74100d10bc01898c35 */}
  Dub ダッシュボードにアクセスして、コンバージョン分析と帰属データを表示します。
</Card>

{/* LOCKED_PATTERN_08edaa2b4bf5c1db98d7c617dc2cf851 */}
  利用可能な Dodo Payments の webhook イベントについて確認します。
</Card>
</CardGroup>

<Info>
ご不明な点があれば、統合のサポートについて [support@dodopayments.com](mailto:support@dodopayments.com) まで Dodo Payments サポートにお問い合わせください。
</Info>
