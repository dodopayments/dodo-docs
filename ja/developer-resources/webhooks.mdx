---
sidebarTitle: 概要
title: Webhooks
description: "<Frame>でイベントが発生した際に、リアルタイム通知を受け取ります。ワークフローを自動化し、イベントを即座に配信してシステムの同期を維持できます。"
icon: box
keywords:
  - Dodo Payments
  - 開発者ガイド
  - webhooks
---

<Frame>
  <img src="/images/cover-images/Webhooks.webp" alt="Webhook Cover Image" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

Webhooksは、Dodo Paymentsアカウントで特定のイベントが発生した際に、リアルタイム通知を提供します。Webhooksを使用してワークフローの自動化、データベースの更新、通知の送信、システムの同期を行えます。

<Info>
当社のWebhook実装は[Standard Webhooks](https://standardwebhooks.com/)仕様に準拠しており、業界のベストプラクティスおよび既存のWebhookライブラリとの互換性を確保しています。
</Info>

## 主な機能

<CardGroup cols={2}>
  <Card title="Real-time Delivery" icon="bolt">
    イベント発生時に即座に通知を受信
  </Card>
  <Card title="Secure by Default" icon="shield-check">
    HMAC SHA256署名の検証に対応
  </Card>
  <Card title="Automatic Retries" icon="rotate">
    指数バックオフによる組み込みの再試行ロジック
  </Card>
  <Card title="Event Filtering" icon="filter">
    必要なイベントのみを購読
  </Card>
</CardGroup>

## はじめに

<Info>
Dodo Payments の webhooks portal は、ネイティブなダッシュボードエクスペリエンスとして再構築されました。既存のエンドポイント、署名シークレット、signature verification、イベント名、webhook payload は変更されていません。統合作業は必要ありません。
</Info>

<Note>
**各機能の場所**

- **Developer → Webhooks の下** — **Endpoints**、**Event catalog**、**Logs**、**Activity**、**Settings** タブ。
- **個別のエンドポイント上** — 配信統計、署名シークレット、**Replay history** を含む **Overview** タブ、さらに **Testing**、**Advanced** タブと一括 replay アクション。
- **メッセージ上** — **Logs** タブから開くと、エンドポイントを開かずに各配信試行を個別に replay できます。
</Note>

<Steps>
<Step title="Access Webhook Settings">
    Dodo Payments Dashboard に移動し、**Developer → Webhooks** を開きます。
</Step>

<Step title="Create Webhook Endpoint">
    **Add endpoint** をクリックして、エンドポイント作成用のサイドシートを開きます。
</Step>

<Step title="Enter Endpoint URL or Choose Integration">
    webhook event を受信する URL を入力するか、integration connector を選択して、イベントをサードパーティサービス（Slack、Discord、Zapier、Resend など）へルーティングします。
</Step>

<Step title="Select Events to Receive">
    エンドポイントが listen する特定のイベントを選択します。イベントは、リソースごとにグループ化された検索可能なツリーに整理されています。個別のイベントを選択することも、親リソースを選択して関連するすべてのイベントを受信することもできます。
    
    <Tip>
    選択したイベントだけがエンドポイントへの webhooks をトリガーするため、不要なトラフィックや処理を避けられます。
    </Tip>
</Step>

<Step title="Create Endpoint">
    **Create endpoint** をクリックして設定を保存します。
</Step>

<Step title="Get Secret Key">
    webhook signing secret は、エンドポイントの **Overview** タブに表示されます。受信した webhooks の真正性を検証するために使用します。
    
    <Warning>
    webhook secret key は安全に管理し、クライアント側のコードや公開リポジトリに決して公開しないでください。
    </Warning>
</Step>

<Step title="Rotate Secret (Optional)">
    必要に応じて、セキュリティを強化するために webhook secret をローテーションできます。**Overview** タブで secret の横にある **Rotate secret** をクリックします。
    
    <Warning>
    secret をローテーションすると、現在の secret は失効し、新しい secret に置き換えられます。古い secret はその後 24 時間のみ有効です。それ以降、古い secret で検証しようとすると失敗します。
    </Warning>
    
    <Info>
    定期的に、または現在の secret が漏えいした疑いがある場合は直ちに secret rotation を実行してください。
    </Info>
</Step>
</Steps>

## Integration Connectors

独自の webhook receiver を構築する代わりに、integration connector を使用して webhook event をサードパーティサービスへ直接ルーティングできます。これにより、一般的なプラットフォーム向けにカスタム webhook handler を記述・保守する必要がなくなります。

### Connector の仕組み

connector は、Dodo Payments event を送信先が想定する形式に変換する処理を実行します。入力する詳細は送信先によって異なります。

| Connector type | What you provide | Destinations |
|----------------|------------------|--------------|
| **Incoming webhook URL** | ベンダー独自のダッシュボードで作成した webhook URL。API key は不要です。 | [Slack](/integrations/slack)、[Discord](/integrations/discord)、[Microsoft Teams](/integrations/microsoft-teams)、[Zapier](/integrations/zapier)、[Windmill](/integrations/windmill) |
| **Vendor API** | API key。送信先 URL は connector template から取得されます。 | [Resend](/integrations/resend)、[SendGrid](/integrations/sendgrid)、[Segment](/integrations/segment)、[Customer.io](/integrations/customer-io)、[HubSpot](/integrations/hubspot)、[Close CRM](/integrations/close-crm)、[Loops](/integrations/loops) |

ダッシュボードの connector picker には、現在ビジネスで利用できるすべての connector が表示されます。そのため、上の表は網羅的な一覧ではなく、手順付きのセットアップ説明がある送信先として扱ってください。各送信先で event を受信した後にできることについては、[External Integrations](/integrations/introduction) を参照してください。

### Connector の設定

エンドポイントの作成または編集時に connector を選択すると、サイドシートに送信先に合わせたセットアップ手順が表示されます。たとえば、Slack で incoming webhook URL を作成する方法や、Resend API key の確認場所などです。保存する前に connector transformation test を実行し、event が送信先向けに正しく変換されることを確認してください。

<Tip>
コードを書かずにサポート対象の送信先へ接続するには connector を使用します。カスタムロジックが必要な場合は、代わりに [transformation](#transformations) を使用した標準エンドポイントを使ってください。
</Tip>

## Subscribed Events の設定

各 webhook endpoint が受信する特定のイベントを設定できます。

<Steps>
<Step title="Navigate to Webhook Endpoints">
    Dodo Payments Dashboard に移動し、**Developer → Webhooks** を開きます。
</Step>

<Step title="Select Your Endpoint">
    設定する webhook endpoint をクリックします。
</Step>

<Step title="Open Event Configuration">
    **Edit** をクリックして、エンドポイント設定用のサイドシートを開きます。
</Step>

<Step title="Browse Event Types">
    イベントタイプセレクターには、利用可能なすべての webhook event がリソースごとにグループ化された検索可能なツリーで表示されます（例: `payment`、`subscription`、`dispute`）。検索バーを使用すると、名前やキーワードから特定のイベントをすばやく見つけられます。
</Step>

<Step title="Select Events">
    受信するイベントの横にあるチェックボックスをオンにします。次の操作ができます。
    - 個別のイベントを選択する（例: `payment.succeeded`、`payment.failed`）
    - 親リソースを選択して関連するすべてのイベントを受信する
    - 必要に応じて特定のイベントを組み合わせる
</Step>

<Step title="Save Configuration">
    **Save** をクリックして変更を適用するか、**Cancel** をクリックして変更を破棄します。
</Step>
</Steps>

<Warning>
すべてのイベントの選択を解除すると、webhook endpoint は通知を一切受信しなくなります。アプリケーションの正常な動作に必要なイベントを少なくとも選択してください。
</Warning>

## Event Catalog

**Developer → Webhooks** に移動し、**Event catalog** タブを開きます。ここには Dodo Payments が送信できるすべてのイベントタイプが一覧表示されるため、エンドポイントを購読する前に利用可能なイベントを確認できます。イベントを選択すると、その schema とサンプル payload を表示できます。読み取る予定のフィールドの形式を確認する最も簡単な方法です。

<Card title="Webhook Events Guide" icon="list" href="/developer-resources/webhooks/intents/webhook-events-guide">
同じイベントを、リソースごとにグループ化されたリファレンスドキュメントとして参照できます。
</Card>

## Webhook Delivery

### タイムアウト

webhooks には、接続操作と読み取り操作の両方に **15 秒のタイムアウトウィンドウ** があります。タイムアウトを避けるため、エンドポイントがすぐに応答するようにしてください。

<Tip>
webhooks は非同期で処理し、まず `200` status code を直ちに返して受信を確認した後、実際の処理をバックグラウンドで行ってください。
</Tip>

### 自動リトライ

webhook delivery が失敗すると、Dodo Payments は指数バックオフで自動的にリトライし、システムに過剰な負荷がかからないようにします。

| Attempt | Delay | Description |
|---------|------------------------------|-------------|
| 1 | Immediately | 最初のリトライは直ちに実行されます |
| 2 | 5 seconds | 短い遅延の後に 2 回目の試行を実行します |
| 3 | 5 minutes | バックオフを増加させて 3 回目の試行を実行します |
| 4 | 30 minutes | バックオフを継続して 4 回目の試行を実行します |
| 5 | 2 hours | より長い遅延の後に 5 回目の試行を実行します |
| 6 | 5 hours | さらに長い遅延の後に 6 回目の試行を実行します |
| 7 | 10 hours | 最大遅延で 7 回目の試行を実行します |
| 8 | 10 hours | **最後の試行** - 失敗した場合、webhook は失敗としてマークされます |

<Info>
webhook event ごとのリトライは **最大 8 回** です。たとえば、webhook が成功するまでに 3 回失敗した場合、最初の試行からの合計配信時間は約 35 分 5 秒です。
</Info>

<Tip>
Dodo Payments dashboard を使用すると、個別のメッセージを手動でリトライしたり、失敗したすべてのメッセージを一括でいつでも復旧したりできます。
</Tip>

### Idempotency

各 webhook event には一意の `webhook-id` header が含まれます。この識別子を使用して idempotency を実装し、重複処理を防止してください。

```javascript
// Example: Storing webhook IDs to prevent duplicate processing
const processedWebhooks = new Set();

app.post('/webhook', (req, res) => {
  const webhookId = req.headers['webhook-id'];
  
  if (processedWebhooks.has(webhookId)) {
    return res.status(200).json({ received: true });
  }
  
  processedWebhooks.add(webhookId);
  // Process the webhook...
});
```

<Warning>
必ず idempotency checks を実装してください。リトライにより、同じ event を複数回受信する可能性があります。
</Warning>

### イベント順序

リトライやネットワーク状況により、webhook event が順不同で届く場合があります。どの順序のイベントにも対応できるようにシステムを設計してください。

<Info>
webhook event が最初に発生した時点に関係なく、**配信時点での最新の payload** を受信します。
</Info>

## Webhooks のセキュリティ保護

webhooks のセキュリティを確保するため、常に payload を検証し、HTTPS を使用してください。

### 署名の検証

各 webhook request には `webhook-signature` header が含まれます。これは webhook payload と timestamp の HMAC SHA256 signature で、secret key によって署名されています。

#### SDK verification（推奨）

すべての公式 SDK には、受信した webhooks を安全に検証・解析するための built-in helper が含まれています。利用できる方法は 2 つです。

* `unwrap()`: webhook secret key を使用して signature を検証します
* `unsafe_unwrap()`: verification なしで payload を解析します

<Tip>
  Dodo Payments client の初期化時に、`DODO_PAYMENTS_WEBHOOK_KEY` を使用して webhook secret を指定します。
</Tip>

<CodeGroup>

```typescript TypeScript/Node.js
import DodoPayments from 'dodopayments';
import express from 'express';

const app = express();
app.use(express.raw({ type: 'application/json' }));

const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: process.env.DODO_PAYMENTS_ENVIRONMENT,
  webhookKey: process.env.DODO_PAYMENTS_WEBHOOK_KEY,
});

app.post('/webhook', async (req, res) => {
  try {
    const unwrapped = client.webhooks.unwrap(req.body.toString(), {
      headers: {
        'webhook-id': req.headers['webhook-id'] as string,
        'webhook-signature': req.headers['webhook-signature'] as string,
        'webhook-timestamp': req.headers['webhook-timestamp'] as string,
      },
    });
    res.json({ received: true });
  } catch (error) {
    res.status(401).json({ error: 'Invalid signature' });
  }
});
```

```python Python
from fastapi import FastAPI, Request, HTTPException
from dodopayments import DodoPayments
import os

app = FastAPI()
client = DodoPayments(
    bearer_token=os.getenv("DODO_PAYMENTS_API_KEY"),
    environment=os.getenv("DODO_PAYMENTS_ENVIRONMENT"),
    webhook_key=os.getenv("DODO_PAYMENTS_WEBHOOK_KEY"),
)

@app.post("/webhook")
async def handle_webhook(request: Request):
    try:
        unwrapped = client.webhooks.unwrap(
            await request.body(),
            headers={
                "webhook-id": request.headers.get("webhook-id", ""),
                "webhook-signature": request.headers.get("webhook-signature", ""),
                "webhook-timestamp": request.headers.get("webhook-timestamp", ""),
            },
        )
        return {"received": True}
    except Exception:
        raise HTTPException(status_code=401, detail="Invalid signature")
```

```go Go
import (
	"context"
	"io"
	"net/http"
	"os"
	"github.com/dodopayments/dodopayments-go"
	"github.com/dodopayments/dodopayments-go/option"
)

func webhookHandler(w http.ResponseWriter, r *http.Request) {
	client := dodopayments.NewClient(
		option.WithBearerToken(os.Getenv("DODO_PAYMENTS_API_KEY")),
		option.WithEnvironment(os.Getenv("DODO_PAYMENTS_ENVIRONMENT")),
		option.WithWebhookKey(os.Getenv("DODO_PAYMENTS_WEBHOOK_KEY")),
	)
	rawBody, _ := io.ReadAll(r.Body)
	if _, err := client.Webhooks.Unwrap(context.Background(), rawBody, map[string]string{
		"webhook-id":       r.Header.Get("webhook-id"),
		"webhook-signature": r.Header.Get("webhook-signature"),
		"webhook-timestamp": r.Header.Get("webhook-timestamp"),
	}); err != nil {
		http.Error(w, "Invalid signature", http.StatusUnauthorized)
		return
	}
	w.WriteHeader(http.StatusOK)
}
```

</CodeGroup>

#### Manual verification（代替方法）

SDK を使用していない場合は、Standard Webhooks spec に従って自分で signature を検証できます。

1. `webhook-id`、`webhook-timestamp`、正確な raw stringified `payload` をピリオド（`.`）で区切って連結し、署名対象メッセージを作成します。
2. Dashboard の webhook secret key を使用して、その文字列の HMAC SHA256 を計算します。
3. 計算した signature と `webhook-signature` header を比較します。一致すれば webhook は authentic です。

<Info>
  Standard Webhooks specification に従っています。signature の検証には、[https://github.com/standard-webhooks/standard-webhooks/tree/main/libraries](https://github.com/standard-webhooks/standard-webhooks/tree/main/libraries) の libraries を使用できます。event payload formats については、[Webhook Payload](/developer-resources/webhooks/intents/payment) を参照してください。
</Info>

### Webhooks への応答

* webhook handler は、event の受信を確認するために `2xx status code` を返す必要があります。
* その他の応答はすべて失敗として扱われ、webhook はリトライされます。

### ベストプラクティス

<AccordionGroup>
<Accordion title="Use HTTPS endpoints only" icon="lock">
  webhook endpoint には必ず HTTPS URL を使用してください。HTTP endpoint は man-in-the-middle attack に対して脆弱で、webhook data が露出します。
</Accordion>

<Accordion title="Respond immediately" icon="bolt">
  webhook の受信時に `200` status code を直ちに返してください。タイムアウトを避けるため、event は非同期で処理します。
  

  ```javascript
  app.post('/webhook', async (req, res) => {
    // Acknowledge receipt immediately
    res.status(200).json({ received: true });
    
    // Process asynchronously
    processWebhookAsync(req.body).catch(console.error);
  });
  ```

</Accordion>

<Accordion title="Handle duplicate events" icon="rotate">
  `webhook-id` header を使用して idempotency を実装し、同じ event を副作用なく安全に複数回処理できるようにします。
</Accordion>

<Accordion title="Secure your webhook secret" icon="key">
  webhook secret は environment variables または secrets manager を使用して安全に保管してください。secret を version control に commit しないでください。
</Accordion>
</AccordionGroup>

## Webhook Payload Structure

webhook payload structure を理解すると、event を正しく解析・処理できます。

### Request Format

```http
POST /your-webhook-url
Content-Type: application/json
```

### Headers

<ParamField header="webhook-id" type="string" required>
この webhook event の一意の識別子です。idempotency checks に使用します。
</ParamField>

<ParamField header="webhook-signature" type="string" required>
webhook の真正性を検証するための HMAC SHA256 signature です。
</ParamField>

<ParamField header="webhook-timestamp" type="string" required>
webhook の送信時刻を示す Unix timestamp（秒）です。
</ParamField>

### Request Body

<ResponseField name="business_id" type="string" required>
Dodo Payments business の識別子です。
</ResponseField>

<ResponseField name="type" type="string" required>
この webhook をトリガーしたイベントタイプです（例: `payment.succeeded`、`subscription.active`）。
</ResponseField>

<ResponseField name="timestamp" type="string" required>
event が発生した時刻を ISO 8601 形式で示します。
</ResponseField>

<ResponseField name="data" type="object" required>
event に関する詳細情報を含む、イベント固有の payload です。

<Expandable title="Data object properties">
  <ResponseField name="payload_type" type="string">
  リソースのタイプです。次のいずれかです: `Payment`、`Subscription`、`Refund`、`Dispute`、`LicenseKey`、`CreditLedgerEntry`、`CreditBalanceLow`、`AbandonedCheckout`、`DunningAttempt`、`EntitlementGrant`、または `Payout`。
  </ResponseField>
  
  追加フィールドはイベントタイプによって異なります。完全な schema については、イベント固有のドキュメントを参照してください。
</Expandable>
</ResponseField>

### Payload の例

```json
{
  "business_id": "string",
  "type": "payment.succeeded | payment.failed |...",
  "timestamp": "2024-01-01T12:00:00Z",
  "data": {
    "payload_type": "Payment | Subscription | Refund | Dispute | LicenseKey | CreditLedgerEntry | CreditBalanceLow | AbandonedCheckout | DunningAttempt | EntitlementGrant | Payout",
    // ... event-specific fields (see below)
  }
}
```

<CardGroup cols={2}>
  <Card title="Event Types" icon="list" href="/developer-resources/webhooks/intents/webhook-events-guide">
    利用可能なすべての webhook event type を参照
  </Card>
  <Card title="Event Payloads" icon="code" href="/developer-resources/webhooks/intents/payment">
    各イベントの詳細な payload schema を表示
  </Card>
  <Card title="Handle Payment Failures" icon="screwdriver-wrench" href="/developer-resources/handle-payment-failures">
    `payment.failed` に対応し、declined payments を復旧
  </Card>
</CardGroup>

## Webhooks のテスト

本番稼働前にエンドポイントが正しく動作することを確認するため、Dodo Payments dashboard から webhook integration を直接テストできます。

<Steps>
<Step title="Navigate to Webhooks">
    Dodo Payments Dashboard に移動し、**Developer → Webhooks** を開きます。
</Step>

<Step title="Select Your Endpoint">
    webhook endpoint をクリックして詳細ページを開きます。
</Step>

<Step title="Open Testing Tab">
    **Testing** タブをクリックして webhook testing interface を開きます。
</Step>
</Steps>

### Example Event の送信

**Testing** タブはこのエンドポイントにサンプル payload を dispatch し、receiver を検証できるようにします。

<Steps>
<Step title="Select Event Type">
    **Select an event type** を使用してテストする event を選択します。例: `payment.succeeded` または `payment.failed`。
</Step>

<Step title="Send Example">
    **Send example** をクリックします。サンプル payload は実際の event と同じ方法で署名され、実際の event と同じように endpoint URL へ配信されます。
    
    <Warning>
    Testing タブから送信された失敗メッセージはリトライされません。receiver の検証に使用し、リトライスケジュールのテストには使用しないでください。
    </Warning>
</Step>

<Step title="Check Your Endpoint">
    このタブには **Last example sent** が送信された時刻が記録されます。event が到着したこと、signature verification が成功したこと、`2xx` status code を返したことを確認してください。
</Step>
</Steps>

### 実装例

webhook verification と handling を示す、完全な Express.js 実装例を次に示します。

<CodeGroup>

```typescript Express.js Webhook Handler
import { Webhook } from "standardwebhooks";
import express from "express";

const app = express();
app.use(express.json());

const webhook = new Webhook(process.env.DODO_WEBHOOK_SECRET);

app.post('/webhook/dodo-payments', async (req, res) => {
  try {
    // Extract webhook headers
    const webhookHeaders = {
      "webhook-id": req.headers["webhook-id"] as string,
      "webhook-signature": req.headers["webhook-signature"] as string,
      "webhook-timestamp": req.headers["webhook-timestamp"] as string,
    };

    // Verify the webhook signature
    const payload = JSON.stringify(req.body);
    await webhook.verify(payload, webhookHeaders);
    
    // Acknowledge receipt immediately
    res.status(200).json({ received: true });
    
    // Process webhook asynchronously
    processWebhookAsync(req.body).catch(console.error);
    
  } catch (error) {
    console.error('Webhook verification failed:', error);
    res.status(400).json({ error: 'Invalid signature' });
  }
});

async function processWebhookAsync(data: any) {
  // Handle the webhook event based on type
  switch (data.type) {
    case 'payment.succeeded':
      await handlePaymentSucceeded(data);
      break;
    case 'subscription.active':
      await handleSubscriptionActive(data);
      break;
    // Add more event handlers...
  }
}
```

```javascript Testing Webhooks Locally
const { Webhook } = require("standardwebhooks");
const axios = require("axios");
const crypto = require("crypto");

// Generate test webhook
const timestamp = new Date();
const webhookId = crypto.randomUUID();
const payload = {
  business_id: "biz_test123",
  type: "payment.succeeded",
  timestamp: timestamp.toISOString(),
  data: {
    payload_type: "Payment",
    payment_id: "pay_test456",
    amount: 2999
  }
};

// Sign the payload
const webhookSecret = process.env.DODO_WEBHOOK_SECRET;
const webhookInstance = new Webhook(webhookSecret);
const payloadString = JSON.stringify(payload);
const signature = webhookInstance.sign(webhookId, timestamp, payloadString);

// Send test webhook
const response = await axios.post('http://localhost:3000/webhook/dodo-payments', payload, {
  headers: {
    "webhook-id": webhookId,
    "webhook-timestamp": Math.floor(timestamp.getTime() / 1000),
    "webhook-signature": signature,
    "Content-Type": "application/json"
  }
});

console.log("Test webhook sent successfully:", response.data);
```

</CodeGroup>

<Tip>
production event を処理する前に、dashboard testing interface を使用して webhook handler を十分にテストしてください。これにより、問題を早期に特定して修正できます。
</Tip>

## CLI を使用した Webhooks のテスト

[Dodo Payments CLI](/developer-resources/sdks/cli) には、ターミナルを離れることなく local development 中に webhooks をテストできる 2 つの command があります。

### Live Webhooks をローカルで listen する

test mode account から実際の webhook event を local development server へリアルタイムで forward します。

```bash
dodo wh listen
```

CLI は Dodo Payments への WebSocket connection を開き、すべての webhook event を local endpoint（例: `http://localhost:3000/webhook`）へ forward します。検証テスト用に signature headers を含むすべての headers が保持されます。

<Info>
listener は **test mode** API keys でのみ動作します。この command を使用する前に `dodo login` を実行し、Test Mode を選択してください。
</Info>

### Mock Webhook Events のトリガー

実際の transaction を作成せずに、任意の endpoint へ mock webhook payload を送信します。

```bash
dodo wh trigger
```

この interactive tool では event type を選択し、現実的な mock payload を endpoint に送信できます。ループするため、1 回の session で複数の event をテストできます。

trigger command は Dodo Payments が配信する全 46 種類の event type に対応しています。subscription、payment、refund、dispute、license key、payout、credit、abandoned checkout、dunning、entitlement grant の各 family が含まれます。正確な一覧については、[Supported Webhook Events](/developer-resources/sdks/cli#supported-webhook-events) を参照してください。

<Warning>
`dodo wh trigger` の mock webhook payload には署名がありません。テスト中の webhook handler では `unwrap()` ではなく `unsafe_unwrap()` を使用してください。
</Warning>

<Card title="CLI Webhook Testing Docs" icon="terminal" href="/developer-resources/sdks/cli#webhooks">
  CLI webhook testing の完全なドキュメントを参照
</Card>

## Advanced Settings

**Advanced** タブには、webhook endpoint の動作を細かく調整するための追加設定オプションがあります。

### Rate Limiting（Throttling）

システムに過剰な負荷がかからないよう、webhook event を endpoint へ配信する rate を制御します。

<Steps>
<Step title="Open Advanced Tab">
    endpoint details page で **Advanced** タブをクリックします。
</Step>

<Step title="Configure Rate Limit">
    「Rate Limit (throttling)」セクションで **Edit** をクリックし、rate limit settings を変更します。
    
    <Info>
    デフォルトでは webhooks に rate limit は適用されず、event は発生するとすぐに配信されます。
    </Info>
</Step>

<Step title="Set Your Limit">
    希望する rate limit を設定し、webhook の配信頻度を制御してシステムの過負荷を防ぎます。
</Step>
</Steps>

<Tip>
webhook handler に event の処理時間が必要な場合や、複数の event をまとめて処理したい場合に rate limiting を使用します。
</Tip>

### Custom Headers

endpoint に送信するすべての webhook request に custom HTTP headers を追加します。認証、ルーティング、メタデータの追加に便利です。

<Steps>
<Step title="Add Headers">
    「Custom Headers」セクションで、各 custom header の **Key** と **Value** を入力します。
</Step>

<Step title="Add Multiple Headers">
    **+** ボタンをクリックして、必要に応じて custom headers を追加します。
</Step>
</Steps>

custom headers は、この endpoint へのすべての webhook request に含まれます。

### Transformations

Transformations を使用すると、webhook の payload を変更し、必要に応じて別の URL へ redirect できます。この強力な機能により、次のことが可能になります。

- 処理前に payload structure を変更する
- 内容に基づいて webhooks を異なる endpoint へ route する
- payload の field を追加または削除する
- data formats を変換する

<Steps>
<Step title="Enable Transformations">
    **Enabled** switch を切り替えて transformation feature を有効にします。
</Step>

<Step title="Configure Transformation">
    **Edit transformation** をクリックし、JavaScript を使用して transformation rules を定義します。
</Step>

<Step title="Test Transformation">
    transformation test interface を使用し、本番稼働前に transformation が正しく動作することを確認します。
</Step>
</Steps>

<Warning>
Transformations は webhook delivery performance に影響する場合があります。十分にテストし、transformation logic はシンプルかつ効率的に保ってください。
</Warning>

<Tip>
Transformations は特に次の用途に便利です。
- 異なる data formats 間で変換する
- 特定の criteria に基づいて event を filter する
- payload に computed fields を追加する
- event を異なる microservices へ route する
</Tip>

## Webhook Logs の監視

**Logs** タブでは webhook delivery status を包括的に確認でき、webhook event を効果的に監視、debug、管理できます。

<Steps>
<Step title="Navigate to Logs Tab">
    **Developer → Webhooks** に移動し、**Logs** タブを開きます。
</Step>

<Step title="Browse Delivery History">
    Event type、Message ID、Event ID、Sent at、Attempted at、Response code、Duration の列を含む、すべての webhook delivery attempts の table を確認できます。
</Step>

<Step title="Search and Filter">
    search bar で ID または event type から特定の message を検索します。status（Succeeded、Failed、Pending など）で filter し、調査が必要な event に絞り込めます。
</Step>

<Step title="View Message Details">
    message をクリックして message detail page を開くと、次の情報が表示されます。
    - 完全な webhook payload
    - response code と duration を含むすべての delivery attempts
    - 各 attempt の timestamp
    - endpoint からの error messages

    各 attempt には **Replay** action があるため、ページを離れずにその message だけを再配信できます。
</Step>
</Steps>

## Activity Monitoring

**Developer → Webhooks** に移動し、**Activity** タブを開くと、各 endpoint の delivery performance を確認できます。

**Delivery activity** には、期間に応じて **Attempts per 5 minutes**、**Attempts per hour**、**Attempts per day** 単位で分類された、時間経過に伴う attempts が表示されます。各 bar は結果別に分割され、segment にカーソルを合わせると status、attempts 数、合計に占める割合が表示されます。endpoint の **Overview** タブにある **Delivery stats (last 24h)** には、過去 1 日分の同じ情報がまとめられます。

<Tip>
**Endpoints** タブの **Error rate (24h)** 列を見ると、endpoint を開く前に、対応が必要な endpoint をすぐに確認できます。
</Tip>

## メッセージの Replay と Recovery

message を再配信する方法は、必要な件数によって異なります。

- **1 件の message** — **Logs** タブから開き、attempt の **Replay** action を使用します。endpoint を開く必要はありません。
- **複数の message** — bulk mode は一度に 1 つの endpoint に対して実行されるため、endpoint を開きます。

### 一括 Replay

**Developer → Webhooks** から endpoint を開きます。3 つの mode が利用でき、それぞれその endpoint のみに対して実行されます。設定する range は mode によって異なります。

| Mode | What it does | What you set |
|------|--------------|--------------|
| **Recover failed messages** | この endpoint への配信に失敗したすべての message を replay します。 | 開始地点: **8 hours ago**、**Yesterday**、**3 days ago**、**Last week**、**2 weeks ago** のいずれか |
| **Replay missing messages** | この endpoint に一度も dispatch されなかった message を replay します。たとえば、新しい event type を購読した後などです。 | 同じ開始地点 |
| **Bulk replay messages** | 選択した filter に一致する message を replay します。すでに正常配信された message も含まれます。 | **Since** と **Until** の範囲（デフォルトは過去 2 週間）、さらに任意の event types、channel、tag |

<Steps>
<Step title="Open More Actions">
    endpoint で **More actions** を開き、上記 3 つの mode のいずれかを選択します。
</Step>

<Step title="Set the Range">
    table に記載されている、その mode に必要な range を入力します。
</Step>

<Step title="Start the Run">
    選択した mode に応じて **Recover** または **Replay** をクリックします。
</Step>
</Steps>

各 run は endpoint の **Overview** タブにある **Replay history** の下に表示されます。mode、time range、status、再送された message 数を確認できます。

## Email Alerts

endpoint への webhook delivery が失敗したときに email notification を受け取り、問題が backlog になる前に対処できます。

<Steps>
<Step title="Navigate to Settings Tab">
  **Developer → Webhooks** に移動し、**Settings** タブを開きます。
</Step>

<Step title="Find Email Alerting">
  **Email alerting** card を見つけます。
</Step>

<Step title="Configure Email Addresses">
  alert を受け取る address を入力します。複数の address はカンマで区切り、alert を無効にする場合は field を空にします。
</Step>

<Step title="Save">
  **Save** をクリックして変更を適用します。
</Step>
</Steps>

<Tip>
email alerts を有効にすると、webhook delivery の問題を早期に検知し、信頼性の高い integrations を維持できます。
</Tip>

## Cloud Platforms へのデプロイ

webhook handler を production にデプロイする準備はできましたか？各 platform に適した best practices とともに、一般的な cloud provider へ webhooks をデプロイするための platform-specific guides を提供しています。

<CardGroup cols={2}>
  <Card title="Vercel" icon="triangle" href="/developer-resources/webhooks/examples/vercel-example">
    serverless functions を使用して webhooks を Vercel にデプロイ
  </Card>
  <Card title="Cloudflare Workers" icon="cloud" href="/developer-resources/webhooks/examples/cloudflare-example">
    Cloudflare の edge network で webhooks を実行
  </Card>
  <Card title="Supabase Edge Functions" icon="database" href="/developer-resources/webhooks/examples/supabase-example">
    webhooks を Supabase と統合
  </Card>
  <Card title="Netlify Functions" icon="circle-nodes" href="/developer-resources/webhooks/examples/netlify-example">
    webhooks を Netlify serverless functions としてデプロイ
  </Card>
</CardGroup>

<Info>
各 platform guide には、環境設定、signature verification、その provider 固有の deployment steps が含まれています。
</Info>

## 関連 API Reference

<CardGroup cols={2}>
<Card title="Create Webhook" icon="code" href="/api-reference/webhooks/create-webhook">
  webhook endpoint を programmatically 作成・設定するための API reference
</Card>
<Card title="List Webhooks" icon="list" href="/api-reference/webhooks/list-webhooks">
  webhook endpoint を取得・管理するための API reference
</Card>
</CardGroup>