---
title: SvelteKit アダプタ
description: Dodo
  PaymentsをSvelteKitアプリルータープロジェクトに統合する方法を学びます。チェックアウト、顧客ポータル、ウェブフック、セキュアな環境設定をカバーしています。
icon: code-merge
---

<CardGroup cols={2}>
<Card title="チェックアウトハンドラー" icon="cart-shopping" href="#checkout-route-handler">
    Dodo PaymentsのチェックアウトをSvelteKitアプリに統合します。
</Card>
<Card title="顧客ポータル" icon="user" href="#customer-portal-route-handler">
    顧客がサブスクリプションや詳細を管理できるようにします。
</Card>
<Card title="ウェブフック" icon="bell" href="#webhook-route-handler">
    Dodo Paymentsのウェブフックイベントを受信して処理します。
</Card>
</CardGroup>

## インストール

<Steps>
<Step title="パッケージをインストール">
プロジェクトのルートで次のコマンドを実行します:

コード_placeholder_2fad78d2ca945de4_END

</Step>
<Step title="環境変数を設定">
プロジェクトのルートに<code>.env</code>ファイルを作成します:

コード_placeholder_c7f24ad84ce9de21_END

<Warning>
<code>.env</code>ファイルやシークレットをバージョン管理にコミットしないでください。
</Warning>
</Step>
</Steps>

## ルートハンドラーの例

<Info>
すべての例は、SvelteKitアプリルーターを使用していることを前提としています。
</Info>

<Tabs>
<Tab title="チェックアウトハンドラー">
  <Info>
  このハンドラーを使用して、Dodo PaymentsのチェックアウトをSvelteKitアプリに統合します。静的（GET）、動的（POST）、およびセッション（POST）支払いフローをサポートしています。
  </Info>
  <CodeGroup>

コード_placeholder_4163899dd8035fd2_END

  </CodeGroup>
  <CodeGroup>

コード_placeholder_9f4b12d14201bef5_END

  </CodeGroup>
  <CodeGroup>

コード_placeholder_8e00b163b3010893_END

  </CodeGroup>
  <CodeGroup>

コード_placeholder_4fbf4ec13900b837_END

  </CodeGroup>

</Tab>
<Tab title="顧客ポータルハンドラー">
  <Info>
  このハンドラーを使用して、顧客がDodo Paymentsの顧客ポータルを通じてサブスクリプションや詳細を管理できるようにします。
  </Info>
  <CodeGroup>

コード_placeholder_28b58db3f3c24688_END

  </CodeGroup>
</Tab>
<Tab title="ウェブフックハンドラー">
  <Info>
  このハンドラーを使用して、Dodo PaymentsのウェブフックイベントをSvelteKitアプリで安全に受信して処理します。
  </Info>
  <CodeGroup>

コード_placeholder_bdf9201bf853b93c_END

  </CodeGroup>
</Tab>
</Tabs>

## チェックアウトルートハンドラー

<Info>
Dodo Paymentsは、ウェブサイトに支払いを統合するための3種類の支払いフローをサポートしており、このアダプタはすべてのタイプの支払いフローをサポートしています。
</Info>

- **静的支払いリンク:** すぐに共有可能なURLで、迅速かつコードなしで支払いを収集できます。
- **動的支払いリンク:** APIまたはSDKを使用してカスタム詳細を持つ支払いリンクをプログラムで生成します。
- **チェックアウトセッション:** 事前に設定された商品カートと顧客詳細を持つ、安全でカスタマイズ可能なチェックアウト体験を作成します。

<AccordionGroup>
<Accordion title="静的チェックアウト (GET)">

### サポートされているクエリパラメータ
<ParamField query="productId" type="string" required>
商品識別子（例: <code>?productId=pdt_nZuwz45WAs64n3l07zpQR</code>）。
</ParamField>
<ParamField query="quantity" type="integer">
商品の数量。
</ParamField>
<ParamField query="fullName" type="string">
顧客のフルネーム。
</ParamField>
<ParamField query="firstName" type="string">
顧客の名。
</ParamField>
<ParamField query="lastName" type="string">
顧客の姓。
</ParamField>
<ParamField query="email" type="string">
顧客のメールアドレス。
</ParamField>
<ParamField query="country" type="string">
顧客の国。
</ParamField>
<ParamField query="addressLine" type="string">
顧客の住所。
</ParamField>
<ParamField query="city" type="string">
顧客の市。
</ParamField>
<ParamField query="state" type="string">
顧客の州/県。
</ParamField>
<ParamField query="zipCode" type="string">
顧客の郵便番号。
</ParamField>
<ParamField query="disableFullName" type="boolean">
フルネームフィールドを無効にします。
</ParamField>
<ParamField query="disableFirstName" type="boolean">
名フィールドを無効にします。
</ParamField>
<ParamField query="disableLastName" type="boolean">
姓フィールドを無効にします。
</ParamField>
<ParamField query="disableEmail" type="boolean">
メールフィールドを無効にします。
</ParamField>
<ParamField query="disableCountry" type="boolean">
国フィールドを無効にします。
</ParamField>
<ParamField query="disableAddressLine" type="boolean">
住所フィールドを無効にします。
</ParamField>
<ParamField query="disableCity" type="boolean">
市フィールドを無効にします。
</ParamField>
<ParamField query="disableState" type="boolean">
州フィールドを無効にします。
</ParamField>
<ParamField query="disableZipCode" type="boolean">
郵便番号フィールドを無効にします。
</ParamField>
<ParamField query="paymentCurrency" type="string">
支払い通貨を指定します（例: <code>USD</code>）。
</ParamField>
<ParamField query="showCurrencySelector" type="boolean">
通貨セレクターを表示します。
</ParamField>
<ParamField query="paymentAmount" type="integer">
支払い金額を指定します（例: <code>1000</code>は$10.00を意味します）。
</ParamField>
<ParamField query="showDiscounts" type="boolean">
割引フィールドを表示します。
</ParamField>
<ParamField query="metadata_*" type="string">
<code>metadata_</code>で始まる任意のクエリパラメータはメタデータとして渡されます。
</ParamField>

<Warning>
<code>productId</code>が欠けている場合、ハンドラーは400レスポンスを返します。無効なクエリパラメータも400レスポンスを返します。
</Warning>

### レスポンスフォーマット
静的チェックアウトは、チェックアウトURLを含むJSONレスポンスを返します:

コード_placeholder_139958eb49cf7ea4_END

</Accordion>
<Accordion title="動的チェックアウト (POST)">
- パラメータをPOSTリクエストのJSONボディとして送信します。
- 一回限りの支払いと定期的な支払いの両方をサポートします。
- サポートされているPOSTボディフィールドの完全なリストについては、次を参照してください:
  - [一回限りの支払い商品のリクエストボディ](https://docs.dodopayments.com/api-reference/payments/post-payments)
  - [サブスクリプション商品のリクエストボディ](https://docs.dodopayments.com/api-reference/subscriptions/post-subscriptions)

### レスポンスフォーマット
動的チェックアウトは、チェックアウトURLを含むJSONレスポンスを返します:

コード_placeholder_139958eb49cf7ea4_END

</Accordion>
<Accordion title="チェックアウトセッション (POST)">
チェックアウトセッションは、完全な支払いフローを処理するより安全でホストされたチェックアウト体験を提供し、一回限りの購入とサブスクリプションの両方に対してカスタマイズ制御を提供します。

[チェックアウトセッション統合ガイド](https://docs.dodopayments.com/developer-resources/checkout-session)を参照して、詳細とサポートされているフィールドの完全なリストを確認してください。

### レスポンスフォーマット
チェックアウトセッションは、チェックアウトURLを含むJSONレスポンスを返します:

コード_placeholder_dd95c237f3ca327d_END

</Accordion>
</AccordionGroup>

## 顧客ポータルルートハンドラー

顧客ポータルルートハンドラーは、Dodo Paymentsの顧客ポータルをSvelteKitアプリケーションにシームレスに統合することを可能にします。

### クエリパラメータ
<ParamField query="customer_id" type="string" required>
ポータルセッションの顧客ID（例: <code>?customer_id=cus_123</code>）。
</ParamField>
<ParamField query="send_email" type="boolean">
<code>true</code>に設定すると、顧客にポータルリンクを含むメールが送信されます。
</ParamField>

<Warning>
<code>customer_id</code>が欠けている場合、400を返します。
</Warning>

## ウェブフックルートハンドラー

- **メソッド:** POSTリクエストのみがサポートされています。他のメソッドは405を返します。
- **署名検証:** <code>webhookKey</code>を使用してウェブフックの署名を検証します。検証に失敗した場合は401を返します。
- **ペイロード検証:** Zodで検証されます。無効なペイロードには400を返します。
- **エラーハンドリング:**
  - 401: 無効な署名
  - 400: 無効なペイロード
  - 500: 検証中の内部エラー
- **イベントルーティング:** ペイロードタイプに基づいて適切なイベントハンドラーを呼び出します。

### サポートされているウェブフックイベントハンドラー
<CodeGroup>

コード_placeholder_b44d7691cac72c58_END

</CodeGroup>

---

## LLMへのプロンプト

コード_placeholder_f72130ed2180660f_END
