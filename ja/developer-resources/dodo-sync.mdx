---
sidebarTitle: データベース同期
title: データベースとの同期
icon: database
description: 分析、報告、統合のために、Dodo Paymentsのデータを自分のデータベースに自動的に同期します。
---

Dodo Paymentsは、支払いデータを自分のデータベースと自動的に同期する組み込みのデータベース同期機能を提供します。**支払い**、**顧客**、**サブスクリプション**、および**ライセンス**を同期して、分析、報告、または他のシステムとの統合のためにデータのローカルコピーを維持できます。

<Info>
**実装**: [npm package](https://www.npmjs.com/package/dodo-sync) 経由で利用可能 | **ソースコード**: [GitHub](https://github.com/dodopayments/dodo-sync)
</Info>

## 何を同期できますか？

私たちのデータベース同期機能は、次のDodo Paymentsエンティティをデータベースに同期することをサポートしています：

<CardGroup cols={2}>
  {/* LOCKED_PATTERN_a232f01eeedc677d3b9b3ccce73b9fda */}
    一回限りの支払い、返金、支払いステータスの更新などを含むすべての支払いトランザクションを同期します。
  </Card>
  {/* LOCKED_PATTERN_5d07d2e523844b62c983ff62b4723a83 */}
    顧客プロファイル、連絡先情報、メタデータなどを含む顧客データを同期状態に保ちます。
  </Card>
  {/* LOCKED_PATTERN_25273516451e819dcf5729a5b31c3fb9 */}
    有効なサブスクリプション、請求サイクル、サブスクリプションのステータス変更などを含むサブスクリプションデータを同期します。
  </Card>
  {/* LOCKED_PATTERN_195400deed106b7fc4997fd4764db9b5 */}
    ライセンスキー、有効化、ライセンスステータスの更新などを含むライセンス情報を同期します。
  </Card>
</CardGroup>

これらのエンティティを `scopes` パラメータで指定することで任意の組み合わせを同期できます。すべての同期操作は増分同期であり、最適なパフォーマンスのために新規または更新されたレコードのみを転送します。

## データベースサポート

現在、**MongoDB**、**PostgreSQL**、**MySQL**、および**ClickHouse**をサポートしています。

私たちは現在、次のサポートの拡張に取り組んでいます：
- **データベース**: Snowflakeなど。
- **パイプライン**: ETLパイプライン、リアルタイム同期。

<Tip>
継続的にデータベースのサポートを拡張しています。新しいデータベース統合を貢献したい場合は、当社の [GitHub リポジトリ](https://github.com/dodopayments/dodo-sync) にプルリクエストを送ってください。
</Tip>

## 始め方

**CLI**を介して、または**コード**内でプログラム的にデータベース同期機能を使用できます。どちらの方法も同じ機能を提供しますので、ワークフローに最適な方法を選択してください。

### CLIの使用

CLIツールは、データベース同期を設定して実行するための迅速な方法を提供します。どこからでも使用できるように、グローバルにインストールしてください：

<CodeGroup>

```bash npm
npm install -g dodo-sync
```

```bash bun
bun add -g dodo-sync
```

</CodeGroup>

#### CLIの実行

CLIは2つのモードをサポートしています：**インタラクティブモード**（ガイド付きセットアップ用）と**マニュアルモード**（直接設定用）。

**インタラクティブモード**: 引数なしでコマンドを実行するだけで、インタラクティブなセットアップウィザードが開始されます。

```bash
dodo-sync
```

**マニュアルモード**: ウィザードをスキップするために引数を直接渡します。

```bash
dodo-sync -i [interval] -d [database] -u [database_uri] --scopes [scopes] --api-key [api_key] --env [environment]
```

**例:**

```bash
# MongoDB
dodo-sync -i 600 -d mongodb -u mongodb://mymongodb.url --scopes "licences,payments,customers,subscriptions" --api-key YOUR_API_KEY --env test_mode

# PostgreSQL
dodo-sync -i 600 -d postgres -u postgresql://user:password@localhost:5432/mydb --scopes "licences,payments,customers,subscriptions" --api-key YOUR_API_KEY --env test_mode

# MySQL
dodo-sync -i 600 -d mysql -u mysql://user:password@localhost:3306/mydb --scopes "licences,payments,customers,subscriptions" --api-key YOUR_API_KEY --env test_mode

# ClickHouse
dodo-sync -i 600 -d clickhouse -u http://localhost:8123 --scopes "licences,payments,customers,subscriptions" --api-key YOUR_API_KEY --env test_mode
```

#### CLI引数

<ParamField path="--interval" type="number" alias="-i">
同期間隔（秒）。同期操作の実行頻度を決定します。指定しない場合、同期は1回実行されて終了します。
</ParamField>

<ParamField path="--database" type="string" alias="-d" required>
使用するデータベースの種類。サポートされている値: `"mongodb"`、`"postgres"`、`"mysql"`、または `"clickhouse"`。
</ParamField>

<ParamField path="--database-uri" type="string" alias="-u" required>
データベースの接続URI:
- **MongoDB**: `mongodb://localhost:27017` または `mongodb+srv://user:pass@cluster.mongodb.net/`
- **PostgreSQL**: `postgresql://user:password@localhost:5432/mydb`
- **MySQL**: `mysql://user:password@localhost:3306/mydb`
- **ClickHouse**: `http://localhost:8123`
</ParamField>

<ParamField path="--scopes" type="string" required>
同期するデータエンティティのカンマ区切りリスト。利用可能なスコープ: `licences`、`payments`、`customers`、`subscriptions`。例: `"payments,customers"`。
</ParamField>

<ParamField path="--api-key" type="string" required>
あなたの Dodo Payments API キー。ライブモードでは `dp_live_`、テストモードでは `dp_test_` で始まる必要があります。
</ParamField>

<ParamField path="--env" type="string" required>
環境ターゲット。`"live_mode"` または `"test_mode"` のいずれかである必要があります。これによりどの Dodo Payments 環境から同期するかが決まります。
</ParamField>

<ParamField path="--rate-limit" type="number" alias="--rl">
1 秒あたりのリクエスト上限。同期エンジンが Dodo Payments API に対してリクエストを送る速度を制御し、過負荷を防ぎます。
</ParamField>

### コード内での使用

プログラム的な制御のために、同期機能をアプリケーションに直接統合します。プロジェクトに依存関係としてインストールします：

<CodeGroup>

```bash npm
npm install dodo-sync
```

```bash bun
bun add dodo-sync
```

</CodeGroup>

#### 自動同期（間隔ベース）

自動同期を使用すると、同期を定期的に継続的に実行できます：

```typescript
import { DodoSync } from 'dodo-sync';

const syncDodoPayments = new DodoSync({
  interval: 60, // Sync every 60 seconds
  database: 'mongodb',
  databaseURI: process.env.MONGODB_URI, // e.g., 'mongodb://localhost:27017'
  scopes: ['licences', 'payments', 'customers', 'subscriptions'],
  dodoPaymentsOptions: {
    bearerToken: process.env.DODO_PAYMENTS_API_KEY,
    environment: 'test_mode' // or 'live_mode'
  }
});

// Initialize connection
await syncDodoPayments.init();

// Start the sync loop
syncDodoPayments.start();
```

<Tip>
自動同期で `.start()` を使用する場合、`interval` オプションは必須です。同期は指定した間隔でプロセスが停止されるまで継続的に実行されます。
</Tip>

#### 手動同期

手動同期を使用すると、オンデマンドで同期操作をトリガーできます（例：cronジョブやAPIエンドポイントから）：

```typescript
import { DodoSync } from 'dodo-sync';

const syncDodoPayments = new DodoSync({
  database: 'mongodb',
  databaseURI: process.env.MONGODB_URI,
  scopes: ['licences', 'payments', 'customers', 'subscriptions'],
  dodoPaymentsOptions: {
    bearerToken: process.env.DODO_PAYMENTS_API_KEY,
    environment: 'test_mode'
  }
});

// Initialize connection
await syncDodoPayments.init();

// Trigger a single sync operation
await syncDodoPayments.run();
```

<Tip>
手動同期を使用する場合、`interval` オプションは不要です。同期操作を実行したいときにいつでも `.run()` を呼び出せます。
</Tip>

#### PostgreSQLの例

PostgreSQL で `dodo-sync` を使用する方法は次のとおりです:

```typescript
import { DodoSync } from 'dodo-sync';

const syncDodoPayments = new DodoSync({
  interval: 60,
  database: 'postgres',
  databaseURI: process.env.POSTGRES_URI, // e.g., 'postgresql://user:password@localhost:5432/mydb'
  scopes: ['licences', 'payments', 'customers', 'subscriptions'],
  dodoPaymentsOptions: {
    bearerToken: process.env.DODO_PAYMENTS_API_KEY,
    environment: 'test_mode'
  }
});

await syncDodoPayments.init();
syncDodoPayments.start();
```

#### MySQLの例

MySQL で `dodo-sync` を使用する方法は次のとおりです:

```typescript
import { DodoSync } from 'dodo-sync';

const syncDodoPayments = new DodoSync({
  interval: 60,
  database: 'mysql',
  databaseURI: process.env.MYSQL_URI, // e.g., 'mysql://user:password@localhost:3306/mydb'
  scopes: ['licences', 'payments', 'customers', 'subscriptions'],
  dodoPaymentsOptions: {
    bearerToken: process.env.DODO_PAYMENTS_API_KEY,
    environment: 'test_mode'
  }
});

await syncDodoPayments.init();
syncDodoPayments.start();
```

#### ClickHouseの例

ClickHouse で `dodo-sync` を使用する方法は次のとおりです:

```typescript
import { DodoSync } from 'dodo-sync';

const syncDodoPayments = new DodoSync({
  interval: 60,
  database: 'clickhouse',
  databaseURI: process.env.CLICKHOUSE_URI, // e.g., 'http://localhost:8123'
  scopes: ['licences', 'payments', 'customers', 'subscriptions'],
  dodoPaymentsOptions: {
    bearerToken: process.env.DODO_PAYMENTS_API_KEY,
    environment: 'test_mode'
  }
});

await syncDodoPayments.init();
syncDodoPayments.start();
```

#### コンストラクタオプション

<ParamField body="database" type="string" required>
使用するデータベース名。サポートされている値: `"mongodb"`、`"postgres"`、`"mysql"`、または `"clickhouse"`。
</ParamField>

<ParamField body="databaseURI" type="string" required>
データベースの接続文字列:
- **MongoDB**: `mongodb://localhost:27017` または `mongodb+srv://...`
- **PostgreSQL**: `postgresql://user:password@localhost:5432/mydb`
- **MySQL**: `mysql://user:password@localhost:3306/mydb`
- **ClickHouse**: `http://localhost:8123`
</ParamField>

<ParamField body="scopes" type="string[]" required>
同期するエンティティの配列。利用可能なオプション: `"licences"`、`"payments"`、`"customers"`、`"subscriptions"`。任意の組み合わせを含めることができます。
</ParamField>

<ParamField body="dodoPaymentsOptions" type="object" required>
認証と環境選択のための Dodo Payments API 設定。完全なオプションについては [TypeScript SDK types](https://github.com/dodopayments/dodopayments-typescript/blob/main/src/client.ts) を参照してください。


**必須プロパティ:**
- `bearerToken`: あなたの Dodo Payments API キー
- `environment`: `"test_mode"` または `"live_mode"` のいずれか
</ParamField>

<ParamField body="interval" type="number">
自動同期で `.start()` を使用する際の同期間（秒）。`.run()` を使用する手動同期時には任意です。
</ParamField>

<ParamField body="rateLimit" type="number">
1 秒あたりのリクエスト数。同期エンジンが Dodo Payments API に対してリクエストを送る速度を制御して、過負荷を避けます。
</ParamField>

## 重要な情報

**MongoDB**: データベースサーバーに `dodopayments_sync` という名前のデータベースが自動的に作成されます。すべての同期データはそこに保存されます。このデータベース名は現在固定されており、変更できません。

**PostgreSQL**: 接続URIで指定したデータベースにテーブル（`Subscriptions`、`Payments`、`Licenses`、`Customers`）が作成されます。データは JSONB 形式で保存されます。

**MySQL**: 接続URIで指定したデータベースにテーブル（`Subscriptions`、`Payments`、`Licenses`、`Customers`）が作成されます。データは JSON 形式で保存されます。

**ClickHouse**: ReplacingMergeTree エンジンを使用してテーブル（`Subscriptions`、`Payments`、`Licenses`、`Customers`）が作成されます。クエリ実行時には `FINAL` キーワードを使用して重複が除外された結果を取得してください。
</Warning>

<Info>
同期エンジンは変更を追跡し、新規または更新されたレコードのみを同期するため、大量のデータセットでも後続の同期を効率的に実行できます。
</Info>

## 追加リソース

<CardGroup cols={2}>
  {/* LOCKED_PATTERN_7f2d6304a6c0fcc02c0a5a8010c86e3c */}
    ソースコードを表示し、問題を報告し、改善に貢献してください
  </Card>
  {/* LOCKED_PATTERN_d20a2fc4db0166922ce0a3e1469437e9 */}
    パッケージの詳細とインストール手順を表示
  </Card>
</CardGroup>
