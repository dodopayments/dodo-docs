---
title: MCPサーバー
description: AIアシスタントがDodo Payments APIとModel Context Protocol
  (MCP)を通じてシームレスな支払い操作、サブスクリプション管理、顧客とのインタラクションを行えるようにします。
icon: /images/logos/mcp.svg
---

## 概要

Model Context Protocol (MCP)は、AIアプリケーションが外部データソースやAPIに安全に接続できるオープンスタンダードです。Dodo Payments MCPサーバーは、Claude、Cursor、その他のMCP互換クライアントに対して、あなたの支払いインフラストラクチャへの構造化されたアクセスを提供します。

Dodo Payments MCPサーバーは、[Stainless](https://www.stainless.com/)で構築されており、**Code Mode**アーキテクチャを使用しています。これにより、AIエージェントはDodo Payments SDKに対してTypeScriptコードを孤立したサンドボックス環境で記述および実行できます。

### 主な機能

- **支払い操作**: 支払いと返金の作成、取得、管理
- **サブスクリプション管理**: 定期請求、アップグレード、キャンセルの処理
- **顧客管理**: 顧客データとポータルアクセスの管理
- **製品カタログ**: 製品、価格、割引の作成と更新
- **ライセンス管理**: ソフトウェアライセンスの有効化、検証、管理
- **使用量ベースの請求**: メーター使用量の追跡と請求

## Code Modeの動作

Dodo Payments MCPサーバーはCode Modeを使用しており、AIエージェントに2つの強力なツールを提供します：

1. **Docs Search Tool**: Dodo Payments APIおよびSDKに関するドキュメントをクエリし、利用可能な操作とパラメータを理解します。
2. **Code Execution Tool**: SDKに対してTypeScriptコードを記述し、ウェブやファイルシステムへのアクセスなしで安全なサンドボックス環境で実行します。

このアーキテクチャにより、AIエージェントは複雑で多段階の支払い操作を決定論的かつ繰り返し実行できます。

<Tip>
コードモードを使用すると、エージェントは複数のAPI呼び出しをチェーンし、条件付きロジックを処理し、計算を実行できます—すべて1回のツール呼び出しで完結します。
</Tip>

## Dodo Knowledge MCP

Dodo Payments MCPサーバー（API操作を実行するため）に加えて、**Dodo Knowledge MCP**を提供しています。これは、AIアシスタントがDodo Paymentsのドキュメントとナレッジベースに即座にアクセスできるセマンティック検索サーバーです。

<Info>
**[ContextMCP.ai](https://contextmcp.ai)を基盤に構築**: Dodo Knowledge MCPはContextMCPを活用し、ベクトル埋め込みによるドキュメント全体の高速なセマンティック検索を実現します。
</Info>

### Dodo Knowledge MCPとは？

Dodo Knowledge MCPは、次の機能を提供するリモートMCPサーバーです。

- **セマンティックドキュメント検索**: 自然言語クエリを使用して関連するドキュメントを見つける
- **コンテキストに基づく回答**: AIアシスタントがDodo Paymentsに関する正確で最新の情報を取得
- **ゼロセットアップ**: APIキーやローカルインストールは不要 - 接続してクエリを開始するだけ

### クイックセットアップ

AIクライアントでDodo Knowledge MCPに接続します:

<Tabs>
<Tab title="Cursor">
`~/.cursor/mcp.json`に追加：

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Desktop">
Claude Desktopの設定に追加：

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

設定ファイルの場所:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Tab>

{/* LOCKED_PATTERN_e01afe5ad2b70c1861708d679d3c0fa9 */}
`~/.codeium/windsurf/mcp_config.json`に追加：

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Code">
ターミナルで実行：

```bash
claude mcp add dodo-knowledge -- npx -y mcp-remote@latest https://knowledge.dodopayments.com/mcp
```

</Tab>
</Tabs>

<Note>
Node.js 18以降が必要です。`mcp-remote`パッケージがリモートMCPサーバーへの接続を処理します。
</Note>

### 両方のMCPサーバーを一緒に使用する

AI支援の開発体験を最適化するために、両方のMCPサーバーの使用をお勧めします:

| サーバー | 目的 | 使用例 |
|--------|---------|----------|
| **Dodo Knowledge MCP** | ドキュメント検索 | "Webhookを処理するにはどうすればよいですか？", "どの支払い方法がサポートされていますか？" |
| **Dodo Payments MCP** | API操作 | 支払いの作成、サブスクリプションの管理、返金の処理 |

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    },
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

<Tip>
両方のサーバーが設定されていれば、AIアシスタントは最初にドキュメントを検索して機能の実装方法を把握し、その後実際のAPI呼び出しを行うことができます—すべて1つの会話で完了します。
</Tip>

### Knowledge MCPのトラブルシューティング

接続の問題が発生した場合:

1. **MCP認証キャッシュをクリア**: `rm -rf ~/.mcp-auth`
2. **クライアントアプリケーションを再起動**
3. **クライアントログ**でエラーメッセージを確認
4. **Node.jsバージョンを確認**: Node.js 18以上が必要

<Card title="Knowledge MCP Server" icon="book-open" href="https://knowledge.dodopayments.com">
  Dodo Knowledge MCP設定ページにアクセス
</Card>

## インストール

ワークフローに最適なインストール方法を選択してください。

### リモートMCPサーバー（推奨）

ローカルのセットアップやインストールなしでホストされたMCPサーバーにアクセスします。これが最も迅速に始める方法です。

<Frame>
  <iframe 
      className="w-full aspect-video rounded-md" 
      src="https://www.youtube.com/embed/TxGgYxNmkT4"
      title="Using Dodo Payment's Remote MCP Server"
      frameBorder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowFullScreen
  ></iframe>
</Frame>

<Steps>
{/* LOCKED_PATTERN_bd1568a9aaf0e86b69853e215d3d5723 */}
    ブラウザで[https://mcp.dodopayments.com](https://mcp.dodopayments.com)にアクセス。
</Step>

{/* LOCKED_PATTERN_b8d4e4a1a3c0fcf28fd2afe7ce9e8316 */}
    クライアント固有のJSON構成をコピーします。CursorやClaude Desktopの場合は、これをMCP設定に追加します：
    

    ```json
    {
      "mcpServers": {
        "dodopayments_api": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8be57788a275cad53ee59f91af51bfd5 */}
    OAuthフローでは以下を求められます：
    - Dodo Payments APIキーを入力
    - 環境（テストまたは本番）を選択
    - MCPクライアントタイプを選択
    
    <Warning>
    APIキーは安全に保管してください。開発中はテストモードキーを使用。
    </Warning>
</Step>

{/* LOCKED_PATTERN_606fac2bd646ef76f5d66097882887ed */}
    **Login and Approve**をクリックして接続を承認します。
    
    <Check>
    接続されると、AIアシスタントがあなたに代わってDodo Payments APIとやり取りできます。
    </Check>
</Step>
</Steps>

### NPMパッケージ

NPMを使用してMCPサーバーをローカルでインストールして実行します。

<Tabs>
{/* LOCKED_PATTERN_c8776436a7ae17e188a287e7c6852f16 */}

```bash
# Set your environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."
export DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key"
export DODO_PAYMENTS_ENVIRONMENT="live_mode"

# Run the latest version
npx -y dodopayments-mcp@latest
```

</Tab>

{/* LOCKED_PATTERN_8d04e6cca1a5903f80148c5b923e5c3f */}

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

</Tab>
</Tabs>

<Tip>
常に最新バージョンを取得するには`@latest`を使用し、安定性を重視する場合は`@1.0.0`のように特定バージョンを固定します。
</Tip>

### Docker

一貫したデプロイメントのために、コンテナ化された環境でMCPサーバーを実行します。

<CodeGroup>

```bash Docker
# Pull the latest image
docker pull ghcr.io/dodopayments/mcp:latest

# Run the container
docker run -e DODO_PAYMENTS_API_KEY="dodo_test_..." \
  -e DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -e DODO_PAYMENTS_ENVIRONMENT="live_mode" \
  ghcr.io/dodopayments/mcp:latest
```

```yaml Docker Compose
services:
  dodopayments-mcp:
    image: ghcr.io/dodopayments/mcp:latest
    environment:
      - DODO_PAYMENTS_API_KEY=dodo_test_...
      - DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_key
      - DODO_PAYMENTS_ENVIRONMENT=live_mode
    restart: unless-stopped
```

</CodeGroup>

<Note>
Dockerイメージは[GitHub Container Registry](https://github.com/orgs/dodopayments/packages/container/package/mcp)で利用できます。
</Note>

## クライアント設定

お好みのAIクライアントでDodo Payments MCPサーバーを設定します。

<Tabs>
<Tab title="Cursor">
CursorでDodo Payments MCPサーバーをセットアップし、支払いデータへの会話型アクセスを有効にします。

**ワンクリックインストール**

以下のボタンを使用して、CursorにMCPサーバーを直接インストールします:

[

![Cursorに追加](https://cursor.com/deeplink/mcp-install-dark.svg)

](https://cursor.com/en-US/install-mcp?name=dodopayments-mcp&config=)

クリック後、Cursorの`mcp.json`で環境変数を設定します（**Cursor Settings** > **Tools & MCP** > **New MCP Server**）。

**手動設定**

<Steps>
{/* LOCKED_PATTERN_b5120fe2afbf9bb96d1de6f9549e8f41 */}
    **Cursor Settings** > **Features** > **Model Context Protocol**に移動、または`Cmd/Ctrl + Shift + P`を押して「MCP Settings」を検索。
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    **Edit Config**をクリックして`mcp_config.json`ファイルを開きます。
</Step>

{/* LOCKED_PATTERN_a65e4e236194f7c177caa156c6d0b797 */}
    次の構成のいずれかを選択：
    
    **Remote Server (Recommended)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **ローカルNPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8368e3e8540b47a92000151cb7e30a33 */}
    設定ファイルを保存し、Cursorを再起動。
    
    <Check>
    AIアシスタントにDodo Paymentsデータについて質問して接続を確認。
    </Check>
</Step>
</Steps>
</Tab>

<Tab title="Claude Desktop">
Claude DesktopでDodo Payments MCPサーバーをセットアップし、支払いデータへの会話型アクセスを有効にします。
<Steps>
{/* LOCKED_PATTERN_ecfdd2e916ed4a93d6b3950e9027df3f */}
    Claude Desktopの設定ファイルを見つけます：
    
    - **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
    - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
    - **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    ファイルをテキストエディターで開き、Dodo Paymentsの設定を追加：
    
    **Remote Server (Recommended)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **ローカルNPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_83563b32c3caf8bd7aa68192b6ebcc7d */}
    Claude Desktopを閉じて再度開き、変更を反映。
    
    <Tip>
    MCPサーバーの接続を確認するにはClaude Desktopで🔌アイコンを探してください。
    </Tip>
</Step>
</Steps>
</Tab>

<Tab title="Claude Code">
Claude Code CLIでDodo Payments MCPサーバーをセットアップします。

ターミナルで次のコマンドを実行します:

```bash
claude mcp add --transport stdio dodopayments_api \
  --env DODO_PAYMENTS_API_KEY="dodo_test_..." \
  DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -- npx -y dodopayments-mcp
```

構成はホームディレクトリの`.claude.json`ファイルに保存されます。

<Tip>
Claude Codeはシェルから環境変数を自動的に拾うため、コマンド実行前にexportすることもできます。
</Tip>
</Tab>

<Tab title="VS Code">
VS CodeでDodo Payments MCPサーバーをセットアップします。

**ワンクリックインストール**

[VS Codeを開く](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dodopayments-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22dodopayments-mcp%22%5D%2C%22env%22%3A%7B%22DODO_PAYMENTS_API_KEY%22%3A%22ここにDODO_PAYMENTS_API_KEYを設定してください。%22%2C%22DODO_PAYMENTS_WEBHOOK_KEY%22%3A%22ここにDODO_PAYMENTS_WEBHOOK_KEYを設定してください。%22%7D%7D)

クリック後、VS Codeの`mcp.json`で環境変数を設定します（**Command Palette** > **MCP: Open User Configuration**）。

**手動設定**

<Steps>
{/* LOCKED_PATTERN_01c11d820b190ade4d2a29feff5a49da */}
    コマンドパレットを開き（`Cmd/Ctrl + Shift + P`）、"MCP: Open User Configuration"を検索。
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Dodo Payments MCPサーバーを追加：
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_fabb79a0cecb26359867d78b61382dd7 */}
    VS Codeをリロード（`Cmd/Ctrl + Shift + P` > "Reload Window"）してMCPサーバーを有効化。
</Step>
</Steps>
</Tab>

<Tab title="Cline (VS Code)">
Cline（VS Code）でDodo Payments MCPサーバーをセットアップし、支払いデータへの会話型アクセスを有効にします。
<Steps>
{/* LOCKED_PATTERN_43db9eecf5eea8e9c4ad60b6c3ff1c0b */}
    Clineの設定ファイルを探します：
    
    - **macOS**: `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
    - **Windows**: `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    - **Linux**: `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
</Step>

{/* LOCKED_PATTERN_0849b9a67e72024819745a9889898853 */}
    Dodo Payments MCPサーバー構成を追加：
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_43be57c73e24741f16da5f79d5398749 */}
    VS Codeウィンドウをリロード（`Cmd/Ctrl + Shift + P` > "Reload Window"）し、MCPサーバー接続を有効化。
</Step>
</Steps>
</Tab>

<Tab title="Zed">
ZedでDodo Payments MCPサーバーをセットアップし、支払いデータへの会話型アクセスを有効にします。
<Steps>
{/* LOCKED_PATTERN_a48799001554eb7ef4b2ac1e56fab915 */}
    **Settings** > **Assistant** > **Model Context Protocol**に移動。
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    次の構成を追加：
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_b1c8821108a3bf0ac4028a0196176d66 */}
    設定を保存。MCPサーバーはZedのAIアシスタントで利用可能になります。
</Step>
</Steps>
</Tab>

{/* LOCKED_PATTERN_71257692833d303ebb288457e9ce7134 */}
他のMCP対応クライアントでDodo Payments MCPサーバーをセットアップします。

**リモートサーバー（推奨）**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

**ローカルNPX**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
      }
    }
  }
}
```

<Info>
完全なMCP対応クライアントリストと設定方法は[modelcontextprotocol.io/clients](https://modelcontextprotocol.io/clients)を参照。
</Info>
</Tab>
</Tabs>

## 環境変数

環境変数を使用してMCPサーバーの動作を設定します。

| 変数 | 説明 | 必須 |
|----------|-------------|----------|
| `DODO_PAYMENTS_API_KEY` | あなたのDodo Payments APIキー | はい |
| `DODO_PAYMENTS_WEBHOOK_KEY` | Webhook署名キー | いいえ |
| `DODO_PAYMENTS_ENVIRONMENT` | 本番では`live_mode`に設定 | いいえ |

## リモートでの実行

ウェブベースのクライアントやエージェントワークフローのために、MCPサーバーをリモートHTTPサーバーとしてデプロイします。

```bash
# Start as remote server
npx -y dodopayments-mcp --transport=http

# Specify a custom port
npx -y dodopayments-mcp --transport=http --port=3000
```

### リモートサーバー設定

デプロイ後、クライアントはサーバーURLを使用して接続できます:

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "url": "http://localhost:3000",
      "headers": {
        "Authorization": "Bearer your_api_key"
      }
    }
  }
}
```

### 認証ヘッダー

リモートサーバーは次のヘッダーを介して認証を受け付けます:

| ヘッダー | 説明 |
|--------|-------------|
| `Authorization` | Bearerトークン認証 |
| `x-dodo-payments-api-key` | 直接APIキーヘッダー |

## クライアント機能

異なるMCPクライアントは、MCP仕様に対するサポートのレベルが異なります。サーバーはこれらの制限を回避するためにツールスキーマを自動的に調整できます。

```bash
# Auto-detect client (default)
npx dodopayments-mcp --client=infer

# Configure for specific clients
npx dodopayments-mcp --client=cursor
npx dodopayments-mcp --client=claude
npx dodopayments-mcp --client=claude-code
npx dodopayments-mcp --client=openai-agents
```

### 利用可能なクライアントプリセット

| クライアント | 説明 |
|--------|-------------|
| `infer` | クライアントの自動検出（デフォルト） |
| `cursor` | Cursorエディター |
| `claude` | Claude AI WebまたはDesktop |
| `claude-code` | Claude Code CLI |
| `openai-agents` | OpenAIエージェントプラットフォーム |

### 手動機能設定

上記にリストされていないクライアントについては、機能を手動で設定します:

```bash
# Disable $ref pointer support and set max tool name length
npx dodopayments-mcp --no-capability=refs --capability=tool-name-length=40
```

利用可能な機能：
- `top-level-unions` - トップレベルの共用体型のサポート
- `valid-json` - JSON文字列引数の正しい解析
- `refs` - スキーマ内の$refポインタのサポート
- `unions` - 共用体型（anyOf）のサポート
- `formats` - フォーマット検証のサポート
- `tool-name-length=N` - ツール名の最大長

<Tip>
詳しい機能情報は`npx dodopayments-mcp --describe-capabilities`を実行。
</Tip>

## ツールフィルタリング

AIアシスタントに公開されるAPI操作を制御して、パフォーマンスを最適化し、セキュリティを維持します。

### 操作タイプでフィルタリング

読み取り専用または書き込み操作へのアクセスを制限します。

```bash
# Only expose read operations (GET requests)
npx dodopayments-mcp --operation=read

# Only expose write operations (POST, PUT, DELETE)
npx dodopayments-mcp --operation=write
```

<Tip>
開発中に誤ってデータを変更しないよう`--operation=read`を使用。
</Tip>

### リソースでフィルタリング

特定のAPIリソースにツールを制限します。

```bash
# Only expose payment-related tools
npx dodopayments-mcp --resource=payments

# Expose multiple resources
npx dodopayments-mcp --resource=payments --resource=customers

# Use wildcards for resource groups
npx dodopayments-mcp --resource=subscription*
```

### 特定のツールを含めるまたは除外する

利用可能な個々のツールを微調整します。

```bash
# Include only specific tools
npx dodopayments-mcp --tool=create_payments --tool=list_payments

# Exclude specific tools
npx dodopayments-mcp --no-tool=delete_products

# Disable the docs search tool
npx dodopayments-mcp --no-tools=docs

# List all available tools
npx dodopayments-mcp --list
```

### フィルタを組み合わせる

正確な制御のために複数のフィルタを同時に適用します。

```bash
# Only payment and subscription read operations
npx dodopayments-mcp --operation=read --resource=payments --resource=subscriptions
```

### URLクエリパラメータフィルタリング（リモートサーバー）

リモートサーバーを使用する際は、URLクエリパラメータを介してフィルタを適用します:

```
https://mcp.dodopayments.com/?operation=read
https://mcp.dodopayments.com/?resource=payments
```

## ダイナミックツールモード

大規模なAPIでは、すべてのツールを一度に読み込むとAIのコンテキストウィンドウが圧倒される可能性があります。ダイナミックツールモードは、より効率的な代替手段を提供します。

```bash
npx dodopayments-mcp --tools=dynamic
```

ダイナミックモードでは、3つのメタツールが個々のAPIツールを置き換えます:

1. **`list_api_endpoints`** - オプションのフィルタリング付きで利用可能なメソッドを検索
2. **`get_api_endpoint_schema`** - 特定メソッドの詳細スキーマを取得
3. **`invoke_api_endpoint`** - 適切なパラメーターで任意のメソッドを実行

<Info>
メソッド数が50を超えるAPIでは動的ツールが自動的に有効になります。すべてのツールを含めたい場合は`--tools=all`で上書き。
</Info>

```bash
# Use both dynamic tools and specific resources
npx dodopayments-mcp --tools=dynamic --resource=payments
```

## 高度な使用法

### プログラムによる統合

カスタムMCPサーバーを構築するか、既存のものをプログラムで拡張します。

```javascript
import { server, endpoints, init } from "dodopayments-mcp/server";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

// Initialize with all default endpoints
init({ server, endpoints });

// Start the server
const transport = new StdioServerTransport();
await server.connect(transport);
```

### カスタムツールの開発

独自のツールとビジネスロジックでMCPサーバーを拡張します。

```javascript
import { server, init } from "dodopayments-mcp/server";
import createPayments from "dodopayments-mcp/tools/payments/create-payments";
import { zodToJsonSchema } from "zod-to-json-schema";
import { z } from "zod";

// Define a custom tool
const customTool = {
  tool: {
    name: 'calculate_payment_total',
    description: 'Calculate payment total with tax and fees',
    inputSchema: zodToJsonSchema(
      z.object({
        amount: z.number().describe('Base amount in cents'),
        tax_rate: z.number().describe('Tax rate as decimal'),
      })
    ),
  },
  handler: async (client, args) => {
    const total = args.amount * (1 + args.tax_rate);
    return {
      content: [{
        type: 'text',
        text: JSON.stringify({ total, currency: 'usd' }),
      }],
    };
  },
};

// Initialize with custom tools
init({ 
  server, 
  endpoints: [createPayments, customTool] 
});
```

## セキュリティベストプラクティス

API資格情報を保護し、安全な統合を維持します。

<AccordionGroup>
{/* LOCKED_PATTERN_58b09b66cd00f40de384d770573ae848 */}
**資格情報をバージョン管理にコミットしないでください**

APIキーは環境変数や安全なシークレット管理システムに保存します。

```bash
# Use environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."

# Use a .env file (add to .gitignore)
echo "DODO_PAYMENTS_API_KEY=dodo_test_..." > .env
```

**定期的にキーをローテーションする**

定期的に新しいAPIキーを生成し、古いものをDodo Paymentsダッシュボードから取り消します。

**開発中はテストキーを使用する**

本番データに影響を与えないよう、開発中は常にテストモードAPIキーを使用してください。
</Accordion>

{/* LOCKED_PATTERN_1d0876565ecc6c625335a3849461e889 */}
**本番環境ではツールフィルタリングを適用**

公開される操作をAIアシスタントが必要とするものだけに制限します。

```bash
# Production: read-only access
npx dodopayments-mcp --operation=read

# Development: full access
npx dodopayments-mcp
```

**リモートサーバーに認証を実装する**

リモート展開時は常に`Authorization`ヘッダーまたは`x-dodo-payments-api-key`ヘッダーによる認証を要求してください。

**API使用状況を監視する**

Dodo PaymentsダッシュボードでMCPサーバーのアクティビティを監視し、異常なパターンに対するアラートを設定します。
</Accordion>

{/* LOCKED_PATTERN_ddb4fc82978c8dda72d606214fdad81d */}
**リモートサーバーにはHTTPSを使用**

リモートMCPサーバーは常にHTTPSエンドポイントの背後にデプロイします。

**レート制限を実装する**

MCPサーバーとAPIレベルの両方でレート制限を実装して、悪用から保護します。

**ネットワークアクセスを制限する**

ファイアウォールルールを設定し、MCPサーバーに接続できるクライアントを制限します。
</Accordion>
</AccordionGroup>

## トラブルシューティング

<AccordionGroup>
{/* LOCKED_PATTERN_3e10fc2511884be492da344a929e2311 */}
**APIキーを確認**

APIキーが正しく設定されており、必要な権限があることを確認します。

```bash
# Test your API key
curl -H "Authorization: Bearer dodo_test_..." \
  https://test.dodopayments.com/payments
```

**ネットワーク接続を確認する**

Dodo Payments APIエンドポイントに到達できることを確認します。

**クライアントログを確認する**

MCPクライアントで詳細ログを有効にして接続問題を診断。
</Accordion>

{/* LOCKED_PATTERN_8f29ea613a43315c29bf194043f8ada2 */}
**APIキー環境を確認**

テストエンドポイントにはテストキーを、本番エンドポイントには本番キーを使用していることを確認します。

**環境変数を確認する**

`DODO_PAYMENTS_ENVIRONMENT`が正しく設定されているか確認（本番は`live_mode`）。

**資格情報を再生成する**

問題が続く場合はダッシュボードから新しいAPIキーを発行。
</Accordion>

{/* LOCKED_PATTERN_565675849d369f82d965d1f3a122632b */}
**入力パラメーターを検証**

AIアシスタントが各ツールに対して正しくフォーマットされたパラメータを提供していることを確認します。

**エラーメッセージを確認する**

APIからのエラーレスポンスを確認して、何が問題だったのか具体的なガイダンスを得ます。

**APIを直接テストする**

curlやPostmanでDodo Payments APIを直接呼び出して動作を確認。
</Accordion>

{/* LOCKED_PATTERN_776734428589daf9897a729aa0a38aed */}
**別のクライアントプリセットを試す**

ツールが正しく機能しない場合は、クライアントを明示的に指定してみてください:

```bash
npx dodopayments-mcp --client=cursor
```

**機能サポートを確認する**

一部のクライアントはすべてのJSONスキーマ機能をサポートしていません。サポート内容を確認するには`--describe-capabilities`を使用。
</Accordion>
</AccordionGroup>

## リソース

<CardGroup cols={2}>
{/* LOCKED_PATTERN_4325afcb089e351f99ef591b38203ac2 */}
  Dodo Payments APIの完全なドキュメントを確認
</Card>

{/* LOCKED_PATTERN_1cbca36bdeb2bbbc77032220bd30c277 */}
  Model Context Protocol標準について詳しく学ぶ
</Card>

{/* LOCKED_PATTERN_02488b19a875a7c4cd24c2f5651e4831 */}
  MCPサーバーのソースコードを確認し、貢献する
</Card>

{/* LOCKED_PATTERN_d4dbe77bbb87d236d98e318084853b86 */}
  NPMからMCPサーバーをインストール
</Card>
</CardGroup>