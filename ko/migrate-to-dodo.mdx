---
title: Dodo Payments로 마이그레이션하기
description: CLI 도구를 사용하여 다른 결제 제공업체에서 Dodo Payments로 데이터를 쉽게 마이그레이션하세요.
icon: arrow-right-arrow-left
keywords:
  - Dodo Payments
  - 마이그레이션 가이드
  - 결제 마이그레이션
  - 프로바이더 전환
  - Dodo Payments로 마이그레이션
  - 라이선스 키 마이그레이션
---

**다른 결제 제공업체에서 Dodo Payments로 전환하시나요?** 우리의 마이그레이션 도구를 사용하면 **데이터 손실 없이 제품, 고객 및 할인 코드를 쉽게 이동할 수 있습니다.** 이 과정은 안전하고 안내가 있으며 몇 분 안에 완료할 수 있습니다.

현재 **Lemon Squeezy, Stripe, Polar.sh, Paddle**에서의 마이그레이션을 지원합니다.

### 지원되는 마이그레이션

<CardGroup cols={3}>
  <Card title="Products & Pricing" icon="box">
    모든 제품과 가격 세부 정보를 마이그레이션하세요.
  </Card>
  <Card title="Customer Data" icon="users">
    연락처 정보를 포함한 전체 고객 데이터베이스를 이전하세요.
  </Card>
  <Card title="Discount Codes & Promotions" icon="tag">
    모든 할인 코드와 프로모션 혜택을 원활하게 이전하세요.
  </Card>
</CardGroup>

## 시작하기

먼저, 마이그레이션 도구를 설치하세요:

```bash
npm install -g dodo-migrate
```

<Note>
컴퓨터에 Node.js ≥ 18이 설치되어 있어야 합니다.
</Note>

## 데이터 마이그레이션하기

### Lemon Squeezy에서 오는 경우:

```bash
dodo-migrate lemonsqueezy
```

### Stripe에서 오는 경우:

```bash
dodo-migrate stripe
```

### Polar.sh에서 오는 경우:

```bash
dodo-migrate polar
```

### Paddle에서 오는 경우:

```bash
dodo-migrate paddle
```

이 도구는 모든 단계를 안내합니다. 필요 사항은 다음과 같습니다:
- 제공업체의 API 키 (Lemon Squeezy, Stripe, Polar.sh, Paddle 등)
- Dodo Payments API 키  
- 먼저 테스트할지 (추천) 또는 라이브로 진행할지
- 마이그레이션할 Dodo Payments 브랜드

## 왜 우리의 마이그레이션 도구를 사용해야 할까요?

- **데이터 손실 없음** - 모든 것이 안전하게 전송됩니다.
- **먼저 테스트** - 라이브로 진행하기 전에 테스트 모드에서 시도해 보세요.
- **안내된 과정** - 각 단계를 안내합니다.
- **필요 시 재개** - 문제가 발생하면 중단한 지점에서 다시 시작할 수 있습니다.

## 고급 사용법

프롬프트 없이 마이그레이션을 실행하려면 모든 세부정보를 미리 제공할 수 있습니다:

### Lemon Squeezy 마이그레이션:

```bash
dodo-migrate lemonsqueezy \
  --provider-api-key=lsq_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX
```

### Stripe 마이그레이션:

```bash
dodo-migrate stripe \
  --provider-api-key=sk_test_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX \
  --migrate-types=products,coupons
```

### Polar.sh 마이그레이션:

```bash
dodo-migrate polar \
  --provider-api-key=polar_org_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX \
  --migrate-types=products,discounts,customers
```

### Paddle 마이그레이션:

```bash
dodo-migrate paddle \
  --provider-api-key=paddle_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX
```

## 도움이 필요하신가요?

- **도구 도움말**: `dodo-migrate --help`
- **Lemon Squeezy 도움말**: `dodo-migrate lemonsqueezy --help`
- **Stripe 도움말**: `dodo-migrate stripe --help`
- **Polar.sh 도움말**: `dodo-migrate polar --help`
- **Paddle 도움말**: `dodo-migrate paddle --help`
- **커뮤니티 지원**: [Discord](https://discord.gg/bYqAp4ayYh)
- **문제 신고**: [GitHub](https://github.com/dodopayments/dodo-migrate)

## 도구 업데이트

```bash
# Update to latest version
npm update -g dodo-migrate

# Remove the tool
npm uninstall -g dodo-migrate
```

- 라이선스 키 문자열
- 활성화 제한
- 만료 날짜
- 고객 및 제품 연결

### 마이그레이션되지 않는 항목

- **라이선스 키 활성화** — 마이그레이션 후 고객은 기기에서 다시 활성화해야 합니다.
- **철회되거나 비활성화된 키** — 활성 키만 마이그레이션됩니다.
- 일치하는 제품이나 고객이 없는 키는 경고와 함께 건너뜁니다.

{/* LOCKED_PATTERN_317ec56569e36d0c9e56c2648890a76e */}
중복 라이선스 키는 적절하게 처리됩니다. 마이그레이션을 다시 실행하는 경우 이미 가져온 키는 감지되어 건너뛰므로 안전하게 재시도할 수 있습니다.
{/* LOCKED_PATTERN_4dec52ce04aa8849a8a60508baae30ae */}

## 도움이 필요하십니까?

- **도구 도움말**: `dodo-migrate --help`
- **레몬 스퀴지 도움말**: `dodo-migrate lemonsqueezy --help`
- **Stripe 도움말**: `dodo-migrate stripe --help`
- **Polar.sh 도움말**: `dodo-migrate polar --help`
- **Paddle 도움말**: `dodo-migrate paddle --help`
- **커뮤니티 지원**: [Discord](https://discord.gg/bYqAp4ayYh)
- **문제 보고**: [GitHub](https://github.com/dodopayments/dodo-migrate)

## 도구 업데이트

```bash
# Update to latest version
npm update -g dodo-migrate

# Remove the tool
npm uninstall -g dodo-migrate
```
