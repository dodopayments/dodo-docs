---
sidebarTitle: 개요
title: Webhooks
description: Dodo Payments에서 이벤트가 발생하면 실시간 알림을 받습니다. 워크플로를 자동화하고 즉시 이벤트를 전달하여
  시스템을 동기화된 상태로 유지하세요.
icon: box
keywords:
  - Dodo Payments
  - 개발자 가이드
  - webhooks
---

<Frame>
  <img src="/images/cover-images/Webhooks.webp" alt="Webhook Cover Image" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

Webhooks는 Dodo Payments 계정에서 특정 이벤트가 발생할 때 실시간 알림을 제공합니다. Webhooks를 사용하여 워크플로를 자동화하고, 데이터베이스를 업데이트하고, 알림을 전송하며, 시스템을 동기화된 상태로 유지할 수 있습니다.

<Info>
당사의 webhook 구현은 [Standard Webhooks](https://standardwebhooks.com/) 사양을 따르므로 업계 모범 사례 및 기존 webhook 라이브러리와의 호환성을 보장합니다.
</Info>

## 주요 기능

<CardGroup cols={2}>
  <Card title="Real-time Delivery" icon="bolt">
    이벤트가 발생하면 즉시 알림 수신
  </Card>
  <Card title="Secure by Default" icon="shield-check">
    HMAC SHA256 서명 검증 포함
  </Card>
  <Card title="Automatic Retries" icon="rotate">
    지수 백오프를 적용한 기본 재시도 로직
  </Card>
  <Card title="Event Filtering" icon="filter">
    필요한 이벤트만 구독
  </Card>
</CardGroup>

## 시작하기

<Info>
Dodo Payments 웹훅 포털이 네이티브 대시보드 환경으로 새롭게 구축되었습니다. 기존 엔드포인트, signing secret, signature verification, 이벤트 이름 및 webhook payload는 변경되지 않았습니다. 통합 작업은 필요하지 않습니다.
</Info>

<Note>
**각 기능의 위치.**

- **Developer → Webhooks 아래** — **Endpoints**, **Event catalog**, **Logs**, **Activity**, **Settings** 탭
- **개별 엔드포인트에서** — delivery 통계, signing secret 및 **Replay history**가 표시되는 **Overview** 탭과 **Testing**, **Advanced** 탭 및 일괄 재생 작업
- **메시지에서** — **Logs** 탭에서 열 수 있으며, 엔드포인트를 열지 않고도 각 delivery attempt를 개별적으로 재생할 수 있습니다.
</Note>

<Steps>
<Step title="Access Webhook Settings">
    Dodo Payments Dashboard로 이동한 다음 **Developer → Webhooks**로 이동합니다.
</Step>

<Step title="Create Webhook Endpoint">
    **Add endpoint**를 클릭하여 엔드포인트 생성 side sheet를 엽니다.
</Step>

<Step title="Enter Endpoint URL or Choose Integration">
    webhook 이벤트를 수신할 URL을 입력하거나 integration connector를 선택하여 이벤트를 타사 서비스(Slack, Discord, Zapier, Resend 등)로 라우팅합니다.
</Step>

<Step title="Select Events to Receive">
    엔드포인트가 수신할 특정 이벤트를 선택합니다. 이벤트는 리소스별로 그룹화된 검색 가능한 트리로 구성됩니다. 개별 이벤트를 선택하거나 상위 리소스를 선택하여 관련된 모든 이벤트를 수신할 수 있습니다.
    
    <Tip>
    선택한 이벤트만 엔드포인트로 webhook을 트리거하므로 불필요한 트래픽과 처리를 줄일 수 있습니다.
    </Tip>
</Step>

<Step title="Create Endpoint">
    **Create endpoint**를 클릭하여 구성을 저장합니다.
</Step>

<Step title="Get Secret Key">
    webhook signing secret은 엔드포인트의 **Overview** 탭에 표시됩니다. 수신한 webhook의 진위를 확인할 때 사용합니다.
    
    <Warning>
    webhook secret key를 안전하게 보관하고 클라이언트 측 코드나 공개 repository에 절대 노출하지 마세요.
    </Warning>
</Step>

<Step title="Rotate Secret (Optional)">
    보안을 강화해야 하는 경우 webhook secret을 교체할 수 있습니다. **Overview** 탭에서 secret 옆의 **Rotate secret**을 클릭합니다.
    
    <Warning>
    secret을 교체하면 기존 secret이 만료되고 새 secret으로 대체됩니다. 이전 secret은 다음 24시간 동안만 유효합니다. 이후 이전 secret으로 verification을 시도하면 실패합니다.
    </Warning>
    
    <Info>
    secret이 유출되었다고 의심되는 경우 또는 정기적으로 secret rotation을 사용하세요.
    </Info>
</Step>
</Steps>

## Integration Connectors

직접 webhook receiver를 구축하는 대신 integration connector를 사용하여 webhook 이벤트를 타사 서비스로 바로 라우팅할 수 있습니다. 이를 통해 널리 사용되는 플랫폼을 위한 custom webhook handler를 작성하고 유지 관리할 필요가 없습니다.

### Connector 작동 방식

connector는 Dodo Payments 이벤트를 대상 서비스가 요구하는 형식으로 변환합니다. 제공해야 하는 세부 정보는 대상에 따라 다릅니다.

| Connector type | 제공할 정보 | 대상 |
|----------------|------------------|--------------|
| **Incoming webhook URL** | 공급업체의 자체 대시보드에서 생성한 webhook URL입니다. API key는 필요하지 않습니다. | [Slack](/integrations/slack), [Discord](/integrations/discord), [Microsoft Teams](/integrations/microsoft-teams), [Zapier](/integrations/zapier), [Windmill](/integrations/windmill) |
| **Vendor API** | API key입니다. 대상 URL은 connector template에서 가져옵니다. | [Resend](/integrations/resend), [SendGrid](/integrations/sendgrid), [Segment](/integrations/segment), [Customer.io](/integrations/customer-io), [HubSpot](/integrations/hubspot), [Close CRM](/integrations/close-crm), [Loops](/integrations/loops) |

대시보드의 connector picker에는 현재 비즈니스에서 이용할 수 있는 전체 목록이 표시됩니다. 따라서 위 표는 전체 목록이 아니라 단계별 설정 안내가 제공되는 대상 목록으로 참고하세요. 이벤트가 각 대상에 도달한 후 수행할 수 있는 작업은 [External Integrations](/integrations/introduction)을 참조하세요.

### Connector 설정

엔드포인트를 생성하거나 편집할 때 connector를 선택하면 side sheet에 해당 대상에 맞는 설정 안내가 표시됩니다. 예를 들어 Slack에서 incoming webhook URL을 생성하는 방법이나 Resend API key를 찾는 위치가 안내됩니다. 저장하기 전에 connector transformation test를 실행하여 이벤트가 대상에 맞게 올바르게 변환되는지 확인하세요.

<Tip>
코드를 작성하지 않고 지원되는 대상에 연결하려면 connector를 사용하세요. custom logic이 필요한 경우에는 대신 [transformation](#transformations)이 적용된 standard endpoint를 사용하세요.
</Tip>

## 구독 이벤트 구성

각 webhook endpoint가 수신할 특정 이벤트를 구성할 수 있습니다.

<Steps>
<Step title="Navigate to Webhook Endpoints">
    Dodo Payments Dashboard로 이동한 다음 **Developer → Webhooks**로 이동합니다.
</Step>

<Step title="Select Your Endpoint">
    구성할 webhook endpoint를 클릭합니다.
</Step>

<Step title="Open Event Configuration">
    **Edit**를 클릭하여 endpoint configuration side sheet를 엽니다.
</Step>

<Step title="Browse Event Types">
    event type selector에는 리소스별로 그룹화된 검색 가능한 트리 형태로 사용 가능한 모든 webhook 이벤트가 표시됩니다(예: `payment`, `subscription`, `dispute`). 검색창을 사용하면 이름이나 keyword로 특정 이벤트를 빠르게 찾을 수 있습니다.
</Step>

<Step title="Select Events">
    수신할 이벤트 옆의 checkbox를 선택합니다. 다음과 같이 할 수 있습니다.
    - 개별 이벤트 선택(예: `payment.succeeded`, `payment.failed`)
    - 상위 리소스를 선택하여 관련된 모든 이벤트 수신
    - 필요에 따라 특정 이벤트 조합
</Step>

<Step title="Save Configuration">
    **Save**를 클릭하여 변경 사항을 적용하거나 **Cancel**을 클릭하여 수정 사항을 취소합니다.
</Step>
</Steps>

<Warning>
모든 이벤트를 선택 해제하면 webhook endpoint는 어떤 notification도 수신하지 않습니다. 애플리케이션이 정상적으로 작동하는 데 필요한 이벤트를 하나 이상 선택하세요.
</Warning>

## 이벤트 카탈로그

**Developer → Webhooks**로 이동하여 **Event catalog** 탭을 엽니다. 이 탭에는 Dodo Payments가 전송할 수 있는 모든 이벤트 유형이 표시되므로 endpoint를 구독하기 전에 사용 가능한 이벤트를 확인할 수 있습니다. 이벤트를 선택하면 schema와 example payload를 볼 수 있으며, 읽으려는 field의 형식을 확인하는 가장 빠른 방법입니다.

<Card title="Webhook Events Guide" icon="list" href="/developer-resources/webhooks/intents/webhook-events-guide">
동일한 이벤트를 리소스별로 그룹화된 reference documentation으로 찾아볼 수 있습니다.
</Card>

## Webhook Delivery

### Timeout

webhook에는 connection 및 read operation 모두에 대해 **15초 timeout window**가 적용됩니다. timeout을 방지하려면 endpoint가 빠르게 응답하도록 하세요.

<Tip>
webhook은 수신 즉시 `200` status code로 수신을 확인한 후, 실제 처리는 background에서 수행하는 비동기 방식으로 처리하세요.
</Tip>

### Automatic Retries

webhook delivery가 실패하면 Dodo Payments는 시스템에 과부하가 발생하지 않도록 exponential backoff를 사용해 자동으로 retry합니다.

| Attempt | Delay | Description |
|---------|------------------------------|-------------|
| 1 | Immediately | 첫 번째 retry는 즉시 수행됩니다 |
| 2 | 5 seconds | 짧은 지연 후 두 번째 attempt가 수행됩니다 |
| 3 | 5 minutes | backoff가 증가한 상태로 세 번째 attempt가 수행됩니다 |
| 4 | 30 minutes | backoff가 계속 적용되며 네 번째 attempt가 수행됩니다 |
| 5 | 2 hours | 더 긴 지연 후 다섯 번째 attempt가 수행됩니다 |
| 6 | 5 hours | 더 긴 지연 후 여섯 번째 attempt가 수행됩니다 |
| 7 | 10 hours | 최대 지연 후 일곱 번째 attempt가 수행됩니다 |
| 8 | 10 hours | **최종 attempt** - 실패하면 webhook이 failed로 표시됩니다 |

<Info>
webhook event당 **retry는 최대 8회**입니다. 예를 들어 webhook이 성공하기 전에 세 번 실패하면 첫 번째 attempt부터 전체 delivery 시간은 약 35분 5초입니다.
</Info>

<Tip>
Dodo Payments dashboard에서 언제든 개별 message를 수동으로 retry하거나 실패한 모든 message를 일괄 복구할 수 있습니다.
</Tip>

### Idempotency

각 webhook event에는 고유한 `webhook-id` header가 포함됩니다. 이 identifier를 사용하여 idempotency를 구현하고 중복 처리를 방지하세요.

```javascript
// Example: Storing webhook IDs to prevent duplicate processing
const processedWebhooks = new Set();

app.post('/webhook', (req, res) => {
  const webhookId = req.headers['webhook-id'];
  
  if (processedWebhooks.has(webhookId)) {
    return res.status(200).json({ received: true });
  }
  
  processedWebhooks.add(webhookId);
  // Process the webhook...
});
```

<Warning>
idempotency check를 항상 구현하세요. retry로 인해 동일한 event를 여러 번 수신할 수 있습니다.
</Warning>

### Event Ordering

retry 또는 network condition으로 인해 webhook event가 순서대로 도착하지 않을 수 있습니다. 어떤 순서로든 event를 처리할 수 있도록 시스템을 설계하세요.

<Info>
webhook event가 처음 생성된 시점과 관계없이 **delivery 시점의 최신 payload**를 수신합니다.
</Info>

## Webhook 보안

webhook의 보안을 유지하려면 항상 payload를 validate하고 HTTPS를 사용하세요.

### Signature 확인

각 webhook request에는 `webhook-signature` header가 포함됩니다. 이 header는 secret key로 서명된 webhook payload와 timestamp의 HMAC SHA256 signature입니다.

#### SDK verification (권장)

모든 공식 SDK에는 수신한 webhook을 안전하게 validate하고 parse할 수 있는 built-in helper가 포함되어 있습니다. 다음 두 가지 method를 사용할 수 있습니다.

* `unwrap()`: webhook secret key를 사용하여 signature를 verify합니다
* `unsafe_unwrap()`: verification 없이 payload를 parse합니다

<Tip>
  Dodo Payments client를 초기화할 때 `DODO_PAYMENTS_WEBHOOK_KEY`를 통해 webhook secret을 제공합니다.
</Tip>

<CodeGroup>

```typescript TypeScript/Node.js
import DodoPayments from 'dodopayments';
import express from 'express';

const app = express();
app.use(express.raw({ type: 'application/json' }));

const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: process.env.DODO_PAYMENTS_ENVIRONMENT,
  webhookKey: process.env.DODO_PAYMENTS_WEBHOOK_KEY,
});

app.post('/webhook', async (req, res) => {
  try {
    const unwrapped = client.webhooks.unwrap(req.body.toString(), {
      headers: {
        'webhook-id': req.headers['webhook-id'] as string,
        'webhook-signature': req.headers['webhook-signature'] as string,
        'webhook-timestamp': req.headers['webhook-timestamp'] as string,
      },
    });
    res.json({ received: true });
  } catch (error) {
    res.status(401).json({ error: 'Invalid signature' });
  }
});
```

```python Python
from fastapi import FastAPI, Request, HTTPException
from dodopayments import DodoPayments
import os

app = FastAPI()
client = DodoPayments(
    bearer_token=os.getenv("DODO_PAYMENTS_API_KEY"),
    environment=os.getenv("DODO_PAYMENTS_ENVIRONMENT"),
    webhook_key=os.getenv("DODO_PAYMENTS_WEBHOOK_KEY"),
)

@app.post("/webhook")
async def handle_webhook(request: Request):
    try:
        unwrapped = client.webhooks.unwrap(
            await request.body(),
            headers={
                "webhook-id": request.headers.get("webhook-id", ""),
                "webhook-signature": request.headers.get("webhook-signature", ""),
                "webhook-timestamp": request.headers.get("webhook-timestamp", ""),
            },
        )
        return {"received": True}
    except Exception:
        raise HTTPException(status_code=401, detail="Invalid signature")
```

```go Go
import (
	"context"
	"io"
	"net/http"
	"os"
	"github.com/dodopayments/dodopayments-go"
	"github.com/dodopayments/dodopayments-go/option"
)

func webhookHandler(w http.ResponseWriter, r *http.Request) {
	client := dodopayments.NewClient(
		option.WithBearerToken(os.Getenv("DODO_PAYMENTS_API_KEY")),
		option.WithEnvironment(os.Getenv("DODO_PAYMENTS_ENVIRONMENT")),
		option.WithWebhookKey(os.Getenv("DODO_PAYMENTS_WEBHOOK_KEY")),
	)
	rawBody, _ := io.ReadAll(r.Body)
	if _, err := client.Webhooks.Unwrap(context.Background(), rawBody, map[string]string{
		"webhook-id":       r.Header.Get("webhook-id"),
		"webhook-signature": r.Header.Get("webhook-signature"),
		"webhook-timestamp": r.Header.Get("webhook-timestamp"),
	}); err != nil {
		http.Error(w, "Invalid signature", http.StatusUnauthorized)
		return
	}
	w.WriteHeader(http.StatusOK)
}
```

</CodeGroup>

#### Manual verification (대안)

SDK를 사용하지 않는 경우 Standard Webhooks spec에 따라 직접 signature를 verify할 수 있습니다.

1. `webhook-id`, `webhook-timestamp` 및 정확한 raw stringified `payload`를 마침표( `.`)로 구분하여 연결해 signed message를 구성합니다.
2. Dashboard의 webhook secret key를 사용하여 해당 문자열의 HMAC SHA256을 계산합니다.
3. 계산한 signature를 `webhook-signature` header와 비교합니다. 일치하면 webhook은 authentic합니다.

<Info>
  Standard Webhooks specification을 따릅니다. signature verification에는 해당 library를 사용할 수 있습니다: [https://github.com/standard-webhooks/standard-webhooks/tree/main/libraries](https://github.com/standard-webhooks/standard-webhooks/tree/main/libraries). event payload format은 [Webhook Payload](/developer-resources/webhooks/intents/payment)을 참조하세요.
</Info>

### Webhook 응답

* webhook handler는 event 수신을 확인하기 위해 `2xx status code`를 반환해야 합니다.
* 그 외의 모든 response는 failure로 처리되며 webhook은 retry됩니다.

### Best Practices

<AccordionGroup>
<Accordion title="Use HTTPS endpoints only" icon="lock">
  webhook endpoint에는 항상 HTTPS URL을 사용하세요. HTTP endpoint는 man-in-the-middle attack에 취약하며 webhook data를 노출합니다.
</Accordion>

<Accordion title="Respond immediately" icon="bolt">
  webhook을 수신하는 즉시 `200` status code를 반환하세요. timeout을 방지하려면 event를 비동기적으로 처리합니다.
  

  ```javascript
  app.post('/webhook', async (req, res) => {
    // Acknowledge receipt immediately
    res.status(200).json({ received: true });
    
    // Process asynchronously
    processWebhookAsync(req.body).catch(console.error);
  });
  ```

</Accordion>

<Accordion title="Handle duplicate events" icon="rotate">
  `webhook-id` header를 사용하여 idempotency를 구현하면 동일한 event를 여러 번 side effect 없이 안전하게 처리할 수 있습니다.
</Accordion>

<Accordion title="Secure your webhook secret" icon="key">
  environment variable 또는 secrets manager를 사용하여 webhook secret을 안전하게 저장하세요. secret을 version control에 절대 commit하지 마세요.
</Accordion>
</AccordionGroup>

## Webhook Payload Structure

webhook payload structure를 이해하면 event를 올바르게 parse하고 처리할 수 있습니다.

### Request Format

```http
POST /your-webhook-url
Content-Type: application/json
```

### Headers

<ParamField header="webhook-id" type="string" required>
이 webhook event의 고유 identifier입니다. idempotency check에 사용하세요.
</ParamField>

<ParamField header="webhook-signature" type="string" required>
webhook authenticity를 확인하기 위한 HMAC SHA256 signature입니다.
</ParamField>

<ParamField header="webhook-timestamp" type="string" required>
webhook이 전송된 시점의 Unix timestamp(초)입니다.
</ParamField>

### Request Body

<ResponseField name="business_id" type="string" required>
Dodo Payments business identifier입니다.
</ResponseField>

<ResponseField name="type" type="string" required>
이 webhook을 트리거한 event type입니다(예: `payment.succeeded`, `subscription.active`).
</ResponseField>

<ResponseField name="timestamp" type="string" required>
event가 발생한 시점의 ISO 8601 형식 timestamp입니다.
</ResponseField>

<ResponseField name="data" type="object" required>
event에 대한 상세 정보를 포함하는 event별 payload입니다.

<Expandable title="Data object properties">
  <ResponseField name="payload_type" type="string">
  리소스 type입니다. 다음 중 하나입니다: `Payment`, `Subscription`, `Refund`, `Dispute`, `LicenseKey`, `CreditLedgerEntry`, `CreditBalanceLow`, `AbandonedCheckout`, `DunningAttempt`, `EntitlementGrant` 또는 `Payout`입니다.
  </ResponseField>
  
  추가 field는 event type에 따라 달라집니다. 전체 schema는 event별 documentation을 참조하세요.
</Expandable>
</ResponseField>

### Example Payload

```json
{
  "business_id": "string",
  "type": "payment.succeeded | payment.failed |...",
  "timestamp": "2024-01-01T12:00:00Z",
  "data": {
    "payload_type": "Payment | Subscription | Refund | Dispute | LicenseKey | CreditLedgerEntry | CreditBalanceLow | AbandonedCheckout | DunningAttempt | EntitlementGrant | Payout",
    // ... event-specific fields (see below)
  }
}
```

<CardGroup cols={2}>
  <Card title="Event Types" icon="list" href="/developer-resources/webhooks/intents/webhook-events-guide">
    사용 가능한 모든 webhook event type 찾아보기
  </Card>
  <Card title="Event Payloads" icon="code" href="/developer-resources/webhooks/intents/payment">
    각 event의 상세 payload schema 보기
  </Card>
  <Card title="Handle Payment Failures" icon="screwdriver-wrench" href="/developer-resources/handle-payment-failures">
    `payment.failed`에 대응하고 declined payment 복구하기
  </Card>
</CardGroup>

## Webhook 테스트

production으로 전환하기 전에 Dodo Payments dashboard에서 webhook integration을 직접 테스트하여 endpoint가 올바르게 작동하는지 확인할 수 있습니다.

<Steps>
<Step title="Navigate to Webhooks">
    Dodo Payments Dashboard로 이동한 다음 **Developer → Webhooks**로 이동합니다.
</Step>

<Step title="Select Your Endpoint">
    webhook endpoint를 클릭하여 details page를 엽니다.
</Step>

<Step title="Open Testing Tab">
    **Testing** 탭을 클릭하여 webhook testing interface를 엽니다.
</Step>
</Steps>

### Example Event 전송

**Testing** 탭은 receiver를 확인할 수 있도록 이 endpoint로 sample payload를 전송합니다.

<Steps>
<Step title="Select Event Type">
    **Select an event type**를 사용하여 테스트할 event를 선택합니다(예: `payment.succeeded` 또는 `payment.failed`).
</Step>

<Step title="Send Example">
    **Send example**을 클릭합니다. sample payload는 실제 event와 동일하게 endpoint URL로 전송되며 같은 방식으로 서명됩니다.
    
    <Warning>
    Testing 탭에서 전송된 failed message는 retry되지 않습니다. receiver 확인에 사용하고 retry schedule 테스트에는 사용하지 마세요.
    </Warning>
</Step>

<Step title="Check Your Endpoint">
    이 탭에는 **Last example sent**가 전송된 시간이 기록됩니다. event가 도착했는지, signature verification이 통과했는지, `2xx` status code를 반환했는지 확인하세요.
</Step>
</Steps>

### 구현 예시

다음은 webhook verification 및 handling을 보여주는 완전한 Express.js 구현입니다.

<CodeGroup>

```typescript Express.js Webhook Handler
import { Webhook } from "standardwebhooks";
import express from "express";

const app = express();
app.use(express.json());

const webhook = new Webhook(process.env.DODO_WEBHOOK_SECRET);

app.post('/webhook/dodo-payments', async (req, res) => {
  try {
    // Extract webhook headers
    const webhookHeaders = {
      "webhook-id": req.headers["webhook-id"] as string,
      "webhook-signature": req.headers["webhook-signature"] as string,
      "webhook-timestamp": req.headers["webhook-timestamp"] as string,
    };

    // Verify the webhook signature
    const payload = JSON.stringify(req.body);
    await webhook.verify(payload, webhookHeaders);
    
    // Acknowledge receipt immediately
    res.status(200).json({ received: true });
    
    // Process webhook asynchronously
    processWebhookAsync(req.body).catch(console.error);
    
  } catch (error) {
    console.error('Webhook verification failed:', error);
    res.status(400).json({ error: 'Invalid signature' });
  }
});

async function processWebhookAsync(data: any) {
  // Handle the webhook event based on type
  switch (data.type) {
    case 'payment.succeeded':
      await handlePaymentSucceeded(data);
      break;
    case 'subscription.active':
      await handleSubscriptionActive(data);
      break;
    // Add more event handlers...
  }
}
```

```javascript Testing Webhooks Locally
const { Webhook } = require("standardwebhooks");
const axios = require("axios");
const crypto = require("crypto");

// Generate test webhook
const timestamp = new Date();
const webhookId = crypto.randomUUID();
const payload = {
  business_id: "biz_test123",
  type: "payment.succeeded",
  timestamp: timestamp.toISOString(),
  data: {
    payload_type: "Payment",
    payment_id: "pay_test456",
    amount: 2999
  }
};

// Sign the payload
const webhookSecret = process.env.DODO_WEBHOOK_SECRET;
const webhookInstance = new Webhook(webhookSecret);
const payloadString = JSON.stringify(payload);
const signature = webhookInstance.sign(webhookId, timestamp, payloadString);

// Send test webhook
const response = await axios.post('http://localhost:3000/webhook/dodo-payments', payload, {
  headers: {
    "webhook-id": webhookId,
    "webhook-timestamp": Math.floor(timestamp.getTime() / 1000),
    "webhook-signature": signature,
    "Content-Type": "application/json"
  }
});

console.log("Test webhook sent successfully:", response.data);
```

</CodeGroup>

<Tip>
production event를 처리하기 전에 dashboard testing interface를 사용하여 webhook handler를 충분히 테스트하세요. 이를 통해 문제를 조기에 발견하고 해결할 수 있습니다.
</Tip>

## CLI로 Webhook 테스트

[Dodo Payments CLI](/developer-resources/sdks/cli)는 terminal을 벗어나지 않고 local development 중 webhook을 테스트할 수 있는 두 가지 command를 제공합니다.

### 로컬에서 Live Webhook 수신

test mode account의 실제 webhook event를 local development server로 실시간 forwarding합니다.

```bash
dodo wh listen
```

CLI는 Dodo Payments에 WebSocket connection을 열고 모든 webhook event를 local endpoint(예: `http://localhost:3000/webhook`)로 forwarding합니다. verification 테스트에 필요한 signature header를 포함한 모든 header가 유지됩니다.

<Info>
listener는 **test mode** API key에서만 작동합니다. 이 command를 사용하기 전에 `dodo login`를 실행하고 Test Mode를 선택하세요.
</Info>

### Mock Webhook Event 트리거

실제 transaction을 생성하지 않고 어떤 endpoint로든 mock webhook payload를 전송합니다.

```bash
dodo wh trigger
```

이 interactive tool을 사용하면 event type을 선택하여 현실적인 mock payload를 endpoint로 전송할 수 있습니다. 반복 실행되므로 한 session에서 여러 event를 테스트할 수 있습니다.

trigger command는 Dodo Payments가 제공하는 전체 46개 event type을 지원하며 subscription, payment, refund, dispute, license key, payout, credit, abandoned checkout, dunning, entitlement grant family가 포함됩니다. 정확한 목록은 [Supported Webhook Events](/developer-resources/sdks/cli#supported-webhook-events)를 참조하세요.

<Warning>
`dodo wh trigger`의 mock webhook payload에는 signature가 없습니다. 테스트 중인 webhook handler에서는 `unwrap()` 대신 `unsafe_unwrap()`를 사용하세요.
</Warning>

<Card title="CLI Webhook Testing Docs" icon="terminal" href="/developer-resources/sdks/cli#webhooks">
  전체 CLI webhook testing documentation 보기
</Card>

## Advanced Settings

**Advanced** 탭에서는 webhook endpoint 동작을 세부 조정할 수 있는 추가 configuration option을 제공합니다.

### Rate Limiting (Throttling)

시스템 과부하를 방지하도록 webhook event가 endpoint로 delivery되는 rate를 제어합니다.

<Steps>
<Step title="Open Advanced Tab">
    endpoint details page에서 **Advanced** 탭을 클릭합니다.
</Step>

<Step title="Configure Rate Limit">
    "Rate Limit (throttling)" section에서 **Edit**를 클릭하여 rate limit settings를 수정합니다.
    
    <Info>
    기본적으로 webhook에는 rate limit이 적용되지 않으므로 event가 발생하는 즉시 delivery됩니다.
    </Info>
</Step>

<Step title="Set Your Limit">
    원하는 rate limit을 구성하여 webhook delivery frequency를 제어하고 시스템 overload를 방지합니다.
</Step>
</Steps>

<Tip>
webhook handler가 event를 처리할 시간이 필요하거나 여러 event를 batch로 묶으려는 경우 rate limiting을 사용하세요.
</Tip>

### Custom Headers

endpoint로 전송되는 모든 webhook request에 custom HTTP header를 추가합니다. authentication, routing 또는 metadata 추가에 유용합니다.

<Steps>
<Step title="Add Headers">
    "Custom Headers" section에서 각 custom header의 **Key**와 **Value**를 입력합니다.
</Step>

<Step title="Add Multiple Headers">
    필요에 따라 **+** button을 클릭하여 custom header를 추가합니다.
</Step>
</Steps>

custom header는 이 endpoint로 전송되는 모든 webhook request에 포함됩니다.

### Transformations

transformation을 사용하면 webhook payload를 수정하고 다른 URL로 redirect할 수 있습니다. 이 강력한 기능으로 다음을 수행할 수 있습니다.

- 처리 전에 payload structure 수정
- content에 따라 webhook을 다른 endpoint로 routing
- payload에서 field 추가 또는 제거
- data format 변환

<Steps>
<Step title="Enable Transformations">
    **Enabled** switch를 켜서 transformation feature를 활성화합니다.
</Step>

<Step title="Configure Transformation">
    **Edit transformation**을 클릭하여 JavaScript로 transformation rule을 정의합니다.
</Step>

<Step title="Test Transformation">
    production으로 전환하기 전에 transformation test interface를 사용하여 transformation이 올바르게 작동하는지 확인합니다.
</Step>
</Steps>

<Warning>
transformation은 webhook delivery performance에 영향을 줄 수 있습니다. 충분히 테스트하고 transformation logic을 단순하고 효율적으로 유지하세요.
</Warning>

<Tip>
transformation은 특히 다음과 같은 경우에 유용합니다.
- 서로 다른 data format 간 변환
- 특정 기준에 따른 event filtering
- payload에 computed field 추가
- event를 서로 다른 microservice로 routing
</Tip>

## Webhook Logs 모니터링

**Logs** 탭에서는 webhook delivery status를 종합적으로 확인할 수 있어 webhook event를 효과적으로 모니터링, debug 및 관리할 수 있습니다.

<Steps>
<Step title="Navigate to Logs Tab">
    **Developer → Webhooks**로 이동하여 **Logs** 탭을 엽니다.
</Step>

<Step title="Browse Delivery History">
    Event type, Message ID, Event ID, Sent at, Attempted at, Response code, Duration column이 포함된 모든 webhook delivery attempt table을 확인합니다.
</Step>

<Step title="Search and Filter">
    search bar로 ID 또는 event type별 특정 message를 찾습니다. status(Succeeded, Failed, Pending 등)로 filter하여 조사할 event에 집중할 수 있습니다.
</Step>

<Step title="View Message Details">
    message를 클릭하여 다음 내용을 표시하는 message detail page를 엽니다.
    - 전체 webhook payload
    - response code와 duration이 포함된 모든 delivery attempt
    - 각 attempt의 timestamp
    - endpoint에서 반환된 error message

    각 attempt에는 **Replay** action이 있으므로 page를 벗어나지 않고 해당 message만 다시 전송할 수 있습니다.
</Step>
</Steps>

## Activity 모니터링

**Developer → Webhooks**로 이동하여 **Activity** 탭을 열면 endpoint 전체의 delivery performance를 확인할 수 있습니다.

**Delivery activity**는 시간에 따른 attempt를 표시하며 window에 따라 **Attempts per 5 minutes**, **Attempts per hour** 또는 **Attempts per day**로 구분됩니다. 각 bar는 outcome별로 나뉘며 segment 위에 마우스를 올리면 status, attempt 수 및 전체에서 차지하는 비율이 표시됩니다. endpoint의 **Overview** 탭에 있는 **Delivery stats (last 24h)**는 지난 하루 동안의 동일한 정보를 요약합니다.

<Tip>
**Endpoints** 탭의 **Error rate (24h)** column을 통해 endpoint를 열기 전에도 주의가 필요한 endpoint를 한눈에 확인할 수 있습니다.
</Tip>

## Message 재생 및 복구

message를 다시 전송하는 방법은 필요한 message 수에 따라 다릅니다.

- **One message** — **Logs** 탭에서 message를 열고 attempt의 **Replay** action을 사용합니다. endpoint를 열 필요가 없습니다.
- **A range of messages** — bulk mode는 한 번에 하나의 endpoint에만 적용되므로 endpoint를 엽니다.

### 일괄 재생

**Developer → Webhooks**에서 endpoint를 엽니다. 세 가지 mode를 사용할 수 있으며 각 mode는 해당 endpoint에만 적용됩니다. 설정할 range는 mode에 따라 다릅니다.

| Mode | What it does | What you set |
|------|--------------|--------------|
| **Recover failed messages** | 이 endpoint에서 실패한 모든 message를 재생합니다. | 시작점: **8 hours ago**, **Yesterday**, **3 days ago**, **Last week** 또는 **2 weeks ago** |
| **Replay missing messages** | 이 endpoint로 dispatch되지 않은 message를 재생합니다. 예를 들어 새 event type을 구독한 후 사용할 수 있습니다. | 동일한 시작점 |
| **Bulk replay messages** | 이미 성공적으로 delivery된 message를 포함하여 선택한 filter와 일치하는 message를 재생합니다. | 기본값이 최근 2주로 설정된 **Since** 및 **Until** 범위와 선택적 event type, channel 또는 tag |

<Steps>
<Step title="Open More Actions">
    endpoint에서 **More actions**를 열고 위 세 가지 mode 중 하나를 선택합니다.
</Step>

<Step title="Set the Range">
    table에 나열된 대로 선택한 mode에서 요구하는 range를 입력합니다.
</Step>

<Step title="Start the Run">
    선택한 mode에 따라 **Recover** 또는 **Replay**를 클릭합니다.
</Step>
</Steps>

각 실행은 endpoint의 **Overview** 탭에 있는 **Replay history**에 표시되며 mode, time range, status 및 다시 전송된 message 수가 함께 표시됩니다.

## Email Alerts

endpoint로의 webhook delivery가 실패할 때 email notification을 받아 문제가 backlog로 이어지기 전에 해결할 수 있습니다.

<Steps>
<Step title="Navigate to Settings Tab">
  **Developer → Webhooks**로 이동하여 **Settings** 탭을 엽니다.
</Step>

<Step title="Find Email Alerting">
  **Email alerting** card를 찾습니다.
</Step>

<Step title="Configure Email Addresses">
  alert를 수신할 address를 입력합니다. 여러 address는 comma로 구분하고, alert를 끄려면 field를 비워 둡니다.
</Step>

<Step title="Save">
  **Save**를 클릭하여 변경 사항을 적용합니다.
</Step>
</Steps>

<Tip>
email alert를 활성화하면 webhook delivery 문제를 조기에 파악하고 안정적인 integration을 유지할 수 있습니다.
</Tip>

## Cloud Platform에 배포

webhook handler를 production에 배포할 준비가 되었나요? 각 platform에 맞는 best practice와 함께 인기 cloud provider에 webhook을 배포할 수 있도록 platform별 guide를 제공합니다.

<CardGroup cols={2}>
  <Card title="Vercel" icon="triangle" href="/developer-resources/webhooks/examples/vercel-example">
    Vercel에 serverless function으로 webhook 배포
  </Card>
  <Card title="Cloudflare Workers" icon="cloud" href="/developer-resources/webhooks/examples/cloudflare-example">
    Cloudflare edge network에서 webhook 실행
  </Card>
  <Card title="Supabase Edge Functions" icon="database" href="/developer-resources/webhooks/examples/supabase-example">
    Supabase와 webhook 통합
  </Card>
  <Card title="Netlify Functions" icon="circle-nodes" href="/developer-resources/webhooks/examples/netlify-example">
    Netlify serverless function으로 webhook 배포
  </Card>
</CardGroup>

<Info>
각 platform guide에는 해당 provider에 맞는 environment setup, signature verification 및 deployment 단계가 포함되어 있습니다.
</Info>

## 관련 API Reference

<CardGroup cols={2}>
<Card title="Create Webhook" icon="code" href="/api-reference/webhooks/create-webhook">
  webhook endpoint를 programmatically 생성하고 구성하기 위한 API reference
</Card>
<Card title="List Webhooks" icon="list" href="/api-reference/webhooks/list-webhooks">
  webhook endpoint를 조회하고 관리하기 위한 API reference
</Card>
</CardGroup>