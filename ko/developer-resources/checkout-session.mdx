---
title: Checkout Sessions
description: Create secure, hosted checkout experiences that handle the complete
  payment flow for both one-time purchases and subscriptions with full
  customization control.
icon: cart-shopping
keywords:
  - Dodo Payments
  - developer guide
  - checkout sessions
---

<CardGroup cols={2}>
<Card title="Quick Start Guide" icon="rocket" href="#creating-your-first-checkout-session">
  Get your first checkout session running in under 5 minutes
</Card>

<Card title="API Reference & Live Testing" icon="code" href="/api-reference/checkout-sessions/create">
  Explore the full API documentation and interactively test Checkout Session requests and responses.
</Card>

<Card title="Preview Checkout" icon="eye" href="/api-reference/checkout-sessions/preview">
  Calculate pricing, taxes, and totals before creating a session.
</Card>
</CardGroup>

<Info>
**Session Validity**: Checkout sessions are valid for 24 hours by default. If you pass `confirm=true` in your request, the session will only be valid for 15 minutes.
</Info>

## Prerequisites

<Steps>
<Step title="Dodo Payments Account">
  You'll need an active Dodo Payments merchant account with API access.
</Step>

<Step title="API Credentials">
  Generate your API credentials from the Dodo Payments dashboard:
</Step>

<Step title="Products Setup">
  Create your products in the Dodo Payments dashboard before implementing checkout sessions.
</Step>
</Steps>

## Creating Your First Checkout Session

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### API Response

All methods above return the same response structure:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
<Step title="Get the checkout URL">
Extract the `checkout_url` from the API response.
</Step>

<Step title="Redirect your customer">
Direct your customer to the checkout URL to complete their purchase.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Alternative Integration Options**: Instead of redirecting, you can embed the checkout directly in your page using [Overlay Checkout](/developer-resources/overlay-checkout) (modal overlay) or [Inline Checkout](/developer-resources/inline-checkout) (fully embedded). Both options use the same checkout session URL.
</Tip>
</Step>

<Step title="Handle the return">
After payment, customers are redirected to your `return_url` with query parameters including payment/subscription ID, status, customer email, and any license keys. See the [`return_url` parameter docs](#request-body) for the full list.
</Step>
</Steps>

## Request Body

<CardGroup cols={2}>
<Card title="Required Fields" icon="asterisk">
  Essential fields needed for every checkout session
</Card>

<Card title="Optional Fields" icon="gear">
  Additional configuration to customize your checkout experience
</Card>
</CardGroup>

### Required Fields

<ParamField body="product_cart" type="array" required>
Array of products to include in the checkout session. Each product must have a valid `product_id` from your Dodo Payments dashboard.

<Tip>
**Mixed Checkout**: You can combine one-time payment products and subscription products in the same checkout session. This enables powerful use cases like setup fees with subscriptions, hardware bundles with SaaS, and more.
</Tip>

<Expandable title="Product Cart Item Properties">
<ParamField body="product_id" type="string" required>
The unique identifier of the product from your Dodo Payments dashboard.

**Example:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
Quantity of the product.

**Example:** `1` for single item, `3` for multiple quantities
</ParamField>

<ParamField body="addons" type="array">
Array of addons to attach to the product. Only valid if the product is a subscription.

<Expandable title="Addon item properties">
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
Amount the customer pays if pay_what_you_want is enabled. If disabled, this field will be ignored.

**형식**: 통화의 가장 낮은 단위로 표시됩니다 (예: USD의 경우 센트). 예를 들어, $1.00를 청구하려면 `100`를 전달합니다.
</ParamField>

{/* LOCKED_PATTERN_1cc543c3a04a4c1f6d9b021f4a2eca5e */}
이 제품에 이미 첨부된 크레딧 자격에 대한 세션당 오버라이드입니다. 별도의 제품을 생성하지 않고도 이 단일 세션에 대해 다른 크레딧 수를 부여하기 위해 사용하십시오 — 예: 제품 기본값 500 대신 1,000 API 크레딧을 부여하는 프로모션 번들.

<Info>
각 `credit_entitlement_id`는 이미 제품에 첨부되어 있어야 합니다. 이 필드는 이 세션이 완료될 때만 부여되는 `credits_amount`을 오버라이드합니다. 제품 수준의 크레딧 자격 설정(만료, 이월 등)은 여전히 적용됩니다.
</Info>

{/* LOCKED_PATTERN_b0d6df2f029cdb2dbad4427c93f9d7a5 */}
{/* LOCKED_PATTERN_3772971471266bd03aa44ad580de1758 */}
오버라이드할 크레딧 자격의 ID. 반드시 제품에 첨부되어 있어야 합니다.
</ParamField>

{/* LOCKED_PATTERN_7a9c0efdd7e8e3c5319e61171d2f9d22 */}
이 결제 세션을 위해 부여할 크레딧 수로, 제품 수준의 `credits_amount`를 오버라이드합니다. 반드시 0보다 커야 합니다.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

<Tip>
**제품 ID 찾기**: Dodo 결제 대시보드의 제품 → 세부정보 보기에서, 또는 [목록 제품 API](/api-reference/products/get-products)를 사용하여 제품 ID를 찾을 수 있습니다.
</Tip>

### 선택적 필드

이 필드를 구성하여 결제 경험을 사용자 지정하고 결제 흐름에 비즈니스 로직을 추가합니다.

<AccordionGroup>
<Accordion title="Customer Information">

<ParamField body="customer" type="object">
고객 정보. ID를 사용하여 기존 고객을 첨부하거나 결제 중에 새 고객 레코드를 생성할 수 있습니다.

<Tabs>
<Tab title="Attach Existing Customer">
ID를 사용하여 기존 고객을 결제 세션에 첨부합니다.

<Expandable title="Existing Customer Properties">
<ParamField body="customer_id" type="string" required>
기존 고객의 고유 식별자입니다. 새 고객을 생성하는 대신 기존 고객에게 결제 세션을 첨부하는 데 사용합니다.
</ParamField>
</Expandable>
</Tab>

<Tab title="New Customer">
결제 중에 새 고객 레코드를 생성합니다.

<Expandable title="New Customer Properties">
<ParamField body="email" type="string" required>
고객의 이메일 주소. 새 고객을 생성할 때 필요합니다.
</ParamField>

<ParamField body="name" type="string">
영수증과 송장에 표시될 고객의 전체 이름.

**예**: `"John Doe"` 또는 `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
국제 형식의 고객 전화번호. 일부 결제 방법 및 사기 방지를 위해 필요합니다.

**형식**: 국가 코드를 포함합니다, 예: US 번호의 경우 `"+1234567890"`
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
정확한 세금 계산, 사기 방지 및 규정 준수를 위한 청구 주소 정보.

<Info>
`confirm`가 `true`로 설정된 경우 모든 청구 주소 필드는 성공적인 세션 생성을 위해 필수 항목이 됩니다.
</Info>

<Expandable title="Billing address object properties">
<ParamField body="street" type="string">
집 번호, 거리 이름 및 아파트/유닛 번호를 포함한 완전한 거리 주소, 가능하면.

**예**: `"123 Main St, Apt 4B"` 또는 `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
도시 또는 자치구 이름.

**예**: `"San Francisco"`, `"New York"`, `"London"`
</ParamField>

<ParamField body="state" type="string">
주, 도, 지역 이름. 전체 이름 또는 표준 약어를 사용하십시오.

**예**: `"California"` 또는 `"CA"`, `"Ontario"` 또는 `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
두 자리 ISO 국가 코드 (ISO 3166-1 alpha-2). 청구 주소가 제공될 때 이 필드는 항상 필요합니다.

**예시들**: `"US"` (미국), `"CA"` (캐나다), `"GB"` (영국), `"DE"` (독일)

<Card title="Country Code Reference" icon="globe" href="/api-reference/misc/supported-countries">
  지원되는 국가와 ISO 코드의 전체 목록 보기
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
국가 요구사항에 따라 우편 번호, ZIP 코드 또는 동등한 것.

**예시들**: `"94102"` (미국), `"M5V 3A8"` (캐나다), `"SW1A 1AA"` (영국)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

<Accordion title="Payment Configuration">

<ParamField body="allowed_payment_method_types" type="array">
결제 중 고객에게 제공할 결제 방법을 제어합니다. 이는 특정 시장이나 비즈니스 요구에 맞게 최적화하는 데 도움이 됩니다.

**사용 가능한 옵션**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**중요**: 선호하는 결제 방법이 사용 불가능할 때 결제 실패를 방지하기 위해 항상 `credit`와 `debit`를 보조 옵션으로 포함하십시오.
</Warning>

**예**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
기본 통화 선택을 고정 청구 통화로 오버라이드합니다. ISO 4217 통화 코드를 사용합니다.

**지원 통화**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR`, 그리고 기타

**예시**: US 달러는 `"USD"`, 유로는 `"EUR"`

<Note>
이 필드는 적응 가격 책정이 활성화된 경우에만 효과적입니다. 적응 가격 책정이 비활성화된 경우 제품의 기본 통화가 사용됩니다.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
재방문 고객을 위해 이전에 저장된 결제 방법을 표시하여 결제 속도와 사용자 경험을 향상시킵니다.
</ParamField>

</Accordion>

<Accordion title="Session Management">

<ParamField body="return_url" type="string">
결제가 완료된 후 고객을 리디렉션할 URL. Dodo 결제 시스템은 리디렉션 시 URL에 다음 쿼리 매개변수를 추가합니다:

| 매개변수 | 유형 | 조건 |
|-----------|------|-----------|
| `payment_id` | `string` | 일회성 결제에 항상 존재 |
| `subscription_id` | `string` | 구독 결제에 항상 존재 |
| `status` | `string` | 항상 존재 |
| `license_key` | `string` | 제품에 라이선스 키가 활성화된 경우 존재. 여러 키가 있을 경우 쉼표로 구분 |
| `email` | `string` | 고객의 이메일 정보가 있는 경우 존재 |

**예시 리디렉션 URL:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
리턴 페이지에서 라이선스 키를 표시하거나 즉시 확인을 보낼 때 `license_key` 및 `email` 쿼리 매개변수를 사용하십시오. 추가 API 호출이 필요하지 않습니다.
</Tip>
</ParamField>

<ParamField body="cancel_url" type="string">
뒤로가기 버튼을 클릭하거나 결제 세션을 취소할 때 고객을 리디렉션할 URL. 제공되지 않으면 뒤로가기 버튼이 표시되지 않습니다.

<Tip>
구매를 완료하지 않고도 고객이 사이트로 돌아갈 수 있는 명확한 방법을 제공하기 위해 `cancel_url`를 설정하십시오. 이는 결제 경험을 개선하고 마찰을 줄입니다.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
이 값이 true인 경우, 모든 세션 세부 정보를 즉시 확정합니다. 필수 데이터가 누락된 경우 API는 오류를 발생시킵니다.
</ParamField>

{/* LOCKED_PATTERN_db6ea1a90bc6c05f9f398a4adeb2dd67 */}
결제 세션에 하나 이상의 누적 할인 코드를 적용하십시오. 코드는 **배열 순서**로 적용됩니다 (첫 번째 코드는 기본 가격을 줄이고, 두 번째 코드는 이미 할인된 가격을 줄이며, 이렇게 최대 **20**개 코드까지 가능).
</ParamField>

```typescript
discount_codes: ['WELCOME10', 'BLACKFRIDAY20']
```

<Info>
아래의 개별적 `discount_code` 필드는 **더 이상 사용되지 않음**이지만 여전히 완전히 지원됩니다 — 기존 통합은 변경 없이 작동합니다. `discount_codes`와 동일 요청에 결합할 수 없습니다. 누적 기능을 활용하려면 편리할 때 `discount_codes`로 전환하십시오.
</Info>
</ParamField>

{/* LOCKED_PATTERN_e63db2d6e61a978842d79e65089cf7dc */}
**더 이상 사용되지 않음** — 새로운 통합을 위해서는 `discount_codes`를 선호하십시오. 이 필드는 기존 호환성을 위한 것이지만 동일 요청에 `discount_codes`와 결합할 수 없습니다.
</ParamField>

<ParamField body="metadata" type="object">
세션에 대한 추가 정보를 저장하기 위한 사용자 지정 키-값 쌍입니다.
</ParamField>

<ParamField body="force_3ds" type="boolean">
이 세션에 대한 판매자 기본 3DS 동작을 무시합니다.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
최소 주소 수집 모드를 활성화합니다. 활성화되면, 결제에서는 다음 항목만 수집합니다:
- **국가**: 세금 결정을 위해 항상 필요
- **ZIP/우편 번호**: 판매세, VAT 또는 GST 계산이 필요한 지역에서만

이는 불필요한 양식 필드를 제거하여 결제 마찰을 크게 줄입니다.

<Tip>
빠른 결제 완료를 위해 최소 주소를 활성화합니다. 전체 주소 수집은 완전한 청구 세부정보가 필요한 기업을 위해 여전히 사용 가능합니다.
</Tip>
</ParamField>

</Accordion>

<Accordion title="UI Customization & Features">

<ParamField body="customization" type="object">
결제 인터페이스의 외관 및 동작을 사용자 지정합니다.

<Expandable title="Customization object properties">
<ParamField body="theme" type="string" default="system">
결제 인터페이스를 위한 테마. 옵션: `light`, `dark`, 또는 `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
결제 인터페이스에 주문 세부 정보 섹션을 표시합니다.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
관련 제품에 대해 "온 디맨드" 태그를 표시합니다.
</ParamField>

<ParamField body="force_language" type="string">
특정 언어로 결제 인터페이스를 강제로 표시합니다. 기본적으로 결제는 고객의 브라우저 언어를 자동 감지합니다.

**지원되는 언어 코드:**
`ar` (아랍어), `ca` (카탈루냐어), `de` (독일어), `en` (영어), `es` (스페인어), `fr` (프랑스어), `he` (히브리어), `id` (인도네시아어), `it` (이탈리아어), `ja` (일본어), `ko` (한국어), `ms` (말레이어), `nl` (네덜란드어), `pl` (폴란드어), `pt` (포르투갈어), `ro` (루마니아어), `ru` (러시아어), `sv` (스웨덴어), `th` (태국어), `tr` (터키어), `zh` (중국어)

**예시**: 스페인어의 경우 `"es"`, 일본어의 경우 `"ja"`
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
결제 세션에 대한 특정 기능 및 동작을 구성합니다.

<Expandable title="Feature flags object properties">
<ParamField body="allow_currency_selection" type="boolean" default="true">
결제 시 고객이 선호하는 통화를 선택할 수 있도록 허용합니다.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
결제 인터페이스에 할인 코드 입력 필드를 표시합니다.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
결제 중 고객 전화번호를 수집합니다.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
고객이 세금 식별 번호를 입력할 수 있도록 허용합니다.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
기존 고객 레코드를 업데이트하는 대신 새로운 고객 레코드를 강제로 생성합니다.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
결제 중에 고객이 이메일 주소를 편집할 수 있도록 허용합니다.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
결제 중에 고객이 이름을 편집할 수 있도록 허용합니다.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
결제 중에 고객이 거리 주소를 편집할 수 있도록 허용합니다.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
결제 중에 고객이 도시를 편집할 수 있도록 허용합니다.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
결제 중에 고객이 주를 편집할 수 있도록 허용합니다.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
결제 중에 고객이 국가를 편집할 수 있도록 허용합니다.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
결제 중에 고객이 우편번호를 편집할 수 있도록 허용합니다.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
최소 주소 수집 모드를 활성화합니다. 활성화되면, 결제에서는 다음 항목만 수집합니다:
- **국가**: 세금 결정을 위해 항상 필요
- **ZIP/우편 번호**: 판매세, VAT 또는 GST 계산이 필요한 지역에서만

이는 불필요한 양식 필드를 제거하여 결제 마찰을 크게 줄입니다.

<Tip>
빠른 결제 완료를 위해 최소 주소를 활성화합니다. 전체 주소 수집은 완전한 청구 세부정보가 필요한 기업을 위해 여전히 사용 가능합니다.
</Tip>
</ParamField>

<ParamField body="redirect_immediately" type="boolean" default="false">
기본 결제 성공 페이지를 건너뛰고 결제가 완료된 후 즉시 고객을 `return_url`로 리디렉션합니다.

<Tip>
사용자 경험을 향상시키는 사용자 지정 성공 페이지가 있거나 모바일 앱 및 내장 결제 흐름에 적합합니다.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

<Accordion title="Custom Fields">

<ParamField body="custom_fields" type="array">
결제 중 고객으로부터 추가 정보를 수집하기 위해 사용자 정의 필드 폼을 사용합니다. 결제 세션당 최대 5개의 사용자 정의 필드를 정의할 수 있습니다. 고객 응답은 웹훅 페이로드에 포함되고 API를 통해 제공됩니다.

<Expandable title="Custom field object properties">
<ParamField body="key" type="string" required>
이 필드의 고유 식별자입니다. 웹훅 페이로드 및 API 응답에서 키로 사용됩니다.

**예시**: `"company_name"`, `"referral_source"`, `"team_size"`
</ParamField>

<ParamField body="label" type="string" required>
결제 폼의 고객에게 표시되는 레이블.

**예시**: `"Company Name"`, `"How did you hear about us?"`, `"Team Size"`
</ParamField>

<ParamField body="field_type" type="string" required>
입력 검증 규칙을 결정하는 필드 유형.

**사용 가능한 유형**:
- `text` - 단일 라인 텍스트 입력
- `number` - 숫자 입력
- `email` - 유효성 검사가 있는 이메일 주소
- `url` - 유효성 검사가 있는 URL
- `date` - 날짜 선택기
- `dropdown` - 미리 정의된 옵션 중에서 선택
- `boolean` - 예/아니오 토글
</ParamField>

<ParamField body="required" type="boolean" default="false">
결제가 완료되기 전에 이 필드를 반드시 채워야 하는지 여부.

<ParamField body="placeholder" type="string">
입력 필드에 표시되는 자리 표시자 텍스트.

<ParamField body="options" type="array">
`dropdown` 필드 유형의 옵션 목록. `field_type`가 `dropdown`인 경우 필요하며, 다른 유형에는 무시됩니다.

**예시**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
사용자 정의 필드에 대한 고객 응답은 다음에 포함됩니다:
- **웹훅**: `payment.succeeded`, `subscription.active`, 기타 관련 이벤트 페이로드에 `custom_field_responses` 배열이 포함됩니다.
- **API 응답**: 결제 및 구독 객체에 `custom_field_responses`가 포함됩니다.
</Info>

</Accordion>

<Accordion title="Subscription Configuration">

<ParamField body="subscription_data" type="object">
구독 제품이 포함된 결제 세션에 대한 추가 구성.

<Expandable title="Subscription data object properties">
<ParamField body="trial_period_days" type="integer">
첫 번째 요금 전에 시범 기간의 일수입니다. 0에서 10,000일까지 설정 가능.
</ParamField>

<ParamField body="on_demand" type="object">
사용 기반 또는 계량 요금제를 위한 온디맨드 구독 구성.

<Expandable title="On-demand object properties">
<ParamField body="mandate_only" type="boolean" required>
true로 설정된 경우, 비용을 청구하지 않고 단지 결제 방법 세부 사항을 미래 사용을 위해 승인합니다.
</ParamField>

<ParamField body="product_price" type="integer">
초기 고객 요금에 대한 제품 가격입니다. 명시되지 않은 경우, 저장된 제품의 가격이 사용됩니다.

**형식**: 통화의 가장 낮은 단위로 표시됩니다 (예: USD의 경우 센트). 예를 들어, $1.00를 청구하려면 `100`를 전달합니다.
</ParamField>

<ParamField body="product_currency" type="string">
제품 가격의 선택적 통화입니다. 명시되지 않은 경우, 제품의 통화로 기본 설정됩니다.
</ParamField>

<ParamField body="product_description" type="string">
청구 및 품목에 대한 선택적 제품 설명 재정의입니다. 명시되지 않은 경우, 저장된 제품 설명이 사용됩니다.
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
적응형 통화 수수료를 제품 가격에 포함할지 (true) 아니면 추가할지 (false) 여부입니다. 적응형 가격 책정이 활성화되지 않은 경우 무시됩니다.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## 사용 예시

다양한 비즈니스 시나리오에 대한 체크아웃 세션 구성을 보여주는 10가지 종합 예시입니다:

### 1. 단순 단일 제품 결제

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. 다중 제품 장바구니

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. 시범 기간이 포함된 구독

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. 사전 확인 결제

<Note>
`confirm`가 `true`으로 설정되면, 고객은 확인 절차를 건너뛰고 바로 결제 페이지로 이동하게 됩니다.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. 통화 오버라이드가 포함된 결제

<Note>
`billing_currency` 오버라이드는 계정 설정에서 적응형 통화가 활성화된 경우에만 적용됩니다. 적응형 통화가 비활성화된 경우, 이 매개변수는 아무런 영향을 미치지 않습니다.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. 재방문 고객을 위한 저장된 결제 방법

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. 세금 ID 수집이 포함된 B2B 결제

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. 스택된 할인 코드가 있는 다크 테마 결제

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_codes: ['BLACKFRIDAY2024'],
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. 지역 결제 방법 (인도를 위한 UPI)

UPI 구성 및 테스트에 대한 자세한 정보는 <a href="/features/payment-methods/india">인도 결제 방법</a> 페이지를 참조하십시오.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. BNPL (구매 후 결제) 결제

BNPL 구성 및 테스트에 대한 자세한 정보는 <a href="/features/payment-methods/bnpl">구매 후 결제 (BNPL)</a> 페이지를 참조하십시오.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. 즉각적인 결제를 위한 기존 결제 방법 사용

고객의 저장된 결제 방법을 사용하여 결제 수단 수집을 건너뛰고 즉시 처리되는 체크아웃 세션을 생성하세요:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
`payment_method_id`를 사용할 때, `confirm`는 `true`로 설정해야 하며 기존 `customer_id`가 제공되어야 합니다. 결제의 통화와의 적격성을 위해 결제 방법이 검증될 것입니다.
</Warning>

<Info>
결제 방법은 고객의 것이며 결제 통화와 호환되어야 합니다. 이는 재방문 고객을 위한 원클릭 구매를 가능하게 합니다.
</Info>

### 12. 간결한 결제 URL을 위한 짧은 링크

사용자 정의 슬러그로 공유 가능한 결제 링크를 생성합니다:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
짧은 링크는 SMS, 이메일 또는 소셜 미디어 공유에 적합합니다. 긴 URL보다 기억하기 쉽고 고객 신뢰도를 높입니다.
</Tip>

### 13. 결제 성공 페이지를 건너뛰고 즉시 리디렉션

기본 성공 페이지를 건너뛰고 결제 완료 후 즉시 고객을 리디렉션합니다:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
기본 결제 성공 페이지보다 더 나은 사용자 경험을 제공하는 사용자 정의 성공 페이지를 사용할 때 `redirect_immediately: true`를 사용하십시오. 이는 모바일 앱 및 내장 체크아웃 흐름에 특히 유용합니다.
</Tip>

<Note>
`redirect_immediately`가 활성화된 경우, 고객은 결제 완료 후 기본 성공 페이지를 완전히 건너뛰고 `return_url`로 즉시 리디렉션됩니다.
</Note>

### 14. 언어 강제 설정

결제가 고객의 브라우저 언어 감지 기능을 무시하고 특정 언어로 표시되도록 강제합니다:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
고객의 선호하는 언어를 알고 있을 경우(예: 계정 설정에서) 또는 특정 지역 시장을 대상으로 할 때 `force_language`를 사용하십시오.
</Tip>

**지원 언어:** 아랍어 (`ar`), 카탈루냐어 (`ca`), 중국어 (`zh`), 네덜란드어 (`nl`), 영어 (`en`), 프랑스어 (`fr`), 독일어 (`de`), 히브리어 (`he`), 인도네시아어 (`id`), 이탈리아어 (`it`), 일본어 (`ja`), 한국어 (`ko`), 말레이어 (`ms`), 폴란드어 (`pl`), 포르투갈어 (`pt`), 루마니아어 (`ro`), 러시아어 (`ru`), 스페인어 (`es`), 스웨덴어 (`sv`), 태국어 (`th`), 터키어 (`tr`)

### 15. 사용자 정의 필드 수집

사용자 정의 필드를 사용하여 결제 중 고객으로부터 추가 정보를 수집합니다:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
사용자 정의 필드 응답은 자동으로 웹훅 페이로드(`payment.succeeded`, `subscription.active`, 등)에 포함되며 API를 통해 검색할 수 있습니다. 이를 사용하여 CRM을 풍부하게 만들거나 온보딩 흐름을 트리거하거나 고객 경험을 맞춤화하십시오.
</Info>

**사용 가능한 필드 유형:** `text`, `number`, `email`, `url`, `date`, `dropdown`, `boolean`

## 결제 세션 미리보기

결제 세션을 생성하기 전에 세금, 할인 및 총액을 포함한 가격 구성을 미리 볼 수 있습니다. 이는 고객이 결제 진행 전에 정확한 가격을 표시하는 데 유용합니다.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_codes: ['SAVE20']
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_codes=["SAVE20"]
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

<Card title="Preview API Reference" icon="code" href="/api-reference/checkout-sessions/preview">
  전체 미리보기 엔드포인트 문서 보기.
</Card>

## 동적 링크에서 결제 세션으로 전환

### 주요 차이점
이전에 동적 링크로 결제 링크를 생성할 때, 고객의 전체 청구 주소를 제공해야 했습니다.

이제 결제 세션을 통해 더 이상 필요하지 않습니다. 단순히 당신이 가지고 있는 정보만 전달하면, 나머지는 우리가 처리합니다. 예를 들면:

- 고객의 청구 국가만 알고 있는 경우, 그것만 제공하십시오.
- 결제 절차는 결제 페이지로 이동하기 전에 누락된 세부 정보를 자동으로 수집합니다.
- 반대로, 모든 필수 정보를 이미 가지고 있고 결제 페이지로 직접 이동하고 싶은 경우, 전체 데이터 세트를 전달하고 `confirm=true`를 요청 본문에 포함하십시오.

### 마이그레이션 과정

동적 링크에서 결제 세션으로의 마이그레이션은 간단합니다:

<Steps>
<Step title="Update your integration">
새로운 API 또는 SDK 메소드를 사용하도록 통합을 업데이트하십시오.
</Step>

<Step title="Adjust request payload">
Checkout 세션 형식에 따라 요청 페이로드를 조정하십시오.
</Step>

<Step title="That's it!">
네. 추가적인 처리나 특별한 마이그레이션 단계는 필요하지 않습니다.
</Step>
</Steps>

## 관련 API 참조

<CardGroup cols={2}>
<Card title="Create Checkout Session" icon="code" href="/api-reference/checkout-sessions/create">
  모든 사용 가능한 매개변수와 옵션을 포함한 결제 세션 생성에 대한 완전한 API 참조
</Card>
<Card title="Preview Checkout Session" icon="eye" href="/api-reference/checkout-sessions/preview">
  세션 생성 전에 가격, 세금 및 총액을 미리 보는 API 참조
</Card>
</CardGroup>