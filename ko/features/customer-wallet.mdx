---
title: 고객 지갑
description: 고객의 선불 금전 잔액을 관리합니다. 지갑에 자금을 충전하고, 잔액을 구독 청구서에 적용하며, 환불을 지갑 자금으로
  처리하고, 거래 내역을 추적할 수 있습니다.
icon: wallet
tag: NEW
keywords:
  - Dodo Payments
  - 고객 지갑
  - 결제 플랫폼
  - SaaS 청구
---

<CardGroup cols={3}>
<Card title="Get Customer Wallets" icon="code" href="/api-reference/customers/get-customer-wallets">
  통화별 고객 금전 잔액을 확인합니다.
</Card>

<Card title="Create Ledger Entry" icon="plus" href="/api-reference/customers/post-customer-wallets-ledger-entries">
  고객 지갑에 자금을 추가하거나 차감합니다.
</Card>

<Card title="List Ledger Entries" icon="list" href="/api-reference/customers/get-customer-wallets-ledger-entries">
  페이지네이션을 사용해 전체 거래 내역을 확인합니다.
</Card>
</CardGroup>

## 고객 지갑이란?

고객 지갑은 사용자를 위한 실제 자금을 보관하는 금전 잔액 계정입니다. 고객 계정을 생성하면 모든 고객에게 지갑이 자동으로 생성됩니다. 다음과 같은 용도로 지갑을 사용할 수 있습니다:

- 향후 구독 결제를 위한 **선불 자금 보관**
- 카드 환불 대신 지갑 잔액으로 **환불 처리**
- 가입 보너스나 로열티 보상과 같은 **프로모션 잔액 지급**
- 청구 중 **지갑 자금을 청구서에 자동 적용**
- 상세 원장 내역으로 **금전 거래 추적**

<Info>
고객 계정을 생성하면 모든 고객에게 자동으로 wallet이 생성됩니다. wallet은 USD, EUR, GBP 통화를 지원하며 각 통화에 대해 별도의 금전 잔액을 보유합니다.
</Info>

<Warning>
**고객 지갑 ≠ Credit-Based Billing**

Customer Wallets는 청구서와 구독 결제에 사용할 수 있는 **실제 금전 잔액**(USD, EUR, GBP)을 보유합니다.

가상 사용 단위(API 호출, 토큰, 컴퓨팅 시간)를 추적하려는 경우에는 대신 [Credit-Based Billing](/features/credit-based-billing)을 참조하세요.
</Warning>

<Frame>
  <img src="/images/customer/customer-wallet.png" alt="고객 지갑" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

## 작동 방식

고객 지갑에는 고객이 구매에 사용할 수 있는 자금이 보관됩니다. 고객에게 청구서나 반복 구독 요금이 발생하면 먼저 지갑 잔액을 확인합니다. 사용 가능한 자금이 있으면 기본 결제 수단으로 청구하기 전에 해당 자금이 청구서에 자동으로 적용됩니다.

### 자동 설정

새 고객을 생성하면 Dodo Payments 자동으로 잔액이 0인 지갑을 생성합니다. 지갑은 API를 통해 즉시 자금을 받을 수 있는 상태가 됩니다.

### 다중 통화 지원

각 지갑은 여러 통화의 잔액을 보관할 수 있습니다:

<ResponseField name="USD Balance" type="integer">
미국 달러 잔액(센트 단위로 저장)
</ResponseField>

<ResponseField name="EUR Balance" type="integer">
유로 잔액(센트 단위로 저장됨)
</ResponseField>

<ResponseField name="GBP Balance" type="integer">
파운드 스털링 잔액(펜스 단위로 저장됨)
</ResponseField>

<Info>
현재 **USD**, **EUR**, **GBP** 잔액만 사용할 수 있습니다. INR native wallet은 중단되었습니다. 자세한 내용은 [Payout Structure](/features/payouts/payout-structure)를 참조하세요. INR은 UPI, RuPay 및 인도 카드 구독의 거래 통화로 계속 지원됩니다.
</Info>

## Wallet 사용

### 고객 잔액 확인

모든 통화에 걸쳐 고객이 보유한 자금을 확인할 수 있습니다. 구매를 처리하기 전에 사용 가능한 잔액을 확인하거나 애플리케이션 UI에 잔액을 표시할 때 유용합니다.

<Card title="Get Customer Wallet Balances" icon="wallet" href="/api-reference/customers/get-customer-wallets">
  지원되는 모든 통화로 고객 wallet의 금전 잔액을 확인합니다.
</Card>

### 자금 추가 또는 차감

고객 wallet에 자금을 충전하거나(환영 보너스 또는 환불 잔액 등) 자금을 차감할 수 있습니다(구독 요금 등). 각 거래에 사유를 입력하여 명확한 감사 추적 기록을 유지할 수 있습니다.

<Note>
The `entry_type` 필드는 `'credit'`를 사용하여 wallet에 자금을 추가하고 `'debit'`를 사용하여 wallet에서 자금을 차감합니다.
</Note>

<Card title="Create Customer Wallet Ledger Entry" icon="plus" href="/api-reference/customers/post-customer-wallets-ledger-entries">
  고객 wallet에서 자금을 추가하거나 차감합니다.
</Card>

### 거래 내역 보기

고객의 모든 입금 및 출금 거래를 확인할 수 있습니다. 이 상세 원장은 계정을 대조하는 데 도움이 되며 고객에게 투명성을 제공합니다.

<Card title="List Customer Wallet Ledger Entries" icon="list" href="/api-reference/customers/get-customer-wallets-ledger-entries">
  고객의 모든 금전 거래를 확인합니다.
</Card>

## 실제 사용 사례

### Wallet으로 환불

고객이 환불을 요청하면 기존의 카드 환불을 처리하는 대신 해당 금액을 wallet 잔액에 추가할 수 있습니다. 이렇게 하면 자금이 생태계 내에 유지되어 향후 구매에 사용할 수 있습니다.

```javascript
async function refundToWallet(customerId, refundAmount, originalPaymentId) {
  await client.customers.wallets.ledgerEntries.create(customerId, {
    amount: refundAmount, // Amount in cents
    currency: 'USD',
    entry_type: 'credit',
    reason: `Refund for payment ${originalPaymentId}`,
    idempotency_key: `refund_${originalPaymentId}`
  });
}
```

### 환영 보너스 / 프로모션 잔액

신규 고객에게 금전적 환영 보너스를 제공하여 첫 구매를 유도할 수 있습니다.

```javascript
async function addWelcomeBonus(customerId) {
  await client.customers.wallets.ledgerEntries.create(customerId, {
    amount: 1000, // $10.00 promotional balance
    currency: 'USD',
    entry_type: 'credit',
    reason: 'Welcome bonus - $10 promotional balance',
    idempotency_key: `welcome_${customerId}`
  });
}
```

### Wallet에서 구독 결제

구독 요금 또는 수동 구매를 처리하기 위해 wallet에서 자금을 차감합니다.

```javascript
async function deductForPurchase(customerId, purchaseAmount, purchaseId) {
  try {
    await client.customers.wallets.ledgerEntries.create(customerId, {
      amount: purchaseAmount,
      currency: 'USD',
      entry_type: 'debit',
      reason: `Subscription charge - monthly billing`,
      idempotency_key: `charge_${purchaseId}`
    });
  } catch (error) {
    if (error.status === 400) {
      console.log('Insufficient wallet balance');
    }
  }
}
```

### 선불 결제 시스템

고객이 계정에 미리 자금을 충전하고 시간이 지남에 따라 해당 잔액을 사용하도록 할 수 있습니다.

<Steps>
<Step title="Add Initial Funds">
고객이 입금하면 고객 wallet에 자금을 추가합니다.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 5000, // $50.00 deposit
  currency: 'USD',
  entry_type: 'credit',
  reason: 'Account funding - prepaid deposit',
  idempotency_key: `deposit_${paymentId}`
});
```

</Step>

<Step title="Apply Balance to Purchases">
고객이 서비스를 이용하거나 구독을 갱신하면 잔액에서 자금을 차감합니다.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 1500, // $15.00 charge
  currency: 'USD', 
  entry_type: 'debit',
  reason: 'Service purchase - monthly subscription',
  idempotency_key: `purchase_${purchaseId}`
});
```

</Step>

<Step title="Monitor Balances">
고객의 자금이 부족해지고 있는지 확인하여 충전을 안내합니다.

```javascript
const wallets = await client.customers.wallets.list(customerId);
const usdWallet = wallets.items.find(w => w.currency === 'USD');
const balance = usdWallet.balance;

if (balance < 1000) { // Less than $10.00
  // Send low balance notification
  await sendLowBalanceNotification(customerId, balance);
}
```

</Step>
</Steps>

### 다중 통화 지원

서로 다른 지역의 고객을 위해 별도의 금전 잔액을 관리합니다.

<AccordionGroup>
<Accordion title="US Customers">
미국 고객의 USD 자금을 관리합니다.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 20000, // $200.00 in cents
  currency: 'USD',
  entry_type: 'credit',
  reason: 'USD account funding',
  idempotency_key: `usd_deposit_${paymentId}`
});
```

</Accordion>

<Accordion title="European Customers">
유럽 고객의 EUR 자금을 관리합니다.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 18000, // €180.00 in cents
  currency: 'EUR',
  entry_type: 'credit',
  reason: 'EUR account funding',
  idempotency_key: `eur_deposit_${paymentId}`
});
```

</Accordion>

<Accordion title="UK Customers">
영국 고객의 GBP 자금을 관리합니다.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 15000, // £150.00 in pence
  currency: 'GBP',
  entry_type: 'credit',
  reason: 'GBP account funding',
  idempotency_key: `gbp_deposit_${paymentId}`
});
```

</Accordion>
</AccordionGroup>

## 모범 사례

### 중복 거래 방지

idempotency key를 사용하여 동일한 이벤트에 대해 실수로 자금을 두 번 추가하거나 차감하지 않도록 합니다.

```javascript
async function addFundsSafely(customerId, amount, reason) {
  const idempotencyKey = `${reason}_${customerId}_${Date.now()}`;
  
  try {
    const result = await client.customers.wallets.ledgerEntries.create(customerId, {
      amount: amount,
      currency: 'USD',
      entry_type: 'credit',
      reason: reason,
      idempotency_key: idempotencyKey
    });
    
    // Replaying the same idempotency_key returns the original entry rather than
    // creating a duplicate, so no special duplicate handling is required.
    return { success: true, wallet: result };
  } catch (error) {
    if (error.status === 400) {
      // e.g. insufficient balance on a debit
      return { success: false, reason: 'insufficient_balance' };
    }

    throw error;
  }
}
```

### 청구 전 잔액 확인

wallet에서 대규모 거래를 처리하기 전에 고객에게 충분한 자금이 있는지 확인합니다.

```javascript
async function checkBalanceBeforeOperation(customerId, requiredAmount) {
  const wallets = await client.customers.wallets.list(customerId);
  const usdWallet = wallets.items.find(w => w.currency === 'USD');
  
  if (!usdWallet || usdWallet.balance < requiredAmount) {
    throw new Error('Insufficient funds for this operation');
  }
  
  return usdWallet.balance;
}
```

## 다음 단계

<Warning>
다음 기능은 향후 릴리스에 추가될 예정입니다:
</Warning>

- **잔액 만료**: 일정 기간 후 자금이 만료되도록 설정
- **향상된 분석**: 상세한 지출 보고서 및 잔액 추이
- **추가 Webhooks**: 잔액 변경 및 잔액 부족 알림에 대한 실시간 알림

<Tip>
기본적인 자금 추가 및 차감 작업으로 시작한 다음, 비즈니스가 성장함에 따라 더 복잡한 자동 결제 workflow를 통합하세요.
</Tip>
