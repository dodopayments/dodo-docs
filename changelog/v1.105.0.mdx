---
title: v1.105.0 (June 22, 2026)
description: "Korean payment methods (Kakao Pay, Naver Pay, PAYCO) and Przelewy24, Bring Your Own Processor (BYOP), biweekly and tri-weekly payout cycles, brand_id on every webhook payload, three-decimal currency support, higher product entitlement and credit caps, plus checkout, payout, and analytics improvements."
icon: "arrows-turn-right"
keywords: ["Dodo Payments", "changelog", "release notes", "product updates", "v1.105.0 (june 22, 2026)", "byop", "korean payment methods", "payout cycles", "webhooks", "brand_id"]
---

## New Features

### 1. **New Payment Methods: Korean Wallets & Przelewy24**

Four new local payment methods are now available, expanding coverage in South Korea and Poland.

**South Korea (KRW)** — three Korean wallets surface on KRW checkouts:

| Method | API type | Subscriptions |
|--------|----------|---------------|
| **Kakao Pay** | `kakao_pay` | Yes |
| **Naver Pay** | `naver_pay` | Yes |
| **PAYCO** | `payco` | No (one-time only) |

The Korean wallets require a billing country of `KR` and `KRW` billing — if your product is priced in another currency, enable Adaptive Currency so the customer is billed in KRW.

**Poland (PLN)** — **Przelewy24 (P24)**, Poland's leading online payment method, now settles in `PLN` (not EUR) and supports one-time payments. Pass `przelewy24` in `allowed_payment_method_types`.

Learn more: [South Korea Payment Methods](/features/payment-methods/south-korea) | [European Payment Methods](/features/payment-methods/europe) | [Payment Methods Overview](/features/payment-methods)

### 2. **Bring Your Own Processor (BYOP)**

You can now connect your own payment processor (such as Stripe or Adyen) and route payments through it per country, while keeping Dodo Payments' invoicing, dispute, and refund tooling. BYOP transactions are routed via Hyperswitch, support custom invoice issuer details, and carry a flat **0.5% usage fee**.

Responses now include `is_byop` so you can identify BYOP-routed payments, and tax quoting, refunds, and disputes are BYOP-aware end to end.

Learn more: [Bring Your Own Processor](/features/byop) | [BYOP with Stripe](/features/byop/stripe) | [BYOP with Adyen](/features/byop/adyen)

### 3. **`brand_id` on Every Webhook Payload**

Every webhook event payload now includes a `brand_id`, so you can attribute payments, subscriptions, refunds, disputes, license keys, credit ledger entries, entitlement grants, dunning attempts, and abandoned checkouts to a specific brand. Where an entity has no brand of its own, `brand_id` falls back to the business's primary brand.

No integration changes are required — the field is additive and appears automatically on existing webhook endpoints.

Learn more: [Multiple Brands](/features/multi-brands) | [Webhook Events](/developer-resources/webhooks/intents/webhook-events-guide)

### 4. **Biweekly & Tri-weekly Payout Cycles**

Two higher-frequency payout cycles are now available for high-volume businesses, alongside the existing bi-monthly, weekly, and monthly options:

| Cycle | Payout dates per month |
|-------|------------------------|
| **Biweekly** | 8 (on the 4th, 7th, 11th, 14th, 18th, 21st, 25th, and 29th) |
| **Tri-weekly** | 15 (on every even-numbered day) |

Each payout covers the earnings accrued since the previous payout date. Both are available on request — contact [support@dodopayments.com](mailto:support@dodopayments.com) to switch your cycle.

Learn more: [Payouts Process](/features/payouts/payout-structure#payout-cycles)

### 5. **Three-Decimal Currency Support**

Currency handling has been reworked to support zero-decimal and **three-decimal** currencies in addition to the standard two-decimal currencies. Presentment conversions are now decimal-aware, so amounts are computed and displayed with the correct precision for each currency.

## Improvements

### 6. **Higher Product Entitlement & Credit Limits**

- **Product entitlements** per product raised from **20 to 50**.
- **Credit entitlements** (credits attached to a product) raised from **3 to 5**.

Learn more: [Entitlements](/features/entitlements/introduction) | [Credit-Based Billing](/features/credit-based-billing)

### 7. **`card_last_four` and `card_network` on List Payments**

The **List Payments** response now includes `card_last_four` and `card_network` on each payment, so you can display card details in lists without fetching each payment individually.

Learn more: [List Payments](/api-reference/payments/get-payments)

### 8. **Signed Customer Credits in Change Plan Preview**

The change-plan preview now returns `customer_credits` as a **signed** value: **negative** when credits are deducted to offset a charge (typical on upgrades) and **positive** when credits are added (from a downgrade proration refund or a wallet top-up), making it clear whether credits are being used or granted.

Learn more: [Subscription Upgrade & Downgrade](/developer-resources/subscription-upgrade-downgrade)

### 9. **Branded ACR & Dunning Emails**

Abandoned-cart recovery (ACR) and subscription dunning recovery emails now use the relevant **brand's name and logo** instead of the business default, so the recovery experience matches the brand the customer purchased from.

### 10. **Improved Tax ID Validation**

Tax ID validation at checkout has been improved with notation detection and registry lookup, reducing validation errors. The Business form now includes a **country field** to prevent Tax ID validation errors, plus an **upload progress indicator**.

### 11. **Dynamic Cardholder Name at Checkout**

Checkout now supports a dynamic cardholder name field, capturing the name as entered on the card.

### 12. **Analytics Improvements**

- **Page-wise analytics** — a new endpoint surfaces discount performance per page.
- Faster overall analytics with query deduplication and per-query tracing.
- Corrected subscription MRR, active-subscription counts, and retention/churn calculations.

### 13. **Dashboard & Storefront Polish**

- **Brand filter** added to the product list.
- **Product collection, add-on, and meter forms** reworked with create / edit / duplicate flows.
- **Payout page** redesigned, with the ability to recover a failed payout to success.
- **Storefront** updated for consistent banner sizing.

### Other Fixes & Improvements

- Korean and several other currencies (VND, PYG, XOF, XAF) now default to local currency at checkout.
- Brazil tax rates zeroed out and the Portugal edtech rate corrected.
- Twelve `UNKNOWN_ERROR` provider errors mapped to unified, actionable error codes.
- Concurrent renewal double-charge prevented by locking the renewal invoice.
- Minor bug fixes and stability improvements across the platform.
