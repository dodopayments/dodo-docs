---
title: Dodo Payments में माइग्रेट करें
description: हमारे CLI टूल का उपयोग करके अन्य भुगतान प्रदाताओं से Dodo Payments
  में अपने डेटा को आसानी से माइग्रेट करें
icon: arrow-right-arrow-left
---

**क्या आप किसी अन्य भुगतान प्रदाता से Dodo Payments पर स्विच कर रहे हैं?** हमारा माइग्रेशन टूल आपके **उत्पादों, ग्राहकों और छूट कोड को बिना किसी डेटा खोए स्थानांतरित करना आसान बनाता है।** यह प्रक्रिया सुरक्षित, मार्गदर्शित है, और इसे केवल कुछ मिनटों में किया जा सकता है।

हम वर्तमान में **Lemon Squeezy, Stripe, Polar.sh, और Paddle** से माइग्रेशन का समर्थन करते हैं।

### समर्थित माइग्रेशन

<CardGroup cols={3}>
  <Card title="उत्पाद और मूल्य निर्धारण" icon="box">
    अपने सभी उत्पादों और मूल्य निर्धारण विवरणों को माइग्रेट करें।
  </Card>
  <Card title="ग्राहक डेटा" icon="users">
    अपने पूरे ग्राहक डेटाबेस को, जिसमें संपर्क विवरण शामिल हैं, स्थानांतरित करें।
  </Card>
  <Card title="छूट कोड और प्रचार" icon="tag">
    सभी छूट कोड और प्रचार प्रस्तावों को बिना किसी परेशानी के स्थानांतरित करें।
  </Card>
</CardGroup>

## शुरू करें

पहले, माइग्रेशन टूल स्थापित करें:

```bash
npm install -g dodo-migrate
```

<Note>
आपके कंप्यूटर पर Node.js ≥ 18 स्थापित होना चाहिए।
</Note>

## अपने डेटा को माइग्रेट करें

### यदि आप Lemon Squeezy से आ रहे हैं:

```bash
dodo-migrate lemonsqueezy
```

### यदि आप Stripe से आ रहे हैं:

```bash
dodo-migrate stripe
```

### यदि आप Polar.sh से आ रहे हैं:

```bash
dodo-migrate polar
```

### यदि आप Paddle से आ रहे हैं:

```bash
dodo-migrate paddle
```

यह टूल आपको हर कदम पर मार्गदर्शन करेगा। आपको आवश्यकता होगी:
- आपके प्रदाता का API कुंजी (Lemon Squeezy, Stripe, Polar.sh, Paddle, आदि)
- आपका Dodo Payments API कुंजी  
- क्या आप पहले परीक्षण करना चाहते हैं (सिफारिश की गई) या लाइव जाना चाहते हैं
- किस Dodo Payments ब्रांड में माइग्रेट करना है

## हमारे माइग्रेशन टूल का उपयोग क्यों करें?

- **कोई डेटा हानि नहीं** - सब कुछ सुरक्षित रूप से स्थानांतरित होता है
- **पहले परीक्षण करें** - लाइव जाने से पहले इसे परीक्षण मोड में आजमाएं
- **मार्गदर्शित प्रक्रिया** - हम आपको प्रत्येक चरण में मार्गदर्शन करते हैं
- **आवश्यकता होने पर फिर से शुरू करें** - यदि कुछ गलत हो जाता है, तो आप वहीं से शुरू कर सकते हैं जहाँ आपने छोड़ा था

## उन्नत उपयोग

यदि आप बिना संकेतों के माइग्रेशन चलाना पसंद करते हैं, तो आप सभी विवरण पहले से प्रदान कर सकते हैं:

### Lemon Squeezy माइग्रेशन:

```bash
dodo-migrate lemonsqueezy \
  --provider-api-key=lsq_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX
```

### Stripe माइग्रेशन:

```bash
dodo-migrate stripe \
  --provider-api-key=sk_test_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX \
  --migrate-types=products,coupons
```

### Polar.sh माइग्रेशन:

```bash
dodo-migrate polar \
  --provider-api-key=polar_org_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX \
  --migrate-types=products,discounts,customers
```

### Paddle माइग्रेशन:

```bash
dodo-migrate paddle \
  --provider-api-key=paddle_XXXXXXXXXXXXXXXX \
  --dodo-api-key=dp_XXXXXXXXXXXXXXXX \
  --mode=test_mode \
  --dodo-brand-id=brand_XXXXXX
```

## मदद चाहिए?

- **टूल सहायता**: `dodo-migrate --help`
- **लेमन स्क्वीज़ी सहायता**: `dodo-migrate lemonsqueezy --help`
- **स्ट्राइप सहायता**: `dodo-migrate stripe --help`
- **Polar.sh सहायता**: `dodo-migrate polar --help`
- **पैडल सहायता**: `dodo-migrate paddle --help`
- **समुदाय समर्थन**: [Discord](https://discord.gg/bYqAp4ayYh)
- **समस्याएँ रिपोर्ट करें**: [GitHub](https://github.com/dodopayments/dodo-migrate)

## टूल अपडेट करें

```bash
# Update to latest version
npm update -g dodo-migrate

# Remove the tool
npm uninstall -g dodo-migrate
```
