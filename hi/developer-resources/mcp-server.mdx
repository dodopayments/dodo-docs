---
title: MCP सर्वर
description: AI सहायकों को Dodo Payments API के साथ Model Context Protocol (MCP)
  के माध्यम से बातचीत करने की अनुमति दें ताकि भुगतान संचालन, सदस्यता प्रबंधन और
  ग्राहक इंटरैक्शन में सहजता हो।
icon: /images/logos/mcp.svg
---

## अवलोकन

Model Context Protocol (MCP) एक ओपन स्टैंडर्ड है जो AI अनुप्रयोगों को सुरक्षित रूप से बाहरी डेटा स्रोतों और APIs से कनेक्ट करने की अनुमति देता है। Dodo Payments MCP सर्वर AI सहायकों जैसे Claude, Cursor, और अन्य MCP-संगत क्लाइंट्स को आपके भुगतान बुनियादी ढांचे तक संरचित पहुंच प्रदान करता है।

Dodo Payments MCP सर्वर [Stainless](https://www.stainless.com/) के साथ बनाया गया है और **Code Mode** आर्किटेक्चर का उपयोग करता है, जो AI एजेंटों को Dodo Payments SDK के खिलाफ TypeScript कोड लिखने और निष्पादित करने की अनुमति देता है, एक अलग सैंडबॉक्स वातावरण में।

### प्रमुख क्षमताएँ

- **भुगतान संचालन**: भुगतान और रिफंड बनाना, पुनः प्राप्त करना और प्रबंधित करना
- **सदस्यता प्रबंधन**: आवर्ती बिलिंग, अपग्रेड और रद्दीकरण को संभालना
- **ग्राहक प्रशासन**: ग्राहक डेटा और पोर्टल पहुंच का प्रबंधन करना
- **उत्पाद कैटलॉग**: उत्पादों, मूल्य निर्धारण और छूट को बनाना और अपडेट करना
- **लाइसेंस प्रबंधन**: सॉफ़्टवेयर लाइसेंस को सक्रिय, मान्य और प्रबंधित करना
- **उपयोग-आधारित बिलिंग**: मीटर किए गए उपयोग के लिए ट्रैक और बिल करना

## कोड मोड कैसे काम करता है

Dodo Payments MCP सर्वर कोड मोड का उपयोग करता है, जो आपके AI एजेंट को दो शक्तिशाली उपकरण प्रदान करता है:

1. **Docs खोज उपकरण**: Dodo Payments API और SDK के बारे में दस्तावेज़ों को क्वेरी करें ताकि उपलब्ध संचालन और पैरामीटर को समझा जा सके
2. **कोड निष्पादन उपकरण**: SDK के खिलाफ TypeScript कोड लिखें जो एक सुरक्षित सैंडबॉक्स वातावरण में निष्पादित होता है, बिना वेब या फ़ाइल प्रणाली की पहुंच के

यह आर्किटेक्चर AI एजेंटों को जटिल, बहु-चरण भुगतान संचालन को निश्चित रूप से और बार-बार करने में सक्षम बनाता है।

<Tip>
Code Mode का उपयोग करते हुए, एजेंट कई API कॉल को श्रृंखला में जोड़ सकते हैं, सशर्त तर्क संभाल सकते हैं, और गणनाएँ कर सकते हैं—सभी एक ही टूल कॉल में।
</Tip>

## डोडो ज्ञान MCP

डोडो भुगतान MCP सर्वर (API संचालन निष्पादित करने के लिए) के अलावा, हम **डोडो ज्ञान MCP** प्रदान करते हैं - एक सेमांटिक सर्च सर्वर जो AI सहायक को डोडो भुगतान दस्तावेज़ और ज्ञान आधार तक त्वरित पहुंच प्रदान करता है।

<Info>
**[ContextMCP.ai](https://contextmcp.ai) के साथ निर्मित**: Dodo Knowledge MCP ContextMCP द्वारा संचालित है, जो वेक्टर एम्बेडिंग्स का उपयोग करके हमारे प्रलेखन में तेज़ सिमेंटिक खोज सक्षम करता है।
</Info>

### डोडो ज्ञान MCP क्या है?

डोडो ज्ञान MCP एक दूरस्थ MCP सर्वर है जो प्रदान करता है:

- **सेमांटिक दस्तावेज़ खोज**: प्राकृतिक भाषा प्रश्नों का उपयोग करके प्रासंगिक दस्तावेज़ खोजें
- **संदर्भात्मक उत्तर**: AI सहायक को डोडो भुगतान के बारे में सटीक, अद्यतन जानकारी मिलती है
- **शून्य सेटअप**: कोई API कुंजी या स्थानीय स्थापना की आवश्यकता नहीं - बस कनेक्ट करें और प्रश्न पूछना शुरू करें

### त्वरित सेटअप

अपने AI क्लाइंट में डोडो ज्ञान MCP से कनेक्ट करें:

<Tabs>
`~/.cursor/mcp.json` में जोड़ें:
<Tab title="Cursor">

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Desktop">
अपने Claude Desktop कॉन्फ़िगरेशन में जोड़ें:
</Tip>

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

कॉन्फ़िगरेशन फ़ाइल स्थान:
- **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
- **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
- **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Tab>

<Tab title="Windsurf">
`~/.codeium/windsurf/mcp_config.json` में जोड़ें:
</Tab>

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    }
  }
}
```

</Tab>

<Tab title="Claude Code">
अपने टर्मिनल में चलाएँ:

```bash
claude mcp add dodo-knowledge -- npx -y mcp-remote@latest https://knowledge.dodopayments.com/mcp
```

</Tab>
</Tabs>

<Note>
Node.js 18 या उच्च संस्करण आवश्यक है। `mcp-remote` पैकेज रिमोट MCP सर्वर से कनेक्शन संभालता है।
</Note>

### दोनों MCP सर्वरों का एक साथ उपयोग करना

AI-सहायता प्राप्त विकास अनुभव के लिए, हम दोनों MCP सर्वरों का उपयोग करने की सिफारिश करते हैं:

| सर्वर | उद्देश्य | उपयोग का मामला |
|--------|---------|----------|
| **डोडो ज्ञान MCP** | दस्तावेज़ खोज | "मैं वेबहुक को कैसे संभालूं?", "कौन से भुगतान विधियाँ समर्थित हैं?" |
| **डोडो भुगतान MCP** | API संचालन | भुगतान बनाना, सदस्यता प्रबंधित करना, रिफंड संभालना |

```json
{
  "mcpServers": {
    "dodo-knowledge": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://knowledge.dodopayments.com/mcp"]
    },
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

<Tip>
दोनों सर्वर कॉन्फ़िगर होने पर, आपका AI सहायक पहले दस्तावेज़ खोज सकता है यह समझने के लिए कि किसी फीचर को कैसे लागू किया जाए, फिर वास्तविक API कॉल चला सकता है—सभी एक ही बातचीत में।
</Tip>

### ज्ञान MCP समस्या निवारण

यदि आप कनेक्शन समस्याओं का सामना करते हैं:

1. **MCP प्रमाणीकरण कैश साफ़ करें**: `rm -rf ~/.mcp-auth`
2. **अपने क्लाइंट एप्लिकेशन को पुनरारंभ करें**
3. **त्रुटि संदेशों के लिए क्लाइंट लॉग जांचें**
4. **Node.js संस्करण सत्यापित करें**: Node.js 18+ आवश्यक है

{/* LOCKED_PATTERN_0d288b5f7e2819b6a54bf391a66e053a */}
  Dodo Knowledge MCP कॉन्फ़िगरेशन पृष्ठ तक पहुँचें
</Card>

## स्थापना

अपने कार्यप्रवाह के लिए सबसे उपयुक्त स्थापना विधि चुनें।

### दूरस्थ MCP सर्वर (अनुशंसित)

कोई स्थानीय सेटअप या स्थापना के बिना होस्ट किए गए MCP सर्वर तक पहुँचें। यह शुरू करने का सबसे तेज़ तरीका है।

<Frame>
  <iframe 
      className="w-full aspect-video rounded-md" 
      src="https://www.youtube.com/embed/TxGgYxNmkT4"
      title="Using Dodo Payment's Remote MCP Server"
      frameBorder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowFullScreen
  ></iframe>
</Frame>

<Steps>
{/* LOCKED_PATTERN_bd1568a9aaf0e86b69853e215d3d5723 */}
    अपने ब्राउज़र में [https://mcp.dodopayments.com](https://mcp.dodopayments.com) पर जाएँ।
</Step>

{/* LOCKED_PATTERN_b8d4e4a1a3c0fcf28fd2afe7ce9e8316 */}
    अपने विशिष्ट क्लाइंट के लिए प्रदान किया गया JSON कॉन्फ़िगरेशन कॉपी करें। Cursor या Claude Desktop के लिए, इसे अपनी MCP सेटिंग्स में जोड़ें:
    

    ```json
    {
      "mcpServers": {
        "dodopayments_api": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

{/* LOCKED_PATTERN_8be57788a275cad53ee59f91af51bfd5 */}
    OAuth फ़्लो आपसे निम्न करने के लिए कहेगा:
    - अपना Dodo Payments API कुंजी दर्ज करें
    - अपना वातावरण चुनें (test या live)
    - अपना MCP क्लाइंट प्रकार चुनें
    
    <Warning>
    अपनी API कुंजी सुरक्षित रखें। विकास के दौरान test mode कीज़ का उपयोग करें।
    </Warning>
</Step>

<Step title="प्रमाणित करें और कॉन्फ़िगर करें">
    OAuth प्रवाह आपको प्रेरित करेगा:
    - अपना डोडो भुगतान API कुंजी दर्ज करें
    - अपने वातावरण का चयन करें (परीक्षण या लाइव)
    - अपने MCP क्लाइंट प्रकार का चयन करें
    
    <Warning>
    अपनी API कुंजी को सुरक्षित रखें। विकास के दौरान परीक्षण मोड कुंजी का उपयोग करें।
    </Warning>
</Step>

{/* LOCKED_PATTERN_606fac2bd646ef76f5d66097882887ed */}
    **Login and Approve** पर क्लिक करके कनेक्शन को अधिकृत करें।
    
    <Check>
    एक बार कनेक्ट हो जाने पर, आपका AI सहायक आपके लिए Dodo Payments API से इंटरैक्ट कर सकता है।
    </Check>
</Step>
</Steps>

### NPM पैकेज

NPM का उपयोग करके स्थानीय रूप से MCP सर्वर स्थापित और चलाएँ।

<Tabs>
{/* LOCKED_PATTERN_c8776436a7ae17e188a287e7c6852f16 */}

```bash
# Set your environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."
export DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key"
export DODO_PAYMENTS_ENVIRONMENT="live_mode"

# Run the latest version
npx -y dodopayments-mcp@latest
```

</Tab>

{/* LOCKED_PATTERN_8d04e6cca1a5903f80148c5b923e5c3f */}

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

</Tab>
</Tabs>

<Tip>
`@latest` का उपयोग करें ताकि हमेशा नवीनतम संस्करण प्राप्त हो, या स्थिरता के लिए `@1.0.0` को पिन करें।
</Tip>

### डॉकर

संगठित तैनाती के लिए कंटेनरयुक्त वातावरण में MCP सर्वर चलाएँ।

<CodeGroup>

```bash Docker
# Pull the latest image
docker pull ghcr.io/dodopayments/mcp:latest

# Run the container
docker run -e DODO_PAYMENTS_API_KEY="dodo_test_..." \
  -e DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -e DODO_PAYMENTS_ENVIRONMENT="live_mode" \
  ghcr.io/dodopayments/mcp:latest
```

```yaml Docker Compose
services:
  dodopayments-mcp:
    image: ghcr.io/dodopayments/mcp:latest
    environment:
      - DODO_PAYMENTS_API_KEY=dodo_test_...
      - DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_key
      - DODO_PAYMENTS_ENVIRONMENT=live_mode
    restart: unless-stopped
```

</CodeGroup>

<Note>
Docker इमेज [GitHub Container Registry](https://github.com/orgs/dodopayments/packages/container/package/mcp) पर उपलब्ध हैं।
</Note>

## क्लाइंट कॉन्फ़िगरेशन

अपने पसंदीदा AI क्लाइंट में डोडो भुगतान MCP सर्वर को कॉन्फ़िगर करें।

<Tabs>
<Tab title="Cursor">
Cursor में अपने भुगतान डेटा तक बातचीत-आधारित पहुंच सक्षम करने के लिए Dodo Payments MCP सर्वर सेट करें।

**एक-क्लिक स्थापना**

नीचे दिए गए बटन का उपयोग करके सीधे कर्सर में MCP सर्वर स्थापित करें:

[

![कर्सर में जोड़ें](https://cursor.com/deeplink/mcp-install-dark.svg)

](https://cursor.com/en-US/install-mcp?name=dodopayments-mcp&config=)

क्लिक करने के बाद, Cursor की `mcp.json` में **Cursor Settings** > **Tools & MCP** > **New MCP Server** के माध्यम से अपने वातावरण चर सेट करें।

**मैनुअल कॉन्फ़िगरेशन**

<Steps>
{/* LOCKED_PATTERN_b5120fe2afbf9bb96d1de6f9549e8f41 */}
    **Cursor Settings** > **Features** > **Model Context Protocol** पर जाएँ या `Cmd/Ctrl + Shift + P` दबाएँ और "MCP Settings" खोजें。
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    **Edit Config** पर क्लिक करके `mcp_config.json` फ़ाइल खोलें。
</Step>

{/* LOCKED_PATTERN_a65e4e236194f7c177caa156c6d0b797 */}
    निम्न कॉन्फ़िगरेशन में से एक चुनें:
    
    **Remote Server (Recommended)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **स्थानीय NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_8368e3e8540b47a92000151cb7e30a33 */}
    कॉन्फ़िगरेशन फ़ाइल सहेजें और Cursor पुनरारंभ करें।
    
    <Check>
    अपने Dodo Payments डेटा के बारे में AI सहायक से पूछकर कनेक्शन सत्यापित करें।
    </Check>
</Step>
</Steps>
</Tab>

<Tab title="Claude Desktop">
Claude Desktop में अपने भुगतान डेटा तक बातचीत-आधारित पहुंच सक्षम करने के लिए Dodo Payments MCP सर्वर सेट करें。
<Steps>
{/* LOCKED_PATTERN_ecfdd2e916ed4a93d6b3950e9027df3f */}
    Claude Desktop कॉन्फ़िगरेशन फ़ाइल खोजें:
    
    - **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
    - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
    - **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Step>

{/* LOCKED_PATTERN_0112f2d89dc6125934d590d4a51a751d */}
    फ़ाइल को टेक्स्ट एडिटर में खोलें और Dodo Payments कॉन्फ़िगरेशन जोड़ें:
    
    **Remote Server (Recommended)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **स्थानीय NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_83563b32c3caf8bd7aa68192b6ebcc7d */}
    परिवर्तन लागू करने के लिए Claude Desktop बंद करें और पुनः खोलें。
    
    <Tip>
    MCP सर्वर जुड़ा हुआ है यह सुनिश्चित करने के लिए Claude Desktop में 🔌 आइकन देखें。
    </Tip>
</Step>
</Steps>
</Tab>

<Tab title="Claude Code">
Claude Code CLI में Dodo Payments MCP सर्वर सेट करें।

अपने टर्मिनल में निम्नलिखित कमांड चलाएँ:

```bash
claude mcp add --transport stdio dodopayments_api \
  --env DODO_PAYMENTS_API_KEY="dodo_test_..." \
  DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -- npx -y dodopayments-mcp
```

कॉन्फ़िगरेशन आपके होम डायरेक्टरी की `.claude.json` फ़ाइल में संग्रहीत होगा।

<Tip>
Claude Code स्वचालित रूप से आपके शेल से वातावरण चर उठाता है, इसलिए आप कमांड चलाने से पहले उन्हें निर्यात भी कर सकते हैं।
</Tip>
</Tab>

<Tab title="VS Code">
VS Code में Dodo Payments MCP सर्वर सेट करें।

**एक-क्लिक स्थापना**

[VS कोड खोलें](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dodopayments-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22dodopayments-mcp%22%5D%2C%22env%22%3A%7B%22DODO_PAYMENTS_API_KEY%22%3A%22Set%20your%20DODO_PAYMENTS_API_KEY%20here.%22%2C%22DODO_PAYMENTS_WEBHOOK_KEY%22%3A%22Set%20your%20DODO_PAYMENTS_WEBHOOK_KEY%20here.%22%7D%7D)

क्लिक करने के बाद, VS Code की `mcp.json` में **Command Palette** > **MCP: Open User Configuration** के माध्यम से अपने वातावरण चर सेट करें।

**मैनुअल कॉन्फ़िगरेशन**

<Steps>
{/* LOCKED_PATTERN_01c11d820b190ade4d2a29feff5a49da */}
    Command Palette (`Cmd/Ctrl + Shift + P`) खोलें और "MCP: Open User Configuration" खोजें。
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    Dodo Payments MCP सर्वर जोड़ें:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_fabb79a0cecb26359867d78b61382dd7 */}
    MCP सर्वर को सक्रिय करने के लिए VS Code (`Cmd/Ctrl + Shift + P` > "Reload Window") को रीलोड करें。
</Step>
</Steps>
</Tab>

<Tab title="Cline (VS Code)">
Cline (VS Code) में अपने भुगतान डेटा तक बातचीत-आधारित पहुंच सक्षम करने के लिए Dodo Payments MCP सर्वर सेट करें。
<Steps>
{/* LOCKED_PATTERN_43db9eecf5eea8e9c4ad60b6c3ff1c0b */}
    Cline कॉन्फ़िगरेशन फ़ाइल खोजें:
    
    - **macOS**: `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
    - **Windows**: `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    - **Linux**: `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
</Step>

{/* LOCKED_PATTERN_0849b9a67e72024819745a9889898853 */}
    Dodo Payments MCP सर्वर कॉन्फ़िगरेशन जोड़ें:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_43be57c73e24741f16da5f79d5398749 */}
    VS Code विंडो (`Cmd/Ctrl + Shift + P` > "Reload Window") को रीलोड करें ताकि MCP सर्वर कनेक्शन सक्रिय हो जाए。
</Step>
</Steps>
</Tab>

<Tab title="Zed">
Zed में अपने भुगतान डेटा तक बातचीत-आधारित पहुंच सक्षम करने के लिए Dodo Payments MCP सर्वर सेट करें。
<Steps>
{/* LOCKED_PATTERN_a48799001554eb7ef4b2ac1e56fab915 */}
    **Settings** > **Assistant** > **Model Context Protocol** पर जाएँ。
</Step>

{/* LOCKED_PATTERN_b62f78a8bb37ff0ea4a9a60d555a640c */}
    निम्न कॉन्फ़िगरेशन जोड़ें:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

{/* LOCKED_PATTERN_b1c8821108a3bf0ac4028a0196176d66 */}
    कॉन्फ़िगरेशन सहेजें। अब MCP सर्वर Zed के AI सहायक में उपलब्ध होना चाहिए。
</Step>
</Steps>
</Tab>

{/* LOCKED_PATTERN_71257692833d303ebb288457e9ce7134 */}
अन्य MCP-संगत क्लाइंट में Dodo Payments MCP सर्वर सेट करें。

**दूरस्थ सर्वर (अनुशंसित)**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

**स्थानीय NPX**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
      }
    }
  }
}
```

<Info>
पूर्ण MCP-संगत क्लाइंट और उनकी कॉन्फ़िगरेशन विधियों की सूची के लिए [modelcontextprotocol.io/clients](https://modelcontextprotocol.io/clients) पर जाएँ。
</Info>
</Tab>
</Tabs>

## वातावरण चर

पर्यावरण चर का उपयोग करके MCP सर्वर के व्यवहार को कॉन्फ़िगर करें।

| Variable | विवरण | आवश्यक |
|----------|-------------|----------|
| `DODO_PAYMENTS_API_KEY` | आपका Dodo Payments API कुंजी | हाँ |
| `DODO_PAYMENTS_WEBHOOK_KEY` | आपका webhook साइनिंग कुंजी | नहीं |
| `DODO_PAYMENTS_ENVIRONMENT` | production के लिए `live_mode` सेट करें | नहीं |

## दूरस्थ रूप से चलाना

वेब-आधारित क्लाइंट्स या एजेंटिक वर्कफ़्लो के लिए MCP सर्वर को एक दूरस्थ HTTP सर्वर के रूप में तैनात करें।

```bash
# Start as remote server
npx -y dodopayments-mcp --transport=http

# Specify a custom port
npx -y dodopayments-mcp --transport=http --port=3000
```

### दूरस्थ सर्वर कॉन्फ़िगरेशन

एक बार तैनात होने पर, क्लाइंट सर्वर URL का उपयोग करके कनेक्ट कर सकते हैं:

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "url": "http://localhost:3000",
      "headers": {
        "Authorization": "Bearer your_api_key"
      }
    }
  }
}
```

### प्राधिकरण हेडर

दूरस्थ सर्वर निम्नलिखित हेडर के माध्यम से प्रमाणीकरण स्वीकार करता है:

| Header | विवरण |
|--------|-------------|
| `Authorization` | Bearer token प्रमाणीकरण |
| `x-dodo-payments-api-key` | Direct API key हेडर |

## क्लाइंट क्षमताएँ

विभिन्न MCP क्लाइंट्स के MCP विनिर्देशन के लिए समर्थन के विभिन्न स्तर हैं। सर्वर स्वचालित रूप से इन सीमाओं के चारों ओर काम करने के लिए उपकरण स्कीमा को समायोजित कर सकता है।

```bash
# Auto-detect client (default)
npx dodopayments-mcp --client=infer

# Configure for specific clients
npx dodopayments-mcp --client=cursor
npx dodopayments-mcp --client=claude
npx dodopayments-mcp --client=claude-code
npx dodopayments-mcp --client=openai-agents
```

### उपलब्ध क्लाइंट प्रीसेट्स

| Client | विवरण |
|--------|-------------|
| `infer` | Auto-detect client (डिफ़ॉल्ट) |
| `cursor` | Cursor संपादक |
| `claude` | Claude AI वेब या डेस्कटॉप |
| `claude-code` | Claude Code CLI |
| `openai-agents` | OpenAI agents प्लेटफ़ॉर्म |

### मैनुअल क्षमता कॉन्फ़िगरेशन

उपरोक्त सूचीबद्ध क्लाइंट्स के लिए, क्षमताओं को मैन्युअल रूप से कॉन्फ़िगर करें:

```bash
# Disable $ref pointer support and set max tool name length
npx dodopayments-mcp --no-capability=refs --capability=tool-name-length=40
```

Available capabilities:
- `top-level-unions` - शीर्ष स्तर के यूनियन प्रकारों का समर्थन
- `valid-json` - JSON स्ट्रिंग तर्क पार्सिंग को सही करना
- `refs` - स्कीमाओं में $ref पॉइंटर्स का समर्थन
- `unions` - यूनियन प्रकारों (anyOf) का समर्थन
- `formats` - फ़ॉर्मेट मान्यताओं का समर्थन
- `tool-name-length=N` - अधिकतम टूल नाम लंबाई

<Tip>
`npx dodopayments-mcp --describe-capabilities` को विस्तृत क्षमता जानकारी के लिए चलाएँ。
</Tip>

## उपकरण फ़िल्टरिंग

अपने AI सहायक के लिए प्रदर्शन को अनुकूलित करने और सुरक्षा बनाए रखने के लिए यह नियंत्रित करें कि कौन से API संचालन प्रदर्शित किए जाते हैं।

### संचालन प्रकार द्वारा फ़िल्टर करें

पढ़ने के लिए केवल या लिखने के संचालन तक पहुंच को प्रतिबंधित करें।

```bash
# Only expose read operations (GET requests)
npx dodopayments-mcp --operation=read

# Only expose write operations (POST, PUT, DELETE)
npx dodopayments-mcp --operation=write
```

<Tip>
विकास के दौरान अपने डेटा में आकस्मिक परिवर्तनों को रोकने के लिए `--operation=read` का उपयोग करें。
</Tip>

### संसाधन द्वारा फ़िल्टर करें

विशिष्ट API संसाधनों के लिए उपकरणों को सीमित करें।

```bash
# Only expose payment-related tools
npx dodopayments-mcp --resource=payments

# Expose multiple resources
npx dodopayments-mcp --resource=payments --resource=customers

# Use wildcards for resource groups
npx dodopayments-mcp --resource=subscription*
```

### विशिष्ट उपकरणों को शामिल या बाहर करें

कौन से व्यक्तिगत उपकरण उपलब्ध हैं, इसे ठीक से ट्यून करें।

```bash
# Include only specific tools
npx dodopayments-mcp --tool=create_payments --tool=list_payments

# Exclude specific tools
npx dodopayments-mcp --no-tool=delete_products

# Disable the docs search tool
npx dodopayments-mcp --no-tools=docs

# List all available tools
npx dodopayments-mcp --list
```

### फ़िल्टर संयोजित करें

सटीक नियंत्रण के लिए एक साथ कई फ़िल्टर लागू करें।

```bash
# Only payment and subscription read operations
npx dodopayments-mcp --operation=read --resource=payments --resource=subscriptions
```

### URL क्वेरी पैरामीटर फ़िल्टरिंग (दूरस्थ सर्वर)

दूरस्थ सर्वर का उपयोग करते समय, URL क्वेरी पैरामीटर के माध्यम से फ़िल्टर लागू करें:

```
https://mcp.dodopayments.com/?operation=read
https://mcp.dodopayments.com/?resource=payments
```

## गतिशील उपकरण मोड

बड़े APIs के लिए, सभी उपकरणों को एक साथ लोड करना AI के संदर्भ विंडो को अभिभूत कर सकता है। गतिशील उपकरण मोड एक अधिक कुशल विकल्प प्रदान करता है।

```bash
npx dodopayments-mcp --tools=dynamic
```

गतिशील मोड में, तीन मेटा-उपकरण व्यक्तिगत API उपकरणों को प्रतिस्थापित करते हैं:

1. **`list_api_endpoints`** - वैकल्पिक फ़िल्टरिंग के साथ उपलब्ध विधियाँ खोजता है
2. **`get_api_endpoint_schema`** - किसी विशेष विधि के लिए विस्तृत स्कीमा प्राप्त करता है
3. **`invoke_api_endpoint`** - उपयुक्त पैरामीटर के साथ किसी भी विधि को निष्पादित करता है

<Info>
डायनामिक टूल्स उन APIs के लिए स्वचालित रूप से सक्षम होते हैं जिनमें 50 से अधिक विधियाँ होती हैं। सभी टूल्स शामिल करने के लिए `--tools=all` से ओवरराइड करें。
</Info>

```bash
# Use both dynamic tools and specific resources
npx dodopayments-mcp --tools=dynamic --resource=payments
```

## उन्नत उपयोग

### प्रोग्रामेटिक एकीकरण

कस्टम MCP सर्वर बनाएं या मौजूदा को प्रोग्रामेटिक रूप से बढ़ाएं।

```javascript
import { server, endpoints, init } from "dodopayments-mcp/server";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

// Initialize with all default endpoints
init({ server, endpoints });

// Start the server
const transport = new StdioServerTransport();
await server.connect(transport);
```

### कस्टम उपकरण विकास

अपने उपकरणों और व्यावसायिक तर्कों के साथ MCP सर्वर का विस्तार करें।

```javascript
import { server, init } from "dodopayments-mcp/server";
import createPayments from "dodopayments-mcp/tools/payments/create-payments";
import { zodToJsonSchema } from "zod-to-json-schema";
import { z } from "zod";

// Define a custom tool
const customTool = {
  tool: {
    name: 'calculate_payment_total',
    description: 'Calculate payment total with tax and fees',
    inputSchema: zodToJsonSchema(
      z.object({
        amount: z.number().describe('Base amount in cents'),
        tax_rate: z.number().describe('Tax rate as decimal'),
      })
    ),
  },
  handler: async (client, args) => {
    const total = args.amount * (1 + args.tax_rate);
    return {
      content: [{
        type: 'text',
        text: JSON.stringify({ total, currency: 'usd' }),
      }],
    };
  },
};

// Initialize with custom tools
init({ 
  server, 
  endpoints: [createPayments, customTool] 
});
```

## सुरक्षा सर्वोत्तम प्रथाएँ

अपने API क्रेडेंशियल्स की सुरक्षा करें और सुरक्षित एकीकरण बनाए रखें।

<AccordionGroup>
{/* LOCKED_PATTERN_58b09b66cd00f40de384d770573ae848 */}
**साखों को संस्करण नियंत्रण में कभी कमिट न करें**

API कुंजियों को वातावरण चर या सुरक्षित सीक्रेट प्रबंधन प्रणालियों में स्टोर करें।

```bash
# Use environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."

# Use a .env file (add to .gitignore)
echo "DODO_PAYMENTS_API_KEY=dodo_test_..." > .env
```

**नियमित रूप से कुंजियाँ घुमाएँ**

नियमित रूप से नए API कुंजी उत्पन्न करें और अपने डोडो भुगतान डैशबोर्ड के माध्यम से पुराने को रद्द करें।

**विकास के लिए परीक्षण कुंजियाँ उपयोग करें**

हमेशा विकास के दौरान test mode API कुंजियाँ उपयोग करें ताकि उत्पादन डेटा प्रभावित न हो。
</Accordion>

{/* LOCKED_PATTERN_1d0876565ecc6c625335a3849461e889 */}
**उत्पादन में टूल फ़िल्टरिंग लागू करें**
</Accordion>

अपने AI सहायक को केवल वही संचालन प्रदर्शित करें जो आवश्यक हैं।

```bash
# Production: read-only access
npx dodopayments-mcp --operation=read

# Development: full access
npx dodopayments-mcp
```

**दूरस्थ सर्वरों के लिए प्रमाणीकरण लागू करें**

जब दूरस्थ परिनियोजन करते हैं, तो हमेशा `Authorization` हेडर या `x-dodo-payments-api-key` हेडर के माध्यम से प्रमाणीकरण आवश्यक करें।

**API उपयोग की निगरानी करें**

अपने Dodo Payments डैशबोर्ड के माध्यम से MCP सर्वर गतिविधि को ट्रैक करें और असामान्य पैटर्न के लिए अलर्ट सेट करें。
</Accordion>

{/* LOCKED_PATTERN_ddb4fc82978c8dda72d606214fdad81d */}
**रिमोट सर्वरों के लिए HTTPS का उपयोग करें**
</Accordion>

हमेशा HTTPS एंडपॉइंट के पीछे दूरस्थ MCP सर्वरों को तैनात करें।

**रेट लिमिटिंग लागू करें**

MCP सर्वर और API स्तरों पर दुरुपयोग से बचाने के लिए दर सीमाएँ लागू करें।

**नेटवर्क पहुंच को प्रतिबंधित करें**

फ़ायरवॉल नियम कॉन्फ़िगर करें ताकि यह सीमित हो कि कौन से क्लाइंट आपके MCP सर्वर से जुड़ सकते हैं。
</Accordion>
</AccordionGroup>

## समस्या निवारण

<AccordionGroup>
{/* LOCKED_PATTERN_3e10fc2511884be492da344a929e2311 */}
**अपनी API कुंजी सत्यापित करें**

सुनिश्चित करें कि आपकी API कुंजी सही ढंग से सेट है और आवश्यक अनुमतियाँ हैं।

```bash
# Test your API key
curl -H "Authorization: Bearer dodo_test_..." \
  https://test.dodopayments.com/payments
```

**अपने नेटवर्क कनेक्शन की जांच करें**

सुनिश्चित करें कि आप डोडो भुगतान API एंडपॉइंट्स तक पहुँच सकते हैं।

**क्लाइंट लॉग की समीक्षा करें**

कनेक्शन समस्याओं का निदान करने के लिए अपने MCP क्लाइंट में विस्तृत लॉगिंग सक्षम करें。
</Accordion>

{/* LOCKED_PATTERN_8f29ea613a43315c29bf194043f8ada2 */}
**API कुंजी वातावरण की पुष्टि करें**
</Accordion>

सुनिश्चित करें कि आप परीक्षण कुंजियों का उपयोग कर रहे हैं परीक्षण एंडपॉइंट्स के साथ और उत्पादन एंडपॉइंट्स के साथ लाइव कुंजियाँ।

**पर्यावरण चर की जांच करें**

`DODO_PAYMENTS_ENVIRONMENT` सही ढंग से सेट किया गया है यह जांचें (production के लिए `live_mode`)।

**क्रेडेंशियल्स को फिर से उत्पन्न करें**

यदि समस्याएँ बनी रहती हैं, तो अपने डैशबोर्ड के माध्यम से नई API कुंजी उत्पन्न करें。
</Accordion>

{/* LOCKED_PATTERN_565675849d369f82d965d1f3a122632b */}
**इनपुट पैरामीटर मान्य करें**

सुनिश्चित करें कि AI सहायक प्रत्येक उपकरण के लिए सही स्वरूपित पैरामीटर प्रदान कर रहा है।

**त्रुटि संदेशों की समीक्षा करें**

देखें कि API से त्रुटि प्रतिक्रिया में क्या गलत हुआ है।

**API के साथ सीधे परीक्षण करें**

Dodo Payments API को सीधे curl या Postman के माध्यम سے कॉल करते समय ऑपरेशन काम करता है यह सत्यापित करें。
</Accordion>

{/* LOCKED_PATTERN_776734428589daf9897a729aa0a38aed */}
**किसी अन्य क्लाइंट प्रीसेट को आज़माएं**

यदि उपकरण सही ढंग से काम नहीं कर रहे हैं, तो अपने क्लाइंट को स्पष्ट रूप से निर्दिष्ट करने का प्रयास करें:

```bash
npx dodopayments-mcp --client=cursor
```

**क्षमता समर्थन की जांच करें**

कुछ क्लाइंट सभी JSON स्कीमा सुविधाओं का समर्थन नहीं करते। यह समझने के लिए `--describe-capabilities` का उपयोग करें कि क्या समर्थित है。
</Accordion>
</AccordionGroup>

## संसाधन

<CardGroup cols={2}>
{/* LOCKED_PATTERN_4325afcb089e351f99ef591b38203ac2 */}
  पूरा Dodo Payments API दस्तावेज़ीकरण देखें
</Card>

{/* LOCKED_PATTERN_1cbca36bdeb2bbbc77032220bd30c277 */}
  Model Context Protocol मानक के बारे में और जानें
</Card>

{/* LOCKED_PATTERN_02488b19a875a7c4cd24c2f5651e4831 */}
  MCP सर्वर स्रोत कोड देखें और योगदान दें
</Card>

{/* LOCKED_PATTERN_d4dbe77bbb87d236d98e318084853b86 */}
  NPM से MCP सर्वर इंस्टॉल करें
</Card>
</CardGroup>
