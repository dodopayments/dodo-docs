---
title: चेकआउट सत्र
description: सुरक्षित, होस्टेड चेकआउट अनुभव बनाएं जो एक बार की खरीद और
  सब्सक्रिप्शन के लिए पूर्ण भुगतान प्रवाह को संभालते हैं, जिसमें पूर्ण अनुकूलन
  नियंत्रण होता है।
icon: cart-shopping
---

<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  अपना पहला चेकआउट सत्र 5 मिनट से कम में चालू करें
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  पूर्ण API दस्तावेज़ का अन्वेषण करें और चेकआउट सत्र अनुरोधों और प्रतिक्रियाओं का इंटरैक्टिव रूप से परीक्षण करें।
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  एक सत्र बनाने से पहले मूल्य निर्धारण, कर, और कुल राशि की गणना करें।
</Card>
</CardGroup>

<Info>
**Session Validity**: डिफ़ॉल्ट रूप से चेकआउट सत्र 24 घंटे के लिए मान्य होते हैं। यदि आप `confirm=true` को अपने अनुरोध में भेजते हैं, तो सत्र केवल 15 मिनट के लिए मान्य रहेगा।
</Info>

## पूर्वापेक्षाएँ

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  आपको API एक्सेस के साथ एक सक्रिय Dodo Payments व्यापारी खाता चाहिए।
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  Dodo Payments डैशबोर्ड से अपनी API क्रेडेंशियल जनरेट करें:
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  चेकआउट सत्र को लागू करने से पहले अपने उत्पाद Dodo Payments डैशबोर्ड में बनाएं।
</Step>
</Steps>

## अपना पहला चेकआउट सत्र बनाना

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### API प्रतिक्रिया

उपरोक्त सभी विधियाँ समान प्रतिक्रिया संरचना लौटाती हैं:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
API प्रतिक्रिया से `checkout_url` निकालें।
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
अपने ग्राहक को उनकी खरीद पूरी करने के लिए चेकआउट URL पर भेजें।

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Alternative Integration Options**: रीडायरेक्ट करने की बजाय, आप [Overlay Checkout](/developer-resources/overlay-checkout) (मॉडल ओवरले) या [Inline Checkout](/developer-resources/inline-checkout) (पूर्ण रूप से एम्बेडेड) का उपयोग करके चेकआउट को सीधे अपनी पेज में शामिल कर सकते हैं। दोनों विकल्प एक ही चेकआउट सत्र URL का उपयोग करते हैं।
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
भुगतान के बाद, ग्राहक आपका `return_url` पर क्वेरी पैरामीटर सहित रीडायरेक्ट किए जाते हैं जिनमें भुगतान/सदस्यता ID, स्थिति, ग्राहक ईमेल, और कोई भी लाइसेंस कुंजियाँ शामिल होती हैं। पूरी सूची के लिए [`return_url` parameter docs](#request-body) देखें।
</Step>
</Steps>

## अनुरोध शरीर

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  हर चेकआउट सत्र के लिए आवश्यक फ़ील्ड
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  अपने चेकआउट अनुभव को अनुकूलित करने के लिए अतिरिक्त कॉन्फ़िगरेशन
</Card>
</CardGroup>

### आवश्यक फ़ील्ड

<ParamField body="product_cart" type="array" required>
चेकआउट सत्र में शामिल करने के लिए उत्पादों की सूची। प्रत्येक उत्पाद के पास आपके Dodo Payments डैशबोर्ड से एक मान्य `product_id` होना चाहिए।

<Tip>
**Mixed Checkout**: आप एक ही चेकआउट सत्र में एक-बार भुगतान उत्पादों और सदस्यता उत्पादों को संयोजित कर सकते हैं। यह सेटअप शुल्क के साथ सदस्यताएँ, हार्डवेयर बंडल के साथ SaaS, और अधिक जैसे शक्तिशाली उपयोग मामलों को सक्षम करता है।
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
आपके Dodo Payments डैशबोर्ड से उत्पाद का अद्वितीय पहचानकर्ता।

**Example:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
उत्पाद की मात्रा।

**Example:** `1` एकल आइटम के लिए, `3` कई मात्राओं के लिए
</ParamField>

<ParamField body="addons" type="array">
सदस्यता उत्पाद के लिए संलग्न करने के लिए ऐडऑन की सूची। केवल तभी मान्य जब उत्पाद सदस्यता हो।

{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
यदि pay_what_you_want सक्षम है तो ग्राहक द्वारा भुगतान की जाने वाली राशि। यदि अक्षम है, तो यह फ़ील्ड अनदेखा किया जाएगा।

**Format**: मुद्रा की सबसे नीचली इकाई में प्रदर्शित (जैसे USD के लिए सेंट)। उदाहरण के लिए, $1.00 चार्ज करने के लिए `100` भेजें।
</ParamField>
</Expandable>
</ParamField>

<Tip>
**Find Your Product IDs**: आप अपने Dodo Payments डैशबोर्ड में Products → View Details के अंतर्गत उत्पाद ID पा सकते हैं, या [List Products API](/api-reference/products/get-products) का उपयोग करके।
</Tip>

### वैकल्पिक फ़ील्ड

इन फ़ील्ड्स को चेकआउट अनुभव को अनुकूलित करने और आपके भुगतान प्रवाह में व्यावसायिक तर्क जोड़ने के लिए कॉन्फ़िगर करें।

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
ग्राहक जानकारी। आप चेकआउट के दौरान उनके ID का उपयोग करके मौजूदा ग्राहक को संलग्न कर सकते हैं या नया ग्राहक रिकॉर्ड बना सकते हैं।

<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}

{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
किसी मौजूदा ग्राहक का अद्वितीय पहचानकर्ता। इसका उपयोग एक नए ग्राहक बनाने के बजाय चेकआउट सत्र को मौजूदा ग्राहक से जोड़ने के लिए करें।
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
चेकआउट के दौरान एक नया ग्राहक रिकॉर्ड बनाएं।

{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
ग्राहक का ईमेल पता। नया ग्राहक बनाने पर आवश्यक।
</ParamField>

<ParamField body="name" type="string">
ग्राहक का पूरा नाम जैसा कि रसीदों और चालानों पर दिखाई देना चाहिए।

**Example**: `"John Doe"` या `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
ग्राहक का फोन नंबर अंतरराष्ट्रीय प्रारूप में। कुछ भुगतान विधियों और धोखाधड़ी रोकथाम के लिए आवश्यक।

**Format**: देश कोड शामिल करें, उदाहरण के लिए `"+1234567890"` (अमेरिकी नंबरों के लिए)
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
सटीक कर गणना, धोखाधड़ी रोकथाम, और नियामक अनुपालन के लिए बिलिंग पता जानकारी।

<Info>
जब `confirm` को `true` पर सेट किया जाता है, तो सफल सत्र निर्माण के लिए सभी बिलिंग पता फ़ील्ड आवश्यक हो जाते हैं।
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
पूर्ण सड़क पता जिसमें मकान संख्या, सड़क का नाम, और यदि लागू हो तो अपार्टमेंट/यूनिट संख्या शामिल हो।

**Example**: `"123 Main St, Apt 4B"` या `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
शहर या नगरपालिका का नाम।

**Example**: `"San Francisco"`, `"New York"`, `"London"`
</ParamField>

<ParamField body="state" type="string">
राज्य, प्रांत, या क्षेत्र का नाम। पूरी नाम या मानक संक्षिप्त रूप का उपयोग करें।

**Example**: `"California"` या `"CA"`, `"Ontario"` या `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
दो-अक्षर ISO देश कोड (ISO 3166-1 alpha-2)। जब billing_address प्रदान किया गया हो, यह फ़ील्ड हमेशा आवश्यक होता है।

**Examples**: `"US"` (संयुक्त राज्य), `"CA"` (कनाडा), `"GB"` (यूनाइटेड किंगडम), `"DE"` (जर्मनी)

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  समर्थित देशों और उनके ISO कोड की पूरी सूची देखें
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
देश की आवश्यकताओं के अनुसार डाक कोड, ZIP कोड, या समान।

**Examples**: `"94102"` (यूएस), `"M5V 3A8"` (कनाडा), `"SW1A 1AA"` (यूके)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
ग्राहकों के लिए उपलब्ध भुगतान विधियों को नियंत्रित करें। यह विशिष्ट बाजारों या व्यावसायिक आवश्यकताओं के लिए अनुकूलन में मदद करता है।

**Available Options**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**Critical**: हमेशा `credit` और `debit` को वैकल्पिक विकल्प के रूप में शामिल करें ताकि पसंदीदा भुगतान विधियाँ उपलब्ध न होने पर चेकआउट असफल न हो।
</Warning>

**उदाहरण**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
डिफ़ॉल्ट मुद्रा चयन को फिक्स बिलिंग मुद्रा के साथ ओवरराइड करें। ISO 4217 मुद्रा कोड का उपयोग करता है।

**Supported Currencies**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR`, और अन्य

**Example**: `"USD"` US डॉलर के लिए, `"EUR"` यूरो के लिए

<Note>
यह फ़ील्ड केवल तभी प्रभावी होता है जब adaptive pricing सक्षम हो। यदि adaptive pricing अक्षम है, तो उत्पाद की डिफ़ॉल्ट मुद्रा का उपयोग किया जाएगा।
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
लौटते ग्राहकों के लिए पहले से सहेजे गए भुगतान विधियों को प्रदर्शित करें, जिससे चेकआउट गति और उपयोगकर्ता अनुभव में सुधार होता है।
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
भुगतान पूरा होने के बाद ग्राहकों को पुनर्निर्देशित करने के लिए URL। Dodo Payments आपके URL में निम्न क्वेरी पैरामीटर जोड़ता है:

| Parameter | Type | Condition |
|-----------|------|-----------|
| `payment_id` | `string` | एक-समय भुगतानों के लिए हमेशा मौजूद |
| `subscription_id` | `string` | सदस्यता भुगतानों के लिए हमेशा मौजूद |
| `status` | `string` | हमेशा मौजूद |
| `license_key` | `string` | यदि उत्पाद में लाइसेंस कुंजियाँ सक्षम हैं तो मौजूद। यदि कई कुंजियाँ हों तो अल्पविराम से अलग |
| `email` | `string` | यदि ग्राहक के पास रिकॉर्ड में ईमेल है तो मौजूद |


**Example redirect URLs:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
लाइसेंस कुंजियाँ प्रदर्शित करने या अपने रिटर्न पेज पर तुरंत पुष्टि भेजने के लिए `license_key` और `email` क्वेरी पैरामीटर का उपयोग करें, ताकि अतिरिक्त API कॉल की आवश्यकता न पड़े।
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
यदि true है, तो सभी सत्र विवरण तुरंत अंतिम रूप से निर्धारित हो जाते हैं। यदि आवश्यक डेटा गायब है तो API त्रुटि देगा।
</ParamField>

<ParamField body="discount_code" type="string">
चेकआउट सत्र पर डिस्काउंट कोड लागू करें।
</ParamField>

<ParamField body="metadata" type="object">
सत्र के बारे में अतिरिक्त जानकारी संग्रहीत करने के लिए कस्टम की-वैल्यू पेयर्स।
</ParamField>

<ParamField body="force_3ds" type="boolean">
इस सत्र के लिए व्यापारी की डिफ़ॉल्ट 3DS व्यवहार को ओवरराइड करें।
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
कम से कम पता संग्रह मोड सक्षम करें। जब सक्षम हो, तो चेकआउट केवल निम्न जानकारी एकत्र करता है:
- **Country**: कर निर्धारण के लिए हमेशा आवश्यक
- **ZIP/Postal code**: केवल उन क्षेत्रों में जहां बिक्री कर, VAT, या GST गणना के लिए आवश्यक हो


यह अनावश्यक फ़ॉर्म फ़ील्ड को हटाकर चेकआउट घर्षण को काफी कम करता है।

<Tip>
तेज़ चेकआउट पूरा करने के लिए कम से कम पता सक्षम करें। जिन व्यवसायों को पूर्ण बिलिंग विवरण की आवश्यकता है, उनके लिए अभी भी पूर्ण पता संग्रह उपलब्ध रहता है।
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
चेकआउट इंटरफ़ेस की उपस्थिति और व्यवहार को अनुकूलित करें।

{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
चेकआउट इंटरफ़ेस के लिए थीम। विकल्प: `light`, `dark`, या `system`।
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
चेकआउट इंटरफ़ेस में ऑर्डर विवरण अनुभाग प्रदर्शित करें।
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
लागू उत्पादों के लिए "ऑन-डिमांड" टैग दिखाएँ।
</ParamField>

<ParamField body="force_language" type="string">
चेकआउट इंटरफ़ेस को एक विशिष्ट भाषा में रेंडर करने के लिए बाध्य करें। डिफ़ॉल्ट रूप से, चेकआउट ग्राहक के ब्राउज़र भाषा का स्वतः पता लगाता है।
</ParamField>

**Supported language codes:**
`ar` (Arabic), `ca` (Catalan), `de` (German), `en` (English), `es` (Spanish), `fr` (French), `he` (Hebrew), `id` (Indonesian), `it` (Italian), `ja` (Japanese), `ko` (Korean), `ms` (Malay), `nl` (Dutch), `pl` (Polish), `pt` (Portuguese), `ro` (Romanian), `ru` (Russian), `sv` (Swedish), `th` (Thai), `tr` (Turkish), `zh` (Chinese)

**Example**: `"es"` Spanish के लिए, `"ja"` Japanese के लिए
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
चेकआउट सत्र के लिए विशिष्ट सुविधाएँ और व्यवहार कॉन्फ़िगर करें।

{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
ग्राहकों को चेकआउट के दौरान उनकी पसंदीदा मुद्रा चुनने दें।
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
चेकआउट इंटरफ़ेस में डिस्काउंट कोड इनपुट फ़ील्ड दिखाएँ।
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
चेकआउट के दौरान ग्राहक के फोन नंबर एकत्र करें।
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
ग्राहकों को कर पहचान संख्या दर्ज करने की अनुमति दें।
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
मौजूदा रिकॉर्ड को अपडेट करने के बजाय एक नया ग्राहक रिकॉर्ड बनाने के लिए मजबूर करें।
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
ग्राहकों को चेकआउट के दौरान उनका ईमेल पता संपादित करने की अनुमति दें।
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
ग्राहकों को चेकआउट के दौरान उनका नाम संपादित करने की अनुमति दें।
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
ग्राहकों को चेकआउट के दौरान सड़क पता संपादित करने की अनुमति दें।
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
ग्राहकों को चेकआउट के दौरान शहर संपादित करने की अनुमति दें।
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
ग्राहकों को चेकआउट के दौरान राज्य संपादित करने की अनुमति दें।
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
ग्राहकों को चेकआउट के दौरान देश संपादित करने की अनुमति दें।
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
ग्राहकों को चेकआउट के दौरान ज़िपकोड संपादित करने की अनुमति दें।
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
कम से कम पता संग्रह मोड सक्षम करें। जब सक्षम हो, तो चेकआउट केवल निम्न जानकारी एकत्र करता है:
- **Country**: कर निर्धारण के लिए हमेशा आवश्यक
- **ZIP/Postal code**: केवल उन क्षेत्रों में जहां बिक्री कर, VAT, या GST गणना के लिए आवश्यक हो


यह अनावश्यक फ़ॉर्म फ़ील्ड को हटाकर चेकआउट घर्षण को काफी कम करता है।

<Tip>
तेज़ चेकआउट पूरा करने के लिए कम से कम पता सक्षम करें। जिन व्यवसायों को पूर्ण बिलिंग विवरण की आवश्यकता है, उनके लिए अभी भी पूर्ण पता संग्रह उपलब्ध रहता है।
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
डिफ़ॉल्ट भुगतान सफलता पृष्ठ को छोड़ें और भुगतान पूरा होने के बाद ग्राहकों को तुरंत आपके `return_url` पर भेजें।

<Tip>
जब आपके पास बेहतर उपयोगकर्ता अनुभव प्रदान करने वाला एक कस्टम सफलता पृष्ठ हो, या मोबाइल ऐप और एम्बेडेड चेकआउट फ्लो हों, तब इसका उपयोग करें।
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
चेकआउट के दौरान ग्राहकों से अतिरिक्त जानकारी एकत्र करने के लिए कस्टम फ़ॉर्म फ़ील्ड जोड़ें। आप प्रति चेकआउट सत्र 5 कस्टम फ़िल्ड्स तक परिभाषित कर सकते हैं। ग्राहक की प्रतिक्रियाएँ वेबहुक पेलोड में शामिल होती हैं और API के माध्यम से उपलब्ध होती हैं।

{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
इस फ़ील्ड की यूनिक पहचानकर्ता। वेबहुक पेलोड और API प्रतिक्रियाओं में यह चाबी के रूप में उपयोग होता है।

**Example**: `"company_name"`, `"referral_source"`, `"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
ग्राहक को चेकआउट फ़ॉर्म पर दिखाई देने वाला लेबल।

**Example**: `"Company Name"`, `"How did you hear about us?"`, `"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
फ़ील्ड का प्रकार जो इनपुट सत्यापन नियमों को निर्धारित करता है।

**Available types**:
- `text` - एकल-पंक्ति टेक्स्ट इनपुट
- `number` - संख्यात्मक इनपुट
- `email` - सत्यापन के साथ ईमेल पता
- `url` - सत्यापन के साथ URL
- `date` - तारीख चुनने वाला
- `dropdown` - पूर्वनिर्धारित विकल्पों से चयन
- `boolean` - हाँ/नहीं टॉगल
</ParamField>

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
क्या इस फ़ील्ड को भरा जाना आवश्यक है इससे पहले कि चेकआउट पूरा हो सके।
</ParamField>

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
इनपुट फ़ील्ड में दिखाई देने वाला प्लेसहोल्डर टेक्स्ट।
</ParamField>

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
`dropdown` फ़ील्ड प्रकार के लिए विकल्पों की सूची। जब `field_type` `dropdown` हो, तब आवश्यक। अन्य प्रकारों के लिए अनदेखा।

**Example**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
कस्टम फ़ील्ड्स के लिए ग्राहक प्रतिक्रियाएँ निम्न में शामिल होती हैं:
- **Webhooks**: `payment.succeeded`, `subscription.created`, और अन्य संबंधित इवेंट पेलोड में `custom_field_responses` एरे होता है
- **API responses**: Payment और subscription ऑब्जेक्ट्स में `custom_field_responses` शामिल होता है
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
सदस्यता उत्पादों वाले चेकआउट सत्रों के लिए अतिरिक्त कॉन्फ़िगरेशन।

{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
पहली वसूली से पहले परीक्षण अवधि के दिन। 0 से 10000 दिनों के बीच होना चाहिए।
</ParamField>

<ParamField body="on_demand" type="object">
उपयोग-आधारित या मीटर-आधारित बिलिंग के लिए ऑन-डिमांड सदस्यता कॉन्फ़िगरेशन।

{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
यदि true पर सेट है, तो कोई शुल्क नहीं लिया जाएगा और भविष्य में उपयोग के लिए केवल भुगतान विधि विवरण अधिकृत किए जाएंगे।
</ParamField>

<ParamField body="product_price" type="integer">
ग्राहक से प्रारंभिक चार्ज के लिए उत्पाद मूल्य। यदि निर्दिष्ट नहीं है तो उत्पाद का संरक्षित मूल्य उपयोग किया जाएगा।

**Format**: मुद्रा की सबसे नीचली इकाई में प्रदर्शित (जैसे USD के लिए सेंट)। उदाहरण के लिए, $1.00 चार्ज करने के लिए `100` भेजें।
</ParamField>

<ParamField body="product_currency" type="string">
उत्पाद मूल्य की वैकल्पिक मुद्रा। यदि निर्दिष्ट नहीं है, तो उत्पाद की मुद्रा डिफ़ॉल्ट रहती है।
</ParamField>

<ParamField body="product_description" type="string">
बिलिंग और लाइन आइटम्स के लिए वैकल्पिक उत्पाद विवरण ओवरराइड। यदि निर्दिष्ट नहीं है तो उत्पाद का संग्रहीत विवरण उपयोग किया जाएगा।
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
क्या उत्पाद मूल्य में adaptive currency फीसेज़ शामिल की जानी चाहिए (true) या ऊपर जोड़ी जानी चाहिए (false)। यदि adaptive pricing सक्षम नहीं है तो अनदेखा किया जाएगा।
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## उपयोग के उदाहरण

यहाँ विभिन्न व्यावसायिक परिदृश्यों के लिए अलग-अलग चेकआउट सत्र कॉन्फ़िगरेशन दर्शाने वाले 10 व्यापक उदाहरण दिए गए हैं:

### 1. सरल एकल उत्पाद चेकआउट

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. बहु-उत्पाद कार्ट

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. परीक्षण अवधि के साथ सदस्यता

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. पूर्व-पूर्व पुष्टि चेकआउट

<Note>
जब `confirm` को `true` पर सेट किया जाता है, तो ग्राहक सीधे चेकआउट पृष्ठ पर ले जाया जाएगा, किसी भी पुष्टि चरण को दरकिनार करते हुए।
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. मुद्रा ओवरराइड के साथ चेकआउट

<Note>
`billing_currency` ओवरराइड केवल तभी प्रभावी होता है जब आपके खाते की सेटिंग्स में adaptive currency सक्षम हो। यदि adaptive currency अक्षम है, तो यह पैरामीटर प्रभावी नहीं होगा।
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. लौटते ग्राहकों के लिए सहेजे गए भुगतान विधियाँ

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. कर ID संग्रह के साथ B2B चेकआउट

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. डार्क थीम चेकआउट के साथ डिस्काउंट कोड

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. क्षेत्रीय भुगतान विधियाँ (भारत के लिए UPI)

UPI कॉन्फ़िगरेशन और परीक्षण के बारे में विस्तृत जानकारी के लिए <a href="/features/payment-methods/india">India Payment Methods</a> पृष्ठ देखें।

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. BNPL (Buy Now Pay Later) चेकआउट

BNPL कॉन्फ़िगरेशन और परीक्षण के बारे में विस्तृत जानकारी के लिए <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a> पृष्ठ देखें।

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. त्वरित चेकआउट के लिए मौजूदा भुगतान विधियों का उपयोग

ग्राहक की सहेजी गई भुगतान विधि का उपयोग करके एक चेकआउट सत्र बनाएं जो तुरंत संसाधित हो, भुगतान विधि संग्रह को छोड़ते हुए:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
जब `payment_method_id` का उपयोग किया जाता है, तो `confirm` को `true` पर सेट करना अनिवार्य है और एक मौजूदा `customer_id` प्रदान किया जाना चाहिए। भुगतान विधि की पात्रता भुगतान मुद्रा के लिए सत्यापित की जाएगी।
</Warning>

<Info>
भुगतान विधि ग्राहक की होनी चाहिए और भुगतान मुद्रा के साथ संगत होनी चाहिए। यह लौटते ग्राहकों के लिए वन-क्लिक खरीदारी सक्षम बनाता है।
</Info>

### 12. साफ़ भुगतान URL के लिए शॉर्ट लिंक

कस्टम स्लग के साथ शॉर्ट किए गए, साझा करने योग्य भुगतान लिंक बनाएं:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
शॉर्ट लिंक SMS, ईमेल, या सोशल मीडिया साझा करने के लिए परिपूर्ण हैं। वे लंबे URL की तुलना में याद रखने में आसान और अधिक ग्राहक विश्वास बनाते हैं।
</Tip>

### 13. भुगतान सफलता पृष्ठ छोड़ें और तुरंत रीडायरेक्ट करें

भुगतान पूरा होने के बाद ग्राहकों को तुरंत रीडायरेक्ट करें, डिफ़ॉल्ट सफलता पृष्ठ को स्किप करते हुए:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
जब आपके पास एक कस्टम सफलता पृष्ठ हो जो डिफ़ॉल्ट भुगतान सफलता पृष्ठ की तुलना में बेहतर उपयोगकर्ता अनुभव प्रदान करता हो, तब `redirect_immediately: true` का उपयोग करें। यह मोबाइल ऐप और एम्बेडेड चेकआउट फ़्लो के लिए विशेष रूप से उपयोगी है।
</Tip>

<Note>
जब `redirect_immediately` सक्षम होता है, तो ग्राहक भुगतान पूरा होने के तुरंत बाद आपके `return_url` पर रीडायरेक्ट हो जाते हैं, डिफ़ॉल्ट सफलता पृष्ठ को पूरी तरह से छोड़ते हुए।
</Note>

### 14. भाषा ज़ोर देकर सेट करना

चेकआउट को एक विशिष्ट भाषा में प्रदर्शित करने के लिए बाध्य करें, ग्राहक के ब्राउज़र भाषा पता लगाने को ओवरराइड करते हुए:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
जब आप जानते हों कि आपका ग्राहक कौन सी भाषा पसंद करता है (उदा. उनके खाते की सेटिंग्स से) या यदि आप विशिष्ट क्षेत्रीय बाजार को लक्षित कर रहे हों, तब `force_language` का उपयोग करें।
</Tip>

**Supported languages:** Arabic (`ar`), Catalan (`ca`), Chinese (`zh`), Dutch (`nl`), English (`en`), French (`fr`), German (`de`), Hebrew (`he`), Indonesian (`id`), Italian (`it`), Japanese (`ja`), Korean (`ko`), Malay (`ms`), Polish (`pl`), Portuguese (`pt`), Romanian (`ro`), Russian (`ru`), Spanish (`es`), Swedish (`sv`), Thai (`th`), Turkish (`tr`)

### 15. कस्टम फ़ील्ड्स एकत्र करना

चेकआउट के दौरान कस्टम फ़ील्ड्स का उपयोग करके ग्राहकों से अतिरिक्त जानकारी एकत्र करें:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
कस्टम फ़ील्ड प्रतिक्रियाएँ स्वचालित रूप से वेबहुक पेलोड (`payment.succeeded`, `subscription.created`, आदि) में शामिल होती हैं और API के माध्यम से प्राप्त की जा सकती हैं। उन्हें अपने CRM को समृद्ध करने, ऑनबोर्डिंग फ्लो ट्रिगर करने, या ग्राहक अनुभव को अनुकूलित करने के लिए उपयोग करें।
</Info>

**Available field types:** `text`, `number`, `email`, `url`, `date`, `dropdown`, `boolean`

## चेकआउट सत्रों का पूर्वावलोकन

चेकआउट सत्र बनाने से पहले आप मूल्य विवरण, कर, छूट, और कुल राशि का पूर्वावलोकन देख सकते हैं। यह ग्राहकों को चेकआउट पर जाने से पहले सटीक मूल्य दिखाने में मदद करता है।

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  पूर्ण पूर्वावलोकन एंडपॉइंट दस्तावेज़ देखें।
</Card>

## Dynamic Links से Checkout Sessions की ओर बढ़ना

### प्रमुख भिन्नताएँ

पिछले समय में, Dynamic Links के साथ भुगतान लिंक बनाते समय आपको ग्राहक का पूरा बिलिंग पता प्रदान करना अनिवार्य था।

Checkout Sessions के साथ, यह अब आवश्यक नहीं है। आप बस जो जानकारी आपके पास है वह भेज सकते हैं, और हम बाकी संभाल लेंगे। उदाहरण के लिए:
- यदि आपको केवल ग्राहक का बिलिंग देश पता है, तो केवल वह प्रदान करें।
- चेकआउट फ्लो आवश्यक विवरण अपने आप एकत्र करेगा और ग्राहक को भुगतान पृष्ठ पर ले जाएगा।
- दूसरी ओर, यदि आपके पास पहले से सभी आवश्यक जानकारी है और आप सीधे भुगतान पृष्ठ पर जाना चाहते हैं, तो आप पूरा डेटा सेट भेज सकते हैं और अपने अनुरोध बॉडी में `confirm=true` शामिल कर सकते हैं।

### माइग्रेशन प्रक्रिया

Dynamic Links से Checkout Sessions में माइग्रेट करना सरल है:

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
अपनी एकीकरण को नए API या SDK विधि के उपयोग के लिए अपडेट करें।
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
चेकआउट सत्र प्रारूप के अनुरूप अनुरोध पेलोड समायोजित करें।
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
हां। आपकी ओर से किसी अतिरिक्त हैंडलिंग या विशेष माइग्रेशन चरणों की आवश्यकता नहीं है।
</Step>
</Steps>
