---
title: C#
description: अपने .NET अनुप्रयोगों में Dodo Payments को आधुनिक async/await
  समर्थन के साथ एकीकृत करें
icon: c
keywords:
  - डोडो पेमेंट्स
  - C# SDK
  - .NET
  - NuGet पैकेज
---

C# SDK से C# में लिखे गए अनुप्रयोगों से Dodo Payments REST API तक सुविधाजनक पहुँच प्रदान होती है। इसमें एक असिंक्रोनस Task-आधारित API है जिसमें स्ट्रॉन्ग टाइपिंग, ऑटोमैटिक रिट्राइज, और व्यापक त्रुटि प्रबंधन है।

## स्थापना

पैकेज को [NuGet](https://www.nuget.org/packages/DodoPayments.Client) से इंस्टॉल करें:

```bash
dotnet add package DodoPayments.Client
```

<Info>
SDK के लिए .NET 8.0 या इसके बाद का संस्करण आवश्यक है। यह ASP.NET Core, कंसोल अनुप्रयोगों, और अन्य .NET प्रोजेक्ट प्रकारों के साथ काम करता है।
</Info>

## त्वरित आरम्भ

क्लाइंट को इनिशियलाइज़ करें और एक चेकआउट सेशन बनाएं:

```csharp
using System;
using DodoPayments.Client;
using DodoPayments.Client.Models.CheckoutSessions;

// Configured using the DODO_PAYMENTS_API_KEY and DODO_PAYMENTS_BASE_URL environment variables
DodoPaymentsClient client = new();

CheckoutSessionCreateParams parameters = new()
{
    ProductCart =
    [
        new()
        {
            ProductID = "product_id",
            Quantity = 1,
        },
    ],
};

var checkoutSessionResponse = await client.CheckoutSessions.Create(parameters);

Console.WriteLine(checkoutSessionResponse.SessionId);
```

<Warning>
हमेशा अपने API कुंजियों को environment variables, user secrets, या Azure Key Vault का उपयोग करके सुरक्षित रूप से संग्रहीत करें। उन्हें अपने स्रोत कोड में हार्डकोड न करें या संस्करण नियंत्रण में कमिट न करें।
</Warning>

## प्रमुख विशेषताएँ

<CardGroup cols={2}>
<Card title="Async/Await" icon="bolt">
  Full async Task-based API के साथ गैर-अवरुद्ध संचालन
</Card>

<Card title="Strong Typing" icon="shield-check">
  Nullable reference types के साथ व्यापक प्रकार सुरक्षा
</Card>

<Card title="Smart Retries" icon="repeat">
  अस्थायी त्रुटियों के लिए एक्सपोनेंशियल बैकऑफ़ के साथ स्वचालित रिट्राइज
</Card>

<Card title="Error Handling" icon="triangle-exclamation">
  सटीक त्रुटि प्रबंधन के लिए बिल्ट-इन अपवाद पदानुक्रम
</Card>
</CardGroup>

## कॉन्फ़िगरेशन

### पर्यावरण चर

```bash .env
DODO_PAYMENTS_API_KEY=your_api_key_here
```

```csharp
// Automatically reads from environment variables
DodoPaymentsClient client = new();
```

| प्रॉपर्टी      | पर्यावरण चर        | आवश्यक | डिफ़ॉल्ट मान                     |
| ------------- | --------------------------- | -------- | --------------------------------- |
| `BearerToken` | `DODO_PAYMENTS_API_KEY`     | true     | -                                 |
| `WebhookKey`  | `DODO_PAYMENTS_WEBHOOK_KEY` | false    | -                                 |
| `BaseUrl`     | `DODO_PAYMENTS_BASE_URL`    | true     | `"https://live.dodopayments.com"` |

### मैनुअल कॉन्फिगरेशन

```csharp
DodoPaymentsClient client = new() { BearerToken = "My Bearer Token" };
```

### वातावरण

लाइव और टेस्ट मोड के बीच स्विच करें:

```csharp
using DodoPayments.Client.Core;

DodoPaymentsClient client = new() { BaseUrl = EnvironmentUrl.TestMode };
```

### रिट्राइज

SDK डिफ़ॉल्ट रूप से 2 बार एक्सपोनेंशियल बैकऑफ़ के साथ स्वचालित रूप से पुनः प्रयास करता है। यह कनेक्शन त्रुटियों और स्थिति कोड 408, 409, 429, और 5xx पर पुनः प्रयास करता है।

```csharp
// Custom retry count
DodoPaymentsClient client = new() { MaxRetries = 3 };
```

### टाइमआउट्स

अनुरोध डिफ़ॉल्ट रूप से 1 मिनट के बाद टाइम आउट हो जाते हैं।

```csharp
DodoPaymentsClient client = new() { Timeout = TimeSpan.FromSeconds(30) };
```

### प्रति-अनुरोध ओवरराइड्स

एकल अनुरोध के लिए अस्थायी रूप से कॉन्फ़िगरेशन को `WithOptions` का उपयोग करके संशोधित करें:

```csharp
var response = await client
    .WithOptions(options => options with
    {
        Timeout = TimeSpan.FromSeconds(10),
        MaxRetries = 5,
    })
    .CheckoutSessions.Create(parameters);
```

## सामान्य ऑपरेशन्स

### एक चेकआउट सेशन बनाएँ

```csharp
var parameters = new CheckoutSessionCreateParams
{
    ProductCart =
    [
        new()
        {
            ProductID = "pdt_123",
            Quantity = 1
        }
    ],
    ReturnUrl = "https://yourdomain.com/return"
};

var session = await client.CheckoutSessions.Create(parameters);
Console.WriteLine($"Checkout URL: {session.CheckoutUrl}");
```

### ग्राहकों का प्रबंधन करें

```csharp
// Create a customer
var customer = await client.Customers.Create(new CustomerCreateParams
{
    Email = "customer@example.com",
    Name = "John Doe"
});

// Retrieve customer
var retrieved = await client.Customers.Retrieve(new CustomerRetrieveParams { CustomerID = "cus_123" });
Console.WriteLine($"Customer: {retrieved.Name} ({retrieved.Email})");
```

### सब्सक्रिप्शन को हैंडल करें

<Warning>
`POST /subscriptions` (SDK की `subscriptions.create` method) **deprecated** है। यह मौजूदा integrations के लिए अभी भी काम करती है, लेकिन नए integrations को [Checkout Session](/developer-resources/checkout-session) के माध्यम से subscriptions बनानी चाहिए।
</Warning>

```csharp
using DodoPayments.Client.Models.Payments;
using DodoPayments.Client.Models.Subscriptions;

// Create a subscription
var subscription = await client.Subscriptions.Create(new SubscriptionCreateParams
{
    Billing = new BillingAddress
    {
        Country = "US",
        City = "San Francisco",
        State = "CA",
        Street = "1 Market St",
        Zipcode = "94105",
    },
    Customer = new AttachExistingCustomer { CustomerID = "cus_123" },
    ProductID = "pdt_456",
    Quantity = 1,
});

// Charge an on-demand subscription
// ProductPrice is in the lowest currency denomination (e.g., 2500 = $25.00 USD)
var charge = await client.Subscriptions.Charge(new SubscriptionChargeParams
{
    SubscriptionID = subscription.SubscriptionId,
    ProductPrice = 2500,
});
```

<Info>
`Billing` के लिए कम से कम दो-अक्षरी ISO `Country` code आवश्यक है। किसी मौजूदा customer को जोड़ने के लिए `AttachExistingCustomer` का उपयोग करें, या customer बनाने के लिए `NewCustomer` का उपयोग करें। `ProductPrice` सबसे छोटी currency denomination में है।
</Info>

## त्रुटि प्रबंधन

SDK HTTP status code के आधार पर विशिष्ट exceptions उत्पन्न करता है। सभी 4xx errors, `DodoPayments4xxException` से inherit करते हैं।

| Status | Exception                                   |
| ------ | ------------------------------------------- |
| 400    | `DodoPaymentsBadRequestException`           |
| 401    | INLINE_CODE_PLACEHOLDER_45800aad96d175132_END         |
| 403    | `DodoPaymentsForbiddenException`            |
| 404    | `DodoPaymentsNotFoundException`             |
| 422    | `DodoPaymentsUnprocessableEntityException`  |
| 429    | `DodoPaymentsRateLimitException`            |
| 5xx    | `DodoPayments5xxException`                  |
| others | `DodoPaymentsUnexpectedStatusCodeException` |

अन्य exception types:
- `DodoPaymentsIOException`: I/O networking errors
- `DodoPaymentsInvalidDataException`: parsed data को interpret करने में विफलता
- `DodoPaymentsException`: सभी exceptions के लिए base class

## Pagination

### Auto-Pagination

`Paginate` method का उपयोग करके सभी pages के सभी results पर iterate करें, जो एक `IAsyncEnumerable` लौटाती है:

```csharp
var page = await client.Payments.List(parameters);
await foreach (var item in page.Paginate())
{
    Console.WriteLine(item);
}
```

### Manual Pagination

```csharp
var page = await client.Payments.List();
while (true)
{
    foreach (var item in page.Items)
    {
        Console.WriteLine(item);
    }
    if (!page.HasNext())
    {
        break;
    }
    page = await page.Next();
}
```

## ASP.NET Core Integration

अपने DI container में client register करें:

```csharp Program.cs
using DodoPayments.Client;

var builder = WebApplication.CreateBuilder(args);

builder.Services.AddSingleton<DodoPaymentsClient>(sp =>
{
    var configuration = sp.GetRequiredService<IConfiguration>();
    return new DodoPaymentsClient
    {
        BearerToken = configuration["DodoPayments:ApiKey"]
    };
});

var app = builder.Build();
app.Run();
```

```json appsettings.json
{
  "DodoPayments": {
    "ApiKey": "your_api_key_here"
  }
}
```

<Tip>
Development के लिए, keys को `appsettings.json` में store करने के बजाय [user secrets](https://learn.microsoft.com/en-us/aspnet/core/security/app-secrets) का उपयोग करें:

```bash
dotnet user-secrets init
dotnet user-secrets set "DodoPayments:ApiKey" "your_api_key_here"
```

</Tip>

## Resources

<CardGroup cols={2}>
<Card title="NuGet Package" icon="box" href="https://www.nuget.org/packages/DodoPayments.Client">
  NuGet Gallery पर package देखें
</Card>

<Card title="GitHub Repository" icon="github" href="https://github.com/dodopayments/dodopayments-csharp">
  source code देखें और योगदान करें
</Card>

<Card title="API Reference" icon="book" href="/api-reference/introduction">
  पूर्ण API documentation
</Card>

<Card title="Discord Community" icon="discord" href="https://discord.gg/bYqAp4ayYh">
  सहायता प्राप्त करें और developers से जुड़ें
</Card>
</CardGroup>

## सहायता

C# SDK के साथ सहायता चाहिए?

- **Discord**: real-time support के लिए हमारे [community server](https://discord.gg/bYqAp4ayYh) से जुड़ें
- **Email**: support@dodopayments.com पर हमसे संपर्क करें
- **GitHub**: [repository](https://github.com/dodopayments/dodopayments-csharp) पर issue खोलें
