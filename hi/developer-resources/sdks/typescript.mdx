---
title: TypeScript
description: अपने TypeScript और Node.js अनुप्रयोगों में Dodo Payments को टाइप
  सुरक्षा और आधुनिक async/await समर्थन के साथ एकीकृत करें
icon: js
keywords:
  - Dodo Payments
  - TypeScript SDK
  - Node.js
  - npm पैकेज
  - TypeScript
---

TypeScript SDK Dodo Payments REST API के लिए TypeScript और JavaScript अनुप्रयोगों के लिए सुविधाजनक सर्वर-साइड पहुंच प्रदान करता है। इसमें व्यापक प्रकार की परिभाषाएँ, त्रुटि प्रबंधन, पुनः प्रयास, समय सीमा, और निर्बाध भुगतान प्रसंस्करण के लिए स्वचालित-पृष्ठन शामिल हैं।

## स्थापना

अपने पसंदीदा पैकेज प्रबंधक का उपयोग करके SDK इंस्टॉल करें:
<CodeGroup>

```bash npm
npm install dodopayments
```

```bash yarn
yarn add dodopayments
```

```bash pnpm
pnpm add dodopayments
```

</CodeGroup>

## त्वरित प्रारंभ

अपने API कुंजी के साथ क्लाइंट को प्रारंभ करें और भुगतान प्रसंस्करण शुरू करें:

```javascript
import DodoPayments from 'dodopayments';

const client = new DodoPayments({
  bearerToken: process.env['DODO_PAYMENTS_API_KEY'], // This is the default and can be omitted
  environment: 'test_mode', // defaults to 'live_mode'
});

const checkoutSessionResponse = await client.checkoutSessions.create({
  product_cart: [{ product_id: 'product_id', quantity: 1 }],
});

console.log(checkoutSessionResponse.session_id);
```

<Warning>
हमेशा अपने API कुंजियाँ environment variables का उपयोग करके सुरक्षित रूप से संग्रहित करें। उन्हें version control में कभी commit न करें या client-side code में उजागर न करें।
</Warning>

## मुख्य विशेषताएँ

<CardGroup cols={2}>
<Card title="TypeScript First" icon="shield-check">
  सभी API endpoints के लिए व्यापक type definitions के साथ पूर्ण TypeScript समर्थन
</Card>

<Card title="Auto-Pagination" icon="arrows-rotate">
  बड़ी datasets के साथ काम करना सहज बनाने के लिए सूची प्रतिक्रियाओं के लिए स्वचालित पेजिनेशन
</Card>

<Card title="Error Handling" icon="triangle-exclamation">
  विभिन्न असफलता परिदृश्यों के लिए विस्तृत संदेशों वाले बिल्ट-इन त्रुटि प्रकार
</Card>

<Card title="Smart Retries" icon="repeat">
  अस्थायी त्रुटियों के लिए exponential backoff के साथ कॉन्फ़िगर करने योग्य स्वचालित पुन: प्रयास
</Card>
</CardGroup>

## कॉन्फ़िगरेशन

### पर्यावरण चर

सुरक्षित कॉन्फ़िगरेशन के लिए पर्यावरण चर सेट करें:

```bash .env
DODO_PAYMENTS_API_KEY=your_api_key_here
```

### समय सीमा कॉन्फ़िगरेशन

वैश्विक या प्रति-अनुरोध समय सीमा कॉन्फ़िगर करें:

```typescript
// Configure default timeout for all requests (default is 1 minute)
const client = new DodoPayments({
  timeout: 20 * 1000, // 20 seconds
});

// Override per-request
await client.checkoutSessions.create(
  { product_cart: [{ product_id: 'product_id', quantity: 1 }] },
  { timeout: 5 * 1000 },
);
```

### पुनः प्रयास कॉन्फ़िगरेशन

स्वचालित पुनः प्रयास व्यवहार कॉन्फ़िगर करें:

```javascript
// Configure default for all requests (default is 2 retries)
const client = new DodoPayments({
  maxRetries: 0, // disable retries
});

// Override per-request
await client.checkoutSessions.create(
  { product_cart: [{ product_id: 'product_id', quantity: 1 }] },
  { maxRetries: 5 },
);
```

<Tip>
SDK स्वचालित रूप से उन अनुरोधों को पुनः प्रयास करता है जो नेटवर्क त्रुटियों या सर्वर समस्याओं (5xx प्रतिक्रियाएँ) के कारण विफल हो जाते हैं, साथ ही exponential backoff के साथ।
</Tip>

## सामान्य संचालन

### चेकआउट सत्र बनाएं

भुगतान जानकारी एकत्र करने के लिए एक चेकआउट सत्र उत्पन्न करें:

```typescript
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'pdt_123',
      quantity: 1
    }
  ],
  return_url: 'https://yourdomain.com/return'
});

console.log('Redirect to:', session.checkout_url);
```

### ग्राहकों का प्रबंधन करें

ग्राहक जानकारी बनाएं और पुनः प्राप्त करें:

```typescript
// Create a customer
const customer = await client.customers.create({
  email: 'customer@example.com',
  name: 'John Doe',
  metadata: {
    user_id: '12345'
  }
});

// Retrieve customer
const retrieved = await client.customers.retrieve('cus_123');
console.log(`Customer: ${retrieved.name} (${retrieved.email})`);
```

### सदस्यताओं को संभालें

आवर्ती सदस्यताओं का निर्माण और प्रबंधन करें:

<Warning>
`POST /subscriptions` (SDK की `subscriptions.create` विधि) **deprecated** है। यह मौजूदा integrations के लिए अभी भी काम करती है, लेकिन नए integrations को [Checkout Session](/developer-resources/checkout-session) के माध्यम से subscriptions बनानी चाहिए।
</Warning>

```typescript
// Create a subscription
const subscription = await client.subscriptions.create({
  billing: {
    country: 'US',
    city: 'San Francisco',
    state: 'CA',
    street: '1 Market St',
    zipcode: '94105',
  },
  customer: {
    customer_id: 'cus_123', // or pass { email, name } to create a new customer
  },
  product_id: 'pdt_456',
  quantity: 1,
});

// Charge an on-demand subscription
// product_price is in the lowest currency denomination (e.g., 2500 = $25.00 USD)
const chargeResponse = await client.subscriptions.charge(subscription.subscription_id, {
  product_price: 2500,
});

// Retrieve subscription usage history (for metered subscriptions)
const usageHistory = await client.subscriptions.retrieveUsageHistory(subscription.subscription_id, {
  start_date: '2024-01-01T00:00:00Z',
  end_date: '2024-03-31T23:59:59Z',
});
```

<Info>
`billing` के लिए कम से कम दो-अक्षर वाला ISO country code आवश्यक है। `customer`, `{ customer_id }` (मौजूदा customer को जोड़ने के लिए) या `{ email, name? }` (नया customer बनाने के लिए) का union है। `product_price` को currency की सबसे छोटी denomination में व्यक्त किया जाता है।
</Info>

## उपयोग-आधारित बिलिंग

### Usage Events ingest करना

उपयोग-आधारित बिलिंग के लिए custom events को track करें:

```typescript
await client.usageEvents.ingest({
  events: [
    {
      event_id: 'api_call_12345',
      customer_id: 'cus_abc123',
      event_name: 'api_request',
      timestamp: '2024-01-15T10:30:00Z',
      metadata: {
        endpoint: '/api/v1/users',
        method: 'GET',
        tokens_used: '150'
      }
    }
  ]
});
```

<Info>
Idempotency के लिए events में अद्वितीय `event_id` values होनी चाहिए। उसी request के भीतर duplicate IDs अस्वीकार कर दी जाती हैं, और मौजूदा IDs वाली subsequent requests को अनदेखा कर दिया जाता है।
</Info>

### Usage Events प्राप्त करना

Usage events के बारे में विस्तृत जानकारी प्राप्त करें:

```typescript
// Get a specific event
const event = await client.usageEvents.retrieve('api_call_12345');

// List events with filtering
const events = await client.usageEvents.list({
  customer_id: 'cus_abc123',
  event_name: 'api_request',
  start: '2024-01-14T10:30:00Z',
  end: '2024-01-15T10:30:00Z'
});
```

## Proxy Configuration

अलग-अलग runtimes के लिए proxy settings configure करें:

### Node.js (undici का उपयोग करके)

```typescript
import DodoPayments from 'dodopayments';
import * as undici from 'undici';

const proxyAgent = new undici.ProxyAgent('http://localhost:8888');
const client = new DodoPayments({
  fetchOptions: {
    dispatcher: proxyAgent,
  },
});
```

### Bun

```typescript
import DodoPayments from 'dodopayments';

const client = new DodoPayments({
  fetchOptions: {
    proxy: 'http://localhost:8888',
  },
});
```

### Deno

```typescript
import DodoPayments from 'npm:dodopayments';

const httpClient = Deno.createHttpClient({ proxy: { url: 'http://localhost:8888' } });
const client = new DodoPayments({
  fetchOptions: {
    client: httpClient,
  },
});
```

## Logging

Environment variables या client options का उपयोग करके log verbosity नियंत्रित करें:

```typescript
// Via client option
const client = new DodoPayments({
  logLevel: 'debug', // Show all log messages
});
```

```bash
# Via environment variable
export DODO_PAYMENTS_LOG=debug
```

**उपलब्ध log levels:**
- `'debug'` - Debug messages, info, warnings और errors दिखाएँ
- `'info'` - Info messages, warnings और errors दिखाएँ  
- `'warn'` - Warnings और errors दिखाएँ (default)
- `'error'` - केवल errors दिखाएँ
- `'off'` - सभी logging अक्षम करें

<Warning>
Debug level पर सभी HTTP requests और responses log किए जाते हैं, जिनमें headers और bodies भी शामिल हैं। कुछ authentication headers को redact किया जाता है, लेकिन bodies में मौजूद संवेदनशील data अभी भी दिखाई दे सकता है।
</Warning>

## Node.js SDK से Migration

यदि आप legacy Node.js SDK से upgrade कर रहे हैं, तो TypeScript SDK बेहतर type safety और features प्रदान करता है:

<Card
  title="View Migration Guide"
  icon="arrow-right"
  href="https://github.com/dodopayments/dodopayments-typescript/blob/main/MIGRATION.md">
  Node.js SDK से TypeScript SDK पर migrate करने का तरीका जानें
</Card>

## Auto-Pagination

DodoPayments API में list methods paginated होते हैं। सभी pages में मौजूद items पर iterate करने के लिए `for await … of` syntax का उपयोग कर सकते हैं:

```typescript
async function fetchAllPayments() {
  const allPayments = [];
  // Automatically fetches more pages as needed.
  for await (const paymentListResponse of client.payments.list()) {
    allPayments.push(paymentListResponse);
  }
  return allPayments;
}
```

वैकल्पिक रूप से, आप एक बार में एक single page का अनुरोध कर सकते हैं:

```typescript
let page = await client.payments.list();
for (const paymentListResponse of page.items) {
  console.log(paymentListResponse);
}

// Convenience methods are provided for manually paginating:
while (page.hasNextPage()) {
  page = await page.getNextPage();
  // ...
}
```

## Requirements

निम्नलिखित runtimes supported हैं:

- Web browsers (नवीनतम Chrome, Firefox, Safari, Edge और अन्य)
- Node.js 20 LTS या बाद के ([non-EOL](https://endoflife.date/nodejs)) versions
- Deno v1.28.0 या उच्चतर
- Bun 1.0 या बाद का
- Cloudflare Workers
- Vercel Edge Runtime
- `"node"` environment के साथ Jest 28 या उच्चतर
- Nitro v2.6 या उच्चतर

TypeScript >= 4.9 supported है।

## Resources

<CardGroup cols={2}>
<Card title="GitHub Repository" icon="github" href="https://github.com/dodopayments/dodopayments-typescript">
  Source code देखें और योगदान करें
</Card>

<Card title="API Reference" icon="book" href="/api-reference/introduction">
  पूर्ण API documentation
</Card>

<Card title="Discord Community" icon="discord" href="https://discord.gg/bYqAp4ayYh">
  सहायता प्राप्त करें और developers से जुड़ें
</Card>

<Card title="Report Issues" icon="bug" href="https://github.com/dodopayments/dodopayments-typescript/issues">
  Bugs report करें या features का अनुरोध करें
</Card>
</CardGroup>

## Support

TypeScript SDK के साथ सहायता चाहिए?

- **Discord**: Real-time support के लिए हमारे [community server](https://discord.gg/bYqAp4ayYh) से जुड़ें
- **Email**: support@dodopayments.com पर हमसे संपर्क करें  
- **GitHub**: [repository](https://github.com/dodopayments/dodopayments-typescript) पर issue खोलें

## Contributing

हम contributions का स्वागत करते हैं! शुरुआत करने के लिए [contributing guidelines](https://github.com/dodopayments/dodopayments-typescript/blob/main/CONTRIBUTING.md) देखें।
