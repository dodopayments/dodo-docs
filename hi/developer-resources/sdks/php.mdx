---
title: PHP
description: अपने PHP अनुप्रयोगों में Dodo Payments को एक आधुनिक, PSR-4 अनुपालन
  SDK के साथ एकीकृत करें
icon: php
---

PHP SDK Dodo Payments को आपके PHP अनुप्रयोगों में एकीकृत करने का एक मजबूत और लचीला तरीका प्रदान करता है। यह आधुनिक PHP मानकों के अनुसार PSR-4 ऑटोलोडिंग के साथ बनाया गया है, और इसमें व्यापक परीक्षण कवरेज और विस्तृत दस्तावेज़ीकरण है।

<Note>
  Dodo Payments PHP API लाइब्रेरी वर्तमान में **बेटा** में है। हम चाहते हैं कि आप इसे आज़माएँ!
  किसी भी सुझाव, बग रिपोर्ट या फीचर रिक्वेस्ट के लिए कृपया [issue दर्ज करें](https://github.com/dodopayments/dodopayments-php/issues)।
</Note>

## स्थापना

SDK को Composer का उपयोग करके स्थापित करें:

```bash
composer require "dodopayments/client 5.3.0"
```

<Info>
  SDK को PHP 8.1.0 या उच्चतर और निर्भरता प्रबंधन के लिए Composer की आवश्यकता है।
</Info>

## त्वरित प्रारंभ

क्लाइंट को प्रारंभ करें और एक चेकआउट सत्र बनाएं:

```php
<?php

use Dodopayments\Client;

$client = new Client(
  bearerToken: getenv('DODO_PAYMENTS_API_KEY') ?: 'My Bearer Token',
  environment: 'test_mode',
);

$checkoutSessionResponse = $client->checkoutSessions->create(
  productCart: [["productID" => "product_id", "quantity" => 1]]
);

var_dump($checkoutSessionResponse->session_id);
```

<Warning>
  अपने API कुंजियों को सुरक्षित रूप से परिवेश चर (environment variables) में स्टोर करें। उन्हें अपने कोडबेस में उजागर न करें और न ही वर्शन कंट्रोल में कमिट करें।
</Warning>

## मुख्य विशेषताएँ

<CardGroup cols={2}>
<Card title="PSR-4 Compliant" icon="check">
  आधुनिक PHP विकास के लिए PHP Standards Recommendations का पालन करता है
</Card>

<Card title="Modern PHP" icon="php">
  टाइप डिक्लेरेशन और स्ट्रिक्ट टाइप्स के साथ PHP 8.1+ के लिए बनाया गया
</Card>

<Card title="Extensive Testing" icon="vial">
  विश्वसनीयता और स्थिरता के लिए व्यापक परीक्षण कवरेज
</Card>

<Card title="Exception Handling" icon="shield-check">
  विभिन्न त्रुटि परिदृश्यों के लिए स्पष्ट exception प्रकार
</Card>
</CardGroup>

## मूल्य वस्तुएं

SDK वैकल्पिक तर्क निर्दिष्ट करने के लिए नामित पैरामीटर का उपयोग करता है। आप स्थैतिक `with` निर्माणकर्ता का उपयोग करके मूल्य ऑब्जेक्ट्स को प्रारंभ कर सकते हैं:

```php
<?php

use Dodopayments\Customers\AttachExistingCustomer;

// Recommended: Use static 'with' constructor with named parameters
$customer = AttachExistingCustomer::with(customerID: "customer_id");
```

बिल्डर एक वैकल्पिक पैटर्न के रूप में भी उपलब्ध हैं:

```php
<?php

use Dodopayments\Customers\AttachExistingCustomer;

// Alternative: Use builder pattern
$customer = (new AttachExistingCustomer)->withCustomerID("customer_id");
```

## कॉन्फ़िगरेशन

### पुनः प्रयास कॉन्फ़िगरेशन

कुछ त्रुटियों को डिफ़ॉल्ट रूप से 2 बार स्वचालित रूप से पुनः प्रयास किया जाता है, जिसमें एक छोटा एक्सपोनेंशियल बैकऑफ होता है। निम्नलिखित त्रुटियाँ स्वचालित पुनः प्रयास को ट्रिगर करती हैं:

- कनेक्शन त्रुटियाँ (नेटवर्क कनेक्टिविटी समस्याएँ)
- 408 अनुरोध समय समाप्त
- 409 संघर्ष
- 429 दर सीमा
- 500+ आंतरिक त्रुटियाँ
- समय समाप्त

वैश्विक या प्रति अनुरोध पुनः प्रयास व्यवहार कॉन्फ़िगर करें:

```php
<?php

use Dodopayments\Client;
use Dodopayments\RequestOptions;

// Configure default for all requests (disable retries)
$client = new Client(maxRetries: 0);

// Or, configure per-request
$result = $client->checkoutSessions->create(
  productCart: [["productID" => "product_id", "quantity" => 1]],
  requestOptions: RequestOptions::with(maxRetries: 5),
);
```

## सामान्य संचालन

### चेकआउट सत्र बनाएं

एक चेकआउट सत्र उत्पन्न करें:

```php
$session = $client->checkoutSessions->create(
  productCart: [
    ["productID" => "prod_123", "quantity" => 1]
  ],
  returnUrl: "https://yourdomain.com/return"
);

header('Location: ' . $session->url);
```

### ग्राहकों का प्रबंधन करें

ग्राहक जानकारी बनाएं और पुनः प्राप्त करें:

```php
// Create a customer
$customer = $client->customers->create(
  email: "customer@example.com",
  name: "John Doe",
  metadata: [
    "user_id" => "12345"
  ]
);

// Retrieve customer
$customer = $client->customers->retrieve("cus_123");
echo "Customer: {$customer->name} ({$customer->email})";
```

### सदस्यताओं को संभालें

आवर्ती सदस्यताओं को बनाएं और प्रबंधित करें:

```php
// Create a subscription
$subscription = $client->subscriptions->create(
  customerID: "cus_123",
  productID: "prod_456",
  priceID: "price_789"
);

// Cancel subscription
$client->subscriptions->cancel($subscription->id);
```

## पृष्ठनुमा

पृष्ठित सूची प्रतिक्रियाओं के साथ काम करें:

```php
$page = $client->payments->list();

var_dump($page);

// Fetch items from the current page
foreach ($page->getItems() as $item) {
  var_dump($item->brand_id);
}

// Auto-paginate: fetch items from all pages
foreach ($page->pagingEachItem() as $item) {
  var_dump($item->brand_id);
}
```

## त्रुटि प्रबंधन

जब लाइब्रेरी API से कनेक्ट नहीं कर पाती या सफलता रहित स्थिति कोड (4xx या 5xx) प्राप्त करती है, तो `APIException` का एक उपवर्ग थ्रो होता है:

```php
<?php

use Dodopayments\Core\Exceptions\APIConnectionException;
use Dodopayments\Core\Exceptions\RateLimitException;
use Dodopayments\Core\Exceptions\APIStatusException;

try {
  $checkoutSessionResponse = $client->checkoutSessions->create(
    productCart: [["productID" => "product_id", "quantity" => 1]]
  );
} catch (APIConnectionException $e) {
  echo "The server could not be reached", PHP_EOL;
  var_dump($e->getPrevious());
} catch (RateLimitException $_) {
  echo "A 429 status code was received; we should back off a bit.", PHP_EOL;
} catch (APIStatusException $e) {
  echo "Another non-200-range status code was received", PHP_EOL;
  echo $e->getMessage();
}
```

### त्रुटि प्रकार

| कारण            | त्रुटि प्रकार                     |
| ---------------- | ------------------------------ |
| HTTP 400         | `BadRequestException`          |
| HTTP 401         | `AuthenticationException`      |
| HTTP 403         | `PermissionDeniedException`    |
| HTTP 404         | `NotFoundException`            |
| HTTP 409         | `ConflictException`            |
| HTTP 422         | `UnprocessableEntityException` |
| HTTP 429         | `RateLimitException`           |
| HTTP >= 500      | `InternalServerException`      |
| अन्य HTTP त्रुटि | `APIStatusException`           |
| टाइमआउट          | `APITimeoutException`          |
| नेटवर्क त्रुटि    | `APIConnectionException`       |

<Tip>
  संभावित त्रुटियों को सुचारू रूप से संभालने और उपयोगकर्ताओं को सार्थक प्रतिक्रिया देने के लिए API कॉल्स को हमेशा try-catch ब्लॉकों में लपेटें।
</Tip>

## उन्नत उपयोग

### अप्रलेखित एंडपॉइंट्स

अप्रलेखित एंडपॉइंट्स पर अनुरोध करें:

```php
<?php

$response = $client->request(
  method: "post",
  path: '/undocumented/endpoint',
  query: ['dog' => 'woof'],
  headers: ['useful-header' => 'interesting-value'],
  body: ['hello' => 'world']
);
```

### अप्रलेखित पैरामीटर

किसी भी एंडपॉइंट पर अप्रलेखित पैरामीटर भेजें या अप्रलेखित प्रतिक्रिया गुण पढ़ें:

```php
<?php

use Dodopayments\RequestOptions;

$checkoutSessionResponse = $client->checkoutSessions->create(
  productCart: [["productID" => "product_id", "quantity" => 1]],
  requestOptions: RequestOptions::with(
    extraQueryParams: ["my_query_parameter" => "value"],
    extraBodyParams: ["my_body_parameter" => "value"],
    extraHeaders: ["my-header" => "value"],
  ),
);
```

<Note>
  एक ही नाम वाले `extra*` पैरामीटर प्रलेखित पैरामीटर को ओवरराइड करते हैं।
</Note>

## ढांचा एकीकरण

### Laravel

Laravel अनुप्रयोगों के लिए एक सेवा बनाएं:

```php
<?php

namespace App\Services;

use Dodopayments\Client;

class PaymentService
{
    protected $client;

    public function __construct()
    {
        $this->client = new Client(
            bearerToken: config('services.dodo.api_key')
        );
    }

    public function createCheckout(array $items)
    {
        return $this->client->checkoutSessions->create(
            productCart: $items,
            returnUrl: route('checkout.return')
        );
    }
}
```

`config/services.php` में विन्यास जोड़ें:

```php
'dodo' => [
    'api_key' => env('DODO_API_KEY'),
    'environment' => env('DODO_ENVIRONMENT', 'sandbox')
],
```

### Symfony

Symfony में एक सेवा बनाएं:

```php
<?php

namespace App\Service;

use Dodopayments\Client;

class DodoPaymentService
{
    private Client $client;

    public function __construct(string $apiKey)
    {
        $this->client = new Client(bearerToken: $apiKey);
    }

    public function createPayment(int $amount, string $currency, string $customerId): object
    {
        return $this->client->payments->create(
            amount: $amount,
            currency: $currency,
            customerID: $customerId
        );
    }
}
```

`config/services.yaml` में पंजीकरण करें:

```yaml
services:
  App\Service\DodoPaymentService:
    arguments:
      $apiKey: "%env(DODO_API_KEY)%"
```

## संसाधन

<CardGroup cols={2}>
<Card title="GitHub Repository" icon="github" href="https://github.com/dodopayments/dodopayments-php">
  स्रोत कोड देखें और योगदान दें
</Card>

<Card title="API Reference" icon="book" href="/api-reference/introduction">
  पूर्ण API प्रलेखन
</Card>

<Card
  title="Discord Community"
  icon="discord"
  href="https://discord.gg/bYqAp4ayYh"
>
  मदद प्राप्त करें और डेवलपर्स से जुड़ें
</Card>

<Card title="Report Issues" icon="bug" href="https://github.com/dodopayments/dodopayments-php/issues">
  बग रिपोर्ट करें या फीचर का अनुरोध करें
</Card>
</CardGroup>

## समर्थन

PHP SDK के साथ मदद चाहिए?

- **Discord**: वास्तविक समय समर्थन के लिए हमारे [समुदाय सर्वर](https://discord.gg/bYqAp4ayYh) में शामिल हों
- **ईमेल**: हमें support@dodopayments.com पर संपर्क करें
- **GitHub**: [रिपॉजिटरी](https://github.com/dodopayments/dodopayments-php) पर एक समस्या खोलें

## योगदान

हम योगदान का स्वागत करते हैं! प्रारंभ करने के लिए [योगदान दिशानिर्देश](https://github.com/dodopayments/dodopayments-php/blob/main/CONTRIBUTING.md) की जांच करें.
