---
title: Close CRM
description: Dodo Payments डेटा को Close CRM के साथ समन्वयित करें ताकि लीड को
  ट्रैक किया जा सके और बिक्री पाइपलाइन का प्रबंधन किया जा सके।
icon: handshake
---

## परिचय

अपने भुगतान डेटा को सीधे Close CRM से जोड़ें ताकि लीड प्रबंधन और बिक्री ट्रैकिंग में आसानी हो। सफल भुगतानों से संपर्क और अवसर स्वचालित रूप से बनाएं, जिससे आपकी बिक्री टीम राजस्व उत्पन्न करने वाली गतिविधियों के बारे में सूचित रहे।

<Info>
इस एकीकरण के लिए उपयुक्त अनुमतियों वाले Close CRM API कुंजी की आवश्यकता होती है।
</Info>

## प्रारंभ करना

<Steps>
{/* LOCKED_PATTERN_c337a250e2e6db475d2eb18e95cb3e6a */}
  अपने Dodo Payments डैशबोर्ड में <b>Webhooks → + Add Endpoint</b> पर जाएँ और इंटीग्रेशन ड्रॉपडाउन को विस्तारित करें।
  <Frame>
    <img src="/images/integrations/close-crm.png" alt="Add Endpoint and integrations dropdown" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_be9c155314e805176e2c71630dfee6de */}
  <b>Close CRM</b> इंटीग्रेशन कार्ड चुनें।
</Step>

{/* LOCKED_PATTERN_b0cbf67cc15e78743c0724f27d5e90ef */}
  कॉन्फ़िगरेशन में अपनी Close CRM API कुंजी प्रदान करें।
</Step>

{/* LOCKED_PATTERN_4ac4616f59ca1d74262754150410c05f */}
  भुगतान डेटा को Close CRM ऑब्जेक्ट्स से मैप करने के लिए ट्रांसफ़ॉर्मेशन कोड संपादित करें।
</Step>

{/* LOCKED_PATTERN_f6e4d766e7306e52042eed3b757944bb */}
  नमूना पेलोड्स के साथ परीक्षण करें और <b>Create</b> पर क्लिक करके सिंक को सक्रिय करें।
</Step>

{/* LOCKED_PATTERN_8d2cbcb809e6363cb1f41f0f62f98444 */}
  🎉 भुगतान ईवेंट अब स्वचालित रूप से Close CRM में रिकॉर्ड बनाएँगे/अपडेट करेंगे।
</Step>
</Steps>

## परिवर्तन कोड उदाहरण

### भुगतान से संपर्क बनाएं

```javascript create_contact.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const p = webhook.payload.data;
    webhook.url = "https://api.close.com/api/v1/contact/";
    webhook.payload = {
      name: p.customer.name,
      emails: [p.customer.email],
      phones: [p.customer.phone || ''],
      custom: {
        payment_amount: (p.total_amount / 100).toFixed(2),
        payment_method: p.payment_method || '',
        dodo_customer_id: p.customer.customer_id
      }
    };
  }
  return webhook;
}
```

### सदस्यता से अवसर बनाएं

```javascript create_opportunity.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "subscription.active") {
    const s = webhook.payload.data;
    webhook.url = "https://api.close.com/api/v1/opportunity/";
    webhook.payload = {
      lead_id: s.customer.customer_id,
      value: (s.recurring_pre_tax_amount / 100).toFixed(2),
      value_period: s.payment_frequency_interval,
      title: `Subscription - ${s.product_id}`,
      custom: {
        subscription_id: s.subscription_id,
        billing_frequency: s.payment_frequency_interval,
        next_billing: s.next_billing_date
      }
    };
  }
  return webhook;
}
```

## सुझाव

- फ़ील्ड मैपिंग को समझने के लिए Close CRM की API दस्तावेज़ीकरण का उपयोग करें
- भुगतान-विशिष्ट डेटा के लिए कस्टम फ़ील्ड शामिल करें
- अवसर मूल्यों के लिए सदस्यता राशियों को मैप करें
- उचित लीड संघों के लिए ग्राहक आईडी का उपयोग करें

## समस्या निवारण

<AccordionGroup>
{/* LOCKED_PATTERN_e4b9b3b678d456b7bd4c4020a0dd36f9 */}
- पुष्टि करें कि API कुंजी में लिखने की अनुमतियाँ हैं
- जांचें कि आवश्यक फ़ील्ड शामिल हैं
- सुनिश्चित करें कि ईमेल प्रारूप मान्य है
- Close CRM API दर सीमाओं की समीक्षा करें
</Accordion>

{/* LOCKED_PATTERN_14f1ac2eb6bb156b1fd74792fba48a19 */}
- सत्यापित करें कि JSON संरचना Close CRM API प्रारूप से मेल खाती है
- जांचें कि सभी आवश्यक फ़ील्ड मौजूद हैं
- सुनिश्चित करें कि फ़ील्ड नाम Close CRM स्कीमा से सटीक रूप से मेल खाते हैं
</Accordion>
</AccordionGroup>