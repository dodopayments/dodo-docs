---
title: Droit d'utilisation Notion
sidebarTitle: Notion
description: Dupliquer un modèle Notion dans l'espace de travail du client
  lorsqu'il a acheté.
icon: book
keywords:
  - Dodo Payments
  - Notion
  - droit d'utilisation
  - modèle Notion
  - dupliquer modèle
---

<Info>
Le droit d'utilisation Notion duplique un modèle que vous avez préparé dans l'espace de travail Notion du client payant. Le client s'autorise via OAuth, choisit l'espace de travail dans lequel il souhaite copier le modèle, et Dodo Payments gère la duplication.
</Info>

## Ce qui est livré

- Le client est invité à connecter son espace de travail Notion via OAuth.
- Une fois l'autorisation accordée, Dodo Payments duplique le modèle configuré dans l'espace de travail qu'il a choisi.
- En cas d'annulation, le modèle dupliqué reste dans l'espace de travail du client (Notion ne prend pas en charge la suppression à distance). L'autorisation est marquée `Revoked` à des fins d'archivage.

Les utilisations courantes incluent les modèles Notion payants, les systèmes de productivité et les modèles OS/CRM.

## Connecter Notion

<Steps>
<Step title="Open Entitlements">
Dans votre tableau de bord Dodo Payments, allez à **Droits d'utilisation** et cliquez sur **+**.
</Step>

<Step title="Pick Notion">
Choisissez **Modèle Notion** comme intégration. Si Notion n'est pas encore connecté, vous serez invité à **Connecter Notion**.

<Frame caption="Connect Notion prompt before the OAuth handoff.">
<img src="/images/entitlements/notion/connect-prompt.png" alt="Nouveau panneau de droits invitant le marchand à connecter Notion" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>

En cliquant, Notion s'ouvre dans un nouvel onglet. Connectez-vous à l'espace de travail qui possède vos modèles sources, vérifiez les permissions demandées par Dodo Payments, puis choisissez les pages (modèles) que l'intégration est autorisée à lire et dupliquer.

<Frame caption="Notion OAuth: review permissions, then pick the template pages to share.">
<div style={{ display: 'flex', gap: '12px', flexWrap: 'wrap', justifyContent: 'center' }}>
<img src="/images/entitlements/notion/oauth-permissions.png" alt="Écran des permissions OAuth Notion pour Dodo Payments" style={{ maxHeight: '420px', width: 'auto' }} />
<img src="/images/entitlements/notion/oauth-select-pages.png" alt="Sélecteur de pages Notion sélectionnant les modèles auxquels Dodo Payments peut accéder" style={{ maxHeight: '420px', width: 'auto' }} />
</div>
</Frame>

Lorsque Notion redirige, vous verrez une confirmation que l'espace de travail est connecté.

<Frame caption="Workspace connected — return to the Dodo Payments dashboard to continue.">
<img src="/images/entitlements/notion/connected.png" alt="Page de confirmation de connexion réussie du modèle Notion" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>
</Step>

<Step title="Pick a template">
Retournez dans le tableau de bord, sélectionnez le **modèle** à dupliquer depuis le sélecteur de modèle. Seules les pages que vous avez autorisées à l'étape précédente apparaissent ici. Donnez un **nom** au droit et cliquez sur **Créer un droit**.

<Frame>
<img src="/images/entitlements/notion/create.png" alt="Formulaire de droit Notion avec espace de travail connecté, sélecteur de modèle et champ de nom" style={{ maxHeight: '500px', width: 'auto' }} />
</Frame>
</Step>

<Step title="Save the entitlement">
Enregistrez et attachez à n'importe quel produit.
</Step>
</Steps>

## Flux client

1. Le client termine le paiement.
2. Dodo Payments crée un grant avec le statut `Pending`. Son `oauth_url` est `null` jusqu’à ce que le client commence le flux d’acceptation, auquel moment il est renseigné avec une URL d’autorisation Notion.
3. Le client s’autorise via le lien reçu dans son e-mail ou dans le Customer Portal, puis sélectionne l’espace de travail dans lequel il souhaite dupliquer le modèle.
4. Dodo Payments duplique le modèle. Le grant passe à `Delivered`.
5. Lors de la révocation, le grant est marqué `Revoked` ; la copie dupliquée reste dans l’espace de travail du client.

<Note>
Notion considère intentionnellement une page dupliquée comme le contenu propre du destinataire. Dodo Payments ne peut pas supprimer la copie en cas de révocation.
</Note>

## Configuration requise

| Champ | Requis | Description |
|---|---|---|
| `notion_template_id` | Oui | L'ID du modèle. Le sélecteur de tableau de bord remplit cela. |

## Créer via API

<CodeGroup>

```typescript TypeScript theme={null}
const entitlement = await client.entitlements.create({
  name: 'Study Notes',
  integration_type: 'notion',
  integration_config: {
    notion_template_id: 'tmpl_abc123',
  },
});
```

```python Python theme={null}
client.entitlements.create(
    name="Study Notes",
    integration_type="notion",
    integration_config={"notion_template_id": "tmpl_abc123"},
)
```

```go Go theme={null}
client.Entitlements.New(ctx, dodopayments.EntitlementNewParams{
  Name:            dodopayments.F("Study Notes"),
  IntegrationType: dodopayments.F(dodopayments.EntitlementIntegrationTypeNotion),
  IntegrationConfig: dodopayments.F[dodopayments.IntegrationConfigUnionParam](
    dodopayments.IntegrationConfigNotionConfigParam{
      NotionTemplateID: dodopayments.F("tmpl_abc123"),
    },
  ),
})
```

</CodeGroup>

## Webhooks

Voir [les événements de webhook `entitlement_grant.*`](/developer-resources/webhooks/intents/entitlement-grant) :

- `entitlement_grant.created` est déclenché avec `status: "Pending"` et `oauth_url` toujours `null` ; l’URL d’autorisation Notion est renseignée une fois que le client commence le flux d’acceptation depuis son e-mail de livraison ou le Customer Portal.
- `entitlement_grant.delivered` est déclenché une fois le modèle dupliqué.
- `entitlement_grant.revoked` est déclenché lors de l’annulation ; la copie du client reste inchangée.

## Dépannage

<AccordionGroup>
<Accordion title="Customer didn't get the template">
L'autorisation reste à l'état `Pending` jusqu'à ce que le client termine le processus OAuth de Notion. Renvoyez l'e-mail de livraison ; le lien OAuth expire au bout d'une semaine, après quoi vous pouvez révoquer et recréer l'autorisation.
</Accordion>

<Accordion title="Template picker is empty">
Reconnectez Notion depuis **Droits d'utilisation → Intégrations → Notion** avec l'espace de travail qui possède les modèles sources.
</Accordion>

<Accordion title="Customer wants the template removed after refund">
Notion n'autorise pas la suppression à distance des pages dupliquées. Le client doit supprimer la copie de son propre espace de travail.
</Accordion>
</AccordionGroup>
