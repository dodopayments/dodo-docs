---
title: Portefeuilles Clients
description: Gérez les soldes monétaires prépayés de vos clients. Approvisionnez
  des portefeuilles, appliquez les soldes aux factures d’abonnement, traitez les
  remboursements comme des fonds de portefeuille et suivez l’historique des
  transactions.
icon: wallet
tag: NOUVEAU
---

<CardGroup cols={3}>
{/* LOCKED_PATTERN_d30ef53819bc90a02a78c5baa6364207 */}
  Vérifiez les soldes monétaires des clients dans toutes les devises.
</Card>

{/* LOCKED_PATTERN_c034cbe47a687d16467a94ac4998c5b6 */}
  Ajoutez ou déduisez des fonds des portefeuilles clients.
</Card>

<Card title="Lister les Entrées de Grand Livre" icon="list" href="/api-reference/customers/get-customer-wallets-ledger-entries">
  Consultez l'historique complet des transactions avec pagination.
</Card>
</CardGroup>

## Qu'est-ce que les Portefeuilles Clients ?

Les portefeuilles clients sont des comptes de solde monétaire qui conservent de véritables fonds pour vos utilisateurs. Chaque client en reçoit un automatiquement lors de la création de son compte. Vous pouvez utiliser ces portefeuilles pour :

- **Stocker des fonds prépayés** pour les futurs paiements d’abonnement
- **Gérer les remboursements** sous forme de solde de portefeuille plutôt que par remboursement par carte
- **Émettre des soldes promotionnels** comme les bonus de bienvenue ou les récompenses de fidélité
- **Appliquer automatiquement les fonds du portefeuille aux factures** lors de la facturation
- **Suivre les transactions monétaires** avec un historique de grand livre détaillé

<Info>
Chaque client reçoit automatiquement un portefeuille lorsque vous créez son compte. Les portefeuilles prennent en charge les devises USD et INR avec des soldes monétaires séparés pour chacune d’elles.
</Info>

<Warning>
**Customer Wallets ≠ Facturation basée sur les crédits**

Les portefeuilles clients conservent des **soldes monétaires réels** (USD, INR) qui peuvent être appliqués aux factures et aux paiements d’abonnement.

Si vous souhaitez suivre des unités d’usage virtuelles (appels d’API, jetons, heures de calcul), consultez [Credit-Based Billing](/features/credit-based-billing) à la place.
</Warning>

<Frame>
  <img src="/images/customer/customer-wallet.png" alt="Customer Wallets" />
</Frame>

## Comment ça marche

Les portefeuilles clients contiennent des fonds que les clients peuvent appliquer à leurs achats. Lorsqu’un client a une facture ou un prélèvement d’abonnement récurrent, son solde de portefeuille est vérifié en premier. Les fonds disponibles sont automatiquement appliqués à la facture avant de débiter leur méthode de paiement principale.

### Configuration automatique

Lorsque vous créez un nouveau client, Dodo Payments crée automatiquement un portefeuille avec un solde nul. Il est prêt à recevoir des fonds immédiatement via notre API.

### Support multi-devises

Chaque portefeuille peut conserver des soldes dans différentes devises :

<ResponseField name="USD Balance" type="integer">
Solde en dollars américains (stocké en cents)
</ResponseField>

<ResponseField name="INR Balance" type="integer">
Solde en roupies indiennes (stocké en paise)
</ResponseField>

<Info>
Actuellement, seuls les soldes en **USD** et **INR** sont disponibles. D’autres devises arriveront bientôt.
</Info>

## Travailler avec les portefeuilles

### Vérifier les soldes clients

Découvrez combien de fonds un client possède dans toutes les devises. Cela permet de vérifier les soldes disponibles avant de traiter un achat ou d’afficher le solde dans l’interface de votre application.

{/* LOCKED_PATTERN_5143b6d2b1af8a27d91dbdcc271f257f */}
  Vérifiez les soldes monétaires du portefeuille d’un client dans toutes les devises prises en charge.
</Card>

### Ajouter ou retirer des fonds

Approvisionnez les portefeuilles clients (comme des bonus de bienvenue ou des soldes de remboursement) ou retirez des fonds (comme des prélèvements d’abonnement). Vous pouvez fournir des motifs pour chaque transaction afin de conserver une piste d’audit claire.

{/* LOCKED_PATTERN_53d873bd6fa65f248f65f62194471195 */}
Le champ `entry_type` utilise `'credit'` pour ajouter des fonds au portefeuille et `'debit'` pour soustraire des fonds du portefeuille.
{/* LOCKED_PATTERN_49aff3a3aee1d0bab41e7eade47939d0 */}

{/* LOCKED_PATTERN_badd0028afde5a79d28ac4649f792da0 */}
  Ajoutez ou retirez des fonds du portefeuille d’un client.
</Card>

### Consulter l’historique des transactions

Voyez chaque transaction de crédit et de débit d’un client. Ce grand livre détaillé vous aide à rapprocher les comptes et apporte de la transparence à vos clients.

{/* LOCKED_PATTERN_1551c6d70e2dc4cc5db66c130a8209ed */}
  Consultez chaque transaction monétaire d’un client.
</Card>

## Exemples concrets

### Remboursement vers le portefeuille

Lorsqu’un client demande un remboursement, vous pouvez ajouter le montant à son solde de portefeuille au lieu de procéder à un remboursement traditionnel par carte. Cela conserve les fonds dans votre écosystème pour de futurs achats.

```javascript
async function refundToWallet(customerId, refundAmount, originalPaymentId) {
  await client.customers.wallets.ledgerEntries.create(customerId, {
    amount: refundAmount, // Amount in cents
    currency: 'USD',
    entry_type: 'credit',
    reason: `Refund for payment ${originalPaymentId}`,
    idempotency_key: `refund_${originalPaymentId}`
  });
}
```

### Bonus de bienvenue / Solde promotionnel

Offrez aux nouveaux clients un bonus monétaire de bienvenue pour les inciter à effectuer leur premier achat.

```javascript
async function addWelcomeBonus(customerId) {
  await client.customers.wallets.ledgerEntries.create(customerId, {
    amount: 1000, // $10.00 promotional balance
    currency: 'USD',
    entry_type: 'credit',
    reason: 'Welcome bonus - $10 promotional balance',
    idempotency_key: `welcome_${customerId}`
  });
}
```

### Paiement d’abonnement depuis le portefeuille

Retirez des fonds d’un portefeuille pour couvrir une charge d’abonnement ou un achat manuel.

```javascript
async function deductForPurchase(customerId, purchaseAmount, purchaseId) {
  try {
    await client.customers.wallets.ledgerEntries.create(customerId, {
      amount: purchaseAmount,
      currency: 'USD',
      entry_type: 'debit',
      reason: `Subscription charge - monthly billing`,
      idempotency_key: `charge_${purchaseId}`
    });
  } catch (error) {
    if (error.status === 400) {
      console.log('Insufficient wallet balance');
    }
  }
}
```

### Système de facturation prépayée

Permettez aux clients d’alimenter leur compte à l’avance et de prélever sur ce solde au fil du temps.

<Steps>
{/* LOCKED_PATTERN_3536454d81e61417903d480ee826165e */}
Ajoutez des fonds au portefeuille du client lorsqu’il effectue un dépôt.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 5000, // $50.00 deposit
  currency: 'USD',
  entry_type: 'credit',
  reason: 'Account funding - prepaid deposit',
  idempotency_key: `deposit_${paymentId}`
});
```

</Step>

{/* LOCKED_PATTERN_f0ce36f0b2e1d1e092055ef58068c98b */}
Déduisez du solde à mesure que le client utilise vos services ou renouvelle des abonnements.
</Step>

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 1500, // $15.00 charge
  currency: 'USD', 
  entry_type: 'debit',
  reason: 'Service purchase - monthly subscription',
  idempotency_key: `purchase_${purchaseId}`
});
```

</Step>

{/* LOCKED_PATTERN_b3cd1d7565f5159ce54a307d70236c3b */}
Vérifiez si les clients sont à court de fonds pour leur proposer un rechargement.
</Step>

```javascript
const wallets = await client.customers.wallets.list(customerId);
const usdWallet = wallets.items.find(w => w.currency === 'USD');
const balance = usdWallet.balance;

if (balance < 1000) { // Less than $10.00
  // Send low balance notification
  await sendLowBalanceNotification(customerId, balance);
}
```

</Step>
</Steps>

### Support multi-devises

Gérez des soldes monétaires séparés pour les clients de différentes régions.

<AccordionGroup>
{/* LOCKED_PATTERN_241461afba6ad1122af8febf799e78e0 */}
Gérez les fonds en USD pour les clients basés aux États-Unis.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 20000, // $200.00 in cents
  currency: 'USD',
  entry_type: 'credit',
  reason: 'USD account funding',
  idempotency_key: `usd_deposit_${paymentId}`
});
```

</Accordion>

{/* LOCKED_PATTERN_a5551b33d37120d971e7846b53734bfd */}
Gérez les fonds en INR pour les clients indiens.

```javascript
await client.customers.wallets.ledgerEntries.create(customerId, {
  amount: 1500000, // Rs 15,000 in paise
  currency: 'INR',
  entry_type: 'credit',
  reason: 'INR account funding',
  idempotency_key: `inr_deposit_${paymentId}`
});
```

</Accordion>
</AccordionGroup>

## Bonnes pratiques

### Éviter les transactions en double

Utilisez des clés d’idempotence pour vous assurer de ne pas ajouter ou déduire accidentellement des fonds deux fois pour un même événement.

```javascript
async function addFundsSafely(customerId, amount, reason) {
  const idempotencyKey = `${reason}_${customerId}_${Date.now()}`;
  
  try {
    const result = await client.customers.wallets.ledgerEntries.create(customerId, {
      amount: amount,
      currency: 'USD',
      entry_type: 'credit',
      reason: reason,
      idempotency_key: idempotencyKey
    });
    
    return { success: true, wallet: result };
  } catch (error) {
    if (error.status === 409) {
      // Transaction already processed
      return { success: true, wallet: null, duplicate: true };
    }
    
    throw error;
  }
}
```

### Vérifier les soldes avant de prélever

Vérifiez que les clients disposent de fonds suffisants avant de tenter de traiter des transactions importantes depuis le portefeuille.

```javascript
async function checkBalanceBeforeOperation(customerId, requiredAmount) {
  const wallets = await client.customers.wallets.list(customerId);
  const usdWallet = wallets.items.find(w => w.currency === 'USD');
  
  if (!usdWallet || usdWallet.balance < requiredAmount) {
    throw new Error('Insufficient funds for this operation');
  }
  
  return usdWallet.balance;
}
```

## Ce qui arrive ensuite

<Warning>
Les fonctionnalités suivantes sont prévues pour les futures versions :
</Warning>

- **Expiration des soldes** : définissez une durée d’expiration des fonds
- **Meilleures analyses** : rapports de dépenses détaillés et tendances des soldes
- **Plus de webhooks** : notifications en temps réel pour les modifications de solde et les alertes de solde faible

<Tip>
Commencez par des opérations d’alimentation et de déduction de base, puis intégrez des workflows de facturation automatisée plus complexes au fur et à mesure de la croissance de votre entreprise.
</Tip>
