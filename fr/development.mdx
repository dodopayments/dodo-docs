---
title: Développement
description: Aperçu des modifications localement pour mettre à jour vos docs
---

<Info>
  **Prérequis** : Veuillez installer Node.js (version 19 ou supérieure) avant de continuer.
</Info>

Suivez ces étapes pour installer et exécuter Mintlify sur votre système d'exploitation :

**Étape 1** : Installer Mintlify :

<CodeGroup>

  ```bash npm
  npm i -g mintlify
  ```

```bash yarn
yarn global add mintlify
```

</CodeGroup>

**Étape 2** : Accédez au répertoire docs (où se trouve le fichier `mint.json`) et exécutez la commande suivante :

```bash
mintlify dev
```

Un aperçu local de votre documentation sera disponible à `http://localhost:3000`.

### Ports personnalisés

Par défaut, Mintlify utilise le port 3000. Vous pouvez personnaliser le port sur lequel Mintlify s'exécute en utilisant le drapeau `--port`. Pour exécuter Mintlify sur le port 3333, par exemple, utilisez cette commande :

```bash
mintlify dev --port 3333
```

Si vous essayez d'exécuter Mintlify sur un port déjà utilisé, il utilisera le prochain port disponible :

```md
Port 3000 is already in use. Trying 3001 instead.
```

## Versions de Mintlify

Veuillez noter que chaque version de la CLI est associée à une version spécifique de Mintlify. Si votre site local ne correspond pas à la version de production, veuillez mettre à jour la CLI :

<CodeGroup>

```bash npm
npm i -g mintlify@latest
```

```bash yarn
yarn global upgrade mintlify
```

</CodeGroup>

## Validation des liens

La CLI peut aider à valider les liens de référence dans votre documentation. Pour identifier les liens brisés, utilisez la commande suivante :

```bash
mintlify broken-links
```

## Déploiement

<Tip>
  Éditeurs illimités disponibles sous le [Plan Pro](https://mintlify.com/pricing) et supérieur.
</Tip>

Si le déploiement est réussi, vous devriez voir ce qui suit :

<Frame>
  <img src="/images/checks-passed.png" style={{ borderRadius: '0.5rem' }} />
</Frame>

## Formatage du code

Nous vous suggérons d'utiliser des extensions sur votre IDE pour reconnaître et formater MDX. Si vous êtes utilisateur de VSCode, envisagez l'[extension MDX VSCode](https://marketplace.visualstudio.com/items?itemName=unifiedjs.vscode-mdx) pour la coloration syntaxique, et [Prettier](https://marketplace.visualstudio.com/items?itemName=esbenp.prettier-vscode) pour le formatage du code.

## Dépannage

<AccordionGroup>
  <Accordion title='Erreur : Impossible de charger le module "sharp" en utilisant le runtime darwin-arm64'>

    Cela peut être dû à une version obsolète de node. Essayez ce qui suit :
    1. Supprimez la version actuellement installée de mintlify : `npm remove -g mintlify`
    2. Mettez à niveau vers Node v19 ou supérieur.
    3. Réinstallez mintlify : `npm install -g mintlify`
  </Accordion>

  <Accordion title="Problème : Rencontrer une erreur inconnue">
  
    Solution : Allez à la racine de votre appareil et supprimez le dossier \~/.mintlify. Ensuite, exécutez à nouveau `mintlify dev`.
  </Accordion>
</AccordionGroup>

Vous vous demandez ce qui a changé dans la version de la CLI ? [Consultez le changelog de la CLI.](https://www.npmjs.com/package/mintlify?activeTab=versions)
