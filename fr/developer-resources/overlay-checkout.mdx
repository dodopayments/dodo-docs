---
title: Guide de Checkout Overlay
description: Une bibliothèque moderne TypeScript pour intégrer le checkout
  overlay de Dodo Payments et écouter les événements de checkout en temps réel.
icon: cart-shopping
---

## Aperçu

Le SDK de Checkout Dodo Payments offre un moyen fluide d'intégrer notre overlay de paiement dans votre application web. Construit avec TypeScript et des normes web modernes, il propose une solution robuste pour gérer les paiements avec un traitement d'événements en temps réel et des thèmes personnalisables.

<Frame>
    <img src="/images/cover-images/overlay-checkout.png" alt="Image de couverture du Checkout Overlay"/>
</Frame>

## Ressources

- 📚 [Documentation d'Intégration](https://docs.dodopayments.com/api-reference/overlay-checkout)
- 🎮 [Démo Interactive](https://atlas.dodopayments.com#overlay-checkout)
- 💻 [Code Source de la Démo](https://github.com/dodopayments/dodo-checkout-demo/blob/main/src/components/Home/OverlayCheckout.tsx)

## Installation

```bash
# Using npm
npm install dodopayments-checkout

# Using yarn
yarn add dodopayments-checkout

# Using pnpm
pnpm add dodopayments-checkout
```

## Démarrage Rapide

```typescript
import { DodoPayments } from "dodopayments-checkout";

// Initialize the SDK
DodoPayments.Initialize({
  mode: "live", // 'test' or 'live'
  onEvent: (event) => {
    console.log("Checkout event:", event);
  },
});

// Open checkout
DodoPayments.Checkout.open({
  checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
});
```

## Configuration

### Options d'Initialisation

```typescript
interface InitializeOptions {
  mode: "test" | "live";
  onEvent: (event: CheckoutEvent) => void;
}
```

| Option | Type | Requis | Description |
|--------|------|----------|-------------|
| `mode` | string | Oui | Mode d'environnement : 'test' ou 'live' |
| `onEvent` | function | Oui | Fonction de rappel pour gérer les événements de checkout |

### Options de Checkout

```typescript
interface CheckoutOptions {
  checkoutUrl: string;
}
```

| Option | Type | Requis | Description |
|--------|------|----------|-------------|
| `checkoutUrl` | string | Oui | URL de session de checkout de l'API de création de session de checkout |

## Gestion des Événements

Le SDK fournit des événements en temps réel auxquels vous pouvez vous abonner :

```typescript
DodoPayments.Initialize({
  onEvent: (event: CheckoutEvent) => {
    switch (event.event_type) {
      case "checkout.opened":
        // Checkout overlay has been opened
        break;
      case "checkout.payment_page_opened":
        // Payment page has been displayed
        break;
      case "checkout.customer_details_submitted":
        // Customer and billing details submitted
        break;
      case "checkout.closed":
        // Checkout has been closed
        break;
      case "checkout.redirect":
        // Checkout will perform a redirect
        break;
      case "checkout.error":
        // An error occurred
        break;
    }
  }
});
```

## Méthodes

### Ouvrir le Checkout

```typescript
DodoPayments.Checkout.open({
  checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
});
```

### Fermer le Checkout

```typescript
DodoPayments.Checkout.close();
```

### Vérifier le Statut

```typescript
const isOpen = DodoPayments.Checkout.isOpen();
```

## Support des Navigateurs

- Chrome (dernier)
- Firefox (dernier)
- Safari (dernier)
- Edge (dernier)
- IE11+

> **Remarque** : Apple Pay n'est actuellement pas pris en charge dans l'expérience de checkout overlay. Nous prévoyons d'ajouter le support pour Apple Pay dans une future version.

## Implémentation CDN

Pour une intégration rapide, vous pouvez utiliser notre CDN :

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dodo Payments Checkout</title>
    
    <!-- Load DodoPayments -->
    <script src="https://cdn.jsdelivr.net/npm/dodopayments-checkout@latest/dist/index.js"></script>
    <script>
        // Initialize the SDK
        DodoPaymentsCheckout.DodoPayments.Initialize({
            mode: "test", // Change to 'live' for production
            onEvent: (event) => {
                console.log('Checkout event:', event);
            }
        });
    </script>
</head>
<body>
    <button onclick="openCheckout()">Checkout Now</button>

    <script>
        function openCheckout() {
            DodoPaymentsCheckout.DodoPayments.Checkout.open({
                checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
            });
        }
    </script>
</body>
</html>
```

## Support TypeScript

Le SDK est écrit en TypeScript et inclut des définitions de type complètes. Toutes les API publiques sont entièrement typées pour une meilleure expérience développeur.

## Gestion des Erreurs

Le SDK fournit des informations d'erreur détaillées via le système d'événements. Implémentez toujours une gestion des erreurs appropriée dans votre `onEvent` fonction de rappel :

```typescript
DodoPayments.Initialize({
  onEvent: (event: CheckoutEvent) => {
    if (event.event_type === "checkout.error") {
      console.error("Checkout error:", event.data?.message);
      // Handle error appropriately
    }
  }
});
```

## Meilleures Pratiques

1. Toujours initialiser le SDK avant d'essayer d'ouvrir le checkout
2. Implémenter une gestion des erreurs appropriée dans votre fonction de rappel d'événements
3. Utiliser le mode test pendant le développement
4. Gérer tous les événements pertinents pour une expérience utilisateur complète
5. Utiliser des URL de checkout valides de l'API de création de session de checkout
6. Utiliser TypeScript pour une meilleure sécurité de type et expérience développeur

## Guide Étape par Étape

### 1. Configuration du Projet

Tout d'abord, assurez-vous d'avoir un projet JavaScript/TypeScript moderne. Nous recommandons d'utiliser Next.js, React ou Vue.js.

### 2. Installer le SDK

Installez le SDK de Checkout Dodo Payments en utilisant votre gestionnaire de paquets préféré :

```bash
# Using npm
npm install dodopayments-checkout

# Using yarn
yarn add dodopayments-checkout

# Using pnpm
pnpm add dodopayments-checkout
```

### 3. Implémentation de Base

Créez un nouveau composant pour votre bouton de checkout :

```typescript
// components/CheckoutButton.tsx
"use client";

import { Button } from "@/components/ui/button";
import { DodoPayments } from "dodopayments-checkout";
import { useEffect, useState } from "react";

export function CheckoutButton() {
  const [isLoading, setIsLoading] = useState(false);

  useEffect(() => {
    // Initialize the SDK
    DodoPayments.Initialize({
      mode: "test", // Change to 'live' for production
      onEvent: (event) => {
        console.log("Checkout event:", event);
        
        // Handle different events
        switch (event.event_type) {
          case "checkout.opened":
            setIsLoading(false);
            break;
          case "checkout.error":
            // Handle error
            break;
        }
      },
    });
  }, []);

  const handleCheckout = async () => {
    try {
      setIsLoading(true);
      await DodoPayments.Checkout.open({
        checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
      });
    } catch (error) {
      console.error("Failed to open checkout:", error);
      setIsLoading(false);
    }
  };

  return (
    <Button 
      onClick={handleCheckout}
      disabled={isLoading}
    >
      {isLoading ? "Loading..." : "Checkout Now"}
    </Button>
  );
}
```

### 4. Ajouter à Votre Page

Utilisez le bouton de checkout dans votre page :

```typescript
// app/page.tsx
import { CheckoutButton } from "@/components/CheckoutButton";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <h1>Welcome to Our Store</h1>
      <CheckoutButton />
    </main>
  );
}
```

### 5. Gérer le Succès et l'Échec

Créez des pages de succès et d'échec :

```typescript
// app/success/page.tsx
export default function SuccessPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center">
      <h1>Payment Successful!</h1>
      <p>Thank you for your purchase.</p>
    </div>
  );
}

// app/failure/page.tsx
export default function FailurePage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center">
      <h1>Payment Failed</h1>
      <p>Please try again or contact support.</p>
    </div>
  );
}
```

### 6. Tester Votre Intégration

1. Démarrez votre serveur de développement :

```bash
npm run dev
```

2. Testez le flux de checkout :
   - Cliquez sur le bouton de checkout
   - Vérifiez que l'overlay apparaît
   - Testez le flux de paiement en utilisant des identifiants de test
   - Confirmez que les redirections fonctionnent correctement

### 7. Passer en Production

Lorsque vous êtes prêt à passer en production :

1. Changez le mode en 'live' :

```typescript
DodoPayments.Initialize({
  mode: "live",
  onEvent: (event) => {
    console.log("Checkout event:", event);
  }
});
```

2. Mettez à jour vos URL de checkout pour utiliser des sessions de checkout en direct
3. Testez le flux complet en production
4. Surveillez les événements et les erreurs

### Problèmes Courants et Solutions

1. **Le checkout ne s'ouvre pas**
   - Vérifiez l'initialisation du SDK
   - Vérifiez les erreurs dans la console
   - Assurez-vous que l'URL de checkout est valide et provient de l'API de création de session de checkout

2. **Les événements ne se déclenchent pas**
   - Confirmez que le gestionnaire d'événements est correctement configuré
   - Vérifiez les erreurs JavaScript
   - Vérifiez la connectivité réseau

3. **Problèmes de style**
   - Assurez-vous qu'il n'y a pas de conflits CSS
   - Vérifiez les paramètres de thème
   - Vérifiez le design réactif

Pour plus d'aide, visitez notre Discord ou contactez notre équipe de support développeur.