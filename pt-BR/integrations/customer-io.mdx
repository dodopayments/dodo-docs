---
title: Customer.io
description: Envie eventos de Dodo Payments para Customer.io para acionar
  automações de e-mail e jornadas do cliente.
icon: chart-area
---

## Introdução

Acione campanhas de e-mail personalizadas e jornadas do cliente com base em eventos de pagamento. Envie e-mails de boas-vindas para novos clientes, atualizações de assinatura e notificações de falha de pagamento automaticamente através do Customer.io.

<Info>
Esta integração exige seu Site ID e API Key do Customer.io.
</Info>

## Começando

<Steps>
{/* LOCKED_PATTERN_c337a250e2e6db475d2eb18e95cb3e6a */}
  No painel do Dodo Payments, navegue até <b>Webhooks → + Add Endpoint</b> e expanda o menu suspenso de integrações.
  <Frame>
    <img src="/images/integrations/customer-io.png" alt="Adicionar Endpoint e menu suspenso de integrações" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_8c81cd65499563be0d85062eba1d51dc */}
  Escolha o cartão de integração <b>Customer.io</b>.
</Step>

{/* LOCKED_PATTERN_aecb1d6398711f1eba5cc3f3f7c56aff */}
  Forneça seu Site ID e API Key do Customer.io na configuração.
</Step>

{/* LOCKED_PATTERN_4ac4616f59ca1d74262754150410c05f */}
  Edite o código de transformação para formatar eventos para a Track API do Customer.io.
</Step>

{/* LOCKED_PATTERN_f6e4d766e7306e52042eed3b757944bb */}
  Teste com cargas úteis de exemplo e clique em <b>Create</b> para ativar a sincronização.
</Step>

{/* LOCKED_PATTERN_8d2cbcb809e6363cb1f41f0f62f98444 */}
  🎉 Os eventos de pagamento agora acionarão automações de e-mail do Customer.io.
</Step>
</Steps>

## Exemplos de Código de Transformação

### Rastrear Eventos de Pagamento

```javascript track_payments.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const p = webhook.payload.data;
    webhook.url = "https://track.customer.io/api/v2/entity";
    webhook.payload = {
      type: "person",
      identifiers: {
        id: p.customer.customer_id
      },
      action: "payment_completed",
      name: "Payment Completed",
      attributes: {
        email: p.customer.email,
        name: p.customer.name,
        payment_amount: (p.total_amount / 100).toFixed(2),
        payment_method: p.payment_method || "unknown",
        payment_id: p.payment_id,
        currency: p.currency || "USD"
      }
    };
  }
  return webhook;
}
```

### Rastrear Ciclo de Vida da Assinatura

```javascript track_subscriptions.js icon="js" expandable
function handler(webhook) {
  const s = webhook.payload.data;
  switch (webhook.eventType) {
    case "subscription.active":
      webhook.url = "https://track.customer.io/api/v2/entity";
      webhook.payload = {
        type: "person",
        identifiers: {
          id: s.customer.customer_id
        },
        action: "subscription_started",
        name: "Subscription Started",
        attributes: {
          email: s.customer.email,
          subscription_id: s.subscription_id,
          product_id: s.product_id,
          amount: (s.recurring_pre_tax_amount / 100).toFixed(2),
          frequency: s.payment_frequency_interval,
          next_billing: s.next_billing_date
        }
      };
      break;
    case "subscription.cancelled":
      webhook.url = "https://track.customer.io/api/v2/entity";
      webhook.payload = {
        type: "person",
        identifiers: {
          id: s.customer.customer_id
        },
        action: "subscription_cancelled",
        name: "Subscription Cancelled",
        attributes: {
          email: s.customer.email,
          subscription_id: s.subscription_id,
          cancelled_at: s.cancelled_at,
          cancel_at_next_billing: s.cancel_at_next_billing_date
        }
      };
      break;
  }
  return webhook;
}
```

### Rastrear Atributos do Cliente

```javascript track_attributes.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const p = webhook.payload.data;
    webhook.url = "https://track.customer.io/api/v2/entity";
    webhook.payload = {
      type: "person",
      identifiers: {
        id: p.customer.customer_id
      },
      action: "identify",
      name: "Customer Identified",
      attributes: {
        email: p.customer.email,
        name: p.customer.name,
        total_spent: (p.total_amount / 100).toFixed(2),
        payment_method: p.payment_method || "unknown",
        last_payment_date: webhook.payload.timestamp,
        customer_since: webhook.payload.timestamp
      }
    };
  }
  return webhook;
}
```

## Dicas

- Use nomes de eventos consistentes que correspondam às suas campanhas do Customer.io
- Inclua atributos relevantes para personalização
- Defina identificadores de clientes adequados para rastreamento preciso
- Use nomes de eventos significativos para acionadores de campanha

## Solução de Problemas

<AccordionGroup>
{/* LOCKED_PATTERN_9fe8a8a31ec280f2b1e7e2d1e9c6663d */}
- Verifique se Site ID e API Key estão corretos
- Verifique se os nomes dos eventos correspondem às suas campanhas do Customer.io
- Garanta que os identificadores dos clientes estejam corretamente configurados
- Revise os limites de taxa da API do Customer.io
</Accordion>

{/* LOCKED_PATTERN_14f1ac2eb6bb156b1fd74792fba48a19 */}
- Valide se a estrutura JSON corresponde ao formato da API do Customer.io
- Verifique se todos os campos obrigatórios estão presentes
- Garanta que os nomes de eventos e atributos estejam formatados corretamente
</Accordion>
</AccordionGroup>