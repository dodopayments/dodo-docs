---
title: Sessões de Checkout
description: Crie experiências de checkout seguras e hospedadas que gerenciam
  todo o fluxo de pagamento para compras únicas e assinaturas com controle total
  de personalização.
icon: cart-shopping
---

<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  Coloque sua primeira sessão de checkout em funcionamento em menos de 5 minutos
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  Explore a documentação completa da API e teste interativamente solicitações e respostas de Sessão de Checkout.
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  Calcule preços, impostos e totais antes de criar uma sessão.
</Card>
</CardGroup>

<Info>
**Validade da Sessão**: As sessões de checkout são válidas por 24 horas por padrão. Se você passar `confirm=true` na sua solicitação, a sessão será válida por apenas 15 minutos.
</Info>

## Pré-requisitos

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  Você precisará de uma conta comercial Dodo Payments ativa com acesso à API.
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  Gere suas credenciais de API no painel da Dodo Payments:
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  Crie seus produtos no painel da Dodo Payments antes de implementar sessões de checkout.
</Step>
</Steps>

## Criando Sua Primeira Sessão de Checkout

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### Resposta da API

Todos os métodos acima retornam a mesma estrutura de resposta:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
Extraia o `checkout_url` da resposta da API.
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
Direcione seu cliente para a URL de checkout para concluir a compra.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Opções de Integração Alternativas**: Em vez de redirecionar, você pode incorporar o checkout diretamente na sua página usando o [Overlay Checkout](/developer-resources/overlay-checkout) (sobreposição modal) ou o [Inline Checkout](/developer-resources/inline-checkout) (incorporado totalmente). Ambas as opções usam a mesma URL de sessão de checkout.
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
Após o pagamento, os clientes são redirecionados para o seu `return_url` com parâmetros de consulta incluindo ID do pagamento/assinatura, status, e-mail do cliente e quaisquer chaves de licença. Veja os [documentos de parâmetros `return_url`](#request-body) para a lista completa.
</Step>
</Steps>

## Corpo da Solicitação

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  Campos essenciais necessários para cada sessão de checkout
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  Configuração adicional para personalizar sua experiência de checkout
</Card>
</CardGroup>

### Campos Obrigatórios

<ParamField body="product_cart" type="array" required>
Array de produtos a serem incluídos na sessão de checkout. Cada produto deve ter um `product_id` válido no painel da Dodo Payments.


<Tip>
**Checkout Misto**: Você pode combinar produtos de pagamento único e produtos de assinatura na mesma sessão de checkout. Isso permite casos de uso avançados como taxas de configuração com assinaturas, pacotes de hardware com SaaS e mais.
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
O identificador exclusivo do produto no seu painel da Dodo Payments.


**Exemplo:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
Quantidade do produto.


**Exemplo:** `1` para item único, `3` para múltiplas quantidades
</ParamField>

<ParamField body="addons" type="array">
Array de complementos para anexar ao produto. Válido apenas se o produto for uma assinatura.


{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
Valor que o cliente paga se pay_what_you_want estiver habilitado. Se desativado, este campo será ignorado.


**Formato**: Representado na menor unidade da moeda (por exemplo, centavos para USD). Por exemplo, para cobrar $1.00, passe `100`.
</ParamField>
</Expandable>
</ParamField>

<Tip>
**Encontre seus IDs de Produto**: Você pode encontrar os IDs de produto no painel da Dodo Payments em Products → View Details, ou usando a [List Products API](/api-reference/products/get-products).
</Tip>

### Campos Opcionais

Configure esses campos para personalizar a experiência de checkout e adicionar lógica de negócios ao seu fluxo de pagamento.

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
Informações do cliente. Você pode anexar um cliente existente usando o ID dele ou criar um novo registro durante o checkout.


<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}
Anexe um cliente existente à sessão de checkout usando o ID dele.


{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
O identificador exclusivo de um cliente existente. Use isso para anexar a sessão de checkout a um cliente já existente em vez de criar um novo.
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
Crie um novo registro de cliente durante o checkout.


{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
Endereço de e-mail do cliente. Obrigatório ao criar um novo cliente.
</ParamField>

<ParamField body="name" type="string">
Nome completo do cliente como deve aparecer em recibos e faturas.


**Exemplo**: `"John Doe"` ou `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
Número de telefone do cliente em formato internacional. Exigido para alguns métodos de pagamento e prevenção a fraudes.


**Formato**: Inclua o código do país, por exemplo, `"+1234567890"` para números dos EUA
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
Informações de endereço de cobrança para cálculo preciso de impostos, prevenção a fraudes e conformidade regulatória.


<Info>
Quando `confirm` estiver definido como `true`, todos os campos de endereço de cobrança tornam-se obrigatórios para a criação bem-sucedida da sessão.
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
Endereço completo, incluindo número, nome da rua e apartamento/unidade, se aplicável.


**Exemplo**: `"123 Main St, Apt 4B"` ou `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
Nome da cidade ou município.


**Exemplo**: `"San Francisco"`, `"New York"`, `"London"`
</ParamField>

<ParamField body="state" type="string">
Nome do estado, província ou região. Use nomes completos ou abreviações padrão.


**Exemplo**: `"California"` ou `"CA"`, `"Ontario"` ou `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
Código ISO de dois dígitos do país (ISO 3166-1 alpha-2). Este campo é sempre obrigatório quando billing_address é fornecido.


**Exemplos**: `"US"` (Estados Unidos), `"CA"` (Canadá), `"GB"` (Reino Unido), `"DE"` (Alemanha)

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  Veja a lista completa de países suportados e seus códigos ISO
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
Código postal, CEP ou equivalente conforme os requisitos do país.


**Exemplos**: `"94102"` (EUA), `"M5V 3A8"` (Canadá), `"SW1A 1AA"` (Reino Unido)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
Controle quais métodos de pagamento estão disponíveis para os clientes durante o checkout. Isso ajuda a otimizar para mercados específicos ou requisitos comerciais.


**Opções disponíveis**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**Crítico**: Sempre inclua `credit` e `debit` como opções de fallback para evitar falhas no checkout quando os métodos de pagamento preferidos não estiverem disponíveis.
</Warning>

**Exemplo**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
Substitua a seleção de moeda padrão por uma moeda de cobrança fixa. Usa códigos de moeda ISO 4217.


**Moedas suportadas**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR`, e mais

**Exemplo**: `"USD"` para Dólares Americanos, `"EUR"` para Euros

<Note>
Este campo só é eficaz quando o preço adaptativo está habilitado. Se o preço adaptativo estiver desativado, será usada a moeda padrão do produto.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
Exiba métodos de pagamento salvos anteriormente para clientes retornantes, melhorando a velocidade do checkout e a experiência do usuário.
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
URL para redirecionar os clientes após a conclusão do pagamento. A Dodo Payments acrescenta os seguintes parâmetros de consulta à sua URL no redirecionamento:

| Parameter | Type | Condition |
|-----------|------|-----------|
| `payment_id` | `string` | Sempre presente para pagamentos únicos |
| `subscription_id` | `string` | Sempre presente para pagamentos de assinatura |
| `status` | `string` | Sempre presente |
| `license_key` | `string` | Presente se o produto tiver chaves de licença habilitadas. Separadas por vírgula se forem múltiplas |
| `email` | `string` | Presente se o cliente tiver um e-mail registrado |

**URLs de redirecionamento de exemplo:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
Use os parâmetros de consulta `license_key` e `email` para exibir chaves de licença ou enviar uma confirmação imediatamente na sua página de retorno, sem precisar de uma chamada adicional à API.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
Se verdadeiro, finaliza todos os detalhes da sessão imediatamente. A API retornará um erro se dados obrigatórios estiverem faltando.
</ParamField>

<ParamField body="discount_code" type="string">
Aplicar um código de desconto à sessão de checkout.
</ParamField>

<ParamField body="metadata" type="object">
Pares de chave-valor personalizados para armazenar informações adicionais sobre a sessão.
</ParamField>

<ParamField body="force_3ds" type="boolean">
Substitua o comportamento padrão de 3DS do comerciante para esta sessão.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Habilite o modo de coleta de endereço mínimo. Quando ativado, o checkout coleta apenas:
- **País**: Sempre necessário para determinação de impostos
- **CEP/Código postal**: Apenas em regiões onde é necessário para cálculo de imposto sobre vendas, IVA ou GST

Isso reduz significativamente o atrito no checkout ao eliminar campos de formulário desnecessários.

<Tip>
Ative endereço mínimo para concluir o checkout mais rapidamente. A coleta completa de endereço continua disponível para empresas que exigem detalhes completos de cobrança.
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
Personalize a aparência e o comportamento da interface de checkout.


{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
Tema para a interface de checkout. Opções: `light`, `dark` ou `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
Exibir a seção de detalhes do pedido na interface de checkout.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
Exibir a tag "sob demanda" para produtos aplicáveis.
</ParamField>

<ParamField body="force_language" type="string">
Force a interface do checkout a renderizar em um idioma específico. Por padrão, o checkout detecta automaticamente o idioma do navegador do cliente.


**Códigos de idioma suportados:**
`ar` (Árabe), `ca` (Catalão), `de` (Alemão), `en` (Inglês), `es` (Espanhol), `fr` (Francês), `he` (Hebraico), `id` (Indonésio), `it` (Italiano), `ja` (Japonês), `ko` (Coreano), `ms` (Malaio), `nl` (Holandês), `pl` (Polonês), `pt` (Português), `ro` (Romeno), `ru` (Russo), `sv` (Sueco), `th` (Tailandês), `tr` (Turco), `zh` (Chinês)

**Exemplo**: `"es"` para Espanhol, `"ja"` para Japonês
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
Configure recursos e comportamentos específicos para a sessão de checkout.


{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
Permita que os clientes selecionem sua moeda preferida durante o checkout.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
Exiba o campo de código de desconto na interface de checkout.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
Colete números de telefone dos clientes durante o checkout.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
Permita que os clientes informem números de identificação fiscal.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
Force a criação de um novo registro de cliente em vez de atualizar os existentes.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
Permita que os clientes editem o endereço de e-mail durante o checkout.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
Permita que os clientes editem o nome durante o checkout.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
Permita que os clientes editem o endereço durante o checkout.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
Permita que os clientes editem a cidade durante o checkout.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
Permita que os clientes editem o estado durante o checkout.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
Permita que os clientes editem o país durante o checkout.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
Permita que os clientes editem o código postal durante o checkout.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Habilite o modo de coleta de endereço mínimo. Quando ativado, o checkout coleta apenas:
- **País**: Sempre necessário para determinação de impostos
- **CEP/Código postal**: Apenas em regiões onde é necessário para cálculo de imposto sobre vendas, IVA ou GST

Isso reduz significativamente o atrito no checkout ao eliminar campos de formulário desnecessários.

<Tip>
Ative endereço mínimo para concluir o checkout mais rapidamente. A coleta completa de endereço continua disponível para empresas que exigem detalhes completos de cobrança.
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
Ignore a página padrão de sucesso do pagamento e redirecione os clientes imediatamente para seu `return_url` após a conclusão do pagamento.

<Tip>
Use isso quando você tiver uma página de sucesso personalizada que ofereça melhor experiência do usuário, ou para aplicativos móveis e fluxos de checkout incorporados.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
Colete informações adicionais dos clientes durante o checkout com campos de formulário personalizados. Você pode definir até 5 campos personalizados por sessão de checkout. As respostas dos clientes são incluídas nas cargas úteis de webhook e estão disponíveis via API.


{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
Identificador único para este campo. Usado como chave nas cargas úteis de webhook e nas respostas da API.


**Exemplo**: `"company_name"`, `"referral_source"`, `"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
Rótulo exibido para o cliente no formulário de checkout.


**Exemplo**: `"Company Name"`, `"How did you hear about us?"`, `"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
Tipo de campo que determina as regras de validação de entrada.


**Tipos disponíveis**:
- `text` - Campo de texto de linha única
- `number` - Entrada numérica
- `email` - Endereço de e-mail com validação
- `url` - URL com validação
- `date` - Seletor de data
- `dropdown` - Seleção entre opções predefinidas
- `boolean` - Alternância Sim/Não
</ParamField>

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
Se este campo deve ser preenchido antes do checkout ser concluído.
</ParamField>

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
Texto de espaço reservado exibido no campo de entrada.
</ParamField>

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
Lista de opções para o tipo de campo `dropdown`. Obrigatório quando `field_type` for `dropdown`, ignorado para outros tipos.


**Exemplo**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
As respostas aos campos personalizados são incluídas em:
- **Webhooks**: `payment.succeeded`, `subscription.created` e outros payloads de eventos relevantes contêm o array `custom_field_responses`
- **Respostas da API**: Objetos de pagamento e assinatura incluem `custom_field_responses`
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
Configuração adicional para sessões de checkout que contêm produtos de assinatura.


{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
Número de dias do período de teste antes da primeira cobrança. Deve estar entre 0 e 10000 dias.
</ParamField>

<ParamField body="on_demand" type="object">
Configuração de assinatura sob demanda para cobrança baseada em uso ou medição.


{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
Se definido como true, não realiza cobrança e apenas autoriza os dados do método de pagamento para uso futuro.
</ParamField>

<ParamField body="product_price" type="integer">
Preço do produto para a cobrança inicial ao cliente. Se não especificado, será usado o preço armazenado do produto.


**Formato**: Representado na menor unidade da moeda (por exemplo, centavos para USD). Por exemplo, para cobrar $1.00, passe `100`.
</ParamField>

<ParamField body="product_currency" type="string">
Moeda opcional para o preço do produto. Se não especificada, o padrão será a moeda do produto.
</ParamField>

<ParamField body="product_description" type="string">
Substituição opcional da descrição do produto para cobrança e itens da fatura. Se não especificada, será usada a descrição armazenada do produto.
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
Se as taxas de moeda adaptativa devem ser incluídas no preço do produto (true) ou adicionadas por cima (false). Ignorado se o preço adaptativo não estiver habilitado.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## Exemplos de Uso

Aqui estão 10 exemplos abrangentes mostrando diferentes configurações de sessões de checkout para vários cenários comerciais:

### 1. Checkout Simples de Produto Único

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. Carrinho com Múltiplos Produtos

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. Assinatura com Período de Teste

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. Checkout Pré-confirmado

<Note>
Quando `confirm` estiver definido como `true`, o cliente será levado diretamente à página de checkout, ignorando quaisquer etapas de confirmação.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. Checkout com Substituição de Moeda

<Note>
A substituição `billing_currency` só entra em vigor quando a moeda adaptativa está habilitada nas configurações da sua conta. Se a moeda adaptativa estiver desativada, este parâmetro não terá efeito.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. Métodos de Pagamento Salvos para Clientes Retornantes

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. Checkout B2B com Coleta de CPF/CNPJ

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. Checkout com Tema Escuro e Código de Desconto

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. Métodos de Pagamento Regionais (UPI para Índia)

Para informações detalhadas sobre configuração e testes de UPI, veja a página <a href="/features/payment-methods/india">India Payment Methods</a>.


```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. Checkout BNPL (Compre Agora Pague Depois)

Para informações detalhadas sobre configuração e testes de BNPL, veja a página <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a>.


```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. Usando Métodos de Pagamento Existentes para Checkout Instantâneo

Use o método de pagamento salvo do cliente para criar uma sessão de checkout que processe imediatamente, ignorando a coleta do método de pagamento:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
Ao usar `payment_method_id`, `confirm` deve estar definido como `true` e um `customer_id` existente deve ser fornecido. O método de pagamento será validado quanto à elegibilidade com a moeda do pagamento.
</Warning>

<Info>
O método de pagamento deve pertencer ao cliente e ser compatível com a moeda do pagamento. Isso permite compras com um clique para clientes retornantes.
</Info>

### 12. Links Curtos para URLs de Pagamento Mais Limpos

Gere links de pagamento encurtados e compartilháveis com slugs personalizados:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
Links curtos são perfeitos para SMS, e-mail ou compartilhamento em redes sociais. São mais fáceis de lembrar e inspiram mais confiança do que URLs longas.
</Tip>

### 13. Ignorar a Página de Sucesso e Redirecionar Imediatamente

Redirecione os clientes imediatamente após a conclusão do pagamento, ignorando a página de sucesso padrão:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
Use `redirect_immediately: true` quando você tiver uma página de sucesso personalizada que ofereça melhor experiência do usuário do que a página padrão de sucesso do pagamento. Isso é especialmente útil para aplicativos móveis e fluxos de checkout incorporados.
</Tip>

<Note>
Quando `redirect_immediately` estiver habilitado, os clientes serão redirecionados para o seu `return_url` imediatamente após a conclusão do pagamento, ignorando completamente a página de sucesso padrão.
</Note>

### 14. Forçar um Idioma

Force o checkout a exibir em um idioma específico, substituindo a detecção de idioma do navegador do cliente:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
Use `force_language` quando você souber o idioma preferido do seu cliente (por exemplo, pelas configurações da conta) ou quando estiver segmentando mercados regionais específicos.
</Tip>

**Idiomas suportados:** Árabe (`ar`), Catalão (`ca`), Chinês (`zh`), Holandês (`nl`), Inglês (`en`), Francês (`fr`), Alemão (`de`), Hebraico (`he`), Indonésio (`id`), Italiano (`it`), Japonês (`ja`), Coreano (`ko`), Malaio (`ms`), Polonês (`pl`), Português (`pt`), Romeno (`ro`), Russo (`ru`), Espanhol (`es`), Sueco (`sv`), Tailandês (`th`), Turco (`tr`)

### 15. Coleta de Campos Personalizados

Colete informações adicionais dos clientes durante o checkout usando campos personalizados:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
As respostas dos campos personalizados são automaticamente incluídas nas cargas úteis de webhook (`payment.succeeded`, `subscription.created`, etc.) e podem ser recuperadas via API. Use-as para enriquecer seu CRM, acionar fluxos de onboarding ou personalizar a experiência do cliente.
</Info>

**Tipos de campo disponíveis:** `text`, `number`, `email`, `url`, `date`, `dropdown`, `boolean`

## Visualizando Sessões de Checkout

Antes de criar uma sessão de checkout, você pode visualizar a divisão de preços incluindo impostos, descontos e totais. Isso é útil para mostrar preços precisos aos clientes antes que avancem para o checkout.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  Veja a documentação completa do endpoint de visualização.
</Card>

## Migrando de Dynamic Links para Sessões de Checkout

### Principais Diferenças
Anteriormente, ao criar um link de pagamento com Dynamic Links, era necessário fornecer o endereço completo de cobrança do cliente.

Com as Sessões de Checkout, isso não é mais necessário. Você pode simplesmente passar as informações de que dispõe e nós cuidamos do resto. Por exemplo:

- Se você só sabe o país de cobrança do cliente, forneça apenas isso.
- O fluxo de checkout coletará automaticamente os dados faltantes antes de levar o cliente à página de pagamento.
- Por outro lado, se você já possui todas as informações necessárias e quer ir direto à página de pagamento, pode enviar o conjunto completo de dados e incluir `confirm=true` no corpo da solicitação.

### Processo de Migração

Migrar de Dynamic Links para Sessões de Checkout é direto:

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
Atualize sua integração para usar o novo método da API ou SDK.
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
Ajuste o payload da solicitação conforme o formato das Sessões de Checkout.
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
Sim. Nenhum tratamento adicional ou etapa especial de migração é necessária da sua parte.
</Step>
</Steps>
