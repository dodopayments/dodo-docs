---
title: DataFast
description: Atributkan pendapatan ke saluran pemasaran dengan analitik
  DataFast. Lacak sumber lalu lintas mana yang mengarahkan pelanggan yang
  membayar dan optimalkan pertumbuhan Anda.
icon: chart-bar
keywords:
  - Dodo Payments
  - Datafast
  - sinkronisasi data
  - analitik
---

## Pendahuluan

DataFast adalah alat analitik yang mengutamakan pendapatan yang membantu Anda menemukan saluran pemasaran mana yang mengarahkan pelanggan yang membayar. Dengan mengintegrasikan Dodo Payments dengan DataFast, Anda dapat mengatributkan pendapatan ke sumber lalu lintas Anda, mengidentifikasi segmen pelanggan bernilai tinggi, dan membuat keputusan berbasis data untuk mengembangkan bisnis Anda.

<Info>
Integrasi ini membutuhkan Kunci API DataFast Anda, yang dapat Anda peroleh dari [dashboard DataFast](https://datafa.st/).
</Info>

## Cara Kerjanya

DataFast melacak pengunjung melalui ID pengunjung unik yang disimpan dalam cookie. Untuk mengatributkan pendapatan ke saluran pemasaran, Anda perlu:

1. **Ambil ID pengunjung DataFast** dari cookie `datafast_visitor_id` saat membuat sesi checkout
2. **Simpan ID pengunjung** di metadata pembayaran Anda
3. **Kirim data pembayaran ke DataFast** saat pembayaran berhasil melalui API Pembayaran mereka

Ini memungkinkan DataFast untuk mencocokkan pembayaran yang berhasil dengan sumber lalu lintas asli, memberikan Anda atribusi pendapatan yang lengkap.

## Memulai

<Steps>
<Step title="Install DataFast Script">
  Pertama, pasang skrip pelacakan DataFast di situs web Anda. Ini akan membuat cookie `datafast_visitor_id` yang melacak pengunjung Anda.
  
  Kunjungi [dokumentasi DataFast](https://datafa.st/docs) untuk petunjuk instalasi sesuai platform Anda.
</Step>

<Step title="Get Your API Key">
  Masuk ke [dashboard DataFast](https://datafa.st/) Anda dan buka pengaturan situs web untuk mendapatkan kunci API Anda.
  
  <Warning>
  Jaga kunci API Anda tetap aman dan jangan pernah mengeksposnya di kode sisi klien.
  </Warning>
</Step>

<Step title="Capture Visitor ID in Checkout">
  Saat membuat sesi checkout, ambil ID pengunjung DataFast dari cookie dan tambahkan ke metadata pembayaran Anda.
</Step>

<Step title="Send Payment Data via Webhook">
  Konfigurasikan webhook untuk mengirim data pembayaran ke API Pembayaran DataFast saat pembayaran berhasil.
</Step>

<Step title="Done!">
  🎉 Data pendapatan kini akan muncul di dashboard DataFast Anda dengan atribusi penuh ke saluran pemasaran.
</Step>
</Steps>

## Panduan Implementasi

### Langkah 1: Tambahkan ID Pengunjung ke Metadata Checkout

Saat membuat sesi checkout, tangkap ID pengunjung DataFast dari cookie dan sertakan dalam metadata pembayaran Anda.

<Note>
Contoh-contoh di bawah menggunakan `POST /payments`, yang sudah **deprecated**. Ini masih berfungsi untuk integrasi yang sudah ada, tetapi integrasi baru sebaiknya menggunakan [Checkout Sessions](/developer-resources/checkout-session) (`POST /checkouts`) — `metadata` diteruskan dengan cara yang sama.
</Note>

<CodeGroup>

```typescript Next.js
import { cookies } from 'next/headers';
import { dodopayments } from '@/lib/dodopayments';

export async function createCheckout(productId: string) {
  // Capture DataFast visitor ID from cookie
  const datafastVisitorId = cookies().get('datafast_visitor_id')?.value;

  const payment = await dodopayments.payments.create({
    product_cart: [{ product_id: productId, quantity: 1 }],
    // ... other payment configuration
    metadata: {
      datafast_visitor_id: datafastVisitorId, // Store visitor ID in metadata
    },
  });

  return payment;
}
```

```javascript Express.js
const express = require('express');
const router = express.Router();

router.post('/create-checkout', async (req, res) => {
  // Capture DataFast visitor ID from cookie
  const datafastVisitorId = req.cookies?.datafast_visitor_id;

  const payment = await dodopayments.payments.create({
    product_cart: [{ product_id: req.body.productId, quantity: 1 }],
    // ... other payment configuration
    metadata: {
      datafast_visitor_id: datafastVisitorId, // Store visitor ID in metadata
    },
  });

  res.json({ payment });
});
```

```javascript Other Frameworks
// Capture DataFast visitor ID from cookie
const datafastVisitorId = getCookie('datafast_visitor_id'); // Use your framework's cookie method

const payment = await dodopayments.payments.create({
  product_cart: [{ product_id: productId, quantity: 1 }],
  // ... other payment configuration
  metadata: {
    datafast_visitor_id: datafastVisitorId, // Store visitor ID in metadata
  },
});
```

</CodeGroup>

### Langkah 2: Kirim Data Pembayaran ke DataFast

Konfigurasikan endpoint webhook untuk mengirim data pembayaran ke Payment API DataFast saat pembayaran berhasil.

<Steps>
<Step title="Open the Webhook Section">
  Di dashboard Dodo Payments, buka <b>Webhooks → + Add Endpoint</b>, lalu perluas dropdown integrasi.
  <Frame>
    <img src="/images/integrations/datafast/add.png" alt="Add Endpoint and integrations dropdown" style={{ maxHeight: '500px', width: 'auto' }} />
  </Frame>
</Step>

<Step title="Select DataFast">
  Pilih kartu integrasi <b>DataFast</b>.
</Step>

<Step title="Enter API Key">
  Masukkan DataFast API Key Anda di kolom konfigurasi.
  <Frame>
    <img src="/images/integrations/datafast/api-key.png" alt="Add API Key" style={{ maxHeight: '500px', width: 'auto' }} />
  </Frame>
</Step>

<Step title="Configure Transformation">
  Edit kode transformasi untuk memformat data pembayaran untuk Payment API DataFast.
</Step>

<Step title="Test & Create">
  Uji dengan payload sampel dan klik <b>Create</b> untuk mengaktifkan integrasi.
</Step>
</Steps>

## Contoh Kode Transformasi

### Atribusi Pembayaran Dasar

```javascript basic_payment.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;
    
    // Only send to DataFast if visitor ID exists in metadata
    if (payment.metadata && payment.metadata.datafast_visitor_id) {
      webhook.payload = {
        amount: payment.total_amount / 100, // Convert from cents to dollars
        currency: payment.currency,
        transaction_id: payment.payment_id,
        datafast_visitor_id: payment.metadata.datafast_visitor_id,
      };
    } else {
      // Cancel dispatch if no visitor ID (prevents unnecessary API calls)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

### Menangani Mata Uang Tanpa Desimal

Beberapa mata uang (seperti JPY) tidak menggunakan tempat desimal. Sesuaikan penghitungan jumlahnya sebagaimana mestinya:

```javascript zero_decimal.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;
    
    if (payment.metadata && payment.metadata.datafast_visitor_id) {
      // Zero decimal currencies: JPY, KRW, CLP, etc.
      const zeroDecimalCurrencies = ['JPY', 'KRW', 'CLP', 'VND', 'UGX', 'MGA'];
      const isZeroDecimal = zeroDecimalCurrencies.includes(payment.currency);
      
      webhook.payload = {
        amount: isZeroDecimal 
          ? payment.total_amount // Use amount as-is for zero decimal currencies
          : payment.total_amount / 100, // Convert from cents for other currencies
        currency: payment.currency,
        transaction_id: payment.payment_id,
        datafast_visitor_id: payment.metadata.datafast_visitor_id,
      };
    } else {
      // Cancel dispatch if no visitor ID (prevents unnecessary API calls)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

### Pembayaran Langganan

Untuk pembayaran langganan berulang, Anda dapat melacak setiap pembayaran:

```javascript subscription_payment.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;
    
    // Check if this is a subscription payment
    const isSubscription = payment.subscription_id !== null;
    
    if (payment.metadata && payment.metadata.datafast_visitor_id) {
      webhook.payload = {
        amount: payment.total_amount / 100,
        currency: payment.currency,
        transaction_id: payment.payment_id,
        datafast_visitor_id: payment.metadata.datafast_visitor_id,
        // Optional: Add subscription context
        ...(isSubscription && {
          subscription_id: payment.subscription_id,
        }),
      };
    } else {
      // Cancel dispatch if no visitor ID (prevents unnecessary API calls)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

## Praktik Terbaik

<Tip>
**Catat visitor ID lebih awal**: Simpan visitor ID DataFast sesegera mungkin dalam alur checkout Anda untuk memastikan atribusi yang akurat, meskipun pengguna meninggalkan halaman lalu kembali lagi nanti.
</Tip>

- **Selalu sertakan visitor ID dalam metadata**: Tanpa visitor ID, DataFast tidak dapat mengatribusikan pendapatan ke channel pemasaran
- **Tangani mata uang tanpa desimal**: Beberapa mata uang (JPY, KRW, dan lainnya) tidak menggunakan tempat desimal—sesuaikan penghitungan jumlahnya
- **Uji dengan pembayaran sampel**: Pastikan integrasi berfungsi dengan benar sebelum digunakan di production
- **Pantau dashboard DataFast Anda**: Periksa apakah pembayaran muncul dengan benar dan memiliki atribusi yang sesuai
- **Gunakan webhook retries**: Payment API DataFast bersifat idempotent, sehingga retry aman dilakukan jika webhook gagal

## Pemecahan Masalah

<AccordionGroup>
<Accordion title="Payments not appearing in DataFast">
- Pastikan DataFast API key Anda benar dan aktif
- Periksa apakah `datafast_visitor_id` telah diambil dan disimpan dalam metadata pembayaran
- Pastikan transformasi webhook memformat payload dengan benar
- Pastikan webhook terpicu pada event `payment.succeeded`
- Periksa dashboard DataFast untuk melihat pesan error atau log API
</Accordion>

<Accordion title="Revenue attribution not working">
- Pastikan tracking script DataFast terpasang dan berfungsi di situs web Anda
- Pastikan cookie `datafast_visitor_id` disetel dengan benar
- Periksa apakah visitor ID cocok antara pembuatan checkout dan penyelesaian pembayaran
- Pastikan Anda mengambil visitor ID sebelum membuat checkout session
- Tinjau [dokumentasi Payment API](https://datafa.st/docs/payments-api) DataFast untuk panduan tambahan
</Accordion>

<Accordion title="Transformation errors">
- Validasi bahwa struktur JSON sesuai dengan format Payment API DataFast
- Pastikan semua field yang diperlukan (`amount`, `currency`, `transaction_id`, `datafast_visitor_id`) tersedia
- Pastikan amount dikonversi dengan benar (bagi dengan 100 untuk sebagian besar mata uang, kecuali mata uang tanpa desimal)
- Pastikan URL endpoint API sudah benar: `https://datafa.st/api/v1/payments`
- Uji transformasi dengan payload webhook sampel
</Accordion>

<Accordion title="Currency conversion issues">
- Untuk mata uang tanpa desimal (JPY, KRW, CLP, VND, UGX, MGA), kirim amount apa adanya tanpa membaginya dengan 100
- Untuk semua mata uang lainnya, bagi amount dengan 100 untuk mengonversinya dari sen ke unit dasar
- Periksa kembali apakah kode mata uang sesuai dengan format ISO 4217 (misalnya, "USD", "EUR", "JPY")
</Accordion>
</AccordionGroup>

## Sumber Daya Tambahan

<CardGroup cols={2}>
<Card title="DataFast Documentation" icon="book" href="https://datafa.st/docs/payments-api">
  Pelajari lebih lanjut tentang Payment API DataFast dan fitur atribusi pendapatannya.
</Card>

<Card title="DataFast Dashboard" icon="chart-bar" href="https://datafa.st/">
  Akses dashboard DataFast Anda untuk melihat analitik pendapatan dan data atribusi.
</Card>
</CardGroup>

<Info>
Butuh bantuan? Hubungi dukungan Dodo Payments di [support@dodopayments.com](mailto:support@dodopayments.com) untuk mendapatkan bantuan terkait integrasi.
</Info>
