---
title: Dub
description: Lacak peristiwa konversi penjualan di Dub ketika pelanggan
  menyelesaikan pembelian melalui Dodo Payments.
icon: /images/logos/dub.svg
---

## Pendahuluan

[Dub](https://dub.co) adalah platform manajemen tautan yang kuat yang membantu Anda membuat, membagikan, dan melacak tautan pendek. Dengan mengintegrasikan Dodo Payments dengan Dub, Anda dapat secara otomatis melacak peristiwa konversi penjualan ketika pelanggan menyelesaikan pembelian, memungkinkan Anda untuk mengukur ROI dari kampanye pemasaran dan program rujukan Anda.

Peristiwa "penjualan" dicatat di Dub ketika seorang pelanggan:
- Menyelesaikan pembayaran satu kali
- Berlangganan ke rencana berbayar
- Melakukan pembayaran langganan berulang

<Info>
Integrasi ini memerlukan akun Dub dengan pelacakan konversi diaktifkan pada tautan Anda.
</Info>

<Tip>
**Integrasi Program Afiliasi**: Integrasi ini juga bekerja mulus dengan **Dub Partners** untuk melacak referal dan komisi afiliasi. Gunakan pelacakan konversi Dub untuk mengatribusikan penjualan ke tautan afiliasi Anda dan mengukur kinerja mitra. Pelajari lebih lanjut tentang pengaturan program afiliasi di [panduan fitur Afiliasi kami](/features/affiliates).
</Tip>

Dub melacak pengunjung melalui ID klik unik (`dub_id`) yang disimpan dalam cookie ketika pengguna mengklik tautan pendek Dub Anda. Untuk mengaitkan penjualan dengan tautan Anda, Anda perlu:

Dub melacak pengunjung melalui ID klik unik (`dub_id`) yang disimpan dalam cookie saat pengguna mengklik tautan pendek Dub Anda. Untuk mengatribusikan penjualan ke tautan Anda, Anda perlu:

1. **Tangkap ID klik Dub** dari cookie `dub_id` saat membuat sesi checkout
2. **Simpan ID klik** dalam metadata pembayaran Anda bersama dengan ID eksternal pelanggan
3. **Kirim data penjualan ke Dub** saat pembayaran berhasil menggunakan Track API mereka

## Prasyarat

Sebelum mengatur integrasi ini, pastikan Anda memiliki:

1. Akun [Dub](https://dub.co) dengan ruang kerja
2. Pelacakan konversi diaktifkan untuk tautan Anda
3. Kunci API Dub Anda (tersedia di dasbor Dub Anda di bawah Pengaturan → Kunci API)

## Memulai

<Steps>
<Step title="Aktifkan Pelacakan Konversi di Dub">
  Di dasbor Dub Anda, aktifkan pelacakan konversi untuk tautan yang ingin Anda lacak penjualannya. Ini memungkinkan Dub untuk mencatat peristiwa penjualan ketika pelanggan menyelesaikan pembelian.

<Steps>
{/* LOCKED_PATTERN_61bbf97d09c18ff0f49ccd9c0f216ae2 */}
  Di dasbor Dub Anda, aktifkan pelacakan konversi untuk tautan yang ingin Anda lacak penjualannya. Ini memungkinkan Dub mencatat event penjualan ketika pelanggan menyelesaikan pembelian.

  <Info>
  Pelajari lebih lanjut tentang mengaktifkan pelacakan konversi di [dokumentasi Dub](https://dub.co/docs/conversions/quickstart).
  </Info>
</Step>

{/* LOCKED_PATTERN_09ec83561863ba8dfb1b09650875a82a */}
  Arahkan ke [dasbor Dub Anda](https://app.dub.co) → Settings → API Keys dan buat kunci API baru dengan lingkup `conversions.write`.


  <Warning>
  Jaga kunci API Anda tetap aman dan jangan pernah mengeksposnya di kode sisi klien.
  </Warning>
</Step>

{/* LOCKED_PATTERN_528142ce64ee996154bcf8fdaffc120b */}
  Saat membuat sesi checkout, tangkap ID klik Dub dari cookie dan tambahkan ke metadata pembayaran Anda.
</Step>

{/* LOCKED_PATTERN_6e8a18d6b9667ee12eda72dd5a47db36 */}
  Konfigurasikan webhook untuk mengirim data penjualan ke Track API Dub saat pembayaran berhasil.
</Step>

{/* LOCKED_PATTERN_8d2cbcb809e6363cb1f41f0f62f98444 */}
  Event konversi penjualan sekarang akan muncul di dasbor analitik Dub Anda dengan atribusi penuh ke tautan Anda.
</Step>
</Steps>

### Langkah 1: Tambahkan ID Klik dan ID Pelanggan ke Metadata Checkout

Saat membuat sesi checkout, tangkap ID klik Dub dari cookie dan sertakan dalam metadata pembayaran Anda bersama dengan ID eksternal pelanggan Anda.

<CodeGroup>

<CodeGroup>

```typescript Next.js
import { cookies } from 'next/headers';
import DodoPayments from 'dodopayments';

const client = new DodoPayments();

export async function createCheckout(productId: string, customerId: string) {
  // Capture Dub click ID from cookie
  const dubClickId = cookies().get('dub_id')?.value;

  const payment = await client.payments.create({
    billing: {
      city: 'New York',
      country: 'US',
      state: 'NY',
      street: '123 Main St',
      zipcode: '10001',
    },
    customer: {
      email: 'customer@example.com',
      name: 'John Doe',
    },
    product_id: productId,
    metadata: {
      dub_click_id: dubClickId,           // Store Dub click ID
      dub_external_id: customerId,        // Store your customer's unique ID
    },
  });

  return payment;
}
```

```javascript Express.js
const express = require('express');
const DodoPayments = require('dodopayments').default;
const router = express.Router();

const client = new DodoPayments();

router.post('/create-checkout', async (req, res) => {
  // Capture Dub click ID from cookie
  const dubClickId = req.cookies?.dub_id;

  const payment = await client.payments.create({
    billing: {
      city: 'New York',
      country: 'US',
      state: 'NY',
      street: '123 Main St',
      zipcode: '10001',
    },
    customer: {
      email: req.body.email,
      name: req.body.name,
    },
    product_id: req.body.productId,
    metadata: {
      dub_click_id: dubClickId,           // Store Dub click ID
      dub_external_id: req.body.customerId, // Store your customer's unique ID
    },
  });

  res.json({ payment });
});
```

```python Python (FastAPI)
from fastapi import FastAPI, Request
from dodopayments import DodoPayments

app = FastAPI()
client = DodoPayments()

@app.post("/create-checkout")
async def create_checkout(request: Request):
    # Capture Dub click ID from cookie
    dub_click_id = request.cookies.get("dub_id")

    body = await request.json()

    payment = client.payments.create(
        billing={
            "city": "New York",
            "country": "US",
            "state": "NY",
            "street": "123 Main St",
            "zipcode": "10001",
        },
        customer={
            "email": body["email"],
            "name": body["name"],
        },
        product_id=body["product_id"],
        metadata={
            "dub_click_id": dub_click_id,          # Store Dub click ID
            "dub_external_id": body["customer_id"], # Store your customer's unique ID
        },
    )

    return {"payment": payment}
```

```go Go
package main

import (
    "github.com/dodopayments/dodopayments-go"
)

func createCheckout(dubClickId, customerId, productId string) (*dodopayments.Payment, error) {
    client := dodopayments.NewClient()

    payment, err := client.Payments.Create(context.Background(), dodopayments.PaymentCreateParams{
        Billing: dodopayments.BillingParam{
            City:    "New York",
            Country: "US",
            State:   "NY",
            Street:  "123 Main St",
            Zipcode: "10001",
        },
        Customer: dodopayments.CustomerParam{
            Email: "customer@example.com",
            Name:  "John Doe",
        },
        ProductID: productId,
        Metadata: map[string]string{
            "dub_click_id":   dubClickId,  // Store Dub click ID
            "dub_external_id": customerId, // Store your customer's unique ID
        },
    })

    return payment, err
}
```

</CodeGroup>

Konfigurasikan endpoint webhook untuk mengirim data penjualan ke API Pelacakan Dub ketika pembayaran berhasil.

<Steps>
<Step title="Buka Bagian Webhook">
  Di dasbor Dodo Payments Anda, navigasikan ke **Webhooks → + Tambah Endpoint** dan perluas dropdown integrasi.
  <Frame>
    <img src="/images/integrations/dub/add.png" alt="Tambahkan Endpoint dan dropdown integrasi" />
  </Frame>
</Step>

<Steps>
{/* LOCKED_PATTERN_c337a250e2e6db475d2eb18e95cb3e6a */}
  Di dasbor Dodo Payments Anda, buka **Webhooks → + Add Endpoint** dan perluas dropdown integrasi.
  <Frame>
    <img src="/images/integrations/dub/add.png" alt="Add Endpoint and integrations dropdown" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_99685f3488b80b5b23189beda3a369d0 */}
  Pilih kartu integrasi **Dub**.
</Step>

{/* LOCKED_PATTERN_b0cbf67cc15e78743c0724f27d5e90ef */}
  Masukkan Kunci API Dub Anda di bidang konfigurasi.
  <Frame>
    <img src="/images/integrations/dub/api-key.png" alt="Add API Key" />
  </Frame>
</Step>

{/* LOCKED_PATTERN_4ac4616f59ca1d74262754150410c05f */}
  Edit kode transformasi untuk memformat data pembayaran ke Track Sale API Dub.
</Step>

{/* LOCKED_PATTERN_f6e4d766e7306e52042eed3b757944bb */}
  Uji dengan payload contoh dan klik **Create** untuk mengaktifkan integrasi.
</Step>
</Steps>

### Pelacakan Penjualan Dasar

Lacak penjualan ketika pembayaran berhasil:

```javascript basic_sale.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    // Only send to Dub if click ID exists in metadata
    if (payment.metadata && payment.metadata.dub_click_id) {
      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount, // Ensure the amount is in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          customer_email: payment.customer.email,
          customer_name: payment.customer.name,
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

```javascript basic_sale.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    // Only send to Dub if click ID exists in metadata
    if (payment.metadata && payment.metadata.dub_click_id) {
      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount, // Ensure the amount is in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          customer_email: payment.customer.email,
          customer_name: payment.customer.name,
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

Lacak baik langganan awal maupun pembayaran berulang:

```javascript subscription_sale.js icon="js" expandable
function handler(webhook) {
  const data = webhook.payload.data;

  // Track initial subscription activation
  if (webhook.eventType === "subscription.active") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount, // Amount in cents
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: data.subscription_id,
        eventName: "Subscription Started",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          billing_interval: data.payment_frequency_interval,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  // Track recurring subscription payments
  if (webhook.eventType === "subscription.renewed") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount,
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: `${data.subscription_id}_${Date.now()}`,
        eventName: "Subscription Renewed",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  return webhook;
}
```

```javascript subscription_sale.js icon="js" expandable
function handler(webhook) {
  const data = webhook.payload.data;

  // Track initial subscription activation
  if (webhook.eventType === "subscription.active") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount, // Amount in cents
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: data.subscription_id,
        eventName: "Subscription Started",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          billing_interval: data.payment_frequency_interval,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  // Track recurring subscription payments
  if (webhook.eventType === "subscription.renewed") {
    if (data.metadata && data.metadata.dub_click_id) {
      webhook.payload = {
        clickId: data.metadata.dub_click_id,
        externalId: data.metadata.dub_external_id || data.customer.customer_id,
        amount: data.recurring_pre_tax_amount,
        currency: data.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: `${data.subscription_id}_${Date.now()}`,
        eventName: "Subscription Renewed",
        metadata: {
          subscription_id: data.subscription_id,
          product_id: data.product_id,
          customer_email: data.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }

  return webhook;
}
```

Kirim hanya jumlah sebelum pajak ke Dub untuk pelacakan pendapatan yang akurat:

```javascript sale_without_tax.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Calculate pre-tax amount (total minus tax)
      const preTaxAmount = payment.total_amount - (payment.tax || 0);

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: preTaxAmount, // Pre-tax amount in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          total_amount: payment.total_amount,
          tax_amount: payment.tax || 0,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

```javascript sale_without_tax.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Calculate pre-tax amount (total minus tax)
      const preTaxAmount = payment.total_amount - (payment.tax || 0);

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: preTaxAmount, // Pre-tax amount in cents
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        metadata: {
          total_amount: payment.total_amount,
          tax_amount: payment.tax || 0,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

Gunakan nama acara kustom untuk mengkategorikan berbagai jenis penjualan:

```javascript custom_events.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Determine event name based on payment type
      let eventName = "Purchase";
      if (payment.subscription_id) {
        eventName = "Subscription Purchase";
      } else if (payment.metadata && payment.metadata.is_upgrade) {
        eventName = "Plan Upgrade";
      }

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount,
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        eventName: eventName,
        metadata: {
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

```javascript custom_events.js icon="js" expandable
function handler(webhook) {
  if (webhook.eventType === "payment.succeeded") {
    const payment = webhook.payload.data;

    if (payment.metadata && payment.metadata.dub_click_id) {
      // Determine event name based on payment type
      let eventName = "Purchase";
      if (payment.subscription_id) {
        eventName = "Subscription Purchase";
      } else if (payment.metadata && payment.metadata.is_upgrade) {
        eventName = "Plan Upgrade";
      }

      webhook.payload = {
        clickId: payment.metadata.dub_click_id,
        externalId: payment.metadata.dub_external_id || payment.customer.customer_id,
        amount: payment.total_amount,
        currency: payment.currency || "USD",
        paymentProcessor: "dodo",
        invoiceId: payment.payment_id,
        eventName: eventName,
        metadata: {
          product_id: payment.product_cart ? payment.product_cart.map(product => product.product_id).join(', ') : undefined,
          customer_email: payment.customer.email,
        },
      };
    } else {
      // Cancel dispatch if no click ID (organic traffic)
      webhook.cancel = true;
    }
  }
  return webhook;
}
```

Jika Anda lebih suka melacak penjualan dari server Anda daripada menggunakan webhook, Anda dapat memanggil API Pelacakan Dub secara langsung setelah pembayaran berhasil:

<CodeGroup>

<CodeGroup>

```typescript Next.js (Server Action)
'use server';

import { Dub } from 'dub';

const dub = new Dub();

export async function trackSale(
  paymentId: string,
  clickId: string,
  customerId: string,
  amount: number,
  currency: string
) {
  await dub.track.sale({
    clickId: clickId,
    externalId: customerId,
    amount: amount,
    currency: currency,
    paymentProcessor: 'dodo',
    invoiceId: paymentId,
  });
}
```

```javascript Node.js
const { Dub } = require('dub');

const dub = new Dub();

async function trackSale(payment) {
  if (payment.metadata?.dub_click_id) {
    await dub.track.sale({
      clickId: payment.metadata.dub_click_id,
      externalId: payment.metadata.dub_external_id,
      amount: payment.total_amount,
      currency: payment.currency,
      paymentProcessor: 'dodo',
      invoiceId: payment.payment_id,
    });
  }
}
```

</CodeGroup>

<Tip>
**Tangkap ID klik lebih awal**: Simpan ID klik Dub sesegera mungkin dalam alur checkout Anda untuk memastikan atribusi yang akurat, bahkan jika pengguna berpindah dan kembali nanti.
</Tip>

<Tip>
**Tangkap ID klik sedini mungkin**: Simpan ID klik Dub sesegera mungkin dalam alur checkout Anda untuk memastikan atribusi yang akurat, bahkan jika pengguna berpindah halaman dan kembali nanti.
</Tip>

- **Selalu sertakan ID klik dalam metadata**: Tanpa ID klik, Dub tidak dapat mengatribusikan pendapatan ke tautan Anda
- **Gunakan ID eksternal secara konsisten**: Kirimkan ID pelanggan yang sama seperti yang Anda gunakan di sistem Anda untuk analitik tingkat pelanggan yang akurat
- **Kelola lalu lintas organik dengan baik**: Set `webhook.cancel = true` saat tidak ada ID klik untuk menghindari panggilan API yang tidak perlu
- **Uji dengan pembayaran contoh**: Verifikasi bahwa integrasi berfungsi dengan benar sebelum aktif
- **Pantau dasbor Dub Anda**: Periksa bahwa penjualan muncul dengan atribusi yang tepat

<Note>
- **Format jumlah**: Dub mengharapkan jumlah dalam sen (misalnya, $10.00 = 1000)
- **Mata uang**: Gunakan kode mata uang ISO 4217 (USD, EUR, GBP, dll.)
- **Uji coba gratis**: Pembayaran $0 tidak dilacak sebagai penjualan
- **Pengembalian dana**: Pertimbangkan untuk melacak pengembalian dana secara terpisah jika diperlukan untuk pelaporan pendapatan yang akurat
</Note>

<Note>
- **Format jumlah**: Dub mengharapkan jumlah dalam sen (misalnya, $10.00 = 1000)
- **Mata uang**: Gunakan kode mata uang ISO 4217 (USD, EUR, GBP, dll.)
- **Uji coba gratis**: Pembayaran $0 tidak dilacak sebagai penjualan
- **Pengembalian dana**: Pertimbangkan melacak pengembalian dana secara terpisah jika diperlukan untuk pelaporan pendapatan yang akurat
</Note>

<AccordionGroup>
<Accordion title="Penjualan tidak muncul di Dub">
- Verifikasi bahwa kunci API Dub Anda benar dan memiliki cakupan `conversions.write`
- Periksa bahwa `dub_click_id` ditangkap dan disimpan dalam metadata pembayaran
- Pastikan transformasi webhook memformat payload dengan benar
- Verifikasi bahwa webhook dipicu pada peristiwa `payment.succeeded`
- Konfirmasi bahwa pelacakan konversi diaktifkan untuk tautan Dub Anda
</Accordion>

<AccordionGroup>
{/* LOCKED_PATTERN_366b5018c3ba3b70a06fdb2ce6f29df0 */}
- Verifikasi bahwa kunci API Dub Anda benar dan memiliki lingkup `conversions.write`
- Periksa apakah `dub_click_id` ditangkap dan disimpan dalam metadata pembayaran
- Pastikan transformasi webhook memformat payload dengan benar
- Verifikasi webhook dipicu pada event `payment.succeeded`
- Konfirmasikan pelacakan konversi diaktifkan untuk tautan Dub Anda
</Accordion>

{/* LOCKED_PATTERN_a32709eeb5a64477cb4e009bd30bfdd2 */}
- Konfirmasikan pengguna mengklik tautan pendek Dub Anda sebelum checkout
- Verifikasi cookie `dub_id` disetel dengan benar di domain Anda
- Periksa bahwa ID klik cocok antara pembuatan checkout dan penyelesaian pembayaran
- Pastikan Anda menangkap ID klik sebelum membuat sesi checkout
</Accordion>

{/* LOCKED_PATTERN_14f1ac2eb6bb156b1fd74792fba48a19 */}
- Validasi struktur JSON sesuai format Track Sale API Dub
- Pastikan semua field yang diperlukan (`clickId`, `externalId`, `amount`) hadir
- Pastikan jumlah dalam sen (integer, bukan desimal)
- Verifikasi URL endpoint API benar: `https://api.dub.co/track/sale`
- Uji transformasi dengan payload webhook contoh
</Accordion>

{/* LOCKED_PATTERN_5f0ac54eda91f1b5f2683659cb5c19a5 */}
- Pastikan Anda hanya melacak pada event `payment.succeeded`, bukan `payment.processing`
- Gunakan nilai unik `invoiceId` untuk setiap penjualan
- Untuk langganan, tambahkan cap waktu atau periode penagihan untuk mencegah duplikasi pada pembaruan otomatis
</Accordion>
</AccordionGroup>

<CardGroup cols={2}>
<Card title="Dokumentasi Konversi Dub" icon="book" href="https://dub.co/docs/conversions/quickstart">
  Pelajari lebih lanjut tentang pelacakan konversi dan fitur analitik Dub.
</Card>

<CardGroup cols={2}>
{/* LOCKED_PATTERN_b26c5b8ead1d8d61de894caed271203c */}
  Pelajari lebih lanjut tentang pelacakan konversi dan fitur analitik Dub.
</Card>

{/* LOCKED_PATTERN_23a04bddc043aa9181423af8be41ab2c */}
  Lihat referensi API lengkap untuk endpoint Track Sale Dub.
</Card>

{/* LOCKED_PATTERN_8b5d6bd2d637fa74100d10bc01898c35 */}
  Akses dasbor Dub Anda untuk melihat analitik konversi dan data atribusi.
</Card>

{/* LOCKED_PATTERN_08edaa2b4bf5c1db98d7c617dc2cf851 */}
  Pelajari semua event webhook Dodo Payments yang tersedia.
</Card>
</CardGroup>

<Info>
Butuh bantuan? Hubungi dukungan Dodo Payments di [support@dodopayments.com](mailto:support@dodopayments.com) untuk bantuan integrasi.
</Info>
