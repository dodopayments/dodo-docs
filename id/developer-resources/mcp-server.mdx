---
title: MCP Server
description: Aktifkan asisten AI untuk berinteraksi dengan Dodo Payments API
  melalui Model Context Protocol (MCP) untuk operasi pembayaran yang mulus,
  manajemen langganan, dan interaksi pelanggan.
icon: /images/logos/mcp.svg
---

## Ikhtisar

Model Context Protocol (MCP) adalah standar terbuka yang memungkinkan aplikasi AI untuk terhubung dengan aman ke sumber data dan API eksternal. Dodo Payments MCP Server menyediakan akses terstruktur ke infrastruktur pembayaran Anda bagi asisten AI seperti Claude, Cursor, dan klien lain yang kompatibel dengan MCP.

Server MCP Dodo Payments dibangun dengan [Stainless](https://www.stainless.com/) dan menggunakan arsitektur **Code Mode**, yang memungkinkan agen AI untuk menulis dan mengeksekusi kode TypeScript terhadap SDK Dodo Payments dalam lingkungan sandbox yang terisolasi.

### Kemampuan Utama

- **Operasi Pembayaran**: Membuat, mengambil, dan mengelola pembayaran dan pengembalian dana
- **Manajemen Langganan**: Menangani penagihan berulang, peningkatan, dan pembatalan
- **Administrasi Pelanggan**: Mengelola data pelanggan dan akses portal
- **Katalog Produk**: Membuat dan memperbarui produk, harga, dan diskon
- **Manajemen Lisensi**: Mengaktifkan, memvalidasi, dan mengelola lisensi perangkat lunak
- **Penagihan Berdasarkan Penggunaan**: Melacak dan menagih penggunaan yang terukur

## Cara Kerja Code Mode

Server MCP Dodo Payments menggunakan Code Mode, memberikan agen AI Anda dua alat yang kuat:

1. **Alat Pencarian Dokumen**: Mengajukan pertanyaan tentang dokumentasi API dan SDK Dodo Payments untuk memahami operasi dan parameter yang tersedia
2. **Alat Eksekusi Kode**: Menulis kode TypeScript terhadap SDK yang dieksekusi dalam lingkungan sandbox yang aman tanpa akses web atau sistem file

Arsitektur ini memungkinkan agen AI untuk melakukan operasi pembayaran yang kompleks dan multi-langkah secara deterministik dan berulang.

<Tip>
Dengan menggunakan Code Mode, agen dapat menghubungkan beberapa panggilan API, menangani logika bersyarat, dan melakukan perhitungan—semua dalam satu pemanggilan alat.
</Tip>

## Instalasi

Pilih metode instalasi yang paling sesuai dengan alur kerja Anda.

### Server MCP Jarak Jauh (Direkomendasikan)

Akses server MCP yang dihosting tanpa pengaturan atau instalasi lokal. Ini adalah cara tercepat untuk memulai.

<Frame>
  <iframe 
      className="w-full aspect-video rounded-md" 
      src="https://www.youtube.com/embed/TxGgYxNmkT4"
      title="Menggunakan Server MCP Jarak Jauh Dodo Payment"
      frameBorder="0"
      allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
      allowFullScreen
  ></iframe>
</Frame>

<Steps>
<Step title="Akses server jarak jauh">
    Navigasikan ke [https://mcp.dodopayments.com](https://mcp.dodopayments.com) di browser Anda.
</Step>

<Step title="Konfigurasi klien MCP Anda">
    Salin konfigurasi JSON yang disediakan untuk klien spesifik Anda. Untuk Cursor atau Claude Desktop, tambahkan ini ke pengaturan MCP Anda:
    

    ```json
    {
      "mcpServers": {
        "dodopayments_api": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

</Step>

<Step title="Autentikasi dan konfigurasi">
    Alur OAuth akan meminta Anda untuk:
    - Masukkan kunci API Dodo Payments Anda
    - Pilih lingkungan Anda (uji coba atau langsung)
    - Pilih jenis klien MCP Anda
    
    <Warning>
    Jaga agar kunci API Anda tetap aman. Gunakan kunci mode uji selama pengembangan.
    </Warning>
</Step>

<Step title="Selesaikan pengaturan">
    Klik **Login dan Setujui** untuk mengotorisasi koneksi.
    
    <Check>
    Setelah terhubung, asisten AI Anda dapat berinteraksi dengan API Dodo Payments atas nama Anda.
    </Check>
</Step>
</Steps>

### Paket NPM

Instal dan jalankan server MCP secara lokal menggunakan NPM.

<Tabs>
<Tab title="NPX (Tanpa Instalasi)">

```bash
# Set your environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."
export DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key"
export DODO_PAYMENTS_ENVIRONMENT="live_mode"

# Run the latest version
npx -y dodopayments-mcp@latest
```

</Tab>

<Tab title="Konfigurasi Klien MCP">

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key",
        "DODO_PAYMENTS_ENVIRONMENT": "live_mode"
      }
    }
  }
}
```

</Tab>
</Tabs>

<Tip>
Gunakan `@latest` untuk selalu menarik versi terbaru, atau kunci ke versi tertentu seperti `@1.0.0` untuk stabilitas.
</Tip>

### Docker

Jalankan server MCP dalam lingkungan terkontainer untuk penyebaran yang konsisten.

<CodeGroup>

```bash Docker
# Pull the latest image
docker pull ghcr.io/dodopayments/mcp:latest

# Run the container
docker run -e DODO_PAYMENTS_API_KEY="dodo_test_..." \
  -e DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -e DODO_PAYMENTS_ENVIRONMENT="live_mode" \
  ghcr.io/dodopayments/mcp:latest
```

```yaml Docker Compose
services:
  dodopayments-mcp:
    image: ghcr.io/dodopayments/mcp:latest
    environment:
      - DODO_PAYMENTS_API_KEY=dodo_test_...
      - DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_key
      - DODO_PAYMENTS_ENVIRONMENT=live_mode
    restart: unless-stopped
```

</CodeGroup>

<Note>
Gambar Docker tersedia di [GitHub Container Registry](https://github.com/orgs/dodopayments/packages/container/package/mcp).
</Note>

## Konfigurasi Klien

Konfigurasikan server MCP Dodo Payments di klien AI pilihan Anda.

<Tabs>
<Tab title="Cursor">
Atur server MCP Dodo Payments di Cursor untuk memungkinkan akses percakapan ke data pembayaran Anda.

**Instalasi Satu Klik**

Gunakan tombol di bawah ini untuk menginstal server MCP langsung di Cursor:

[

![Tambahkan ke Cursor](https://cursor.com/deeplink/mcp-install-dark.svg)

](https://cursor.com/en-US/install-mcp?name=dodopayments-mcp&config=)

Setelah mengklik, atur variabel lingkungan Anda di `mcp.json` melalui **Pengaturan Cursor** > **Alat & MCP** > **Server MCP Baru**.

**Konfigurasi Manual**

<Steps>
<Step title="Buka pengaturan Cursor">
    Navigasikan ke **Pengaturan Cursor** > **Fitur** > **Model Context Protocol** atau tekan `Cmd/Ctrl + Shift + P` dan cari "Pengaturan MCP".
</Step>

<Step title="Edit konfigurasi">
    Klik **Edit Config** untuk membuka file `mcp_config.json`.
</Step>

<Step title="Tambahkan konfigurasi Dodo Payments">
    Pilih salah satu dari konfigurasi berikut:
    
    **Server Jarak Jauh (Direkomendasikan)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **Local NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

<Step title="Simpan dan restart">
    Simpan file konfigurasi dan restart Cursor.
    
    <Check>
    Verifikasi koneksi dengan menanyakan asisten AI tentang data Dodo Payments Anda.
    </Check>
</Step>
</Steps>
</Tab>

<Tab title="Claude Desktop">
Atur server MCP Dodo Payments di Claude Desktop untuk memungkinkan akses percakapan ke data pembayaran Anda.
<Steps>
<Step title="Temukan file konfigurasi">
    Temukan file konfigurasi Claude Desktop:
    
    - **macOS**: `~/Library/Application Support/Claude/claude_desktop_config.json`
    - **Windows**: `%APPDATA%\Claude\claude_desktop_config.json`
    - **Linux**: `~/.config/Claude/claude_desktop_config.json`
</Step>

<Step title="Edit konfigurasi">
    Buka file di editor teks dan tambahkan konfigurasi Dodo Payments:
    
    **Server Jarak Jauh (Direkomendasikan)**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
        }
      }
    }
    ```

    
    **Local NPX**

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

<Step title="Restart Claude Desktop">
    Tutup dan buka kembali Claude Desktop agar perubahan berlaku.
    
    <Tip>
    Cari ikon 🔌 di Claude Desktop untuk mengonfirmasi bahwa server MCP terhubung.
    </Tip>
</Step>
</Steps>
</Tab>

<Tab title="Claude Code">
Atur server MCP Dodo Payments di CLI Claude Code.

Jalankan perintah berikut di terminal Anda:

```bash
claude mcp add --transport stdio dodopayments_api \
  --env DODO_PAYMENTS_API_KEY="dodo_test_..." \
  DODO_PAYMENTS_WEBHOOK_KEY="your_webhook_key" \
  -- npx -y dodopayments-mcp
```

Konfigurasi akan disimpan di file `.claude.json` di direktori home Anda.

<Tip>
Claude Code secara otomatis mengambil variabel lingkungan dari shell Anda, jadi Anda juga dapat mengekspornya sebelum menjalankan perintah.
</Tip>
</Tab>

<Tab title="VS Code">
Atur server MCP Dodo Payments di VS Code.

**Instalasi Satu Klik**

[Buka VS Code](https://vscode.stainless.com/mcp/%7B%22name%22%3A%22dodopayments-mcp%22%2C%22command%22%3A%22npx%22%2C%22args%22%3A%5B%22-y%22%2C%22dodopayments-mcp%22%5D%2C%22env%22%3A%7B%22DODO_PAYMENTS_API_KEY%22%3A%22Set%20your%20DODO_PAYMENTS_API_KEY%20here.%22%2C%22DODO_PAYMENTS_WEBHOOK_KEY%22%3A%22Set%20your%20DODO_PAYMENTS_WEBHOOK_KEY%20here.%22%7D%7D)

Setelah mengklik, atur variabel lingkungan Anda di `mcp.json` melalui **Command Palette** > **MCP: Buka Konfigurasi Pengguna**.

**Konfigurasi Manual**

<Steps>
<Step title="Buka konfigurasi MCP">
    Buka Command Palette (`Cmd/Ctrl + Shift + P`) dan cari "MCP: Buka Konfigurasi Pengguna".
</Step>

<Step title="Tambahkan konfigurasi server">
    Tambahkan server MCP Dodo Payments:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

<Step title="Muat ulang jendela">
    Muat ulang VS Code (`Cmd/Ctrl + Shift + P` > "Muat Ulang Jendela") untuk mengaktifkan server MCP.
</Step>
</Steps>
</Tab>

<Tab title="Cline (VS Code)">
Atur server MCP Dodo Payments di Cline (VS Code) untuk memungkinkan akses percakapan ke data pembayaran Anda.
<Steps>
<Step title="Buka pengaturan Cline">
    Temukan file konfigurasi Cline:
    
    - **macOS**: `~/Library/Application Support/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
    - **Windows**: `%APPDATA%\Code\User\globalStorage\saoudrizwan.claude-dev\settings\cline_mcp_settings.json`
    - **Linux**: `~/.config/Code/User/globalStorage/saoudrizwan.claude-dev/settings/cline_mcp_settings.json`
</Step>

<Step title="Konfigurasi server MCP">
    Tambahkan konfigurasi server MCP Dodo Payments:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

<Step title="Muat ulang VS Code">
    Muat ulang jendela VS Code (`Cmd/Ctrl + Shift + P` > "Muat Ulang Jendela") untuk mengaktifkan koneksi server MCP.
</Step>
</Steps>
</Tab>

<Tab title="Zed">
Atur server MCP Dodo Payments di Zed untuk memungkinkan akses percakapan ke data pembayaran Anda.
<Steps>
<Step title="Buka pengaturan Zed">
    Navigasikan ke **Pengaturan** > **Asisten** > **Model Context Protocol**.
</Step>

<Step title="Tambahkan konfigurasi server">
    Tambahkan konfigurasi berikut:
    

    ```json
    {
      "mcpServers": {
        "dodopayments": {
          "command": "npx",
          "args": ["-y", "dodopayments-mcp@latest"],
          "env": {
            "DODO_PAYMENTS_API_KEY": "dodo_test_...",
            "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
          }
        }
      }
    }
    ```

</Step>

<Step title="Simpan dan verifikasi">
    Simpan konfigurasi. Server MCP sekarang harus tersedia di asisten AI Zed.
</Step>
</Steps>
</Tab>

<Tab title="Klien Lain">
Atur server MCP Dodo Payments di klien lain yang kompatibel dengan MCP.

**Server Jarak Jauh (Direkomendasikan)**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "mcp-remote@latest", "https://mcp.dodopayments.com/sse"]
    }
  }
}
```

**Local NPX**

```json
{
  "mcpServers": {
    "dodopayments": {
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "your_webhook_key"
      }
    }
  }
}
```

<Info>
Kunjungi [modelcontextprotocol.io/clients](https://modelcontextprotocol.io/clients) untuk daftar lengkap klien yang kompatibel dengan MCP dan metode konfigurasinya.
</Info>
</Tab>
</Tabs>

## Variabel Lingkungan

Konfigurasikan perilaku server MCP menggunakan variabel lingkungan.

| Variabel | Deskripsi | Diperlukan |
|----------|-------------|----------|
| `DODO_PAYMENTS_API_KEY` | Kunci API Dodo Payments Anda | Ya |
| `DODO_PAYMENTS_WEBHOOK_KEY` | Kunci tanda tangan webhook Anda | Tidak |
| `DODO_PAYMENTS_ENVIRONMENT` | Atur ke `live_mode` untuk produksi | Tidak |

## Menjalankan Secara Jarak Jauh

Terapkan server MCP sebagai server HTTP jarak jauh untuk klien berbasis web atau alur kerja agentic.

```bash
# Start as remote server
npx -y dodopayments-mcp --transport=http

# Specify a custom port
npx -y dodopayments-mcp --transport=http --port=3000
```

### Konfigurasi Server Jarak Jauh

Setelah diterapkan, klien dapat terhubung menggunakan URL server:

```json
{
  "mcpServers": {
    "dodopayments_api": {
      "url": "http://localhost:3000",
      "headers": {
        "Authorization": "Bearer your_api_key"
      }
    }
  }
}
```

### Header Otorisasi

Server jarak jauh menerima autentikasi melalui header berikut:

| Header | Deskripsi |
|--------|-------------|
| `Authorization` | Autentikasi token Bearer |
| `x-dodo-payments-api-key` | Header kunci API langsung |

## Kemampuan Klien

Klien MCP yang berbeda memiliki tingkat dukungan yang bervariasi untuk spesifikasi MCP. Server dapat secara otomatis menyesuaikan skema alat untuk mengatasi keterbatasan ini.

```bash
# Auto-detect client (default)
npx dodopayments-mcp --client=infer

# Configure for specific clients
npx dodopayments-mcp --client=cursor
npx dodopayments-mcp --client=claude
npx dodopayments-mcp --client=claude-code
npx dodopayments-mcp --client=openai-agents
```

### Preset Klien yang Tersedia

| Klien | Deskripsi |
|--------|-------------|
| `infer` | Deteksi otomatis klien (default) |
| `cursor` | Editor Cursor |
| `claude` | Claude AI web atau desktop |
| `claude-code` | CLI Claude Code |
| `openai-agents` | Platform agen OpenAI |

### Konfigurasi Kemampuan Manual

Untuk klien yang tidak terdaftar di atas, konfigurasikan kemampuan secara manual:

```bash
# Disable $ref pointer support and set max tool name length
npx dodopayments-mcp --no-capability=refs --capability=tool-name-length=40
```

Kemampuan yang tersedia:
- `top-level-unions` - Dukungan untuk tipe union tingkat atas
- `valid-json` - Parsing argumen string JSON yang benar
- `refs` - Dukungan untuk pointer $ref dalam skema
- `unions` - Dukungan untuk tipe union (anyOf)
- `formats` - Dukungan untuk validasi format
- `tool-name-length=N` - Panjang maksimum nama alat

<Tip>
Jalankan `npx dodopayments-mcp --describe-capabilities` untuk informasi kemampuan yang lebih rinci.
</Tip>

## Penyaringan Alat

Kontrol operasi API mana yang diekspos ke asisten AI Anda untuk mengoptimalkan kinerja dan menjaga keamanan.

### Filter berdasarkan jenis operasi

Batasi akses ke operasi baca saja atau operasi tulis.

```bash
# Only expose read operations (GET requests)
npx dodopayments-mcp --operation=read

# Only expose write operations (POST, PUT, DELETE)
npx dodopayments-mcp --operation=write
```

<Tip>
Gunakan `--operation=read` selama pengembangan untuk mencegah modifikasi yang tidak disengaja pada data Anda.
</Tip>

### Filter berdasarkan sumber daya

Batasi alat ke sumber daya API tertentu.

```bash
# Only expose payment-related tools
npx dodopayments-mcp --resource=payments

# Expose multiple resources
npx dodopayments-mcp --resource=payments --resource=customers

# Use wildcards for resource groups
npx dodopayments-mcp --resource=subscription*
```

### Sertakan atau kecualikan alat tertentu

Sesuaikan alat individu mana yang tersedia.

```bash
# Include only specific tools
npx dodopayments-mcp --tool=create_payments --tool=list_payments

# Exclude specific tools
npx dodopayments-mcp --no-tool=delete_products

# Disable the docs search tool
npx dodopayments-mcp --no-tools=docs

# List all available tools
npx dodopayments-mcp --list
```

### Gabungkan filter

Terapkan beberapa filter secara bersamaan untuk kontrol yang tepat.

```bash
# Only payment and subscription read operations
npx dodopayments-mcp --operation=read --resource=payments --resource=subscriptions
```

### Penyaringan Parameter Kuery URL (Server Jarak Jauh)

Saat menggunakan server jarak jauh, terapkan filter melalui parameter kuery URL:

```
https://mcp.dodopayments.com/?operation=read
https://mcp.dodopayments.com/?resource=payments
```

## Mode Alat Dinamis

Untuk API besar, memuat semua alat sekaligus dapat membebani jendela konteks AI. Mode alat dinamis memberikan alternatif yang lebih efisien.

```bash
npx dodopayments-mcp --tools=dynamic
```

Dalam mode dinamis, tiga meta-alat menggantikan alat API individu:

1. **`list_api_endpoints`** - Menemukan metode yang tersedia dengan penyaringan opsional
2. **`get_api_endpoint_schema`** - Mendapatkan skema rinci untuk metode tertentu
3. **`invoke_api_endpoint`** - Menjalankan metode apa pun dengan parameter yang sesuai

<Info>
Alat dinamis secara otomatis diaktifkan untuk API dengan lebih dari 50 metode. Ganti dengan `--tools=all` untuk menyertakan semua alat.
</Info>

```bash
# Use both dynamic tools and specific resources
npx dodopayments-mcp --tools=dynamic --resource=payments
```

## Penggunaan Lanjutan

### Integrasi Programatik

Bangun server MCP kustom atau perluas yang sudah ada secara programatik.

```javascript
import { server, endpoints, init } from "dodopayments-mcp/server";
import { StdioServerTransport } from "@modelcontextprotocol/sdk/server/stdio.js";

// Initialize with all default endpoints
init({ server, endpoints });

// Start the server
const transport = new StdioServerTransport();
await server.connect(transport);
```

### Pengembangan Alat Kustom

Perluas server MCP dengan alat dan logika bisnis Anda sendiri.

```javascript
import { server, init } from "dodopayments-mcp/server";
import createPayments from "dodopayments-mcp/tools/payments/create-payments";
import { zodToJsonSchema } from "zod-to-json-schema";
import { z } from "zod";

// Define a custom tool
const customTool = {
  tool: {
    name: 'calculate_payment_total',
    description: 'Calculate payment total with tax and fees',
    inputSchema: zodToJsonSchema(
      z.object({
        amount: z.number().describe('Base amount in cents'),
        tax_rate: z.number().describe('Tax rate as decimal'),
      })
    ),
  },
  handler: async (client, args) => {
    const total = args.amount * (1 + args.tax_rate);
    return {
      content: [{
        type: 'text',
        text: JSON.stringify({ total, currency: 'usd' }),
      }],
    };
  },
};

// Initialize with custom tools
init({ 
  server, 
  endpoints: [createPayments, customTool] 
});
```

## Praktik Terbaik Keamanan

Lindungi kredensial API Anda dan jaga integrasi tetap aman.

<AccordionGroup>
<Accordion title="Manajemen Kunci API">
**Jangan pernah mengkomit kredensial ke kontrol versi**

Simpan kunci API dalam variabel lingkungan atau sistem manajemen rahasia yang aman.

```bash
# Use environment variables
export DODO_PAYMENTS_API_KEY="dodo_test_..."

# Use a .env file (add to .gitignore)
echo "DODO_PAYMENTS_API_KEY=dodo_test_..." > .env
```

**Putar kunci secara teratur**

Hasilkan kunci API baru secara berkala dan cabut yang lama melalui dasbor Dodo Payments Anda.

**Gunakan kunci uji untuk pengembangan**

Selalu gunakan kunci API mode uji selama pengembangan untuk menghindari mempengaruhi data produksi.
</Accordion>

<Accordion title="Kontrol Akses">
**Terapkan penyaringan alat di produksi**

Batasi operasi yang diekspos hanya pada apa yang dibutuhkan asisten AI Anda.

```bash
# Production: read-only access
npx dodopayments-mcp --operation=read

# Development: full access
npx dodopayments-mcp
```

**Terapkan autentikasi untuk server jarak jauh**

Saat menerapkan secara jarak jauh, selalu minta autentikasi melalui header `Authorization` atau header `x-dodo-payments-api-key`.

**Pantau penggunaan API**

Lacak aktivitas server MCP melalui dasbor Dodo Payments Anda dan atur peringatan untuk pola yang tidak biasa.
</Accordion>

<Accordion title="Keamanan Jaringan">
**Gunakan HTTPS untuk server jarak jauh**

Selalu terapkan server MCP jarak jauh di belakang endpoint HTTPS.

**Terapkan pembatasan laju**

Lindungi dari penyalahgunaan dengan menerapkan batasan laju di tingkat server MCP dan API.

**Batasi akses jaringan**

Konfigurasikan aturan firewall untuk membatasi klien mana yang dapat terhubung ke server MCP Anda.
</Accordion>
</AccordionGroup>

## Pemecahan Masalah

<AccordionGroup>
<Accordion title="Masalah Koneksi">
**Verifikasi kunci API Anda**

Pastikan kunci API Anda diatur dengan benar dan memiliki izin yang diperlukan.

```bash
# Test your API key
curl -H "Authorization: Bearer dodo_test_..." \
  https://api.dodopayments.com/payments
```

**Periksa koneksi jaringan Anda**

Verifikasi Anda dapat mengakses endpoint API Dodo Payments.

**Tinjau log klien**

Aktifkan logging verbose di klien MCP Anda untuk mendiagnosis masalah koneksi.
</Accordion>

<Accordion title="Kesalahan Autentikasi">
**Konfirmasi lingkungan kunci API**

Pastikan Anda menggunakan kunci uji dengan endpoint uji dan kunci langsung dengan endpoint produksi.

**Periksa variabel lingkungan**

Verifikasi `DODO_PAYMENTS_ENVIRONMENT` diatur dengan benar (`live_mode` untuk produksi).

**Hasilkan ulang kredensial**

Jika masalah berlanjut, hasilkan kunci API baru melalui dasbor Anda.
</Accordion>

<Accordion title="Kegagalan Eksekusi Alat">
**Validasi parameter input**

Pastikan asisten AI memberikan parameter yang diformat dengan benar untuk setiap alat.

**Tinjau pesan kesalahan**

Periksa respons kesalahan dari API untuk panduan spesifik tentang apa yang salah.

**Uji langsung dengan API**

Verifikasi operasi berfungsi saat memanggil API Dodo Payments langsung melalui curl atau Postman.
</Accordion>

<Accordion title="Masalah Kompatibilitas Klien">
**Coba preset klien yang berbeda**

Jika alat tidak berfungsi dengan benar, coba tentukan klien Anda secara eksplisit:

```bash
npx dodopayments-mcp --client=cursor
```

**Periksa dukungan kemampuan**

Beberapa klien tidak mendukung semua fitur skema JSON. Gunakan `--describe-capabilities` untuk memahami apa yang didukung.
</Accordion>
</AccordionGroup>

## Sumber Daya

<CardGroup cols={2}>
<Card title="Referensi API" icon="book" href="/api-reference/introduction">
  Jelajahi dokumentasi lengkap API Dodo Payments
</Card>

<Card title="Protokol MCP" icon="link" href="https://modelcontextprotocol.io">
  Pelajari lebih lanjut tentang standar Protokol Konteks Model
</Card>

<Card title="Repositori GitHub" icon="github" href="https://github.com/dodopayments/dodopayments-typescript/tree/main/packages/mcp-server">
  Lihat kode sumber server MCP dan berkontribusi
</Card>

<Card title="Paket NPM" icon="npm" href="https://www.npmjs.com/package/dodopayments-mcp">
  Instal server MCP dari NPM
</Card>
</CardGroup>