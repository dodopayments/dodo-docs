---
title: Sesi Checkout
description: Buat pengalaman checkout yang aman dan dihosting yang menangani
  alur pembayaran lengkap untuk pembelian satu kali dan langganan dengan kontrol
  kustomisasi penuh.
icon: cart-shopping
---
<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  Jalankan sesi checkout pertama Anda dalam waktu kurang dari 5 menit
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  Jelajahi dokumentasi API lengkap dan uji permintaan serta respons Checkout Session secara interaktif.
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  Hitung harga, pajak, dan total sebelum membuat sesi.
</Card>
</CardGroup>

<Info>
**Validitas Sesi**: Checkout session berlaku selama 24 jam secara default. Jika Anda menyertakan `confirm=true` dalam permintaan, sesi hanya akan berlaku selama 15 menit.
</Info>

## Prasyarat

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  Anda memerlukan akun pedagang Dodo Payments aktif dengan akses API.
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  Hasilkan kredensial API Anda dari dasbor Dodo Payments:
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  Buat produk Anda di dasbor Dodo Payments sebelum menerapkan sesi checkout.
</Step>
</Steps>

## Membuat Sesi Checkout Pertama Anda

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### Respons API

Semua metode di atas mengembalikan struktur respons yang sama:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
Ambil `checkout_url` dari respons API.
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
Arahkan pelanggan Anda ke URL checkout untuk menyelesaikan pembelian mereka.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Pilihan Integrasi Alternatif**: Alih-alih mengarahkan, Anda dapat menyematkan checkout langsung di halaman Anda menggunakan [Overlay Checkout](/developer-resources/overlay-checkout) (overlay modal) atau [Inline Checkout](/developer-resources/inline-checkout) (sepenuhnya tertanam). Kedua opsi menggunakan URL sesi checkout yang sama.
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
Setelah pembayaran, pelanggan dialihkan ke `return_url` Anda dengan parameter query termasuk ID pembayaran/langganan, status, email pelanggan, dan kunci lisensi apa pun. Lihat [dOKUMENTASI parameter `return_url`](#request-body) untuk daftar lengkap.
</Step>
</Steps>

## Badan Permintaan

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  Bidang penting yang dibutuhkan untuk setiap sesi checkout
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  Konfigurasi tambahan untuk menyesuaikan pengalaman checkout Anda
</Card>
</CardGroup>

### Bidang Wajib

<ParamField body="product_cart" type="array" required>
Array produk untuk dimasukkan dalam sesi checkout. Setiap produk harus memiliki `product_id` yang valid dari dasbor Dodo Payments Anda.

<Tip>
**Checkout Campuran**: Anda dapat menggabungkan produk berbayar sekali dan produk langganan dalam sesi checkout yang sama. Ini memungkinkan kasus penggunaan kuat seperti biaya pemasangan dengan langganan, bundel perangkat keras dengan SaaS, dan lainnya.
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
Identifikasi unik produk dari dasbor Dodo Payments Anda.

**Contoh:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
Jumlah produk.

**Contoh:** `1` untuk satu item, `3` untuk kuantitas lebih dari satu
</ParamField>

<ParamField body="addons" type="array">
Array addon yang melekat pada produk. Hanya berlaku jika produk adalah langganan.

{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
Jumlah yang dibayar pelanggan jika pay_what_you_want diaktifkan. Jika dinonaktifkan, bidang ini akan diabaikan.

**Format**: Diwakili dalam denominasi terendah mata uang (misalnya, sen untuk USD). Misalnya, untuk mengenakan biaya $1.00, kirim `100`.
</ParamField>
</Expandable>
</ParamField>

<Tip>
**Temukan ID Produk Anda**: Anda dapat menemukan ID produk di dasbor Dodo Payments Anda di bawah Products → View Details, atau dengan menggunakan [List Products API](/api-reference/products/get-products).
</Tip>

### Bidang Opsional

Konfigurasikan bidang ini untuk menyesuaikan pengalaman checkout dan menambahkan logika bisnis ke alur pembayaran Anda.

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
Informasi pelanggan. Anda dapat melampirkan pelanggan yang ada menggunakan ID mereka atau membuat catatan pelanggan baru selama checkout.

<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}
Lampirkan pelanggan yang sudah ada ke sesi checkout menggunakan ID mereka.

{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
Pengidentifikasi unik dari pelanggan yang ada. Gunakan ini untuk melampirkan sesi checkout ke pelanggan yang sudah ada alih-alih membuat yang baru.
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
Buat catatan pelanggan baru selama checkout.

{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
Alamat email pelanggan. Wajib saat membuat pelanggan baru.
</ParamField>

<ParamField body="name" type="string">
Nama lengkap pelanggan seperti yang harus muncul di tanda terima dan faktur.

**Contoh**: `"John Doe"` atau `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
Nomor telepon pelanggan dalam format internasional. Wajib untuk beberapa metode pembayaran dan pencegahan penipuan.

**Format**: Sertakan kode negara, misalnya `"+1234567890"` untuk nomor AS
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
Informasi alamat penagihan untuk perhitungan pajak yang akurat, pencegahan penipuan, dan kepatuhan regulasi.

<Info>
Ketika `confirm` disetel ke `true`, semua bidang alamat penagihan menjadi wajib untuk pembuatan sesi yang berhasil.
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
Alamat jalan lengkap termasuk nomor rumah, nama jalan, dan nomor apartemen/unit jika berlaku.

**Contoh**: `"123 Main St, Apt 4B"` atau `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
Nama kota atau kabupaten.

**Contoh**: `"San Francisco"`, `"New York"`, `"London"`
</ParamField>

<ParamField body="state" type="string">
Nama negara bagian, provinsi, atau wilayah. Gunakan nama lengkap atau singkatan standar.

**Contoh**: `"California"` atau `"CA"`, `"Ontario"` atau `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
Kode negara ISO dua huruf (ISO 3166-1 alpha-2). Bidang ini selalu wajib saat billing_address disediakan.

**Contoh**: `"US"` (United States), `"CA"` (Canada), `"GB"` (United Kingdom), `"DE"` (Germany)

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  Lihat daftar lengkap negara yang didukung dan kode ISO-nya
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
Kode pos, ZIP code, atau setara berdasarkan persyaratan negara.

**Contoh**: `"94102"` (US), `"M5V 3A8"` (Canada), `"SW1A 1AA"` (UK)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
Kontrol metode pembayaran mana yang tersedia untuk pelanggan selama checkout. Ini membantu mengoptimalkan untuk pasar atau kebutuhan bisnis tertentu.

**Opsi yang Tersedia**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**Kritis**: Selalu sertakan `credit` dan `debit` sebagai opsi cadangan untuk mencegah kegagalan checkout saat metode pembayaran utama tidak tersedia.
</Warning>

**Contoh**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
Timpa pemilihan mata uang default dengan mata uang tagihan tetap. Menggunakan kode mata uang ISO 4217.

**Mata Uang yang Didukung**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR`, dan lainnya

**Contoh**: `"USD"` untuk Dolar AS, `"EUR"` untuk Euro

<Note>
Bidang ini hanya efektif saat harga adaptif diaktifkan. Jika harga adaptif dinonaktifkan, mata uang default produk akan digunakan.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
Tampilkan metode pembayaran yang telah disimpan sebelumnya untuk pelanggan yang kembali, meningkatkan kecepatan checkout dan pengalaman pengguna.
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
URL untuk mengarahkan pelanggan setelah pembayaran selesai. Dodo Payments menambahkan parameter query berikut ke URL Anda saat pengalihan:

| Parameter | Tipe | Kondisi |
|-----------|------|-----------|
| `payment_id` | `string` | Selalu hadir untuk pembayaran sekali |
| `subscription_id` | `string` | Selalu hadir untuk pembayaran langganan |
| `status` | `string` | Selalu hadir |
| `license_key` | `string` | Hadir jika produk mengaktifkan kunci lisensi. Dipisahkan koma jika ada banyak kunci |
| `email` | `string` | Hadir jika pelanggan memiliki email pada catatan |

**Contoh URL pengalihan:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
Gunakan parameter query `license_key` dan `email` untuk menampilkan kunci lisensi atau mengirim konfirmasi langsung di halaman kembali Anda, tanpa perlu panggilan API tambahan.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
Jika benar, menyelesaikan semua detail sesi segera. API akan mengeluarkan kesalahan jika data yang diperlukan hilang.
</ParamField>

<ParamField body="discount_code" type="string">
Terapkan kode diskon ke sesi checkout.
</ParamField>

<ParamField body="metadata" type="object">
Pasangan kunci-nilai khusus untuk menyimpan informasi tambahan tentang sesi.
</ParamField>

<ParamField body="force_3ds" type="boolean">
Timpa perilaku 3DS default pedagang untuk sesi ini.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Aktifkan mode pengumpulan alamat minimal. Saat diaktifkan, checkout hanya mengumpulkan:
- **Negara**: Selalu wajib untuk penentuan pajak
- **Kode ZIP/Pos**: Hanya di wilayah yang membutuhkannya untuk perhitungan pajak penjualan, VAT, atau GST

Ini secara signifikan mengurangi gesekan checkout dengan menghilangkan bidang formulir yang tidak perlu.

<Tip>
Aktifkan alamat minimal untuk penyelesaian checkout yang lebih cepat. Pengumpulan alamat lengkap tetap tersedia untuk bisnis yang memerlukan detail penagihan lengkap.
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
Sesuaikan tampilan dan perilaku antarmuka checkout.

{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
Tema untuk antarmuka checkout. Opsi: `light`, `dark`, atau `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
Tampilkan bagian detail pesanan di antarmuka checkout.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
Tampilkan tag "on-demand" untuk produk yang berlaku.
</ParamField>

<ParamField body="force_language" type="string">
Paksa antarmuka checkout untuk dirender dalam bahasa tertentu. Secara default, checkout secara otomatis mendeteksi bahasa browser pelanggan.

**Kode bahasa yang didukung:**
`ar` (Arabic), `ca` (Catalan), `de` (German), `en` (English), `es` (Spanish), `fr` (French), `he` (Hebrew), `id` (Indonesian), `it` (Italian), `ja` (Japanese), `ko` (Korean), `ms` (Malay), `nl` (Dutch), `pl` (Polish), `pt` (Portuguese), `ro` (Romanian), `ru` (Russian), `sv` (Swedish), `th` (Thai), `tr` (Turkish), `zh` (Chinese)

**Contoh**: `"es"` untuk Spanish, `"ja"` untuk Japanese
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
Konfigurasikan fitur dan perilaku spesifik untuk sesi checkout.

{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
Izinkan pelanggan memilih mata uang yang mereka sukai selama checkout.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
Tampilkan bidang input kode diskon di antarmuka checkout.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
Kumpulkan nomor telepon pelanggan selama checkout.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
Izinkan pelanggan memasukkan nomor identifikasi pajak.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
Paksa pembuatan catatan pelanggan baru alih-alih memperbarui yang sudah ada.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
Izinkan pelanggan mengubah alamat email mereka selama checkout.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
Izinkan pelanggan mengubah nama mereka selama checkout.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
Izinkan pelanggan mengubah alamat jalan selama checkout.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
Izinkan pelanggan mengubah kota selama checkout.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
Izinkan pelanggan mengubah negara bagian selama checkout.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
Izinkan pelanggan mengubah negara selama checkout.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
Izinkan pelanggan mengubah kode pos selama checkout.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Aktifkan mode pengumpulan alamat minimal. Saat diaktifkan, checkout hanya mengumpulkan:
- **Negara**: Selalu wajib untuk penentuan pajak
- **Kode ZIP/Pos**: Hanya di wilayah yang membutuhkannya untuk perhitungan pajak penjualan, VAT, atau GST

Ini secara signifikan mengurangi gesekan checkout dengan menghilangkan bidang formulir yang tidak perlu.

<Tip>
Aktifkan alamat minimal untuk penyelesaian checkout yang lebih cepat. Pengumpulan alamat lengkap tetap tersedia untuk bisnis yang memerlukan detail penagihan lengkap.
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
Lewati halaman sukses pembayaran default dan alihkan pelanggan langsung ke `return_url` Anda setelah pembayaran selesai.

<Tip>
Gunakan ini saat Anda memiliki halaman sukses khusus yang memberikan pengalaman pengguna lebih baik, atau untuk aplikasi seluler dan alur checkout tertanam.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
Kumpulkan informasi tambahan dari pelanggan selama checkout dengan bidang formulir khusus. Anda dapat mendefinisikan hingga 5 bidang khusus per sesi checkout. Respons pelanggan disertakan dalam payload webhook dan tersedia melalui API.

{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
Pengidentifikasi unik untuk bidang ini. Digunakan sebagai kunci dalam payload webhook dan respons API.

**Contoh**: `"company_name"`, `"referral_source"`, `"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
Label yang ditampilkan kepada pelanggan di formulir checkout.

**Contoh**: `"Company Name"`, `"How did you hear about us?"`, `"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
Jenis bidang yang menentukan aturan validasi input.

**Tipe yang tersedia**:
- `text` - Input teks satu baris
- `number` - Input numerik
- `email` - Alamat email dengan validasi
- `url` - URL dengan validasi
- `date` - Pemilih tanggal
- `dropdown` - Pilih dari opsi yang sudah ditentukan
- `boolean` - Sakelar Ya/Tidak
</ParamField>

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
Apakah bidang ini harus diisi sebelum checkout dapat selesai.

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
Teks placeholder yang ditampilkan di bidang input.

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
Daftar opsi untuk tipe bidang `dropdown`. Wajib saat `field_type` adalah `dropdown`, diabaikan untuk tipe lain.

**Contoh**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
Respons pelanggan terhadap bidang khusus disertakan dalam:
- **Webhooks**: `payment.succeeded`, `subscription.created`, dan payload event relevan lainnya menyertakan array `custom_field_responses`
- **Respons API**: objek pembayaran dan langganan menyertakan `custom_field_responses`
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
Konfigurasi tambahan untuk sesi checkout yang berisi produk langganan.

{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
Jumlah hari untuk periode uji coba sebelum tagihan pertama. Harus antara 0 dan 10000 hari.
</ParamField>

<ParamField body="on_demand" type="object">
Konfigurasi langganan on-demand untuk penagihan berbasis penggunaan atau metered.

{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
Jika disetel ke true, tidak melakukan penagihan apa pun dan hanya mengotorisasi detail metode pembayaran untuk penggunaan di masa depan.
</ParamField>

<ParamField body="product_price" type="integer">
Harga produk untuk tagihan awal ke pelanggan. Jika tidak ditentukan, harga tersimpan dari produk akan digunakan.

**Format**: Diwakili dalam denominasi terendah mata uang (misalnya, sen untuk USD). Misalnya, untuk mengenakan biaya $1.00, kirim `100`.
</ParamField>

<ParamField body="product_currency" type="string">
Mata uang opsional untuk harga produk. Jika tidak ditentukan, default ke mata uang produk.

<ParamField body="product_description" type="string">
Penggantian deskripsi produk opsional untuk penagihan dan item baris. Jika tidak ditentukan, deskripsi tersimpan produk akan digunakan.

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
Apakah biaya mata uang adaptif harus dimasukkan dalam harga produk (true) atau ditambahkan di atasnya (false). Diabaikan jika harga adaptif tidak diaktifkan.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## Contoh Penggunaan

Berikut 10 contoh komprehensif yang memamerkan berbagai konfigurasi sesi checkout untuk berbagai skenario bisnis:

### 1. Checkout Produk Tunggal Sederhana

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. Keranjang Multi-Produk

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. Langganan dengan Periode Percobaan

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. Checkout Pra-konfirmasi

<Note>
Ketika `confirm` disetel ke `true`, pelanggan akan langsung dibawa ke halaman checkout, melewati langkah konfirmasi apa pun.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. Checkout dengan Penimpaan Mata Uang

<Note>
Penimpaan `billing_currency` hanya berlaku saat mata uang adaptif diaktifkan dalam pengaturan akun Anda. Jika mata uang adaptif dinonaktifkan, parameter ini tidak akan berpengaruh.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. Metode Pembayaran Tersimpan untuk Pelanggan Kembali

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. Checkout B2B dengan Pengumpulan Tax ID

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. Checkout Tema Gelap dengan Kode Diskon

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. Metode Pembayaran Regional (UPI untuk India)

Untuk informasi terperinci tentang konfigurasi dan pengujian UPI, lihat halaman <a href="/features/payment-methods/india">India Payment Methods</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. Checkout BNPL (Buy Now Pay Later)

Untuk informasi terperinci tentang konfigurasi dan pengujian BNPL, lihat halaman <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. Menggunakan Metode Pembayaran yang Ada untuk Checkout Instan

Gunakan metode pembayaran yang tersimpan milik pelanggan untuk membuat sesi checkout yang diproses segera, melewati pengumpulan metode pembayaran:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
Saat menggunakan `payment_method_id`, `confirm` harus disetel ke `true` dan `customer_id` yang sudah ada harus disediakan. Metode pembayaran akan divalidasi untuk kecocokan dengan mata uang pembayaran.
</Warning>

<Info>
Metode pembayaran harus milik pelanggan dan kompatibel dengan mata uang pembayaran. Ini memungkinkan pembelian sekali klik untuk pelanggan yang kembali.
</Info>

### 12. Tautan Pendek untuk URL Pembayaran yang Lebih Rapi

Buat tautan pembayaran yang dipendekkan dan dapat dibagikan dengan slug kustom:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
Tautan pendek sangat cocok untuk SMS, email, atau berbagi media sosial. Mereka lebih mudah diingat dan membangun kepercayaan pelanggan lebih baik daripada URL panjang.
</Tip>

### 13. Lewati Halaman Sukses Pembayaran dengan Pengalihan Langsung

Alihkan pelanggan segera setelah pembayaran selesai, melewati halaman sukses default:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
Gunakan `redirect_immediately: true` ketika Anda memiliki halaman sukses kustom yang memberikan pengalaman pengguna lebih baik daripada halaman sukses pembayaran default. Ini sangat berguna untuk aplikasi seluler dan alur checkout tertanam.
</Tip>

<Note>
Ketika `redirect_immediately` diaktifkan, pelanggan dialihkan ke `return_url` Anda segera setelah pembayaran selesai, sepenuhnya melewati halaman sukses default.
</Note>

### 14. Memaksa Bahasa Tertentu

Paksa checkout menampilkan dalam bahasa tertentu, menggantikan deteksi bahasa browser pelanggan:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
Gunakan `force_language` ketika Anda mengetahui bahasa pilihan pelanggan (misalnya dari pengaturan akun mereka) atau ketika menargetkan pasar regional tertentu.
</Tip>

**Bahasa yang didukung:** Arabic (`ar`), Catalan (`ca`), Chinese (`zh`), Dutch (`nl`), English (`en`), French (`fr`), German (`de`), Hebrew (`he`), Indonesian (`id`), Italian (`it`), Japanese (`ja`), Korean (`ko`), Malay (`ms`), Polish (`pl`), Portuguese (`pt`), Romanian (`ro`), Russian (`ru`), Spanish (`es`), Swedish (`sv`), Thai (`th`), Turkish (`tr`)

### 15. Mengumpulkan Field Kustom

Kumpulkan informasi tambahan dari pelanggan selama checkout menggunakan field kustom:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
Respons field kustom secara otomatis disertakan dalam payload webhook (`payment.succeeded`, `subscription.created`, dll.) dan dapat diambil melalui API. Gunakan untuk memperkaya CRM Anda, memicu alur onboarding, atau menyesuaikan pengalaman pelanggan.
</Info>

**Jenis field yang tersedia:** `text`, `number`, `email`, `url`, `date`, `dropdown`, `boolean`

## Pratinjau Sesi Checkout

Sebelum membuat sesi checkout, Anda dapat melihat pratinjau rincian harga termasuk pajak, diskon, dan total. Ini berguna untuk menampilkan harga yang akurat kepada pelanggan sebelum mereka melanjutkan ke checkout.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  Lihat dokumentasi endpoint pratinjau lengkap.
</Card>

## Beralih dari Dynamic Links ke Checkout Sessions

### Perbedaan Utama

Sebelumnya, saat membuat tautan pembayaran dengan Dynamic Links, Anda diwajibkan memberikan alamat penagihan lengkap pelanggan.

Dengan Checkout Sessions, ini tidak lagi diperlukan. Anda cukup meneruskan informasi yang Anda miliki, dan kami akan menangani sisanya. Misalnya:
- Jika Anda hanya mengetahui negara penagihan pelanggan, cukup berikan itu.
- Alur checkout akan secara otomatis mengumpulkan detail yang hilang sebelum membawa pelanggan ke halaman pembayaran.
- Di sisi lain, jika Anda sudah memiliki semua informasi yang diperlukan dan ingin langsung menuju halaman pembayaran, Anda dapat mengirimkan seluruh set data dan menyertakan `confirm=true` dalam badan permintaan Anda.

### Proses Migrasi

Migrasi dari Dynamic Links ke Checkout Sessions cukup sederhana:

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
Perbarui integrasi Anda untuk menggunakan metode API atau SDK baru.
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
Sesuaikan payload permintaan sesuai format Checkout Sessions.
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
Ya. Tidak diperlukan penanganan tambahan atau langkah migrasi khusus di pihak Anda.
</Step>
</Steps>
