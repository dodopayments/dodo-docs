---
title: C#
description: Integrera Dodo Payments i dina .NET-applikationer med modern async/await-stöd
icon: c
---

C# SDK:n ger bekväm åtkomst till Dodo Payments REST API från applikationer skrivna i C#. Den har ett async Task-baserat API med stark typning och är för närvarande i beta.

<Info>
C# SDK:n är för närvarande i beta. Vi arbetar aktivt med förbättringar och välkomnar din feedback.
</Info>

## Installation

Installera SDK:n med .NET CLI:

```bash
dotnet add package DodoPayments.Client
```

<Info>
SDK:n kräver .NET Standard 2.0 eller senare. Den fungerar med ASP.NET Core, konsolapplikationer och andra .NET-projekttyper.
</Info>

## Snabbstart

Initiera klienten och skapa en checkout-session:

```csharp
using System;
using DodoPayments.Client;
using DodoPayments.Client.Models.CheckoutSessions;

// Configured using the DODO_PAYMENTS_API_KEY and DODO_PAYMENTS_BASE_URL environment variables
DodoPaymentsClient client = new();

CheckoutSessionCreateParams parameters = new()
{
    ProductCart =
    [
        new()
        {
            ProductID = "product_id",
            Quantity = 1,
        },
    ],
};

var checkoutSessionResponse = await client.CheckoutSessions.Create(parameters);

Console.WriteLine(checkoutSessionResponse.SessionId);
```

<Warning>
Lagra alltid dina API-nycklar säkert med hjälp av miljövariabler, användarsecret eller Azure Key Vault. Hårdkoda dem aldrig i din källkod eller kommitta dem till versionskontroll.
</Warning>

## Kärnfunktioner

<CardGroup cols={2}>
<Card title="Async/Await" icon="bolt">
  Fullt async Task-baserat API för icke-blockerande operationer
</Card>

<Card title="Stark Typning" icon="shield-check">
  Omfattande typ-säkerhet med nullable referenstyper
</Card>

<Card title="Dependency Injection" icon="layer-group">
  Förstklassigt stöd för .NET dependency injection
</Card>

<Card title="Konfiguration" icon="gear">
  Enkel konfiguration via miljövariabler eller appsettings.json
</Card>
</CardGroup>

## Konfiguration

### Miljövariabler

Konfigurera med hjälp av miljövariabler:

```bash .env
DODO_PAYMENTS_API_KEY=your_api_key_here
```

```csharp
// Automatically reads from environment variables
DodoPaymentsClient client = new();
```

Se denna tabell för tillgängliga alternativ:

| Egenskap      | Miljövariabel              | Obligatorisk | Standardvärde                     |
| ------------- | --------------------------- | ------------ | --------------------------------- |
| `BearerToken` | `DODO_PAYMENTS_API_KEY`     | true         | -                                 |
| `WebhookKey`  | `DODO_PAYMENTS_WEBHOOK_KEY` | false        | -                                 |
| `BaseUrl`     | `DODO_PAYMENTS_BASE_URL`    | true         | `"https://live.dodopayments.com"` |

### Manuell Konfiguration

Konfigurera manuellt med bärartoken:

```csharp
DodoPaymentsClient client = new() { BearerToken = "My Bearer Token" };
```

## Vanliga Operationer

### Skapa en Checkout-session

Generera en checkout-session:

```csharp
var parameters = new CheckoutSessionCreateParams
{
    ProductCart =
    [
        new()
        {
            ProductID = "prod_123",
            Quantity = 1
        }
    ],
    ReturnUrl = "https://yourdomain.com/return"
};

var session = await client.CheckoutSessions.Create(parameters);
Console.WriteLine($"Checkout URL: {session.Url}");
```

### Hantera Kunder

Skapa och hämta kundinformation:

```csharp
// Create a customer
var createParams = new CustomerCreateParams
{
    Email = "customer@example.com",
    Name = "John Doe",
    Metadata = new Dictionary<string, string>
    {
        { "user_id", "12345" }
    }
};

var customer = await client.Customers.Create(createParams);

// Retrieve customer
var retrieved = await client.Customers.Retrieve("cus_123");
Console.WriteLine($"Customer: {retrieved.Name} ({retrieved.Email})");
```

### Hantera Prenumerationer

Skapa och hantera återkommande prenumerationer:

```csharp
// Create a subscription
var subscriptionParams = new SubscriptionCreateParams
{
    CustomerID = "cus_123",
    ProductID = "prod_456",
    PriceID = "price_789"
};

var subscription = await client.Subscriptions.Create(subscriptionParams);

// Cancel subscription
await client.Subscriptions.Cancel(subscription.Id);
```

## Pagineringshantering

SDK:n definierar metoder som returnerar paginerade listor med resultat med bekväma sätt att få åtkomst till resultat antingen en sida i taget eller objekt för objekt över alla sidor.

### Automatisk Pagineringshantering

För att iterera genom alla resultat över alla sidor, använd `Paginate` metoden:

```csharp
using System;

var page = await client.Payments.List(parameters);
await foreach (var item in page.Paginate())
{
    Console.WriteLine(item);
}
```

### Manuell Pagineringshantering

För att få åtkomst till individuella sidobjekt och manuellt begära nästa sida:

```csharp
using System;

var page = await client.Payments.List();
while (true)
{
    foreach (var item in page.Items)
    {
        Console.WriteLine(item);
    }
    if (!page.HasNext())
    {
        break;
    }
    page = await page.Next();
}
```

## ASP.NET Core Integration

### Tjänstregistrering

Registrera klienten i `Program.cs`:

```csharp Program.cs
using DodoPayments.Client;

var builder = WebApplication.CreateBuilder(args);

// Register DodoPayments client
builder.Services.AddSingleton<DodoPaymentsClient>(sp =>
{
    var configuration = sp.GetRequiredService<IConfiguration>();
    return new DodoPaymentsClient
    {
        BearerToken = configuration["DodoPayments:ApiKey"]
    };
});

builder.Services.AddControllers();

var app = builder.Build();
app.MapControllers();
app.Run();
```

### Konfiguration

Lägg till konfiguration i `appsettings.json`:

```json appsettings.json
{
  "DodoPayments": {
    "ApiKey": "your_api_key_here"
  }
}
```

### API-controller

Skapa en controller med hjälp av beroendeinjektion:

```csharp
using Microsoft.AspNetCore.Mvc;
using DodoPayments.Client;
using DodoPayments.Client.Models.CheckoutSessions;

[ApiController]
[Route("api/[controller]")]
public class PaymentsController : ControllerBase
{
    private readonly DodoPaymentsClient _client;
    private readonly ILogger<PaymentsController> _logger;

    public PaymentsController(DodoPaymentsClient client, ILogger<PaymentsController> logger)
    {
        _client = client;
        _logger = logger;
    }

    [HttpPost("checkout")]
    public async Task<IActionResult> CreateCheckout([FromBody] CheckoutRequest request)
    {
        try
        {
            var parameters = new CheckoutSessionCreateParams
            {
                ProductCart = request.Items,
                ReturnUrl = Url.Action("Return", "Checkout", null, Request.Scheme)
            };

            var session = await _client.CheckoutSessions.Create(parameters);

            return Ok(new { checkoutUrl = session.Url });
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Failed to create checkout");
            return BadRequest(new { error = ex.Message });
        }
    }
}

public record CheckoutRequest(List<ProductCartItem> Items);
```

### Tjänstelager-mönster

Skapa en tjänst för affärslogik:

```csharp
public interface IPaymentService
{
    Task<CheckoutSessionResponse> CreateCheckoutAsync(List<ProductCartItem> items);
    Task<PaymentResponse> ProcessPaymentAsync(decimal amount, string currency, string customerId);
}

public class PaymentService : IPaymentService
{
    private readonly DodoPaymentsClient _client;
    private readonly ILogger<PaymentService> _logger;

    public PaymentService(DodoPaymentsClient client, ILogger<PaymentService> logger)
    {
        _client = client;
        _logger = logger;
    }

    public async Task<CheckoutSessionResponse> CreateCheckoutAsync(List<ProductCartItem> items)
    {
        var parameters = new CheckoutSessionCreateParams
        {
            ProductCart = items,
            ReturnUrl = "https://yourdomain.com/return"
        };

        return await _client.CheckoutSessions.Create(parameters);
    }

    public async Task<PaymentResponse> ProcessPaymentAsync(
        decimal amount,
        string currency,
        string customerId)
    {
        try
        {
            var parameters = new PaymentCreateParams
            {
                Amount = (long)(amount * 100), // Convert to cents
                Currency = currency,
                CustomerID = customerId
            };

            return await _client.Payments.Create(parameters);
        }
        catch (Exception ex)
        {
            _logger.LogError(ex, "Failed to process payment");
            throw;
        }
    }
}
```

## Användarhemligheter (Utveckling)

För utveckling, använd användarhemligheter för att lagra API-nycklar:

```bash
dotnet user-secrets init
dotnet user-secrets set "DodoPayments:ApiKey" "your_api_key_here"
```

## Testning

Exempel på enhetstest med xUnit:

```csharp
using Xunit;
using DodoPayments.Client;
using DodoPayments.Client.Models.CheckoutSessions;

public class PaymentServiceTests
{
    private readonly DodoPaymentsClient _client;

    public PaymentServiceTests()
    {
        _client = new DodoPaymentsClient
        {
            BearerToken = "test_key"
        };
    }

    [Fact]
    public async Task CreateCheckout_ShouldReturnSession()
    {
        // Arrange
        var parameters = new CheckoutSessionCreateParams
        {
            ProductCart =
            [
                new()
                {
                    ProductID = "prod_test",
                    Quantity = 1
                }
            ]
        };

        // Act
        var session = await _client.CheckoutSessions.Create(parameters);

        // Assert
        Assert.NotNull(session);
        Assert.NotNull(session.SessionId);
    }
}
```

## Resurser

<CardGroup cols={2}>
<Card title="GitHub-repo" icon="github" href="https://github.com/dodopayments/dodopayments-csharp">
  Visa källkod och bidra
</Card>

<Card title="API-referens" icon="book" href="/api-reference/introduction">
  Komplett API-dokumentation
</Card>

<Card title="Discord-community" icon="discord" href="https://discord.gg/bYqAp4ayYh">
  Få hjälp och koppla samman med utvecklare
</Card>

<Card title="Rapportera problem" icon="bug" href="https://github.com/dodopayments/dodopayments-csharp/issues">
  Rapportera buggar eller begära funktioner
</Card>
</CardGroup>

## Support

Behöver du hjälp med C# SDK:n?

- **Discord**: Gå med i vår [community-server](https://discord.gg/bYqAp4ayYh) för realtidsstöd
- **E-post**: Kontakta oss på support@dodopayments.com
- **GitHub**: Öppna ett ärende på [repo](https://github.com/dodopayments/dodopayments-csharp)

## Bidra

Eftersom SDK:n är i beta är din feedback och dina bidrag särskilt värdefulla! Kolla in [bidragsriktlinjerna](https://github.com/dodopayments/dodopayments-csharp/blob/main/CONTRIBUTING.md) för att komma igång.
