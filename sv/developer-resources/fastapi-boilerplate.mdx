---
sidebarTitle: FastAPI
title: FastAPI-startmall
description: Kom snabbt igång med vår FastAPI-startmall för att integrera <Card title="GitHub Repository" icon="github" href="https://github.com/dodopayments/fastapi-boilerplate"> i dina
  Python-backendapplikationer
icon: /images/logos/fastapi.svg
tag: "NEW"
keywords:
  - "Dodo Payments"
  - FastAPI
  - Python
  - startmall
  - snabbstart
  - fastapi-startmall
---

<Card title="GitHub Repository" icon="github" href="https://github.com/dodopayments/fastapi-boilerplate">
  Komplett FastAPI + Dodo Payments-startmall
</Card>

## Översikt

FastAPI-startmallen ger dig en produktionsklar utgångspunkt för att integrera Dodo Payments med ditt Python-backend. Mallen innehåller hantering av checkout-sessioner, verifiering av webhooks, integration med kundportalen och asynkrona API-mönster som hjälper dig att snabbt börja ta emot betalningar.

<Info>
Den här startmallen använder FastAPI med inbyggda async/await-mönster, Pydantic för validering och `dodopayments` Python SDK för smidig API-integration.
</Info>

### Funktioner

- **Snabb installation** – Kom igång på mindre än 5 minuter
- **Stöd för asynkron kod** – Byggd med FastAPI:s inbyggda async/await-mönster
- **Checkout-sessioner** – Förkonfigurerat checkout-flöde med Python SDK
- **Webhook-hantering** – Säker webhook-endpoint med signaturverifiering
- **Customer Portal** – Skapa kundportalsessioner enkelt
- **Typsäkerhet** – Fullständig Pydantic-validering och typanvisningar
- **Miljökonfiguration** – Färdig konfiguration av miljövariabler

## Förutsättningar

Innan du börjar bör du ha:

- **Python 3.9+** (rekommenderas: Python 3.11+)
- **pip** eller **uv** för pakethantering
- **Dodo Payments-konto** (för åtkomst till API- och Webhook Keys från instrumentpanelen)

## Snabbstart

<Steps>
<Step title="Clone the Repository">

```bash
git clone https://github.com/dodopayments/fastapi-boilerplate.git
cd fastapi-boilerplate
```

</Step>

<Step title="Create Virtual Environment">
Konfigurera en isolerad Python-miljö:

```bash
python -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
```

Eller använd uv för snabbare beroendehantering:

```bash
uv venv
source .venv/bin/activate
```

</Step>

<Step title="Install Dependencies">

```bash
pip install -r requirements.txt
```

Eller med uv:

```bash
uv pip install -r requirements.txt
```

</Step>

<Step title="Get API Credentials">
Registrera dig på [Dodo Payments](https://dodopayments.com/) och hämta dina autentiseringsuppgifter från instrumentpanelen:

- **API Key:** [Instrumentpanel → Developer → API Keys](https://app.dodopayments.com/developer/api-keys)
- **Webhook Key:** [Instrumentpanel → Developer → Webhooks](https://app.dodopayments.com/developer/webhooks)

<Tip>
Se till att du är i **Test Mode** under utvecklingen!
</Tip>
</Step>

<Step title="Configure Environment Variables">
Skapa en `.env`-fil i rotkatalogen:

```bash
cp .env.example .env
```

Uppdatera värdena med dina Dodo Payments-autentiseringsuppgifter:

```bash .env
DODO_PAYMENTS_API_KEY=your_api_key_here
DODO_PAYMENTS_WEBHOOK_KEY=your_webhook_signing_key_here
DODO_PAYMENTS_ENVIRONMENT=test_mode
```

<Warning>
Checka aldrig in din `.env`-fil i versionshanteringen. Den ingår redan i `.gitignore`.
</Warning>
</Step>

<Step title="Run the Development Server">

```bash
uvicorn main:app --reload
```

Öppna [http://localhost:8000/docs](http://localhost:8000/docs) för att se den interaktiva API-dokumentationen!

<Check>
Du bör se FastAPI:s Swagger UI med alla tillgängliga endpoints redo att testas.
</Check>
</Step>
</Steps>

## Projektstruktur

```text
fastapi-boilerplate/
├── main.py                 # FastAPI application entry point
├── routers/
│   ├── checkout.py         # Checkout session endpoints
│   ├── webhook.py          # Webhook handler endpoint
│   └── customer_portal.py  # Customer portal endpoints
├── config.py               # Environment configuration
├── requirements.txt        # Python dependencies
├── .env.example            # Environment template
└── README.md
```

## API-endpoints

Startmallen innehåller följande förkonfigurerade endpoints:

| Endpoint | Method | Beskrivning |
|----------|--------|-------------|
| `/checkout` | POST | Skapa en ny checkout-session |
| `/webhook` | POST | Hantera Dodo Payments-webhooks |
| `/customer-portal` | POST | Generera URL till kundportalen |

## Kodexempel

### Skapa en checkout-session

```python
from fastapi import APIRouter, HTTPException
from dodopayments import AsyncDodoPayments
from pydantic import BaseModel
from config import settings

router = APIRouter()
dodo = AsyncDodoPayments(
    bearer_token=settings.DODO_PAYMENTS_API_KEY,
    environment=settings.DODO_PAYMENTS_ENVIRONMENT
)

class CheckoutRequest(BaseModel):
    product_id: str
    quantity: int = 1
    customer_email: str | None = None
    customer_name: str | None = None

@router.post("/checkout")
async def create_checkout(request: CheckoutRequest):
    try:
        session = await dodo.checkout_sessions.create(
            product_cart=[{
                "product_id": request.product_id,
                "quantity": request.quantity
            }],
            customer={
                "email": request.customer_email,
                "name": request.customer_name
            } if request.customer_email else None,
            return_url="http://localhost:8000/success"
        )
        return {"checkout_url": session.checkout_url, "session_id": session.session_id}
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))
```

### Hantera webhooks

```python
from fastapi import APIRouter, Request, HTTPException
import hmac
import hashlib
from config import settings

router = APIRouter()

def verify_webhook_signature(payload: bytes, signature: str) -> bool:
    expected = hmac.new(
        settings.DODO_PAYMENTS_WEBHOOK_KEY.encode(),
        payload,
        hashlib.sha256
    ).hexdigest()
    return hmac.compare_digest(expected, signature)

@router.post("/webhook")
async def handle_webhook(request: Request):
    payload = await request.body()
    signature = request.headers.get("webhook-signature", "")
    
    if not verify_webhook_signature(payload, signature):
        raise HTTPException(status_code=401, detail="Invalid signature")
    
    event = await request.json()
    event_type = event.get("type")
    
    match event_type:
        case "payment.succeeded":
            # Handle successful payment
            payment_id = event["data"]["payment_id"]
            print(f"Payment succeeded: {payment_id}")
            
        case "subscription.active":
            # Handle subscription activation
            subscription_id = event["data"]["subscription_id"]
            print(f"Subscription activated: {subscription_id}")
            
        case "refund.succeeded":
            # Handle refund
            refund_id = event["data"]["refund_id"]
            print(f"Refund processed: {refund_id}")
    
    return {"status": "received"}
```

### Integrering med Customer Portal

```python
from fastapi import APIRouter, HTTPException
from dodopayments import AsyncDodoPayments
from pydantic import BaseModel
from config import settings

router = APIRouter()
dodo = AsyncDodoPayments(
    bearer_token=settings.DODO_PAYMENTS_API_KEY,
    environment=settings.DODO_PAYMENTS_ENVIRONMENT
)

class PortalRequest(BaseModel):
    customer_id: str

@router.post("/customer-portal")
async def create_portal_session(request: PortalRequest):
    try:
        session = await dodo.customers.customer_portal.create(
            customer_id=request.customer_id,
        )
        return {"portal_url": session.link}
    except Exception as e:
        raise HTTPException(status_code=400, detail=str(e))
```

## Webhook-händelser

Startmallen visar hur du hanterar vanliga webhook-händelser:

| Händelse | Beskrivning |
|-------|-------------|
| `payment.succeeded` | Betalningen genomfördes utan problem |
| `payment.failed` | Betalningsförsöket misslyckades |
| `subscription.active` | Prenumerationen är nu aktiv |
| `subscription.cancelled` | Prenumerationen avslutades |
| `refund.succeeded` | Återbetalningen behandlades utan problem |

Lägg till din verksamhetslogik i webhook-hanteraren för att:
- Uppdatera användarbehörigheter i din databas
- Skicka bekräftelsemeddelanden
- Ge åtkomst till digitala produkter
- Spåra analysdata och mätvärden

## Testa webhooks lokalt

För lokal utveckling kan du använda verktyg som [ngrok](https://ngrok.com/) för att exponera din lokala server:

```bash
ngrok http 8000
```

Uppdatera webhook-URL:en i din [Dodo Payments-instrumentpanel](https://app.dodopayments.com/developer/webhooks):

```
https://your-ngrok-url.ngrok.io/webhook
```

## Driftsättning

### Docker

```dockerfile
FROM python:3.11-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY . .

CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

Bygg och kör:

```bash
docker build -t fastapi-dodo .
docker run -p 8000:8000 --env-file .env fastapi-dodo
```

### Att tänka på i produktion

<Warning>
Innan du driftsätter i produktion:
- Ändra `DODO_PAYMENTS_ENVIRONMENT` till `live_mode`
- Använd produktionsnycklar från instrumentpanelen
- Uppdatera webhook-URL:en till din produktionsdomän
- Aktivera HTTPS för alla endpoints
</Warning>

## Felsökning

<AccordionGroup>
<Accordion title="Import errors or missing modules">
Kontrollera att din virtuella miljö är aktiverad och att beroendena är installerade:

```bash
source venv/bin/activate
pip install -r requirements.txt
```

</Accordion>

<Accordion title="Checkout session creation fails">
**Vanliga orsaker:**
- Ogiltigt produkt-ID – verifiera att det finns i din Dodo-instrumentpanel
- Fel API-nyckel eller miljöinställning i `.env`
- Kontrollera FastAPI-loggarna för detaljerade felmeddelanden
</Accordion>

<Accordion title="Webhooks not receiving events">
Använd [ngrok](https://ngrok.com) för att exponera din server vid lokal testning:

```bash
ngrok http 8000
```

Uppdatera webhook-URL:en i din [Dodo-instrumentpanel](https://app.dodopayments.com/developer/webhooks) till ngrok-URL:en. Se till att uppdatera din `.env`-fil med rätt verifieringsnyckel för webhooken.
</Accordion>

<Accordion title="Webhook signature verification fails">
- Kontrollera att `DODO_PAYMENTS_WEBHOOK_KEY` är korrekt inställd i din `.env`
- Verifiera att du använder den råa request body för signaturverifiering
- Kontrollera att du läser `webhook-signature`-headern korrekt
</Accordion>
</AccordionGroup>

## Läs mer

<CardGroup cols={2}>
<Card title="Python SDK" icon="python" href="/developer-resources/sdks/python">
  Komplett dokumentation för Python SDK med stöd för async
</Card>

<Card title="Webhooks Documentation" icon="webhook" href="/developer-resources/webhooks">
  Läs mer om alla webhook-händelser och bästa praxis
</Card>

<Card title="Checkout Sessions" icon="credit-card" href="/developer-resources/checkout-session">
  Fördjupa dig i konfigurationen av checkout-sessioner
</Card>

<Card title="API Reference" icon="book" href="/api-reference/introduction">
  Komplett Dodo Payments-API-dokumentation
</Card>
</CardGroup>

## Support

Behöver du hjälp med startmallen?

- Gå med i vår [Discord-community](https://discord.gg/bYqAp4ayYh) för frågor och diskussioner
- Besök [GitHub-repositoriet](https://github.com/dodopayments/fastapi-boilerplate) för problem och uppdateringar
- Kontakta vårt [supportteam](mailto:support@dodopayments.com) för hjälp
