---
title: Kassa Sessioner
description: Skapa säkra, värd kassaupplevelser som hanterar hela
  betalningsflödet för både engångsköp och prenumerationer med full
  anpassningskontroll.
icon: cart-shopping
---

<CardGroup cols={2}>
{/* LOCKED_PATTERN_fb701bb7372119ae64cf7d6283e3e6ba */}
  Få din första checkout-session igång på mindre än 5 minuter
</Card>

{/* LOCKED_PATTERN_f9bed6d18a61ed6b9e639fe134e1e892 */}
  Utforska den fullständiga API-dokumentationen och testa Checkout Session-förfrågningar och -svar interaktivt.
</Card>

{/* LOCKED_PATTERN_cde9806cd07d35eb0c667ab9e02fe238 */}
  Beräkna priser, skatter och totalbelopp innan du skapar en session.
</Card>
</CardGroup>

<Info>
**Sessionens giltighet**: Checkout-sessioner är giltiga i 24 timmar som standard. Om du skickar `confirm=true` i din förfrågan kommer sessionen bara vara giltig i 15 minuter.
</Info>

## Förutsättningar

<Steps>
{/* LOCKED_PATTERN_d85476e67eb88b5be3834394c43dcc7d */}
  Du behöver ett aktivt Dodo Payments-handelskonto med API-åtkomst.
</Step>

{/* LOCKED_PATTERN_a4fc3d29ec3fa98438a77dffd4b074ee */}
  Generera dina API-referenser från Dodo Payments-instrumentpanelen:
</Step>

{/* LOCKED_PATTERN_47823d7b8bdec0e6762bf2eacbdf8903 */}
  Skapa dina produkter i Dodo Payments-instrumentpanelen innan du implementerar checkout-sessioner.
</Step>
</Steps>

## Skapa din första Checkout-session

<Tabs>
<Tab title="Node.js SDK">

```javascript expandable
import DodoPayments from 'dodopayments';

// Initialize the Dodo Payments client
const client = new DodoPayments({
  bearerToken: process.env.DODO_PAYMENTS_API_KEY,
  environment: 'test_mode', // defaults to 'live_mode'
});

async function createCheckoutSession() {
  try {
    const session = await client.checkoutSessions.create({
      // Products to sell - use IDs from your Dodo Payments dashboard
      product_cart: [
        {
          product_id: 'prod_123', // Replace with your actual product ID
          quantity: 1
        }
      ],
      
      // Pre-fill customer information to reduce friction
      customer: {
        email: 'customer@example.com',
        name: 'John Doe',
        phone_number: '+1234567890'
      },
      
      // Billing address for tax calculation and compliance
      billing_address: {
        street: '123 Main St',
        city: 'San Francisco',
        state: 'CA',
        country: 'US', // Required: ISO 3166-1 alpha-2 country code
        zipcode: '94102'
      },
      
      // Where to redirect after successful payment
      return_url: 'https://yoursite.com/checkout/success',
      
      // Custom data for your internal tracking
      metadata: {
        order_id: 'order_123',
        source: 'web_app'
      }
    });

    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example usage in an Express.js route
app.post('/create-checkout', async (req, res) => {
  try {
    const session = await createCheckoutSession();
    res.json({ checkout_url: session.checkout_url });
  } catch (error) {
    res.status(500).json({ error: 'Failed to create checkout session' });
  }
});
```

</Tab>

<Tab title="Python SDK">

```python expandable
import os
from dodopayments import DodoPayments

# Initialize the Dodo Payments client
client = DodoPayments(
    bearer_token=os.environ.get("DODO_PAYMENTS_API_KEY"),  # This is the default and can be omitted
    environment="test_mode",  # defaults to "live_mode"
)

def create_checkout_session():
    """
    Create a checkout session for a single product with customer data pre-filled.
    Returns the session object containing checkout_url for customer redirection.
    """
    try:
        session = client.checkout_sessions.create(
            # Products to sell - use IDs from your Dodo Payments dashboard
            product_cart=[
                {
                    "product_id": "prod_123",  # Replace with your actual product ID
                    "quantity": 1
                }
            ],
            
            # Pre-fill customer information to reduce checkout friction
            customer={
                "email": "customer@example.com",
                "name": "John Doe",
                "phone_number": "+1234567890"
            },
            
            # Billing address for tax calculation and compliance
            billing_address={
                "street": "123 Main St",
                "city": "San Francisco", 
                "state": "CA",
                "country": "US",  # Required: ISO 3166-1 alpha-2 country code
                "zipcode": "94102"
            },
            
            # Where to redirect after successful payment
            return_url="https://yoursite.com/checkout/success",
            
            # Custom data for your internal tracking
            metadata={
                "order_id": "order_123",
                "source": "web_app"
            }
        )
        
        # Redirect your customer to this URL to complete payment
        print(f"Checkout URL: {session.checkout_url}")
        print(f"Session ID: {session.session_id}")
        
        return session
        
    except Exception as error:
        print(f"Failed to create checkout session: {error}")
        raise error

# Example usage in a Flask route
from flask import Flask, jsonify, request

app = Flask(__name__)

@app.route('/create-checkout', methods=['POST'])
def create_checkout():
    try:
        session = create_checkout_session()
        return jsonify({"checkout_url": session.checkout_url})
    except Exception as error:
        return jsonify({"error": "Failed to create checkout session"}), 500
```

</Tab>

<Tab title="REST API">

```javascript expandable
// Direct API call using fetch - useful for any JavaScript environment
async function createCheckoutSession() {
  try {
    const response = await fetch('https://test.dodopayments.com/checkouts', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
        'Authorization': `Bearer ${process.env.DODO_PAYMENTS_API_KEY}`
      },
      body: JSON.stringify({
        // Products to sell - use IDs from your Dodo Payments dashboard
        product_cart: [
          {
            product_id: 'prod_123', // Replace with your actual product ID
            quantity: 1
          }
        ],
        
        // Pre-fill customer information to reduce checkout friction
        customer: {
          email: 'customer@example.com',
          name: 'John Doe',
          phone_number: '+1234567890'
        },
        
        // Billing address for tax calculation and compliance
        billing_address: {
          street: '123 Main St',
          city: 'San Francisco',
          state: 'CA', 
          country: 'US', // Required: ISO 3166-1 alpha-2 country code
          zipcode: '94102'
        },
        
        // Where to redirect after successful payment
        return_url: 'https://yoursite.com/checkout/success',
        
        // Custom data for your internal tracking
        metadata: {
          order_id: 'order_123',
          source: 'web_app'
        }
      })
    });

    if (!response.ok) {
      throw new Error(`HTTP error! status: ${response.status}`);
    }

    const session = await response.json();
    
    // Redirect your customer to this URL to complete payment
    console.log('Checkout URL:', session.checkout_url);
    console.log('Session ID:', session.session_id);
    
    return session;
    
  } catch (error) {
    console.error('Failed to create checkout session:', error);
    throw error;
  }
}

// Example: Redirect user to checkout
createCheckoutSession().then(session => {
  window.location.href = session.checkout_url;
});
```

</Tab>
</Tabs>

### API-svar

Alla metoder ovan returnerar samma svarstruktur:

```json
{
  "session_id": "cks_Gi6KGJ2zFJo9rq9Ukifwa",
  "checkout_url": "https://test.checkout.dodopayments.com/session/cks_Gi6KGJ2zFJo9rq9Ukifwa"
}
```

<Steps>
{/* LOCKED_PATTERN_926485b2e330d860c51cdacaa6e05795 */}
Extrahera `checkout_url` från API-svaret.
</Step>

{/* LOCKED_PATTERN_3db3b3f593acf4e304e6de8a86a39123 */}
Skicka din kund till checkout-URL:en för att slutföra köpet.

```javascript
// Redirect immediately
window.location.href = session.checkout_url;

// Or open in new window
window.open(session.checkout_url, '_blank');
```

<Tip>
**Alternativa integrationsalternativ**: Istället för att omdirigera kan du bädda in checkouten direkt på din sida med [Overlay Checkout](/developer-resources/overlay-checkout) (modal overlay) eller [Inline Checkout](/developer-resources/inline-checkout) (fullt inbäddad). Båda alternativen använder samma checkout-sessionens URL.
</Tip>
</Step>

{/* LOCKED_PATTERN_5e0e1343830b12a876473f4d0dec5a91 */}
Efter betalning omdirigeras kunder till din `return_url` med frågeparametrar inklusive betalning-/prenumerations-ID, status, kundens e-postadress och eventuella licensnycklar. Se [`return_url` parameter docs](#request-body) för hela listan.
</Step>
</Steps>

## Begärningskropp

<CardGroup cols={2}>
{/* LOCKED_PATTERN_6c2ca8dc229cbbf8f872551278cf51d9 */}
  Väsentliga fält som behövs för varje checkout-session
</Card>

{/* LOCKED_PATTERN_b477c06aaef65331600184ac61660a59 */}
  Ytterligare konfiguration för att anpassa checkout-upplevelsen
</Card>
</CardGroup>

### Obligatoriska fält

<ParamField body="product_cart" type="array" required>
Array av produkter som ingår i checkout-sessionen. Varje produkt måste ha en giltig `product_id` från din Dodo Payments-instrumentpanel.

<Tip>
**Blandad checkout**: Du kan kombinera engångsbetalningsprodukter och prenumerationsprodukter i samma checkout-session. Detta möjliggör kraftfulla scenarier som installationsavgifter med prenumerationer, hårdvarupaket med SaaS och mer.
</Tip>

{/* LOCKED_PATTERN_0001312096b92069b0be723101ae0ad4 */}
<ParamField body="product_id" type="string" required>
Den unika identifieraren för produkten från din Dodo Payments-instrumentpanel.

**Exempel:** `"prod_123abc456def"`
</ParamField>

<ParamField body="quantity" type="integer" required>
Produktens kvantitet.

**Exempel:** `1` för en vara, `3` för flera kvantiteter
</ParamField>

<ParamField body="addons" type="array">
Array av tillägg som kopplas till produkten. Gäller endast om produkten är en prenumeration.

{/* LOCKED_PATTERN_cef5ee7d3104f26b4f50177bb74acbcb */}
<ParamField body="addon_id" type="string" required />

<ParamField body="quantity" type="integer" required />
</Expandable>
</ParamField>

<ParamField body="amount" type="integer">
Belopp som kunden betalar om pay_what_you_want är aktiverat. Om det är inaktiverat ignoreras detta fält.

**Format**: Anges i den lägsta valutanheten (t.ex. cent för USD). För att debitera $1.00 skickar du `100`.
</ParamField>
</Expandable>
</ParamField>

<Tip>
**Hitta dina produkt-ID:n**: Du hittar produkt-ID:n i din Dodo Payments-instrumentpanel under Products → View Details, eller genom att använda [List Products API](/api-reference/products/get-products).
</Tip>

### Valfria fält

Konfigurera dessa fält för att anpassa checkout-upplevelsen och lägga till affärslogik i din betalningsflöde.

<AccordionGroup>
{/* LOCKED_PATTERN_a5398762f23d14e1cc714965c5d2083d */}

<ParamField body="customer" type="object">
Kundinformation. Du kan antingen koppla en befintlig kund med deras ID eller skapa en ny kundpost under checkout.

<Tabs>
{/* LOCKED_PATTERN_1407979c58b02f7099effb987a366bdc */}
Koppla en befintlig kund till checkout-sessionen med deras ID.

{/* LOCKED_PATTERN_6726ea271928b9326bde0295856c8b3c */}
<ParamField body="customer_id" type="string" required>
Den unika identifieraren för en befintlig kund. Använd denna för att koppla checkout-sessionen till en befintlig kund istället för att skapa en ny.
</ParamField>
</Expandable>
</Tab>

{/* LOCKED_PATTERN_32e6f7d1b22a2a92fdcd7ef9acf84ae1 */}
Skapa en ny kundpost under checkout.

{/* LOCKED_PATTERN_f757b4d9a0c64993223bac5c1a679581 */}
<ParamField body="email" type="string" required>
Kundens e-postadress. Obligatoriskt vid skapande av ny kund.
</ParamField>

<ParamField body="name" type="string">
Kundens fullständiga namn som det ska visas på kvitton och fakturor.

**Exempel**: `"John Doe"` eller `"Jane Smith"`
</ParamField>

<ParamField body="phone_number" type="string">
Kundens telefonnummer i internationellt format. Krävs för vissa betalmetoder och bedrägeriförebyggande.

**Format**: Inkludera landskod, t.ex. `"+1234567890"` för amerikanska nummer
</ParamField>
</Expandable>
</Tab>
</Tabs>
</ParamField>

<ParamField body="billing_address" type="object">
Faktureringsadressinformation för korrekt skatteberäkning, bedrägeriförebyggande och regelefterlevnad.

<Info>
När `confirm` är satt till `true` blir alla faktureringsadressfält obligatoriska för att sessionen ska kunna skapas.
</Info>

{/* LOCKED_PATTERN_94bc014de17f3f7f1be1ebda0bbeb4de */}
<ParamField body="street" type="string">
Fullständig gatuadress inklusive husnummer, gatunamn och lägenhets-/enhetsnummer om tillämpligt.

**Exempel**: `"123 Main St, Apt 4B"` eller `"456 Oak Avenue"`
</ParamField>

<ParamField body="city" type="string">
Stads- eller kommunnamn.

**Exempel**: `"San Francisco"`, `"New York"`, `"London"`
</ParamField>

<ParamField body="state" type="string">
Delstat, provins eller region. Använd fullständiga namn eller standardförkortningar.

**Exempel**: `"California"` eller `"CA"`, `"Ontario"` eller `"ON"`
</ParamField>

<ParamField body="country" type="string" required>
Tvåbokstavskod enligt ISO (ISO 3166-1 alpha-2). Detta fält är alltid obligatoriskt när billing_address anges.

**Exempel**: `"US"` (USA), `"CA"` (Kanada), `"GB"` (Storbritannien), `"DE"` (Tyskland)

{/* LOCKED_PATTERN_a7cbb1992ba2ab773c7ee4fce342d9a3 */}
  Visa komplett lista över stödda länder och deras ISO-koder
</Card>
</ParamField>

<ParamField body="zipcode" type="string">
Postnummer, ZIP-kod eller motsvarande enligt landets krav.

**Exempel**: `"94102"` (USA), `"M5V 3A8"` (Kanada), `"SW1A 1AA"` (Storbritannien)
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_ae102172f5d0e449e61ce1a10a39ac3d */}

<ParamField body="allowed_payment_method_types" type="array">
Kontrollera vilka betalningsmetoder som är tillgängliga för kunderna under checkout. Detta hjälper dig optimera för specifika marknader eller affärskrav.

**Tillgängliga alternativ**: `credit`, `debit`, `upi_collect`, `apple_pay`, `google_pay`, `amazon_pay`, `klarna`, `affirm`, `afterpay_clearpay`, `cashapp`, `multibanco`, `bancontact_card`, `eps`, `ideal`, `przelewy24`, `paypal`

<Warning>
**Kritiskt**: Inkludera alltid `credit` och `debit` som reservalternativ för att förhindra fel i checkouten när önskade betalmetoder inte är tillgängliga.
</Warning>

**Exempel**: 

```json expandable
["apple_pay", "google_pay", "credit", "debit"]
```

</ParamField>

<ParamField body="billing_currency" type="string">
Åsidosätt standardvalutan med en fast faktureringsvaluta. Använder ISO 4217-valutakoder.

**Stödda valutor**: `USD`, `EUR`, `GBP`, `CAD`, `AUD`, `INR`, och fler

**Exempel**: `"USD"` för US-dollar, `"EUR"` för euro

<Note>
Detta fält fungerar endast när adaptiv prissättning är aktiverat. Om adaptiv prissättning är avaktiverat används produktens standardvaluta.
</Note>
</ParamField>

<ParamField body="show_saved_payment_methods" type="boolean" default="false">
Visa tidigare sparade betalmetoder för återkommande kunder, vilket förbättrar genomströmningen och användarupplevelsen.
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_277704a9e8dbc1fe556077c773102b34 */}

<ParamField body="return_url" type="string">
URL för att omdirigera kunder efter betalning. Dodo Payments lägger till följande frågeparametrar på din URL vid omdirigering:

| Parameter | Typ | Villkor |
|-----------|------|-----------|
| `payment_id` | `string` | Alltid närvarande vid engångsbetalningar |
| `subscription_id` | `string` | Alltid närvarande vid prenumerationsbetalningar |
| `status` | `string` | Alltid närvarande |
| `license_key` | `string` | Närvarande om produkten har licensnycklar aktiverade. Kommaavgränsade vid flera nycklar |
| `email` | `string` | Närvarande om kunden har en registrerad e-postadress |

**Exempel på omdirigerings-URL:er:**

```text
# One-time payment with license key
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&license_key=LK-001&email=customer%40example.com

# Subscription payment with multiple license keys
https://yoursite.com/return?subscription_id=sub_xxx&status=active&license_key=LK-001,LK-002&email=customer%40example.com

# Payment without license keys
https://yoursite.com/return?payment_id=pay_xxx&status=succeeded&email=customer%40example.com
```

<Tip>
Använd `license_key` och `email` frågeparametrarna för att visa licensnycklar eller skicka en bekräftelse direkt på din retursida, utan att behöva ett extra API-anrop.
</Tip>
</ParamField>

<ParamField body="confirm" type="boolean" default="false">
Om true finaliseras alla sessionens uppgifter omgående. API:t kastar ett fel om nödvändig data saknas.
</ParamField>

<ParamField body="discount_code" type="string">
Använd en rabattkod i checkout-sessionen.
</ParamField>

<ParamField body="metadata" type="object">
Anpassade nyckel-värde-par för att lagra ytterligare information om sessionen.
</ParamField>

<ParamField body="force_3ds" type="boolean">
Åsidosätt handlarens standardbeteende för 3DS för denna session.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Aktivera läget för minimal adressinsamling. När det är aktiverat samlar checkouten endast in:
- **Land**: Alltid obligatoriskt för skattebestämning
- **ZIP/Postal code**: Endast i regioner där det är nödvändigt för moms, VAT eller GST-beräkning

Detta minskar friktionen i checkouten avsevärt genom att eliminera onödiga formulärfält.

<Tip>
Aktivera minimal adressinsamling för snabbare avslut. Fullständig adressinsamling finns kvar för företag som kräver kompletta faktureringsuppgifter.
</Tip>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_aaff5fb3ae0ea92c2072c6145f1b2ecc */}

<ParamField body="customization" type="object">
Anpassa checkout-gränssnittets utseende och beteende.

{/* LOCKED_PATTERN_ce9afa614a11dbad38751d22b01daf71 */}
<ParamField body="theme" type="string" default="system">
Tema för checkout-gränssnittet. Alternativ: `light`, `dark`, eller `system`.
</ParamField>

<ParamField body="show_order_details" type="boolean" default="true">
Visa avsnittet för orderdetaljer i checkout-gränssnittet.
</ParamField>

<ParamField body="show_on_demand_tag" type="boolean" default="true">
Visa etiketten "on-demand" för tillämpliga produkter.
</ParamField>

<ParamField body="force_language" type="string">
Tvinga checkout-gränssnittet att rendera på ett specifikt språk. Som standard upptäcker checkouten webbläsarens språk automatiskt.

**Stödda språkkoder:**
`ar` (Arabiska), `ca` (Katalanska), `de` (Tyska), `en` (Engelska), `es` (Spanska), `fr` (Franska), `he` (Hebreiska), `id` (Indonesiska), `it` (Italienska), `ja` (Japanska), `ko` (Koreanska), `ms` (Malayiska), `nl` (Nederländska), `pl` (Polska), `pt` (Portugisiska), `ro` (Rumänska), `ru` (Ryska), `sv` (Svenska), `th` (Thailändska), `tr` (Turkiska), `zh` (Kinesiska)

**Exempel**: `"es"` för spanska, `"ja"` för japanska
</ParamField>
</Expandable>
</ParamField>

<ParamField body="feature_flags" type="object">
Konfigurera särskilda funktioner och beteenden för checkout-sessionen.

{/* LOCKED_PATTERN_feff180650f54f392ad59033a6a8a8a2 */}
<ParamField body="allow_currency_selection" type="boolean" default="true">
Tillåt kunder att välja sin föredragna valuta under checkout.
</ParamField>

<ParamField body="allow_discount_code" type="boolean" default="true">
Visa inmatningsfält för rabattkod i checkout-gränssnittet.
</ParamField>

<ParamField body="allow_phone_number_collection" type="boolean" default="true">
Samla in kundens telefonnummer under checkout.
</ParamField>

<ParamField body="allow_tax_id" type="boolean" default="true">
Tillåt kunder att ange skatteidentifikationsnummer.
</ParamField>

<ParamField body="always_create_new_customer" type="boolean" default="false">
Tvinga fram skapandet av en ny kundpost istället för att uppdatera befintliga.
</ParamField>

<ParamField body="allow_customer_editing_email" type="boolean">
Tillåt kunder att redigera sin e-postadress under checkout.
</ParamField>

<ParamField body="allow_customer_editing_name" type="boolean">
Tillåt kunder att ändra sitt namn under checkout.
</ParamField>

<ParamField body="allow_customer_editing_street" type="boolean">
Tillåt kunder att redigera gatuadressen under checkout.
</ParamField>

<ParamField body="allow_customer_editing_city" type="boolean">
Tillåt kunder att redigera staden under checkout.
</ParamField>

<ParamField body="allow_customer_editing_state" type="boolean">
Tillåt kunder att redigera delstaten under checkout.
</ParamField>

<ParamField body="allow_customer_editing_country" type="boolean">
Tillåt kunder att redigera landet under checkout.
</ParamField>

<ParamField body="allow_customer_editing_zipcode" type="boolean">
Tillåt kunder att redigera postnumret under checkout.
</ParamField>

<ParamField body="minimal_address" type="boolean" default="false">
Aktivera läget för minimal adressinsamling. När det är aktiverat samlar checkouten endast in:
- **Land**: Alltid obligatoriskt för skattebestämning
- **ZIP/Postal code**: Endast i regioner där det är nödvändigt för moms, VAT eller GST-beräkning

Detta minskar friktionen i checkouten avsevärt genom att eliminera onödiga formulärfält.

<Tip>
Aktivera minimal adressinsamling för snabbare avslut. Fullständig adressinsamling finns kvar för företag som kräver kompletta faktureringsuppgifter.
</Tip>
</ParamField>

{/* LOCKED_PATTERN_5fbf5b9c0f26b44449ae3797dd36fd4a */}
Hoppa över standardsidan för lyckad betalning och omdirigera kunder direkt till din `return_url` efter betalning.


<Tip>
Använd detta när du har en anpassad lyckad-betalning-sida som ger bättre användarupplevelse än standardsidan. Detta är särskilt användbart för mobilappar och inbäddade checkout-flöden.
</Tip>
</ParamField>
</Expandable>
</ParamField>

</Accordion>

{/* LOCKED_PATTERN_4a5df35cacb73b823bd1a3d94eadd4d4 */}

{/* LOCKED_PATTERN_20ecde7f32736f9833caaa5eb2670afa */}
Samla in ytterligare information från kunder under checkout med anpassade formulärfält. Du kan definiera upp till 5 anpassade fält per checkout-session. Kundsvar ingår i webhook-payloads och är tillgängliga via API:et.

{/* LOCKED_PATTERN_61c44e62206b0e12febbf6934f8bebb6 */}
{/* LOCKED_PATTERN_10471271f4a9e15e186e38b3534da8b5 */}
Unik identifierare för detta fält. Används som nyckel i webhook-payloads och API-svar.

**Exempel**: `"company_name"`, `"referral_source"`, `"team_size"`
</ParamField>

{/* LOCKED_PATTERN_defe2c3d80d34f4235ec60603532a230 */}
Etikett som visas för kunden i checkout-formuläret.

**Exempel**: `"Company Name"`, `"How did you hear about us?"`, `"Team Size"`
</ParamField>

{/* LOCKED_PATTERN_d818908542fced4d20317e9f0185bb2a */}
Fälttyp som avgör valideringsregler för inmatning.

**Tillgängliga typer**:
- `text` - Enradig textinmatning
- `number` - Numerisk inmatning
- `email` - E-postadress med validering
- `url` - URL med validering
- `date` - Datoväljare
- `dropdown` - Välj från fördefinierade alternativ
- `boolean` - Ja/Nej-växling
</ParamField>

{/* LOCKED_PATTERN_614e61cf669d0b4ecd844306b45a29cf */}
Om detta fält måste fyllas i innan checkout kan slutföras.

{/* LOCKED_PATTERN_137bb8d7f12c976b83664349dbfe9653 */}
Platshållartext som visas i inmatningsfältet.

{/* LOCKED_PATTERN_468666a65f7c2a07cfcc71b205e78fee */}
Lista över alternativ för `dropdown` fälttyp. Obligatoriskt när `field_type` är `dropdown`, ignoreras för andra typer.

**Exempel**: `["Google", "Twitter", "Friend referral", "Other"]`
</ParamField>
</Expandable>
</ParamField>

<Info>
Kundens svar på anpassade fält ingår i:
- **Webhooks**: `payment.succeeded`, `subscription.created` och andra relevanta händelsepayloads innehåller `custom_field_responses`-arrayen
- **API-svar**: Betalnings- och prenumerationsobjekt inkluderar `custom_field_responses`
</Info>

</Accordion>

{/* LOCKED_PATTERN_c42da97edc8ca4e3c1d9e5f4fd4fb995 */}

<ParamField body="subscription_data" type="object">
Ytterligare konfiguration för checkout-sessioner som innehåller prenumerationsprodukter.

{/* LOCKED_PATTERN_e3628d58afe4a94d2db9666286924fec */}
<ParamField body="trial_period_days" type="integer">
Antal dagar i testperioden innan första debitering. Måste vara mellan 0 och 10000 dagar.
</ParamField>

<ParamField body="on_demand" type="object">
On-demand-prenumerationskonfiguration för usage-baserad eller mätardriven fakturering.
{/* LOCKED_PATTERN_ed25762981bf5967a5235cab80588cbb */}
<ParamField body="mandate_only" type="boolean" required>
Om det är satt till true utförs ingen debitering utan endast auktoriseras betalningsmetod för framtida användning.
</ParamField>

### 6. Sparade betalningsmetoder för återkommande kunder

<ParamField body="product_price" type="integer">
Produktpris för första debiteringen till kunden. Om det inte anges används produktens lagrade pris.
{/* LOCKED_PATTERN_1418e1c4311ab094caec29ebf63cfcbd */}
**Format**: Anges i den lägsta valutanheten (t.ex. cent för USD). För att debitera $1.00 skickar du `100`.
</ParamField>

### 7. B2B checkout med skatte-ID-insamling

<ParamField body="product_currency" type="string">
Valfri valuta för produktpriset. Om det inte anges används produktens valuta.
</ParamField>

<ParamField body="product_description" type="string">
Valfri överstyrning av produktbeskrivningen för fakturering och rader. Om det inte anges används produktens lagrade beskrivning.
</ParamField>

<ParamField body="adaptive_currency_fees_inclusive" type="boolean">
Om adaptiva valutapåslag ska ingå i produktpriset (true) eller läggas ovanpå (false). Ignoreras om adaptiv prissättning inte är aktiverad.
</ParamField>
</Expandable>
</ParamField>
</Expandable>
</ParamField>

</Accordion>
</AccordionGroup>

## Användningsexempel

Här är 10 omfattande exempel som visar olika checkout-sessionkonfigurationer för olika affärsscenarier:

### 1. Enkel checkout för en produkt

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_ebook_guide',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  },
  return_url: 'https://yoursite.com/success'
});
```

### 2. Kundvagn med flera produkter

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_laptop',
      quantity: 1
    },
    {
      product_id: 'prod_mouse',
      quantity: 2
    },
    {
      product_id: 'prod_warranty',
      quantity: 1
    }
  ],
  customer: {
    email: 'customer@example.com',
    name: 'John Doe',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '123 Tech Street',
    city: 'San Francisco',
    state: 'CA',
    country: 'US',
    zipcode: '94102'
  },
  return_url: 'https://electronics-store.com/order-confirmation'
});
```

### 3. Prenumeration med provperiod

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_plan',
      quantity: 1
    }
  ],
  subscription_data: {
    trial_period_days: 14
  },
  customer: {
    email: 'user@startup.com',
    name: 'Jane Smith'
  },
  return_url: 'https://saas-app.com/onboarding',
  metadata: {
    plan_type: 'professional',
    referral_source: 'google_ads'
  }
});
```

### 4. Förbekräftad checkout

<Note>
När `confirm` är satt till `true` tas kunden direkt till checkout-sidan och hoppar över alla bekräftelsesteg.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_course',
      quantity: 1
    }
  ],
  customer: {
    email: 'student@university.edu',
    name: 'Alex Johnson',
    phone_number: '+1555123456'
  },
  billing_address: {
    street: '456 University Ave',
    city: 'Boston',
    state: 'MA',
    country: 'US',
    zipcode: '02134'
  },
  confirm: true,
  return_url: 'https://learning-platform.com/course-access',
  metadata: {
    course_category: 'programming',
    enrollment_date: '2024-01-15'
  }
});
```

### 5. Checkout med valutaöverskrivning

<Note>
Överskrivningen `billing_currency` träder endast i kraft när adaptiv valuta är aktiverad i dina kontoinställningar. Om adaptiv valuta är avaktiverad har denna parameter ingen effekt.
</Note>

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_consulting_service',
      quantity: 1
    }
  ],
  customer: {
    email: 'client@company.co.uk',
    name: 'Oliver Williams'
  },
  billing_address: {
    street: '789 Business Park',
    city: 'London',
    state: 'England',
    country: 'GB',
    zipcode: 'SW1A 1AA'
  },
  billing_currency: 'GBP',
  feature_flags: {
    allow_currency_selection: true,
    allow_tax_id: true
  },
  return_url: 'https://consulting-firm.com/service-confirmation'
});
```

### 6. Sparade betalmetoder för återkommande kunder

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  customer: {
    email: 'returning.customer@example.com',
    name: 'Robert Johnson',
    phone_number: '+1555987654'
  },
  show_saved_payment_methods: true,
  feature_flags: {
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://subscription-service.com/welcome-back',
  metadata: {
    customer_tier: 'premium',
    account_age: 'returning'
  }
});
```

### 7. B2B-checkout med insamling av skatte-ID

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_enterprise_license',
      quantity: 5
    }
  ],
  customer: {
    email: 'procurement@enterprise.com',
    name: 'Sarah Davis',
    phone_number: '+1800555000'
  },
  billing_address: {
    street: '1000 Corporate Blvd',
    city: 'New York',
    state: 'NY',
    country: 'US',
    zipcode: '10001'
  },
  feature_flags: {
    allow_tax_id: true,
    allow_phone_number_collection: true,
    always_create_new_customer: false
  },
  return_url: 'https://enterprise-software.com/license-delivery',
  metadata: {
    customer_type: 'enterprise',
    contract_id: 'ENT-2024-001',
    sales_rep: 'john.sales@company.com'
  }
});
```

### 8. Checkout med mörkt tema och rabattkod

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_gaming_chair',
      quantity: 1
    }
  ],
  discount_code: 'BLACKFRIDAY2024',
  customization: {
    theme: 'dark',
    show_order_details: true,
    show_on_demand_tag: false
  },
  feature_flags: {
    allow_discount_code: true
  },
  customer: {
    email: 'gamer@example.com',
    name: 'Mike Chen'
  },
  return_url: 'https://gaming-store.com/order-complete'
});
```

### 9. Regionala betalmetoder (UPI för Indien)

För detaljerad information om UPI-konfiguration och testning, se sidan <a href="/features/payment-methods/india">India Payment Methods</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_online_course_hindi',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'upi_collect',
    'credit',
    'debit'
  ],
  customer: {
    email: 'student@example.in',
    name: 'Priya Sharma',
    phone_number: '+919876543210'
  },
  billing_address: {
    street: 'MG Road',
    city: 'Bangalore',
    state: 'Karnataka',
    country: 'IN',
    zipcode: '560001'
  },
  billing_currency: 'INR',
  return_url: 'https://education-platform.in/course-access',
  metadata: {
    region: 'south_india',
    language: 'hindi'
  }
});
```

### 10. Checkout med BNPL (Buy Now Pay Later)

För detaljerad information om BNPL-konfiguration och testning, se sidan <a href="/features/payment-methods/bnpl">Buy Now Pay Later (BNPL)</a>.

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_luxury_watch',
      quantity: 1
    }
  ],
  allowed_payment_method_types: [
    'klarna',
    'afterpay_clearpay',
    'credit',
    'debit'
  ],
  customer: {
    email: 'fashion.lover@example.com',
    name: 'Emma Thompson',
    phone_number: '+1234567890'
  },
  billing_address: {
    street: '555 Fashion Ave',
    city: 'Los Angeles',
    state: 'CA',
    country: 'US',
    zipcode: '90210'
  },
  feature_flags: {
    allow_phone_number_collection: true
  },
  return_url: 'https://luxury-store.com/purchase-confirmation',
  metadata: {
    product_category: 'luxury',
    price_range: 'high_value'
  }
});
```

### 11. Använda befintliga betalmetoder för omedelbar checkout

Använd en kunds sparade betalmetod för att skapa en checkout-session som behandlas omedelbart, utan att samla in betalmetod på nytt:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_premium_plan',
      quantity: 1
    }
  ],
  customer: {
    customer_id: 'cus_123'  // Required when using payment_method_id
  },
  payment_method_id: 'pm_abc123',  // Use customer's saved payment method
  confirm: true,  // Required when using payment_method_id
  return_url: 'https://yourapp.com/success'
});
```

<Warning>
Vid användning av `payment_method_id` måste `confirm` sättas till `true` och en befintlig `customer_id` måste anges. Betalmetoden valideras för kompatibilitet med betalningens valuta.
</Warning>

<Info>
Betalmetoden måste tillhöra kunden och vara kompatibel med betalningens valuta. Detta möjliggör köp med ett klick för återkommande kunder.
</Info>

### 12. Kortlänkar för renare betalnings-URL:er

Generera förkortade, delbara betalningslänkar med anpassade sluggar:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_monthly_subscription',
      quantity: 1
    }
  ],
  short_link: true,  // Generate a shortened payment link
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'John Doe'
  }
});

// The checkout_url will be a shortened, cleaner link
console.log(session.checkout_url);  // e.g., https://checkout.dodopayments.com/buy/abc123
```

<Tip>
Kortlänkar är perfekta för SMS, e-post eller delning på sociala medier. De är lättare att komma ihåg och inger större förtroende hos kunder än långa URL:er.
</Tip>

### 13. Hoppa över betalningssidan för att omdirigera direkt

Omdirigera kunder direkt efter betalning, utan att visa standardsidan för lyckad betalning:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_digital_product',
      quantity: 1
    }
  ],
  feature_flags: {
    redirect_immediately: true  // Skip success page, redirect immediately
  },
  return_url: 'https://yourapp.com/success',
  customer: {
    email: 'customer@example.com',
    name: 'Jane Smith'
  }
});
```

<Tip>
Använd `redirect_immediately: true` när du har en anpassad lyckad-betalning-sida som ger bättre användarupplevelse än standardsidan. Detta är särskilt användbart för mobilappar och inbäddade checkout-flöden.
</Tip>

<Note>
När `redirect_immediately` är aktiverat omdirigeras kunder till din `return_url` direkt efter betalning, utan att visa standardsidan.
</Note>

### 14. Tvinga fram ett språk

Tvinga checkouten att visas på ett specifikt språk, och skriv över kundens webbläsarspråksdetektion:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_subscription',
      quantity: 1
    }
  ],
  customization: {
    force_language: 'ja'  // Force Japanese language
  },
  customer: {
    email: 'customer@example.jp',
    name: 'Tanaka Yuki'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Tip>
Använd `force_language` när du vet kundens föredragna språk (t.ex. från deras kontoinställningar) eller när du riktar dig mot specifika regionala marknader.
</Tip>

**Stödda språk:** Arabiska (`ar`), Katalanska (`ca`), Kinesiska (`zh`), Nederländska (`nl`), Engelska (`en`), Franska (`fr`), Tyska (`de`), Hebreiska (`he`), Indonesiska (`id`), Italienska (`it`), Japanska (`ja`), Koreanska (`ko`), Malayiska (`ms`), Polska (`pl`), Portugisiska (`pt`), Rumänska (`ro`), Ryska (`ru`), Spanska (`es`), Svenska (`sv`), Thailändska (`th`), Turkiska (`tr`)

### 15. Samla in anpassade fält

Samla in extra information från kunder under checkout med anpassade fält:

```javascript expandable
const session = await client.checkoutSessions.create({
  product_cart: [
    {
      product_id: 'prod_saas_plan',
      quantity: 1
    }
  ],
  custom_fields: [
    {
      key: 'company_name',
      label: 'Company Name',
      field_type: 'text',
      required: true,
      placeholder: 'Acme Inc.'
    },
    {
      key: 'team_size',
      label: 'Team Size',
      field_type: 'dropdown',
      required: true,
      options: ['1-10', '11-50', '51-200', '201-500', '500+']
    },
    {
      key: 'referral_source',
      label: 'How did you hear about us?',
      field_type: 'dropdown',
      required: false,
      options: ['Google', 'Twitter', 'Friend referral', 'Blog post', 'Other']
    },
    {
      key: 'website',
      label: 'Company Website',
      field_type: 'url',
      required: false,
      placeholder: 'https://example.com'
    }
  ],
  customer: {
    email: 'buyer@company.com',
    name: 'Jane Doe'
  },
  return_url: 'https://yourapp.com/success'
});
```

<Info>
Svar på anpassade fält inkluderas automatiskt i webhook-payloads (`payment.succeeded`, `subscription.created` etc.) och kan hämtas via API:et. Använd dem för att berika ditt CRM, trigga onboarding-flöden eller anpassa kundupplevelsen.
</Info>

**Tillgängliga fälttyper**: `text`, `number`, `email`, `url`, `date`, `dropdown`, `boolean`

## Förhandsgranska checkout-sessioner

Innan du skapar en checkout-session kan du förhandsgranska prisuppdelningen inklusive skatter, rabatter och totalbelopp. Det är användbart för att visa korrekt prissättning för kunder innan de går vidare till checkout.

<Tabs>
<Tab title="Node.js SDK">

```javascript
const preview = await client.checkoutSessions.preview({
  product_cart: [
    { product_id: 'prod_123', quantity: 1 }
  ],
  billing_address: {
    country: 'US',
    state: 'CA',
    zipcode: '94102'
  },
  discount_code: 'SAVE20'
});

console.log('Subtotal:', preview.subtotal);
console.log('Tax:', preview.tax);
console.log('Discount:', preview.discount);
console.log('Total:', preview.total);
```

</Tab>

<Tab title="Python SDK">

```python
preview = client.checkout_sessions.preview(
    product_cart=[
        {"product_id": "prod_123", "quantity": 1}
    ],
    billing_address={
        "country": "US",
        "state": "CA",
        "zipcode": "94102"
    },
    discount_code="SAVE20"
)

print(f"Subtotal: {preview.subtotal}")
print(f"Tax: {preview.tax}")
print(f"Discount: {preview.discount}")
print(f"Total: {preview.total}")
```

</Tab>
</Tabs>

{/* LOCKED_PATTERN_5c479be80ff82df5704d654ba0e0d13d */}
  Visa den fullständiga dokumentationen för förhandsgranskningsändpunkten.
</Card>

## Att gå från Dynamic Links till Checkout Sessions

### Viktiga skillnader

Tidigare, när du skapade en betalningslänk med Dynamic Links, var du tvungen att tillhandahålla kundens fullständiga faktureringsadress.

- Om du bara vet kundens faktureringsland, ange endast det.
- Checkout-flödet samlar automatiskt in de saknade uppgifterna innan kunden skickas vidare till betalningssidan.
- Å andra sidan, om du redan har all nödvändig information och vill hoppa direkt till betalningssidan, kan du skicka hela datamängden och inkludera `confirm=true` i din förfrågan.

### Migreringsprocess

Migrering från Dynamic Links till Checkout Sessions är smidig:

<Steps>
{/* LOCKED_PATTERN_7b2970df3159f88c8665775525ec7ade */}
Uppdatera din integration för att använda den nya API- eller SDK-metoden.
</Step>

{/* LOCKED_PATTERN_9a0d7dfb0d22a80cdd653a6f423ff618 */}
Justera förfrågningspayloaden enligt Checkout Sessions-formatet.
</Step>

{/* LOCKED_PATTERN_ea09e0cebc00c36bba010ba9740abe0d */}
Ja. Ingen ytterligare hantering eller speciella migreringssteg krävs från din sida.
</Step>
</Steps>
