---
openapi-schema: SubscriptionResponse
title: Prenumeration
description: Payloaden som skickas till din webhook-endpoint när en
  prenumeration skapas, uppdateras eller ändrar tillstånd.
icon: repeat
keywords:
  - Dodo Payments
  - prenumerationswebhook
  - prenumerationshändelser
  - webhook-integration
  - prenumeration
---

## Prenumerations Webhook Händelser

Följande webhook-händelser är tillgängliga för att spåra förändringar i prenumerationens livscykel:

| Händelse | Beskrivning |
|-------|-------------|
| `subscription.active` | Prenumerationen har aktiverats |
| `subscription.updated` | Prenumerationsobjektet har uppdaterats (alla ändringar av fält utlöser denna händelse) |
| `subscription.on_hold` | Prenumerationen har pausats på grund av en misslyckad förnyelse |
| `subscription.paused` | Prenumerationen har pausats |
| `subscription.renewed` | Prenumerationen har förnyats för nästa faktureringsperiod |
| `subscription.plan_changed` | Prenumerationsplanen har uppgraderats, nedgraderats eller ändrats |
| `subscription.update_payment_method` | Prenumerationens betalningsmetod har uppdaterats |
| `subscription.cancelled` | Prenumerationen har avslutats |
| `subscription.failed` | Det gick inte att skapa prenumerationen under skapandet av medgivandet |
| `subscription.expired` | Prenumerationen har nått slutet av sin löptid |

### Använda `subscription.updated` för synkronisering i realtid

Webhooken `subscription.updated` utlöses när som helst ett prenumerationsfält ändras, vilket gör att du kan hålla din applikationsstatus synkroniserad utan att behöva fråga API:et upprepade gånger:

```javascript
app.post('/webhooks/dodo', async (req, res) => {
  const event = req.body;
  
  if (event.type === 'subscription.updated') {
    const subscription = event.data;
    
    // Sync subscription changes to your database
    await syncSubscription(subscription.subscription_id, {
      status: subscription.status,
      next_billing_date: subscription.next_billing_date,
      metadata: subscription.metadata,
      // ... other fields you want to track
    });
    
    console.log(`Subscription ${subscription.subscription_id} updated`);
  }
  
  res.json({ received: true });
});
```

<Tip>
Prenumerera på `subscription.updated` för att få meddelanden i realtid om alla prenumerationsändringar, vilket eliminerar behovet av att fråga API:et om uppdateringar.
</Tip>

## Webhook Payload Schema