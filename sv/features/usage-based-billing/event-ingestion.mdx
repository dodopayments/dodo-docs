---
sidebarTitle: Event Ingestion
title: Event Ingestion
description: Skicka användningsevenemang från din applikation för att spåra
  kundkonsumtion och driva fakturering.
icon: bolt
---

Evenemang är grunden för användningsbaserad fakturering. Skicka evenemang när fakturerbara åtgärder inträffar, och mätare aggregerar dem till avgifter.

<Card title="API Reference - Events Ingestion" icon="code" href="/api-reference/usage-events/ingest-events">
Fullständig API-dokumentation med exempel och svarskoder.
</Card>

## Eventstruktur

{/* LOCKED_PATTERN_4d7f2e9ce45e7323dd2d626df4a7c5b8 */}
Unikt identifierare. Använd UUID:er eller kombinera kund-ID + tidsstämpel + åtgärd.
</ParamField>

<ParamField body="customer_id" type="string" required>
Dodo Payments kund-ID. Måste vara en giltig befintlig kund.
</ParamField>

<ParamField body="event_name" type="string" required>
Händelsetyp som matchar din mätarens händelsenamn (skiftlägeskänsligt). Exempel: `api.call`, `image.generated`
</ParamField>
</Accordion>

{/* LOCKED_PATTERN_94ee55474fc7877494948c444fe08c91 */}

<ParamField body="timestamp" type="string">
ISO 8601-tidsstämpel. Standard är servertid om den utelämnas. Inkludera för korrekt fakturering vid fördröjda/buntade händelser.
</ParamField>

<ParamField body="metadata" type="object">
Ytterligare egenskaper för aggregering och filtrering:
- Numeriska värden: `bytes`, `tokens`, `duration_ms`
- Filter: `endpoint`, `method`, `quality`

```javascript
metadata: {
  endpoint: "/v1/orders",
  method: "POST",
  tokens: 1024
}
```

</ParamField>
</Accordion>

## Skicka evenemang

<CodeGroup>

```javascript Single Event
await fetch('https://test.dodopayments.com/events/ingest', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${process.env.DODO_API_KEY}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    events: [{
      event_id: "api_call_1234",
      customer_id: "cus_abc123",
      event_name: "api.call",
      metadata: { endpoint: "/v1/orders" }
    }]
  })
});
```

```javascript Batch Events
await fetch('https://test.dodopayments.com/events/ingest', {
  method: 'POST',
  headers: {
    'Authorization': `Bearer ${process.env.DODO_API_KEY}`,
    'Content-Type': 'application/json'
  },
  body: JSON.stringify({
    events: [
      { event_id: "call_1", customer_id: "cus_abc", event_name: "api.call" },
      { event_id: "call_2", customer_id: "cus_abc", event_name: "api.call" },
      { event_id: "call_3", customer_id: "cus_abc", event_name: "api.call" }
    ]
  })
});
```

```python Python
import requests

requests.post(
    'https://test.dodopayments.com/events/ingest',
    headers={'Authorization': f'Bearer {api_key}', 'Content-Type': 'application/json'},
    json={'events': [{'event_id': 'call_1', 'customer_id': 'cus_abc', 'event_name': 'api.call'}]}
)
```

```curl cURL
curl -X POST 'https://test.dodopayments.com/events/ingest' \
  -H 'Authorization: Bearer YOUR_API_KEY' \
  -H 'Content-Type: application/json' \
  -d '{"events": [{"event_id": "call_1", "customer_id": "cus_abc", "event_name": "api.call"}]}'
```

</CodeGroup>

<Tip>
Batcha upp till 100 händelser per förfrågan för bättre prestanda.
</Tip>

## Ingestionsmönster

Färdiga evenemangsmönster för vanliga användningsfall. Börja med ett beprövat mönster istället för att bygga från grunden.

<CardGroup cols={2}>
<Card title="LLM Blueprint" icon="brain-circuit" href="/developer-resources/ingestion-blueprints/llm">
Spåra AI-tokenanvändning över OpenAI, Anthropic, Groq, Gemini och fler.
</Card>

<Card title="API Gateway Blueprint" icon="cloud" href="/developer-resources/ingestion-blueprints/api-gateway">
Mät API-förfrågningar med slutpunktfiltrering och stöd för hastighetsbegränsning.
</Card>

<Card title="Object Storage Blueprint" icon="box-archive" href="/developer-resources/ingestion-blueprints/object-storage">
Spåra filuppladdningar och lagringskonsumtion för molnlagringstjänster.
</Card>

<Card title="Stream Blueprint" icon="tower-broadcast" href="/developer-resources/ingestion-blueprints/stream">
Mät strömningsbandbredd för video, ljud och realtidsdata.
</Card>

<Card title="Time Range Blueprint" icon="clock" href="/developer-resources/ingestion-blueprints/time-range">
Fakturera efter förfluten tid för serverlösa funktioner och compute-instansser.
</Card>

<Card title="View All Blueprints" icon="copy" href="/features/usage-based-billing/ingestion-blueprints">
Se alla tillgängliga mallar med detaljerade implementeringsguider.
</Card>
</CardGroup>

## Bästa praxis

<AccordionGroup>
{/* LOCKED_PATTERN_c11962fb3ea9e1a82d1994f0860ee77a */}
Använd deterministiska ID:n för att förhindra dubbletter: `${customerId}_${action}_${timestamp}`
</Accordion>

{/* LOCKED_PATTERN_d4b029e15b52ca6ac907535b9dad9ee9 */}
Försök igen vid 5xx-fel med exponentiell backoff. Försök inte igen vid 4xx-fel.
</Accordion>

{/* LOCKED_PATTERN_90e261cbcc49667becacc297210d42c3 */}
Utgå från detta för realtids-händelser. Inkludera för fördröjda/buntade händelser för noggrannhet.
</Accordion>

{/* LOCKED_PATTERN_fa83e100effc6c87ff1c68c56ea8afa9 */}
Spåra framgångsgrader och köa misslyckade händelser för omförsök.
</Accordion>
</AccordionGroup>

## Felsökning

<AccordionGroup>
{/* LOCKED_PATTERN_fc1788ddf0982a3a2fe9f6e81812c541 */}
- Händelsenamnet måste exakt matcha mätaren (skiftlägeskänsligt)
- Kund-ID måste finnas
- Kontrollera att mätarfilter inte utesluter händelser
- Verifiera att tidsstämplarna är aktuella
</Accordion>

{/* LOCKED_PATTERN_1af6f154c82eaebbb38a748cef2be54a */}
Verifiera att API-nyckeln är korrekt och använd formatet: `Bearer YOUR_API_KEY`
</Accordion>

{/* LOCKED_PATTERN_bfe0427c6207c44242acb102b2467be5 */}
Se till att alla obligatoriska fält finns: `event_id`, `customer_id`, `event_name`
</Accordion>

{/* LOCKED_PATTERN_61cd934926cd73f663eac6935ef56a1b */}
- Metadata-nycklar måste exakt matcha mätarens "Over Property"
- Använd siffror, inte strängar: `tokens: 150` inte `tokens: "150"`
</Accordion>
</AccordionGroup>

## Nästa steg

<CardGroup cols={2}>
{/* LOCKED_PATTERN_7febaa0d6629309525313c7e9c57326c */}
Definiera hur dina händelser aggregeras till debiterbara mängder med filter och aggregeringsfunktioner.
</Card>

{/* LOCKED_PATTERN_6689573160770e9b2834bd7f18ddfe6b */}
Använd färdiga mallar för vanliga användningsfall som LLM-övervakning, API-gateways och lagring.
</Card>

{/* LOCKED_PATTERN_b84a97fd71b10036af1d667e737cab38 */}
Bygg en komplett AI-bildgenerator med användningsbaserad fakturering från grunden.
</Card>

{/* LOCKED_PATTERN_6cf38e9ce4cdee1948462b0af44d1016 */}
Fullständig API-dokumentation med alla parametrar, svarskoder och interaktiv testning.
</Card>
</CardGroup>
