---
title: Guía de Checkout Overlay
description: Una moderna biblioteca de TypeScript para incrustar el checkout
  overlay de Dodo Payments y escuchar eventos de checkout en tiempo real.
icon: cart-shopping
---

## Resumen

El SDK de Checkout de Dodo Payments proporciona una forma fluida de integrar nuestro overlay de pago en tu aplicación web. Construido con TypeScript y estándares web modernos, ofrece una solución robusta para manejar pagos con manejo de eventos en tiempo real y temas personalizables.

<Frame>
    <img src="/images/cover-images/overlay-checkout.png" alt="Imagen de Portada del Checkout Overlay"/>
</Frame>

## Recursos

- 📚 [Documentación de Integración](https://docs.dodopayments.com/api-reference/overlay-checkout)
- 🎮 [Demo Interactiva](https://atlas.dodopayments.com#overlay-checkout)
- 💻 [Código Fuente de la Demo](https://github.com/dodopayments/dodo-checkout-demo/blob/main/src/components/Home/OverlayCheckout.tsx)

## Instalación

```bash
# Using npm
npm install dodopayments-checkout

# Using yarn
yarn add dodopayments-checkout

# Using pnpm
pnpm add dodopayments-checkout
```

## Inicio Rápido

```typescript
import { DodoPayments } from "dodopayments-checkout";

// Initialize the SDK
DodoPayments.Initialize({
  mode: "live", // 'test' or 'live'
  onEvent: (event) => {
    console.log("Checkout event:", event);
  },
});

// Open checkout
DodoPayments.Checkout.open({
  checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
});
```

## Configuración

### Opciones de Inicialización

```typescript
interface InitializeOptions {
  mode: "test" | "live";
  onEvent: (event: CheckoutEvent) => void;
}
```

| Opción | Tipo | Requerido | Descripción |
|--------|------|----------|-------------|
| `mode` | string | Sí | Modo de entorno: 'test' o 'live' |
| `onEvent` | function | Sí | Función de callback para manejar eventos de checkout |

### Opciones de Checkout

```typescript
interface CheckoutOptions {
  checkoutUrl: string;
}
```

| Opción | Tipo | Requerido | Descripción |
|--------|------|----------|-------------|
| `checkoutUrl` | string | Sí | URL de sesión de checkout de la API de creación de sesión de checkout |

## Manejo de Eventos

El SDK proporciona eventos en tiempo real a los que puedes escuchar:

```typescript
DodoPayments.Initialize({
  onEvent: (event: CheckoutEvent) => {
    switch (event.event_type) {
      case "checkout.opened":
        // Checkout overlay has been opened
        break;
      case "checkout.payment_page_opened":
        // Payment page has been displayed
        break;
      case "checkout.customer_details_submitted":
        // Customer and billing details submitted
        break;
      case "checkout.closed":
        // Checkout has been closed
        break;
      case "checkout.redirect":
        // Checkout will perform a redirect
        break;
      case "checkout.error":
        // An error occurred
        break;
    }
  }
});
```

## Métodos

### Abrir Checkout

```typescript
DodoPayments.Checkout.open({
  checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
});
```

### Cerrar Checkout

```typescript
DodoPayments.Checkout.close();
```

### Verificar Estado

```typescript
const isOpen = DodoPayments.Checkout.isOpen();
```

## Soporte de Navegadores

- Chrome (última versión)
- Firefox (última versión)
- Safari (última versión)
- Edge (última versión)
- IE11+

> **Nota**: Apple Pay no es actualmente compatible con la experiencia de checkout overlay. Planeamos agregar soporte para Apple Pay en una futura versión.

## Implementación de CDN

Para una integración rápida, puedes usar nuestro CDN:

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dodo Payments Checkout</title>
    
    <!-- Load DodoPayments -->
    <script src="https://cdn.jsdelivr.net/npm/dodopayments-checkout@latest/dist/index.js"></script>
    <script>
        // Initialize the SDK
        DodoPaymentsCheckout.DodoPayments.Initialize({
            mode: "test", // Change to 'live' for production
            onEvent: (event) => {
                console.log('Checkout event:', event);
            }
        });
    </script>
</head>
<body>
    <button onclick="openCheckout()">Checkout Now</button>

    <script>
        function openCheckout() {
            DodoPaymentsCheckout.DodoPayments.Checkout.open({
                checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
            });
        }
    </script>
</body>
</html>
```

## Soporte de TypeScript

El SDK está escrito en TypeScript e incluye definiciones de tipo completas. Todas las APIs públicas están completamente tipadas para una mejor experiencia de desarrollo.

## Manejo de Errores

El SDK proporciona información detallada sobre errores a través del sistema de eventos. Siempre implementa un manejo de errores adecuado en tu `onEvent` callback:

```typescript
DodoPayments.Initialize({
  onEvent: (event: CheckoutEvent) => {
    if (event.event_type === "checkout.error") {
      console.error("Checkout error:", event.data?.message);
      // Handle error appropriately
    }
  }
});
```

## Mejores Prácticas

1. Siempre inicializa el SDK antes de intentar abrir el checkout
2. Implementa un manejo de errores adecuado en tu callback de eventos
3. Usa el modo de prueba durante el desarrollo
4. Maneja todos los eventos relevantes para una experiencia de usuario completa
5. Usa URLs de checkout válidas de la API de creación de sesión de checkout
6. Usa TypeScript para una mejor seguridad de tipos y experiencia de desarrollo

## Guía Paso a Paso

### 1. Configuración del Proyecto

Primero, asegúrate de tener un proyecto moderno de JavaScript/TypeScript. Recomendamos usar Next.js, React o Vue.js.

### 2. Instalar el SDK

Instala el SDK de Checkout de Dodo Payments usando tu gestor de paquetes preferido:

```bash
# Using npm
npm install dodopayments-checkout

# Using yarn
yarn add dodopayments-checkout

# Using pnpm
pnpm add dodopayments-checkout
```

### 3. Implementación Básica

Crea un nuevo componente para tu botón de checkout:

```typescript
// components/CheckoutButton.tsx
"use client";

import { Button } from "@/components/ui/button";
import { DodoPayments } from "dodopayments-checkout";
import { useEffect, useState } from "react";

export function CheckoutButton() {
  const [isLoading, setIsLoading] = useState(false);

  useEffect(() => {
    // Initialize the SDK
    DodoPayments.Initialize({
      mode: "test", // Change to 'live' for production
      onEvent: (event) => {
        console.log("Checkout event:", event);
        
        // Handle different events
        switch (event.event_type) {
          case "checkout.opened":
            setIsLoading(false);
            break;
          case "checkout.error":
            // Handle error
            break;
        }
      },
    });
  }, []);

  const handleCheckout = async () => {
    try {
      setIsLoading(true);
      await DodoPayments.Checkout.open({
        checkoutUrl: "https://checkout.dodopayments.com/session/cks_123" // Get this url from create checkout session api
      });
    } catch (error) {
      console.error("Failed to open checkout:", error);
      setIsLoading(false);
    }
  };

  return (
    <Button 
      onClick={handleCheckout}
      disabled={isLoading}
    >
      {isLoading ? "Loading..." : "Checkout Now"}
    </Button>
  );
}
```

### 4. Agregar a Tu Página

Usa el botón de checkout en tu página:

```typescript
// app/page.tsx
import { CheckoutButton } from "@/components/CheckoutButton";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <h1>Welcome to Our Store</h1>
      <CheckoutButton />
    </main>
  );
}
```

### 5. Manejar Éxitos y Fracasos

Crea páginas de éxito y fracaso:

```typescript
// app/success/page.tsx
export default function SuccessPage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center">
      <h1>Payment Successful!</h1>
      <p>Thank you for your purchase.</p>
    </div>
  );
}

// app/failure/page.tsx
export default function FailurePage() {
  return (
    <div className="flex min-h-screen flex-col items-center justify-center">
      <h1>Payment Failed</h1>
      <p>Please try again or contact support.</p>
    </div>
  );
}
```

### 6. Probar Tu Integración

1. Inicia tu servidor de desarrollo:

```bash
npm run dev
```

2. Prueba el flujo de checkout:
   - Haz clic en el botón de checkout
   - Verifica que el overlay aparezca
   - Prueba el flujo de pago usando credenciales de prueba
   - Confirma que las redirecciones funcionen correctamente

### 7. Pasar a Producción

Cuando estés listo para pasar a producción:

1. Cambia el modo a 'live':

```typescript
DodoPayments.Initialize({
  mode: "live",
  onEvent: (event) => {
    console.log("Checkout event:", event);
  }
});
```

2. Actualiza tus URLs de checkout para usar sesiones de checkout en vivo
3. Prueba el flujo completo en producción
4. Monitorea eventos y errores

### Problemas Comunes y Soluciones

1. **El checkout no se abre**
   - Verifica la inicialización del SDK
   - Revisa si hay errores en la consola
   - Asegúrate de que la URL de checkout sea válida y provenga de la API de creación de sesión de checkout

2. **Los eventos no se disparan**
   - Confirma que el manejador de eventos esté configurado correctamente
   - Revisa si hay errores de JavaScript
   - Verifica la conectividad de red

3. **Problemas de estilo**
   - Asegúrate de que no haya conflictos de CSS
   - Revisa la configuración del tema
   - Verifica el diseño responsivo

Para más ayuda, visita nuestro Discord o contacta a nuestro equipo de soporte para desarrolladores.