---
title: Construya con Agentes de Codificación AI
sidebarTitle: Agentes de Codificación AI
description: Use el Plugin Dodo Agent para desarrollar integraciones con Claude
  Code, Codex CLI, Cursor y OpenCode — servidores y habilidades MCP en una sola
  instalación.
icon: robot
keywords:
  - Dodo Payments
  - agente AI
  - Claude Code
  - Codex CLI
  - Cursor
  - OpenCode
  - MCP
  - agente de codificación
  - plugin de agente
---

El Plugin Dodo Agent conecta dos servidores MCP y ocho habilidades de integración en su agente de codificación AI en una sola instalación. Funciona con **Claude Code**, **Codex CLI**, **Cursor** y **OpenCode** — y los servidores MCP y habilidades CLI funcionan con cualquier cliente compatible con MCP.

<Info>
**Tres elementos básicos, un plugin.** El Plugin de Agente agrupa todo lo que necesita:
- **Servidor API MCP** — acceso en vivo a pagos, suscripciones, clientes, productos, reembolsos, licencias y uso. Se autentica a través de OAuth del navegador (no se requieren claves locales).
- **Servidor de Conocimiento MCP** — búsqueda semántica en toda la documentación de Dodo Payments. No se necesitan credenciales.
- **Ocho habilidades de agente** — hojas de referencia que su agente carga a demanda para pagos, suscripciones, webhooks, facturación basada en uso, facturación basada en crédito, claves de licencia, BillingSDK y mejores prácticas.
</Info>

## Instalar el plugin

Elija su agente de codificación a continuación. La instalación agrega ambos servidores MCP y las ocho habilidades automáticamente.

<AccordionGroup>
<Accordion title="Claude Code" defaultOpen>
Instalar desde el marketplace:

```bash
claude plugins marketplace add dodopayments/dodo-agent-plugin
claude plugins install dodopayments@dodopayments
```

El servidor API MCP utiliza OAuth del navegador por defecto — no se requieren claves al momento de la instalación. La primera vez que su agente llame a una herramienta Dodo, se le pedirá que inicie sesión.

<Card title="Dodo Agent Plugin on GitHub" icon="github" href="https://github.com/dodopayments/dodo-agent-plugin">
  Código fuente, opciones de configuración e instrucciones de desarrollo local
</Card>
</Accordion>

<Accordion title="Codex CLI">
Codex instala plugins en dos pasos: registra el marketplace desde tu terminal y luego instala el plugin desde dentro de la TUI de Codex.

<Steps>
<Step title="Register the marketplace">

```bash
codex plugin marketplace add dodopayments/dodo-agent-plugin
```

</Step>

<Step title="Install from the Codex TUI">
Abre Codex y ejecuta el comando `/plugins`:

```bash
codex
```

Luego escribe `/plugins`, cambia al marketplace **Dodo Payments**, selecciona el plugin **dodopayments** y elige **Instalar plugin**.
</Step>
</Steps>

Ambos servidores MCP y las ocho habilidades se registran automáticamente una vez instalado el plugin.

<Note>
Codex CLI no tiene un subcomando `codex plugin install` — la instalación de plugins siempre ocurre a través del flujo `/plugins` dentro de la TUI. Consulta los [documentos oficiales de plugins de Codex](https://developers.openai.com/codex/plugins).
</Note>

Si previamente agregaste el marketplace y el plugin no aparece bajo `/plugins`, actualízalo:

```bash
codex plugin marketplace upgrade dodopayments
```

</Accordion>

<Accordion title="Cursor">
Instalación manual — clona el repositorio en el directorio de plugins locales de Cursor:

```bash
git clone https://github.com/dodopayments/dodo-agent-plugin.git ~/.cursor/plugins/local/dodo-agent-plugin
```

Reinicia Cursor. El plugin carga habilidades desde `.claude/skills/` (a través de la capa de compatibilidad de Cursor con Claude Code) y servidores MCP desde `.mcp.json`.

<Info>
El soporte del marketplace de Cursor está en camino. Por ahora, utiliza la instalación manual anterior.
</Info>
</Accordion>

<Accordion title="OpenCode">
OpenCode se distribuye a través de npm. Agrega el plugin a tu `opencode.json`:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["@dodopayments/opencode-plugin"]
}
```

Reinicia OpenCode. Ambos servidores MCP (`dodopayments-api`, `dodo-knowledge`) se registran automáticamente a través del hook de configuración del plugin, y las ocho habilidades se descubren automáticamente desde el paquete instalado. No se requiere bloque manual `mcp`.
</Accordion>
</AccordionGroup>

<Info>
¿Usando un agente diferente? Las guías [MCP Server](/developer-resources/mcp-server) y [Agent Skills](/developer-resources/agent-skills) cubren Cursor, Claude Desktop, VS Code, Windsurf, Cline, Zed, y cualquier cliente compatible con MCP.
</Info>

## Lo que obtienes

Una vez que el plugin está instalado, tu agente tiene acceso a dos servidores MCP y ocho habilidades.

### Servidores MCP

| Servidor | Propósito | Autenticación |
|--------|---------|------|
| `dodopayments-api` | Acceso API en vivo — pagos, suscripciones, clientes, productos, reembolsos, licencias, uso | OAuth (navegador) |
| `dodo-knowledge` | Búsqueda semántica en toda la documentación de Dodo Payments | Ninguna |

Ambos servidores están conectados a través de `mcp-remote` para que funcionen en cualquier cliente compatible con MCP.

### Habilidades del agente

| Habilidad | Descripción |
|-------|-------------|
| `best-practices` | Guía completa para integrar Dodo Payments con mejores prácticas |
| `checkout-integration` | Creación de sesiones de pago y flujos de pago |
| `subscription-integration` | Implementación de flujos de facturación por suscripción |
| `webhook-integration` | Configuración y manejo de webhooks para eventos de pago |
| `usage-based-billing` | Implementación de facturación medida con eventos y medidores |
| `credit-based-billing` | Asignaciones de crédito, saldos y deducción de crédito medida |
| `license-keys` | Gestión de claves de licencia para productos digitales |
| `billing-sdk` | Uso de componentes React del BillingSDK |

Las habilidades se cargan automáticamente — tu agente elige la correcta cuando detecta una tarea relevante. Consulta la [documentación de Habilidades del Agente](/developer-resources/agent-skills) para ver la lista completa y la instalación individual.

### Prueba este prompt primero

Una vez que el plugin esté activo, prueba:

```
Set up Dodo Payments webhook handlers in my Next.js app for payment.succeeded and subscription.active events.
```

Tu agente cargará la habilidad `webhook-integration`, utilizará el MCP `dodo-knowledge` para obtener las últimas formas de carga útil y escribirá un controlador con verificación de firma siguiendo la especificación de Webhooks Estándar.

## Otros agentes compatibles

El Plugin de Agente cubre Claude Code, Codex CLI, Cursor y OpenCode. Si usas un agente diferente, conéctate a Dodo Payments a través de los servidores MCP y el CLI de habilidades:

| Agente | Camino más rápido | También soporta |
|-------|-------------|---------------|
| **Claude Code** | Plugin de Agente (un comando) | Servidor MCP, habilidades individuales |
| **Codex CLI** | Plugin de Agente (un comando) | Servidor MCP |
| **Cursor** | Plugin de Agente (clonado git) | Configuración del servidor MCP, CLI de habilidades |
| **OpenCode** | Plugin de Agente (npm) | Configuración del servidor MCP, CLI de habilidades |
| GitHub Copilot (VS Code) | [Guía del Servidor MCP](/developer-resources/mcp-server) | [CLI de habilidades](/developer-resources/agent-skills) |
| Claude Desktop | [Guía del Servidor MCP](/developer-resources/mcp-server) | — |
| Windsurf | [Guía del Servidor MCP](/developer-resources/mcp-server) | [CLI de habilidades](/developer-resources/agent-skills) |
| Cline / Zed / otros | [Guía del Servidor MCP](/developer-resources/mcp-server) | [CLI de habilidades](/developer-resources/agent-skills) |

## Documentos creados para agentes

Cada página de documentación de Dodo Payments está disponible en un formato optimizado para consumo por IA:

- **Índice completo de documentos**: [`docs.dodopayments.com/llms.txt`](https://docs.dodopayments.com/llms.txt) — sirve el índice completo de la documentación para la ingestión de contexto.
- **Markdown simple**: Añade `.md` a cualquier URL de documentación para obtener la versión en markdown sin formato (por ejemplo, `/api-reference/introduction.md`).
- **Repositorio de código fuente**: [`github.com/dodopayments/dodo-docs`](https://github.com/dodopayments/dodo-docs) — clona para indexación sin conexión.

## Lo que tu agente puede hacer

Con el plugin instalado, tu agente de codificación puede:

- **Crear sesiones de pago y enlaces de pago** — [Pagos únicos](/features/one-time-payment-products) y [suscripciones](/features/subscription)
- **Establecer facturación por suscripción y basada en uso de extremo a extremo** — [Suscripciones](/features/subscription), [Facturación basada en uso](/features/usage-based-billing), [Facturación basada en crédito](/features/credit-based-billing)
- **Generar manejadores compatibles con Webhooks Estándar** con verificación de firma — [Webhooks](/developer-resources/webhooks)
- **Conectar componentes React de BillingSDK** para tablas de precios y gestión de suscripciones — [BillingSDK](/developer-resources/billingsdk)
- **Escribir flujos de clave de licencia** para productos digitales — [Claves de licencia](/features/license-keys)
- **Implementar facturación basada en crédito** con asignaciones, saldos, rollover y exceso — [Créditos](/features/credit-based-billing)

## Seguridad y mejores prácticas

<Warning>
**Nunca comprometas claves API de producción.** Usa [modo de prueba](/miscellaneous/test-mode-vs-live-mode) durante el desarrollo. El Plugin de Agente utiliza OAuth de navegador por defecto — solo cambia a claves API locales si tu flujo de trabajo lo requiere.
</Warning>

- **Usa primero el modo de prueba.** Prueba tu integración con claves `dodo_test_...` antes de ir en vivo. Consulta [Modo de Prueba vs Modo en Vivo](/miscellaneous/test-mode-vs-live-mode).
- **OAuth es el modo predeterminado.** El Plugin de Agente se autentica a través de OAuth de navegador (sin secretos locales). Solo usa el modo de claves API si lo necesitas — consulta la sección Configurar abajo.
- **Revisa el código generado por el agente.** Siempre verifica que los manejadores de webhooks incluyan la verificación de firma siguiendo la [Especificación de Webhooks Estándar](https://standardwebhooks.com/).

## Configura con una clave API

Por defecto, el Plugin de Agente utiliza el servidor MCP remoto con OAuth de navegador — no se necesitan credenciales locales. Si tu flujo de trabajo requiere una clave API local (por ejemplo, entornos CI, servidores sin cabeza), puedes cambiar a modo stdio.

<AccordionGroup>
<Accordion title="Local API key mode — Claude Code">
Abre `/plugins` en Claude Code, selecciona **Dodo Payments**, y elige **Configurar opciones**. Completa:

- `dodo_api_key` — tu clave `dodo_test_...` o `dodo_live_...`
- `dodo_webhook_key` — tu secreto de firma de webhook
- `dodo_environment` — `test_mode` o `live_mode`

Luego edita `.mcp.json` para apuntar `dodopayments-api` al servidor stdio local:

```json
{
  "mcpServers": {
    "dodopayments-api": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "dodopayments-mcp@latest"],
      "env": {
        "DODO_PAYMENTS_API_KEY": "${user_config.dodo_api_key}",
        "DODO_PAYMENTS_WEBHOOK_KEY": "${user_config.dodo_webhook_key}",
        "DODO_PAYMENTS_ENVIRONMENT": "${user_config.dodo_environment}"
      }
    }
  }
}
```

Ejecuta `/reload-plugins` para aplicar cambios a tu sesión actual.
</Accordion>

<Accordion title="Local API key mode — OpenCode">
Declara `dodopayments-api` tú mismo en `opencode.json` — tu entrada tiene prioridad sobre el servidor remoto predeterminado del plugin:

```json
{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["@dodopayments/opencode-plugin"],
  "mcp": {
    "dodopayments-api": {
      "type": "local",
      "command": ["npx", "-y", "dodopayments-mcp@latest"],
      "environment": {
        "DODO_PAYMENTS_API_KEY": "dodo_test_...",
        "DODO_PAYMENTS_WEBHOOK_KEY": "whsec_...",
        "DODO_PAYMENTS_ENVIRONMENT": "test_mode"
      },
      "enabled": true
    }
  }
}
```

Reinicia OpenCode para aplicar.
</Accordion>
</AccordionGroup>

## Próximos pasos

<CardGroup cols={2}>
<Card title="MCP Server" icon="terminal" href="/developer-resources/mcp-server">
  Referencia completa para ambos servidores MCP — todos los clientes soportados, configuración y herramientas disponibles
</Card>

<Card title="Agent Skills" icon="wand-magic-sparkles" href="/developer-resources/agent-skills">
  Instalación individual de habilidades, referencia de habilidades e instrucciones de configuración por agente
</Card>

<Card title="Sentra IDE Assistant" icon="bolt" href="/developer-resources/sentra">
  Asistente de facturación impulsado por IA para VS Code, Cursor y Windsurf — pregunta, construye y planifica en tu editor
</Card>

<Card title="API Reference" icon="book" href="/api-reference/introduction">
  Referencia completa de OpenAPI para todos los puntos finales de Dodo Payments
</Card>
</CardGroup>